[toolchain]
# D7 wants 1.84.1 in common with wormhole. faer 0.24.4 → generativity 1.2.1
# uses edition2024, which Cargo 1.84.1 cannot parse (B3, measured 2026-09-01).
# Pin stays on the last toolchain that builds the workspace; rust-version is 1.85.
channel = "1.98.0"
components = ["rustfmt", "clippy"]
