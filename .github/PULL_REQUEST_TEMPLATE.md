## Reduction

- Lines (workspace `*.rs`): before → after
- `pub` items (`kanimiso`, R4): before → after
- Budget changes in `scripts/lint_redundancy.sh` (down only):

## Oracle

- Against (scipy / statsmodels / hmmlearn / river / closed form / brute force):
- Tolerance and how it was chosen (measured × 3–4, AGENTS.md R9):
- Measured absolute error (leave the number in the test comment too):

## Homogeneous implementations (R2 / R6)

- Confirm no existing isomorphic implementation in this repo or in wormhole / coronel / jelly-wave:

## v0.1 numerical change

- Did any v0.1 number change? If yes, why (oracle disagreement is the acceptance condition, not v0.1 parity):
