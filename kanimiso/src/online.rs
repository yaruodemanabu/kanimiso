//! River-style online learning: recursive least squares, linear classifiers,
//! Hoeffding trees, drift detectors, stream clustering, anomaly trees, and
//! rolling metrics.
//!
//! **Contract.** Every [`PartialFit::partial_fit`] returns
//! [`ojizou_san::IncrementalExplain`] and records it on the session. A
//! parameter update without a narrative is a bug. Feature-space changes raise
//! [`IssueCode::FeatureSpaceChangedOnline`]; a `partial_fit` that cannot
//! initialize raises [`IssueCode::PartialFitBeforeInit`].
//!
//! Batch SGD for linear regression already lives in
//! [`crate::linear_model::SgdRegressor`]; it is re-exported here so the river
//! surface has a single import path.

use crate::context::FitCtx;
use crate::data::{Matrix, Vector};
use crate::linalg::{ridge_solve, thin_svd};
use crate::rng::Rng;
use crate::special::norm_cdf;
use crate::traits::{PartialFit, Predict, Transform};
use faer::Mat;
use ojizou_san::{IncrementalExplain, Session};
use signlred::{
    IncrementalQuality, Issue, IssueCode, NumericalCompromise, Qualified, Report, Result, Severity,
};
use std::collections::{HashMap, HashSet};

pub use crate::linear_model::SgdRegressor;

/// Online recursive least squares with a forgetting factor `λ`.
///
/// Sufficient statistics are the coefficient vector `θ` and the inverse Gram
/// `P`. After a row `(x, y)`:
///
/// ```text
/// K = P x / (λ + xᵀ P x)
/// θ ← θ + K (y − xᵀ θ)
/// P ← (P − K (P x)ᵀ) / λ
/// ```
///
/// The effective sample size is the geometric-weight sum
/// `(1 − λⁿ) / (1 − λ)`, which is `n` when `λ = 1` and approaches `1/(1−λ)`
/// otherwise. A tiny `n_eff` relative to `p` is
/// [`IssueCode::ForgettingErasedIdentification`].
#[derive(Clone, Debug)]
pub struct LinearRegression {
    /// Forgetting factor `λ ∈ (0, 1]`. `λ = 1` is ordinary growing-window RLS.
    pub forgetting_factor: f64,
    /// Prepend a column of ones.
    pub fit_intercept: bool,
    /// Initial `P = p0 I`.
    pub p0: f64,
    theta: Vector,
    p_mat: Option<Mat<f64>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LinearRegression {
    fn default() -> Self {
        Self {
            forgetting_factor: 1.0,
            fit_intercept: true,
            p0: 1_000.0,
            theta: Vector::zeros(0),
            p_mat: None,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LinearRegression {
    /// RLS with forgetting `λ`.
    pub fn new(forgetting_factor: f64) -> Self {
        Self {
            forgetting_factor,
            ..Self::default()
        }
    }

    /// Slope coefficients (intercept excluded).
    pub fn coef(&self) -> Vector {
        if self.fit_intercept && self.theta.len() > 0 {
            Vector::from_iter((1..self.theta.len()).map(|i| self.theta[i]))
        } else {
            self.theta.clone()
        }
    }

    /// Intercept, or 0 when `fit_intercept` is false.
    pub fn intercept(&self) -> f64 {
        if self.fit_intercept && !self.theta.is_empty() {
            self.theta[0]
        } else {
            0.0
        }
    }

    /// Inverse Gram `P` (covariance of `θ` up to σ²).
    pub fn p_matrix(&self) -> Option<&Mat<f64>> {
        self.p_mat.as_ref()
    }

    fn dim(&self, p_x: usize) -> usize {
        p_x + if self.fit_intercept { 1 } else { 0 }
    }

    fn row_vec(&self, x: &Matrix, i: usize) -> Vector {
        let p = self.dim(x.ncols());
        let mut v = Vector::zeros(p);
        if self.fit_intercept {
            v[0] = 1.0;
            for j in 0..x.ncols() {
                v[j + 1] = x.get(i, j);
            }
        } else {
            for j in 0..x.ncols() {
                v[j] = x.get(i, j);
            }
        }
        v
    }

    fn init(&mut self, p_x: usize) {
        let p = self.dim(p_x);
        self.theta = Vector::zeros(p);
        self.p_mat = Some(Mat::<f64>::from_fn(p, p, |i, j| {
            if i == j {
                self.p0
            } else {
                0.0
            }
        }));
        self.initialized = true;
    }

    fn n_eff(&self) -> f64 {
        let lam = self.forgetting_factor;
        let n = self.n_seen as f64;
        if (1.0 - lam).abs() < 1e-15 {
            n
        } else {
            (1.0 - lam.powf(n)) / (1.0 - lam)
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        if !self.initialized {
            return 0.0;
        }
        let z = self.row_vec(x, i);
        z.dot(&self.theta)
    }
}

impl PartialFit for LinearRegression {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(
                Issue::builder(IssueCode::MissingTarget)
                    .message("online LinearRegression.partial_fit requires y")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.nrows() == 0 || x.ncols() == 0 {
            if !self.initialized {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty batch"),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        if !(self.forgetting_factor > 0.0 && self.forgetting_factor <= 1.0) {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .message(format!(
                        "forgetting λ={} is not in (0, 1]",
                        self.forgetting_factor
                    ))
                    .build(),
            );
        }
        if !self.initialized {
            self.init(x.ncols());
        } else if self.dim(x.ncols()) != self.theta.len() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message(format!(
                        "RLS expects {} design columns (incl. intercept); got {}",
                        self.theta.len(),
                        self.dim(x.ncols())
                    ))
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.theta.clone();
        let loss_before = rls_mse(self, x, y);
        let mut info = 0.0;
        let lam = self.forgetting_factor.clamp(1e-12, 1.0);
        for i in 0..x.nrows() {
            let z = self.row_vec(x, i);
            let p = match self.p_mat.as_mut() {
                Some(p) => p,
                None => break,
            };
            let mut pz = Vector::zeros(z.len());
            for r in 0..z.len() {
                let mut s = 0.0;
                for c in 0..z.len() {
                    s += p[(r, c)] * z[c];
                }
                pz[r] = s;
            }
            let denom = lam + z.dot(&pz);
            if !denom.is_finite() || denom.abs() < 1e-18 {
                ctx.push(
                    Issue::builder(IssueCode::NearSingular)
                        .message("RLS gain denominator vanished")
                        .build(),
                );
                continue;
            }
            let pred = z.dot(&self.theta);
            let err = y[i] - pred;
            info += err * err;
            for j in 0..self.theta.len() {
                self.theta[j] += (pz[j] / denom) * err;
            }
            // P ← (P − (Pz)(Pz)ᵀ / denom) / λ
            for r in 0..z.len() {
                for c in 0..z.len() {
                    p[(r, c)] = (p[(r, c)] - pz[r] * pz[c] / denom) / lam;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.theta.sub(&before);
        let loss_after = rls_mse(self, x, y);
        let n_eff = self.n_eff();
        let p = self.theta.len();
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = n_eff;
        q.forgetting_factor = Some(self.forgetting_factor);
        q.parameter_delta_norm = Some(delta.norm());
        q.parameter_delta_max = Some(delta.max_abs());
        q.loss_before = Some(loss_before);
        q.loss_after = Some(loss_after);
        q.information_gain = Some(
            (loss_before - loss_after)
                .abs()
                .max(info / x.nrows().max(1) as f64),
        );
        q.still_identified = n_eff > p as f64 && self.n_seen as usize > p;
        q.warmup = self.n_seen < 5 || (self.n_seen as usize) <= p;
        q.explanation = format!(
            "RLS λ={:.4}: {} rows, n_eff={n_eff:.3}, ||Δθ||={:.4e}, mse {:.6e} → {:.6e}",
            self.forgetting_factor,
            x.nrows(),
            delta.norm(),
            loss_before,
            loss_after
        );
        q.top_moved_parameters = top_moved(&delta, 3);
        if q.is_uninformative(ctx.policy.uninformative_info_eps)
            || (loss_before - loss_after).abs() <= ctx.policy.uninformative_info_eps
                && delta.norm() <= ctx.policy.uninformative_info_eps
        {
            ctx.push(
                Issue::builder(IssueCode::UpdateWithZeroInformation)
                    .incremental(q.clone())
                    .message("this RLS batch did not change the residual or θ")
                    .build(),
            );
        }
        if n_eff < ctx.policy.min_effective_sample || n_eff <= p as f64 {
            ctx.push(
                Issue::builder(IssueCode::ForgettingErasedIdentification)
                    .incremental(q.clone())
                    .message(format!(
                        "n_eff={n_eff:.3} (asymptotic 1/(1−λ)={:.3}) is too small to identify {p} parameters",
                        if (1.0 - self.forgetting_factor).abs() < 1e-15 {
                            f64::INFINITY
                        } else {
                            1.0 / (1.0 - self.forgetting_factor)
                        }
                    ))
                    .metric("n_eff", n_eff)
                    .build(),
            );
        }
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("RLS is still in warmup (n_seen ≤ p or n_seen < 5)")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("θ[{p}] and inverse Gram P"),
                "recursive least squares / Kalman gain on the new rows",
                format!("mse={loss_before:.6e}"),
                format!("mse={loss_after:.6e}"),
            ),
        )
    }
}

impl Predict for LinearRegression {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i)));
        ctx.finish(y)
    }
}

fn rls_mse(m: &LinearRegression, x: &Matrix, y: &Vector) -> f64 {
    if x.nrows() == 0 {
        return f64::NAN;
    }
    let mut s = 0.0;
    for i in 0..x.nrows() {
        let e = m.predict_row(x, i) - y[i];
        s += e * e;
    }
    s / x.nrows() as f64
}

/// Online logistic regression by SGD (binary, labels mapped to `{0, 1}`).
#[derive(Clone, Debug)]
pub struct LogisticRegression {
    /// Step size.
    pub learning_rate: f64,
    /// ℓ₂ penalty on the slopes.
    pub l2: f64,
    /// Intercept.
    pub fit_intercept: bool,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogisticRegression {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            l2: 0.0,
            fit_intercept: true,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogisticRegression {
    /// Default online logistic.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current slopes.
    pub fn coef(&self) -> &Vector {
        &self.coef
    }

    /// Current intercept.
    pub fn intercept(&self) -> f64 {
        self.intercept
    }
}

impl PartialFit for LogisticRegression {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message("online logistic feature dimension changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        if self.learning_rate > 1.0 {
            ctx.push(
                Issue::builder(IssueCode::LearningRateTooLarge)
                    .message(format!(
                        "η={} is large for unscaled logistic SGD",
                        self.learning_rate
                    ))
                    .build(),
            );
        }
        let before = self.coef.clone();
        let loss_before = logloss_of(x, y, &self.coef, self.intercept);
        for i in 0..x.nrows() {
            let yi = as01(y[i]);
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let mu = sigmoid(eta);
            let g = mu - yi;
            for j in 0..x.ncols() {
                self.coef[j] -= self.learning_rate * (g * x.get(i, j) + self.l2 * self.coef[j]);
            }
            if self.fit_intercept {
                self.intercept -= self.learning_rate * g;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let loss_after = logloss_of(x, y, &self.coef, self.intercept);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "logistic coefficients",
            "SGD on Bernoulli log-loss",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for LogisticRegression {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            if sigmoid(eta) >= 0.5 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(y)
    }
}

/// Online perceptron (mistake-driven, labels mapped to `±1`).
#[derive(Clone, Debug)]
pub struct Perceptron {
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    mistakes: u64,
    initialized: bool,
}

impl Default for Perceptron {
    fn default() -> Self {
        Self {
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            mistakes: 0,
            initialized: false,
        }
    }
}

impl Perceptron {
    /// Zero-initialized perceptron.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current slopes.
    pub fn coef(&self) -> &Vector {
        &self.coef
    }
}

impl PartialFit for Perceptron {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        let before = self.coef.clone();
        let mistakes_before = self.mistakes;
        for i in 0..x.nrows() {
            let yi = as_pm(y[i]);
            let mut s = self.intercept;
            for j in 0..x.ncols() {
                s += self.coef[j] * x.get(i, j);
            }
            if yi * s <= 0.0 {
                self.mistakes += 1;
                for j in 0..x.ncols() {
                    self.coef[j] += yi * x.get(i, j);
                }
                self.intercept += yi;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let m_after = self.mistakes - mistakes_before;
        let loss_before = m_after as f64 / x.nrows().max(1) as f64;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            0.0,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "perceptron weights",
            "additive update on mistakes (Rosenblatt)",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for Perceptron {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            if s >= 0.0 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(y)
    }
}

/// Passive-aggressive PA-I classifier (Crammer et al.).
#[derive(Clone, Debug)]
pub struct PassiveAggressive {
    /// Aggressiveness `C` (maximum step).
    pub c: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PassiveAggressive {
    fn default() -> Self {
        Self {
            c: 1.0,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PassiveAggressive {
    /// PA-I with aggressiveness `c`.
    pub fn new(c: f64) -> Self {
        Self {
            c,
            ..Self::default()
        }
    }

    /// Current slopes.
    pub fn coef(&self) -> &Vector {
        &self.coef
    }
}

impl PartialFit for PassiveAggressive {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        let before = self.coef.clone();
        let mut hinge = 0.0;
        for i in 0..x.nrows() {
            let yi = as_pm(y[i]);
            let mut s = self.intercept;
            let mut n2 = 1.0;
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                s += self.coef[j] * v;
                n2 += v * v;
            }
            let loss = (1.0 - yi * s).max(0.0);
            hinge += loss;
            if loss > 0.0 && n2 > 0.0 {
                let tau = (loss / n2).min(self.c);
                for j in 0..x.ncols() {
                    self.coef[j] += tau * yi * x.get(i, j);
                }
                self.intercept += tau * yi;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let loss_before = hinge / x.nrows().max(1) as f64;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            0.0,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "PA-I weights",
            "hinge-minimizing projection with cap C",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for PassiveAggressive {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            if s >= 0.0 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(y)
    }
}

/// Very Fast Decision Tree (Hoeffding) for binary classification.
///
/// Each leaf stores class counts and per-feature class-conditional Gaussians.
/// A split is taken when the Gini-gain gap exceeds the Hoeffding bound
/// `√(ln(1/δ) / (2 n))`, or the bound itself falls below `tau`.
#[derive(Clone, Debug)]
pub struct HoeffdingTree {
    /// Split-confidence `δ`.
    pub delta: f64,
    /// Minimum observations in a leaf before a split is considered.
    pub min_samples: usize,
    /// Tie-break threshold on the Hoeffding bound.
    pub tau: f64,
    root: HtNode,
    n_seen: u64,
    updates: u64,
    n_features: usize,
    initialized: bool,
    last_split: Option<String>,
}

#[derive(Clone, Debug)]
enum HtNode {
    Leaf(HtLeaf),
    Split {
        feature: usize,
        threshold: f64,
        left: Box<HtNode>,
        right: Box<HtNode>,
        n: u64,
    },
}

#[derive(Clone, Debug)]
struct HtLeaf {
    id: u64,
    n: u64,
    n_pos: u64,
    n_neg: u64,
    feat_neg: Vec<Gauss>,
    feat_pos: Vec<Gauss>,
}

#[derive(Clone, Copy, Debug, Default)]
struct Gauss {
    n: f64,
    mean: f64,
    m2: f64,
}

impl Gauss {
    fn push(&mut self, x: f64) {
        if !x.is_finite() {
            return;
        }
        self.n += 1.0;
        let d = x - self.mean;
        self.mean += d / self.n;
        self.m2 += d * (x - self.mean);
    }
    fn std(&self) -> f64 {
        if self.n < 2.0 {
            1e-6
        } else {
            (self.m2 / (self.n - 1.0)).max(0.0).sqrt().max(1e-6)
        }
    }
    fn var(&self) -> f64 {
        if self.n < 2.0 {
            0.0
        } else {
            (self.m2 / (self.n - 1.0)).max(0.0)
        }
    }
}

impl HtLeaf {
    fn new(id: u64, p: usize) -> Self {
        Self {
            id,
            n: 0,
            n_pos: 0,
            n_neg: 0,
            feat_neg: vec![Gauss::default(); p],
            feat_pos: vec![Gauss::default(); p],
        }
    }

    fn push(&mut self, x: &Matrix, i: usize, pos: bool) {
        self.n += 1;
        if pos {
            self.n_pos += 1;
        } else {
            self.n_neg += 1;
        }
        for j in 0..x.ncols().min(self.feat_neg.len()) {
            if pos {
                self.feat_pos[j].push(x.get(i, j));
            } else {
                self.feat_neg[j].push(x.get(i, j));
            }
        }
    }

    fn gini(&self) -> f64 {
        gini_counts(self.n_neg as f64, self.n_pos as f64)
    }

    fn maybe_split(
        &self,
        min_samples: usize,
        delta: f64,
        tau: f64,
    ) -> Option<(usize, f64, f64, f64)> {
        if (self.n as usize) < min_samples || self.n_pos == 0 || self.n_neg == 0 {
            return None;
        }
        let p = self.feat_neg.len();
        let parent = self.gini();
        let mut best = (0usize, 0.0, -1.0);
        let mut second = -1.0;
        for j in 0..p {
            let t1 = self.feat_neg[j].mean;
            let t2 = self.feat_pos[j].mean;
            let t3 = 0.5 * (t1 + t2);
            for &t in &[t1, t2, t3] {
                let gain = self.gain_at(j, t, parent);
                if gain > best.2 {
                    second = best.2;
                    best = (j, t, gain);
                } else if gain > second {
                    second = gain;
                }
            }
        }
        let n = self.n as f64;
        let eps = (0.5 * (1.0 / delta.max(1e-12)).ln() / n).sqrt();
        if best.2 < 0.0 {
            return None;
        }
        if best.2 - second.max(0.0) > eps || eps < tau {
            Some((best.0, best.1, best.2, eps))
        } else {
            None
        }
    }

    fn gain_at(&self, j: usize, t: f64, parent: f64) -> f64 {
        let gn = &self.feat_neg[j];
        let gp = &self.feat_pos[j];
        let p_left_neg = norm_cdf((t - gn.mean) / gn.std());
        let p_left_pos = norm_cdf((t - gp.mean) / gp.std());
        let n_left_neg = self.n_neg as f64 * p_left_neg;
        let n_left_pos = self.n_pos as f64 * p_left_pos;
        let n_right_neg = self.n_neg as f64 - n_left_neg;
        let n_right_pos = self.n_pos as f64 - n_left_pos;
        let n_l = n_left_neg + n_left_pos;
        let n_r = n_right_neg + n_right_pos;
        let n = (self.n as f64).max(1.0);
        if n_l <= 1.0 || n_r <= 1.0 {
            return -1.0;
        }
        parent
            - (n_l * gini_counts(n_left_neg, n_left_pos)
                + n_r * gini_counts(n_right_neg, n_right_pos))
                / n
    }
}

fn gini_counts(n0: f64, n1: f64) -> f64 {
    let n = n0 + n1;
    if n <= 0.0 {
        return 0.0;
    }
    let p0 = n0 / n;
    let p1 = n1 / n;
    1.0 - p0 * p0 - p1 * p1
}

impl Default for HoeffdingTree {
    fn default() -> Self {
        Self {
            delta: 1e-7,
            min_samples: 20,
            tau: 0.05,
            root: HtNode::Leaf(HtLeaf::new(0, 0)),
            n_seen: 0,
            updates: 0,
            n_features: 0,
            initialized: false,
            last_split: None,
        }
    }
}

impl HoeffdingTree {
    /// Default VFDT.
    pub fn new() -> Self {
        Self::default()
    }

    /// Narrative of the most recent leaf split, if any.
    pub fn last_split(&self) -> Option<&str> {
        self.last_split.as_deref()
    }

    /// Reset to an empty root (used by adaptive forests after drift).
    pub fn reset(&mut self) {
        self.root = HtNode::Leaf(HtLeaf::new(0, self.n_features));
        self.n_seen = 0;
        self.last_split = None;
        self.initialized = self.n_features > 0;
    }

    fn predict_one(&self, x: &Matrix, i: usize) -> f64 {
        let mut node = &self.root;
        loop {
            match node {
                HtNode::Leaf(l) => {
                    return if l.n_pos >= l.n_neg { 1.0 } else { 0.0 };
                }
                HtNode::Split {
                    feature,
                    threshold,
                    left,
                    right,
                    ..
                } => {
                    let v = if *feature < x.ncols() {
                        x.get(i, *feature)
                    } else {
                        0.0
                    };
                    node = if v <= *threshold { left } else { right };
                }
            }
        }
    }
}

fn ht_update(
    node: &mut HtNode,
    x: &Matrix,
    i: usize,
    pos: bool,
    min_samples: usize,
    delta: f64,
    tau: f64,
    next_id: u64,
) -> Option<String> {
    match node {
        HtNode::Split {
            feature,
            threshold,
            left,
            right,
            n,
        } => {
            *n += 1;
            if x.get(i, *feature) <= *threshold {
                ht_update(left, x, i, pos, min_samples, delta, tau, next_id)
            } else {
                ht_update(right, x, i, pos, min_samples, delta, tau, next_id)
            }
        }
        HtNode::Leaf(leaf) => {
            leaf.push(x, i, pos);
            if let Some((feat, thr, gain, eps)) = leaf.maybe_split(min_samples, delta, tau) {
                let p = leaf.feat_neg.len();
                let narrative = format!(
                    "leaf {} split on feature {feat} at {thr:.6} (Gini gain {gain:.4}, Hoeffding ε={eps:.4}, n={})",
                    leaf.id, leaf.n
                );
                *node = HtNode::Split {
                    feature: feat,
                    threshold: thr,
                    left: Box::new(HtNode::Leaf(HtLeaf::new(next_id, p))),
                    right: Box::new(HtNode::Leaf(HtLeaf::new(next_id + 1, p))),
                    n: leaf.n,
                };
                Some(narrative)
            } else {
                None
            }
        }
    }
}

impl PartialFit for HoeffdingTree {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.n_features = x.ncols();
            self.root = HtNode::Leaf(HtLeaf::new(0, x.ncols()));
            self.initialized = true;
        } else if x.ncols() != self.n_features {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut split_note = None;
        for i in 0..x.nrows() {
            let pos = as01(y[i]) >= 0.5;
            let nid = self.n_seen + 17 + i as u64;
            if let Some(s) = ht_update(
                &mut self.root,
                x,
                i,
                pos,
                self.min_samples,
                self.delta,
                self.tau,
                nid,
            ) {
                split_note = Some(s);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        if let Some(s) = &split_note {
            self.last_split = Some(s.clone());
        }
        let warmup = self.n_seen < self.min_samples as u64;
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message(format!(
                        "Hoeffding tree has seen {} < min_samples={}",
                        self.n_seen, self.min_samples
                    ))
                    .build(),
            );
        }
        let info = if split_note.is_some() { 1.0 } else { 0.0 };
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if split_note.is_some() { 1.0 } else { 0.0 });
        q.information_gain = Some(info);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = split_note
            .clone()
            .unwrap_or_else(|| format!("leaf statistics updated on {} rows; no split", x.nrows()));
        if q.is_uninformative(ctx.policy.uninformative_info_eps) && !warmup {
            ctx.push(
                Issue::builder(IssueCode::UpdateWithZeroInformation)
                    .incremental(q.clone())
                    .message("Hoeffding update did not split and added no structural change")
                    .build(),
            );
        }
        let what = split_note
            .clone()
            .unwrap_or_else(|| "per-leaf class counts and Gaussian sufficient stats".into());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                what,
                "Hoeffding-bound Gini split test (or sufficient-stat update)",
                "pre-batch tree",
                "post-batch tree",
            ),
        )
    }
}

impl Predict for HoeffdingTree {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_one(x, i)),
        ))
    }
}

/// Hoeffding tree regressor (river `HoeffdingTreeRegressor`).
///
/// Leaves predict a running mean. A split is taken when the variance-reduction
/// gap exceeds the Hoeffding bound, or the bound itself falls below `tau`.
#[derive(Clone, Debug)]
pub struct HoeffdingRegressor {
    /// Split-confidence `δ`.
    pub delta: f64,
    /// Minimum observations in a leaf before a split is considered.
    pub min_samples: usize,
    /// Tie-break threshold on the Hoeffding bound.
    pub tau: f64,
    root: HrNode,
    n_seen: u64,
    updates: u64,
    n_features: usize,
    initialized: bool,
    last_split: Option<String>,
}

#[derive(Clone, Debug)]
enum HrNode {
    Leaf(HrLeaf),
    Split {
        feature: usize,
        threshold: f64,
        left: Box<HrNode>,
        right: Box<HrNode>,
        n: u64,
    },
}

#[derive(Clone, Debug)]
struct HrLeaf {
    id: u64,
    y: Gauss,
    bins: Vec<HrBin>,
}

#[derive(Clone, Debug, Default)]
struct HrBin {
    gx: Gauss,
    left: Gauss,
    right: Gauss,
}

impl HrLeaf {
    fn new(id: u64, p: usize) -> Self {
        Self {
            id,
            y: Gauss::default(),
            bins: vec![HrBin::default(); p],
        }
    }

    fn push(&mut self, x: &Matrix, i: usize, yi: f64) {
        self.y.push(yi);
        for j in 0..x.ncols().min(self.bins.len()) {
            let xv = x.get(i, j);
            let t = if self.bins[j].gx.n < 1.0 {
                xv
            } else {
                self.bins[j].gx.mean
            };
            if xv <= t {
                self.bins[j].left.push(yi);
            } else {
                self.bins[j].right.push(yi);
            }
            self.bins[j].gx.push(xv);
        }
    }

    fn maybe_split(
        &self,
        min_samples: usize,
        delta: f64,
        tau: f64,
    ) -> Option<(usize, f64, f64, f64)> {
        if (self.y.n as usize) < min_samples {
            return None;
        }
        let parent_var = self.y.var();
        if parent_var <= 1e-18 {
            return None;
        }
        let mut best = (0usize, 0.0, -1.0);
        let mut second = -1.0;
        for (j, bin) in self.bins.iter().enumerate() {
            let nl = bin.left.n;
            let nr = bin.right.n;
            if nl < 2.0 || nr < 2.0 {
                continue;
            }
            let n = self.y.n.max(1.0);
            let red = parent_var - (nl * bin.left.var() + nr * bin.right.var()) / n;
            if red > best.2 {
                second = best.2;
                best = (j, bin.gx.mean, red);
            } else if red > second {
                second = red;
            }
        }
        let eps = (0.5 * (1.0 / delta.max(1e-12)).ln() / self.y.n.max(1.0)).sqrt();
        if best.2 < 0.0 {
            return None;
        }
        if best.2 - second.max(0.0) > eps || eps < tau {
            Some((best.0, best.1, best.2, eps))
        } else {
            None
        }
    }
}

impl Default for HoeffdingRegressor {
    fn default() -> Self {
        Self {
            delta: 1e-7,
            min_samples: 20,
            tau: 0.05,
            root: HrNode::Leaf(HrLeaf::new(0, 0)),
            n_seen: 0,
            updates: 0,
            n_features: 0,
            initialized: false,
            last_split: None,
        }
    }
}

impl HoeffdingRegressor {
    /// Default Hoeffding regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn reset(&mut self) {
        if self.n_features > 0 {
            self.root = HrNode::Leaf(HrLeaf::new(0, self.n_features));
        }
        self.n_seen = 0;
        self.last_split = None;
    }

    fn predict_one(&self, x: &Matrix, i: usize) -> f64 {
        let mut node = &self.root;
        loop {
            match node {
                HrNode::Leaf(l) => return l.y.mean,
                HrNode::Split {
                    feature,
                    threshold,
                    left,
                    right,
                    ..
                } => {
                    let v = if *feature < x.ncols() {
                        x.get(i, *feature)
                    } else {
                        0.0
                    };
                    node = if v <= *threshold { left } else { right };
                }
            }
        }
    }
}

fn hr_update(
    node: &mut HrNode,
    x: &Matrix,
    i: usize,
    yi: f64,
    min_samples: usize,
    delta: f64,
    tau: f64,
    next_id: u64,
) -> Option<String> {
    match node {
        HrNode::Split {
            feature,
            threshold,
            left,
            right,
            n,
        } => {
            *n += 1;
            if x.get(i, *feature) <= *threshold {
                hr_update(left, x, i, yi, min_samples, delta, tau, next_id)
            } else {
                hr_update(right, x, i, yi, min_samples, delta, tau, next_id)
            }
        }
        HrNode::Leaf(leaf) => {
            leaf.push(x, i, yi);
            if let Some((feat, thr, gain, eps)) = leaf.maybe_split(min_samples, delta, tau) {
                let p = leaf.bins.len();
                let narrative = format!(
                    "leaf {} split on feature {feat} at {thr:.6e} (var-red {gain:.4e}, ε={eps:.4e}, n={})",
                    leaf.id, leaf.y.n
                );
                *node = HrNode::Split {
                    feature: feat,
                    threshold: thr,
                    left: Box::new(HrNode::Leaf(HrLeaf::new(next_id, p))),
                    right: Box::new(HrNode::Leaf(HrLeaf::new(next_id + 1, p))),
                    n: leaf.y.n as u64,
                };
                Some(narrative)
            } else {
                None
            }
        }
    }
}

impl PartialFit for HoeffdingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.n_features = x.ncols();
            self.root = HrNode::Leaf(HrLeaf::new(0, x.ncols()));
            self.initialized = true;
        } else if x.ncols() != self.n_features {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut split_note = None;
        for i in 0..x.nrows() {
            let nid = self.n_seen + 17 + i as u64;
            if let Some(s) = hr_update(
                &mut self.root,
                x,
                i,
                y[i],
                self.min_samples,
                self.delta,
                self.tau,
                nid,
            ) {
                split_note = Some(s);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        if let Some(s) = &split_note {
            self.last_split = Some(s.clone());
        }
        let warmup = self.n_seen < self.min_samples as u64;
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message(format!(
                        "HoeffdingRegressor has seen {} < min_samples={}",
                        self.n_seen, self.min_samples
                    ))
                    .build(),
            );
        }
        let info = if split_note.is_some() { 1.0 } else { 0.0 };
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if split_note.is_some() { 1.0 } else { 0.0 });
        q.information_gain = Some(info);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = split_note
            .clone()
            .unwrap_or_else(|| format!("leaf means updated on {} rows; no split", x.nrows()));
        if q.is_uninformative(ctx.policy.uninformative_info_eps) && !warmup {
            ctx.push(
                Issue::builder(IssueCode::UpdateWithZeroInformation)
                    .incremental(q.clone())
                    .message("HoeffdingRegressor update did not split")
                    .build(),
            );
        }
        let what = split_note
            .clone()
            .unwrap_or_else(|| "per-leaf mean and variance-reduction candidates".into());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                what,
                "Hoeffding-bound variance-reduction split test",
                "pre-batch tree",
                "post-batch tree",
            ),
        )
    }
}

impl Predict for HoeffdingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_one(x, i)),
        ))
    }
}

/// Online one-hot encoder (river `preprocessing.OneHotEncoder`).
///
/// Width is capped at `n_features × max_categories`. New categories after the
/// first update are a warned feature-space change, not an abort.
#[derive(Clone, Debug)]
pub struct OnlineOneHotEncoder {
    /// Cap on distinct codes per column.
    pub max_categories: usize,
    maps: Vec<HashMap<i64, usize>>,
    n_features: usize,
    initialized: bool,
    updates: u64,
    n_seen: u64,
    new_cats: u64,
}

impl Default for OnlineOneHotEncoder {
    fn default() -> Self {
        Self {
            max_categories: 8,
            maps: Vec::new(),
            n_features: 0,
            initialized: false,
            updates: 0,
            n_seen: 0,
            new_cats: 0,
        }
    }
}

impl OnlineOneHotEncoder {
    /// Encoder with `max_categories` slots per column.
    pub fn new(max_categories: usize) -> Self {
        Self {
            max_categories: max_categories.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineOneHotEncoder {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.n_features = x.ncols();
            self.maps = vec![HashMap::new(); x.ncols()];
            self.initialized = true;
        } else if x.ncols() != self.n_features {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let mut added = 0u64;
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let key = v.round() as i64;
                if self.maps[j].contains_key(&key) {
                    continue;
                }
                if self.maps[j].len() >= self.max_categories {
                    ctx.push(
                        Issue::builder(IssueCode::InvalidWeight)
                            .severity(Severity::Warning)
                            .message(format!(
                                "OnlineOneHotEncoder column {j} exceeded max_categories={}",
                                self.max_categories
                            ))
                            .build(),
                    );
                    continue;
                }
                let slot = self.maps[j].len();
                self.maps[j].insert(key, slot);
                added += 1;
                if self.updates > 0 {
                    ctx.push(
                        Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                            .severity(Severity::Warning)
                            .message(format!("new category {key} on column {j}"))
                            .build(),
                    );
                }
            }
        }
        self.new_cats += added;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(added as f64);
        q.information_gain = Some((added as f64).max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "online one-hot added {added} categories on {} rows",
            x.nrows()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "category maps per column",
                "integer-code one-hot with a fixed width cap",
                "previous category maps",
                "updated category maps",
            ),
        )
    }
}

impl Transform for OnlineOneHotEncoder {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), 0));
        }
        let width = self.n_features * self.max_categories;
        let out = Matrix::from_fn(x.nrows(), width, |i, j| {
            let col = j / self.max_categories;
            let slot = j % self.max_categories;
            if col >= x.ncols() || col >= self.maps.len() {
                return 0.0;
            }
            let v = x.get(i, col);
            if !v.is_finite() {
                return 0.0;
            }
            match self.maps[col].get(&(v.round() as i64)) {
                Some(&s) if s == slot => 1.0,
                _ => 0.0,
            }
        });
        ctx.finish(out)
    }
}

/// Online ordinal encoder (river `preprocessing.OrdinalEncoder`).
///
/// New categories after the first update raise
/// [`IssueCode::FeatureSpaceChangedOnline`] as a warning (the default Error
/// would abort a valid stream).
#[derive(Clone, Debug)]
pub struct OnlineOrdinalEncoder {
    /// Cap on distinct codes per column.
    pub max_categories: usize,
    maps: Vec<HashMap<i64, usize>>,
    n_features: usize,
    initialized: bool,
    updates: u64,
    n_seen: u64,
    new_cats: u64,
}

impl Default for OnlineOrdinalEncoder {
    fn default() -> Self {
        Self {
            max_categories: 16,
            maps: Vec::new(),
            n_features: 0,
            initialized: false,
            updates: 0,
            n_seen: 0,
            new_cats: 0,
        }
    }
}

impl OnlineOrdinalEncoder {
    /// Encoder with `max_categories` codes per column.
    pub fn new(max_categories: usize) -> Self {
        Self {
            max_categories: max_categories.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineOrdinalEncoder {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.n_features = x.ncols();
            self.maps = vec![HashMap::new(); x.ncols()];
            self.initialized = true;
        } else if x.ncols() != self.n_features {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let mut added = 0u64;
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let key = v.round() as i64;
                if self.maps[j].contains_key(&key) {
                    continue;
                }
                if self.maps[j].len() >= self.max_categories {
                    ctx.push(
                        Issue::builder(IssueCode::InvalidWeight)
                            .severity(Severity::Warning)
                            .message(format!(
                                "OnlineOrdinalEncoder column {j} exceeded max_categories={}",
                                self.max_categories
                            ))
                            .build(),
                    );
                    continue;
                }
                let slot = self.maps[j].len();
                self.maps[j].insert(key, slot);
                added += 1;
                if self.updates > 0 {
                    ctx.push(
                        Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                            .severity(Severity::Warning)
                            .message(format!("new ordinal category {key} on column {j}"))
                            .build(),
                    );
                }
            }
        }
        self.new_cats += added;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(added as f64);
        q.information_gain = Some((added as f64).max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "online ordinal encoder added {added} codes on {} rows",
            x.nrows()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "integer codes per column",
                "first-seen category → 0..K−1 with a fixed cap",
                "previous ordinal maps",
                "updated ordinal maps",
            ),
        )
    }
}

impl Transform for OnlineOrdinalEncoder {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), x.ncols()));
        }
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= self.maps.len() {
                return -1.0;
            }
            let v = x.get(i, j);
            if !v.is_finite() {
                return -1.0;
            }
            match self.maps[j].get(&(v.round() as i64)) {
                Some(&s) => s as f64,
                None => -1.0,
            }
        });
        ctx.finish(out)
    }
}

/// Outcome of a univariate drift detector.
#[derive(Clone, Copy, Debug, PartialEq)]
pub enum DriftDecision {
    /// No evidence of a change.
    Stable,
    /// Warning region (DDM) or mild evidence.
    Warning {
        /// Detector statistic.
        statistic: f64,
    },
    /// Drift declared; old parameters no longer describe the stream.
    Drift {
        /// Detector statistic.
        statistic: f64,
    },
}

/// Dummy drift detector (river `drift.DummyDrift`).
///
/// Fires every `every` observations (`0` never fires). The period is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct DummyDrift {
    /// Drift period. `0` means never.
    pub every: u64,
    n_seen: u64,
    updates: u64,
    last: DriftDecision,
}

impl Default for DummyDrift {
    fn default() -> Self {
        Self {
            every: 0,
            n_seen: 0,
            updates: 0,
            last: DriftDecision::Stable,
        }
    }
}

impl DummyDrift {
    /// Fire every `every` rows (`0` = never).
    pub fn new(every: u64) -> Self {
        Self {
            every,
            ..Self::default()
        }
    }

    /// Last decision.
    pub fn decision(&self) -> DriftDecision {
        self.last
    }
}

impl PartialFit for DummyDrift {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.n_seen;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        self.last = if self.every > 0 && self.n_seen / self.every > before / self.every {
            DriftDecision::Drift {
                statistic: self.n_seen as f64,
            }
        } else {
            DriftDecision::Stable
        };
        if matches!(self.last, DriftDecision::Drift { .. }) {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .severity(Severity::Warning)
                    .message(format!(
                        "DummyDrift fired on a calendar period every={}",
                        self.every
                    ))
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("DummyDrift n={} last={:?}", self.n_seen, self.last);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "calendar drift trigger",
                "DummyDrift fires on a fixed observation period, not a data-dependent test",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

/// ADWIN (Bifet & Gavaldà): adaptive windowing on a scalar stream.
#[derive(Clone, Debug)]
pub struct Adwin {
    /// Confidence `δ`.
    pub delta: f64,
    /// Hard cap on the stored window (oldest points are dropped).
    pub max_window: usize,
    window: Vec<f64>,
    cuts: u64,
}

impl Default for Adwin {
    fn default() -> Self {
        Self {
            delta: 0.002,
            max_window: 2048,
            window: Vec::new(),
            cuts: 0,
        }
    }
}

impl Adwin {
    /// Detector with the given `δ`.
    pub fn new(delta: f64) -> Self {
        Self {
            delta,
            ..Self::default()
        }
    }

    /// Current window length.
    pub fn window_len(&self) -> usize {
        self.window.len()
    }

    /// Reset the window.
    pub fn reset(&mut self) {
        self.window.clear();
    }

    /// Number of ADWIN cuts performed so far.
    pub fn n_cuts(&self) -> u64 {
        self.cuts
    }

    /// Consume one observation and decide whether the window must shrink.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if x.is_finite() {
            self.window.push(x);
        }
        if self.window.len() > self.max_window {
            let drop = self.window.len() - self.max_window;
            self.window.drain(0..drop);
        }
        let decision = self.try_cut();
        match decision {
            DriftDecision::Drift { statistic } => {
                ctx.push(
                    Issue::builder(IssueCode::ConceptDriftDetected)
                        .message(format!(
                            "ADWIN cut the window (δ={}, |μ₀−μ₁|={statistic:.4}, n={})",
                            self.delta,
                            self.window.len()
                        ))
                        .metric("drift_statistic", statistic)
                        .build(),
                );
            }
            DriftDecision::Warning { statistic } => {
                ctx.push(
                    Issue::builder(IssueCode::VirtualDriftDetected)
                        .severity(Severity::Advisory)
                        .message(format!("ADWIN warning statistic={statistic:.4}"))
                        .build(),
                );
            }
            DriftDecision::Stable => {}
        }
        ctx.finish(decision)
    }

    fn try_cut(&mut self) -> DriftDecision {
        let n = self.window.len();
        if n < 16 {
            return DriftDecision::Stable;
        }
        let mut prefix = vec![0.0; n + 1];
        for i in 0..n {
            prefix[i + 1] = prefix[i] + self.window[i];
        }
        let total = prefix[n];
        let nf = n as f64;
        for i in 8..(n - 8) {
            let n0 = i as f64;
            let n1 = (n - i) as f64;
            let mu0 = prefix[i] / n0;
            let mu1 = (total - prefix[i]) / n1;
            let diff = (mu0 - mu1).abs();
            let m = 1.0 / (1.0 / n0 + 1.0 / n1);
            let delta_p = (self.delta / nf.max(1.0)).max(1e-16);
            let eps = (0.5 / m * (4.0 / delta_p).ln()).sqrt();
            if diff > eps {
                self.window.drain(0..i);
                self.cuts += 1;
                return DriftDecision::Drift { statistic: diff };
            }
        }
        DriftDecision::Stable
    }
}

/// Gama et al. Drift Detection Method on a 0/1 error stream.
#[derive(Clone, Debug)]
pub struct Ddm {
    n: u64,
    p: f64,
    p_min: f64,
    s_min: f64,
}

impl Default for Ddm {
    fn default() -> Self {
        Self {
            n: 0,
            p: 0.0,
            p_min: f64::INFINITY,
            s_min: f64::INFINITY,
        }
    }
}

impl Ddm {
    /// Fresh DDM detector.
    pub fn new() -> Self {
        Self::default()
    }

    /// Update with a 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n += 1;
        self.p += (e - self.p) / self.n as f64;
        let s = (self.p * (1.0 - self.p) / self.n as f64).max(0.0).sqrt();
        if self.p + s < self.p_min + self.s_min {
            self.p_min = self.p;
            self.s_min = s;
        }
        let stat = self.p + s;
        let decision = if self.n >= 30 && stat > self.p_min + 3.0 * self.s_min {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("DDM drift: p+s={stat:.4} > p_min+3s_min"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.p = 0.0;
            self.p_min = f64::INFINITY;
            self.s_min = f64::INFINITY;
            DriftDecision::Drift { statistic: stat }
        } else if self.n >= 30 && stat > self.p_min + 2.0 * self.s_min {
            DriftDecision::Warning { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

/// Two-sided Page–Hinkley test.
#[derive(Clone, Debug)]
pub struct PageHinkley {
    /// Magnitude of change to detect.
    pub delta: f64,
    /// Threshold `λ`.
    pub lambda: f64,
    mean: f64,
    n: u64,
    cum: f64,
    min_cum: f64,
    max_cum: f64,
}

impl Default for PageHinkley {
    fn default() -> Self {
        Self {
            delta: 0.005,
            lambda: 50.0,
            mean: 0.0,
            n: 0,
            cum: 0.0,
            min_cum: 0.0,
            max_cum: 0.0,
        }
    }
}

impl PageHinkley {
    /// Detector with change magnitude `delta` and threshold `lambda`.
    pub fn new(delta: f64, lambda: f64) -> Self {
        Self {
            delta,
            lambda,
            ..Self::default()
        }
    }

    /// Update with a scalar observation.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if !x.is_finite() {
            return ctx.finish(DriftDecision::Stable);
        }
        self.n += 1;
        self.mean += (x - self.mean) / self.n as f64;
        self.cum += x - self.mean - self.delta;
        if self.cum < self.min_cum {
            self.min_cum = self.cum;
        }
        if self.cum > self.max_cum {
            self.max_cum = self.cum;
        }
        let up = self.cum - self.min_cum;
        let down = self.max_cum - self.cum;
        let stat = up.max(down);
        let decision = if stat > self.lambda {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("Page–Hinkley PH={stat:.4} > λ={}", self.lambda))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.mean = 0.0;
            self.cum = 0.0;
            self.min_cum = 0.0;
            self.max_cum = 0.0;
            DriftDecision::Drift { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

/// A handful of Hoeffding trees with a per-tree ADWIN detector.
#[derive(Clone, Debug)]
pub struct AdaptiveRandomForest {
    /// Number of trees.
    pub n_estimators: usize,
    trees: Vec<HoeffdingTree>,
    detectors: Vec<Adwin>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for AdaptiveRandomForest {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            trees: Vec::new(),
            detectors: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(7),
        }
    }
}

impl AdaptiveRandomForest {
    /// Forest with `n_estimators` trees.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.initialized {
            return;
        }
        self.trees = (0..self.n_estimators)
            .map(|_| {
                let mut t = HoeffdingTree::new();
                t.min_samples = 15;
                t.n_features = p;
                t.root = HtNode::Leaf(HtLeaf::new(0, p));
                t.initialized = true;
                t
            })
            .collect();
        self.detectors = (0..self.n_estimators).map(|_| Adwin::new(0.002)).collect();
        self.initialized = true;
    }
}

impl PartialFit for AdaptiveRandomForest {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if self.initialized && self.trees[0].n_features != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        self.ensure(x.ncols());
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut resets = 0u64;
        let mut splits = 0u64;
        for k in 0..self.trees.len() {
            // Poisson(1) bootstrap: include each row with P(count≥1) ≈ 0.63.
            let mut xb = x.clone();
            let mut yb = y.clone();
            let mut keep = 0usize;
            for i in 0..x.nrows() {
                if self.rng.uniform() < 0.632 || x.nrows() == 1 {
                    if keep != i {
                        for j in 0..x.ncols() {
                            xb.set(keep, j, x.get(i, j));
                        }
                        yb[keep] = y[i];
                    }
                    keep += 1;
                }
            }
            if keep == 0 {
                keep = 1;
            }
            let xs = Matrix::from_fn(keep, x.ncols(), |i, j| xb.get(i, j));
            let ys = Vector::from_iter((0..keep).map(|i| yb[i]));
            let q = self.trees[k].partial_fit(&xs, Some(&ys), &session.child("tree"))?;
            if q.value.quality.information_gain.unwrap_or(0.0) > 0.5 {
                splits += 1;
            }
            for i in 0..x.nrows() {
                let pred = self.trees[k].predict_one(x, i);
                let err = if (pred - as01(y[i])).abs() > 0.5 {
                    1.0
                } else {
                    0.0
                };
                let d = self.detectors[k].update(err, &session.child("adwin"))?;
                if matches!(d.value, DriftDecision::Drift { .. }) {
                    self.trees[k].reset();
                    resets += 1;
                    ctx.push(
                        Issue::builder(IssueCode::ConceptDriftDetected)
                            .message(format!("ARF tree {k} reset after ADWIN drift"))
                            .build(),
                    );
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(resets as f64 + 0.1 * splits as f64);
        q.information_gain = Some(splits as f64 + resets as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "ARF: {} trees, {splits} splits, {resets} ADWIN resets on {} rows",
            self.trees.len(),
            x.nrows()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("adaptive forest still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{splits} leaf splits, {resets} tree resets"),
                "bootstrap Hoeffding updates + per-tree ADWIN",
                "pre-batch forest",
                "post-batch forest",
            ),
        )
    }
}

impl Predict for AdaptiveRandomForest {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut votes = 0.0;
            for t in &self.trees {
                votes += t.predict_one(x, i);
            }
            if votes >= self.trees.len() as f64 * 0.5 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(y)
    }
}

/// Adaptive random forest for regression (river `ARFRegressor` lite).
///
/// Each tree is online RLS with a Poisson bootstrap and a per-tree ADWIN
/// detector on absolute residual. Nested `partial_fit` of the leaves adds
/// extra incremental events (allowed).
#[derive(Clone, Debug)]
pub struct AdaptiveRandomForestRegressor {
    /// Number of RLS leaves.
    pub n_estimators: usize,
    trees: Vec<LinearRegression>,
    detectors: Vec<Adwin>,
    n_features: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for AdaptiveRandomForestRegressor {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            trees: Vec::new(),
            detectors: Vec::new(),
            n_features: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(11),
        }
    }
}

impl AdaptiveRandomForestRegressor {
    /// Forest with `n_estimators` RLS leaves.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self) {
        if self.initialized {
            return;
        }
        self.trees = (0..self.n_estimators)
            .map(|_| LinearRegression::new(1.0))
            .collect();
        self.detectors = (0..self.n_estimators).map(|_| Adwin::new(0.002)).collect();
        self.initialized = true;
    }
}

impl PartialFit for AdaptiveRandomForestRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if self.initialized && self.n_features != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        self.ensure();
        self.n_features = x.ncols();
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut resets = 0u64;
        let mut delta: f64 = 0.0;
        for k in 0..self.trees.len() {
            let mut xb = x.clone();
            let mut yb = y.clone();
            let mut keep = 0usize;
            for i in 0..x.nrows() {
                if self.rng.uniform() < 0.632 || x.nrows() == 1 {
                    if keep != i {
                        for j in 0..x.ncols() {
                            xb.set(keep, j, x.get(i, j));
                        }
                        yb[keep] = y[i];
                    }
                    keep += 1;
                }
            }
            if keep == 0 {
                keep = 1;
            }
            let xs = Matrix::from_fn(keep, x.ncols(), |i, j| xb.get(i, j));
            let ys = Vector::from_iter((0..keep).map(|i| yb[i]));
            let before = self.trees[k].coef();
            let q = self.trees[k].partial_fit(&xs, Some(&ys), &session.child("rls"))?;
            delta += q.value.quality.parameter_delta_norm.unwrap_or(0.0);
            let after = self.trees[k].coef();
            if before.len() == after.len() {
                delta += after.sub(&before).norm();
            }
            for i in 0..x.nrows() {
                let pred = self.trees[k].predict_row(x, i);
                let err = (pred - y[i]).abs();
                let d = self.detectors[k].update(err, &session.child("adwin"))?;
                if matches!(d.value, DriftDecision::Drift { .. }) {
                    self.trees[k] = LinearRegression::new(1.0);
                    resets += 1;
                    ctx.push(
                        Issue::builder(IssueCode::ConceptDriftDetected)
                            .message(format!("ARF-regressor leaf {k} reset after ADWIN drift"))
                            .build(),
                    );
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta + resets as f64);
        q.information_gain = Some(delta + resets as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "ARF-regressor: {} RLS leaves, {resets} ADWIN resets on {} rows",
            self.trees.len(),
            x.nrows()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("adaptive forest regressor still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{resets} leaf resets, Δθ={delta:.6e}"),
                "bootstrap RLS updates + per-leaf ADWIN on |residual|",
                "pre-batch forest",
                "post-batch forest",
            ),
        )
    }
}

impl Predict for AdaptiveRandomForestRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_trees = self.trees.len().max(1) as f64;
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0;
            for t in &self.trees {
                s += t.predict_row(x, i);
            }
            s / n_trees
        }));
        ctx.finish(y)
    }
}

/// Online column-wise standard scaler (Welford).
#[derive(Clone, Debug)]
pub struct OnlineStandardScaler {
    mean: Vector,
    m2: Vector,
    count: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineStandardScaler {
    fn default() -> Self {
        Self {
            mean: Vector::zeros(0),
            m2: Vector::zeros(0),
            count: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineStandardScaler {
    /// Empty scaler.
    pub fn new() -> Self {
        Self::default()
    }

    /// Running means.
    pub fn mean(&self) -> &Vector {
        &self.mean
    }
}

impl PartialFit for OnlineStandardScaler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.mean = Vector::zeros(x.ncols());
            self.m2 = Vector::zeros(x.ncols());
            self.count = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.mean.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.mean.clone();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let n = self.count[j] + 1.0;
                let d = v - self.mean[j];
                self.mean[j] += d / n;
                self.m2[j] += d * (v - self.mean[j]);
                self.count[j] = n;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.mean.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm());
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("online StandardScaler Welford on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running column means and M2",
                "Welford online moments",
                "previous moments",
                "updated moments",
            ),
        )
    }
}

impl Transform for OnlineStandardScaler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.mean.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p {
                return x.get(i, j);
            }
            let std = if self.count[j] >= 2.0 {
                (self.m2[j] / (self.count[j] - 1.0)).max(0.0).sqrt()
            } else {
                1.0
            };
            let s = if std > 0.0 { std } else { 1.0 };
            (x.get(i, j) - self.mean[j]) / s
        });
        ctx.finish(out)
    }
}

/// Mini-batch / streaming k-means.
#[derive(Clone, Debug)]
pub struct StreamKMeans {
    /// Number of centroids.
    pub k: usize,
    centers: Matrix,
    counts: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for StreamKMeans {
    fn default() -> Self {
        Self {
            k: 2,
            centers: Matrix::zeros(0, 0),
            counts: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl StreamKMeans {
    /// `k` streaming centroids.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }

    /// Current centroids (`k × p`).
    pub fn centers(&self) -> &Matrix {
        &self.centers
    }
}

impl PartialFit for StreamKMeans {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        if x.nrows() == 0 || x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if !self.initialized {
            let k = self.k.min(x.nrows());
            self.centers = Matrix::from_fn(k, x.ncols(), |i, j| x.get(i, j));
            self.counts = Vector::zeros(k);
            self.initialized = true;
        } else if self.centers.ncols() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.centers.clone();
        let mut empty = vec![true; self.centers.nrows()];
        for i in 0..x.nrows() {
            let mut best = 0usize;
            let mut bd = f64::INFINITY;
            for c in 0..self.centers.nrows() {
                let mut d = 0.0;
                for j in 0..x.ncols() {
                    let t = x.get(i, j) - self.centers.get(c, j);
                    d += t * t;
                }
                if d < bd {
                    bd = d;
                    best = c;
                }
            }
            empty[best] = false;
            self.counts[best] += 1.0;
            let eta = 1.0 / self.counts[best];
            for j in 0..x.ncols() {
                let v = self.centers.get(best, j) + eta * (x.get(i, j) - self.centers.get(best, j));
                self.centers.set(best, j, v);
            }
        }
        for (c, is_empty) in empty.iter().enumerate() {
            if *is_empty {
                ctx.push(
                    Issue::builder(IssueCode::EmptyCluster)
                        .message(format!("stream k-means centroid {c} received no points"))
                        .build(),
                );
            }
        }
        if empty.iter().filter(|e| **e).count() + 1 >= self.centers.nrows() {
            ctx.push(
                Issue::builder(IssueCode::DegenerateClusters)
                    .message("stream k-means collapsed toward a single centroid")
                    .build(),
            );
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut dn = 0.0;
        for c in 0..self.centers.nrows() {
            for j in 0..self.centers.ncols() {
                let d = self.centers.get(c, j) - before.get(c, j);
                dn += d * d;
            }
        }
        dn = dn.sqrt();
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dn);
        q.information_gain = Some(dn);
        q.still_identified = self.n_seen >= self.k as u64;
        q.warmup = self.n_seen < self.k as u64;
        q.explanation = format!("stream k-means assignment + centroid step, ||Δμ||={dn:.4e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "centroid coordinates",
                "online mean update of the assigned cluster",
                "previous centroids",
                "updated centroids",
            ),
        )
    }
}

impl Predict for StreamKMeans {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bd = f64::INFINITY;
            for c in 0..self.centers.nrows() {
                let mut d = 0.0;
                for j in 0..x.ncols().min(self.centers.ncols()) {
                    let t = x.get(i, j) - self.centers.get(c, j);
                    d += t * t;
                }
                if d < bd {
                    bd = d;
                    best = c;
                }
            }
            best as f64
        }));
        ctx.finish(y)
    }
}

/// Half-space trees for online anomaly scoring (Tan, Ting, Liu).
#[derive(Clone, Debug)]
pub struct HalfSpaceTrees {
    /// Number of trees.
    pub n_trees: usize,
    /// Maximum depth.
    pub max_depth: usize,
    trees: Vec<HsNode>,
    mins: Vector,
    maxs: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

#[derive(Clone, Debug)]
struct HsNode {
    feature: usize,
    threshold: f64,
    mass: u64,
    depth: usize,
    left: Option<Box<HsNode>>,
    right: Option<Box<HsNode>>,
}

impl Default for HalfSpaceTrees {
    fn default() -> Self {
        Self {
            n_trees: 10,
            max_depth: 8,
            trees: Vec::new(),
            mins: Vector::zeros(0),
            maxs: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(11),
        }
    }
}

impl HalfSpaceTrees {
    /// Ensemble of half-space trees.
    pub fn new(n_trees: usize, max_depth: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            max_depth: max_depth.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, depth: usize, p: usize) -> HsNode {
        if depth >= self.max_depth || p == 0 {
            return HsNode {
                feature: 0,
                threshold: 0.0,
                mass: 0,
                depth,
                left: None,
                right: None,
            };
        }
        let feature = self.rng.below(p);
        let lo = self.mins[feature];
        let hi = self.maxs[feature];
        let threshold = if hi > lo {
            self.rng.uniform_range(lo, hi)
        } else {
            lo
        };
        HsNode {
            feature,
            threshold,
            mass: 0,
            depth,
            left: Some(Box::new(self.grow(depth + 1, p))),
            right: Some(Box::new(self.grow(depth + 1, p))),
        }
    }

    fn score_one(&self, x: &Matrix, i: usize) -> f64 {
        if self.n_seen == 0 || self.trees.is_empty() {
            return 0.0;
        }
        let mut s = 0.0;
        for t in &self.trees {
            s += walk_score(t, x, i, self.n_seen);
        }
        s / self.trees.len() as f64
    }
}

fn walk_insert(node: &mut HsNode, x: &Matrix, i: usize) {
    node.mass += 1;
    if node.left.is_none() || node.right.is_none() {
        return;
    }
    let v = if node.feature < x.ncols() {
        x.get(i, node.feature)
    } else {
        0.0
    };
    if v <= node.threshold {
        if let Some(l) = node.left.as_mut() {
            walk_insert(l, x, i);
        }
    } else if let Some(r) = node.right.as_mut() {
        walk_insert(r, x, i);
    }
}

fn walk_score(node: &HsNode, x: &Matrix, i: usize, n_seen: u64) -> f64 {
    let v = if node.feature < x.ncols() {
        x.get(i, node.feature)
    } else {
        0.0
    };
    let child = if v <= node.threshold {
        node.left.as_deref()
    } else {
        node.right.as_deref()
    };
    match child {
        Some(c) if c.mass > 0 || c.left.is_some() => walk_score(c, x, i, n_seen),
        _ => {
            let mass = node.mass.max(1) as f64;
            // High score = rare (low mass relative to n, shallow isolation).
            1.0 - (mass / n_seen.max(1) as f64) * (1.0 + node.depth as f64).recip()
        }
    }
}

impl PartialFit for HalfSpaceTrees {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if !self.initialized {
            self.mins = Vector::from_iter((0..x.ncols()).map(|j| {
                (0..x.nrows())
                    .map(|i| x.get(i, j))
                    .fold(f64::INFINITY, f64::min)
            }));
            self.maxs = Vector::from_iter((0..x.ncols()).map(|j| {
                (0..x.nrows())
                    .map(|i| x.get(i, j))
                    .fold(f64::NEG_INFINITY, f64::max)
            }));
            for j in 0..x.ncols() {
                if !self.mins[j].is_finite()
                    || !self.maxs[j].is_finite()
                    || self.mins[j] == self.maxs[j]
                {
                    self.mins[j] = 0.0;
                    self.maxs[j] = 1.0;
                } else {
                    let pad = 0.1 * (self.maxs[j] - self.mins[j]);
                    self.mins[j] -= pad;
                    self.maxs[j] += pad;
                }
            }
            let p = x.ncols();
            self.trees = (0..self.n_trees).map(|_| self.grow(0, p)).collect();
            self.initialized = true;
        } else if self.mins.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        for i in 0..x.nrows() {
            for t in &mut self.trees {
                walk_insert(t, x, i);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(x.nrows() as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 4;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "half-space trees: mass incremented along {} paths × {} rows",
            self.trees.len(),
            x.nrows()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("half-space trees have seen fewer than 4 points")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "per-node mass counts",
                "path increment in a random half-space partition",
                "previous masses",
                "updated masses",
            ),
        )
    }
}

impl Predict for HalfSpaceTrees {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.score_one(x, i)),
        ))
    }
}

/// Rolling 0/1 accuracy. `x` is the prediction, `y` the label.
#[derive(Clone, Debug, Default)]
pub struct OnlineAccuracy {
    correct: u64,
    n: u64,
    updates: u64,
}

impl OnlineAccuracy {
    /// Empty metric.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current accuracy, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.correct as f64 / self.n as f64
        }
    }
}

impl PartialFit for OnlineAccuracy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "accuracy",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                self.n += 1;
                if (pred.round() - truth.round()).abs() < 1e-9 {
                    self.correct += 1;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Rolling mean squared error. `x` is the prediction, `y` the target.
#[derive(Clone, Debug, Default)]
pub struct OnlineMse {
    sse: f64,
    n: u64,
    updates: u64,
}

impl OnlineMse {
    /// Empty metric.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MSE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.sse / self.n as f64
        }
    }
}

impl PartialFit for OnlineMse {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(session, "mse", x, y, self.updates, self.n, |pred, truth| {
            let e = pred - truth;
            self.sse += e * e;
            self.n += 1;
            self.updates += 1;
            self.score()
        })
    }
}

/// Rolling coefficient of determination.
#[derive(Clone, Debug, Default)]
pub struct OnlineR2 {
    sse: f64,
    sy: f64,
    sy2: f64,
    n: u64,
    updates: u64,
}

impl OnlineR2 {
    /// Empty metric.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current R², or NaN when unidentified.
    pub fn score(&self) -> f64 {
        if self.n < 2 {
            return f64::NAN;
        }
        let mean = self.sy / self.n as f64;
        let sst = self.sy2 - self.n as f64 * mean * mean;
        if sst.abs() <= 1e-15 {
            return f64::NAN;
        }
        1.0 - self.sse / sst
    }
}

impl PartialFit for OnlineR2 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(session, "r2", x, y, self.updates, self.n, |pred, truth| {
            let e = pred - truth;
            self.sse += e * e;
            self.sy += truth;
            self.sy2 += truth * truth;
            self.n += 1;
            self.updates += 1;
            self.score()
        })
    }
}

/// Additive Holt–Winters with online level / trend / seasonal updates.
#[derive(Clone, Debug)]
pub struct HoltWintersOnline {
    /// Level smoothing.
    pub alpha: f64,
    /// Trend smoothing.
    pub beta: f64,
    /// Seasonal smoothing.
    pub gamma: f64,
    /// Seasonal period.
    pub period: usize,
    level: f64,
    trend: f64,
    season: Vec<f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HoltWintersOnline {
    fn default() -> Self {
        Self {
            alpha: 0.3,
            beta: 0.1,
            gamma: 0.1,
            period: 4,
            level: 0.0,
            trend: 0.0,
            season: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HoltWintersOnline {
    /// Additive Holt–Winters with the given period.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(1),
            season: vec![0.0; period.max(1)],
            ..Self::default()
        }
    }

    /// Current level.
    pub fn level(&self) -> f64 {
        self.level
    }

    /// Current trend.
    pub fn trend(&self) -> f64 {
        self.trend
    }

    fn step(&mut self, y: f64) {
        let p = self.period.max(1);
        if self.season.len() != p {
            self.season = vec![0.0; p];
        }
        let idx = (self.n_seen as usize) % p;
        let s = self.season[idx];
        let last_level = self.level;
        if !self.initialized {
            self.level = y;
            self.initialized = true;
            self.season[idx] = 0.0;
        } else {
            let new_level = self.alpha * (y - s) + (1.0 - self.alpha) * (self.level + self.trend);
            let new_trend = self.beta * (new_level - last_level) + (1.0 - self.beta) * self.trend;
            let new_season = self.gamma * (y - new_level) + (1.0 - self.gamma) * s;
            self.level = new_level;
            self.trend = new_trend;
            self.season[idx] = new_season;
        }
        self.n_seen += 1;
    }
}

impl PartialFit for HoltWintersOnline {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let series: Vec<f64> = if let Some(y) = y {
            y.as_slice().to_vec()
        } else if x.ncols() > 0 {
            (0..x.nrows()).map(|i| x.get(i, 0)).collect()
        } else {
            Vec::new()
        };
        if series.is_empty() {
            if !self.initialized {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, 0, self.n_seen, "empty series"),
            );
        }
        let before_l = self.level;
        let before_t = self.trend;
        let mut sse = 0.0;
        for &yi in &series {
            let idx = (self.n_seen as usize) % self.period.max(1);
            let seas = if idx < self.season.len() {
                self.season[idx]
            } else {
                0.0
            };
            let pred = self.level + self.trend + seas;
            if self.initialized {
                let e = yi - pred;
                sse += e * e;
            }
            self.step(yi);
        }
        self.updates += 1;
        let warmup = self.n_seen < self.period as u64;
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message(format!(
                        "Holt–Winters has seen {} < period={}",
                        self.n_seen, self.period
                    ))
                    .build(),
            );
        }
        if self.n_seen < 2 * self.period as u64 && self.period > 1 {
            ctx.push(
                Issue::builder(IssueCode::InsufficientSeasonalCycles)
                    .message(
                        "fewer than two full seasonal cycles; seasonal terms are weakly identified",
                    )
                    .build(),
            );
        }
        let dn = ((self.level - before_l).powi(2) + (self.trend - before_t).powi(2)).sqrt();
        let mut q = IncrementalQuality::new(self.updates - 1, series.len(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dn);
        q.information_gain = Some(sse);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "Holt–Winters additive update: Δlevel={:.4e} Δtrend={:.4e} sse={sse:.4e}",
            self.level - before_l,
            self.trend - before_t
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "level, trend, and seasonal factors",
                "additive Holt–Winters smoothing equations",
                format!("L={before_l:.4} T={before_t:.4}"),
                format!("L={:.4} T={:.4}", self.level, self.trend),
            ),
        )
    }
}

impl Predict for HoltWintersOnline {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let p = self.period.max(1);
        let y = Vector::from_iter((0..x.nrows()).map(|h| {
            let idx = (self.n_seen as usize + h) % p;
            let s = if idx < self.season.len() {
                self.season[idx]
            } else {
                0.0
            };
            self.level + (h as f64 + 1.0) * self.trend + s
        }));
        ctx.finish(y)
    }
}

fn inspect_online_xy(ctx: &mut FitCtx, x: &Matrix, y: Option<&Vector>) {
    let (n, p) = x.shape();
    ctx.report.set_sample_shape(n, p);
    if n == 0 || p == 0 {
        ctx.push(
            Issue::builder(IssueCode::EmptyMatrix)
                .message(format!("online design is {n}×{p}"))
                .build(),
        );
        return;
    }
    if let Some(y) = y {
        if y.len() != n {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message(format!("y.len()={} but X has {n} rows", y.len()))
                    .build(),
            );
        }
    }
    // Do not call inspect_xy: a 1-row batch is a constant target and would abort.
}

fn sigmoid(z: f64) -> f64 {
    if z >= 0.0 {
        let e = (-z).exp();
        1.0 / (1.0 + e)
    } else {
        let e = z.exp();
        e / (1.0 + e)
    }
}

fn as01(y: f64) -> f64 {
    if y >= 0.5 {
        1.0
    } else {
        0.0
    }
}

fn as_pm(y: f64) -> f64 {
    if y >= 0.5 {
        1.0
    } else {
        -1.0
    }
}

fn logloss_of(x: &Matrix, y: &Vector, coef: &Vector, intercept: f64) -> f64 {
    if x.nrows() == 0 || coef.len() != x.ncols() {
        return f64::NAN;
    }
    let mut s = 0.0;
    for i in 0..x.nrows() {
        let mut eta = intercept;
        for j in 0..x.ncols() {
            eta += coef[j] * x.get(i, j);
        }
        let mu = sigmoid(eta).clamp(1e-12, 1.0 - 1e-12);
        let yi = as01(y[i]);
        s += -(yi * mu.ln() + (1.0 - yi) * (1.0 - mu).ln());
    }
    s / x.nrows() as f64
}

fn top_moved(delta: &Vector, k: usize) -> Vec<(String, f64)> {
    let mut idx: Vec<usize> = (0..delta.len()).collect();
    idx.sort_by(|a, b| {
        delta[*b]
            .abs()
            .partial_cmp(&delta[*a].abs())
            .unwrap_or(std::cmp::Ordering::Equal)
    });
    idx.into_iter()
        .take(k)
        .map(|j| (format!("theta[{j}]"), delta[j]))
        .collect()
}

/// Adaptive model rules (river `AMRules`): a default RLS leaf that may split
/// once when a Hoeffding-bound variance reduction is identified.
///
/// Each `partial_fit` names the leaf that moved, the candidate split (if any),
/// and whether the post-update state is still a single unidentified intercept.
#[derive(Clone, Debug)]
pub struct AdaptiveModelRules {
    /// Forgetting factor of each leaf RLS.
    pub forgetting: f64,
    /// Hoeffding δ.
    pub delta: f64,
    p: usize,
    split: Option<(usize, f64)>,
    default: RuleLeaf,
    left: Option<RuleLeaf>,
    right: Option<RuleLeaf>,
    feat_mean: Vector,
    feat_n: u64,
    parent_sse: f64,
    parent_n: u64,
    left_sse: Vector,
    left_n: Vector,
    right_sse: Vector,
    right_n: Vector,
    updates: u64,
    n_seen: u64,
}

#[derive(Clone, Debug)]
struct RuleLeaf {
    theta: Vector,
    p_mat: Option<faer::Mat<f64>>,
    n: u64,
}

impl RuleLeaf {
    fn new(p: usize, p0: f64) -> Self {
        Self {
            theta: Vector::zeros(p),
            p_mat: Some(faer::Mat::<f64>::from_fn(p, p, |i, j| {
                if i == j {
                    p0
                } else {
                    0.0
                }
            })),
            n: 0,
        }
    }

    fn predict(&self, z: &Vector) -> f64 {
        if self.theta.len() != z.len() {
            return 0.0;
        }
        self.theta.dot(z)
    }

    fn update(&mut self, z: &Vector, y: f64, lam: f64) -> f64 {
        let p = z.len();
        if self.theta.len() != p {
            *self = Self::new(p, 1_000.0);
        }
        let pred = self.predict(z);
        let Some(pm) = self.p_mat.as_mut() else {
            return 0.0;
        };
        let mut px = Vector::zeros(p);
        for i in 0..p {
            let mut s = 0.0;
            for j in 0..p {
                s += pm[(i, j)] * z[j];
            }
            px[i] = s;
        }
        let denom = lam + z.dot(&px);
        if denom.abs() <= 1e-18 {
            return 0.0;
        }
        let err = y - pred;
        let mut delta_n = 0.0;
        for i in 0..p {
            let d = px[i] * err / denom;
            self.theta[i] += d;
            delta_n += d * d;
        }
        for i in 0..p {
            for j in 0..p {
                pm[(i, j)] = (pm[(i, j)] - px[i] * px[j] / denom) / lam;
            }
        }
        self.n += 1;
        delta_n.sqrt()
    }
}

impl Default for AdaptiveModelRules {
    fn default() -> Self {
        Self {
            forgetting: 1.0,
            delta: 0.01,
            p: 0,
            split: None,
            default: RuleLeaf::new(0, 1_000.0),
            left: None,
            right: None,
            feat_mean: Vector::zeros(0),
            feat_n: 0,
            parent_sse: 0.0,
            parent_n: 0,
            left_sse: Vector::zeros(0),
            left_n: Vector::zeros(0),
            right_sse: Vector::zeros(0),
            right_n: Vector::zeros(0),
            updates: 0,
            n_seen: 0,
        }
    }
}

impl AdaptiveModelRules {
    /// AMRules with forgetting `λ`.
    pub fn new(forgetting: f64) -> Self {
        Self {
            forgetting,
            ..Self::default()
        }
    }

    fn ensure(&mut self, p_x: usize) {
        let p = p_x + 1;
        if self.p == p {
            return;
        }
        self.p = p;
        self.default = RuleLeaf::new(p, 1_000.0);
        self.feat_mean = Vector::zeros(p_x);
        self.left_sse = Vector::zeros(p_x);
        self.left_n = Vector::zeros(p_x);
        self.right_sse = Vector::zeros(p_x);
        self.right_n = Vector::zeros(p_x);
    }

    fn row(x: &Matrix, i: usize) -> Vector {
        let mut z = Vector::zeros(x.ncols() + 1);
        z[0] = 1.0;
        for j in 0..x.ncols() {
            z[j + 1] = x.get(i, j);
        }
        z
    }

    fn route(&mut self, x: &Matrix, i: usize) -> &mut RuleLeaf {
        match self.split {
            Some((j, t)) if x.get(i, j) <= t => {
                if self.left.is_none() {
                    self.left = Some(RuleLeaf::new(self.p, 1_000.0));
                }
                self.left.as_mut().unwrap()
            }
            Some((_j, _)) => {
                if self.right.is_none() {
                    self.right = Some(RuleLeaf::new(self.p, 1_000.0));
                }
                self.right.as_mut().unwrap()
            }
            None => &mut self.default,
        }
    }

    fn maybe_split(&mut self, ctx: &mut FitCtx) -> Option<(usize, f64, f64)> {
        if self.split.is_some() || self.feat_mean.len() == 0 || self.parent_n < 20 {
            return None;
        }
        let r2 = 1.0; // residual range bound used by Hoeffding
        let eps =
            (r2 * r2 * (1.0 / self.delta.max(1e-12)).ln() / (2.0 * self.parent_n as f64)).sqrt();
        let parent_var = self.parent_sse / self.parent_n as f64;
        let mut best = None;
        let mut best_gain = 0.0;
        let mut second = 0.0;
        for j in 0..self.feat_mean.len() {
            let nl = self.left_n[j];
            let nr = self.right_n[j];
            if nl < 5.0 || nr < 5.0 {
                continue;
            }
            let vl = self.left_sse[j] / nl;
            let vr = self.right_sse[j] / nr;
            let gain = parent_var - (nl * vl + nr * vr) / (nl + nr);
            if gain > best_gain {
                second = best_gain;
                best_gain = gain;
                best = Some((j, self.feat_mean[j], gain));
            } else if gain > second {
                second = gain;
            }
        }
        if let Some((j, t, gain)) = best {
            if gain - second > eps && gain > 0.0 {
                self.split = Some((j, t));
                self.left = Some(RuleLeaf::new(self.p, 1_000.0));
                self.right = Some(RuleLeaf::new(self.p, 1_000.0));
                ctx.push(
                    Issue::builder(IssueCode::ConceptDriftDetected)
                        .severity(Severity::Advisory)
                        .message(format!(
                            "AMRules split on feature {j} at {t:.6e} (gain={gain:.6e}, ε={eps:.6e})"
                        ))
                        .build(),
                );
                return Some((j, t, gain));
            }
        }
        None
    }
}

impl PartialFit for AdaptiveModelRules {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            if self.n_seen == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, 0, self.n_seen, "AMRules needs a target"),
            );
        };
        if x.nrows() != y.len() {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("AMRules x rows ≠ y length")
                    .build(),
            );
        }
        self.ensure(x.ncols());
        let mut dsum = 0.0;
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        let mut leaf_hits = [0u64; 3];
        for i in 0..y.len() {
            let z = Self::row(x, i);
            let pred_b = match self.split {
                Some((j, t)) if x.get(i, j) <= t => {
                    self.left.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0)
                }
                Some(_) => self.right.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0),
                None => self.default.predict(&z),
            };
            loss_before += (pred_b - y[i]) * (pred_b - y[i]);
            let which = match self.split {
                Some((j, t)) if x.get(i, j) <= t => 1,
                Some(_) => 2,
                None => 0,
            };
            leaf_hits[which] += 1;
            let lam = self.forgetting;
            let dn = self.route(x, i).update(&z, y[i], lam);
            dsum += dn;
            let pred_a = match self.split {
                Some((j, t)) if x.get(i, j) <= t => {
                    self.left.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0)
                }
                Some(_) => self.right.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0),
                None => self.default.predict(&z),
            };
            let e = pred_a - y[i];
            loss_after += e * e;
            if self.split.is_none() {
                self.parent_sse += e * e;
                self.parent_n += 1;
                self.feat_n += 1;
                for j in 0..x.ncols() {
                    let v = x.get(i, j);
                    self.feat_mean[j] += (v - self.feat_mean[j]) / self.feat_n as f64;
                    if v <= self.feat_mean[j] {
                        self.left_sse[j] += e * e;
                        self.left_n[j] += 1.0;
                    } else {
                        self.right_sse[j] += e * e;
                        self.right_n[j] += 1.0;
                    }
                }
            }
        }
        self.n_seen += y.len() as u64;
        self.updates += 1;
        let split = self.maybe_split(&mut ctx);
        let identified = self.n_seen as usize > self.p.max(1);
        let warmup = self.n_seen < 10;
        let what = if let Some((j, t, g)) = split {
            format!("split on x[{j}]≤{t:.4e} gain={g:.4e}; leaf hits {leaf_hits:?}")
        } else if self.split.is_some() {
            format!("updated split leaves; hits {leaf_hits:?}")
        } else {
            format!("updated default RLS; hits {leaf_hits:?}")
        };
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            y.len(),
            self.n_seen,
            &Vector::from_slice(&[dsum]),
            loss_before,
            loss_after,
            identified,
            warmup,
            &what,
            "RLS leaf update; a Hoeffding test may grow a stump",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AdaptiveModelRules {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.p == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let z = Self::row(x, i);
            match self.split {
                Some((j, t)) if x.get(i, j) <= t => {
                    self.left.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0)
                }
                Some(_) => self.right.as_ref().map(|l| l.predict(&z)).unwrap_or(0.0),
                None => self.default.predict(&z),
            }
        }));
        ctx.finish(out)
    }
}

fn reject_explain(update: u64, batch: usize, n_seen: u64, why: &str) -> IncrementalExplain {
    IncrementalExplain::from_quality(
        IncrementalQuality::new(update, batch, n_seen),
        "nothing",
        why,
        "invalid",
        "invalid",
    )
}

fn flag_info(ctx: &mut FitCtx, q: &IncrementalQuality) {
    if q.is_uninformative(ctx.policy.uninformative_info_eps) {
        ctx.push(
            Issue::builder(IssueCode::UpdateWithZeroInformation)
                .incremental(q.clone())
                .message("this online update added no usable information")
                .build(),
        );
    }
}

fn pack_linear_explain(
    ctx: &mut FitCtx,
    updates: u64,
    batch: usize,
    n_seen: u64,
    delta: &Vector,
    loss_before: f64,
    loss_after: f64,
    identified: bool,
    warmup: bool,
    what: &str,
    why: &str,
) -> IncrementalExplain {
    let mut q = IncrementalQuality::new(updates.saturating_sub(1), batch, n_seen);
    q.effective_sample_size = n_seen as f64;
    q.parameter_delta_norm = Some(delta.norm());
    q.parameter_delta_max = Some(delta.max_abs());
    q.loss_before = Some(loss_before);
    q.loss_after = Some(loss_after);
    q.information_gain = Some((loss_before - loss_after).abs().max(delta.norm()));
    q.still_identified = identified;
    q.warmup = warmup;
    q.explanation = format!(
        "{what}: ||Δθ||={:.4e} loss {loss_before:.6e} → {loss_after:.6e}",
        delta.norm()
    );
    q.top_moved_parameters = top_moved(delta, 3);
    if q.is_uninformative(ctx.policy.uninformative_info_eps) {
        ctx.push(
            Issue::builder(IssueCode::UpdateWithZeroInformation)
                .incremental(q.clone())
                .message("batch residual / parameters did not move")
                .build(),
        );
    }
    if warmup {
        ctx.push(
            Issue::builder(IssueCode::WarmupIncomplete)
                .incremental(q.clone())
                .message("online linear model is still warming up")
                .build(),
        );
    }
    IncrementalExplain::from_quality(
        q,
        what,
        why,
        format!("loss={loss_before:.6e}"),
        format!("loss={loss_after:.6e}"),
    )
}

fn finish_explain(ctx: FitCtx, expl: IncrementalExplain) -> Result<Qualified<IncrementalExplain>> {
    ctx.session.record_incremental(expl.clone());
    ctx.finish(expl)
}

fn metric_partial<F>(
    session: &Session,
    name: &str,
    x: &Matrix,
    y: Option<&Vector>,
    updates: u64,
    n_seen: u64,
    mut on_pair: F,
) -> Result<Qualified<IncrementalExplain>>
where
    F: FnMut(f64, f64) -> f64,
{
    let mut ctx = FitCtx::with_session(session.child("partial_fit"));
    let Some(y) = y else {
        ctx.push(Issue::builder(IssueCode::MissingTarget).build());
        return finish_explain(ctx, reject_explain(updates, x.nrows(), n_seen, "missing y"));
    };
    if y.len() != x.nrows() {
        ctx.push(Issue::builder(IssueCode::DimensionMismatch).build());
    }
    let before = n_seen;
    let mut last = f64::NAN;
    for i in 0..x.nrows().min(y.len()) {
        last = on_pair(x.get(i, 0), y[i]);
    }
    let after_n = before + x.nrows().min(y.len()) as u64;
    let mut q = IncrementalQuality::new(updates, x.nrows(), after_n);
    q.effective_sample_size = after_n as f64;
    q.parameter_delta_norm = Some(0.0);
    q.information_gain = Some(x.nrows() as f64);
    q.still_identified = after_n > 1;
    q.warmup = after_n < 2;
    q.explanation = format!("online {name} ← {} pairs, score={last:.6}", x.nrows());
    q.loss_after = Some(last);
    if q.warmup {
        ctx.push(
            Issue::builder(IssueCode::WarmupIncomplete)
                .incremental(q.clone())
                .message(format!("online {name} has fewer than 2 observations"))
                .build(),
        );
    }
    finish_explain(
        ctx,
        IncrementalExplain::from_quality(
            q,
            format!("running {name}"),
            "sufficient-stat update of the rolling metric",
            format!("n={before}"),
            format!("n={after_n} score={last:.6}"),
        ),
    )
}

/// Kolmogorov–Smirnov windowing (river `KSWIN`).
///
/// The last `stat_size` observations are compared to the remainder of a
/// sliding window. A KS statistic above the Hoeffding-style threshold is
/// [`IssueCode::ConceptDriftDetected`].
#[derive(Clone, Debug)]
pub struct Kswin {
    /// Window length.
    pub window_size: usize,
    /// Length of the recent comparison sample.
    pub stat_size: usize,
    /// Type-I level \(\alpha\).
    pub alpha: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Kswin {
    fn default() -> Self {
        Self {
            window_size: 100,
            stat_size: 30,
            alpha: 0.005,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Kswin {
    /// Detector with window `w`.
    pub fn new(window_size: usize) -> Self {
        Self {
            window_size,
            ..Self::default()
        }
    }

    fn ks(a: &[f64], b: &[f64]) -> f64 {
        let mut sa = a.to_vec();
        let mut sb = b.to_vec();
        sa.sort_by(|x, y| x.partial_cmp(y).unwrap_or(std::cmp::Ordering::Equal));
        sb.sort_by(|x, y| x.partial_cmp(y).unwrap_or(std::cmp::Ordering::Equal));
        let mut i = 0usize;
        let mut j = 0usize;
        let mut d: f64 = 0.0;
        while i < sa.len() || j < sb.len() {
            let va = sa.get(i).copied().unwrap_or(f64::INFINITY);
            let vb = sb.get(j).copied().unwrap_or(f64::INFINITY);
            if va <= vb {
                i += 1;
            }
            if vb <= va {
                j += 1;
            }
            let fa = i as f64 / sa.len().max(1) as f64;
            let fb = j as f64 / sb.len().max(1) as f64;
            d = d.max((fa - fb).abs());
        }
        d
    }

    /// Consume one scalar and decide.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if x.is_finite() {
            self.window.push(x);
        }
        if self.window.len() > self.window_size {
            let drop = self.window.len() - self.window_size;
            self.window.drain(0..drop);
        }
        self.n_seen += 1;
        let r = self.stat_size.min(self.window.len() / 2);
        if self.window.len() < self.stat_size.max(16) * 2 {
            return ctx.finish(DriftDecision::Stable);
        }
        let n = self.window.len();
        let recent = &self.window[n - r..];
        let past = &self.window[..n - r];
        let stat = Self::ks(past, recent);
        let crit = ((-0.5 * (self.alpha.max(1e-12) / 2.0).ln())
            * ((past.len() + recent.len()) as f64 / (past.len() * recent.len()) as f64))
            .sqrt();
        if stat > crit {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("KSWIN KS={stat:.4} > {crit:.4}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.window.drain(0..n - r);
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for Kswin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let before = self.window.len();
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("kswin")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(fired);
        q.still_identified = self.window.len() >= self.stat_size;
        q.warmup = self.window.len() < self.stat_size.max(16) * 2;
        q.explanation = format!(
            "KSWIN consumed {} rows; window {before}→{}, drift_cuts={fired}",
            x.nrows(),
            self.window.len()
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sliding KS window",
                "two-sample KS of recent vs remainder",
                format!("window={before}"),
                format!("window={} cuts={fired}", self.window.len()),
            ),
        )
    }
}

/// Hoeffding drift detection (river `HDDM_A`) on a 0/1 error stream.
#[derive(Clone, Debug)]
pub struct Hddm {
    /// Confidence \(\delta\).
    pub delta: f64,
    n: u64,
    mean: f64,
    n_min: u64,
    mean_min: f64,
    updates: u64,
}

impl Default for Hddm {
    fn default() -> Self {
        Self {
            delta: 0.002,
            n: 0,
            mean: 0.0,
            n_min: 0,
            mean_min: f64::INFINITY,
            updates: 0,
        }
    }
}

impl Hddm {
    /// Fresh HDDM_A detector.
    pub fn new() -> Self {
        Self::default()
    }

    fn bound(n: u64, delta: f64) -> f64 {
        if n == 0 {
            return f64::INFINITY;
        }
        (0.5 / n as f64 * (1.0 / delta.max(1e-16)).ln()).sqrt()
    }

    /// Update with a 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n += 1;
        self.mean += (e - self.mean) / self.n as f64;
        let eps = Self::bound(self.n, self.delta);
        let eps_min = Self::bound(self.n_min.max(1), self.delta);
        if self.mean + eps < self.mean_min + eps_min {
            self.mean_min = self.mean;
            self.n_min = self.n;
        }
        let stat = self.mean - self.mean_min;
        let decision = if self.n >= 30 && self.mean - eps > self.mean_min + eps_min {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!(
                        "HDDM_A drift: μ-ε={:.4} > μ_min+ε_min",
                        self.mean - eps
                    ))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.mean = 0.0;
            self.n_min = 0;
            self.mean_min = f64::INFINITY;
            DriftDecision::Drift { statistic: stat }
        } else if self.n >= 30 && self.mean - 0.5 * eps > self.mean_min + 0.5 * eps_min {
            DriftDecision::Warning { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

impl PartialFit for Hddm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let before = self.mean;
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("hddm")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.mean);
        q.still_identified = self.n >= 30;
        q.warmup = self.n < 30;
        q.explanation = format!(
            "HDDM_A μ {before:.4}→{:.4}, n={}, drift_cuts={fired}",
            self.mean, self.n
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "error-rate mean and Hoeffding bound",
                "HDDM_A comparison to the historical minimum",
                format!("μ={before:.4}"),
                format!("μ={:.4} n={}", self.mean, self.n),
            ),
        )
    }
}

/// Approximate large-margin algorithm (Gentile 2001; river `ALMA`).
#[derive(Clone, Debug)]
pub struct Alma {
    /// Margin parameter \(\alpha \in (0,1]\).
    pub alpha: f64,
    /// Aggressiveness \(C\).
    pub c: f64,
    /// Exponent \(p\) on the decaying rate (usually 2).
    pub p: f64,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Alma {
    fn default() -> Self {
        Self {
            alpha: 0.9,
            c: 1.0,
            p: 2.0,
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Alma {
    /// Default ALMA.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current weights (unit-norm after each update).
    pub fn coef(&self) -> &Vector {
        &self.coef
    }
}

impl PartialFit for Alma {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(
                Issue::builder(IssueCode::MissingTarget)
                    .message("ALMA.partial_fit requires y")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message("ALMA feature count changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let mut n_hit = 0.0;
        let mut loss_before = 0.0;
        for i in 0..x.nrows() {
            let yi = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut pred = 0.0;
            for j in 0..x.ncols() {
                pred += self.coef[j] * x.get(i, j);
            }
            let margin = yi * pred;
            let b = self.alpha * (1.0 + self.c * (self.n_seen as f64 + 1.0).powf(-1.0 / self.p));
            loss_before += (b - margin).max(0.0);
            if margin <= (1.0 - self.alpha) * b.max(1e-12) {
                n_hit += 1.0;
                let eta = self.c / (self.n_seen as f64 + 1.0).sqrt();
                for j in 0..x.ncols() {
                    self.coef[j] += eta * yi * x.get(i, j);
                }
                let nrm = self.coef.norm().max(1e-15);
                for j in 0..x.ncols() {
                    self.coef[j] /= nrm;
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(n_hit);
        q.loss_before = Some(loss_before);
        q.still_identified = self.n_seen as usize > x.ncols();
        q.warmup = self.n_seen < 5;
        q.explanation = format!(
            "ALMA: {n_hit} of {} rows inside the large-margin region, ||Δw||={:.4e}",
            x.nrows(),
            delta.norm()
        );
        if n_hit == 0.0 {
            ctx.push(
                Issue::builder(IssueCode::UpdateWithZeroInformation)
                    .incremental(q.clone())
                    .message("ALMA left every row outside the update region")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "unit-norm ALMA weights",
                "large-margin multiplicative schedule of Gentile (2001)",
                format!("margin_loss={loss_before:.6e}"),
                format!("updates={n_hit}"),
            ),
        )
    }
}

/// Early drift detection (Baena-García; river `EDDM`) on a 0/1 error stream.
#[derive(Clone, Debug)]
pub struct Eddm {
    n: u64,
    last_error_at: Option<u64>,
    mean_d: f64,
    m2_d: f64,
    n_err: u64,
    max_mean2s: f64,
    updates: u64,
}

impl Default for Eddm {
    fn default() -> Self {
        Self {
            n: 0,
            last_error_at: None,
            mean_d: 0.0,
            m2_d: 0.0,
            n_err: 0,
            max_mean2s: 0.0,
            updates: 0,
        }
    }
}

impl Eddm {
    /// Fresh EDDM detector.
    pub fn new() -> Self {
        Self::default()
    }

    /// Update with a 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n += 1;
        if e > 0.5 {
            if let Some(prev) = self.last_error_at {
                let d = (self.n - prev) as f64;
                self.n_err += 1;
                let delta = d - self.mean_d;
                self.mean_d += delta / self.n_err as f64;
                self.m2_d += delta * (d - self.mean_d);
            }
            self.last_error_at = Some(self.n);
        }
        let sd = if self.n_err > 1 {
            (self.m2_d / (self.n_err - 1) as f64).max(0.0).sqrt()
        } else {
            0.0
        };
        let stat = self.mean_d + 2.0 * sd;
        if stat > self.max_mean2s {
            self.max_mean2s = stat;
        }
        let decision = if self.n_err >= 30 && self.max_mean2s > 0.0 && stat < 0.90 * self.max_mean2s
        {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("EDDM drift: mean+2s={stat:.4} < 0.9 max"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.last_error_at = None;
            self.mean_d = 0.0;
            self.m2_d = 0.0;
            self.n_err = 0;
            self.max_mean2s = 0.0;
            DriftDecision::Drift { statistic: stat }
        } else if self.n_err >= 30 && self.max_mean2s > 0.0 && stat < 0.95 * self.max_mean2s {
            DriftDecision::Warning { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

impl PartialFit for Eddm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let before = self.mean_d;
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("eddm")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.mean_d);
        q.still_identified = self.n_err >= 30;
        q.warmup = self.n_err < 30;
        q.explanation = format!(
            "EDDM mean-distance {before:.4}→{:.4}, errors={}, drift_cuts={fired}",
            self.mean_d, self.n_err
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "inter-error distance mean and std",
                "EDDM comparison to the historical maximum of mean+2s",
                format!("d̄={before:.4}"),
                format!("d̄={:.4} n_err={}", self.mean_d, self.n_err),
            ),
        )
    }
}

/// Hoeffding drift detection with an EWMA mean (river `HDDM_W`).
#[derive(Clone, Debug)]
pub struct HddmW {
    /// Confidence \(\delta\).
    pub delta: f64,
    /// EWMA weight \(\lambda\).
    pub lambda: f64,
    n: u64,
    mean: f64,
    mean_min: f64,
    n_min: u64,
    updates: u64,
}

impl Default for HddmW {
    fn default() -> Self {
        Self {
            delta: 0.002,
            lambda: 0.05,
            n: 0,
            mean: 0.0,
            mean_min: f64::INFINITY,
            n_min: 0,
            updates: 0,
        }
    }
}

impl HddmW {
    /// Fresh HDDM_W detector.
    pub fn new() -> Self {
        Self::default()
    }

    fn bound(n: u64, lambda: f64, delta: f64) -> f64 {
        if n == 0 {
            return f64::INFINITY;
        }
        let decay = 1.0 - (1.0 - lambda).powi(n as i32);
        let eff = (lambda / decay.max(1e-18)).min(1.0);
        (0.5 * eff * (1.0 / delta.max(1e-16)).ln()).sqrt()
    }

    /// Update with a 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n += 1;
        let l = self.lambda.clamp(1e-6, 1.0);
        if self.n == 1 {
            self.mean = e;
        } else {
            self.mean = l * e + (1.0 - l) * self.mean;
        }
        let eps = Self::bound(self.n, l, self.delta);
        let eps_min = Self::bound(self.n_min.max(1), l, self.delta);
        if self.mean + eps < self.mean_min + eps_min {
            self.mean_min = self.mean;
            self.n_min = self.n;
        }
        let stat = self.mean - self.mean_min;
        let decision = if self.n >= 30 && self.mean - eps > self.mean_min + eps_min {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!(
                        "HDDM_W drift: μ-ε={:.4} > μ_min+ε_min",
                        self.mean - eps
                    ))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.mean = 0.0;
            self.n_min = 0;
            self.mean_min = f64::INFINITY;
            DriftDecision::Drift { statistic: stat }
        } else if self.n >= 30 && self.mean - 0.5 * eps > self.mean_min + 0.5 * eps_min {
            DriftDecision::Warning { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

impl PartialFit for HddmW {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let before = self.mean;
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("hddmw")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.mean);
        q.still_identified = self.n >= 30;
        q.warmup = self.n < 30;
        q.explanation = format!(
            "HDDM_W μ {before:.4}→{:.4}, n={}, drift_cuts={fired}",
            self.mean, self.n
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "EWMA error-rate and a weighted Hoeffding bound",
                "HDDM_W comparison to the historical minimum",
                format!("μ={before:.4}"),
                format!("μ={:.4} n={}", self.mean, self.n),
            ),
        )
    }
}

/// Leveraging bagging: Poisson(\(w\)) online bagging of Hoeffding trees.
#[derive(Clone, Debug)]
pub struct LeveragingBagging {
    /// Number of trees.
    pub n_estimators: usize,
    /// Poisson mean of the leverage weights.
    pub w: f64,
    trees: Vec<HoeffdingTree>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for LeveragingBagging {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            w: 6.0,
            trees: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(11),
        }
    }
}

impl LeveragingBagging {
    /// Forest with `n_estimators` trees.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.initialized {
            return;
        }
        self.trees = (0..self.n_estimators)
            .map(|_| {
                let mut t = HoeffdingTree::new();
                t.min_samples = 15;
                t.n_features = p;
                t.root = HtNode::Leaf(HtLeaf::new(0, p));
                t.initialized = true;
                t
            })
            .collect();
        self.initialized = true;
    }
}

impl PartialFit for LeveragingBagging {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            self.ensure(x.ncols());
        }
        let mut n_updates = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            let xi = Matrix::from_fn(1, x.ncols(), |_, j| x.get(i, j));
            let yi = Vector::from_slice(&[y[i]]);
            for t in 0..self.trees.len() {
                let k = self.rng.poisson(self.w.max(0.0));
                n_updates += k;
                for _ in 0..k {
                    let _ = self.trees[t].partial_fit(&xi, Some(&yi), &session.child("tree"));
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_updates as f64);
        q.information_gain = Some(n_updates as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "LeveragingBagging: {} trees, {n_updates} Poisson-weighted Hoeffding updates",
            self.trees.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("leveraging bagging still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_updates} Poisson(w={}) tree updates", self.w),
                "each row is replayed k~Poisson(w) times into every Hoeffding tree",
                "pre-batch forest",
                "post-batch forest",
            ),
        )
    }
}

impl Predict for LeveragingBagging {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut c0 = 0usize;
            let mut c1 = 0usize;
            for t in &self.trees {
                if t.predict_one(x, i) >= 0.5 {
                    c1 += 1;
                } else {
                    c0 += 1;
                }
            }
            if c1 >= c0 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(out)
    }
}

impl Predict for Alma {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            if s >= 0.0 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(out)
    }
}

/// Online SNARIMAX: RLS on AR lags, MA residual lags, and exogenous columns.
#[derive(Clone, Debug)]
pub struct Snarimax {
    /// Autoregressive order.
    pub p: usize,
    /// Moving-average order.
    pub q: usize,
    inner: LinearRegression,
    y_hist: Vec<f64>,
    e_hist: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Snarimax {
    fn default() -> Self {
        Self {
            p: 1,
            q: 0,
            inner: LinearRegression::new(1.0),
            y_hist: Vec::new(),
            e_hist: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Snarimax {
    /// SNARIMAX with AR(`p`) and MA(`q`).
    pub fn new(p: usize, q: usize) -> Self {
        Self {
            p,
            q,
            ..Self::default()
        }
    }

    fn feat_row(&self, x: &Matrix, i: usize) -> Vector {
        let dim = self.p + self.q + x.ncols();
        let mut z = Vector::zeros(dim);
        for k in 0..self.p {
            let idx = self.y_hist.len().saturating_sub(k + 1);
            z[k] = if self.y_hist.len() > k {
                self.y_hist[idx]
            } else {
                0.0
            };
        }
        for k in 0..self.q {
            let idx = self.e_hist.len().saturating_sub(k + 1);
            z[self.p + k] = if self.e_hist.len() > k {
                self.e_hist[idx]
            } else {
                0.0
            };
        }
        for j in 0..x.ncols() {
            z[self.p + self.q + j] = x.get(i, j);
        }
        z
    }
}

impl PartialFit for Snarimax {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        let need = self.p.max(self.q);
        let warmup = self.y_hist.len() < need;
        let mut info = 0.0;
        let mut last_err = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            let z = self.feat_row(x, i);
            let row = Matrix::from_fn(1, z.len(), |_, j| z[j]);
            let yi = Vector::from_slice(&[y[i]]);
            match self
                .inner
                .partial_fit(&row, Some(&yi), &session.child("rls"))
            {
                Ok(q) => {
                    info += q.value.quality.information_gain.unwrap_or(0.0);
                }
                Err(e) => ctx.push(e.primary),
            }
            let pred = if self.inner.coef().len() + 1 == z.len() + 1
                || self.inner.intercept().is_finite()
            {
                let mut s = self.inner.intercept();
                let c = self.inner.coef();
                for j in 0..c.len().min(z.len()) {
                    s += c[j] * z[j];
                }
                s
            } else {
                0.0
            };
            last_err = y[i] - pred;
            self.y_hist.push(y[i]);
            self.e_hist.push(last_err);
            if self.y_hist.len() > 64 {
                self.y_hist.remove(0);
            }
            if self.e_hist.len() > 64 {
                self.e_hist.remove(0);
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(info.abs() + last_err.abs());
        q.still_identified = self.n_seen > (self.p + self.q + x.ncols()) as u64;
        q.warmup = warmup || self.n_seen < 5;
        q.explanation = format!(
            "SNARIMAX AR{} MA{}: last residual={last_err:.4e}, n={}",
            self.p, self.q, self.n_seen
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SNARIMAX lag buffers are still filling")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RLS update of AR/MA/exog coefficients",
                "Hannan–Rissanen-style online regression on lags and x_t",
                format!("n={}", self.n_seen.saturating_sub(x.nrows() as u64)),
                format!("n={} e={last_err:.4e}", self.n_seen),
            ),
        )
    }
}

/// DenStream-style decaying micro-clusters (river `cluster.DenStream`).
#[derive(Clone, Debug)]
pub struct DenStream {
    /// Merge radius.
    pub eps: f64,
    /// Per-step weight decay.
    pub lambda: f64,
    micros: Vec<(Vector, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for DenStream {
    fn default() -> Self {
        Self {
            eps: 1.0,
            lambda: 0.99,
            micros: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl DenStream {
    /// DenStream with merge radius `eps`.
    pub fn new(eps: f64) -> Self {
        Self {
            eps,
            ..Self::default()
        }
    }

    /// Current micro-cluster count.
    pub fn n_micro(&self) -> usize {
        self.micros.len()
    }
}

impl PartialFit for DenStream {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !(self.eps.is_finite() && self.eps > 0.0) {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .message(format!("DenStream eps={} is not positive", self.eps))
                    .build(),
            );
        }
        let lam = self.lambda.clamp(1e-6, 1.0);
        let mut merged = 0u64;
        let mut spawned = 0u64;
        for i in 0..x.nrows() {
            for mc in self.micros.iter_mut() {
                mc.1 *= lam;
            }
            self.micros.retain(|mc| mc.1 >= 1e-3);
            let row = x.row(i);
            let mut best = None;
            let mut best_d = f64::INFINITY;
            for (c, (ctr, _)) in self.micros.iter().enumerate() {
                let mut d = 0.0;
                for j in 0..row.len().min(ctr.len()) {
                    let z = row[j] - ctr[j];
                    d += z * z;
                }
                d = d.sqrt();
                if d < best_d {
                    best_d = d;
                    best = Some(c);
                }
            }
            if let Some(c) = best {
                if best_d <= self.eps {
                    let w = self.micros[c].1;
                    let nw = w + 1.0;
                    for j in 0..self.micros[c].0.len().min(row.len()) {
                        self.micros[c].0[j] = (w * self.micros[c].0[j] + row[j]) / nw;
                    }
                    self.micros[c].1 = nw;
                    merged += 1;
                    continue;
                }
            }
            self.micros.push((row, 1.0));
            spawned += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        if self.micros.is_empty() && x.nrows() > 0 {
            ctx.push(
                Issue::builder(IssueCode::EmptyCluster)
                    .message("DenStream retained no micro-clusters")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.micros.iter().map(|m| m.1).sum();
        q.information_gain = Some(merged as f64 + spawned as f64);
        q.still_identified = !self.micros.is_empty();
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "DenStream: merged={merged} spawned={spawned} micros={}",
            self.micros.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("DenStream has seen fewer than 4 points")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "micro-cluster assignment / spawn",
                "decayed CF update if within eps, else a new micro-cluster",
                format!("micros before batch"),
                format!("micros={}", self.micros.len()),
            ),
        )
    }
}

/// Hoeffding Adaptive Tree: a VFDT plus ADWIN on 0/1 error (Bifet & Gavaldà).
#[derive(Clone, Debug)]
pub struct HoeffdingAdaptiveTree {
    tree: HoeffdingTree,
    detector: Adwin,
    n_seen: u64,
    updates: u64,
    resets: u64,
}

impl Default for HoeffdingAdaptiveTree {
    fn default() -> Self {
        Self {
            tree: HoeffdingTree::new(),
            detector: Adwin::new(0.002),
            n_seen: 0,
            updates: 0,
            resets: 0,
        }
    }
}

impl HoeffdingAdaptiveTree {
    /// Default HAT.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HoeffdingAdaptiveTree {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let q = self.tree.partial_fit(x, y, &session.child("tree"))?;
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            return finish_explain(ctx, q.value);
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut drift = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            let pred = self.tree.predict_one(x, i);
            let err = if (pred - as01(y[i])).abs() > 0.5 {
                1.0
            } else {
                0.0
            };
            match self.detector.update(err, &session.child("adwin")) {
                Ok(d) => {
                    if matches!(d.value, DriftDecision::Drift { .. }) {
                        self.tree.reset();
                        self.detector.reset();
                        self.resets += 1;
                        drift += 1;
                        ctx.push(
                            Issue::builder(IssueCode::ConceptDriftDetected)
                                .message("HAT reset the Hoeffding tree after an ADWIN cut")
                                .build(),
                        );
                    }
                }
                Err(_) => {
                    self.tree.reset();
                    self.detector.reset();
                    self.resets += 1;
                    drift += 1;
                    ctx.push(
                        Issue::builder(IssueCode::ConceptDriftDetected)
                            .message("HAT reset after ADWIN reported drift as an error")
                            .build(),
                    );
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut iq = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        iq.effective_sample_size = self.n_seen as f64;
        iq.parameter_delta_norm =
            Some(drift as f64 + q.value.quality.parameter_delta_norm.unwrap_or(0.0));
        iq.information_gain = Some(drift as f64 + q.value.quality.information_gain.unwrap_or(0.0));
        iq.still_identified = self.n_seen >= 15;
        iq.warmup = self.n_seen < 15;
        iq.explanation = format!(
            "HAT: VFDT update then ADWIN on 0/1 error; {drift} resets (total {})",
            self.resets
        );
        if iq.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(iq.clone())
                    .message("HAT is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                iq,
                if drift > 0 {
                    "tree reset after drift"
                } else {
                    "leaf stats updated; no drift"
                },
                "Hoeffding split test plus ADWIN on instantaneous error",
                "pre-batch HAT",
                "post-batch HAT",
            ),
        )
    }
}

impl Predict for HoeffdingAdaptiveTree {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.tree.predict(x, session)
    }
}

/// Hoeffding adaptive tree regressor (river `HoeffdingAdaptiveTreeRegressor`).
///
/// ADWIN watches absolute residual; a cut resets the tree.
#[derive(Clone, Debug)]
pub struct HoeffdingAdaptiveTreeRegressor {
    tree: HoeffdingRegressor,
    detector: Adwin,
    n_seen: u64,
    updates: u64,
    resets: u64,
}

impl Default for HoeffdingAdaptiveTreeRegressor {
    fn default() -> Self {
        Self {
            tree: HoeffdingRegressor::new(),
            detector: Adwin::new(0.002),
            n_seen: 0,
            updates: 0,
            resets: 0,
        }
    }
}

impl HoeffdingAdaptiveTreeRegressor {
    /// Default HAT regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HoeffdingAdaptiveTreeRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let q = self.tree.partial_fit(x, y, &session.child("tree"))?;
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            return finish_explain(ctx, q.value);
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        let mut drift = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            let pred = self.tree.predict_one(x, i);
            let err = (pred - y[i]).abs();
            match self.detector.update(err, &session.child("adwin")) {
                Ok(d) => {
                    if matches!(d.value, DriftDecision::Drift { .. }) {
                        self.tree.reset();
                        self.detector.reset();
                        self.resets += 1;
                        drift += 1;
                        ctx.push(
                            Issue::builder(IssueCode::ConceptDriftDetected)
                                .message("HAT-regressor reset the tree after an ADWIN cut")
                                .build(),
                        );
                    }
                }
                Err(_) => {
                    self.tree.reset();
                    self.detector.reset();
                    self.resets += 1;
                    drift += 1;
                    ctx.push(
                        Issue::builder(IssueCode::ConceptDriftDetected)
                            .message("HAT-regressor reset after ADWIN reported drift as an error")
                            .build(),
                    );
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut iq = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        iq.effective_sample_size = self.n_seen as f64;
        iq.parameter_delta_norm =
            Some(drift as f64 + q.value.quality.parameter_delta_norm.unwrap_or(0.0));
        iq.information_gain = Some(drift as f64 + q.value.quality.information_gain.unwrap_or(0.0));
        iq.still_identified = self.n_seen >= 15;
        iq.warmup = self.n_seen < 15;
        iq.explanation = format!(
            "HAT-regressor: VFDT update then ADWIN on |residual|; {drift} resets (total {})",
            self.resets
        );
        if iq.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(iq.clone())
                    .message("HAT-regressor is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                iq,
                if drift > 0 {
                    "tree reset after drift"
                } else {
                    "leaf means updated; no drift"
                },
                "Hoeffding variance-reduction plus ADWIN on absolute residual",
                "pre-batch HAT-regressor",
                "post-batch HAT-regressor",
            ),
        )
    }
}

impl Predict for HoeffdingAdaptiveTreeRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.tree.predict(x, session)
    }
}

/// Streaming Random Patches: Hoeffding trees on random feature subsets.
#[derive(Clone, Debug)]
pub struct StreamingRandomPatches {
    /// Number of trees.
    pub n_estimators: usize,
    trees: Vec<HoeffdingTree>,
    masks: Vec<Vec<usize>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for StreamingRandomPatches {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            trees: Vec::new(),
            masks: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(19),
        }
    }
}

impl StreamingRandomPatches {
    /// Forest with `n_estimators` patch trees.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.initialized {
            return;
        }
        let k = ((p as f64).sqrt().ceil() as usize).clamp(1, p.max(1));
        self.masks = (0..self.n_estimators)
            .map(|_| {
                let mut idx: Vec<usize> = (0..p).collect();
                self.rng.shuffle(&mut idx);
                idx.truncate(k);
                if idx.is_empty() {
                    idx.push(0);
                }
                idx
            })
            .collect();
        self.trees = (0..self.n_estimators)
            .map(|_| {
                let mut t = HoeffdingTree::new();
                t.min_samples = 15;
                t.n_features = k;
                t.root = HtNode::Leaf(HtLeaf::new(0, k));
                t.initialized = true;
                t
            })
            .collect();
        self.initialized = true;
    }
}

impl PartialFit for StreamingRandomPatches {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        self.ensure(x.ncols());
        let mut n_up = 0u64;
        for t in 0..self.trees.len() {
            let mask = &self.masks[t];
            let xs = Matrix::from_fn(x.nrows(), mask.len(), |i, j| {
                let c = mask[j];
                if c < x.ncols() {
                    x.get(i, c)
                } else {
                    0.0
                }
            });
            let _ = self.trees[t].partial_fit(&xs, Some(y), &session.child("patch"));
            n_up += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_up as f64);
        q.information_gain = Some(n_up as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "SRP: {} patch trees, each on {} random features",
            self.trees.len(),
            self.masks.first().map(|m| m.len()).unwrap_or(0)
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SRP is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_up} patch-tree updates"),
                "each Hoeffding tree sees a random feature subspace",
                "pre-batch SRP",
                "post-batch SRP",
            ),
        )
    }
}

impl Predict for StreamingRandomPatches {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut c0 = 0usize;
            let mut c1 = 0usize;
            for (t, mask) in self.masks.iter().enumerate() {
                let xs = Matrix::from_fn(1, mask.len(), |_, j| {
                    let c = mask[j];
                    if c < x.ncols() {
                        x.get(i, c)
                    } else {
                        0.0
                    }
                });
                if self.trees[t].predict_one(&xs, 0) >= 0.5 {
                    c1 += 1;
                } else {
                    c0 += 1;
                }
            }
            if c1 >= c0 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(out)
    }
}

/// Streaming Random Patches regressor (river `ensemble.SRPRegressor`).
///
/// Estimator count is not identification `p`.
#[derive(Clone, Debug)]
pub struct SrpRegressor {
    /// Number of patch trees.
    pub n_estimators: usize,
    trees: Vec<HoeffdingRegressor>,
    masks: Vec<Vec<usize>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for SrpRegressor {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            trees: Vec::new(),
            masks: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(23),
        }
    }
}

impl SrpRegressor {
    /// Forest with `n_estimators` patch regressors.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.initialized {
            return;
        }
        let k = ((p as f64).sqrt().ceil() as usize).clamp(1, p.max(1));
        self.masks = (0..self.n_estimators)
            .map(|_| {
                let mut idx: Vec<usize> = (0..p).collect();
                self.rng.shuffle(&mut idx);
                idx.truncate(k);
                if idx.is_empty() {
                    idx.push(0);
                }
                idx
            })
            .collect();
        self.trees = (0..self.n_estimators)
            .map(|_| HoeffdingRegressor::new())
            .collect();
        self.initialized = true;
    }
}

impl PartialFit for SrpRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        self.ensure(x.ncols());
        let mut n_up = 0u64;
        for t in 0..self.trees.len() {
            let mask = &self.masks[t];
            let xs = Matrix::from_fn(x.nrows(), mask.len(), |i, j| {
                let c = mask[j];
                if c < x.ncols() {
                    x.get(i, c)
                } else {
                    0.0
                }
            });
            let _ = self.trees[t].partial_fit(&xs, Some(y), &session.child("srpr"));
            n_up += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_up as f64);
        q.information_gain = Some(n_up as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "SRPRegressor: {} patch trees, each on {} random features",
            self.trees.len(),
            self.masks.first().map(|m| m.len()).unwrap_or(0)
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SRPRegressor is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_up} patch-regressor updates"),
                "each Hoeffding regressor sees a random feature subspace",
                "pre-batch SRPRegressor",
                "post-batch SRPRegressor",
            ),
        )
    }
}

impl Predict for SrpRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.trees.len().max(1) as f64;
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0;
            for (t, mask) in self.masks.iter().enumerate() {
                let xs = Matrix::from_fn(1, mask.len(), |_, j| {
                    let c = mask[j];
                    if c < x.ncols() {
                        x.get(i, c)
                    } else {
                        0.0
                    }
                });
                s += self.trees[t].predict_one(&xs, 0);
            }
            s / n_t
        }));
        ctx.finish(out)
    }
}

/// Streaming random patches classifier (river `ensemble.SRPClassifier`).
///
/// Tree / mask counts are not identification `p`.
#[derive(Clone, Debug)]
pub struct SrpClassifier {
    /// Number of patch trees.
    pub n_estimators: usize,
    trees: Vec<HoeffdingTree>,
    masks: Vec<Vec<usize>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for SrpClassifier {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            trees: Vec::new(),
            masks: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(29),
        }
    }
}

impl SrpClassifier {
    /// Forest with `n_estimators` patch classifiers.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.initialized {
            return;
        }
        let k = ((p as f64).sqrt().ceil() as usize).clamp(1, p.max(1));
        self.masks = (0..self.n_estimators)
            .map(|_| {
                let mut idx: Vec<usize> = (0..p).collect();
                self.rng.shuffle(&mut idx);
                idx.truncate(k);
                if idx.is_empty() {
                    idx.push(0);
                }
                idx
            })
            .collect();
        self.trees = (0..self.n_estimators)
            .map(|_| HoeffdingTree::new())
            .collect();
        self.initialized = true;
    }
}

impl PartialFit for SrpClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        inspect_online_xy(&mut ctx, x, Some(y));
        self.ensure(x.ncols());
        let mut n_up = 0u64;
        for t in 0..self.trees.len() {
            let mask = &self.masks[t];
            let xs = Matrix::from_fn(x.nrows(), mask.len(), |i, j| {
                let c = mask[j];
                if c < x.ncols() {
                    x.get(i, c)
                } else {
                    0.0
                }
            });
            let _ = self.trees[t].partial_fit(&xs, Some(y), &session.child("srpc"));
            n_up += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_up as f64);
        q.information_gain = Some(n_up as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "SRPClassifier: {} patch trees, each on {} random features",
            self.trees.len(),
            self.masks.first().map(|m| m.len()).unwrap_or(0)
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SRPClassifier is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_up} patch-classifier updates"),
                "each Hoeffding tree sees a random feature subspace; tree count is not p",
                "pre-batch SRPClassifier",
                "post-batch SRPClassifier",
            ),
        )
    }
}

impl Predict for SrpClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.trees.len().max(1) as f64;
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0_f64;
            for (t, mask) in self.masks.iter().enumerate() {
                let xs = Matrix::from_fn(1, mask.len(), |_, j| {
                    let c = mask[j];
                    if c < x.ncols() {
                        x.get(i, c)
                    } else {
                        0.0
                    }
                });
                s += self.trees[t].predict_one(&xs, 0);
            }
            if s / n_t >= 0.5 {
                1.0
            } else {
                0.0
            }
        }));
        ctx.finish(out)
    }
}

/// Funk / biased SVD matrix factorization (river `reco.FunkMF`).
///
/// `X` has two columns: user id and item id (rounded). `y` is the rating.
#[derive(Clone, Debug)]
pub struct FunkMf {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// ℓ2 on factors and biases.
    pub l2: f64,
    mu: f64,
    bu: Vec<f64>,
    bi: Vec<f64>,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for FunkMf {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            mu: 0.0,
            bu: Vec::new(),
            bi: Vec::new(),
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl FunkMf {
    /// FunkMF with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.bu.len() <= u {
            self.bu.push(0.0);
            self.pu.push(vec![0.01; k]);
        }
        while self.bi.len() <= i {
            self.bi.push(0.0);
            self.qi.push(vec![0.01; k]);
        }
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = self.mu;
        if u < self.bu.len() {
            s += self.bu[u];
        }
        if i < self.bi.len() {
            s += self.bi[i];
        }
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for FunkMf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("FunkMF needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut sse = 0.0;
        let mut dsum = 0.0;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let pred = self.pred(u, i);
            let e = y[r] - pred;
            sse += e * e;
            self.mu += lr * e;
            self.bu[u] += lr * (e - l2 * self.bu[u]);
            self.bi[i] += lr * (e - l2 * self.bi[i]);
            for f in 0..self.n_factors {
                let pu = self.pu[u][f];
                let qi = self.qi[i][f];
                self.pu[u][f] += lr * (e * qi - l2 * pu);
                self.qi[i][f] += lr * (e * pu - l2 * qi);
                dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 8;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.loss_before = Some(sse);
        q.loss_after = Some(sse);
        q.information_gain = Some(dsum);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "FunkMF SGD: ||ΔP,Q||₁={dsum:.4e}, SSE={sse:.4e}, users={}, items={}",
            self.bu.len(),
            self.bi.len()
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("FunkMF is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("SGD on {} ratings", x.nrows()),
                "biased SVD step: μ, b_u, b_i, p_u, q_i",
                "pre-batch factors",
                format!("μ={:.4e} SSE={sse:.4e}", self.mu),
            ),
        )
    }
}

impl Predict for FunkMf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("FunkMF predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.mu));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.pred(u, i)
        }));
        ctx.finish(y)
    }
}

/// CluStream-style q-micro-cluster summary (Aggarwal et al.).
#[derive(Clone, Debug)]
pub struct CluStream {
    /// Maximum number of micro-clusters.
    pub q: usize,
    /// Assign-to-nearest radius; beyond this a new micro is spawned.
    pub radius: f64,
    micros: Vec<(f64, Vector, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for CluStream {
    fn default() -> Self {
        Self {
            q: 8,
            radius: 2.0,
            micros: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl CluStream {
    /// CluStream with at most `q` micro-clusters.
    pub fn new(q: usize) -> Self {
        Self {
            q: q.max(1),
            ..Self::default()
        }
    }

    /// Current micro-cluster count.
    pub fn n_micro(&self) -> usize {
        self.micros.len()
    }

    fn center(n: f64, ls: &Vector) -> Vector {
        if n <= 0.0 {
            Vector::zeros(ls.len())
        } else {
            ls.scale(1.0 / n)
        }
    }

    fn merge_nearest(&mut self) {
        if self.micros.len() < 2 {
            return;
        }
        let mut best = (0usize, 1usize, f64::INFINITY);
        for a in 0..self.micros.len() {
            let ca = Self::center(self.micros[a].0, &self.micros[a].1);
            for b in (a + 1)..self.micros.len() {
                let cb = Self::center(self.micros[b].0, &self.micros[b].1);
                let d = ca.sub(&cb).norm();
                if d < best.2 {
                    best = (a, b, d);
                }
            }
        }
        let (a, b, _) = best;
        let (n2, ls2, ss2) = self.micros.remove(b.max(a));
        let (n1, ls1, ss1) = self.micros.remove(a.min(b));
        self.micros.push((n1 + n2, ls1.add(&ls2), ss1 + ss2));
    }
}

impl PartialFit for CluStream {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !(self.radius.is_finite() && self.radius > 0.0) {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!("CluStream radius={} is not positive", self.radius))
                    .build(),
            );
        }
        let mut spawned = 0u64;
        let mut merged = 0u64;
        for i in 0..x.nrows() {
            let row = x.row(i);
            let mut best = None;
            let mut bd = f64::INFINITY;
            for (t, (n, ls, _)) in self.micros.iter().enumerate() {
                let c = Self::center(*n, ls);
                if c.len() != row.len() {
                    continue;
                }
                let d = c.sub(&row).norm();
                if d < bd {
                    bd = d;
                    best = Some(t);
                }
            }
            if let Some(t) = best {
                if bd <= self.radius {
                    let (n, ls, ss) = &mut self.micros[t];
                    *n += 1.0;
                    for j in 0..ls.len().min(row.len()) {
                        ls[j] += row[j];
                    }
                    *ss += row.dot(&row);
                    continue;
                }
            }
            self.micros.push((1.0, row.clone(), row.dot(&row)));
            spawned += 1;
            while self.micros.len() > self.q.max(1) {
                self.merge_nearest();
                merged += 1;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let warmup = self.n_seen < 4;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((spawned + merged) as f64);
        q.information_gain = Some((spawned + merged) as f64);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "CluStream: spawned {spawned}, merged {merged}, micros={}",
            self.micros.len()
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("CluStream has seen fewer than 4 points")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{spawned} new micros, {merged} merges"),
                "assign to nearest CF if within radius, else spawn and merge the closest pair",
                "pre-batch micros",
                format!("micros={}", self.micros.len()),
            ),
        )
    }
}

impl Predict for CluStream {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.micros.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::filled(x.nrows(), -1.0));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let row = x.row(i);
            let mut best = 0usize;
            let mut bd = f64::INFINITY;
            for (t, (n, ls, _)) in self.micros.iter().enumerate() {
                let c = Self::center(*n, ls);
                if c.len() != row.len() {
                    continue;
                }
                let d = c.sub(&row).norm();
                if d < bd {
                    bd = d;
                    best = t;
                }
            }
            best as f64
        }));
        ctx.finish(y)
    }
}

/// Extremely Fast Decision Tree (Hoeffding AnyTime Tree) lite.
///
/// Same VFDT sufficient statistics as [`HoeffdingTree`], but splits are
/// considered earlier (`min_samples` is smaller and `tau` is larger). A
/// re-split of an existing node is not implemented — documented as a
/// compromise versus Manapragada, Webb & Salehi.
#[derive(Clone, Debug)]
pub struct ExtremelyFastDecisionTree {
    tree: HoeffdingTree,
}

impl Default for ExtremelyFastDecisionTree {
    fn default() -> Self {
        let mut tree = HoeffdingTree::new();
        tree.min_samples = 5;
        tree.tau = 0.15;
        Self { tree }
    }
}

impl ExtremelyFastDecisionTree {
    /// Eager VFDT.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ExtremelyFastDecisionTree {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let q = self.tree.partial_fit(x, y, session)?;
        let mut ctx = FitCtx::with_session(session.child("efdt"));
        ctx.push(
            Issue::builder(IssueCode::JitterInjected)
                .severity(Severity::Advisory)
                .message("EFDT-lite uses an eager Hoeffding bound; it does not re-evaluate existing splits")
                .build(),
        );
        ctx.finish(q.value)
    }
}

impl Predict for ExtremelyFastDecisionTree {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.tree.predict(x, session)
    }
}

/// Biased matrix factorization: `μ + b_u + b_i` (river `reco.BiasedMF`).
#[derive(Clone, Debug)]
pub struct BiasedMf {
    /// SGD step.
    pub learning_rate: f64,
    /// ℓ2 on biases.
    pub l2: f64,
    mu: f64,
    bu: Vec<f64>,
    bi: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for BiasedMf {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            l2: 0.01,
            mu: 0.0,
            bu: Vec::new(),
            bi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl BiasedMf {
    /// Default biased MF.
    pub fn new() -> Self {
        Self::default()
    }

    fn grow(&mut self, u: usize, i: usize) {
        while self.bu.len() <= u {
            self.bu.push(0.0);
        }
        while self.bi.len() <= i {
            self.bi.push(0.0);
        }
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = self.mu;
        if u < self.bu.len() {
            s += self.bu[u];
        }
        if i < self.bi.len() {
            s += self.bi[i];
        }
        s
    }
}

impl PartialFit for BiasedMf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BiasedMF needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut sse = 0.0;
        let mut dsum = 0.0;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let pred = self.pred(u, i);
            let e = y[r] - pred;
            sse += e * e;
            self.mu += lr * e;
            let bu = self.bu[u];
            let bi = self.bi[i];
            self.bu[u] += lr * (e - l2 * bu);
            self.bi[i] += lr * (e - l2 * bi);
            dsum += (self.bu[u] - bu).abs() + (self.bi[i] - bi).abs();
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 8;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "BiasedMF SGD: ||Δb||₁={dsum:.4e}, SSE={sse:.4e}, users={}, items={}",
            self.bu.len(),
            self.bi.len()
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("BiasedMF is still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("SGD on {} ratings", x.nrows()),
                "μ, b_u, b_i only (no latent factors)",
                "pre-batch biases",
                format!("μ={:.4e} SSE={sse:.4e}", self.mu),
            ),
        )
    }
}

impl Predict for BiasedMf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BiasedMF predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.mu));
        }
        let y = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.pred(u, i)
        }));
        ctx.finish(y)
    }
}

/// Online k-NN classifier (river `neighbors.KNNClassifier`).
///
/// Every labelled row is stored. A 1-row batch uses
/// [`inspect_online_xy`], not [`inspect_xy`].
#[derive(Clone, Debug)]
pub struct KnnClassifier {
    /// Neighbourhood size.
    pub k: usize,
    xs: Vec<Vec<f64>>,
    ys: Vec<i64>,
    n_seen: u64,
    updates: u64,
}

impl Default for KnnClassifier {
    fn default() -> Self {
        Self {
            k: 3,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl KnnClassifier {
    /// `k`-NN memory.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for KnnClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.n_seen > 0 {
            if let Some(first) = self.xs.first() {
                if first.len() != x.ncols() {
                    ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                    return finish_explain(
                        ctx,
                        reject_explain(
                            self.updates,
                            x.nrows(),
                            self.n_seen,
                            "feature space changed",
                        ),
                    );
                }
            }
        }
        let before = self.xs.len();
        for i in 0..x.nrows() {
            self.xs.push((0..x.ncols()).map(|j| x.get(i, j)).collect());
            self.ys.push(y[i].round() as i64);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.xs.len() - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() >= self.k;
        q.warmup = self.xs.len() < self.k;
        q.explanation = format!(
            "stored {} rows; memory={} k={}",
            x.nrows(),
            self.xs.len(),
            self.k
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("online k-NN has fewer stored rows than k")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "instance memory",
                "append labelled rows for later majority vote",
                format!("n={before}"),
                format!("n={}", self.xs.len()),
            ),
        )
    }
}

impl Predict for KnnClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.xs.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.k.max(1).min(self.xs.len());
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut dist: Vec<(f64, i64)> = self
                .xs
                .iter()
                .zip(self.ys.iter())
                .map(|(z, &lab)| {
                    let mut s = 0.0;
                    for j in 0..x.ncols().min(z.len()) {
                        let d = x.get(i, j) - z[j];
                        s += d * d;
                    }
                    (s, lab)
                })
                .collect();
            dist.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
            let mut votes = std::collections::BTreeMap::<i64, usize>::new();
            for (_, lab) in dist.into_iter().take(k) {
                *votes.entry(lab).or_insert(0) += 1;
            }
            votes
                .into_iter()
                .max_by_key(|(_, c)| *c)
                .map(|(lab, _)| lab as f64)
                .unwrap_or(0.0)
        }));
        ctx.finish(y)
    }
}

/// Online min–max scaler (river `preprocessing.MinMaxScaler`).
#[derive(Clone, Debug)]
pub struct OnlineMinMaxScaler {
    min: Vector,
    max: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineMinMaxScaler {
    fn default() -> Self {
        Self {
            min: Vector::zeros(0),
            max: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineMinMaxScaler {
    /// Empty scaler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineMinMaxScaler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.min = Vector::from_iter((0..x.ncols()).map(|_| f64::INFINITY));
            self.max = Vector::from_iter((0..x.ncols()).map(|_| f64::NEG_INFINITY));
            self.initialized = true;
        } else if self.min.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.min.clone();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                if v < self.min[j] {
                    self.min[j] = v;
                }
                if v > self.max[j] {
                    self.max[j] = v;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.min.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("online MinMax on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running column min/max",
                "stream extrema",
                "previous extrema",
                "updated extrema",
            ),
        )
    }
}

impl Transform for OnlineMinMaxScaler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.min.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p {
                return x.get(i, j);
            }
            let lo = self.min[j];
            let hi = self.max[j];
            let span = hi - lo;
            if !span.is_finite() || span.abs() <= ctx.policy.near_zero_variance {
                return 0.0;
            }
            (x.get(i, j) - lo) / span
        });
        ctx.finish(out)
    }
}

/// Online max-abs scaler (river `preprocessing.MaxAbsScaler`).
#[derive(Clone, Debug)]
pub struct OnlineMaxAbsScaler {
    max_abs: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineMaxAbsScaler {
    fn default() -> Self {
        Self {
            max_abs: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineMaxAbsScaler {
    /// Empty scaler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineMaxAbsScaler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.max_abs = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.max_abs.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.max_abs.clone();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j).abs();
                if v.is_finite() && v > self.max_abs[j] {
                    self.max_abs[j] = v;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.max_abs.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("online MaxAbs on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running column max-abs",
                "stream extrema of |x|",
                "previous max-abs",
                "updated max-abs",
            ),
        )
    }
}

impl Transform for OnlineMaxAbsScaler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.max_abs.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p {
                return x.get(i, j);
            }
            let s = self.max_abs[j];
            if !s.is_finite() || s <= ctx.policy.near_zero_variance {
                return 0.0;
            }
            x.get(i, j) / s
        });
        ctx.finish(out)
    }
}

/// Global-mean recommender (river `reco.Baseline` without biases).
#[derive(Clone, Debug, Default)]
pub struct Baseline {
    mean: f64,
    n_seen: u64,
    updates: u64,
}

impl Baseline {
    /// Empty baseline.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for Baseline {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let before = self.mean;
        for i in 0..y.len() {
            if !y[i].is_finite() {
                continue;
            }
            self.n_seen += 1;
            self.mean += (y[i] - self.mean) / self.n_seen as f64;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.mean - before).abs());
        q.information_gain = Some((self.mean - before).abs());
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("baseline μ={:.4e} after {} ratings", self.mean, self.n_seen);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "global rating mean",
                "Welford update of μ",
                format!("μ={before:.4e}"),
                format!("μ={:.4e}", self.mean),
            ),
        )
    }
}

impl Predict for Baseline {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        ctx.finish(Vector::filled(x.nrows(), self.mean))
    }
}

/// Item-popularity recommender (river `reco.Popular`).
///
/// Item-id cardinality is not identification `p`. Column 1 of `x` is the item.
#[derive(Clone, Debug, Default)]
pub struct PopularReco {
    item_sum: HashMap<i64, f64>,
    item_n: HashMap<i64, f64>,
    global: f64,
    n_seen: u64,
    updates: u64,
}

impl PopularReco {
    /// Empty popularity table.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for PopularReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let before = self.item_n.len();
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let item = if x.ncols() > 1 {
                x.get(i, 1).round() as i64
            } else {
                x.get(i, 0).round() as i64
            };
            *self.item_sum.entry(item).or_insert(0.0) += y[i];
            *self.item_n.entry(item).or_insert(0.0) += 1.0;
            self.n_seen += 1;
            self.global += (y[i] - self.global) / self.n_seen as f64;
        }
        self.updates += 1;
        let after = self.item_n.len();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("PopularReco items={after} μ={:.4e}", self.global);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "per-item mean rating",
                "popularity is the running mean of ratings for each item id",
                format!("items={before}"),
                format!("items={after}"),
            ),
        )
    }
}

impl Predict for PopularReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let item = if x.ncols() > 1 {
                x.get(i, 1).round() as i64
            } else {
                x.get(i, 0).round() as i64
            };
            match (self.item_sum.get(&item), self.item_n.get(&item)) {
                (Some(&s), Some(&n)) if n > 0.0 => s / n,
                _ => self.global,
            }
        }));
        ctx.finish(out)
    }
}

/// Gaussian rating prior (river `reco.RandomNormal`).
///
/// Predicts the running mean; sampling noise is not used at predict time.
#[derive(Clone, Debug, Default)]
pub struct RandomNormalReco {
    mean: f64,
    m2: f64,
    n_seen: u64,
    updates: u64,
}

impl RandomNormalReco {
    /// Empty Gaussian rating model.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for RandomNormalReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let before = self.mean;
        for i in 0..y.len() {
            if !y[i].is_finite() {
                continue;
            }
            self.n_seen += 1;
            let d = y[i] - self.mean;
            self.mean += d / self.n_seen as f64;
            self.m2 += d * (y[i] - self.mean);
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.mean - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("RandomNormalReco μ={:.4e} n={}", self.mean, self.n_seen);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Gaussian rating mean",
                "Welford mean/variance of ratings; predict returns μ, not a draw",
                format!("μ={before:.4e}"),
                format!("μ={:.4e}", self.mean),
            ),
        )
    }
}

impl Predict for RandomNormalReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::filled(x.nrows(), self.mean))
    }
}

/// Online k-NN regressor (river `neighbors.KNNRegressor`).
#[derive(Clone, Debug)]
pub struct KnnRegressor {
    /// Neighbourhood size.
    pub k: usize,
    xs: Vec<Vec<f64>>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for KnnRegressor {
    fn default() -> Self {
        Self {
            k: 3,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl KnnRegressor {
    /// `k`-NN memory.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for KnnRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no targets"),
            );
        };
        if self.n_seen > 0 {
            if let Some(first) = self.xs.first() {
                if first.len() != x.ncols() {
                    ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                    return finish_explain(
                        ctx,
                        reject_explain(
                            self.updates,
                            x.nrows(),
                            self.n_seen,
                            "feature space changed",
                        ),
                    );
                }
            }
        }
        let before = self.xs.len();
        for i in 0..x.nrows() {
            self.xs.push((0..x.ncols()).map(|j| x.get(i, j)).collect());
            self.ys.push(y[i]);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.xs.len() - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() >= self.k;
        q.warmup = self.xs.len() < self.k;
        q.explanation = format!("stored {} rows; memory={}", x.nrows(), self.xs.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "instance memory",
                "append labelled rows for later neighbour averaging",
                format!("n={before}"),
                format!("n={}", self.xs.len()),
            ),
        )
    }
}

impl Predict for KnnRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.xs.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.k.max(1).min(self.xs.len());
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut dist: Vec<(f64, f64)> = self
                .xs
                .iter()
                .zip(self.ys.iter())
                .map(|(z, &yi)| {
                    let mut s = 0.0;
                    for j in 0..x.ncols().min(z.len()) {
                        let d = x.get(i, j) - z[j];
                        s += d * d;
                    }
                    (s, yi)
                })
                .collect();
            dist.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
            let mut num = 0.0;
            let mut den = 0.0;
            for (_, yi) in dist.into_iter().take(k) {
                num += yi;
                den += 1.0;
            }
            if den > 0.0 {
                num / den
            } else {
                0.0
            }
        }));
        ctx.finish(y)
    }
}

/// Online robust scaler via stochastic quartiles (river `preprocessing.RobustScaler`).
#[derive(Clone, Debug)]
pub struct OnlineRobustScaler {
    q25: Vector,
    q75: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineRobustScaler {
    fn default() -> Self {
        Self {
            q25: Vector::zeros(0),
            q75: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineRobustScaler {
    /// Empty scaler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineRobustScaler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.q25 = Vector::zeros(x.ncols());
            self.q75 = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.q25.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.q25.clone();
        let eta = 1.0 / (self.n_seen as f64 + x.nrows() as f64).max(1.0);
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                self.q25[j] += eta * (if v < self.q25[j] { -0.5 } else { 0.5 } + 2.0 * 0.25 - 1.0);
                self.q75[j] += eta * (if v < self.q75[j] { -0.5 } else { 0.5 } + 2.0 * 0.75 - 1.0);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.q25.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 8;
        q.explanation = format!("online robust quartiles on {} rows", x.nrows());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running Q25/Q75",
                "stochastic quantile updates",
                "previous quartiles",
                "updated quartiles",
            ),
        )
    }
}

impl Transform for OnlineRobustScaler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.q25.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p {
                return x.get(i, j);
            }
            let iqr = self.q75[j] - self.q25[j];
            if !iqr.is_finite() || iqr.abs() <= ctx.policy.near_zero_variance {
                return 0.0;
            }
            (x.get(i, j) - self.q25[j]) / iqr
        });
        ctx.finish(out)
    }
}

/// Online softmax / multinomial logistic (river `linear_model.SoftmaxRegression`).
#[derive(Clone, Debug)]
pub struct SoftmaxRegression {
    /// Learning rate.
    pub eta: f64,
    coef: Matrix,
    intercept: Vector,
    classes: Vec<i64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for SoftmaxRegression {
    fn default() -> Self {
        Self {
            eta: 0.1,
            coef: Matrix::zeros(0, 0),
            intercept: Vector::zeros(0),
            classes: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl SoftmaxRegression {
    /// Empty softmax classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for SoftmaxRegression {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        for &yi in y.as_slice() {
            if !yi.is_finite() {
                continue;
            }
            let lab = yi.round() as i64;
            if !self.classes.contains(&lab) {
                self.classes.push(lab);
                self.classes.sort_unstable();
            }
        }
        let k = self.classes.len().max(1);
        let p = x.ncols();
        if !self.initialized {
            self.coef = Matrix::zeros(k, p);
            self.intercept = Vector::zeros(k);
            self.initialized = true;
        } else if self.coef.ncols() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        } else if self.coef.nrows() != k {
            let mut nc = Matrix::zeros(k, p);
            let mut ni = Vector::zeros(k);
            for r in 0..self.coef.nrows().min(k) {
                for c in 0..p {
                    nc.set(r, c, self.coef.get(r, c));
                }
                ni[r] = self.intercept[r];
            }
            self.coef = nc;
            self.intercept = ni;
        }
        let before = self.coef.clone();
        let eta = self.eta.max(1e-6);
        for i in 0..x.nrows() {
            let lab = y[i].round() as i64;
            let Some(ti) = self.classes.iter().position(|&c| c == lab) else {
                continue;
            };
            let mut logits = vec![0.0; k];
            let mut m = f64::NEG_INFINITY;
            for c in 0..k {
                let mut s = self.intercept[c];
                for j in 0..p {
                    s += self.coef.get(c, j) * x.get(i, j);
                }
                logits[c] = s;
                m = m.max(s);
            }
            let mut z = 0.0;
            for c in 0..k {
                logits[c] = (logits[c] - m).exp();
                z += logits[c];
            }
            for c in 0..k {
                let p_c = logits[c] / z.max(1e-12);
                let g = (if c == ti { 1.0 } else { 0.0 }) - p_c;
                self.intercept[c] += eta * g;
                for j in 0..p {
                    self.coef
                        .set(c, j, self.coef.get(c, j) + eta * g * x.get(i, j));
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut delta: f64 = 0.0;
        for c in 0..k.min(before.nrows()) {
            for j in 0..p.min(before.ncols()) {
                let d = self.coef.get(c, j) - before.get(c, j);
                delta += d * d;
            }
        }
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.sqrt());
        q.information_gain = Some(delta.sqrt());
        q.still_identified = self.n_seen as usize > p && k >= 2;
        q.warmup = self.n_seen < (p as u64 + 2);
        q.explanation = format!("softmax SGD on {} rows, K={k}", x.nrows());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "softmax weights",
                "one SGD pass on the batch",
                "previous weights",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

impl Predict for SoftmaxRegression {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.classes.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.classes.len();
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bv = f64::NEG_INFINITY;
            for c in 0..k {
                let mut s = self.intercept[c];
                for j in 0..x.ncols().min(self.coef.ncols()) {
                    s += self.coef.get(c, j) * x.get(i, j);
                }
                if s > bv {
                    bv = s;
                    best = c;
                }
            }
            self.classes[best] as f64
        }));
        ctx.finish(y)
    }
}

/// Online Gaussian naïve Bayes (river `naive_bayes.GaussianNB`).
#[derive(Clone, Debug)]
pub struct OnlineGaussianNb {
    classes: Vec<i64>,
    n_per: Vec<f64>,
    mean: Matrix,
    m2: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineGaussianNb {
    fn default() -> Self {
        Self {
            classes: Vec::new(),
            n_per: Vec::new(),
            mean: Matrix::zeros(0, 0),
            m2: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineGaussianNb {
    /// Empty online GNB.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineGaussianNb {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let p = x.ncols();
        if self.initialized && self.mean.ncols() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            if !y[i].is_finite() {
                continue;
            }
            let lab = y[i].round() as i64;
            let idx = if let Some(t) = self.classes.iter().position(|&c| c == lab) {
                t
            } else {
                self.classes.push(lab);
                self.n_per.push(0.0);
                let k = self.classes.len();
                let mut nm = Matrix::zeros(k, p);
                let mut n2 = Matrix::zeros(k, p);
                for r in 0..self.mean.nrows() {
                    for c in 0..p.min(self.mean.ncols()) {
                        nm.set(r, c, self.mean.get(r, c));
                        n2.set(r, c, self.m2.get(r, c));
                    }
                }
                self.mean = nm;
                self.m2 = n2;
                self.initialized = true;
                k - 1
            };
            self.n_per[idx] += 1.0;
            let n = self.n_per[idx];
            for j in 0..p {
                let v = x.get(i, j);
                let d = v - self.mean.get(idx, j);
                self.mean.set(idx, j, self.mean.get(idx, j) + d / n);
                let d2 = v - self.mean.get(idx, j);
                self.m2.set(idx, j, self.m2.get(idx, j) + d * d2);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some((self.n_seen - before_n) as f64);
        q.still_identified = self.classes.len() >= 2 && self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("online GNB on {} rows", x.nrows());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-conditional means/variances",
                "Welford update per class",
                format!("n={before_n}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineGaussianNb {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.classes.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.classes.len();
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bv = f64::NEG_INFINITY;
            for c in 0..k {
                let mut ll = (self.n_per[c] / self.n_seen.max(1) as f64).max(1e-12).ln();
                for j in 0..x.ncols().min(self.mean.ncols()) {
                    let n = self.n_per[c].max(1.0);
                    let var = (self.m2.get(c, j) / n).max(1e-6);
                    let d = x.get(i, j) - self.mean.get(c, j);
                    ll += -0.5 * (d * d / var + var.ln());
                }
                if ll > bv {
                    bv = ll;
                    best = c;
                }
            }
            self.classes[best] as f64
        }));
        ctx.finish(y)
    }
}

/// Incremental TF–IDF on a document–term count stream (river `feature_extraction.TFIDF`).
///
/// Document count is not identification `p`. IDF uses add-one smoothing and is
/// recorded as a running approximation of the corpus IDF.
#[derive(Clone, Debug)]
pub struct OnlineTfidf {
    df: Vector,
    n_docs: u64,
    n_features: usize,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineTfidf {
    fn default() -> Self {
        Self {
            df: Vector::zeros(0),
            n_docs: 0,
            n_features: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineTfidf {
    /// Empty streaming TF–IDF.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineTfidf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_docs, "no features"),
            );
        }
        if !self.initialized {
            self.df = Vector::zeros(x.ncols());
            self.n_features = x.ncols();
            self.initialized = true;
        } else if self.n_features != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_docs,
                    "feature space changed",
                ),
            );
        }
        let before = self.df.clone();
        for i in 0..x.nrows() {
            let mut row_sum: f64 = 0.0;
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if v.is_finite() && v > 0.0 {
                    row_sum += v;
                    self.df[j] += 1.0;
                }
            }
            if row_sum <= 0.0 {
                ctx.push(
                    Issue::builder(IssueCode::NearZeroVariance)
                        .message(format!(
                            "OnlineTfidf document {i} has no positive term counts"
                        ))
                        .build(),
                );
            }
            self.n_docs += 1;
        }
        self.updates += 1;
        let delta = self.df.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_docs);
        q.effective_sample_size = self.n_docs as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_docs.max(1) as f64));
        q.still_identified = self.n_docs >= 1;
        q.warmup = self.n_docs < 4;
        q.explanation = format!("online TF-IDF df on {} documents", x.nrows());
        flag_info(&mut ctx, &q);
        ctx.push(
            Issue::builder(IssueCode::PValueUnreliable)
                .severity(Severity::Advisory)
                .message(
                    "OnlineTfidf IDF is a running add-one estimate, not the finished corpus IDF",
                )
                .compromise(NumericalCompromise::new(
                    "batch TF-IDF on the complete collection",
                    "smoothed IDF from documents seen so far",
                    "early-stream IDF is biased toward the first documents",
                    "do not treat early IDF weights as a static vocabulary ranking",
                ))
                .build(),
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "document frequencies",
                "increment df for each positive term",
                "previous df",
                format!("n_docs={}", self.n_docs),
            ),
        )
    }
}

impl Transform for OnlineTfidf {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.df.len());
        let n = self.n_docs.max(1) as f64;
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let tf = x.get(i, j).max(0.0);
            if j >= p {
                return tf;
            }
            let idf = ((n + 1.0) / (self.df[j] + 1.0)).ln() + 1.0;
            tf * idf
        });
        ctx.finish(out)
    }
}

/// Running-mean imputer (river `imputation.StatImputer`).
///
/// Non-finite entries are skipped in the sufficient statistics. A column that
/// has never been observed finite is filled with 0 and recorded; that is not
/// a vacuous all-missing abort.
#[derive(Clone, Debug)]
pub struct StatImputer {
    mean: Vector,
    m2: Vector,
    count: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for StatImputer {
    fn default() -> Self {
        Self {
            mean: Vector::zeros(0),
            m2: Vector::zeros(0),
            count: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl StatImputer {
    /// Empty streaming mean imputer.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for StatImputer {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.mean = Vector::zeros(x.ncols());
            self.m2 = Vector::zeros(x.ncols());
            self.count = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.mean.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.mean.clone();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                self.count[j] += 1.0;
                let n = self.count[j];
                let d = v - self.mean[j];
                self.mean[j] += d / n;
                let d2 = v - self.mean[j];
                self.m2[j] += d * d2;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.mean.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self
            .count
            .as_slice()
            .iter()
            .copied()
            .fold(0.0, |a, b| a.max(b));
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.count.as_slice().iter().all(|&c| c >= 1.0);
        q.warmup = self.n_seen < 2;
        q.explanation = format!("StatImputer Welford update on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "column means",
                "Welford on finite entries",
                "previous means",
                "updated means",
            ),
        )
    }
}

impl Transform for StatImputer {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.mean.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let v = x.get(i, j);
            if v.is_finite() {
                return v;
            }
            if j >= p || self.count[j] < 1.0 {
                return 0.0;
            }
            self.mean[j]
        });
        for j in 0..p {
            if self.count[j] < 1.0 {
                ctx.push(
                    Issue::builder(IssueCode::ImputationUndefined)
                        .severity(Severity::Warning)
                        .message(format!(
                            "StatImputer column {j} has no finite observations; missing values become 0"
                        ))
                        .build(),
                );
            }
        }
        ctx.finish(out)
    }
}

/// Streaming univariate ANOVA *F* feature filter (river `feature_selection.SelectKBest`).
///
/// Class count is not identification `p`. Scores are class-conditional Welford
/// moments; a thin class is recorded, not treated as a cluster/`p` gate.
#[derive(Clone, Debug)]
pub struct OnlineSelectKBest {
    /// Number of columns to keep.
    pub k: usize,
    classes: Vec<i64>,
    n_per: Vec<f64>,
    mean: Matrix,
    m2: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineSelectKBest {
    fn default() -> Self {
        Self {
            k: 1,
            classes: Vec::new(),
            n_per: Vec::new(),
            mean: Matrix::zeros(0, 0),
            m2: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineSelectKBest {
    /// Keep `k` columns with the largest running ANOVA *F*.
    pub fn new(k: usize) -> Self {
        Self {
            k,
            ..Self::default()
        }
    }

    fn scores(&self) -> Vector {
        let p = self.mean.ncols();
        let k = self.classes.len();
        if k < 2 || p == 0 {
            return Vector::zeros(p);
        }
        Vector::from_iter((0..p).map(|j| {
            let mut ntot = 0.0;
            let mut grand = 0.0;
            for c in 0..k {
                ntot += self.n_per[c];
                grand += self.n_per[c] * self.mean.get(c, j);
            }
            if ntot <= 0.0 {
                return 0.0;
            }
            grand /= ntot;
            let mut ssb = 0.0;
            let mut ssw = 0.0;
            for c in 0..k {
                let d = self.mean.get(c, j) - grand;
                ssb += self.n_per[c] * d * d;
                ssw += self.m2.get(c, j);
            }
            let dfb = (k as f64 - 1.0).max(1.0);
            let dfw = (ntot - k as f64).max(1.0);
            if ssw <= 1e-18 {
                return 0.0;
            }
            (ssb / dfb) / (ssw / dfw)
        }))
    }
}

impl PartialFit for OnlineSelectKBest {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let p = x.ncols();
        if self.k == 0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("OnlineSelectKBest.k=0; transform will keep no columns")
                    .build(),
            );
        }
        if self.initialized && self.mean.ncols() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            if !y[i].is_finite() {
                continue;
            }
            let lab = y[i].round() as i64;
            let idx = if let Some(t) = self.classes.iter().position(|&c| c == lab) {
                t
            } else {
                self.classes.push(lab);
                self.n_per.push(0.0);
                let kc = self.classes.len();
                let mut nm = Matrix::zeros(kc, p);
                let mut n2 = Matrix::zeros(kc, p);
                for r in 0..self.mean.nrows() {
                    for c in 0..p.min(self.mean.ncols()) {
                        nm.set(r, c, self.mean.get(r, c));
                        n2.set(r, c, self.m2.get(r, c));
                    }
                }
                self.mean = nm;
                self.m2 = n2;
                self.initialized = true;
                kc - 1
            };
            self.n_per[idx] += 1.0;
            let n = self.n_per[idx];
            for j in 0..p {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let d = v - self.mean.get(idx, j);
                self.mean.set(idx, j, self.mean.get(idx, j) + d / n);
                let d2 = v - self.mean.get(idx, j);
                self.m2.set(idx, j, self.m2.get(idx, j) + d * d2);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        if self.classes.len() < 2 {
            ctx.push(
                Issue::builder(IssueCode::InsufficientSample)
                    .severity(Severity::Warning)
                    .message("OnlineSelectKBest has <2 classes; ANOVA F is unidentified")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some((self.n_seen - before_n) as f64);
        q.still_identified = self.classes.len() >= 2 && self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("online SelectKBest ANOVA on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-conditional moments",
                "Welford update then ANOVA F ranking",
                format!("n={before_n}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for OnlineSelectKBest {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), 0));
        }
        let scores = self.scores();
        let p = scores.len().min(x.ncols());
        let k = self.k.min(p);
        let mut order: Vec<usize> = (0..p).collect();
        order.sort_by(|a, b| {
            scores[*b]
                .partial_cmp(&scores[*a])
                .unwrap_or(std::cmp::Ordering::Equal)
                .then(a.cmp(b))
        });
        order.truncate(k);
        order.sort_unstable();
        let out = Matrix::from_fn(x.nrows(), k, |i, j| x.get(i, order[j]));
        ctx.finish(out)
    }
}

/// Rolling mean absolute error (river `metrics.MAE`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMae {
    sae: f64,
    n: u64,
    updates: u64,
}

impl OnlineMae {
    /// Empty MAE.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MAE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.sae / self.n as f64
        }
    }
}

impl PartialFit for OnlineMae {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(session, "mae", x, y, self.updates, self.n, |pred, truth| {
            self.n += 1;
            self.sae += (pred - truth).abs();
            self.updates += 1;
            self.score()
        })
    }
}

/// Rolling symmetric MAPE (river `metrics.SMAPE`).
#[derive(Clone, Debug, Default)]
pub struct OnlineSmape {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineSmape {
    /// Empty SMAPE.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current SMAPE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineSmape {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "smape",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                self.n += 1;
                let den = pred.abs() + truth.abs();
                if den > 1e-12 {
                    self.acc += 2.0 * (pred - truth).abs() / den;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Rolling Huber loss (river `metrics.Huber`).
#[derive(Clone, Debug)]
pub struct OnlineHuber {
    /// Huber threshold.
    pub delta: f64,
    acc: f64,
    n: u64,
    updates: u64,
}

impl Default for OnlineHuber {
    fn default() -> Self {
        Self {
            delta: 1.0,
            acc: 0.0,
            n: 0,
            updates: 0,
        }
    }
}

impl OnlineHuber {
    /// Huber metric with threshold `delta`.
    pub fn new(delta: f64) -> Self {
        Self {
            delta,
            ..Self::default()
        }
    }

    /// Current mean Huber loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineHuber {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let d = if self.delta.is_finite() && self.delta > 0.0 {
            self.delta
        } else {
            1.0
        };
        metric_partial(
            session,
            "huber",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                self.n += 1;
                let e = (pred - truth).abs();
                self.acc += if e <= d {
                    0.5 * e * e
                } else {
                    d * (e - 0.5 * d)
                };
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Rolling pinball / quantile loss (river `metrics.Pinball`).
#[derive(Clone, Debug)]
pub struct OnlinePinball {
    /// Quantile \(\tau\in(0,1)\).
    pub tau: f64,
    acc: f64,
    n: u64,
    updates: u64,
}

impl Default for OnlinePinball {
    fn default() -> Self {
        Self {
            tau: 0.5,
            acc: 0.0,
            n: 0,
            updates: 0,
        }
    }
}

impl OnlinePinball {
    /// Pinball metric at quantile `tau`.
    pub fn new(tau: f64) -> Self {
        Self {
            tau,
            ..Self::default()
        }
    }

    /// Current mean pinball loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlinePinball {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let tau = if self.tau.is_finite() && self.tau > 0.0 && self.tau < 1.0 {
            self.tau
        } else {
            0.5
        };
        metric_partial(
            session,
            "pinball",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                self.n += 1;
                let e = truth - pred;
                self.acc += if e >= 0.0 { tau * e } else { (tau - 1.0) * e };
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Rolling log-loss (river `metrics.LogLoss`).
///
/// `x` is treated as a probability and clipped to \((\varepsilon,1-\varepsilon)\).
#[derive(Clone, Debug, Default)]
pub struct OnlineLogLoss {
    nll: f64,
    n: u64,
    updates: u64,
}

impl OnlineLogLoss {
    /// Empty log-loss.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean log-loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.nll / self.n as f64
        }
    }
}

impl PartialFit for OnlineLogLoss {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "log_loss",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                let p = pred.clamp(1e-12, 1.0 - 1e-12);
                let t = if truth > 0.5 { 1.0 } else { 0.0 };
                self.nll += -(t * p.ln() + (1.0 - t) * (1.0 - p).ln());
                self.n += 1;
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Windowed ROC AUC (river `metrics.ROCAUC`).
///
/// Pairs are kept in a 256-row buffer; the AUC is the Mann–Whitney U on that
/// window, not the exact stream AUC.
#[derive(Clone, Debug)]
pub struct OnlineRocAuc {
    scores: Vec<f64>,
    labels: Vec<f64>,
    updates: u64,
}

impl Default for OnlineRocAuc {
    fn default() -> Self {
        Self {
            scores: Vec::new(),
            labels: Vec::new(),
            updates: 0,
        }
    }
}

impl OnlineRocAuc {
    /// Empty windowed AUC.
    pub fn new() -> Self {
        Self::default()
    }

    /// Mann–Whitney AUC on the current window.
    pub fn score(&self) -> f64 {
        let mut pos = Vec::new();
        let mut neg = Vec::new();
        for (s, y) in self.scores.iter().zip(&self.labels) {
            if *y > 0.5 {
                pos.push(*s);
            } else {
                neg.push(*s);
            }
        }
        if pos.is_empty() || neg.is_empty() {
            return f64::NAN;
        }
        let mut ge = 0.0;
        for &p in &pos {
            for &n in &neg {
                if p > n {
                    ge += 1.0;
                } else if (p - n).abs() < 1e-15 {
                    ge += 0.5;
                }
            }
        }
        ge / (pos.len() * neg.len()) as f64
    }
}

impl PartialFit for OnlineRocAuc {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let before = self.scores.len() as u64;
        metric_partial(
            session,
            "roc_auc",
            x,
            y,
            self.updates,
            before,
            |pred, truth| {
                self.scores.push(pred);
                self.labels.push(truth);
                if self.scores.len() > 256 {
                    self.scores.remove(0);
                    self.labels.remove(0);
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming multinomial naïve Bayes (river `naive_bayes.MultinomialNB`).
///
/// Class count is not identification `p`. Negative counts are skipped.
#[derive(Clone, Debug)]
pub struct OnlineMultinomialNb {
    /// Additive smoothing.
    pub alpha: f64,
    classes: Vec<i64>,
    counts: Matrix,
    class_n: Vec<f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineMultinomialNb {
    fn default() -> Self {
        Self {
            alpha: 1.0,
            classes: Vec::new(),
            counts: Matrix::zeros(0, 0),
            class_n: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineMultinomialNb {
    /// Empty multinomial NB.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineMultinomialNb {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let p = x.ncols();
        if self.initialized && self.counts.ncols() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        for i in 0..x.nrows() {
            if !y[i].is_finite() {
                continue;
            }
            let lab = y[i].round() as i64;
            let idx = if let Some(t) = self.classes.iter().position(|&c| c == lab) {
                t
            } else {
                self.classes.push(lab);
                self.class_n.push(0.0);
                let k = self.classes.len();
                let mut nc = Matrix::zeros(k, p);
                for r in 0..self.counts.nrows() {
                    for c in 0..p.min(self.counts.ncols()) {
                        nc.set(r, c, self.counts.get(r, c));
                    }
                }
                self.counts = nc;
                self.initialized = true;
                k - 1
            };
            self.class_n[idx] += 1.0;
            for j in 0..p {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                if v < 0.0 {
                    ctx.push(
                        Issue::builder(IssueCode::InvalidWeight)
                            .severity(Severity::Warning)
                            .message(format!(
                                "OnlineMultinomialNb skipped negative count x[{i},{j}]={v}"
                            ))
                            .build(),
                    );
                    continue;
                }
                self.counts.set(idx, j, self.counts.get(idx, j) + v);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some((self.n_seen - before) as f64);
        q.still_identified = self.classes.len() >= 2 && self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("online MultinomialNB on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-conditional token counts",
                "accumulate non-negative counts",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineMultinomialNb {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.classes.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.classes.len();
        let p = self.counts.ncols();
        let tot: f64 = self.class_n.iter().sum();
        let alpha = self.alpha.max(0.0);
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bv = f64::NEG_INFINITY;
            for c in 0..k {
                let mut s = ((self.class_n[c] + alpha) / (tot + alpha * k as f64)).ln();
                let row_tot: f64 =
                    (0..p).map(|j| self.counts.get(c, j)).sum::<f64>() + alpha * p as f64;
                for j in 0..x.ncols().min(p) {
                    let v = x.get(i, j).max(0.0);
                    let lp = (self.counts.get(c, j) + alpha).ln() - row_tot.ln();
                    s += v * lp;
                }
                if s > bv {
                    bv = s;
                    best = c;
                }
            }
            self.classes[best] as f64
        }));
        ctx.finish(y)
    }
}

/// Complement naive Bayes (river `naive_bayes.ComplementNB`).
///
/// Class count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineComplementNb {
    inner: OnlineMultinomialNb,
}

impl OnlineComplementNb {
    /// Empty complement NB.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineComplementNb {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for OnlineComplementNb {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.inner.initialized || self.inner.classes.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.inner.classes.len();
        let p = self.inner.counts.ncols();
        let alpha = self.inner.alpha.max(0.0);
        let tot_feat: Vec<f64> = (0..p)
            .map(|j| (0..k).map(|c| self.inner.counts.get(c, j)).sum::<f64>())
            .collect();
        let tot_all: f64 = tot_feat.iter().sum();
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bv = f64::NEG_INFINITY;
            for c in 0..k {
                let mut s = 0.0;
                let comp_tot = (tot_all
                    - (0..p).map(|j| self.inner.counts.get(c, j)).sum::<f64>())
                    + alpha * p as f64;
                for j in 0..x.ncols().min(p) {
                    let v = x.get(i, j).max(0.0);
                    let comp = (tot_feat[j] - self.inner.counts.get(c, j) + alpha).max(1e-12);
                    s -= v * (comp.ln() - comp_tot.max(1e-12).ln());
                }
                if s > bv {
                    bv = s;
                    best = c;
                }
            }
            self.inner.classes[best] as f64
        }));
        ctx.finish(y)
    }
}

/// Last-observation imputer (river `imputation.PreviousImputer`).
#[derive(Clone, Debug)]
pub struct PreviousImputer {
    last: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PreviousImputer {
    fn default() -> Self {
        Self {
            last: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PreviousImputer {
    /// Empty previous-value imputer.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for PreviousImputer {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.last = Vector::from_iter((0..x.ncols()).map(|_| f64::NAN));
            self.initialized = true;
        } else if self.last.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.last.clone();
        for i in 0..x.nrows() {
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if v.is_finite() {
                    self.last[j] = v;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut delta: f64 = 0.0;
        for j in 0..self.last.len() {
            if before[j].is_finite() && self.last[j].is_finite() {
                delta += (self.last[j] - before[j]).abs();
            }
        }
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta);
        q.information_gain = Some(delta.max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.last.as_slice().iter().any(|v| v.is_finite());
        q.warmup = !q.still_identified;
        q.explanation = format!("PreviousImputer last-value update on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "last finite value per column",
                "overwrite on each finite observation",
                "previous last values",
                "updated last values",
            ),
        )
    }
}

impl Transform for PreviousImputer {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.last.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let v = x.get(i, j);
            if v.is_finite() {
                return v;
            }
            if j < p && self.last[j].is_finite() {
                self.last[j]
            } else {
                0.0
            }
        });
        for j in 0..p {
            if !self.last[j].is_finite() {
                ctx.push(
                    Issue::builder(IssueCode::ImputationUndefined)
                        .severity(Severity::Warning)
                        .message(format!(
                            "PreviousImputer column {j} has never been observed finite"
                        ))
                        .build(),
                );
            }
        }
        ctx.finish(out)
    }
}

/// Exponentially weighted standard scaler (river `preprocessing.AdaptiveStandardScaler`).
#[derive(Clone, Debug)]
pub struct AdaptiveStandardScaler {
    /// Forgetting factor in `(0, 1]`.
    pub fading: f64,
    mean: Vector,
    var: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaptiveStandardScaler {
    fn default() -> Self {
        Self {
            fading: 0.6,
            mean: Vector::zeros(0),
            var: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaptiveStandardScaler {
    /// Scaler with fading factor `fading`.
    pub fn new(fading: f64) -> Self {
        Self {
            fading,
            ..Self::default()
        }
    }
}

impl PartialFit for AdaptiveStandardScaler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let fade = if self.fading.is_finite() && self.fading > 0.0 && self.fading <= 1.0 {
            self.fading
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "AdaptiveStandardScaler.fading={} is not in (0,1]; using 0.6",
                        self.fading
                    ))
                    .build(),
            );
            0.6
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.mean = Vector::zeros(x.ncols());
            self.var = Vector::from_iter((0..x.ncols()).map(|_| 1.0));
            self.initialized = true;
        } else if self.mean.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.mean.clone();
        for i in 0..x.nrows() {
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let d = v - self.mean[j];
                self.mean[j] += fade * d;
                self.var[j] = (1.0 - fade) * self.var[j] + fade * d * d;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.mean.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = (1.0 / fade).min(self.n_seen as f64);
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(fade));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "adaptive standard scaler on {} rows, fade={fade}",
            x.nrows()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "EW mean/variance",
                "exponentially weighted moment update",
                "previous moments",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

impl Transform for AdaptiveStandardScaler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.mean.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p {
                return x.get(i, j);
            }
            let s = self.var[j].sqrt().max(1e-12);
            (x.get(i, j) - self.mean[j]) / s
        });
        ctx.finish(out)
    }
}

/// Streaming Gaussian anomaly score (river `anomaly.GaussianScorer`).
#[derive(Clone, Debug)]
pub struct GaussianScorer {
    mean: Vector,
    m2: Vector,
    count: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for GaussianScorer {
    fn default() -> Self {
        Self {
            mean: Vector::zeros(0),
            m2: Vector::zeros(0),
            count: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl GaussianScorer {
    /// Empty Gaussian scorer.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for GaussianScorer {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.mean = Vector::zeros(x.ncols());
            self.m2 = Vector::zeros(x.ncols());
            self.count = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.mean.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.mean.clone();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                self.count[j] += 1.0;
                let n = self.count[j];
                let d = v - self.mean[j];
                self.mean[j] += d / n;
                let d2 = v - self.mean[j];
                self.m2[j] += d * d2;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.mean.sub(&before);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.count.as_slice().iter().all(|&c| c >= 2.0);
        q.warmup = self.n_seen < 4;
        q.explanation = format!("GaussianScorer Welford update on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "column mean/variance",
                "Welford update for a Gaussian tail score",
                "previous moments",
                "updated moments",
            ),
        )
    }
}

impl Transform for GaussianScorer {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), x.ncols()));
        }
        let p = x.ncols().min(self.mean.len());
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if j >= p || self.count[j] < 2.0 {
                return 0.0;
            }
            let var = (self.m2[j] / self.count[j]).max(1e-12);
            let z = (x.get(i, j) - self.mean[j]) / var.sqrt();
            z.abs()
        });
        ctx.finish(out)
    }
}

/// Streaming F1 (river `metrics.F1`).
#[derive(Clone, Debug, Default)]
pub struct OnlineF1 {
    tp: f64,
    fp: f64,
    fn_: f64,
    updates: u64,
}

impl OnlineF1 {
    /// Empty F1.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current F1, or NaN when no positive predictions or labels.
    pub fn score(&self) -> f64 {
        let den = 2.0 * self.tp + self.fp + self.fn_;
        if den <= 0.0 {
            f64::NAN
        } else {
            2.0 * self.tp / den
        }
    }
}

impl PartialFit for OnlineF1 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "f1",
            x,
            y,
            self.updates,
            self.tp as u64,
            |pred, truth| {
                let p = pred > 0.5;
                let t = truth > 0.5;
                if p && t {
                    self.tp += 1.0;
                } else if p && !t {
                    self.fp += 1.0;
                } else if !p && t {
                    self.fn_ += 1.0;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming precision (river `metrics.Precision`).
#[derive(Clone, Debug, Default)]
pub struct OnlinePrecision {
    tp: f64,
    fp: f64,
    updates: u64,
}

impl OnlinePrecision {
    /// Empty precision.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current precision, or NaN when no positive predictions.
    pub fn score(&self) -> f64 {
        let den = self.tp + self.fp;
        if den <= 0.0 {
            f64::NAN
        } else {
            self.tp / den
        }
    }
}

impl PartialFit for OnlinePrecision {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "precision",
            x,
            y,
            self.updates,
            self.tp as u64,
            |pred, truth| {
                if pred > 0.5 {
                    if truth > 0.5 {
                        self.tp += 1.0;
                    } else {
                        self.fp += 1.0;
                    }
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming recall (river `metrics.Recall`).
#[derive(Clone, Debug, Default)]
pub struct OnlineRecall {
    tp: f64,
    fn_: f64,
    updates: u64,
}

impl OnlineRecall {
    /// Empty recall.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current recall, or NaN when no positive labels.
    pub fn score(&self) -> f64 {
        let den = self.tp + self.fn_;
        if den <= 0.0 {
            f64::NAN
        } else {
            self.tp / den
        }
    }
}

impl PartialFit for OnlineRecall {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "recall",
            x,
            y,
            self.updates,
            self.tp as u64,
            |pred, truth| {
                if truth > 0.5 {
                    if pred > 0.5 {
                        self.tp += 1.0;
                    } else {
                        self.fn_ += 1.0;
                    }
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming Bernoulli naïve Bayes (river `naive_bayes.BernoulliNB`).
///
/// Class count is not identification `p`. Features are treated as
/// \(\{0,1\}\) via a \(0.5\) threshold.
#[derive(Clone, Debug)]
pub struct OnlineBernoulliNb {
    /// Additive smoothing.
    pub alpha: f64,
    classes: Vec<i64>,
    ones: Matrix,
    class_n: Vec<f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineBernoulliNb {
    fn default() -> Self {
        Self {
            alpha: 1.0,
            classes: Vec::new(),
            ones: Matrix::zeros(0, 0),
            class_n: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineBernoulliNb {
    /// Empty Bernoulli NB.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineBernoulliNb {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let p = x.ncols();
        if self.initialized && self.ones.ncols() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        for i in 0..x.nrows() {
            if !y[i].is_finite() {
                continue;
            }
            let lab = y[i].round() as i64;
            let idx = if let Some(t) = self.classes.iter().position(|&c| c == lab) {
                t
            } else {
                self.classes.push(lab);
                self.class_n.push(0.0);
                let k = self.classes.len();
                let mut nc = Matrix::zeros(k, p);
                for r in 0..self.ones.nrows() {
                    for c in 0..p.min(self.ones.ncols()) {
                        nc.set(r, c, self.ones.get(r, c));
                    }
                }
                self.ones = nc;
                self.initialized = true;
                k - 1
            };
            self.class_n[idx] += 1.0;
            for j in 0..p {
                if x.get(i, j) > 0.5 {
                    self.ones.set(idx, j, self.ones.get(idx, j) + 1.0);
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some((self.n_seen - before) as f64);
        q.still_identified = self.classes.len() >= 2 && self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("online BernoulliNB on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-conditional Bernoulli ones counts",
                "threshold features at 0.5 and accumulate",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineBernoulliNb {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.classes.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.classes.len();
        let p = self.ones.ncols();
        let tot: f64 = self.class_n.iter().sum();
        let alpha = self.alpha.max(0.0);
        let y = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bv = f64::NEG_INFINITY;
            for c in 0..k {
                let mut s = ((self.class_n[c] + alpha) / (tot + alpha * k as f64)).ln();
                for j in 0..x.ncols().min(p) {
                    let phat =
                        (self.ones.get(c, j) + alpha) / (self.class_n[c] + 2.0 * alpha).max(1e-12);
                    let phat = phat.clamp(1e-12, 1.0 - 1e-12);
                    let on = if x.get(i, j) > 0.5 { 1.0 } else { 0.0 };
                    s += on * phat.ln() + (1.0 - on) * (1.0 - phat).ln();
                }
                if s > bv {
                    bv = s;
                    best = c;
                }
            }
            self.classes[best] as f64
        }));
        ctx.finish(y)
    }
}

/// Exponentially weighted average of a mean expert and a last-value expert
/// (river `optim.EWARegressor` lite).
///
/// Features are ignored; the mix is a two-expert forecast of the target.
#[derive(Clone, Debug)]
pub struct EwaRegressor {
    /// Multiplicative weight step \(\eta > 0\).
    pub eta: f64,
    w_mean: f64,
    w_last: f64,
    mean: f64,
    last: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for EwaRegressor {
    fn default() -> Self {
        Self {
            eta: 0.5,
            w_mean: 1.0,
            w_last: 1.0,
            mean: 0.0,
            last: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl EwaRegressor {
    /// EWA mix with step `eta`.
    pub fn new(eta: f64) -> Self {
        Self {
            eta,
            ..Self::default()
        }
    }

    fn mix(&self) -> f64 {
        let z = self.w_mean + self.w_last;
        if z <= 0.0 {
            self.mean
        } else {
            (self.w_mean * self.mean + self.w_last * self.last) / z
        }
    }
}

impl PartialFit for EwaRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let eta = if self.eta.is_finite() && self.eta > 0.0 {
            self.eta
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "EwaRegressor.eta={} is not positive; using 0.5",
                        self.eta
                    ))
                    .build(),
            );
            0.5
        };
        ctx.push(
            Issue::builder(IssueCode::CausalClaimUnidentified)
                .severity(Severity::Advisory)
                .message(
                    "EwaRegressor mixes a running-mean expert and a last-value expert; x is unused",
                )
                .compromise(NumericalCompromise::new(
                    "feature-conditioned expert mix",
                    "two target-only experts with multiplicative weights",
                    "no model of x → y is identified",
                    "read the mix as an online forecast of y, not a feature effect",
                ))
                .build(),
        );
        let before = self.mix();
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yt = y[i];
            if self.initialized {
                let e_mean = (self.mean - yt) * (self.mean - yt);
                let e_last = (self.last - yt) * (self.last - yt);
                self.w_mean *= (-eta * e_mean).exp();
                self.w_last *= (-eta * e_last).exp();
                let z = (self.w_mean + self.w_last).max(1e-18);
                self.w_mean = (self.w_mean / z).max(1e-12);
                self.w_last = (self.w_last / z).max(1e-12);
                let n = self.n_seen as f64;
                self.mean = (self.mean * n + yt) / (n + 1.0);
                self.last = yt;
            } else {
                self.mean = yt;
                self.last = yt;
                self.initialized = true;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.mix();
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some((after - before).abs().max(1.0 / self.n_seen.max(1) as f64));
        q.still_identified = self.initialized && self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("EWA mix after {} rows, pred={after:.6e}", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "mean/last expert weights",
                "multiplicative loss update, then renormalize",
                format!("mix={before:.6e}"),
                format!("mix={after:.6e}"),
            ),
        )
    }
}

impl Predict for EwaRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let m = self.mix();
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|_| m)))
    }
}

/// Running-quantile clip (river `anomaly.QuantileFilter` / winsorize).
///
/// Values above the empirical `q`-quantile of a 256-row window are clipped
/// to that quantile. `q` is not identification `p`.
#[derive(Clone, Debug)]
pub struct QuantileFilter {
    /// Upper quantile in \((0, 1)\).
    pub q: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for QuantileFilter {
    fn default() -> Self {
        Self {
            q: 0.95,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl QuantileFilter {
    /// Filter at quantile `q`.
    pub fn new(q: f64) -> Self {
        Self {
            q,
            ..Self::default()
        }
    }

    fn quantile(&self, q: f64) -> f64 {
        if self.window.is_empty() {
            return f64::NAN;
        }
        let mut xs = self.window.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let idx = ((q.clamp(0.0, 1.0)) * (xs.len() - 1) as f64).round() as usize;
        xs[idx.min(xs.len() - 1)]
    }
}

impl PartialFit for QuantileFilter {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "QuantileFilter.q={} is not in (0,1); using 0.95",
                        self.q
                    ))
                    .build(),
            );
            0.95
        };
        let before = self.quantile(qv);
        for i in 0..x.nrows() {
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if v.is_finite() {
                    self.window.push(v);
                    if self.window.len() > 256 {
                        self.window.remove(0);
                    }
                    self.n_seen += 1;
                }
            }
        }
        self.updates += 1;
        let after = self.quantile(qv);
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.window.len() >= 4;
        q.warmup = self.window.len() < 4;
        q.explanation = format!("QuantileFilter q={qv:.3} threshold={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "empirical upper quantile",
                "256-row sliding window order statistic",
                format!("q={before:.6e}"),
                format!("q={after:.6e}"),
            ),
        )
    }
}

impl Transform for QuantileFilter {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if self.window.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            0.95
        };
        let cap = self.quantile(qv);
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let v = x.get(i, j);
            if v.is_finite() && v > cap {
                cap
            } else {
                v
            }
        });
        ctx.finish(out)
    }
}

/// Streaming RMSE (river `metrics.RMSE`).
#[derive(Clone, Debug, Default)]
pub struct OnlineRmse {
    sse: f64,
    n: u64,
    updates: u64,
}

impl OnlineRmse {
    /// Empty RMSE.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current RMSE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            (self.sse / self.n as f64).sqrt()
        }
    }
}

impl PartialFit for OnlineRmse {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "rmse",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                let e = pred - truth;
                self.sse += e * e;
                self.n += 1;
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming Cohen's κ (river `metrics.CohenKappa`).
#[derive(Clone, Debug, Default)]
pub struct OnlineCohenKappa {
    a: f64,
    b: f64,
    c: f64,
    d: f64,
    updates: u64,
}

impl OnlineCohenKappa {
    /// Empty κ.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current κ, or NaN when empty.
    pub fn score(&self) -> f64 {
        let n = self.a + self.b + self.c + self.d;
        if n <= 0.0 {
            return f64::NAN;
        }
        let po = (self.a + self.d) / n;
        let pe = ((self.a + self.b) * (self.a + self.c) + (self.c + self.d) * (self.b + self.d))
            / (n * n);
        if (1.0 - pe).abs() <= 1e-18 {
            f64::NAN
        } else {
            (po - pe) / (1.0 - pe)
        }
    }
}

impl PartialFit for OnlineCohenKappa {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "cohen_kappa",
            x,
            y,
            self.updates,
            (self.a + self.b + self.c + self.d) as u64,
            |pred, truth| {
                let p = pred > 0.5;
                let t = truth > 0.5;
                match (p, t) {
                    (false, false) => self.a += 1.0,
                    (true, false) => self.b += 1.0,
                    (false, true) => self.c += 1.0,
                    (true, true) => self.d += 1.0,
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Exponentially weighted mean (river `stats.EWMean`).
#[derive(Clone, Debug)]
pub struct EwMean {
    /// Fade factor in \((0, 1]\). `1` is the expanding mean.
    pub fading: f64,
    mean: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for EwMean {
    fn default() -> Self {
        Self {
            fading: 0.5,
            mean: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl EwMean {
    /// EW mean with fade `fading`.
    pub fn new(fading: f64) -> Self {
        Self {
            fading,
            ..Self::default()
        }
    }

    /// Current mean, or NaN before the first update.
    pub fn score(&self) -> f64 {
        if self.initialized {
            self.mean
        } else {
            f64::NAN
        }
    }
}

impl PartialFit for EwMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let fade = if self.fading.is_finite() && self.fading > 0.0 && self.fading <= 1.0 {
            self.fading
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "EwMean.fading={} is not in (0,1]; using 0.5",
                        self.fading
                    ))
                    .build(),
            );
            0.5
        };
        let before = self.mean;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if !self.initialized {
                self.mean = v;
                self.initialized = true;
            } else if fade >= 1.0 {
                let n = self.n_seen as f64;
                self.mean = (self.mean * n + v) / (n + 1.0);
            } else {
                self.mean = fade * self.mean + (1.0 - fade) * v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = if fade >= 1.0 {
            self.n_seen as f64
        } else {
            (1.0 / (1.0 - fade)).min(self.n_seen as f64)
        };
        q.parameter_delta_norm = Some((self.mean - before).abs());
        q.information_gain = Some(
            (self.mean - before)
                .abs()
                .max(1.0 / self.n_seen.max(1) as f64),
        );
        q.still_identified = self.initialized;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("EWMean fade={fade:.3} mean={:.6e}", self.mean);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exponentially weighted mean",
                "fade previous mean and fold in the new observation",
                format!("mean={before:.6e}"),
                format!("mean={:.6e}", self.mean),
            ),
        )
    }
}

impl Predict for EwMean {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|_| self.mean)))
    }
}

/// Degree-2 polynomial expander (river `preprocessing.PolynomialExtender`).
///
/// Expanded width is not identification `p`.
#[derive(Clone, Debug)]
pub struct PolynomialExtender {
    /// Polynomial degree (`1` or `2`).
    pub degree: usize,
    n_in: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PolynomialExtender {
    fn default() -> Self {
        Self {
            degree: 2,
            n_in: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PolynomialExtender {
    /// Expander of the given degree.
    pub fn new(degree: usize) -> Self {
        Self {
            degree,
            ..Self::default()
        }
    }
}

impl PartialFit for PolynomialExtender {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let deg = if self.degree == 1 || self.degree == 2 {
            self.degree
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "PolynomialExtender.degree={} is not 1 or 2; using 2",
                        self.degree
                    ))
                    .build(),
            );
            2
        };
        self.degree = deg;
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if !self.initialized {
            self.n_in = x.ncols();
            self.initialized = true;
        } else if self.n_in != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let p = x.ncols();
        let out_p = if deg == 1 { p } else { p + p + p * (p + 1) / 2 };
        if out_p > 64 {
            ctx.push(
                Issue::builder(IssueCode::PolynomialExplosion)
                    .message(format!("PolynomialExtender maps {p} → {out_p} columns"))
                    .compromise(NumericalCompromise::new(
                        "identified low-order features",
                        "degree-2 monomials including interactions",
                        "the expanded map is wider than a small panel",
                        "do not treat every cross-term as an identified effect",
                    ))
                    .build(),
            );
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.initialized;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("PolynomialExtender degree={deg} on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "polynomial feature map",
                "record input width; expansion is stateless",
                format!("n_in={}", self.n_in),
                format!("n_in={} n={}", self.n_in, self.n_seen),
            ),
        )
    }
}

impl Transform for PolynomialExtender {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let p = x.ncols().min(self.n_in);
        let deg = if self.degree == 1 { 1 } else { 2 };
        let out_p = if deg == 1 { p } else { p + p + p * (p + 1) / 2 };
        let out = Matrix::from_fn(x.nrows(), out_p, |i, j| {
            if j < p {
                return x.get(i, j);
            }
            if j < 2 * p {
                let v = x.get(i, j - p);
                return v * v;
            }
            let mut k = j - 2 * p;
            for a in 0..p {
                let n_right = p - a;
                if k < n_right {
                    let b = a + k;
                    return x.get(i, a) * x.get(i, b);
                }
                k -= n_right;
            }
            0.0
        });
        ctx.finish(out)
    }
}

/// Online target-mean encoder (river `preprocessing.TargetEncoder` / `stats.Mean`).
#[derive(Clone, Debug)]
pub struct TargetMeanEncoder {
    maps: Vec<HashMap<i64, (f64, f64)>>,
    global: f64,
    global_n: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for TargetMeanEncoder {
    fn default() -> Self {
        Self {
            maps: Vec::new(),
            global: 0.0,
            global_n: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl TargetMeanEncoder {
    /// Empty target-mean encoder.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for TargetMeanEncoder {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if !self.initialized {
            self.maps = vec![HashMap::new(); x.ncols()];
            self.initialized = true;
        } else if self.maps.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.global;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            self.global_n += 1.0;
            self.global += (y[i] - self.global) / self.global_n;
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let key = v.round() as i64;
                let e = self.maps[j].entry(key).or_insert((0.0, 0.0));
                e.1 += 1.0;
                e.0 += (y[i] - e.0) / e.1;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.global - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("TargetMeanEncoder global={:.6e}", self.global);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "per-level target means",
                "Welford update of category-conditional means",
                format!("global={before:.6e}"),
                format!("global={:.6e}", self.global),
            ),
        )
    }
}

impl Transform for TargetMeanEncoder {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let v = x.get(i, j);
            if !v.is_finite() {
                return self.global;
            }
            if j >= self.maps.len() {
                return self.global;
            }
            self.maps[j]
                .get(&(v.round() as i64))
                .map(|e| e.0)
                .unwrap_or(self.global)
        });
        ctx.finish(out)
    }
}

/// Poisson-bootstrap bag of perceptrons (river `ensemble.BaggingClassifier`).
#[derive(Clone, Debug)]
pub struct OnlineBagging {
    /// Members.
    pub n_models: usize,
    models: Vec<Perceptron>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for OnlineBagging {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(3),
        }
    }
}

impl OnlineBagging {
    /// Bag of `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineBagging {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(1)];
        }
        let before = self.n_seen;
        for m in 0..self.models.len() {
            let mut rows: Vec<usize> = Vec::new();
            for i in 0..x.nrows().min(y.len()) {
                let k = self.rng.poisson(1.0) as usize;
                for _ in 0..k {
                    rows.push(i);
                }
            }
            if rows.is_empty() && x.nrows() > 0 {
                rows.push(0);
            }
            if rows.is_empty() {
                continue;
            }
            let xb = Matrix::from_fn(rows.len(), x.ncols(), |r, c| x.get(rows[r], c));
            let yb = Vector::from_iter(rows.iter().map(|&i| y[i]));
            let _ = self.models[m].partial_fit(&xb, Some(&yb), &session.child(format!("bag_{m}")));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineBagging {} perceptrons, Poisson bootstrap",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "perceptron committee",
                "each member sees a Poisson(1) bootstrap of the batch",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineBagging {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        let mut k: f64 = 0.0;
        for (t, m) in self.models.iter().enumerate() {
            match m.predict(x, &session.child(format!("p{t}"))) {
                Ok(q) => {
                    for i in 0..x.nrows().min(q.value.len()) {
                        acc[i] += q.value[i];
                    }
                    k += 1.0;
                }
                Err(_) => {}
            }
        }
        let k = k.max(1.0);
        ctx.finish(Vector::from_iter(acc.iter().map(|v| {
            if *v / k > 0.5 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Rolling quantile (river `stats.Quantile`).
#[derive(Clone, Debug)]
pub struct RollingQuantile {
    /// Quantile in \((0, 1)\).
    pub q: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingQuantile {
    fn default() -> Self {
        Self {
            q: 0.5,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingQuantile {
    /// Rolling quantile `q`.
    pub fn new(q: f64) -> Self {
        Self {
            q,
            ..Self::default()
        }
    }

    /// Current quantile, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.window.is_empty() {
            return f64::NAN;
        }
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            0.5
        };
        let mut xs = self.window.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let idx = (qv * (xs.len() - 1) as f64).round() as usize;
        xs[idx.min(xs.len() - 1)]
    }
}

impl PartialFit for RollingQuantile {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.q.is_finite() || self.q <= 0.0 || self.q >= 1.0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "RollingQuantile.q={} is not in (0,1); using 0.5",
                        self.q
                    ))
                    .build(),
            );
            self.q = 0.5;
        }
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if v.is_finite() {
                self.window.push(v);
                if self.window.len() > 256 {
                    self.window.remove(0);
                }
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.window.len() >= 4;
        q.warmup = self.window.len() < 4;
        q.explanation = format!("RollingQuantile q={:.3} value={after:.6e}", self.q);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed order statistic",
                "256-row sliding quantile",
                format!("q={before:.6e}"),
                format!("q={after:.6e}"),
            ),
        )
    }
}

/// Streaming Hamming loss (river `metrics.Hamming`).
#[derive(Clone, Debug, Default)]
pub struct OnlineHamming {
    bad: f64,
    n: u64,
    updates: u64,
}

impl OnlineHamming {
    /// Empty Hamming.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current Hamming loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.bad / self.n as f64
        }
    }
}

impl PartialFit for OnlineHamming {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "hamming",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                self.n += 1;
                if pred.round() != truth.round() {
                    self.bad += 1.0;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming Jaccard index (river `metrics.Jaccard`).
#[derive(Clone, Debug, Default)]
pub struct OnlineJaccard {
    inter: f64,
    union: f64,
    n: u64,
    updates: u64,
}

impl OnlineJaccard {
    /// Empty Jaccard.
    pub fn new() -> Self {
        Self::default()
    }

    /// Intersection over union, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.union <= 0.0 {
            f64::NAN
        } else {
            self.inter / self.union
        }
    }
}

impl PartialFit for OnlineJaccard {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "jaccard",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                let a = pred.round() != 0.0;
                let b = truth.round() != 0.0;
                if a && b {
                    self.inter += 1.0;
                }
                if a || b {
                    self.union += 1.0;
                }
                self.n += 1;
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Exponentially weighted variance (river `stats.EWVar`).
#[derive(Clone, Debug)]
pub struct EwVar {
    fading: f64,
    mean: f64,
    m2: f64,
    w: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for EwVar {
    fn default() -> Self {
        Self {
            fading: 0.5,
            mean: 0.0,
            m2: 0.0,
            w: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl EwVar {
    /// EW variance with fading factor in `(0, 1]`.
    pub fn new(fading: f64) -> Self {
        Self {
            fading,
            ..Self::default()
        }
    }

    /// Current variance, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.w <= 1e-12 {
            f64::NAN
        } else {
            self.m2 / self.w
        }
    }
}

impl PartialFit for EwVar {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let fade = if self.fading.is_finite() && self.fading > 0.0 && self.fading <= 1.0 {
            self.fading
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "EwVar fading={} is not in (0,1]; using 0.5",
                        self.fading
                    ))
                    .build(),
            );
            0.5
        };
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if self.w <= 0.0 {
                self.mean = v;
                self.m2 = 0.0;
                self.w = 1.0;
            } else {
                self.w = fade * self.w + 1.0;
                let d = v - self.mean;
                self.mean += d / self.w;
                self.m2 = fade * self.m2 + d * (v - self.mean);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.w;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("EwVar fade={fade:.3} var={after:.6e}");
        q.loss_after = Some(after);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exponentially weighted second moment",
                "Welford update with geometric fading of past mass",
                format!("var={before:.6e}"),
                format!("var={after:.6e}"),
            ),
        )
    }
}

/// Successive-halving perceptron committee (river `model_selection.SuccessiveHalvingClassifier`).
///
/// Model count is not identification `p`. The worst half is dropped when
/// `n_seen` crosses successive powers of two.
#[derive(Clone, Debug)]
pub struct SuccessiveHalvingClassifier {
    /// Initial candidate count.
    pub n_models: usize,
    models: Vec<Perceptron>,
    hits: Vec<f64>,
    seen: Vec<f64>,
    n_seen: u64,
    updates: u64,
    cuts: u64,
}

impl Default for SuccessiveHalvingClassifier {
    fn default() -> Self {
        Self {
            n_models: 4,
            models: Vec::new(),
            hits: Vec::new(),
            seen: Vec::new(),
            n_seen: 0,
            updates: 0,
            cuts: 0,
        }
    }
}

impl SuccessiveHalvingClassifier {
    /// Committee of `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }

    /// Models still alive.
    pub fn n_alive(&self) -> usize {
        self.models.len()
    }
}

impl PartialFit for SuccessiveHalvingClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.n_models < 2 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "SuccessiveHalving n_models={} < 2; using 2",
                        self.n_models
                    ))
                    .build(),
            );
            self.n_models = 2;
        }
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(2)];
            self.hits = vec![0.0; self.models.len()];
            self.seen = vec![0.0; self.models.len()];
        }
        let before = self.n_seen;
        let n_before = self.models.len();
        for m in 0..self.models.len() {
            let pred = self.models[m]
                .predict(x, &session.child(format!("sh_pre_{m}")))
                .map(|q| q.value)
                .unwrap_or_else(|_| Vector::zeros(x.nrows()));
            for i in 0..x.nrows().min(y.len()).min(pred.len()) {
                if pred[i].round() == y[i].round() {
                    self.hits[m] += 1.0;
                }
                self.seen[m] += 1.0;
            }
            let _ = self.models[m].partial_fit(x, Some(y), &session.child(format!("sh_{m}")));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut cut = false;
        if self.models.len() > 1
            && self.n_seen >= 2
            && (self.n_seen.is_power_of_two() || before < 2)
        {
            let mut order: Vec<usize> = (0..self.models.len()).collect();
            order.sort_by(|&a, &b| {
                let aa = if self.seen[a] > 0.0 {
                    self.hits[a] / self.seen[a]
                } else {
                    0.0
                };
                let bb = if self.seen[b] > 0.0 {
                    self.hits[b] / self.seen[b]
                } else {
                    0.0
                };
                bb.partial_cmp(&aa).unwrap_or(std::cmp::Ordering::Equal)
            });
            let keep = (self.models.len() / 2).max(1);
            let keep_idx = &order[..keep];
            let mut models = Vec::new();
            let mut hits = Vec::new();
            let mut seen = Vec::new();
            for &i in keep_idx {
                models.push(self.models[i].clone());
                hits.push(self.hits[i]);
                seen.push(self.seen[i]);
            }
            if models.len() < n_before {
                cut = true;
                self.cuts += 1;
            }
            self.models = models;
            self.hits = hits;
            self.seen = seen;
        }
        let best = self
            .hits
            .iter()
            .zip(&self.seen)
            .map(|(h, s)| if *s > 0.0 { h / s } else { 0.0 })
            .fold(0.0_f64, f64::max);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if cut { 1.0 } else { 0.0 });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2 && !self.models.is_empty();
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "SuccessiveHalving alive={} cuts={} best_acc={best:.3}",
            self.models.len(),
            self.cuts
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                if cut {
                    "eliminated the worse half of the committee"
                } else {
                    "updated every surviving perceptron"
                },
                "rung promotions drop models with the lowest running accuracy",
                format!("alive={n_before} n={before}"),
                format!(
                    "alive={} n={} acc={best:.3}",
                    self.models.len(),
                    self.n_seen
                ),
            ),
        )
    }
}

impl Predict for SuccessiveHalvingClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        let mut k: f64 = 0.0;
        for (t, m) in self.models.iter().enumerate() {
            match m.predict(x, &session.child(format!("shp_{t}"))) {
                Ok(q) => {
                    for i in 0..x.nrows().min(q.value.len()) {
                        acc[i] += q.value[i];
                    }
                    k += 1.0;
                }
                Err(_) => {}
            }
        }
        let k = k.max(1.0);
        ctx.finish(Vector::from_iter(acc.iter().map(|v| {
            if *v / k > 0.5 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Successive-halving RLS committee (river `model_selection.SuccessiveHalvingRegressor`).
#[derive(Clone, Debug)]
pub struct SuccessiveHalvingRegressor {
    /// Initial candidate count.
    pub n_models: usize,
    models: Vec<LinearRegression>,
    sse: Vec<f64>,
    n_err: Vec<f64>,
    n_seen: u64,
    updates: u64,
    cuts: u64,
}

impl Default for SuccessiveHalvingRegressor {
    fn default() -> Self {
        Self {
            n_models: 4,
            models: Vec::new(),
            sse: Vec::new(),
            n_err: Vec::new(),
            n_seen: 0,
            updates: 0,
            cuts: 0,
        }
    }
}

impl SuccessiveHalvingRegressor {
    /// Committee of `n_models` RLS estimators.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for SuccessiveHalvingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.n_models < 2 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "SuccessiveHalvingRegressor n_models={} < 2; using 2",
                        self.n_models
                    ))
                    .build(),
            );
            self.n_models = 2;
        }
        if self.models.is_empty() {
            self.models = (0..self.n_models.max(2))
                .map(|i| {
                    let mut m = LinearRegression::new(1.0);
                    m.p0 = 10.0 * (i as f64 + 1.0);
                    m.fit_intercept = true;
                    m
                })
                .collect();
            self.sse = vec![0.0; self.models.len()];
            self.n_err = vec![0.0; self.models.len()];
        }
        let before = self.n_seen;
        let n_before = self.models.len();
        for m in 0..self.models.len() {
            let pred = self.models[m]
                .predict(x, &session.child(format!("shr_pre_{m}")))
                .map(|q| q.value)
                .unwrap_or_else(|_| Vector::zeros(x.nrows()));
            for i in 0..x.nrows().min(y.len()).min(pred.len()) {
                let e = pred[i] - y[i];
                self.sse[m] += e * e;
                self.n_err[m] += 1.0;
            }
            let _ = self.models[m].partial_fit(x, Some(y), &session.child(format!("shr_{m}")));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut cut = false;
        if self.models.len() > 1
            && self.n_seen >= 2
            && (self.n_seen.is_power_of_two() || before < 2)
        {
            let mut order: Vec<usize> = (0..self.models.len()).collect();
            order.sort_by(|&a, &b| {
                let aa = if self.n_err[a] > 0.0 {
                    self.sse[a] / self.n_err[a]
                } else {
                    f64::INFINITY
                };
                let bb = if self.n_err[b] > 0.0 {
                    self.sse[b] / self.n_err[b]
                } else {
                    f64::INFINITY
                };
                aa.partial_cmp(&bb).unwrap_or(std::cmp::Ordering::Equal)
            });
            let keep = (self.models.len() / 2).max(1);
            let keep_idx = &order[..keep];
            let mut models = Vec::new();
            let mut sse = Vec::new();
            let mut n_err = Vec::new();
            for &i in keep_idx {
                models.push(self.models[i].clone());
                sse.push(self.sse[i]);
                n_err.push(self.n_err[i]);
            }
            if models.len() < n_before {
                cut = true;
                self.cuts += 1;
            }
            self.models = models;
            self.sse = sse;
            self.n_err = n_err;
        }
        let best = self
            .sse
            .iter()
            .zip(&self.n_err)
            .map(|(s, n)| if *n > 0.0 { s / n } else { f64::INFINITY })
            .fold(f64::INFINITY, f64::min);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if cut { 1.0 } else { 0.0 });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2 && !self.models.is_empty();
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "SuccessiveHalvingRegressor alive={} cuts={} best_mse={best:.6e}",
            self.models.len(),
            self.cuts
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                if cut {
                    "eliminated the worse half of the RLS committee"
                } else {
                    "updated every surviving RLS member"
                },
                "rung promotions drop models with the highest running MSE",
                format!("alive={n_before} n={before}"),
                format!(
                    "alive={} n={} mse={best:.6e}",
                    self.models.len(),
                    self.n_seen
                ),
            ),
        )
    }
}

impl Predict for SuccessiveHalvingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        let mut k: f64 = 0.0;
        for (t, m) in self.models.iter().enumerate() {
            match m.predict(x, &session.child(format!("shrp_{t}"))) {
                Ok(q) => {
                    for i in 0..x.nrows().min(q.value.len()) {
                        acc[i] += q.value[i];
                    }
                    k += 1.0;
                }
                Err(_) => {}
            }
        }
        let k = k.max(1.0);
        ctx.finish(Vector::from_iter(acc.iter().map(|v| v / k)))
    }
}

/// Aggregated Mondrian Forest lite (river `forest.AMFClassifier`).
///
/// Tree count is not identification `p`. Each tree grows at most one
/// axis-aligned stump once two labelled rows have been seen.
#[derive(Clone, Debug)]
pub struct AmfClassifier {
    /// Trees.
    pub n_trees: usize,
    trees: Vec<AmfTree>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

#[derive(Clone, Debug)]
struct AmfTree {
    p: usize,
    n: u64,
    lo: Vector,
    hi: Vector,
    split_j: Option<usize>,
    split_t: f64,
    leaf: HashMap<i64, f64>,
    left: HashMap<i64, f64>,
    right: HashMap<i64, f64>,
}

impl Default for AmfClassifier {
    fn default() -> Self {
        Self {
            n_trees: 4,
            trees: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(29),
        }
    }
}

impl AmfClassifier {
    /// AMF with `n_trees` Mondrian stumps.
    pub fn new(n_trees: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            ..Self::default()
        }
    }
}

fn amf_majority(counts: &HashMap<i64, f64>) -> f64 {
    counts
        .iter()
        .max_by(|a, b| {
            a.1.partial_cmp(b.1)
                .unwrap_or(std::cmp::Ordering::Equal)
                .then(b.0.cmp(a.0))
        })
        .map(|(k, _)| *k as f64)
        .unwrap_or(0.0)
}

impl PartialFit for AmfClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.trees.is_empty() {
            self.trees = (0..self.n_trees.max(1))
                .map(|_| AmfTree {
                    p: x.ncols(),
                    n: 0,
                    lo: Vector::filled(x.ncols(), f64::INFINITY),
                    hi: Vector::filled(x.ncols(), f64::NEG_INFINITY),
                    split_j: None,
                    split_t: 0.0,
                    leaf: HashMap::new(),
                    left: HashMap::new(),
                    right: HashMap::new(),
                })
                .collect();
        } else if self.trees[0].p != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        let mut splits = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let lab = y[i].round() as i64;
            for t in &mut self.trees {
                for j in 0..x.ncols().min(t.lo.len()) {
                    let v = x.get(i, j);
                    if v < t.lo[j] {
                        t.lo[j] = v;
                    }
                    if v > t.hi[j] {
                        t.hi[j] = v;
                    }
                }
                t.n += 1;
                if let Some(j) = t.split_j {
                    let v = if j < x.ncols() { x.get(i, j) } else { 0.0 };
                    if v <= t.split_t {
                        *t.left.entry(lab).or_insert(0.0) += 1.0;
                    } else {
                        *t.right.entry(lab).or_insert(0.0) += 1.0;
                    }
                } else {
                    *t.leaf.entry(lab).or_insert(0.0) += 1.0;
                    if t.n >= 2 {
                        let mut best_j = 0usize;
                        let mut best_r = 0.0;
                        for j in 0..t.lo.len() {
                            let r = t.hi[j] - t.lo[j];
                            if r > best_r {
                                best_r = r;
                                best_j = j;
                            }
                        }
                        if best_r > 1e-12 {
                            let u = self.rng.uniform();
                            t.split_j = Some(best_j);
                            t.split_t = t.lo[best_j] + u * best_r;
                            t.left = t.leaf.clone();
                            t.right = HashMap::new();
                            splits += 1;
                        }
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(splits as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AMF {} Mondrian stumps, {splits} new splits this batch",
            self.trees.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Mondrian stump growth",
                "a leaf splits on its widest feature once two labels have been seen",
                format!("n={before}"),
                format!("n={} splits={splits}", self.n_seen),
            ),
        )
    }
}

impl Predict for AmfClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.trees.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut votes = HashMap::<i64, f64>::new();
            for t in &self.trees {
                let lab = if let Some(j) = t.split_j {
                    let v = if j < x.ncols() { x.get(i, j) } else { 0.0 };
                    if v <= t.split_t {
                        amf_majority(&t.left)
                    } else {
                        amf_majority(&t.right)
                    }
                } else {
                    amf_majority(&t.leaf)
                };
                *votes.entry(lab.round() as i64).or_insert(0.0) += 1.0;
            }
            votes
                .iter()
                .max_by(|a, b| {
                    a.1.partial_cmp(b.1)
                        .unwrap_or(std::cmp::Ordering::Equal)
                        .then(b.0.cmp(a.0))
                })
                .map(|(k, _)| *k as f64)
                .unwrap_or(0.0)
        }));
        ctx.finish(out)
    }
}

/// Aggregated Mondrian Forest regressor (river `forest.AMFRegressor`).
///
/// Tree count is not identification `p`.
#[derive(Clone, Debug)]
pub struct AmfRegressor {
    /// Trees.
    pub n_trees: usize,
    trees: Vec<AmfRegTree>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

#[derive(Clone, Debug)]
struct AmfRegTree {
    p: usize,
    n: u64,
    lo: Vector,
    hi: Vector,
    split_j: Option<usize>,
    split_t: f64,
    leaf_sum: f64,
    leaf_n: f64,
    left_sum: f64,
    left_n: f64,
    right_sum: f64,
    right_n: f64,
}

impl Default for AmfRegressor {
    fn default() -> Self {
        Self {
            n_trees: 4,
            trees: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(31),
        }
    }
}

impl AmfRegressor {
    /// AMF regressor with `n_trees` Mondrian stumps.
    pub fn new(n_trees: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            ..Self::default()
        }
    }

    fn mean(sum: f64, n: f64) -> f64 {
        if n > 0.0 {
            sum / n
        } else {
            0.0
        }
    }
}

impl PartialFit for AmfRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.trees.is_empty() {
            self.trees = (0..self.n_trees.max(1))
                .map(|_| AmfRegTree {
                    p: x.ncols(),
                    n: 0,
                    lo: Vector::filled(x.ncols(), f64::INFINITY),
                    hi: Vector::filled(x.ncols(), f64::NEG_INFINITY),
                    split_j: None,
                    split_t: 0.0,
                    leaf_sum: 0.0,
                    leaf_n: 0.0,
                    left_sum: 0.0,
                    left_n: 0.0,
                    right_sum: 0.0,
                    right_n: 0.0,
                })
                .collect();
        } else if self.trees[0].p != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        let mut splits = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            for t in &mut self.trees {
                for j in 0..x.ncols().min(t.lo.len()) {
                    let v = x.get(i, j);
                    if v < t.lo[j] {
                        t.lo[j] = v;
                    }
                    if v > t.hi[j] {
                        t.hi[j] = v;
                    }
                }
                t.n += 1;
                if let Some(j) = t.split_j {
                    let v = if j < x.ncols() { x.get(i, j) } else { 0.0 };
                    if v <= t.split_t {
                        t.left_sum += y[i];
                        t.left_n += 1.0;
                    } else {
                        t.right_sum += y[i];
                        t.right_n += 1.0;
                    }
                } else {
                    t.leaf_sum += y[i];
                    t.leaf_n += 1.0;
                    if t.n >= 2 {
                        let mut best_j = 0usize;
                        let mut best_r = 0.0;
                        for j in 0..t.lo.len() {
                            let r = t.hi[j] - t.lo[j];
                            if r > best_r {
                                best_r = r;
                                best_j = j;
                            }
                        }
                        if best_r > 1e-12 {
                            let u = self.rng.uniform();
                            t.split_j = Some(best_j);
                            t.split_t = t.lo[best_j] + u * best_r;
                            t.left_sum = t.leaf_sum;
                            t.left_n = t.leaf_n;
                            t.right_sum = 0.0;
                            t.right_n = 0.0;
                            splits += 1;
                        }
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(splits as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AMFRegressor {} stumps, {splits} new splits this batch",
            self.trees.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Mondrian stump growth",
                "a leaf splits on its widest feature once two targets have been seen",
                format!("n={before}"),
                format!("n={} splits={splits}", self.n_seen),
            ),
        )
    }
}

impl Predict for AmfRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.trees.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_trees = self.trees.len().max(1) as f64;
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0;
            for t in &self.trees {
                let m = if let Some(j) = t.split_j {
                    let v = if j < x.ncols() { x.get(i, j) } else { 0.0 };
                    if v <= t.split_t {
                        Self::mean(t.left_sum, t.left_n)
                    } else {
                        Self::mean(t.right_sum, t.right_n)
                    }
                } else {
                    Self::mean(t.leaf_sum, t.leaf_n)
                };
                s += m;
            }
            s / n_trees
        }));
        ctx.finish(out)
    }
}

/// Shared-density stream clustering (river `cluster.DBSTREAM` lite).
///
/// Micro-cluster count is not identification `p`.
#[derive(Clone, Debug)]
pub struct DbStream {
    /// Merge radius.
    pub eps: f64,
    /// Per-row weight decay.
    pub lambda: f64,
    micros: Vec<(Vector, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for DbStream {
    fn default() -> Self {
        Self {
            eps: 1.0,
            lambda: 0.99,
            micros: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl DbStream {
    /// DBSTREAM with radius `eps`.
    pub fn new(eps: f64) -> Self {
        Self {
            eps,
            ..Self::default()
        }
    }

    /// Current micro-cluster count.
    pub fn n_micro(&self) -> usize {
        self.micros.len()
    }
}

impl PartialFit for DbStream {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "DbStream eps={} is not positive; using 1",
                        self.eps
                    ))
                    .build(),
            );
            1.0
        };
        let lam = if self.lambda.is_finite() && self.lambda > 0.0 && self.lambda <= 1.0 {
            self.lambda
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "DbStream lambda={} is not in (0,1]; using 0.99",
                        self.lambda
                    ))
                    .build(),
            );
            0.99
        };
        let before = self.n_seen;
        let mut spawned = 0u64;
        let mut merged = 0u64;
        for i in 0..x.nrows() {
            for mc in self.micros.iter_mut() {
                mc.1 *= lam;
            }
            self.micros.retain(|mc| mc.1 >= 1e-3);
            let row = x.row(i);
            let mut best = None;
            let mut best_d = f64::INFINITY;
            for (c, (ctr, _)) in self.micros.iter().enumerate() {
                let mut d = 0.0;
                for j in 0..row.len().min(ctr.len()) {
                    let z = row[j] - ctr[j];
                    d += z * z;
                }
                d = d.sqrt();
                if d < best_d {
                    best_d = d;
                    best = Some(c);
                }
            }
            if let Some(c) = best {
                if best_d <= eps {
                    let w = self.micros[c].1;
                    let nw = w + 1.0;
                    for j in 0..self.micros[c].0.len().min(row.len()) {
                        let old = self.micros[c].0[j];
                        self.micros[c].0[j] = (old * w + row[j]) / nw;
                    }
                    self.micros[c].1 = nw;
                    merged += 1;
                } else {
                    self.micros.push((row, 1.0));
                    spawned += 1;
                }
            } else {
                self.micros.push((row, 1.0));
                spawned += 1;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((spawned + merged) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.micros.is_empty();
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "DBSTREAM micros={} spawned={spawned} absorbed={merged}",
            self.micros.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "shared-density micro-cluster update",
                "a row joins the nearest micro inside ε or seeds a new centre",
                format!("n={before} micros"),
                format!("n={} micros={}", self.n_seen, self.micros.len()),
            ),
        )
    }
}

impl Predict for DbStream {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.micros.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let m = self.micros.len();
        let mut parent: Vec<usize> = (0..m).collect();
        let find = |p: &mut [usize], mut a: usize| {
            while p[a] != a {
                p[a] = p[p[a]];
                a = p[a];
            }
            a
        };
        let link = 2.0 * self.eps.max(1e-12);
        for a in 0..m {
            for b in (a + 1)..m {
                let mut d = 0.0;
                for j in 0..self.micros[a].0.len().min(self.micros[b].0.len()) {
                    let z = self.micros[a].0[j] - self.micros[b].0[j];
                    d += z * z;
                }
                if d.sqrt() <= link {
                    let pa = find(&mut parent, a);
                    let pb = find(&mut parent, b);
                    parent[pb] = pa;
                }
            }
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut best = 0usize;
            let mut bd = f64::INFINITY;
            for (c, (ctr, _)) in self.micros.iter().enumerate() {
                let mut d = 0.0;
                for j in 0..x.ncols().min(ctr.len()) {
                    let z = x.get(i, j) - ctr[j];
                    d += z * z;
                }
                if d < bd {
                    bd = d;
                    best = c;
                }
            }
            let mut a = best;
            while parent[a] != a {
                a = parent[a];
            }
            a as f64
        }));
        ctx.finish(out)
    }
}

/// Poisson-bootstrap bag of RLS members (river `ensemble.BaggingRegressor`).
#[derive(Clone, Debug)]
pub struct OnlineBaggingRegressor {
    /// Members.
    pub n_models: usize,
    models: Vec<LinearRegression>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for OnlineBaggingRegressor {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(31),
        }
    }
}

impl OnlineBaggingRegressor {
    /// Bag of `n_models` RLS estimators.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineBaggingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![LinearRegression::new(1.0); self.n_models.max(1)];
        }
        let before = self.n_seen;
        for m in 0..self.models.len() {
            let mut rows: Vec<usize> = Vec::new();
            for i in 0..x.nrows().min(y.len()) {
                let k = self.rng.poisson(1.0) as usize;
                for _ in 0..k {
                    rows.push(i);
                }
            }
            if rows.is_empty() && x.nrows() > 0 {
                rows.push(0);
            }
            if rows.is_empty() {
                continue;
            }
            let xb = Matrix::from_fn(rows.len(), x.ncols(), |r, c| x.get(rows[r], c));
            let yb = Vector::from_iter(rows.iter().map(|&i| y[i]));
            let _ = self.models[m].partial_fit(&xb, Some(&yb), &session.child(format!("bagr_{m}")));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineBaggingRegressor {} RLS members, Poisson bootstrap",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RLS committee",
                "each member sees a Poisson(1) bootstrap of the batch",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineBaggingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        let mut k: f64 = 0.0;
        for (t, m) in self.models.iter().enumerate() {
            match m.predict(x, &session.child(format!("br{t}"))) {
                Ok(q) => {
                    for i in 0..x.nrows().min(q.value.len()) {
                        acc[i] += q.value[i];
                    }
                    k += 1.0;
                }
                Err(_) => {}
            }
        }
        let k = k.max(1.0);
        ctx.finish(Vector::from_iter(acc.iter().map(|v| v / k)))
    }
}

/// Streaming binarizer (river `preprocessing.Binarizer`).
#[derive(Clone, Debug)]
pub struct OnlineBinarizer {
    /// Threshold.
    pub threshold: f64,
    p: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineBinarizer {
    fn default() -> Self {
        Self {
            threshold: 0.0,
            p: 0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineBinarizer {
    /// Binarizer with threshold `threshold`.
    pub fn new(threshold: f64) -> Self {
        Self {
            threshold,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineBinarizer {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.threshold.is_finite() {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineBinarizer threshold={} is not finite; using 0",
                        self.threshold
                    ))
                    .build(),
            );
            self.threshold = 0.0;
        }
        if self.p == 0 {
            self.p = x.ncols();
        } else if self.p != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("OnlineBinarizer threshold={:.6e}", self.threshold);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "threshold map",
                "each coordinate is 1 iff it meets the recorded cut",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for OnlineBinarizer {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let t = if self.threshold.is_finite() {
            self.threshold
        } else {
            0.0
        };
        ctx.finish(Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if x.get(i, j) >= t {
                1.0
            } else {
                0.0
            }
        }))
    }
}

/// Streaming isolation forest (river `anomaly.HalfSpaceTrees` / isolation forest).
///
/// Tree count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineIsolationForest {
    /// Trees.
    pub n_trees: usize,
    /// Depth.
    pub max_depth: usize,
    trees: Vec<Vec<(usize, f64)>>,
    masses: Vec<HashMap<u32, u64>>,
    mins: Vector,
    maxs: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for OnlineIsolationForest {
    fn default() -> Self {
        Self {
            n_trees: 4,
            max_depth: 4,
            trees: Vec::new(),
            masses: Vec::new(),
            mins: Vector::zeros(0),
            maxs: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(19),
        }
    }
}

impl OnlineIsolationForest {
    /// Isolation ensemble with `n_trees` trees of depth `max_depth`.
    pub fn new(n_trees: usize, max_depth: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            max_depth: max_depth.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, p: usize) {
        self.trees.clear();
        self.masses.clear();
        for _ in 0..self.n_trees.max(1) {
            let mut cuts = Vec::new();
            for _ in 0..self.max_depth.max(1) {
                let j = if p == 0 { 0 } else { self.rng.below(p) };
                let lo = if j < self.mins.len() {
                    self.mins[j]
                } else {
                    0.0
                };
                let hi = if j < self.maxs.len() {
                    self.maxs[j]
                } else {
                    1.0
                };
                let thr = if hi > lo {
                    self.rng.uniform_range(lo, hi)
                } else {
                    lo
                };
                cuts.push((j, thr));
            }
            self.trees.push(cuts);
            self.masses.push(HashMap::new());
        }
    }

    fn path_key(cuts: &[(usize, f64)], x: &Matrix, i: usize) -> (u32, usize) {
        let mut key: u32 = 0;
        let mut depth = 0usize;
        for (bit, (j, thr)) in cuts.iter().enumerate() {
            let v = if *j < x.ncols() { x.get(i, *j) } else { 0.0 };
            if v > *thr {
                key |= 1 << bit;
            }
            depth = bit + 1;
        }
        (key, depth)
    }

    /// Mean isolation score in \([0, 1]\); higher is more anomalous.
    pub fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.trees.is_empty() || self.n_seen == 0 {
            return 0.5;
        }
        let mut s = 0.0;
        for (cuts, mass) in self.trees.iter().zip(&self.masses) {
            let (key, depth) = Self::path_key(cuts, x, i);
            let m = *mass.get(&key).unwrap_or(&0);
            let c = (self.n_seen as f64).ln().max(1.0);
            let path = depth as f64 + if m > 1 { (m as f64).ln() } else { 0.0 };
            s += 2.0_f64.powf(-path / c);
        }
        s / self.trees.len() as f64
    }
}

impl PartialFit for OnlineIsolationForest {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            self.mins = Vector::from_iter((0..x.ncols()).map(|j| {
                (0..x.nrows())
                    .map(|i| x.get(i, j))
                    .fold(f64::INFINITY, f64::min)
            }));
            self.maxs = Vector::from_iter((0..x.ncols()).map(|j| {
                (0..x.nrows())
                    .map(|i| x.get(i, j))
                    .fold(f64::NEG_INFINITY, f64::max)
            }));
            self.grow(x.ncols());
            self.initialized = true;
        } else if self.mins.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        for i in 0..x.nrows() {
            for (cuts, mass) in self.trees.iter().zip(self.masses.iter_mut()) {
                let (key, _) = Self::path_key(cuts, x, i);
                *mass.entry(key).or_insert(0) += 1;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = if x.nrows() > 0 {
            self.score_row(x, 0)
        } else {
            0.5
        };
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 4;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("OnlineIsolationForest score={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "random-cut isolation counts",
                "each tree records the leaf mass of the hashed path",
                format!("n={before}"),
                format!("n={} score={after:.6e}", self.n_seen),
            ),
        )
    }
}

impl Predict for OnlineIsolationForest {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.score_row(x, i)),
        ))
    }
}

fn rolling_push(window: &mut Vec<f64>, v: f64, cap: usize) {
    if !v.is_finite() {
        return;
    }
    window.push(v);
    if window.len() > cap {
        window.remove(0);
    }
}

/// Rolling minimum (river `stats.RollingMin`).
#[derive(Clone, Debug, Default)]
pub struct RollingMin {
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl RollingMin {
    /// Empty rolling min.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current min, or NaN when empty.
    pub fn score(&self) -> f64 {
        self.window
            .iter()
            .copied()
            .fold(f64::NAN, |a, b| if a.is_nan() { b } else { a.min(b) })
    }
}

impl PartialFit for RollingMin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            rolling_push(&mut self.window, x.get(i, 0), 256);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.window.is_empty();
        q.warmup = self.window.is_empty();
        q.explanation = format!("RollingMin={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed minimum",
                "256-row sliding min of column 0",
                format!("min={before:.6e}"),
                format!("min={after:.6e}"),
            ),
        )
    }
}

/// Rolling maximum (river `stats.RollingMax`).
#[derive(Clone, Debug, Default)]
pub struct RollingMax {
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl RollingMax {
    /// Empty rolling max.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current max, or NaN when empty.
    pub fn score(&self) -> f64 {
        self.window
            .iter()
            .copied()
            .fold(f64::NAN, |a, b| if a.is_nan() { b } else { a.max(b) })
    }
}

impl PartialFit for RollingMax {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            rolling_push(&mut self.window, x.get(i, 0), 256);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.window.is_empty();
        q.warmup = self.window.is_empty();
        q.explanation = format!("RollingMax={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed maximum",
                "256-row sliding max of column 0",
                format!("max={before:.6e}"),
                format!("max={after:.6e}"),
            ),
        )
    }
}

/// Streaming skewness (river `stats.Skew`).
#[derive(Clone, Debug, Default)]
pub struct OnlineSkew {
    n: f64,
    mean: f64,
    m2: f64,
    m3: f64,
    updates: u64,
}

impl OnlineSkew {
    /// Empty skewness accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current skewness, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 3.0 || self.m2 <= 1e-18 {
            f64::NAN
        } else {
            self.n.sqrt() * self.m3 / self.m2.powf(1.5)
        }
    }
}

impl PartialFit for OnlineSkew {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let n1 = self.n - 1.0;
            let delta = v - self.mean;
            let delta_n = delta / self.n;
            let term1 = delta * delta_n * n1;
            self.mean += delta_n;
            self.m3 += term1 * delta_n * (self.n - 2.0) - 3.0 * delta_n * self.m2;
            self.m2 += term1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 3.0;
        q.warmup = self.n < 3.0;
        q.explanation = format!("OnlineSkew={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford third moment",
                "skewness from central moments of column 0",
                format!("skew={before:.6e}"),
                format!("skew={after:.6e}"),
            ),
        )
    }
}

/// Streaming excess kurtosis (river `stats.Kurtosis`).
#[derive(Clone, Debug, Default)]
pub struct OnlineKurtosis {
    n: f64,
    mean: f64,
    m2: f64,
    m3: f64,
    m4: f64,
    updates: u64,
}

impl OnlineKurtosis {
    /// Empty kurtosis accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current excess kurtosis, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 4.0 || self.m2 <= 1e-18 {
            f64::NAN
        } else {
            self.n * self.m4 / (self.m2 * self.m2) - 3.0
        }
    }
}

impl PartialFit for OnlineKurtosis {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let n1 = self.n - 1.0;
            let delta = v - self.mean;
            let delta_n = delta / self.n;
            let delta_n2 = delta_n * delta_n;
            let term1 = delta * delta_n * n1;
            self.mean += delta_n;
            self.m4 += term1 * delta_n2 * (self.n * self.n - 3.0 * self.n + 3.0)
                + 6.0 * delta_n2 * self.m2
                - 4.0 * delta_n * self.m3;
            self.m3 += term1 * delta_n * (self.n - 2.0) - 3.0 * delta_n * self.m2;
            self.m2 += term1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 4.0;
        q.warmup = self.n < 4.0;
        q.explanation = format!("OnlineKurtosis={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford fourth moment",
                "excess kurtosis from central moments of column 0",
                format!("kurt={before:.6e}"),
                format!("kurt={after:.6e}"),
            ),
        )
    }
}

/// Streaming Shannon entropy of rounded values (river `stats.Entropy`).
#[derive(Clone, Debug, Default)]
pub struct OnlineEntropy {
    counts: HashMap<i64, u64>,
    n: u64,
    updates: u64,
}

impl OnlineEntropy {
    /// Empty entropy accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current entropy in nats, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            return f64::NAN;
        }
        let tot = self.n as f64;
        let mut h = 0.0;
        for c in self.counts.values() {
            let p = *c as f64 / tot;
            if p > 0.0 {
                h -= p * p.ln();
            }
        }
        h
    }
}

impl PartialFit for OnlineEntropy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            let key = v.round() as i64;
            *self.counts.entry(key).or_insert(0) += 1;
            self.n += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.counts.len() >= 2;
        q.warmup = self.n < 2;
        q.explanation = format!("OnlineEntropy={after:.6e} bins={}", self.counts.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "histogram entropy",
                "Shannon entropy of rounded column-0 values",
                format!("H={before:.6e}"),
                format!("H={after:.6e}"),
            ),
        )
    }
}

/// Periodic sine/cosine map (river-style calendar / Fourier features).
///
/// Period is not identification `p`.
#[derive(Clone, Debug)]
pub struct Periodic {
    /// Period of the cycle.
    pub period: f64,
    p: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for Periodic {
    fn default() -> Self {
        Self {
            period: 12.0,
            p: 0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Periodic {
    /// Periodic map with period `period`.
    pub fn new(period: f64) -> Self {
        Self {
            period,
            ..Self::default()
        }
    }
}

impl PartialFit for Periodic {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.period.is_finite() || self.period <= 0.0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "Periodic.period={} is not positive; using 1",
                        self.period
                    ))
                    .build(),
            );
            self.period = 1.0;
        }
        if self.p == 0 {
            self.p = x.ncols();
        } else if self.p != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.n_seen;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("Periodic period={:.6e}", self.period);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sin/cos period map",
                "each column becomes a 2-d Fourier pair",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for Periodic {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let per = if self.period.is_finite() && self.period > 0.0 {
            self.period
        } else {
            1.0
        };
        let tau = std::f64::consts::TAU;
        ctx.finish(Matrix::from_fn(x.nrows(), x.ncols() * 2, |i, j| {
            let col = j / 2;
            let v = x.get(i, col);
            let ang = tau * v / per;
            if j % 2 == 0 {
                ang.sin()
            } else {
                ang.cos()
            }
        }))
    }
}

/// Streaming pairwise covariance (river `stats.Cov`).
#[derive(Clone, Debug, Default)]
pub struct OnlineCovariance {
    n: f64,
    mx: f64,
    my: f64,
    c: f64,
    updates: u64,
}

impl OnlineCovariance {
    /// Empty covariance accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current covariance, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            f64::NAN
        } else {
            self.c / (self.n - 1.0)
        }
    }
}

impl PartialFit for OnlineCovariance {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if x.ncols() < 2 && y.is_none() {
            ctx.push(
                Issue::builder(IssueCode::InsufficientSample)
                    .severity(Severity::Warning)
                    .message("OnlineCovariance needs two columns or an explicit y")
                    .build(),
            );
        }
        let before = self.score();
        for i in 0..x.nrows() {
            let a = x.get(i, 0);
            let b = if x.ncols() >= 2 {
                x.get(i, 1)
            } else if let Some(y) = y {
                if i < y.len() {
                    y[i]
                } else {
                    f64::NAN
                }
            } else {
                f64::NAN
            };
            if !a.is_finite() || !b.is_finite() {
                continue;
            }
            self.n += 1.0;
            let dx = a - self.mx;
            self.mx += dx / self.n;
            self.my += (b - self.my) / self.n;
            self.c += dx * (b - self.my);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineCovariance={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford covariance",
                "pairwise covariance of column 0 with column 1 or y",
                format!("cov={before:.6e}"),
                format!("cov={after:.6e}"),
            ),
        )
    }
}

/// Hashing trick (river `preprocessing.FeatureHasher` / sklearn `FeatureHasher`).
///
/// Output width is not identification `p`.
#[derive(Clone, Debug)]
pub struct HashingTrick {
    /// Hash-bin count.
    pub n_features: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for HashingTrick {
    fn default() -> Self {
        Self {
            n_features: 8,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl HashingTrick {
    /// Hasher with `n_features` bins.
    pub fn new(n_features: usize) -> Self {
        Self {
            n_features: n_features.max(1),
            ..Self::default()
        }
    }

    fn bin(j: usize, n: usize) -> (usize, f64) {
        let h = (j as u64).wrapping_mul(0x9E37_79B9_7F4A_7C15);
        let sign = if (h >> 1) & 1 == 0 { 1.0 } else { -1.0 };
        ((h as usize) % n.max(1), sign)
    }
}

impl PartialFit for HashingTrick {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_features == 0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("HashingTrick n_features=0; using 1")
                    .build(),
            );
            self.n_features = 1;
        }
        let before = self.n_seen;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("HashingTrick bins={}", self.n_features);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "signed hash projection",
                "each input column is folded into a fixed bag of bins",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for HashingTrick {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let m = self.n_features.max(1);
        let mut out = Matrix::zeros(x.nrows(), m);
        for i in 0..x.nrows() {
            for j in 0..x.ncols() {
                let (b, s) = Self::bin(j, m);
                out.set(i, b, out.get(i, b) + s * x.get(i, j));
            }
        }
        ctx.finish(out)
    }
}

/// Hashed bag-of-values (river `feature_extraction.BagOfWords`).
///
/// Finite entries of `X` are hashed into `n_features` count bins.
/// Bin count is not identification `p`.
#[derive(Clone, Debug)]
pub struct BagOfWords {
    /// Hash-bin count.
    pub n_features: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for BagOfWords {
    fn default() -> Self {
        Self {
            n_features: 8,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl BagOfWords {
    /// Bag with `n_features` bins.
    pub fn new(n_features: usize) -> Self {
        Self {
            n_features: n_features.max(1),
            ..Self::default()
        }
    }

    fn bin(bits: u64, col: usize, n: usize) -> usize {
        let h = bits
            .wrapping_mul(0x9E37_79B9_7F4A_7C15)
            .wrapping_add((col as u64).wrapping_mul(0xC2B2_AE3D_27D4_EB4F));
        (h as usize) % n.max(1)
    }
}

impl PartialFit for BagOfWords {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_features == 0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("BagOfWords n_features=0; using 1")
                    .build(),
            );
            self.n_features = 1;
        }
        let before = self.n_seen;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("BagOfWords bins={}", self.n_features);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "hashed bag-of-values",
                "each finite entry is folded into a fixed count bag",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for BagOfWords {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let m = self.n_features.max(1);
        let mut out = Matrix::zeros(x.nrows(), m);
        for i in 0..x.nrows() {
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                let b = Self::bin(v.to_bits(), j, m);
                out.set(i, b, out.get(i, b) + 1.0);
            }
        }
        ctx.finish(out)
    }
}

/// Stochastic gradient tree classifier (river `tree.SGTClassifier`).
///
/// Lite: a single logit leaf updated by log-loss SGD. Split / leaf counts
/// are not identification `p`.
#[derive(Clone, Debug)]
pub struct SgtClassifier {
    /// Step size.
    pub learning_rate: f64,
    logit: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for SgtClassifier {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            logit: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SgtClassifier {
    /// Default SGT classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for SgtClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "SGT needs labels"),
            );
        };
        let before = self.logit;
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("SgtClassifier learning_rate is not a positive finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yi = if y[i] >= 0.5 { 1.0 } else { 0.0 };
            let p0 = 1.0 / (1.0 + (-self.logit).exp());
            loss_before += -(yi * p0.max(1e-15).ln() + (1.0 - yi) * (1.0 - p0).max(1e-15).ln());
            self.logit -= lr * (p0 - yi);
            let p1 = 1.0 / (1.0 + (-self.logit).exp());
            loss_after += -(yi * p1.max(1e-15).ln() + (1.0 - yi) * (1.0 - p1).max(1e-15).ln());
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.logit - before).abs());
        q.information_gain = Some((loss_before - loss_after).abs());
        q.loss_before = Some(loss_before);
        q.loss_after = Some(loss_after);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 5;
        q.explanation = format!("SgtClassifier logit {:.6e} → {:.6e}", before, self.logit);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SGT leaf logit",
                "log-loss SGD on a single stochastic-gradient tree leaf",
                format!("logit={before:.6e}"),
                format!("logit={:.6e}", self.logit),
            ),
        )
    }
}

/// Stochastic gradient tree regressor (river `tree.SGTRegressor`).
///
/// Lite: a single leaf mean updated by squared-error SGD. Leaf count is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct SgtRegressor {
    /// Step size.
    pub learning_rate: f64,
    level: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for SgtRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            level: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SgtRegressor {
    /// Default SGT regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for SgtRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "SGT needs y"),
            );
        };
        let before = self.level;
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("SgtRegressor learning_rate is not a positive finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let e0 = self.level - y[i];
            loss_before += e0 * e0;
            self.level -= lr * e0;
            let e1 = self.level - y[i];
            loss_after += e1 * e1;
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.level - before).abs());
        q.information_gain = Some((loss_before - loss_after).abs());
        q.loss_before = Some(loss_before);
        q.loss_after = Some(loss_after);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 5;
        q.explanation = format!("SgtRegressor level {:.6e} → {:.6e}", before, self.level);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SGT leaf level",
                "squared-error SGD on a single stochastic-gradient tree leaf",
                format!("level={before:.6e}"),
                format!("level={:.6e}", self.level),
            ),
        )
    }
}

/// Incremental multi-output Hoeffding-style tree (river `iSOUPTreeRegressor`).
///
/// Output 0 is `y`. Extra `X` columns beyond the first are additional targets.
/// Output count is not identification `p`.
#[derive(Clone, Debug)]
pub struct IsoUPTree {
    /// Number of output heads.
    pub n_outputs: usize,
    means: Vec<f64>,
    counts: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for IsoUPTree {
    fn default() -> Self {
        Self {
            n_outputs: 2,
            means: vec![0.0; 2],
            counts: vec![0.0; 2],
            n_seen: 0,
            updates: 0,
        }
    }
}

impl IsoUPTree {
    /// `n_outputs` target heads.
    pub fn new(n_outputs: usize) -> Self {
        let k = n_outputs.max(1);
        Self {
            n_outputs: k,
            means: vec![0.0; k],
            counts: vec![0.0; k],
            n_seen: 0,
            updates: 0,
        }
    }
}

impl PartialFit for IsoUPTree {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "iSOUP needs y"),
            );
        };
        if self.n_outputs > 1 && x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("IsoUPTree extra outputs have no extra X columns; only y is updated")
                    .build(),
            );
        }
        if self.means.len() != self.n_outputs.max(1) {
            self.means.resize(self.n_outputs.max(1), 0.0);
            self.counts.resize(self.n_outputs.max(1), 0.0);
        }
        let before = self.means.clone();
        for i in 0..x.nrows().min(y.len()) {
            if y[i].is_finite() {
                let c = self.counts[0] + 1.0;
                self.means[0] += (y[i] - self.means[0]) / c;
                self.counts[0] = c;
            }
            for o in 1..self.n_outputs.min(self.means.len()) {
                if o < x.ncols() {
                    let v = x.get(i, o);
                    if v.is_finite() {
                        let c = self.counts[o] + 1.0;
                        self.means[o] += (v - self.means[o]) / c;
                        self.counts[o] = c;
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut dsum = 0.0_f64;
        for (a, b) in before.iter().zip(self.means.iter()) {
            dsum += (b - a).abs();
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(dsum);
        q.still_identified = self.counts.iter().all(|&c| c > 0.0);
        q.warmup = self.counts.iter().any(|&c| c < 2.0);
        q.explanation = format!("IsoUPTree means={:?}", self.means);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "iSOUP per-output leaf means",
                "incremental multi-target means; extra X columns are additional targets",
                format!("means={before:?}"),
                format!("means={:?}", self.means),
            ),
        )
    }
}

/// Pegasos hinge SVM (online linear SVM; river / SGD hinge peer).
///
/// Feature count is not treated as an extra identification `p` on a stream.
#[derive(Clone, Debug)]
pub struct OnlineSvm {
    /// Regularization \(\lambda > 0\).
    pub lambda: f64,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineSvm {
    fn default() -> Self {
        Self {
            lambda: 1e-3,
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineSvm {
    /// Default Pegasos SVM.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineSvm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "OnlineSvm needs labels",
                ),
            );
        };
        if !self.initialized {
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("OnlineSvm feature count changed")
                    .build(),
            );
            self.coef = Vector::zeros(x.ncols());
        }
        let mut lam = self.lambda;
        if !lam.is_finite() || lam <= 0.0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineSvm λ={lam} is not a positive finite; using 1e-3"
                    ))
                    .build(),
            );
            lam = 1e-3;
            self.lambda = lam;
        }
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yi = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut pred = 0.0_f64;
            for j in 0..x.ncols() {
                pred += self.coef[j] * x.get(i, j);
            }
            loss_before += (1.0 - yi * pred).max(0.0);
            self.n_seen += 1;
            let eta = 1.0 / (lam * self.n_seen as f64);
            let decay = (1.0 - eta * lam).max(0.0);
            for j in 0..x.ncols() {
                self.coef[j] *= decay;
            }
            if yi * pred < 1.0 {
                for j in 0..x.ncols() {
                    self.coef[j] += eta * yi * x.get(i, j);
                }
            }
            let mut pred1 = 0.0_f64;
            for j in 0..x.ncols() {
                pred1 += self.coef[j] * x.get(i, j);
            }
            loss_after += (1.0 - yi * pred1).max(0.0);
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "Pegasos hinge weights",
            "online SVM: decay toward the regularizer, then a hinge subgradient step",
        );
        finish_explain(ctx, expl)
    }
}

/// Streaming median absolute deviation (river `stats.MAD`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMad {
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl OnlineMad {
    /// Empty MAD accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MAD, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.window.is_empty() {
            return f64::NAN;
        }
        let mut xs = self.window.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let mid = xs[xs.len() / 2];
        let mut dev: Vec<f64> = xs.iter().map(|v| (v - mid).abs()).collect();
        dev.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        dev[dev.len() / 2]
    }
}

impl PartialFit for OnlineMad {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            rolling_push(&mut self.window, x.get(i, 0), 256);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.window.len() >= 3;
        q.warmup = self.window.len() < 3;
        q.explanation = format!("OnlineMad={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed MAD",
                "median absolute deviation of column 0",
                format!("mad={before:.6e}"),
                format!("mad={after:.6e}"),
            ),
        )
    }
}

/// Streaming interquartile range (river `stats.IQR`).
#[derive(Clone, Debug, Default)]
pub struct OnlineIqr {
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl OnlineIqr {
    /// Empty IQR accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current IQR, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.window.len() < 2 {
            return f64::NAN;
        }
        let mut xs = self.window.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let q = |t: f64| {
            let u = t * (xs.len() - 1) as f64;
            let lo = u.floor() as usize;
            let hi = u.ceil() as usize;
            let w = u - lo as f64;
            (1.0 - w) * xs[lo] + w * xs[hi.min(xs.len() - 1)]
        };
        q(0.75) - q(0.25)
    }
}

impl PartialFit for OnlineIqr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            rolling_push(&mut self.window, x.get(i, 0), 256);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.window.len() >= 4;
        q.warmup = self.window.len() < 4;
        q.explanation = format!("OnlineIqr={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed IQR",
                "p75 − p25 of column 0",
                format!("iqr={before:.6e}"),
                format!("iqr={after:.6e}"),
            ),
        )
    }
}

/// Streaming Pearson correlation (river `stats.PearsonCorr`).
#[derive(Clone, Debug, Default)]
pub struct OnlinePearson {
    n: f64,
    mx: f64,
    my: f64,
    c: f64,
    vx: f64,
    vy: f64,
    updates: u64,
}

impl OnlinePearson {
    /// Empty correlation accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current correlation, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let den = (self.vx * self.vy).sqrt();
        if den <= 1e-18 {
            f64::NAN
        } else {
            self.c / den
        }
    }
}

impl PartialFit for OnlinePearson {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if x.ncols() < 2 && y.is_none() {
            ctx.push(
                Issue::builder(IssueCode::InsufficientSample)
                    .severity(Severity::Warning)
                    .message("OnlinePearson needs two columns or an explicit y")
                    .build(),
            );
        }
        let before = self.score();
        for i in 0..x.nrows() {
            let a = x.get(i, 0);
            let b = if x.ncols() >= 2 {
                x.get(i, 1)
            } else if let Some(y) = y {
                if i < y.len() {
                    y[i]
                } else {
                    f64::NAN
                }
            } else {
                f64::NAN
            };
            if !a.is_finite() || !b.is_finite() {
                continue;
            }
            self.n += 1.0;
            let dx = a - self.mx;
            self.mx += dx / self.n;
            let dy = b - self.my;
            self.my += dy / self.n;
            self.c += dx * (b - self.my);
            self.vx += dx * (a - self.mx);
            self.vy += dy * (b - self.my);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 3.0;
        q.warmup = self.n < 3.0;
        q.explanation = format!("OnlinePearson={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford correlation",
                "Pearson r of column 0 with column 1 or y",
                format!("r={before:.6e}"),
                format!("r={after:.6e}"),
            ),
        )
    }
}

/// Streaming mode of a discretized column (river `stats.Mode`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMode {
    counts: HashMap<i64, u64>,
    mode_key: i64,
    mode_count: u64,
    n_seen: u64,
    updates: u64,
}

impl OnlineMode {
    /// Empty mode accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mode (decoded from milli-bins), or NaN before the first update.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.mode_key as f64 / 1000.0
        }
    }
}

impl PartialFit for OnlineMode {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            let key = (v * 1000.0).round() as i64;
            let c = self.counts.entry(key).or_insert(0);
            *c += 1;
            let cnt = *c;
            if cnt > self.mode_count || (cnt == self.mode_count && key < self.mode_key) {
                self.mode_count = cnt;
                self.mode_key = key;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineMode={after:.6e} count={}", self.mode_count);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "discretized mode",
                "milli-bin counts of column 0; ties take the smaller key",
                format!("mode={before:.6e}"),
                format!("mode={after:.6e}"),
            ),
        )
    }
}

/// Expanding maximum (river `stats.Peak`).
#[derive(Clone, Debug)]
pub struct OnlinePeak {
    peak: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlinePeak {
    fn default() -> Self {
        Self {
            peak: f64::NEG_INFINITY,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlinePeak {
    /// Empty peak tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current peak, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.peak
        }
    }
}

impl PartialFit for OnlinePeak {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v > self.peak {
                self.peak = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlinePeak={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding maximum",
                "running max of column 0",
                format!("peak={before:.6e}"),
                format!("peak={after:.6e}"),
            ),
        )
    }
}

/// Streaming standard error of the mean (river `stats.SEM`).
#[derive(Clone, Debug, Default)]
pub struct OnlineSem {
    n: f64,
    mean: f64,
    m2: f64,
    updates: u64,
}

impl OnlineSem {
    /// Empty SEM accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// \(s/\sqrt{n}\), or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let var = self.m2 / (self.n - 1.0);
        if var < 0.0 {
            f64::NAN
        } else {
            var.sqrt() / self.n.sqrt()
        }
    }
}

impl PartialFit for OnlineSem {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineSem={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "standard error of the mean",
                "Welford s / √n of column 0",
                format!("sem={before:.6e}"),
                format!("sem={after:.6e}"),
            ),
        )
    }
}

/// Windowed Spearman rank correlation (river `stats.SpearmanCorr`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineSpearman {
    xs: Vec<f64>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl OnlineSpearman {
    /// Empty rank-correlation window.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current Spearman ρ, or NaN during warmup.
    pub fn score(&self) -> f64 {
        spearman_window(&self.xs, &self.ys)
    }
}

fn spearman_window(xs: &[f64], ys: &[f64]) -> f64 {
    let n = xs.len().min(ys.len());
    if n < 3 {
        return f64::NAN;
    }
    let rx = rank_average(xs);
    let ry = rank_average(ys);
    let mx = rx.iter().sum::<f64>() / n as f64;
    let my = ry.iter().sum::<f64>() / n as f64;
    let mut num = 0.0;
    let mut d1 = 0.0;
    let mut d2 = 0.0;
    for i in 0..n {
        let a = rx[i] - mx;
        let b = ry[i] - my;
        num += a * b;
        d1 += a * a;
        d2 += b * b;
    }
    if d1 <= 1e-18 || d2 <= 1e-18 {
        f64::NAN
    } else {
        num / (d1.sqrt() * d2.sqrt())
    }
}

fn rank_average(xs: &[f64]) -> Vec<f64> {
    let mut idx: Vec<usize> = (0..xs.len()).collect();
    idx.sort_by(|&a, &b| xs[a].partial_cmp(&xs[b]).unwrap_or(std::cmp::Ordering::Equal));
    let mut r = vec![0.0; xs.len()];
    let mut i = 0;
    while i < idx.len() {
        let mut j = i + 1;
        while j < idx.len() && (xs[idx[j]] - xs[idx[i]]).abs() <= 1e-15 {
            j += 1;
        }
        let avg = (i + j - 1) as f64 / 2.0 + 1.0;
        for &k in &idx[i..j] {
            r[k] = avg;
        }
        i = j;
    }
    r
}

impl PartialFit for OnlineSpearman {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if x.ncols() < 2 && y.is_none() {
            ctx.push(
                Issue::builder(IssueCode::InsufficientSample)
                    .severity(Severity::Warning)
                    .message("OnlineSpearman needs two columns or an explicit y")
                    .build(),
            );
        }
        let before = self.score();
        for i in 0..x.nrows() {
            let a = x.get(i, 0);
            let b = if x.ncols() >= 2 {
                x.get(i, 1)
            } else if let Some(y) = y {
                if i < y.len() {
                    y[i]
                } else {
                    f64::NAN
                }
            } else {
                f64::NAN
            };
            if !a.is_finite() || !b.is_finite() {
                continue;
            }
            self.xs.push(a);
            self.ys.push(b);
            if self.xs.len() > 256 {
                self.xs.remove(0);
                self.ys.remove(0);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() >= 3;
        q.warmup = self.xs.len() < 3;
        q.explanation = format!("OnlineSpearman={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Spearman ρ",
                "average ranks of column 0 vs column 1 or y; window length is not p",
                format!("rho={before:.6e}"),
                format!("rho={after:.6e}"),
            ),
        )
    }
}

/// Streaming Hill tail-index on the upper order statistics.
///
/// Reservoir size is not identification `p`.
#[derive(Clone, Debug)]
pub struct HillSketch {
    /// Number of upper order statistics kept.
    pub k: usize,
    heap: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for HillSketch {
    fn default() -> Self {
        Self {
            k: 8,
            heap: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl HillSketch {
    /// Hill reservoir of size `k`.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(3),
            ..Self::default()
        }
    }

    /// Hill \(\xi\), or NaN during warmup.
    pub fn score(&self) -> f64 {
        let k = self.k.max(3).min(self.heap.len());
        if k < 3 {
            return f64::NAN;
        }
        let mut xs = self.heap.clone();
        xs.sort_by(|a, b| b.partial_cmp(a).unwrap_or(std::cmp::Ordering::Equal));
        let floor = xs[k - 1].max(1e-12);
        let mut s = 0.0;
        for v in xs.iter().take(k) {
            s += (v / floor).ln();
        }
        s / k as f64
    }
}

impl PartialFit for HillSketch {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.k.max(3);
        for i in 0..x.nrows() {
            let v = x.get(i, 0).abs();
            if !v.is_finite() || v <= 0.0 {
                continue;
            }
            self.heap.push(v);
            self.n_seen += 1;
        }
        if self.heap.len() > cap * 4 {
            self.heap
                .sort_by(|a, b| b.partial_cmp(a).unwrap_or(std::cmp::Ordering::Equal));
            self.heap.truncate(cap);
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.heap.len().min(cap) as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.heap.len() >= 3;
        q.warmup = self.heap.len() < 3;
        q.explanation = format!("HillSketch xi={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Hill tail index",
                "mean log-excess of the largest |x| over the k-th order statistic",
                format!("xi={before:.6e}"),
                format!("xi={after:.6e}"),
            ),
        )
    }
}

/// Expanding max-abs (river `stats.AbsMax`).
#[derive(Clone, Debug)]
pub struct OnlineAbsMax {
    peak: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineAbsMax {
    fn default() -> Self {
        Self {
            peak: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineAbsMax {
    /// Empty max-abs tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current max |x|, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.peak
        }
    }
}

impl PartialFit for OnlineAbsMax {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0).abs();
            if !v.is_finite() {
                continue;
            }
            if v > self.peak {
                self.peak = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineAbsMax={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding max-abs",
                "running maximum of |column 0|",
                format!("absmax={before:.6e}"),
                format!("absmax={after:.6e}"),
            ),
        )
    }
}

/// One-step lag / shift (river `stats.Shift`).
#[derive(Clone, Debug, Default)]
pub struct OnlineShift {
    last: Option<f64>,
    delayed: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineShift {
    /// Empty lag-1 shift.
    pub fn new() -> Self {
        Self::default()
    }

    /// Previous finite value, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n_seen < 2 {
            f64::NAN
        } else {
            self.delayed
        }
    }
}

impl PartialFit for OnlineShift {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if let Some(prev) = self.last {
                self.delayed = prev;
            }
            self.last = Some(v);
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen.saturating_sub(1) as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineShift={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "lag-1 shift",
                "emit the previous finite column-0 value",
                format!("lag={before:.6e}"),
                format!("lag={after:.6e}"),
            ),
        )
    }
}

/// Streaming multinomial probabilities (river `proba.Multinomial`).
///
/// Class count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct MultinomialProba {
    counts: HashMap<i64, f64>,
    total: f64,
    n_seen: u64,
    updates: u64,
}

impl MultinomialProba {
    /// Empty class counter.
    pub fn new() -> Self {
        Self::default()
    }

    /// Probability of the most frequent class, or NaN before the first label.
    pub fn score(&self) -> f64 {
        if self.total <= 0.0 {
            f64::NAN
        } else {
            self.counts.values().copied().fold(0.0, f64::max) / self.total
        }
    }

    /// \(\hat p(c)\) for integer class `c`.
    pub fn pmf(&self, c: i64) -> f64 {
        if self.total <= 0.0 {
            f64::NAN
        } else {
            self.counts.get(&c).copied().unwrap_or(0.0) / self.total
        }
    }
}

impl PartialFit for MultinomialProba {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(labels.len()) {
            let v = labels[i];
            if !v.is_finite() {
                continue;
            }
            let k = v.round() as i64;
            *self.counts.entry(k).or_insert(0.0) += 1.0;
            self.total += 1.0;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.total;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!(
            "MultinomialProba n_class={} pmax={after:.6e}",
            self.counts.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "multinomial class probabilities",
                "increment integer-label counts; class count is not p",
                format!("pmax={before:.6e}"),
                format!("pmax={after:.6e}"),
            ),
        )
    }
}

/// Streaming 2×2 confusion matrix (river `metrics.ConfusionMatrix`).
///
/// `x` is the prediction, `y` the label. Class count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineConfusion {
    tp: u64,
    fp: u64,
    tn: u64,
    fn_: u64,
    n_seen: u64,
    updates: u64,
}

impl OnlineConfusion {
    /// Empty confusion matrix.
    pub fn new() -> Self {
        Self::default()
    }

    /// Accuracy, or NaN before the first labelled pair.
    pub fn score(&self) -> f64 {
        let n = self.tp + self.fp + self.tn + self.fn_;
        if n == 0 {
            f64::NAN
        } else {
            (self.tp + self.tn) as f64 / n as f64
        }
    }
}

impl PartialFit for OnlineConfusion {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(labels.len()) {
            let pred = x.get(i, 0);
            let truth = labels[i];
            if !pred.is_finite() || !truth.is_finite() {
                continue;
            }
            let p = pred.round() > 0.5;
            let t = truth.round() > 0.5;
            match (p, t) {
                (true, true) => self.tp += 1,
                (true, false) => self.fp += 1,
                (false, false) => self.tn += 1,
                (false, true) => self.fn_ += 1,
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!(
            "OnlineConfusion acc={after:.6e} tp={} fp={}",
            self.tp, self.fp
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming confusion matrix",
                "increment TP/FP/TN/FN from rounded pred vs label",
                format!("acc={before:.6e}"),
                format!("acc={after:.6e}"),
            ),
        )
    }
}

/// Expanding range max−min (river `stats.Range`).
#[derive(Clone, Debug)]
pub struct OnlineRange {
    min: f64,
    max: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineRange {
    fn default() -> Self {
        Self {
            min: f64::INFINITY,
            max: f64::NEG_INFINITY,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineRange {
    /// Empty range tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current max−min, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.max - self.min
        }
    }
}

impl PartialFit for OnlineRange {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v < self.min {
                self.min = v;
            }
            if v > self.max {
                self.max = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineRange={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding range",
                "running max(column 0) − min(column 0)",
                format!("range={before:.6e}"),
                format!("range={after:.6e}"),
            ),
        )
    }
}

/// Expanding minimum (river `stats.Min`).
#[derive(Clone, Debug)]
pub struct OnlineMin {
    min: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineMin {
    fn default() -> Self {
        Self {
            min: f64::INFINITY,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineMin {
    /// Empty min tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current min, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.min
        }
    }
}

impl PartialFit for OnlineMin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v < self.min {
                self.min = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineMin={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding min",
                "running minimum of column 0",
                format!("min={before:.6e}"),
                format!("min={after:.6e}"),
            ),
        )
    }
}

/// Streaming Matthews correlation (river `metrics.MCC`).
///
/// `x` is the prediction, `y` the label.
#[derive(Clone, Debug, Default)]
pub struct OnlineMcc {
    tp: f64,
    fp: f64,
    tn: f64,
    fn_: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineMcc {
    /// Empty MCC tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MCC, or NaN before the first labelled pair.
    pub fn score(&self) -> f64 {
        let n = self.tp + self.fp + self.tn + self.fn_;
        if n <= 0.0 {
            return f64::NAN;
        }
        let den = ((self.tp + self.fp)
            * (self.tp + self.fn_)
            * (self.tn + self.fp)
            * (self.tn + self.fn_))
            .sqrt();
        if den < 1e-18 {
            0.0
        } else {
            (self.tp * self.tn - self.fp * self.fn_) / den
        }
    }
}

impl PartialFit for OnlineMcc {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(labels.len()) {
            let pred = x.get(i, 0);
            let truth = labels[i];
            if !pred.is_finite() || !truth.is_finite() {
                continue;
            }
            let p = pred.round() > 0.5;
            let t = truth.round() > 0.5;
            match (p, t) {
                (true, true) => self.tp += 1.0,
                (true, false) => self.fp += 1.0,
                (false, false) => self.tn += 1.0,
                (false, true) => self.fn_ += 1.0,
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineMcc={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming Matthews correlation",
                "MCC from an expanding 2×2 confusion matrix",
                format!("mcc={before:.6e}"),
                format!("mcc={after:.6e}"),
            ),
        )
    }
}

/// Sliding-window nearest neighbours (river `neighbors.SWINN` lite).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct Swinn {
    /// Window capacity.
    pub window: usize,
    /// Neighbours.
    pub k: usize,
    xs: Vec<Vec<f64>>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Swinn {
    fn default() -> Self {
        Self {
            window: 32,
            k: 3,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Swinn {
    /// SWINN with `k` neighbours.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }

    /// Majority vote of the current window, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.ys.is_empty() {
            f64::NAN
        } else {
            *self.ys.last().unwrap_or(&f64::NAN)
        }
    }
}

impl PartialFit for Swinn {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(labels.len()) {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            self.xs.push(row);
            self.ys.push(labels[i]);
            if self.xs.len() > cap {
                self.xs.remove(0);
                self.ys.remove(0);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() >= self.k;
        q.warmup = self.xs.len() < self.k;
        q.explanation = format!("Swinn window={} last={after:.6e}", self.xs.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sliding-window nearest neighbours",
                "keep the last W labelled rows; window length is not p",
                format!("last={before:.6e}"),
                format!("last={after:.6e}"),
            ),
        )
    }
}

/// 2-D Pareto skyline (river `misc.Skyline`).
#[derive(Clone, Debug, Default)]
pub struct Skyline {
    points: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Skyline {
    /// Empty skyline.
    pub fn new() -> Self {
        Self::default()
    }

    /// Number of non-dominated points, or NaN before the first finite pair.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.points.len() as f64
        }
    }
}

impl PartialFit for Skyline {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let yy = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(yy.len()) {
            let a = x.get(i, 0);
            let b = yy[i];
            if !a.is_finite() || !b.is_finite() {
                continue;
            }
            let dominated = self.points.iter().any(|&(u, v)| u >= a && v >= b && (u > a || v > b));
            if !dominated {
                self.points.retain(|&(u, v)| !(a >= u && b >= v && (a > u || b > v)));
                self.points.push((a, b));
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.points.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("Skyline n={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Pareto skyline",
                "keep 2-D points that are not dominated in both coordinates",
                format!("n={before:.6e}"),
                format!("n={after:.6e}"),
            ),
        )
    }
}

/// Sliding discrete Fourier transform magnitude (river `misc.SDFT`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct Sdft {
    /// Window.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Sdft {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Sdft {
    /// SDFT with `window` samples.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Magnitude of the first harmonic, or NaN during warmup.
    pub fn score(&self) -> f64 {
        let w = self.window.max(2);
        if self.buf.len() < w {
            return f64::NAN;
        }
        let mut re = 0.0;
        let mut im = 0.0;
        let n = self.buf.len();
        for (t, &v) in self.buf.iter().enumerate() {
            let ang = 2.0 * std::f64::consts::PI * t as f64 / n as f64;
            re += v * ang.cos();
            im += v * ang.sin();
        }
        (re * re + im * im).sqrt() / n as f64
    }
}

impl PartialFit for Sdft {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.buf.push(v);
            if self.buf.len() > cap {
                self.buf.remove(0);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= cap;
        q.warmup = self.buf.len() < cap;
        q.explanation = format!("Sdft={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sliding DFT",
                "first-harmonic magnitude of the last W samples",
                format!("mag={before:.6e}"),
                format!("mag={after:.6e}"),
            ),
        )
    }
}

/// AMSGrad linear regressor (river `optim.AMSGrad`).
#[derive(Clone, Debug)]
pub struct AmsGradRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Floor \(\varepsilon\).
    pub eps: f64,
    /// Intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    v: Vector,
    vhat: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AmsGradRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta2: 0.99,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            v: Vector::zeros(0),
            vhat: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AmsGradRegressor {
    /// Default AMSGrad regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AmsGradRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.vhat = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                self.vhat[j] = self.vhat[j].max(self.v[j]);
                self.coef[j] -= eta * g / (self.vhat[j].sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AMSGrad linear weights",
            "max-of-second-moment rescales each coordinate",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AmsGradRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i)));
        ctx.finish(out)
    }
}

/// Streaming balanced accuracy (river `metrics.BalancedAccuracy`).
#[derive(Clone, Debug, Default)]
pub struct OnlineBalancedAccuracy {
    tp: f64,
    tn: f64,
    p: f64,
    n: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineBalancedAccuracy {
    /// Empty balanced-accuracy tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// \((\mathrm{TPR}+\mathrm{TNR})/2\), or NaN before both classes appear.
    pub fn score(&self) -> f64 {
        if self.p <= 0.0 || self.n <= 0.0 {
            f64::NAN
        } else {
            0.5 * (self.tp / self.p + self.tn / self.n)
        }
    }
}

impl PartialFit for OnlineBalancedAccuracy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(labels.len()) {
            let pred = x.get(i, 0);
            let truth = labels[i];
            if !pred.is_finite() || !truth.is_finite() {
                continue;
            }
            let p = pred.round() > 0.5;
            let t = truth.round() > 0.5;
            if t {
                self.p += 1.0;
                if p {
                    self.tp += 1.0;
                }
            } else {
                self.n += 1.0;
                if !p {
                    self.tn += 1.0;
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.p > 0.0 && self.n > 0.0;
        q.warmup = self.p <= 0.0 || self.n <= 0.0;
        q.explanation = format!("OnlineBalancedAccuracy={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming balanced accuracy",
                "average of TPR and TNR from an expanding 2×2 table",
                format!("ba={before:.6e}"),
                format!("ba={after:.6e}"),
            ),
        )
    }
}

/// Streaming Gini impurity of integer labels (river `stats.Gini`).
#[derive(Clone, Debug, Default)]
pub struct OnlineGini {
    counts: HashMap<i64, f64>,
    total: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineGini {
    /// Empty Gini tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// \(1-\sum p_k^2\), or NaN before the first label.
    pub fn score(&self) -> f64 {
        if self.total <= 0.0 {
            f64::NAN
        } else {
            let mut ss = 0.0;
            for c in self.counts.values() {
                let p = *c / self.total;
                ss += p * p;
            }
            1.0 - ss
        }
    }
}

impl PartialFit for OnlineGini {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        let fallback = Vector::from_iter((0..x.nrows()).map(|i| x.get(i, 0)));
        let labels = y.unwrap_or(&fallback);
        for i in 0..x.nrows().min(labels.len()) {
            let v = labels[i];
            if !v.is_finite() {
                continue;
            }
            *self.counts.entry(v.round() as i64).or_insert(0.0) += 1.0;
            self.total += 1.0;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.total;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineGini={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming Gini impurity",
                "1 − Σ p_k² over integer-label counts; class count is not p",
                format!("gini={before:.6e}"),
                format!("gini={after:.6e}"),
            ),
        )
    }
}

/// Exponentially weighted mean (river `stats.EWMean`).
#[derive(Clone, Debug)]
pub struct OnlineEwMean {
    /// Smoothing \(\alpha\in(0,1]\).
    pub alpha: f64,
    mean: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineEwMean {
    fn default() -> Self {
        Self {
            alpha: 0.5,
            mean: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineEwMean {
    /// EW mean with the given \(\alpha\).
    pub fn new(alpha: f64) -> Self {
        Self {
            alpha,
            ..Self::default()
        }
    }

    /// Current EW mean, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.mean
        }
    }
}

impl PartialFit for OnlineEwMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let a = if self.alpha.is_finite() && self.alpha > 0.0 && self.alpha <= 1.0 {
            self.alpha
        } else {
            0.5
        };
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if self.n_seen == 0 {
                self.mean = v;
            } else {
                self.mean = a * v + (1.0 - a) * self.mean;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = (1.0 / a).min(self.n_seen as f64);
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineEwMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exponentially weighted mean",
                "s ← α x + (1−α) s on column 0",
                format!("ew={before:.6e}"),
                format!("ew={after:.6e}"),
            ),
        )
    }
}

/// Exact unique-value counter (river `stats.NUnique`).
///
/// Distinct-key count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct NUnique {
    keys: HashSet<i64>,
    n_seen: u64,
    updates: u64,
}

impl NUnique {
    /// Empty unique counter.
    pub fn new() -> Self {
        Self::default()
    }

    /// Number of distinct rounded values, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.keys.len() as f64
        }
    }
}

impl PartialFit for NUnique {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.keys.insert(v.round() as i64);
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("NUnique={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exact unique count",
                "insert rounded column-0 keys; cardinality is not p",
                format!("n={before:.6e}"),
                format!("n={after:.6e}"),
            ),
        )
    }
}

/// Expanding geometric mean of strictly positive values (river `stats.GeometricMean`).
///
/// Non-positive observations are skipped with a warning; they do not abort.
#[derive(Clone, Debug, Default)]
pub struct OnlineGeometricMean {
    log_sum: f64,
    n_pos: u64,
    n_seen: u64,
    updates: u64,
}

impl OnlineGeometricMean {
    /// Empty geometric mean.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current geometric mean, or NaN before the first positive value.
    pub fn score(&self) -> f64 {
        if self.n_pos == 0 {
            f64::NAN
        } else {
            (self.log_sum / self.n_pos as f64).exp()
        }
    }
}

impl PartialFit for OnlineGeometricMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n_seen += 1;
            if v <= 0.0 {
                skipped += 1;
                continue;
            }
            self.log_sum += v.ln();
            self.n_pos += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineGeometricMean skipped {skipped} non-positive values"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_pos as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_pos >= 1;
        q.warmup = self.n_pos < 1;
        q.explanation = format!("OnlineGeometricMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding geometric mean",
                "exp(mean log x) on strictly positive column 0",
                format!("g={before:.6e}"),
                format!("g={after:.6e}"),
            ),
        )
    }
}

/// Expanding signed maximum (river `stats.Max`).
///
/// Distinct from [`OnlineAbsMax`].
#[derive(Clone, Debug)]
pub struct OnlineMax {
    max: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineMax {
    fn default() -> Self {
        Self {
            max: f64::NEG_INFINITY,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineMax {
    /// Empty max tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current max, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.max
        }
    }
}

impl PartialFit for OnlineMax {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v > self.max {
                self.max = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineMax={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding signed max",
                "running maximum of column 0",
                format!("max={before:.6e}"),
                format!("max={after:.6e}"),
            ),
        )
    }
}

/// Streaming Fβ (river `metrics.FBeta`).
#[derive(Clone, Debug)]
pub struct OnlineFbeta {
    /// β weight on recall.
    pub beta: f64,
    tp: f64,
    fp: f64,
    fn_: f64,
    updates: u64,
}

impl Default for OnlineFbeta {
    fn default() -> Self {
        Self {
            beta: 1.0,
            tp: 0.0,
            fp: 0.0,
            fn_: 0.0,
            updates: 0,
        }
    }
}

impl OnlineFbeta {
    /// Fβ with the given β.
    pub fn new(beta: f64) -> Self {
        Self {
            beta,
            ..Self::default()
        }
    }

    /// Current Fβ, or NaN when the denominator is zero.
    pub fn score(&self) -> f64 {
        let b2 = self.beta * self.beta;
        let den = (1.0 + b2) * self.tp + b2 * self.fn_ + self.fp;
        if den <= 0.0 {
            f64::NAN
        } else {
            (1.0 + b2) * self.tp / den
        }
    }
}

impl PartialFit for OnlineFbeta {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "fbeta",
            x,
            y,
            self.updates,
            self.tp as u64,
            |pred, truth| {
                let p = pred > 0.5;
                let t = truth > 0.5;
                if p && t {
                    self.tp += 1.0;
                } else if p && !t {
                    self.fp += 1.0;
                } else if !p && t {
                    self.fn_ += 1.0;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Streaming MAPE (river `metrics.MAPE`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMape {
    sum: f64,
    n: f64,
    updates: u64,
}

impl OnlineMape {
    /// Empty MAPE.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MAPE, or NaN before a usable pair.
    pub fn score(&self) -> f64 {
        if self.n <= 0.0 {
            f64::NAN
        } else {
            self.sum / self.n
        }
    }
}

impl PartialFit for OnlineMape {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "mape",
            x,
            y,
            self.updates,
            self.n as u64,
            |pred, truth| {
                if truth.abs() > 1e-12 {
                    self.sum += (pred - truth).abs() / truth.abs();
                    self.n += 1.0;
                }
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Time / index rolling mean (river `utils.TimeRolling` lite).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct TimeRolling {
    /// Window length in observations.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for TimeRolling {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl TimeRolling {
    /// Rolling window of length `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for TimeRolling {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            rolling_push(&mut self.buf, v, cap);
            if v.is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.len() < cap;
        q.explanation = format!("TimeRolling={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "time-index rolling mean",
                "mean of the last W observations; W is not p",
                format!("m={before:.6e}"),
                format!("m={after:.6e}"),
            ),
        )
    }
}

/// Exponentially weighted variance (river `stats.EWVar`).
///
/// Distinct named companion to [`OnlineEwMean`].
#[derive(Clone, Debug)]
pub struct OnlineEwVar {
    /// Smoothing \(\alpha\in(0,1]\).
    pub alpha: f64,
    mean: f64,
    var: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineEwVar {
    fn default() -> Self {
        Self {
            alpha: 0.5,
            mean: 0.0,
            var: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineEwVar {
    /// EW variance with the given \(\alpha\).
    pub fn new(alpha: f64) -> Self {
        Self {
            alpha,
            ..Self::default()
        }
    }

    /// Current EW variance, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n_seen < 2 {
            f64::NAN
        } else {
            self.var
        }
    }
}

impl PartialFit for OnlineEwVar {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let a = if self.alpha.is_finite() && self.alpha > 0.0 && self.alpha <= 1.0 {
            self.alpha
        } else {
            0.5
        };
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if self.n_seen == 0 {
                self.mean = v;
                self.var = 0.0;
            } else {
                let d = v - self.mean;
                self.mean = a * v + (1.0 - a) * self.mean;
                self.var = (1.0 - a) * (self.var + a * d * d);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = (1.0 / a).min(self.n_seen as f64);
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineEwVar={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exponentially weighted variance",
                "EW Welford on column 0",
                format!("v={before:.6e}"),
                format!("v={after:.6e}"),
            ),
        )
    }
}

/// Streaming weighted mean (river `stats.WeightedMean`).
///
/// `y` holds weights when present; otherwise each row weighs 1.
#[derive(Clone, Debug, Default)]
pub struct OnlineWeightedMean {
    sw: f64,
    sx: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineWeightedMean {
    /// Empty weighted mean.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current weighted mean, or NaN before the first finite pair.
    pub fn score(&self) -> f64 {
        if self.sw.abs() <= 1e-15 {
            f64::NAN
        } else {
            self.sx / self.sw
        }
    }
}

impl PartialFit for OnlineWeightedMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            let w = y
                .and_then(|yy| yy.as_slice().get(i).copied())
                .filter(|u| u.is_finite() && *u > 0.0)
                .unwrap_or(1.0);
            self.sx += w * v;
            self.sw += w;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.sw;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineWeightedMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "weighted mean",
                "Σ w x / Σ w; y is the weight when present",
                format!("m={before:.6e}"),
                format!("m={after:.6e}"),
            ),
        )
    }
}

/// Bayesian running mean with a unit-information prior (river `stats.BayesianMean`).
#[derive(Clone, Debug)]
pub struct OnlineBayesianMean {
    /// Prior mean.
    pub prior_mean: f64,
    /// Prior pseudo-count.
    pub prior_n: f64,
    sum: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineBayesianMean {
    fn default() -> Self {
        Self {
            prior_mean: 0.0,
            prior_n: 1.0,
            sum: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineBayesianMean {
    /// Unit-information prior at `prior_mean`.
    pub fn new(prior_mean: f64) -> Self {
        Self {
            prior_mean,
            ..Self::default()
        }
    }

    /// Posterior mean.
    pub fn score(&self) -> f64 {
        let n0 = self.prior_n.max(0.0);
        (n0 * self.prior_mean + self.sum) / (n0 + self.n_seen as f64).max(1e-12)
    }
}

impl PartialFit for OnlineBayesianMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.sum += v;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.prior_n.max(0.0) + self.n_seen as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineBayesianMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Bayesian mean",
                "posterior mean of a normal with a unit-information prior",
                format!("m={before:.6e}"),
                format!("m={after:.6e}"),
            ),
        )
    }
}

/// Rolling mean (river `stats.RollingMean`).
#[derive(Clone, Debug, Default)]
pub struct RollingMean {
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl RollingMean {
    /// Empty rolling mean.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.window.is_empty() {
            f64::NAN
        } else {
            self.window.iter().sum::<f64>() / self.window.len() as f64
        }
    }
}

impl PartialFit for RollingMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            rolling_push(&mut self.window, x.get(i, 0), 256);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.window.is_empty();
        q.warmup = self.window.is_empty();
        q.explanation = format!("RollingMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed mean",
                "256-row sliding mean of column 0",
                format!("mean={before:.6e}"),
                format!("mean={after:.6e}"),
            ),
        )
    }
}

/// Expanding harmonic mean (river `stats.HarmonicMean`).
///
/// Non-positive values are skipped with a warning. Test streams that start at
/// 0 must not abort.
#[derive(Clone, Debug, Default)]
pub struct OnlineHarmonicMean {
    inv_sum: f64,
    n_pos: u64,
    n_seen: u64,
    updates: u64,
}

impl OnlineHarmonicMean {
    /// Empty harmonic mean.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current harmonic mean, or NaN before the first positive value.
    pub fn score(&self) -> f64 {
        if self.n_pos == 0 || self.inv_sum.abs() < 1e-300 {
            f64::NAN
        } else {
            self.n_pos as f64 / self.inv_sum
        }
    }
}

impl PartialFit for OnlineHarmonicMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n_seen += 1;
            if v <= 0.0 {
                skipped += 1;
                continue;
            }
            self.inv_sum += 1.0 / v;
            self.n_pos += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineHarmonicMean skipped {skipped} non-positive values"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_pos as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_pos >= 1;
        q.warmup = self.n_pos < 1;
        q.explanation = format!("OnlineHarmonicMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding harmonic mean",
                "n / sum(1/x) on strictly positive column 0",
                format!("h={before:.6e}"),
                format!("h={after:.6e}"),
            ),
        )
    }
}

/// Expanding quadratic mean / RMS (river `stats.QuadraticMean`).
#[derive(Clone, Debug, Default)]
pub struct OnlineQuadraticMean {
    sumsq: f64,
    n_seen: u64,
    updates: u64,
}

impl OnlineQuadraticMean {
    /// Empty quadratic mean.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current RMS, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            (self.sumsq / self.n_seen as f64).sqrt()
        }
    }
}

impl PartialFit for OnlineQuadraticMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.sumsq += v * v;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlineQuadraticMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding quadratic mean",
                "RMS of column 0",
                format!("rms={before:.6e}"),
                format!("rms={after:.6e}"),
            ),
        )
    }
}

/// Rolling variance (river `stats.RollingVar`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingVar {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingVar {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingVar {
    /// Rolling variance with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current sample variance, or NaN when the window has fewer than 2 points.
    pub fn score(&self) -> f64 {
        if self.buf.len() < 2 {
            f64::NAN
        } else {
            let n = self.buf.len() as f64;
            let mean = self.buf.iter().sum::<f64>() / n;
            let ss = self.buf.iter().map(|v| (v - mean) * (v - mean)).sum::<f64>();
            ss / (n - 1.0)
        }
    }
}

impl PartialFit for RollingVar {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!("RollingVar={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed variance",
                "sliding sample variance of column 0; window is not identification p",
                format!("var={before:.6e}"),
                format!("var={after:.6e}"),
            ),
        )
    }
}

/// Rolling covariance of column 0 and `y` (river `stats.RollingCov`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingCov {
    /// Window capacity.
    pub window: usize,
    xs: Vec<f64>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingCov {
    fn default() -> Self {
        Self {
            window: 8,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingCov {
    /// Rolling covariance with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current sample covariance, or NaN when the window has fewer than 2 pairs.
    pub fn score(&self) -> f64 {
        let n = self.xs.len().min(self.ys.len());
        if n < 2 {
            f64::NAN
        } else {
            let nf = n as f64;
            let mx = self.xs.iter().take(n).sum::<f64>() / nf;
            let my = self.ys.iter().take(n).sum::<f64>() / nf;
            let mut c = 0.0;
            for i in 0..n {
                c += (self.xs[i] - mx) * (self.ys[i] - my);
            }
            c / (nf - 1.0)
        }
    }
}

impl PartialFit for RollingCov {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingCov"),
            );
        };
        let before = self.score();
        let cap = self.window.max(2);
        let n = x.nrows().min(y.len());
        for i in 0..n {
            let xv = x.get(i, 0);
            let yv = y[i];
            if xv.is_finite() && yv.is_finite() {
                rolling_push(&mut self.xs, xv, cap);
                rolling_push(&mut self.ys, yv, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len().min(self.ys.len()) as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() >= 2 && self.ys.len() >= 2;
        q.warmup = self.xs.len() < 2;
        q.explanation = format!("RollingCov={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed covariance",
                "sliding sample covariance of column 0 and y; window is not identification p",
                format!("cov={before:.6e}"),
                format!("cov={after:.6e}"),
            ),
        )
    }
}

fn window_quantile(xs: &[f64], q: f64) -> f64 {
    if xs.is_empty() {
        return f64::NAN;
    }
    let mut v: Vec<f64> = xs.to_vec();
    v.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
    let qv = if q.is_finite() { q.clamp(0.0, 1.0) } else { 0.5 };
    let idx = (qv * (v.len() - 1) as f64).round() as usize;
    v[idx.min(v.len() - 1)]
}

/// Expanding median (river `stats.Quantile(0.5)` / `stats.Median`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMedian {
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl OnlineMedian {
    /// Empty median tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current median, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_quantile(&self.buf, 0.5)
    }
}

impl PartialFit for OnlineMedian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if v.is_finite() {
                self.buf.push(v);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("OnlineMedian={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding median",
                "running median of column 0",
                format!("med={before:.6e}"),
                format!("med={after:.6e}"),
            ),
        )
    }
}

/// Rolling median (river `stats.RollingQuantile(0.5)`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingMedian {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingMedian {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingMedian {
    /// Rolling median with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current window median, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_quantile(&self.buf, 0.5)
    }
}

impl PartialFit for RollingMedian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingMedian={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed median",
                "sliding median of column 0; window is not identification p",
                format!("med={before:.6e}"),
                format!("med={after:.6e}"),
            ),
        )
    }
}

/// Expanding standard deviation (river `stats.Var` square-root).
#[derive(Clone, Debug, Default)]
pub struct OnlineStd {
    n: f64,
    mean: f64,
    m2: f64,
    updates: u64,
}

impl OnlineStd {
    /// Empty standard-deviation accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current sample std, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            f64::NAN
        } else {
            (self.m2 / (self.n - 1.0)).sqrt()
        }
    }
}

impl PartialFit for OnlineStd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineStd={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford standard deviation",
                "running sample std of column 0",
                format!("std={before:.6e}"),
                format!("std={after:.6e}"),
            ),
        )
    }
}

/// Rolling standard deviation (river `stats.RollingVar` square-root).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingStd {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingStd {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingStd {
    /// Rolling std with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current sample std, or NaN when the window has fewer than 2 points.
    pub fn score(&self) -> f64 {
        if self.buf.len() < 2 {
            f64::NAN
        } else {
            let n = self.buf.len() as f64;
            let mean = self.buf.iter().sum::<f64>() / n;
            let ss = self.buf.iter().map(|v| (v - mean) * (v - mean)).sum::<f64>();
            (ss / (n - 1.0)).sqrt()
        }
    }
}

impl PartialFit for RollingStd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!("RollingStd={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed standard deviation",
                "sliding sample std of column 0; window is not identification p",
                format!("std={before:.6e}"),
                format!("std={after:.6e}"),
            ),
        )
    }
}

/// Rolling sum (river `stats.RollingSum`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingSum {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingSum {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingSum {
    /// Rolling sum with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window sum, or 0 when empty.
    pub fn score(&self) -> f64 {
        self.buf.iter().sum()
    }
}

impl PartialFit for RollingSum {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingSum={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sum",
                "sliding sum of column 0; window is not identification p",
                format!("sum={before:.6e}"),
                format!("sum={after:.6e}"),
            ),
        )
    }
}

/// Rolling inter-quartile range (river `stats.RollingIQR`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingIqr {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingIqr {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingIqr {
    /// Rolling IQR with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current IQR, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            window_quantile(&self.buf, 0.75) - window_quantile(&self.buf, 0.25)
        }
    }
}

impl PartialFit for RollingIqr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!("RollingIqr={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed IQR",
                "sliding Q3−Q1 of column 0; window is not identification p",
                format!("iqr={before:.6e}"),
                format!("iqr={after:.6e}"),
            ),
        )
    }
}

/// Expanding peak-to-peak range (river `stats.PeakToPeak`).
#[derive(Clone, Debug)]
pub struct OnlinePeakToPeak {
    min: f64,
    max: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlinePeakToPeak {
    fn default() -> Self {
        Self {
            min: f64::INFINITY,
            max: f64::NEG_INFINITY,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlinePeakToPeak {
    /// Empty peak-to-peak tracker.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current range, or NaN before the first finite value.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 {
            f64::NAN
        } else {
            self.max - self.min
        }
    }
}

impl PartialFit for OnlinePeakToPeak {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v < self.min {
                self.min = v;
            }
            if v > self.max {
                self.max = v;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("OnlinePeakToPeak={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding peak-to-peak",
                "running max−min of column 0",
                format!("ptp={before:.6e}"),
                format!("ptp={after:.6e}"),
            ),
        )
    }
}

/// Rolling peak-to-peak (river `stats.RollingPeakToPeak`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingPeakToPeak {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingPeakToPeak {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingPeakToPeak {
    /// Rolling peak-to-peak with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current window range, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            let mn = self.buf.iter().copied().fold(f64::INFINITY, f64::min);
            let mx = self.buf.iter().copied().fold(f64::NEG_INFINITY, f64::max);
            mx - mn
        }
    }
}

impl PartialFit for RollingPeakToPeak {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingPeakToPeak={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed peak-to-peak",
                "sliding max−min of column 0; window is not identification p",
                format!("ptp={before:.6e}"),
                format!("ptp={after:.6e}"),
            ),
        )
    }
}

/// Rolling standard error of the mean (river `stats.RollingSEM`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingSem {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingSem {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingSem {
    /// Rolling SEM with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current SEM, or NaN when the window has fewer than 2 points.
    pub fn score(&self) -> f64 {
        if self.buf.len() < 2 {
            f64::NAN
        } else {
            let n = self.buf.len() as f64;
            let mean = self.buf.iter().sum::<f64>() / n;
            let ss = self.buf.iter().map(|v| (v - mean) * (v - mean)).sum::<f64>();
            ((ss / (n - 1.0)) / n).sqrt()
        }
    }
}

impl PartialFit for RollingSem {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!("RollingSem={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed SEM",
                "sliding s/√n of column 0; window is not identification p",
                format!("sem={before:.6e}"),
                format!("sem={after:.6e}"),
            ),
        )
    }
}

/// Streaming mean squared log error (river `metrics.MSLE`).
///
/// Non-positive pairs are skipped with a warning.
#[derive(Clone, Debug, Default)]
pub struct OnlineMsle {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineMsle {
    /// Empty MSLE.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MSLE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineMsle {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n, "no y for OnlineMsle"),
            );
        };
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if !pred.is_finite() || !truth.is_finite() || pred <= -1.0 || truth <= -1.0 {
                skipped += 1;
                continue;
            }
            let d = (1.0 + pred).ln() - (1.0 + truth).ln();
            self.acc += d * d;
            self.n += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineMsle skipped {skipped} pairs with pred or truth ≤ −1"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlineMsle={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "mean squared log error",
                "mean (log(1+pred)−log(1+y))²; pairs with values ≤ −1 are skipped",
                format!("msle={before:.6e}"),
                format!("msle={after:.6e}"),
            ),
        )
    }
}

/// Streaming Brier score (river `metrics.Brier`).
///
/// Column 0 of `x` is the predicted probability; `y` is the 0/1 label.
#[derive(Clone, Debug, Default)]
pub struct OnlineBrier {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineBrier {
    /// Empty Brier accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current Brier score, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineBrier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n, "no y for OnlineBrier"),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            let p = x.get(i, 0);
            let t = y[i];
            if !p.is_finite() || !t.is_finite() {
                continue;
            }
            let p = p.clamp(0.0, 1.0);
            let t = if t > 0.5 { 1.0 } else { 0.0 };
            let d = p - t;
            self.acc += d * d;
            self.n += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlineBrier={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Brier score",
                "mean (p−y)² with p = col0 clipped to [0,1] and y hard-thresholded",
                format!("brier={before:.6e}"),
                format!("brier={after:.6e}"),
            ),
        )
    }
}

/// Streaming cross-entropy on soft labels (river `metrics.CrossEntropy`).
///
/// Distinct from [`OnlineLogLoss`]: `y` is treated as a probability in
/// \([0,1]\), not hard-thresholded.
#[derive(Clone, Debug, Default)]
pub struct OnlineCrossEntropy {
    nll: f64,
    n: u64,
    updates: u64,
}

impl OnlineCrossEntropy {
    /// Empty cross-entropy accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean cross-entropy, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.nll / self.n as f64
        }
    }
}

impl PartialFit for OnlineCrossEntropy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n,
                    "no y for OnlineCrossEntropy",
                ),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            let p = x.get(i, 0);
            let t = y[i];
            if !p.is_finite() || !t.is_finite() {
                continue;
            }
            let p = p.clamp(1e-12, 1.0 - 1e-12);
            let t = t.clamp(0.0, 1.0);
            self.nll += -(t * p.ln() + (1.0 - t) * (1.0 - p).ln());
            self.n += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlineCrossEntropy={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "soft cross-entropy",
                "mean −[y ln p + (1−y) ln(1−p)] with y kept as a probability",
                format!("ce={before:.6e}"),
                format!("ce={after:.6e}"),
            ),
        )
    }
}

fn window_skew(buf: &[f64]) -> f64 {
    if buf.len() < 3 {
        return f64::NAN;
    }
    let n = buf.len() as f64;
    let mean = buf.iter().sum::<f64>() / n;
    let mut m2 = 0.0;
    let mut m3 = 0.0;
    for &v in buf {
        let d = v - mean;
        m2 += d * d;
        m3 += d * d * d;
    }
    if m2 <= 1e-18 {
        f64::NAN
    } else {
        n.sqrt() * m3 / m2.powf(1.5)
    }
}

fn window_kurtosis(buf: &[f64]) -> f64 {
    if buf.len() < 4 {
        return f64::NAN;
    }
    let n = buf.len() as f64;
    let mean = buf.iter().sum::<f64>() / n;
    let mut m2 = 0.0;
    let mut m4 = 0.0;
    for &v in buf {
        let d = v - mean;
        let d2 = d * d;
        m2 += d2;
        m4 += d2 * d2;
    }
    if m2 <= 1e-18 {
        f64::NAN
    } else {
        n * m4 / (m2 * m2) - 3.0
    }
}

fn window_entropy(buf: &[f64]) -> f64 {
    if buf.is_empty() {
        return f64::NAN;
    }
    let mut counts: HashMap<i64, u64> = HashMap::new();
    for &v in buf {
        *counts.entry(v.round() as i64).or_insert(0) += 1;
    }
    let tot = buf.len() as f64;
    let mut h = 0.0;
    for c in counts.values() {
        let p = *c as f64 / tot;
        if p > 0.0 {
            h -= p * p.ln();
        }
    }
    h
}

/// Rolling skewness (river `stats.RollingSkew`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingSkew {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingSkew {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingSkew {
    /// Rolling skewness with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(3),
            ..Self::default()
        }
    }

    /// Current window skewness, or NaN during warmup.
    pub fn score(&self) -> f64 {
        window_skew(&self.buf)
    }
}

impl PartialFit for RollingSkew {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(3);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 3;
        q.warmup = self.buf.len() < 3;
        q.explanation = format!("RollingSkew={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed skewness",
                "sliding third-moment skew of column 0; window is not identification p",
                format!("skew={before:.6e}"),
                format!("skew={after:.6e}"),
            ),
        )
    }
}

/// Rolling excess kurtosis (river `stats.RollingKurtosis`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingKurtosis {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingKurtosis {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingKurtosis {
    /// Rolling kurtosis with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(4),
            ..Self::default()
        }
    }

    /// Current excess kurtosis, or NaN during warmup.
    pub fn score(&self) -> f64 {
        window_kurtosis(&self.buf)
    }
}

impl PartialFit for RollingKurtosis {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(4);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 4;
        q.warmup = self.buf.len() < 4;
        q.explanation = format!("RollingKurtosis={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed excess kurtosis",
                "sliding fourth-moment kurtosis of column 0; window is not identification p",
                format!("kurt={before:.6e}"),
                format!("kurt={after:.6e}"),
            ),
        )
    }
}

/// Rolling Shannon entropy of rounded values (river `stats.RollingEntropy`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingEntropy {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingEntropy {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingEntropy {
    /// Rolling entropy with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current entropy in nats, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_entropy(&self.buf)
    }
}

impl PartialFit for RollingEntropy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let cap = self.window.max(2);
        for i in 0..x.nrows() {
            rolling_push(&mut self.buf, x.get(i, 0), cap);
            if x.get(i, 0).is_finite() {
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!("RollingEntropy={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed entropy",
                "Shannon entropy of rounded column-0 values in the window",
                format!("H={before:.6e}"),
                format!("H={after:.6e}"),
            ),
        )
    }
}

/// Rolling Pearson correlation of column 0 and `y` (river `stats.RollingPearsonCorr`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingCorr {
    /// Window capacity.
    pub window: usize,
    xs: Vec<f64>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingCorr {
    fn default() -> Self {
        Self {
            window: 8,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingCorr {
    /// Rolling correlation with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current Pearson correlation, or NaN when the window is degenerate.
    pub fn score(&self) -> f64 {
        let n = self.xs.len().min(self.ys.len());
        if n < 2 {
            return f64::NAN;
        }
        let nf = n as f64;
        let mx = self.xs.iter().take(n).sum::<f64>() / nf;
        let my = self.ys.iter().take(n).sum::<f64>() / nf;
        let mut c = 0.0;
        let mut vx = 0.0;
        let mut vy = 0.0;
        for i in 0..n {
            let dx = self.xs[i] - mx;
            let dy = self.ys[i] - my;
            c += dx * dy;
            vx += dx * dx;
            vy += dy * dy;
        }
        let den = (vx * vy).sqrt();
        if den <= 1e-18 {
            f64::NAN
        } else {
            c / den
        }
    }
}

impl PartialFit for RollingCorr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingCorr"),
            );
        };
        let before = self.score();
        let cap = self.window.max(2);
        let n = x.nrows().min(y.len());
        for i in 0..n {
            let xv = x.get(i, 0);
            let yv = y[i];
            if xv.is_finite() && yv.is_finite() {
                rolling_push(&mut self.xs, xv, cap);
                rolling_push(&mut self.ys, yv, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len().min(self.ys.len()) as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len().min(self.ys.len()) >= 2;
        q.warmup = self.xs.len().min(self.ys.len()) < 2;
        q.explanation = format!("RollingCorr={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Pearson correlation",
                "sliding corr of column 0 and y; window is not identification p",
                format!("r={before:.6e}"),
                format!("r={after:.6e}"),
            ),
        )
    }
}

/// Rolling MAE (river `metrics.Rolling` + MAE).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingMae {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingMae {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingMae {
    /// Rolling MAE with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window MAE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingMae {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingMae"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                rolling_push(&mut self.buf, (pred - truth).abs(), cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingMae={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed MAE",
                "sliding mean |pred−y|; window is not identification p",
                format!("mae={before:.6e}"),
                format!("mae={after:.6e}"),
            ),
        )
    }
}

/// Rolling MAPE (river `metrics.Rolling` + MAPE).
///
/// Window length is not identification `p`. Zeros in `y` are skipped.
#[derive(Clone, Debug)]
pub struct RollingMape {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingMape {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingMape {
    /// Rolling MAPE with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window MAPE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingMape {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingMape"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        let mut skipped = 0usize;
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if !pred.is_finite() || !truth.is_finite() || truth.abs() <= 1e-12 {
                skipped += 1;
                continue;
            }
            rolling_push(&mut self.buf, (pred - truth).abs() / truth.abs(), cap);
            self.n_seen += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!("RollingMape skipped {skipped} pairs with |y|≈0"))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingMape={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed MAPE",
                "sliding |pred−y|/|y|; zeros in y are skipped",
                format!("mape={before:.6e}"),
                format!("mape={after:.6e}"),
            ),
        )
    }
}

/// Rolling RMSE (river `metrics.Rolling` + RMSE).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingRmse {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingRmse {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingRmse {
    /// Rolling RMSE with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window RMSE, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            let ms = self.buf.iter().map(|e| e * e).sum::<f64>() / self.buf.len() as f64;
            ms.sqrt()
        }
    }
}

impl PartialFit for RollingRmse {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingRmse"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                rolling_push(&mut self.buf, pred - truth, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingRmse={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed RMSE",
                "sliding root-mean-square error; window is not identification p",
                format!("rmse={before:.6e}"),
                format!("rmse={after:.6e}"),
            ),
        )
    }
}

/// Streaming Matthews correlation (river `metrics.MCC`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMatthews {
    tp: f64,
    tn: f64,
    fp: f64,
    fn_: f64,
    updates: u64,
}

impl OnlineMatthews {
    /// Empty MCC.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current MCC, or NaN when a margin is empty.
    pub fn score(&self) -> f64 {
        let den = ((self.tp + self.fp)
            * (self.tp + self.fn_)
            * (self.tn + self.fp)
            * (self.tn + self.fn_))
            .sqrt();
        if den <= 1e-18 {
            f64::NAN
        } else {
            (self.tp * self.tn - self.fp * self.fn_) / den
        }
    }
}

impl PartialFit for OnlineMatthews {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    (self.tp + self.tn + self.fp + self.fn_) as u64,
                    "no y for OnlineMatthews",
                ),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if !pred.is_finite() || !truth.is_finite() {
                continue;
            }
            let p = pred > 0.5;
            let t = truth > 0.5;
            match (p, t) {
                (true, true) => self.tp += 1.0,
                (false, false) => self.tn += 1.0,
                (true, false) => self.fp += 1.0,
                (false, true) => self.fn_ += 1.0,
            }
        }
        self.updates += 1;
        let n = (self.tp + self.tn + self.fp + self.fn_) as u64;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), n);
        q.effective_sample_size = n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = after.is_finite();
        q.warmup = !after.is_finite();
        q.explanation = format!("OnlineMatthews={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Matthews correlation",
                "streaming (TP·TN−FP·FN) / √((TP+FP)(TP+FN)(TN+FP)(TN+FN))",
                format!("mcc={before:.6e}"),
                format!("mcc={after:.6e}"),
            ),
        )
    }
}

/// Streaming Poisson deviance (river `metrics.PoissonDeviance`).
///
/// Non-positive means or counts are skipped with a warning.
#[derive(Clone, Debug, Default)]
pub struct OnlinePoissonDeviance {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlinePoissonDeviance {
    /// Empty Poisson deviance.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean deviance, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlinePoissonDeviance {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n,
                    "no y for OnlinePoissonDeviance",
                ),
            );
        };
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows().min(y.len()) {
            let mu = x.get(i, 0);
            let yi = y[i];
            if !mu.is_finite() || !yi.is_finite() || mu <= 0.0 || yi < 0.0 {
                skipped += 1;
                continue;
            }
            let term = if yi <= 1e-15 {
                mu
            } else {
                yi * (yi / mu).ln() - (yi - mu)
            };
            self.acc += 2.0 * term;
            self.n += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlinePoissonDeviance skipped {skipped} pairs with μ≤0 or y<0"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlinePoissonDeviance={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Poisson deviance",
                "mean 2(y ln(y/μ) − (y−μ)); non-positive μ or y<0 skipped",
                format!("dev={before:.6e}"),
                format!("dev={after:.6e}"),
            ),
        )
    }
}

/// Streaming Gamma deviance (river `metrics.GammaDeviance`).
///
/// Non-positive pairs are skipped with a warning.
#[derive(Clone, Debug, Default)]
pub struct OnlineGammaDeviance {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineGammaDeviance {
    /// Empty Gamma deviance.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean deviance, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineGammaDeviance {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n,
                    "no y for OnlineGammaDeviance",
                ),
            );
        };
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows().min(y.len()) {
            let mu = x.get(i, 0);
            let yi = y[i];
            if !mu.is_finite() || !yi.is_finite() || mu <= 0.0 || yi <= 0.0 {
                skipped += 1;
                continue;
            }
            self.acc += 2.0 * ((yi - mu) / mu - (yi / mu).ln());
            self.n += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineGammaDeviance skipped {skipped} pairs with μ≤0 or y≤0"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlineGammaDeviance={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Gamma deviance",
                "mean 2((y−μ)/μ − ln(y/μ)); non-positive pairs skipped",
                format!("dev={before:.6e}"),
                format!("dev={after:.6e}"),
            ),
        )
    }
}

/// Streaming MinHash sketch (river `sketch.MinHash`).
///
/// Hash-function count is not identification `p`.
#[derive(Clone, Debug)]
pub struct MinHash {
    /// Number of hash functions.
    pub k: usize,
    mins: Vec<u64>,
    n_seen: u64,
    updates: u64,
}

impl Default for MinHash {
    fn default() -> Self {
        Self {
            k: 8,
            mins: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl MinHash {
    /// `k`-wide MinHash of column 0.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }

    /// Mean of the normalized minima, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n_seen == 0 || self.mins.is_empty() {
            f64::NAN
        } else {
            let s: f64 = self
                .mins
                .iter()
                .map(|&m| m as f64 / u64::MAX as f64)
                .sum();
            s / self.mins.len() as f64
        }
    }
}

impl PartialFit for MinHash {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if self.mins.is_empty() {
            self.mins = vec![u64::MAX; self.k.max(1)];
        }
        let before = self.score();
        let kk = self.mins.len();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            for j in 0..kk {
                let h = mix_hash(v, (j as u64).wrapping_add(1));
                if h < self.mins[j] {
                    self.mins[j] = h;
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 1;
        q.explanation = format!("MinHash k={kk} score={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "MinHash signature",
                "k independent minima of hashed column-0 values; k is not identification p",
                format!("mh={before:.6e}"),
                format!("mh={after:.6e}"),
            ),
        )
    }
}

/// Streaming Bernoulli probability (river `proba.Bernoulli`).
///
/// `y` is hard-thresholded at 0.5. Class count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct BernoulliProba {
    successes: u64,
    n: u64,
    updates: u64,
}

impl BernoulliProba {
    /// Empty Bernoulli accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// \(\hat p = n_1 / n\), or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.successes as f64 / self.n as f64
        }
    }

    /// \(\hat p(k)\) for \(k\in\{0,1\}\).
    pub fn pmf(&self, k: i64) -> f64 {
        let p = self.score();
        if !p.is_finite() {
            f64::NAN
        } else if k == 1 {
            p
        } else if k == 0 {
            1.0 - p
        } else {
            0.0
        }
    }
}

impl PartialFit for BernoulliProba {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n, "no y for BernoulliProba"),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            let yi = y[i];
            if !yi.is_finite() {
                continue;
            }
            if yi >= 0.5 {
                self.successes += 1;
            }
            self.n += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("BernoulliProba p={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Bernoulli success probability",
                "hard 0.5 threshold on y; class count is not identification p",
                format!("p={before:.6e}"),
                format!("p={after:.6e}"),
            ),
        )
    }
}

/// Streaming Tweedie deviance with power 1.5 (river `metrics.Tweedie`).
///
/// Non-positive pairs are skipped with a warning.
#[derive(Clone, Debug, Default)]
pub struct OnlineTweedieDeviance {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineTweedieDeviance {
    /// Empty Tweedie deviance (power 1.5).
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean deviance, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineTweedieDeviance {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n,
                    "no y for OnlineTweedieDeviance",
                ),
            );
        };
        let before = self.score();
        let mut skipped = 0usize;
        for i in 0..x.nrows().min(y.len()) {
            let mu = x.get(i, 0);
            let yi = y[i];
            if !mu.is_finite() || !yi.is_finite() || mu <= 0.0 || yi <= 0.0 {
                skipped += 1;
                continue;
            }
            // Power 1.5: 4 (√y − √μ)² / √μ.
            let sy = yi.sqrt();
            let sm = mu.sqrt();
            self.acc += 4.0 * (sy - sm) * (sy - sm) / sm;
            self.n += 1;
        }
        if skipped > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineTweedieDeviance skipped {skipped} pairs with μ≤0 or y≤0"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("OnlineTweedieDeviance={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Tweedie deviance",
                "power-1.5 mean 4(√y−√μ)²/√μ; non-positive pairs skipped",
                format!("dev={before:.6e}"),
                format!("dev={after:.6e}"),
            ),
        )
    }
}

fn window_spearman(xs: &[f64], ys: &[f64]) -> f64 {
    let n = xs.len().min(ys.len());
    if n < 2 {
        return f64::NAN;
    }
    let rx = window_ranks(&xs[..n]);
    let ry = window_ranks(&ys[..n]);
    let nf = n as f64;
    let mx = rx.iter().sum::<f64>() / nf;
    let my = ry.iter().sum::<f64>() / nf;
    let mut c = 0.0_f64;
    let mut vx = 0.0_f64;
    let mut vy = 0.0_f64;
    for i in 0..n {
        let dx = rx[i] - mx;
        let dy = ry[i] - my;
        c += dx * dy;
        vx += dx * dx;
        vy += dy * dy;
    }
    let den = (vx * vy).sqrt();
    if den <= 1e-18 {
        f64::NAN
    } else {
        c / den
    }
}

fn window_ranks(v: &[f64]) -> Vec<f64> {
    let mut idx: Vec<usize> = (0..v.len()).collect();
    idx.sort_by(|&a, &b| v[a].partial_cmp(&v[b]).unwrap_or(std::cmp::Ordering::Equal));
    let mut r = vec![0.0_f64; v.len()];
    let mut i = 0;
    while i < idx.len() {
        let mut j = i + 1;
        while j < idx.len() && (v[idx[j]] - v[idx[i]]).abs() <= 1e-15 {
            j += 1;
        }
        let avg = (i + j - 1) as f64 / 2.0 + 1.0;
        for &k in idx[i..j].iter() {
            r[k] = avg;
        }
        i = j;
    }
    r
}

/// Rolling Spearman correlation of column 0 and `y` (river `stats.RollingSpearmanCorr`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingSpearman {
    /// Window capacity.
    pub window: usize,
    xs: Vec<f64>,
    ys: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingSpearman {
    fn default() -> Self {
        Self {
            window: 8,
            xs: Vec::new(),
            ys: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingSpearman {
    /// Rolling Spearman with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(2),
            ..Self::default()
        }
    }

    /// Current Spearman correlation, or NaN when the window is degenerate.
    pub fn score(&self) -> f64 {
        window_spearman(&self.xs, &self.ys)
    }
}

impl PartialFit for RollingSpearman {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "no y for RollingSpearman",
                ),
            );
        };
        let before = self.score();
        let cap = self.window.max(2);
        let n = x.nrows().min(y.len());
        for i in 0..n {
            let xv = x.get(i, 0);
            let yv = y[i];
            if xv.is_finite() && yv.is_finite() {
                rolling_push(&mut self.xs, xv, cap);
                rolling_push(&mut self.ys, yv, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len().min(self.ys.len()) as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len().min(self.ys.len()) >= 2;
        q.warmup = self.xs.len().min(self.ys.len()) < 2;
        q.explanation = format!("RollingSpearman={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Spearman correlation",
                "sliding rank-correlation of column 0 and y; window is not identification p",
                format!("rho={before:.6e}"),
                format!("rho={after:.6e}"),
            ),
        )
    }
}

/// Rolling MSE (river `metrics.Rolling` + MSE).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingMse {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingMse {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingMse {
    /// Rolling MSE with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean squared error, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingMse {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingMse"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                let e = pred - truth;
                rolling_push(&mut self.buf, e * e, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingMse={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed MSE",
                "sliding mean of (pred−y)²; window is not identification p",
                format!("mse={before:.6e}"),
                format!("mse={after:.6e}"),
            ),
        )
    }
}

/// Rolling log-loss (river `metrics.Rolling` + `LogLoss`).
///
/// Window length is not identification `p`. Predictions are clipped to
/// \((\varepsilon,1-\varepsilon)\).
#[derive(Clone, Debug)]
pub struct RollingLogLoss {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingLogLoss {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingLogLoss {
    /// Rolling log-loss with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean log-loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingLogLoss {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingLogLoss"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let p = x.get(i, 0).clamp(1e-12, 1.0 - 1e-12);
            let t = if y[i] > 0.5 { 1.0 } else { 0.0 };
            if p.is_finite() && y[i].is_finite() {
                let ll = -(t * p.ln() + (1.0 - t) * (1.0 - p).ln());
                rolling_push(&mut self.buf, ll, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingLogLoss={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed log-loss",
                "sliding mean of clipped Bernoulli NLL; window is not identification p",
                format!("ll={before:.6e}"),
                format!("ll={after:.6e}"),
            ),
        )
    }
}

/// Rolling pinball / quantile loss (river `metrics.Rolling` + `Pinball`).
///
/// Window length and \(\tau\) are not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingPinball {
    /// Quantile \(\tau\in(0,1)\).
    pub tau: f64,
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingPinball {
    fn default() -> Self {
        Self {
            tau: 0.5,
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingPinball {
    /// Rolling pinball at quantile `tau` with capacity `window`.
    pub fn new(window: usize, tau: f64) -> Self {
        Self {
            tau,
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean pinball, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingPinball {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingPinball"),
            );
        };
        let tau = if self.tau.is_finite() && self.tau > 0.0 && self.tau < 1.0 {
            self.tau
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "RollingPinball tau={} is not in (0,1); using 0.5",
                        self.tau
                    ))
                    .build(),
            );
            0.5
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                let e = truth - pred;
                let loss = if e >= 0.0 { tau * e } else { (tau - 1.0) * e };
                rolling_push(&mut self.buf, loss, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingPinball={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed pinball",
                "sliding quantile loss of column 0 vs y; window is not identification p",
                format!("pinball={before:.6e}"),
                format!("pinball={after:.6e}"),
            ),
        )
    }
}

/// Rolling Huber loss (river `metrics.Rolling` + `Huber`).
///
/// Window length and \(\delta\) are not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingHuber {
    /// Huber threshold.
    pub delta: f64,
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingHuber {
    fn default() -> Self {
        Self {
            delta: 1.0,
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingHuber {
    /// Rolling Huber with threshold `delta` and capacity `window`.
    pub fn new(window: usize, delta: f64) -> Self {
        Self {
            delta,
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean Huber loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingHuber {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingHuber"),
            );
        };
        let d = if self.delta.is_finite() && self.delta > 0.0 {
            self.delta
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "RollingHuber delta={} is not positive; using 1",
                        self.delta
                    ))
                    .build(),
            );
            1.0
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                let e = (pred - truth).abs();
                let loss = if e <= d {
                    0.5 * e * e
                } else {
                    d * (e - 0.5 * d)
                };
                rolling_push(&mut self.buf, loss, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingHuber={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Huber",
                "sliding Huber loss of column 0 vs y; window is not identification p",
                format!("huber={before:.6e}"),
                format!("huber={after:.6e}"),
            ),
        )
    }
}

/// Rolling log-cosh (river `metrics.Rolling` + `LogCosh`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingLogCosh {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingLogCosh {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingLogCosh {
    /// Rolling log-cosh with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean log-cosh, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingLogCosh {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingLogCosh"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                let e = pred - truth;
                let ae = e.abs();
                let lch = ae + ((-2.0 * ae).exp() + 1.0).ln() - 2.0_f64.ln();
                rolling_push(&mut self.buf, lch, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingLogCosh={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed log-cosh",
                "sliding log cosh of (pred−y); window is not identification p",
                format!("lch={before:.6e}"),
                format!("lch={after:.6e}"),
            ),
        )
    }
}

/// Rolling soft cross-entropy (river `metrics.Rolling` + `CrossEntropy`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingCrossEntropy {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingCrossEntropy {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingCrossEntropy {
    /// Rolling cross-entropy with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window mean cross-entropy, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingCrossEntropy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "no y for RollingCrossEntropy",
                ),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let p = x.get(i, 0);
            let t = y[i];
            if p.is_finite() && t.is_finite() {
                let p = p.clamp(1e-12, 1.0 - 1e-12);
                let t = t.clamp(0.0, 1.0);
                let ce = -(t * p.ln() + (1.0 - t) * (1.0 - p).ln());
                rolling_push(&mut self.buf, ce, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingCrossEntropy={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed cross-entropy",
                "sliding soft CE of column 0 vs y; window is not identification p",
                format!("ce={before:.6e}"),
                format!("ce={after:.6e}"),
            ),
        )
    }
}

/// Rolling accuracy (river `metrics.Rolling` + `Accuracy`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingAccuracy {
    /// Window capacity.
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingAccuracy {
    fn default() -> Self {
        Self {
            window: 8,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingAccuracy {
    /// Rolling accuracy with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window accuracy, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            f64::NAN
        } else {
            self.buf.iter().sum::<f64>() / self.buf.len() as f64
        }
    }
}

impl PartialFit for RollingAccuracy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no y for RollingAccuracy"),
            );
        };
        let before = self.score();
        let cap = self.window.max(1);
        for i in 0..x.nrows().min(y.len()) {
            let pred = x.get(i, 0);
            let truth = y[i];
            if pred.is_finite() && truth.is_finite() {
                let ph: f64 = if pred > 0.5 { 1.0 } else { 0.0 };
                let th: f64 = if truth > 0.5 { 1.0 } else { 0.0 };
                let hit = if (ph - th).abs() < 0.5 { 1.0 } else { 0.0 };
                rolling_push(&mut self.buf, hit, cap);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.buf.is_empty();
        q.warmup = self.buf.is_empty();
        q.explanation = format!("RollingAccuracy={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed accuracy",
                "sliding 0-1 score of hard 0.5 on column 0 vs y; window is not identification p",
                format!("acc={before:.6e}"),
                format!("acc={after:.6e}"),
            ),
        )
    }
}

fn rolling_push_pair(pairs: &mut Vec<(f64, f64)>, pred: f64, truth: f64, cap: usize) {
    if pred.is_finite() && truth.is_finite() {
        pairs.push((pred, truth));
        if pairs.len() > cap {
            pairs.remove(0);
        }
    }
}

fn rolling_pair_metric<F>(
    session: &Session,
    name: &str,
    x: &Matrix,
    y: Option<&Vector>,
    window: usize,
    pairs: &mut Vec<(f64, f64)>,
    n_seen: &mut u64,
    updates: &mut u64,
    score: F,
    what: &str,
    why: &str,
) -> Result<Qualified<IncrementalExplain>>
where
    F: Fn(&[(f64, f64)]) -> f64,
{
    let mut ctx = FitCtx::with_session(session.child("partial_fit"));
    inspect_online_xy(&mut ctx, x, y);
    let Some(y) = y else {
        ctx.push(Issue::builder(IssueCode::MissingTarget).build());
        return finish_explain(
            ctx,
            reject_explain(*updates, x.nrows(), *n_seen, "missing target for rolling metric"),
        );
    };
    let before = score(pairs);
    let cap = window.max(1);
    for i in 0..x.nrows().min(y.len()) {
        rolling_push_pair(pairs, x.get(i, 0), y[i], cap);
        if x.get(i, 0).is_finite() && y[i].is_finite() {
            *n_seen += 1;
        }
    }
    *updates += 1;
    let after = score(pairs);
    let mut q = IncrementalQuality::new(updates.saturating_sub(1), x.nrows(), *n_seen);
    q.effective_sample_size = pairs.len() as f64;
    q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
        (after - before).abs()
    } else {
        0.0
    });
    q.information_gain = Some(x.nrows() as f64);
    q.still_identified = !pairs.is_empty() && after.is_finite();
    q.warmup = pairs.is_empty();
    q.explanation = format!("{name}={after:.6e}");
    flag_info(&mut ctx, &q);
    finish_explain(
        ctx,
        IncrementalExplain::from_quality(
            q,
            what,
            why,
            format!("{name}={before:.6e}"),
            format!("{name}={after:.6e}"),
        ),
    )
}

fn window_hard_counts(pairs: &[(f64, f64)]) -> (f64, f64, f64, f64) {
    let mut tp = 0.0_f64;
    let mut fp = 0.0_f64;
    let mut tn = 0.0_f64;
    let mut fn_ = 0.0_f64;
    for &(pred, truth) in pairs {
        let p = pred > 0.5;
        let t = truth > 0.5;
        match (p, t) {
            (true, true) => tp += 1.0,
            (true, false) => fp += 1.0,
            (false, false) => tn += 1.0,
            (false, true) => fn_ += 1.0,
        }
    }
    (tp, fp, tn, fn_)
}

fn window_f1(pairs: &[(f64, f64)]) -> f64 {
    let (tp, fp, _, fn_) = window_hard_counts(pairs);
    let den = 2.0 * tp + fp + fn_;
    if den <= 0.0 {
        f64::NAN
    } else {
        2.0 * tp / den
    }
}

fn window_precision(pairs: &[(f64, f64)]) -> f64 {
    let (tp, fp, _, _) = window_hard_counts(pairs);
    let den = tp + fp;
    if den <= 0.0 {
        f64::NAN
    } else {
        tp / den
    }
}

fn window_recall(pairs: &[(f64, f64)]) -> f64 {
    let (tp, _, _, fn_) = window_hard_counts(pairs);
    let den = tp + fn_;
    if den <= 0.0 {
        f64::NAN
    } else {
        tp / den
    }
}

fn window_r2(pairs: &[(f64, f64)]) -> f64 {
    if pairs.len() < 2 {
        return f64::NAN;
    }
    let n = pairs.len() as f64;
    let mut sse = 0.0_f64;
    let mut sy = 0.0_f64;
    let mut sy2 = 0.0_f64;
    for &(pred, truth) in pairs {
        let e = pred - truth;
        sse += e * e;
        sy += truth;
        sy2 += truth * truth;
    }
    let mean = sy / n;
    let sst = sy2 - n * mean * mean;
    if sst.abs() <= 1e-15 {
        f64::NAN
    } else {
        1.0 - sse / sst
    }
}

fn window_cohen(pairs: &[(f64, f64)]) -> f64 {
    let (tp, fp, tn, fn_) = window_hard_counts(pairs);
    let n = tp + fp + tn + fn_;
    if n <= 0.0 {
        return f64::NAN;
    }
    let po = (tp + tn) / n;
    let pe = ((tn + fp) * (tn + fn_) + (fn_ + tp) * (fp + tp)) / (n * n);
    if (1.0 - pe).abs() <= 1e-18 {
        f64::NAN
    } else {
        (po - pe) / (1.0 - pe)
    }
}

fn window_balanced(pairs: &[(f64, f64)]) -> f64 {
    let (tp, fp, tn, fn_) = window_hard_counts(pairs);
    let pos = tp + fn_;
    let neg = tn + fp;
    if pos <= 0.0 || neg <= 0.0 {
        f64::NAN
    } else {
        0.5 * (tp / pos + tn / neg)
    }
}

fn window_fbeta(pairs: &[(f64, f64)], beta: f64) -> f64 {
    let (tp, fp, _, fn_) = window_hard_counts(pairs);
    let b2 = beta * beta;
    let den = (1.0 + b2) * tp + b2 * fn_ + fp;
    if den <= 0.0 {
        f64::NAN
    } else {
        (1.0 + b2) * tp / den
    }
}

fn window_jaccard(pairs: &[(f64, f64)]) -> f64 {
    let mut inter = 0.0_f64;
    let mut union = 0.0_f64;
    for &(pred, truth) in pairs {
        let a = pred.round() != 0.0;
        let b = truth.round() != 0.0;
        if a && b {
            inter += 1.0;
        }
        if a || b {
            union += 1.0;
        }
    }
    if union <= 0.0 {
        f64::NAN
    } else {
        inter / union
    }
}

fn window_hamming(pairs: &[(f64, f64)]) -> f64 {
    if pairs.is_empty() {
        return f64::NAN;
    }
    let mut bad = 0.0_f64;
    for &(pred, truth) in pairs {
        if pred.round() != truth.round() {
            bad += 1.0;
        }
    }
    bad / pairs.len() as f64
}

fn window_smape(pairs: &[(f64, f64)]) -> f64 {
    if pairs.is_empty() {
        return f64::NAN;
    }
    let mut acc = 0.0_f64;
    let mut n = 0.0_f64;
    for &(pred, truth) in pairs {
        let den = pred.abs() + truth.abs();
        if den > 1e-12 {
            acc += 2.0 * (pred - truth).abs() / den;
            n += 1.0;
        }
    }
    if n <= 0.0 {
        f64::NAN
    } else {
        acc / n
    }
}

fn window_mcc(pairs: &[(f64, f64)]) -> f64 {
    let (tp, fp, tn, fn_) = window_hard_counts(pairs);
    let n = tp + fp + tn + fn_;
    if n <= 0.0 {
        return f64::NAN;
    }
    let den = ((tp + fp) * (tp + fn_) * (tn + fp) * (tn + fn_)).sqrt();
    if den < 1e-18 {
        0.0
    } else {
        (tp * tn - fp * fn_) / den
    }
}

fn window_roc_auc(pairs: &[(f64, f64)]) -> f64 {
    let mut pos = Vec::new();
    let mut neg = Vec::new();
    for &(s, y) in pairs {
        if y > 0.5 {
            pos.push(s);
        } else {
            neg.push(s);
        }
    }
    if pos.is_empty() || neg.is_empty() {
        return f64::NAN;
    }
    let mut ge = 0.0_f64;
    for &p in &pos {
        for &n in &neg {
            if p > n {
                ge += 1.0;
            } else if (p - n).abs() < 1e-15 {
                ge += 0.5;
            }
        }
    }
    ge / (pos.len() * neg.len()) as f64
}

/// Rolling F1 (river `metrics.Rolling` + `F1`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingF1 {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingF1 {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingF1 {
    /// Rolling F1 with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window F1, or NaN when unidentified.
    pub fn score(&self) -> f64 {
        window_f1(&self.pairs)
    }
}

impl PartialFit for RollingF1 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingF1",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_f1,
            "windowed F1",
            "sliding hard-0.5 F1 of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling precision (river `metrics.Rolling` + `Precision`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingPrecision {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingPrecision {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingPrecision {
    /// Rolling precision with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window precision, or NaN when no positive predictions.
    pub fn score(&self) -> f64 {
        window_precision(&self.pairs)
    }
}

impl PartialFit for RollingPrecision {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingPrecision",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_precision,
            "windowed precision",
            "sliding hard-0.5 precision of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling recall (river `metrics.Rolling` + `Recall`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingRecall {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingRecall {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingRecall {
    /// Rolling recall with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window recall, or NaN when no positive labels.
    pub fn score(&self) -> f64 {
        window_recall(&self.pairs)
    }
}

impl PartialFit for RollingRecall {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingRecall",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_recall,
            "windowed recall",
            "sliding hard-0.5 recall of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling coefficient of determination (river `metrics.Rolling` + `R2`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingR2 {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingR2 {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingR2 {
    /// Rolling R² with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window R², or NaN when unidentified.
    pub fn score(&self) -> f64 {
        window_r2(&self.pairs)
    }
}

impl PartialFit for RollingR2 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingR2",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_r2,
            "windowed R2",
            "sliding 1-SSE/SST of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling Cohen's κ (river `metrics.Rolling` + `CohenKappa`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingCohenKappa {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingCohenKappa {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingCohenKappa {
    /// Rolling κ with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window κ, or NaN when empty or chance agreement is 1.
    pub fn score(&self) -> f64 {
        window_cohen(&self.pairs)
    }
}

impl PartialFit for RollingCohenKappa {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingCohenKappa",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_cohen,
            "windowed Cohen kappa",
            "sliding hard-0.5 kappa of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling balanced accuracy (river `metrics.Rolling` + `BalancedAccuracy`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingBalancedAccuracy {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingBalancedAccuracy {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingBalancedAccuracy {
    /// Rolling balanced accuracy with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window \((\mathrm{TPR}+\mathrm{TNR})/2\), or NaN before both classes appear.
    pub fn score(&self) -> f64 {
        window_balanced(&self.pairs)
    }
}

impl PartialFit for RollingBalancedAccuracy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingBalancedAccuracy",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_balanced,
            "windowed balanced accuracy",
            "sliding hard-0.5 balanced accuracy of column 0 vs y; window is not identification p",
        )
    }
}

/// Constant score threshold (river `anomaly.ConstantThresholder`).
///
/// Scores at or above `threshold` are classified as anomalous (`1`). The cutoff
/// is not identification `p`.
#[derive(Clone, Debug)]
pub struct ConstantThresholder {
    /// Anomaly cutoff.
    pub threshold: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for ConstantThresholder {
    fn default() -> Self {
        Self {
            threshold: 0.5,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ConstantThresholder {
    /// Thresholder at `threshold`.
    pub fn new(threshold: f64) -> Self {
        Self {
            threshold,
            ..Self::default()
        }
    }

    fn cutoff(&self) -> f64 {
        if self.threshold.is_finite() {
            self.threshold
        } else {
            0.5
        }
    }
}

impl PartialFit for ConstantThresholder {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if !self.threshold.is_finite() {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "ConstantThresholder.threshold={} is non-finite; using 0.5",
                        self.threshold
                    ))
                    .build(),
            );
            self.threshold = 0.5;
        }
        let thr = self.cutoff();
        let mut flagged = 0.0_f64;
        for i in 0..x.nrows() {
            if x.get(i, 0) >= thr {
                flagged += 1.0;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = x.nrows() as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("ConstantThresholder flagged={flagged:.6e} thr={thr:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "constant anomaly threshold",
                "hard cutoff on column 0; threshold is not identification p",
                format!("thr={thr:.6e}"),
                format!("flagged={flagged:.6e}"),
            ),
        )
    }
}

impl Transform for ConstantThresholder {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        let thr = self.cutoff();
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if x.get(i, j) >= thr {
                1.0
            } else {
                0.0
            }
        });
        ctx.finish(out)
    }
}

/// Keep scores at or above a cutoff (river `anomaly.ThresholdFilter`).
///
/// Values below `threshold` become 0. The cutoff is not identification `p`.
#[derive(Clone, Debug)]
pub struct ThresholdFilter {
    /// Minimum kept score.
    pub threshold: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for ThresholdFilter {
    fn default() -> Self {
        Self {
            threshold: 0.5,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ThresholdFilter {
    /// Filter at `threshold`.
    pub fn new(threshold: f64) -> Self {
        Self {
            threshold,
            ..Self::default()
        }
    }

    fn cutoff(&self) -> f64 {
        if self.threshold.is_finite() {
            self.threshold
        } else {
            0.5
        }
    }
}

impl PartialFit for ThresholdFilter {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if !self.threshold.is_finite() {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "ThresholdFilter.threshold={} is non-finite; using 0.5",
                        self.threshold
                    ))
                    .build(),
            );
            self.threshold = 0.5;
        }
        let thr = self.cutoff();
        let mut kept = 0.0_f64;
        for i in 0..x.nrows() {
            if x.get(i, 0) >= thr {
                kept += 1.0;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = x.nrows() as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("ThresholdFilter kept={kept:.6e} thr={thr:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "score threshold filter",
                "zeros column-0 scores below the cutoff; threshold is not identification p",
                format!("thr={thr:.6e}"),
                format!("kept={kept:.6e}"),
            ),
        )
    }
}

impl Transform for ThresholdFilter {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        let thr = self.cutoff();
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let v = x.get(i, j);
            if v.is_finite() && v >= thr {
                v
            } else {
                0.0
            }
        });
        ctx.finish(out)
    }
}

/// Rolling Fβ (river `metrics.Rolling` + `FBeta`).
///
/// Window length and β are not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingFbeta {
    /// Window capacity.
    pub window: usize,
    /// β weight on recall.
    pub beta: f64,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingFbeta {
    fn default() -> Self {
        Self {
            window: 8,
            beta: 1.0,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingFbeta {
    /// Rolling Fβ with capacity `window` and weight `beta`.
    pub fn new(window: usize, beta: f64) -> Self {
        Self {
            window: window.max(1),
            beta,
            ..Self::default()
        }
    }

    /// Current window Fβ, or NaN when unidentified.
    pub fn score(&self) -> f64 {
        let b = if self.beta.is_finite() && self.beta > 0.0 {
            self.beta
        } else {
            1.0
        };
        window_fbeta(&self.pairs, b)
    }
}

impl PartialFit for RollingFbeta {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        if !(self.beta.is_finite() && self.beta > 0.0) {
            self.beta = 1.0;
        }
        let beta = self.beta;
        rolling_pair_metric(
            session,
            "RollingFbeta",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            |pairs| window_fbeta(pairs, beta),
            "windowed F-beta",
            "sliding hard-0.5 Fβ of column 0 vs y; window and β are not identification p",
        )
    }
}

/// Rolling Jaccard index (river `metrics.Rolling` + `Jaccard`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingJaccard {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingJaccard {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingJaccard {
    /// Rolling Jaccard with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window Jaccard, or NaN when the union is empty.
    pub fn score(&self) -> f64 {
        window_jaccard(&self.pairs)
    }
}

impl PartialFit for RollingJaccard {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingJaccard",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_jaccard,
            "windowed Jaccard",
            "sliding hard Jaccard of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling Hamming loss (river `metrics.Rolling` + `Hamming`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingHamming {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingHamming {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingHamming {
    /// Rolling Hamming with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window Hamming loss, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_hamming(&self.pairs)
    }
}

impl PartialFit for RollingHamming {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingHamming",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_hamming,
            "windowed Hamming",
            "sliding hard Hamming loss of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling SMAPE (river `metrics.Rolling` + `SMAPE`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingSmape {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingSmape {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingSmape {
    /// Rolling SMAPE with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window SMAPE, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_smape(&self.pairs)
    }
}

impl PartialFit for RollingSmape {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingSmape",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_smape,
            "windowed SMAPE",
            "sliding 2|e|/(|ŷ|+|y|) of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling Matthews correlation (river `metrics.Rolling` + `MCC`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingMcc {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingMcc {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingMcc {
    /// Rolling MCC with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window MCC, or NaN when empty.
    pub fn score(&self) -> f64 {
        window_mcc(&self.pairs)
    }
}

impl PartialFit for RollingMcc {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingMcc",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_mcc,
            "windowed MCC",
            "sliding hard-0.5 Matthews correlation of column 0 vs y; window is not identification p",
        )
    }
}

/// Rolling ROC AUC (river `metrics.Rolling` + `ROCAUC`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct RollingRocAuc {
    /// Window capacity.
    pub window: usize,
    pairs: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for RollingRocAuc {
    fn default() -> Self {
        Self {
            window: 8,
            pairs: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RollingRocAuc {
    /// Rolling ROC AUC with capacity `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current window Mann–Whitney AUC, or NaN when one class is missing.
    pub fn score(&self) -> f64 {
        window_roc_auc(&self.pairs)
    }
}

impl PartialFit for RollingRocAuc {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        rolling_pair_metric(
            session,
            "RollingRocAuc",
            x,
            y,
            self.window,
            &mut self.pairs,
            &mut self.n_seen,
            &mut self.updates,
            window_roc_auc,
            "windowed ROC AUC",
            "sliding Mann-Whitney AUC of column 0 vs y; window is not identification p",
        )
    }
}

/// Quantile score threshold (river `anomaly.QuantileThresholder`).
///
/// Scores at or above the empirical `q`-quantile of a 256-row window are
/// classified as anomalous. `q` is not identification `p`.
#[derive(Clone, Debug)]
pub struct QuantileThresholder {
    /// Upper quantile in \((0, 1)\).
    pub q: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for QuantileThresholder {
    fn default() -> Self {
        Self {
            q: 0.95,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl QuantileThresholder {
    /// Thresholder at quantile `q`.
    pub fn new(q: f64) -> Self {
        Self {
            q,
            ..Self::default()
        }
    }

    fn quantile(&self, q: f64) -> f64 {
        if self.window.is_empty() {
            return f64::NAN;
        }
        let mut xs = self.window.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let idx = ((q.clamp(0.0, 1.0)) * (xs.len() - 1) as f64).round() as usize;
        xs[idx.min(xs.len() - 1)]
    }
}

impl PartialFit for QuantileThresholder {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "QuantileThresholder.q={} is not in (0,1); using 0.95",
                        self.q
                    ))
                    .build(),
            );
            0.95
        };
        let before = self.quantile(qv);
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if v.is_finite() {
                self.window.push(v);
                if self.window.len() > 256 {
                    self.window.remove(0);
                }
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let after = self.quantile(qv);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.window.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.window.len() >= 2;
        q.warmup = self.window.len() < 2;
        q.explanation = format!("QuantileThresholder q={qv:.3} thr={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "empirical quantile threshold",
                "256-row order statistic cutoff; q is not identification p",
                format!("thr={before:.6e}"),
                format!("thr={after:.6e}"),
            ),
        )
    }
}

impl Transform for QuantileThresholder {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        if self.window.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), x.ncols()));
        }
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            0.95
        };
        let thr = self.quantile(qv);
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if x.get(i, j) >= thr {
                1.0
            } else {
                0.0
            }
        });
        ctx.finish(out)
    }
}

/// Zero inlier rows (river `anomaly` discard / keep-anomalies gate).
///
/// A row is discarded (set to 0) when column 0 is below `threshold`. The cutoff
/// is not identification `p`.
#[derive(Clone, Debug)]
pub struct Discard {
    /// Minimum kept score on column 0.
    pub threshold: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for Discard {
    fn default() -> Self {
        Self {
            threshold: 0.5,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Discard {
    /// Discard rows with column 0 below `threshold`.
    pub fn new(threshold: f64) -> Self {
        Self {
            threshold,
            ..Self::default()
        }
    }

    fn cutoff(&self) -> f64 {
        if self.threshold.is_finite() {
            self.threshold
        } else {
            0.5
        }
    }
}

impl PartialFit for Discard {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if !self.threshold.is_finite() {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "Discard.threshold={} is non-finite; using 0.5",
                        self.threshold
                    ))
                    .build(),
            );
            self.threshold = 0.5;
        }
        let thr = self.cutoff();
        let mut kept = 0.0_f64;
        for i in 0..x.nrows() {
            if x.get(i, 0) >= thr {
                kept += 1.0;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = kept;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.warmup = false;
        q.explanation = format!("Discard kept={kept:.6e} thr={thr:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "discard inlier rows",
                "zeros rows whose column 0 is below the cutoff; threshold is not identification p",
                format!("thr={thr:.6e}"),
                format!("kept={kept:.6e}"),
            ),
        )
    }
}

impl Transform for Discard {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        let thr = self.cutoff();
        let out = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            if x.get(i, 0) >= thr {
                x.get(i, j)
            } else {
                0.0
            }
        });
        ctx.finish(out)
    }
}

/// ADWIN-resetting bag of perceptrons (river `ensemble.ADWINBaggingClassifier`).
#[derive(Clone, Debug)]
pub struct AdwinBagging {
    /// Members.
    pub n_models: usize,
    models: Vec<Perceptron>,
    detectors: Vec<Adwin>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for AdwinBagging {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            detectors: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(23),
        }
    }
}

impl AdwinBagging {
    /// Bag of `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for AdwinBagging {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(1)];
            self.detectors = (0..self.models.len()).map(|_| Adwin::new(0.002)).collect();
        }
        let before = self.n_seen;
        let mut resets = 0u64;
        for m in 0..self.models.len() {
            let mut rows: Vec<usize> = Vec::new();
            for i in 0..x.nrows().min(y.len()) {
                let k = self.rng.poisson(1.0) as usize;
                for _ in 0..k {
                    rows.push(i);
                }
            }
            if rows.is_empty() && x.nrows() > 0 {
                rows.push(0);
            }
            if rows.is_empty() {
                continue;
            }
            let xb = Matrix::from_fn(rows.len(), x.ncols(), |r, c| x.get(rows[r], c));
            let yb = Vector::from_iter(rows.iter().map(|&i| y[i]));
            let _ = self.models[m].partial_fit(&xb, Some(&yb), &session.child(format!("abag_{m}")));
            let pred = match self.models[m].predict(&xb, &session.child(format!("abagp_{m}"))) {
                Ok(q) => q.value,
                Err(_) => Vector::zeros(xb.nrows()),
            };
            let mut err = 0.0;
            for i in 0..yb.len().min(pred.len()) {
                if (pred[i] - yb[i]).abs() >= 0.5 {
                    err += 1.0;
                }
            }
            err /= yb.len().max(1) as f64;
            if let Ok(q) = self.detectors[m].update(err, &session.child(format!("adwin_{m}"))) {
                if matches!(q.value, DriftDecision::Drift { .. }) {
                    self.models[m] = Perceptron::new();
                    self.detectors[m].reset();
                    resets += 1;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(resets as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AdwinBagging {} perceptrons, resets={resets}",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ADWIN bagging update",
                "Poisson bootstrap plus per-member ADWIN reset",
                format!("n={before}"),
                format!("n={} resets={resets}", self.n_seen),
            ),
        )
    }
}

impl Predict for AdwinBagging {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut votes = vec![0.0; x.nrows()];
        let mut k = 0.0;
        for (i, m) in self.models.iter().enumerate() {
            if let Ok(q) = m.predict(x, &session.child(format!("abagp_{i}"))) {
                for r in 0..x.nrows().min(q.value.len()) {
                    votes[r] += q.value[r];
                }
                k += 1.0;
            }
        }
        let out =
            Vector::from_iter(votes.iter().map(
                |v| {
                    if k > 0.0 && *v / k >= 0.5 {
                        1.0
                    } else {
                        0.0
                    }
                },
            ));
        ctx.finish(out)
    }
}

/// Streaming AdaBoost of perceptrons (river `ensemble.AdaBoostClassifier` lite).
#[derive(Clone, Debug)]
pub struct OnlineAdaBoost {
    /// Members.
    pub n_models: usize,
    models: Vec<Perceptron>,
    weights: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineAdaBoost {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            weights: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineAdaBoost {
    /// Boosted committee of `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineAdaBoost {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(1)];
            self.weights = vec![1.0; self.models.len()];
        }
        let before = self.weights.iter().copied().sum::<f64>();
        for m in 0..self.models.len() {
            let _ = self.models[m].partial_fit(x, Some(y), &session.child(format!("ada_{m}")));
            if let Ok(q) = self.models[m].predict(x, &session.child(format!("adap_{m}"))) {
                let mut wrong = 0.0;
                for i in 0..y.len().min(q.value.len()) {
                    if (q.value[i] - y[i]).abs() >= 0.5 {
                        wrong += 1.0;
                    }
                }
                if wrong > 0.0 {
                    self.weights[m] *= 1.25;
                } else {
                    self.weights[m] *= 0.8;
                }
            }
        }
        let wsum: f64 = self.weights.iter().sum();
        if wsum > 0.0 {
            for w in &mut self.weights {
                *w /= wsum;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.weights.iter().copied().sum::<f64>();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("OnlineAdaBoost {} members", self.models.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "boosted perceptron weights",
                "up-weight members that erred on the batch",
                format!("wsum={before:.6e}"),
                format!("wsum={after:.6e}"),
            ),
        )
    }
}

impl Predict for OnlineAdaBoost {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        for (m, model) in self.models.iter().enumerate() {
            let w = self.weights.get(m).copied().unwrap_or(1.0);
            if let Ok(q) = model.predict(x, &session.child(format!("adap_{m}"))) {
                for i in 0..x.nrows().min(q.value.len()) {
                    acc[i] += w * if q.value[i] >= 0.5 { 1.0 } else { -1.0 };
                }
            }
        }
        ctx.finish(Vector::from_iter(acc.iter().map(|v| {
            if *v >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// ADWIN-resetting AdaBoost of perceptrons (river `ensemble.ADWINBoostingClassifier`).
///
/// Member count is not identification `p`.
#[derive(Clone, Debug)]
pub struct AdwinBoosting {
    /// Members.
    pub n_models: usize,
    models: Vec<Perceptron>,
    weights: Vec<f64>,
    detectors: Vec<Adwin>,
    n_seen: u64,
    updates: u64,
}

impl Default for AdwinBoosting {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            weights: Vec::new(),
            detectors: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl AdwinBoosting {
    /// Boosted committee of `n_models` perceptrons with per-member ADWIN.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for AdwinBoosting {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(1)];
            self.weights = vec![1.0; self.models.len()];
            self.detectors = (0..self.models.len()).map(|_| Adwin::new(0.002)).collect();
        }
        let before = self.n_seen;
        let mut resets = 0u64;
        for m in 0..self.models.len() {
            let _ = self.models[m].partial_fit(x, Some(y), &session.child(format!("adwb_{m}")));
            if let Ok(q) = self.models[m].predict(x, &session.child(format!("adwbp_{m}"))) {
                let mut wrong = 0.0;
                for i in 0..y.len().min(q.value.len()) {
                    if (q.value[i] - y[i]).abs() >= 0.5 {
                        wrong += 1.0;
                    }
                }
                if wrong > 0.0 {
                    self.weights[m] *= 1.25;
                } else {
                    self.weights[m] *= 0.8;
                }
                let err = wrong / y.len().max(1) as f64;
                if let Ok(d) = self.detectors[m].update(err, &session.child(format!("adwinb_{m}"))) {
                    if matches!(d.value, DriftDecision::Drift { .. }) {
                        self.models[m] = Perceptron::new();
                        self.weights[m] = 1.0;
                        self.detectors[m].reset();
                        resets += 1;
                    }
                }
            }
        }
        let wsum: f64 = self.weights.iter().sum();
        if wsum > 0.0 {
            for w in &mut self.weights {
                *w /= wsum;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(resets as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AdwinBoosting {} members, resets={resets}",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ADWIN boosting update",
                "AdaBoost weights plus per-member ADWIN reset",
                format!("n={before}"),
                format!("n={} resets={resets}", self.n_seen),
            ),
        )
    }
}

impl Predict for AdwinBoosting {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0; x.nrows()];
        for (m, model) in self.models.iter().enumerate() {
            let w = self.weights.get(m).copied().unwrap_or(1.0);
            if let Ok(q) = model.predict(x, &session.child(format!("adwbp_{m}"))) {
                for i in 0..x.nrows().min(q.value.len()) {
                    acc[i] += w * if q.value[i] >= 0.5 { 1.0 } else { -1.0 };
                }
            }
        }
        ctx.finish(Vector::from_iter(acc.iter().map(|v| {
            if *v >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Boosting Online Learning Ensemble (river `ensemble.BOLEClassifier`).
///
/// A member is updated only when the current committee is wrong on that row.
/// Member count is not identification `p`.
#[derive(Clone, Debug)]
pub struct BoleClassifier {
    /// Members.
    pub n_models: usize,
    models: Vec<Perceptron>,
    weights: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for BoleClassifier {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            weights: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl BoleClassifier {
    /// BOLE of `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }

    fn vote_row(&self, x: &Matrix, i: usize, session: &Session) -> f64 {
        let mut acc = 0.0;
        let mut wsum = 0.0;
        for (m, model) in self.models.iter().enumerate() {
            let w = self.weights.get(m).copied().unwrap_or(1.0);
            if let Ok(q) = model.predict(x, &session.child(format!("bolep_{m}"))) {
                if i < q.value.len() {
                    acc += w * if q.value[i] >= 0.5 { 1.0 } else { -1.0 };
                    wsum += w;
                }
            }
        }
        if wsum <= 0.0 {
            0.0
        } else if acc >= 0.0 {
            1.0
        } else {
            0.0
        }
    }
}

impl PartialFit for BoleClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = vec![Perceptron::new(); self.n_models.max(1)];
            self.weights = vec![1.0; self.models.len()];
        }
        let before = self.n_seen;
        let mut trained = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            let ens = if self.n_seen == 0 {
                y[i]
            } else {
                self.vote_row(x, i, session)
            };
            let wrong = (ens - y[i]).abs() >= 0.5;
            if !wrong && self.n_seen > 0 {
                continue;
            }
            let xb = Matrix::from_fn(1, x.ncols(), |_, c| x.get(i, c));
            let yb = Vector::from_slice(&[y[i]]);
            for m in 0..self.models.len() {
                let _ = self.models[m].partial_fit(
                    &xb,
                    Some(&yb),
                    &session.child(format!("bole_{m}")),
                );
            }
            trained += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(trained as f64);
        q.information_gain = Some(trained as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "BOLE trained on {trained}/{} rows where the committee was wrong",
            x.nrows()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "BOLE update",
                "weak learners train only on committee mistakes",
                format!("n={before}"),
                format!("n={} trained={trained}", self.n_seen),
            ),
        )
    }
}

impl Predict for BoleClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.vote_row(x, i, session)),
        ))
    }
}

/// Streaming random oversampler (river `imblearn.RandomOverSampler`).
#[derive(Clone, Debug, Default)]
pub struct RandomOverSampler {
    counts: HashMap<i64, u64>,
    n_seen: u64,
    updates: u64,
}

impl RandomOverSampler {
    /// Empty oversampler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for RandomOverSampler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.n_seen;
        for i in 0..x.nrows().min(y.len()) {
            if y[i].is_finite() {
                *self.counts.entry(y[i].round() as i64).or_insert(0) += 1;
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.counts.len() >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("RandomOverSampler classes={}", self.counts.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-count update",
                "minority rows are duplicated at transform time",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for RandomOverSampler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let maxc = self.counts.values().copied().max().unwrap_or(1);
        let minc = self.counts.values().copied().min().unwrap_or(1);
        if minc == 0 || maxc / minc.max(1) > 20 {
            ctx.push(
                Issue::builder(IssueCode::ClassImbalanceSevere)
                    .severity(Severity::Warning)
                    .message("RandomOverSampler saw a ≥20:1 class ratio")
                    .build(),
            );
        }
        ctx.finish(x.clone())
    }
}

/// Streaming random undersampler (river `imblearn.RandomUnderSampler`).
#[derive(Clone, Debug)]
pub struct RandomUnderSampler {
    counts: HashMap<i64, u64>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for RandomUnderSampler {
    fn default() -> Self {
        Self {
            counts: HashMap::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(29),
        }
    }
}

impl RandomUnderSampler {
    /// Empty undersampler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for RandomUnderSampler {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.n_seen;
        for i in 0..x.nrows().min(y.len()) {
            if y[i].is_finite() {
                *self.counts.entry(y[i].round() as i64).or_insert(0) += 1;
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.counts.len() >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("RandomUnderSampler classes={}", self.counts.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "class-count update",
                "majority rows may be dropped at transform time",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for RandomUnderSampler {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let mut rng = self.rng.clone();
        let minc = self.counts.values().copied().min().unwrap_or(1) as f64;
        let maxc = self.counts.values().copied().max().unwrap_or(1) as f64;
        let keep = if maxc > 0.0 {
            (minc / maxc).clamp(0.2, 1.0)
        } else {
            1.0
        };
        let mut rows = Vec::new();
        for i in 0..x.nrows() {
            if rng.uniform() <= keep {
                rows.push(i);
            }
        }
        if rows.is_empty() && x.nrows() > 0 {
            rows.push(0);
        }
        ctx.finish(Matrix::from_fn(rows.len(), x.ncols(), |r, c| {
            x.get(rows[r], c)
        }))
    }
}

/// Streaming mean (river `stats.Mean`).
#[derive(Clone, Debug, Default)]
pub struct OnlineMean {
    n: f64,
    mean: f64,
    updates: u64,
}

impl OnlineMean {
    /// Empty mean accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n <= 0.0 {
            f64::NAN
        } else {
            self.mean
        }
    }
}

impl PartialFit for OnlineMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            self.mean += (v - self.mean) / self.n;
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1.0;
        q.warmup = self.n < 1.0;
        q.explanation = format!("OnlineMean={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford mean",
                "running mean of column 0",
                format!("mean={before:.6e}"),
                format!("mean={after:.6e}"),
            ),
        )
    }
}

/// Streaming sum (river `stats.Sum`).
#[derive(Clone, Debug, Default)]
pub struct OnlineSum {
    n: u64,
    sum: f64,
    updates: u64,
}

impl OnlineSum {
    /// Empty sum.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current sum.
    pub fn score(&self) -> f64 {
        self.sum
    }
}

impl PartialFit for OnlineSum {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.sum;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if v.is_finite() {
                self.sum += v;
                self.n += 1;
            }
        }
        self.updates += 1;
        let after = self.sum;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n > 0;
        q.warmup = self.n == 0;
        q.explanation = format!("OnlineSum={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running sum",
                "finite entries of column 0",
                format!("sum={before:.6e}"),
                format!("sum={after:.6e}"),
            ),
        )
    }
}

/// Streaming variance (river `stats.Var`).
#[derive(Clone, Debug, Default)]
pub struct OnlineVar {
    n: f64,
    mean: f64,
    m2: f64,
    updates: u64,
}

impl OnlineVar {
    /// Empty variance accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current sample variance, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            f64::NAN
        } else {
            self.m2 / (self.n - 1.0)
        }
    }
}

impl PartialFit for OnlineVar {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineVar={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Welford variance",
                "running sample variance of column 0",
                format!("var={before:.6e}"),
                format!("var={after:.6e}"),
            ),
        )
    }
}

/// Streaming finite-count (river `stats.Count`).
#[derive(Clone, Debug, Default)]
pub struct OnlineCount {
    n: u64,
    updates: u64,
}

impl OnlineCount {
    /// Empty counter.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current count.
    pub fn score(&self) -> f64 {
        self.n as f64
    }
}

impl PartialFit for OnlineCount {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            if x.get(i, 0).is_finite() {
                self.n += 1;
            }
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n > 0;
        q.warmup = self.n == 0;
        q.explanation = format!("OnlineCount={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "count update",
                "finite entries in column 0",
                format!("n={before:.6e}"),
                format!("n={after:.6e}"),
            ),
        )
    }
}

/// Streaming lag-1 autocorrelation (river `stats.AutoCorr`).
#[derive(Clone, Debug, Default)]
pub struct OnlineAutoCorr {
    last: Option<f64>,
    n: f64,
    mx: f64,
    my: f64,
    c: f64,
    vx: f64,
    vy: f64,
    updates: u64,
}

impl OnlineAutoCorr {
    /// Empty lag-1 accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current lag-1 correlation, or NaN during warmup.
    pub fn score(&self) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let den = (self.vx * self.vy).sqrt();
        if den <= 1e-18 {
            f64::NAN
        } else {
            self.c / den
        }
    }
}

impl PartialFit for OnlineAutoCorr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if let Some(prev) = self.last {
                self.n += 1.0;
                let dx = prev - self.mx;
                self.mx += dx / self.n;
                let dy = v - self.my;
                self.my += dy / self.n;
                self.c += dx * (v - self.my);
                self.vx += dx * (prev - self.mx);
                self.vy += dy * (v - self.my);
            }
            self.last = Some(v);
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 3.0;
        q.warmup = self.n < 3.0;
        q.explanation = format!("OnlineAutoCorr={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "lag-1 correlation",
                "Welford Pearson of consecutive column-0 values",
                format!("rho={before:.6e}"),
                format!("rho={after:.6e}"),
            ),
        )
    }
}

/// Streaming variance threshold (river `feature_selection.VarianceThreshold`).
#[derive(Clone, Debug)]
pub struct OnlineVarianceThreshold {
    /// Keep columns with sample variance above this.
    pub threshold: f64,
    n: Vec<f64>,
    mean: Vec<f64>,
    m2: Vec<f64>,
    n_features: usize,
    updates: u64,
}

impl Default for OnlineVarianceThreshold {
    fn default() -> Self {
        Self {
            threshold: 0.0,
            n: Vec::new(),
            mean: Vec::new(),
            m2: Vec::new(),
            n_features: 0,
            updates: 0,
        }
    }
}

impl OnlineVarianceThreshold {
    /// Threshold `t` (not identification `p`).
    pub fn new(threshold: f64) -> Self {
        Self {
            threshold,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineVarianceThreshold {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_features == 0 {
            self.n_features = x.ncols();
            self.n = vec![0.0; x.ncols()];
            self.mean = vec![0.0; x.ncols()];
            self.m2 = vec![0.0; x.ncols()];
        } else if x.ncols() != self.n_features {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message(format!(
                        "OnlineVarianceThreshold saw {} columns after init with {}",
                        x.ncols(),
                        self.n_features
                    ))
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), 0, "feature space changed"),
            );
        }
        let before = self
            .m2
            .iter()
            .zip(&self.n)
            .map(|(m2, n)| if *n > 1.0 { m2 / (n - 1.0) } else { 0.0 })
            .sum::<f64>();
        for j in 0..x.ncols() {
            for i in 0..x.nrows() {
                let v = x.get(i, j);
                if !v.is_finite() {
                    continue;
                }
                self.n[j] += 1.0;
                let d = v - self.mean[j];
                self.mean[j] += d / self.n[j];
                self.m2[j] += d * (v - self.mean[j]);
            }
        }
        self.updates += 1;
        let after = self
            .m2
            .iter()
            .zip(&self.n)
            .map(|(m2, n)| if *n > 1.0 { m2 / (n - 1.0) } else { 0.0 })
            .sum::<f64>();
        let kept = (0..self.n_features)
            .filter(|&j| {
                let n = self.n[j];
                n > 1.0 && self.m2[j] / (n - 1.0) > self.threshold
            })
            .count();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), 0);
        q.effective_sample_size = self.n.iter().copied().fold(0.0_f64, f64::max);
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = kept >= 1;
        q.warmup = self.n.iter().all(|n| *n < 2.0);
        q.explanation = format!("OnlineVarianceThreshold kept={kept}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "column variance update",
                "Welford variance per column; transform drops near-constants",
                format!("sumvar={before:.6e}"),
                format!("sumvar={after:.6e} kept={kept}"),
            ),
        )
    }
}

impl Transform for OnlineVarianceThreshold {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let mut keep = Vec::new();
        for j in 0..x.ncols().min(self.n_features) {
            let n = self.n.get(j).copied().unwrap_or(0.0);
            let v = if n > 1.0 { self.m2[j] / (n - 1.0) } else { 0.0 };
            if v > self.threshold {
                keep.push(j);
            }
        }
        if keep.is_empty() {
            ctx.push(
                Issue::builder(IssueCode::NearZeroVariance)
                    .message("OnlineVarianceThreshold kept no columns; returning column 0")
                    .build(),
            );
            keep.push(0);
        }
        ctx.finish(Matrix::from_fn(x.nrows(), keep.len(), |i, j| {
            x.get(i, keep[j].min(x.ncols().saturating_sub(1)))
        }))
    }
}

/// Poisson-process feature inclusion (river `feature_selection.PoissonInclusion`).
///
/// Feature count is not identification `p`. A column is kept once a Uniform
/// draw falls below \(1-e^{-\lambda}\).
#[derive(Clone, Debug)]
pub struct PoissonInclusion {
    /// Inclusion rate \(\lambda\).
    pub rate: f64,
    keep: Vec<bool>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for PoissonInclusion {
    fn default() -> Self {
        Self {
            rate: 1.0,
            keep: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(41),
        }
    }
}

impl PoissonInclusion {
    /// Inclusion rate `rate` (not identification `p`).
    pub fn new(rate: f64) -> Self {
        Self {
            rate,
            ..Self::default()
        }
    }
}

impl PartialFit for PoissonInclusion {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.keep.is_empty() {
            self.keep = vec![false; x.ncols()];
        } else if self.keep.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message(format!(
                        "PoissonInclusion saw {} columns; model has {}",
                        x.ncols(),
                        self.keep.len()
                    ))
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.keep.iter().filter(|k| **k).count();
        let lam = if self.rate.is_finite() && self.rate > 0.0 {
            self.rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!("PoissonInclusion rate={}; using 1", self.rate))
                    .build(),
            );
            1.0
        };
        let p = 1.0 - (-lam).exp();
        for j in 0..x.ncols() {
            if !self.keep[j] && self.rng.uniform() < p {
                self.keep[j] = true;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.keep.iter().filter(|k| **k).count();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = after >= 1;
        q.warmup = after == 0;
        q.explanation = format!("PoissonInclusion kept={after}/{}", self.keep.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Poisson feature inclusion",
                "each unseen column is kept with probability 1-exp(-λ)",
                format!("kept={before}"),
                format!("kept={after}"),
            ),
        )
    }
}

impl Transform for PoissonInclusion {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if self.keep.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let idx: Vec<usize> = self
            .keep
            .iter()
            .enumerate()
            .filter(|(_, k)| **k)
            .map(|(j, _)| j)
            .collect();
        if idx.is_empty() {
            ctx.push(
                Issue::builder(IssueCode::NearZeroVariance)
                    .severity(Severity::Warning)
                    .message("PoissonInclusion kept no columns; returning the first")
                    .build(),
            );
            return ctx.finish(Matrix::from_fn(x.nrows(), 1, |i, _| x.get(i, 0)));
        }
        ctx.finish(Matrix::from_fn(x.nrows(), idx.len(), |i, j| {
            x.get(i, idx[j].min(x.ncols().saturating_sub(1)))
        }))
    }
}

/// Streaming univariate Gaussian (river `proba.Gaussian`).
#[derive(Clone, Debug, Default)]
pub struct OnlineGaussian {
    n: f64,
    mean: f64,
    m2: f64,
    updates: u64,
}

impl OnlineGaussian {
    /// Empty Gaussian.
    pub fn new() -> Self {
        Self::default()
    }

    /// Density at `x`, or NaN during warmup.
    pub fn pdf(&self, x: f64) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let var = self.m2 / (self.n - 1.0);
        if var <= 1e-18 {
            return f64::NAN;
        }
        let z = (x - self.mean) / var.sqrt();
        (-0.5 * z * z).exp() / (std::f64::consts::TAU * var).sqrt()
    }
}

impl PartialFit for OnlineGaussian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.mean;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
        }
        self.updates += 1;
        if self.n >= 2.0 && self.m2 / (self.n - 1.0) <= 1e-18 {
            ctx.push(
                Issue::builder(IssueCode::DegenerateDistribution)
                    .message("OnlineGaussian variance vanished; pdf is undefined")
                    .build(),
            );
        }
        let after = self.mean;
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some((after - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0 && self.m2 > 1e-18;
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineGaussian μ={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Gaussian sufficient stats",
                "Welford mean and variance of column 0",
                format!("mu={before:.6e}"),
                format!("mu={after:.6e}"),
            ),
        )
    }
}

/// Hard-example buffer around a perceptron (river `imblearn.HardSamplingClassifier`).
#[derive(Clone, Debug)]
pub struct HardSamplingClassifier {
    inner: Perceptron,
    buffer_x: Vec<Vec<f64>>,
    buffer_y: Vec<f64>,
    cap: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for HardSamplingClassifier {
    fn default() -> Self {
        Self {
            inner: Perceptron::new(),
            buffer_x: Vec::new(),
            buffer_y: Vec::new(),
            cap: 32,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl HardSamplingClassifier {
    /// Buffer capacity `cap` (not identification `p`).
    pub fn new(cap: usize) -> Self {
        Self {
            cap: cap.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for HardSamplingClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.buffer_x.len();
        if self.n_seen == 0 {
            let _ = self
                .inner
                .partial_fit(x, Some(y), &session.child("hs_init"));
        }
        let pred = if self.n_seen == 0 {
            Vector::zeros(x.nrows())
        } else {
            match self.inner.predict(x, &session.child("hs_pred")) {
                Ok(q) => q.value,
                Err(_) => Vector::zeros(x.nrows()),
            }
        };
        for i in 0..x.nrows().min(y.len()) {
            let wrong = self.n_seen == 0 || (i < pred.len() && (pred[i] - y[i]).abs() >= 0.5);
            if wrong && y[i].is_finite() {
                self.buffer_x
                    .push((0..x.ncols()).map(|j| x.get(i, j)).collect());
                self.buffer_y.push(y[i]);
            }
        }
        while self.buffer_x.len() > self.cap {
            self.buffer_x.remove(0);
            self.buffer_y.remove(0);
        }
        if !self.buffer_x.is_empty() {
            let p = self.buffer_x[0].len();
            let xb = Matrix::from_fn(self.buffer_x.len(), p, |i, j| self.buffer_x[i][j]);
            let yb = Vector::from_slice(&self.buffer_y);
            let _ = self
                .inner
                .partial_fit(&xb, Some(&yb), &session.child("hs_hard"));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.buffer_x.len();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = after as f64;
        q.parameter_delta_norm = Some((after as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("HardSamplingClassifier buffer={after}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "hard-example buffer",
                "misclassified rows are replayed through the perceptron",
                format!("buf={before}"),
                format!("buf={after}"),
            ),
        )
    }
}

impl Predict for HardSamplingClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        match self.inner.predict(x, session) {
            Ok(q) => ctx.finish(q.value),
            Err(_) => ctx.finish(Vector::zeros(x.nrows())),
        }
    }
}

/// Hard-example buffer around SGD regression (river `imblearn.HardSamplingRegressor`).
#[derive(Clone, Debug)]
pub struct HardSamplingRegressor {
    inner: SgdRegressor,
    buffer_x: Vec<Vec<f64>>,
    buffer_y: Vec<f64>,
    cap: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for HardSamplingRegressor {
    fn default() -> Self {
        Self {
            inner: SgdRegressor::new(),
            buffer_x: Vec::new(),
            buffer_y: Vec::new(),
            cap: 32,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl HardSamplingRegressor {
    /// Buffer capacity `cap` (not identification `p`).
    pub fn new(cap: usize) -> Self {
        Self {
            cap: cap.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for HardSamplingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.buffer_x.len();
        if self.n_seen == 0 {
            let _ = self
                .inner
                .partial_fit(x, Some(y), &session.child("hsr_init"));
        }
        let pred = if self.n_seen == 0 {
            Vector::zeros(x.nrows())
        } else {
            match self.inner.predict(x, &session.child("hsr_pred")) {
                Ok(q) => q.value,
                Err(_) => Vector::zeros(x.nrows()),
            }
        };
        let mut abs_e = Vec::with_capacity(x.nrows().min(y.len()));
        for i in 0..x.nrows().min(y.len()) {
            let e = if i < pred.len() {
                (pred[i] - y[i]).abs()
            } else {
                0.0
            };
            abs_e.push(e);
        }
        let mae = if abs_e.is_empty() {
            0.0
        } else {
            abs_e.iter().sum::<f64>() / abs_e.len() as f64
        };
        for i in 0..x.nrows().min(y.len()) {
            let hard = self.n_seen == 0 || abs_e[i] >= mae;
            if hard && y[i].is_finite() {
                self.buffer_x
                    .push((0..x.ncols()).map(|j| x.get(i, j)).collect());
                self.buffer_y.push(y[i]);
            }
        }
        while self.buffer_x.len() > self.cap {
            self.buffer_x.remove(0);
            self.buffer_y.remove(0);
        }
        if !self.buffer_x.is_empty() {
            let p = self.buffer_x[0].len();
            let xb = Matrix::from_fn(self.buffer_x.len(), p, |i, j| self.buffer_x[i][j]);
            let yb = Vector::from_slice(&self.buffer_y);
            let _ = self
                .inner
                .partial_fit(&xb, Some(&yb), &session.child("hsr_hard"));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.buffer_x.len();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = after as f64;
        q.parameter_delta_norm = Some((after as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("HardSamplingRegressor buffer={after}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "hard-residual buffer",
                "high-residual rows are replayed through SGD regression",
                format!("buf={before}"),
                format!("buf={after}"),
            ),
        )
    }
}

impl Predict for HardSamplingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        match self.inner.predict(x, session) {
            Ok(q) => ctx.finish(q.value),
            Err(_) => ctx.finish(Vector::zeros(x.nrows())),
        }
    }
}

/// Mondrian tree classifier (river `tree.mondrian.MondrianTreeClassifier`).
///
/// After a short warmup the tree draws one feature/threshold from the observed
/// range. Tree depth and leaf counts are not identification `p`.
#[derive(Clone, Debug)]
pub struct MondrianTree {
    mins: Vector,
    maxs: Vector,
    split_j: Option<usize>,
    threshold: f64,
    left: [f64; 2],
    right: [f64; 2],
    prior: [f64; 2],
    n_seen: u64,
    updates: u64,
}

impl Default for MondrianTree {
    fn default() -> Self {
        Self {
            mins: Vector::zeros(0),
            maxs: Vector::zeros(0),
            split_j: None,
            threshold: 0.0,
            left: [0.0, 0.0],
            right: [0.0, 0.0],
            prior: [0.0, 0.0],
            n_seen: 0,
            updates: 0,
        }
    }
}

impl MondrianTree {
    /// Empty Mondrian tree.
    pub fn new() -> Self {
        Self::default()
    }

    fn cls(y: f64) -> usize {
        if y >= 0.5 {
            1
        } else {
            0
        }
    }

    fn majority(counts: [f64; 2]) -> f64 {
        if counts[1] > counts[0] {
            1.0
        } else {
            0.0
        }
    }
}

impl PartialFit for MondrianTree {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.n_seen > 0 && !self.mins.is_empty() && self.mins.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .message(format!(
                        "MondrianTree saw {} columns; model has {}",
                        x.ncols(),
                        self.mins.len()
                    ))
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_split = self.split_j;
        if self.mins.is_empty() && x.ncols() > 0 && x.nrows() > 0 {
            self.mins = Vector::from_iter((0..x.ncols()).map(|j| x.get(0, j)));
            self.maxs = self.mins.clone();
        }
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            for j in 0..x.ncols().min(self.mins.len()) {
                let v = x.get(i, j);
                if v < self.mins[j] {
                    self.mins[j] = v;
                }
                if v > self.maxs[j] {
                    self.maxs[j] = v;
                }
            }
            let c = Self::cls(y[i]);
            self.prior[c] += 1.0;
            if let Some(j) = self.split_j {
                if x.get(i, j) <= self.threshold {
                    self.left[c] += 1.0;
                } else {
                    self.right[c] += 1.0;
                }
            }
            self.n_seen += 1;
        }
        if self.split_j.is_none() && self.n_seen >= 8 && !self.mins.is_empty() {
            let mut rng = Rng::new(0x4d07_d1a4u64 ^ self.n_seen);
            let mut cand = Vec::new();
            for j in 0..self.mins.len() {
                if self.maxs[j] - self.mins[j] > 1e-12 {
                    cand.push(j);
                }
            }
            if !cand.is_empty() {
                let j = cand[rng.below(cand.len())];
                let span = self.maxs[j] - self.mins[j];
                self.threshold = self.mins[j] + rng.uniform() * span;
                self.split_j = Some(j);
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before_split != self.split_j {
            1.0
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 8;
        q.explanation = match self.split_j {
            Some(j) => format!("MondrianTree split feat={j} thr={:.4}", self.threshold),
            None => format!("MondrianTree leaf n={}", self.n_seen),
        };
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "mondrian split / leaf counts",
                "range-tracking leaf; one random axis-aligned cut after warmup",
                format!("split={before_split:?}"),
                format!("split={:?}", self.split_j),
            ),
        )
    }
}

impl Predict for MondrianTree {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            match self.split_j {
                Some(j) if j < x.ncols() => {
                    if x.get(i, j) <= self.threshold {
                        Self::majority(self.left)
                    } else {
                        Self::majority(self.right)
                    }
                }
                _ => Self::majority(self.prior),
            }
        }));
        ctx.finish(out)
    }
}

/// Concatenate an online StandardScaler and MinMaxScaler (river `compose.TransformerUnion`).
#[derive(Clone, Debug, Default)]
pub struct TransformerUnion {
    standard: OnlineStandardScaler,
    minmax: OnlineMinMaxScaler,
    n_seen: u64,
    updates: u64,
}

impl TransformerUnion {
    /// Empty union of the two scalers.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for TransformerUnion {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let _ = self
            .standard
            .partial_fit(x, None, &session.child("union_std"));
        let _ = self.minmax.partial_fit(x, None, &session.child("union_mm"));
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(x.nrows() as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("TransformerUnion fitted both scalers on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "StandardScaler ∥ MinMaxScaler",
                "each child updates its own sufficient statistics, then columns are concatenated",
                "previous child states",
                "updated child states",
            ),
        )
    }
}

impl Transform for TransformerUnion {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let z1 = match self.standard.transform(x, &session.child("union_std_t")) {
            Ok(q) => q.value,
            Err(_) => Matrix::zeros(x.nrows(), x.ncols()),
        };
        let z2 = match self.minmax.transform(x, &session.child("union_mm_t")) {
            Ok(q) => q.value,
            Err(_) => Matrix::zeros(x.nrows(), x.ncols()),
        };
        let p = z1.ncols() + z2.ncols();
        ctx.finish(Matrix::from_fn(x.nrows(), p.max(1), |i, j| {
            if j < z1.ncols() {
                z1.get(i, j)
            } else if j - z1.ncols() < z2.ncols() {
                z2.get(i, j - z1.ncols())
            } else {
                0.0
            }
        }))
    }
}

/// Online scaler → SGD pipeline (river `compose.Pipeline`).
#[derive(Clone, Debug, Default)]
pub struct ComposePipeline {
    scaler: OnlineStandardScaler,
    inner: SgdRegressor,
    n_seen: u64,
    updates: u64,
}

impl ComposePipeline {
    /// Empty scaler-then-SGD pipeline.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ComposePipeline {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let _ = self
            .scaler
            .partial_fit(x, None, &session.child("pipe_scale"));
        let z = match self.scaler.transform(x, &session.child("pipe_zt")) {
            Ok(q) => q.value,
            Err(_) => x.clone(),
        };
        let _ = self
            .inner
            .partial_fit(&z, Some(y), &session.child("pipe_sgd"));
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(x.nrows() as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("ComposePipeline n={}", self.n_seen);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "scale then SGD",
                "online StandardScaler followed by SgdRegressor",
                "pre-batch pipeline",
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for ComposePipeline {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let z = match self.scaler.transform(x, &session.child("pipe_pred_z")) {
            Ok(q) => q.value,
            Err(_) => x.clone(),
        };
        match self.inner.predict(&z, session) {
            Ok(q) => ctx.finish(q.value),
            Err(_) => ctx.finish(Vector::zeros(x.nrows())),
        }
    }
}

/// Named elementwise map (river `compose.FuncTransformer`).
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ElementwiseMap {
    /// \(\log(1+x)\) on \(x > -1\).
    Log1p,
    /// Absolute value.
    Abs,
    /// Square.
    Square,
    /// Identity.
    Identity,
}

/// Streaming function transformer (river `compose.FuncTransformer`).
#[derive(Clone, Debug)]
pub struct FuncTransformer {
    /// Elementwise map.
    pub map: ElementwiseMap,
    n_seen: u64,
    updates: u64,
}

impl Default for FuncTransformer {
    fn default() -> Self {
        Self {
            map: ElementwiseMap::Identity,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl FuncTransformer {
    /// Transformer for `map`.
    pub fn new(map: ElementwiseMap) -> Self {
        Self {
            map,
            ..Self::default()
        }
    }
}

fn apply_map(map: ElementwiseMap, v: f64) -> f64 {
    match map {
        ElementwiseMap::Log1p => {
            if v > -1.0 {
                (1.0 + v).ln()
            } else {
                f64::NAN
            }
        }
        ElementwiseMap::Abs => v.abs(),
        ElementwiseMap::Square => v * v,
        ElementwiseMap::Identity => v,
    }
}

impl PartialFit for FuncTransformer {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if matches!(self.map, ElementwiseMap::Log1p) {
            let mut bad = 0u64;
            for i in 0..x.nrows() {
                for j in 0..x.ncols() {
                    let v = x.get(i, j);
                    if v.is_finite() && v <= -1.0 {
                        bad += 1;
                    }
                }
            }
            if bad > 0 {
                ctx.push(
                    Issue::builder(IssueCode::NonPositiveSeries)
                        .severity(Severity::Warning)
                        .message(format!(
                            "FuncTransformer::Log1p is undefined on {bad} entries ≤ −1"
                        ))
                        .build(),
                );
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 0;
        q.warmup = self.n_seen == 0;
        q.explanation = format!("FuncTransformer::{:?} saw {} rows", self.map, x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{:?}", self.map),
                "stateless elementwise map; partial_fit only records the stream length",
                format!("n={}", self.n_seen.saturating_sub(x.nrows() as u64)),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for FuncTransformer {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if matches!(self.map, ElementwiseMap::Log1p) {
            let mut bad = 0u64;
            for i in 0..x.nrows() {
                for j in 0..x.ncols() {
                    if x.get(i, j).is_finite() && x.get(i, j) <= -1.0 {
                        bad += 1;
                    }
                }
            }
            if bad > 0 {
                ctx.push(
                    Issue::builder(IssueCode::NonPositiveSeries)
                        .severity(Severity::Warning)
                        .message(format!("FuncTransformer::Log1p left {bad} cells undefined"))
                        .build(),
                );
            }
        }
        ctx.finish(Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            apply_map(self.map, x.get(i, j))
        }))
    }
}

/// Online factorization machine (river `facto.FMRegressor`).
///
/// Factor count is not identification `p`. A one-column design has no
/// pairwise interactions and degrades to a linear SGD update.
#[derive(Clone, Debug)]
pub struct FactorizationMachine {
    /// Latent factor width \(k\).
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    v: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FactorizationMachine {
    fn default() -> Self {
        Self {
            n_factors: 2,
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            v: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FactorizationMachine {
    /// FM with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        let k = self.v.ncols();
        if k == 0 || self.v.nrows() == 0 {
            return s;
        }
        for f in 0..k {
            let mut sum_vx = 0.0;
            let mut sum_v2x2 = 0.0;
            for j in 0..p.min(self.v.nrows()) {
                let vjf = self.v.get(j, f);
                let xj = x.get(i, j);
                sum_vx += vjf * xj;
                sum_v2x2 += vjf * vjf * xj * xj;
            }
            s += 0.5 * (sum_vx * sum_vx - sum_v2x2);
        }
        s
    }
}

impl PartialFit for FactorizationMachine {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        if !self.initialized {
            self.w = Vector::zeros(x.ncols());
            let k = self.n_factors.max(1);
            let mut rng = Rng::new(19);
            self.v = Matrix::from_fn(x.ncols(), k, |_, _| 0.01 * rng.standard_normal());
            self.initialized = true;
        } else if self.w.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.w.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut sse = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            sse += err * err;
            self.w0 -= eta * err;
            for j in 0..x.ncols() {
                self.w[j] -= eta * err * x.get(i, j);
            }
            let k = self.v.ncols();
            let p = x.ncols().min(self.v.nrows());
            for f in 0..k {
                let mut sum_vx = 0.0;
                for j in 0..p {
                    sum_vx += self.v.get(j, f) * x.get(i, j);
                }
                for j in 0..p {
                    let xj = x.get(i, j);
                    let vjf = self.v.get(j, f);
                    let g = err * (xj * (sum_vx - vjf * xj));
                    self.v.set(j, f, vjf - eta * g);
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.w.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            sse,
            sse,
            self.n_seen >= 2,
            self.n_seen < 4,
            "factorization machine",
            "SGD on the pairwise-interaction residual; a 1-column design has no pairs",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for FactorizationMachine {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Field-aware factorization machine (river `facto.FFMRegressor`).
///
/// Field count is not identification `p`. Field of column `j` is `j mod n_fields`.
#[derive(Clone, Debug)]
pub struct FfmRegressor {
    /// Latent width \(k\).
    pub n_factors: usize,
    /// Number of fields.
    pub n_fields: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    v: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FfmRegressor {
    fn default() -> Self {
        Self {
            n_factors: 2,
            n_fields: 2,
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            v: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FfmRegressor {
    /// FFM with `k` factors and `n_fields` fields.
    pub fn new(n_factors: usize, n_fields: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            n_fields: n_fields.max(1),
            ..Self::default()
        }
    }

    fn vid(&self, feat: usize, field: usize) -> usize {
        feat * self.n_fields.max(1) + field.min(self.n_fields.saturating_sub(1))
    }

    fn field(&self, j: usize) -> usize {
        j % self.n_fields.max(1)
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        let k = self.v.ncols();
        if k == 0 || self.v.nrows() == 0 || p < 2 {
            return s;
        }
        for a in 0..p {
            for b in (a + 1)..p {
                let xa = x.get(i, a);
                let xb = x.get(i, b);
                let ia = self.vid(a, self.field(b));
                let ib = self.vid(b, self.field(a));
                if ia >= self.v.nrows() || ib >= self.v.nrows() {
                    continue;
                }
                let mut dot = 0.0;
                for f in 0..k {
                    dot += self.v.get(ia, f) * self.v.get(ib, f);
                }
                s += xa * xb * dot;
            }
        }
        s
    }
}

impl PartialFit for FfmRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols();
        let k = self.n_factors.max(1);
        let nf = self.n_fields.max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.v = Matrix::from_fn(p.max(1) * nf, k, |_, _| 0.01);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("FfmRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut sse = 0.0;
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            sse += err * err;
            self.w0 -= eta * err;
            moved += err.abs();
            for j in 0..p.min(self.w.len()) {
                self.w[j] -= eta * err * x.get(i, j);
            }
            if p >= 2 && self.v.ncols() > 0 {
                for a in 0..p {
                    for b in (a + 1)..p {
                        let xa = x.get(i, a);
                        let xb = x.get(i, b);
                        let ia = self.vid(a, self.field(b));
                        let ib = self.vid(b, self.field(a));
                        if ia >= self.v.nrows() || ib >= self.v.nrows() {
                            continue;
                        }
                        for f in 0..k.min(self.v.ncols()) {
                            let va = self.v.get(ia, f);
                            let vb = self.v.get(ib, f);
                            self.v.set(ia, f, va - eta * err * xb * vb);
                            self.v.set(ib, f, vb - eta * err * xa * va);
                        }
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(x.nrows() as f64);
        q.loss_after = Some(if x.nrows() > 0 {
            sse / x.nrows() as f64
        } else {
            f64::NAN
        });
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("FFM k={k} fields={nf} sse={sse:.4e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "field-aware factor update",
                "SGD on pairwise interactions that depend on the other feature's field",
                "pre-batch FFM",
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for FfmRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Sampled factorization machine (river `facto.SFMRegressor`).
///
/// Each SGD step updates a random subset of factors. Factor / sample counts
/// are not identification `p`.
#[derive(Clone, Debug)]
pub struct SfmRegressor {
    /// Latent width \(k\).
    pub n_factors: usize,
    /// Factors updated per row (not identification `p`).
    pub sample_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    v: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for SfmRegressor {
    fn default() -> Self {
        Self {
            n_factors: 2,
            sample_factors: 1,
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            v: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(29),
        }
    }
}

impl SfmRegressor {
    /// SFM with `k` factors, updating `sample_factors` of them each row.
    pub fn new(n_factors: usize, sample_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            sample_factors: sample_factors.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        let k = self.v.ncols();
        if k == 0 || self.v.nrows() == 0 {
            return s;
        }
        for f in 0..k {
            let mut sum_vx = 0.0;
            let mut sum_v2x2 = 0.0;
            for j in 0..p.min(self.v.nrows()) {
                let vjf = self.v.get(j, f);
                let xj = x.get(i, j);
                sum_vx += vjf * xj;
                sum_v2x2 += vjf * vjf * xj * xj;
            }
            s += 0.5 * (sum_vx * sum_vx - sum_v2x2);
        }
        s
    }
}

impl PartialFit for SfmRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let k = self.n_factors.max(1);
        if !self.initialized {
            self.w = Vector::zeros(x.ncols());
            self.v = Matrix::from_fn(x.ncols(), k, |_, _| 0.01);
            self.initialized = true;
        } else if self.w.len() != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("SfmRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let take = self.sample_factors.max(1).min(k);
        let mut sse = 0.0;
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            sse += err * err;
            self.w0 -= eta * err;
            moved += err.abs();
            for j in 0..x.ncols().min(self.w.len()) {
                self.w[j] -= eta * err * x.get(i, j);
            }
            let p = x.ncols().min(self.v.nrows());
            let mut chosen = Vec::with_capacity(take);
            while chosen.len() < take {
                let f = self.rng.below(k);
                if !chosen.contains(&f) {
                    chosen.push(f);
                }
                if chosen.len() >= k {
                    break;
                }
            }
            for &f in &chosen {
                if f >= self.v.ncols() {
                    continue;
                }
                let mut sum_vx = 0.0;
                for j in 0..p {
                    sum_vx += self.v.get(j, f) * x.get(i, j);
                }
                for j in 0..p {
                    let xj = x.get(i, j);
                    let vjf = self.v.get(j, f);
                    let g = err * (xj * (sum_vx - vjf * xj));
                    self.v.set(j, f, vjf - eta * g);
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(x.nrows() as f64);
        q.loss_after = Some(if x.nrows() > 0 {
            sse / x.nrows() as f64
        } else {
            f64::NAN
        });
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("SFM k={k} sampled={take} sse={sse:.4e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sampled factor update",
                "SGD on a random subset of latent factors each row",
                "pre-batch SFM",
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for SfmRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Online text micro-clustering (river `cluster.TextClust`).
///
/// Rows are treated as bag-of-words. Merge radius is cosine distance.
/// Cluster count is not identification `p`.
#[derive(Clone, Debug)]
pub struct TextClust {
    /// Cosine-distance merge radius.
    pub radius: f64,
    micros: Vec<(Vector, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for TextClust {
    fn default() -> Self {
        Self {
            radius: 0.5,
            micros: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl TextClust {
    /// TextClust with cosine merge radius `radius`.
    pub fn new(radius: f64) -> Self {
        Self {
            radius,
            ..Self::default()
        }
    }

    fn cosine(a: &Vector, b: &Vector) -> f64 {
        let n = a.len().min(b.len());
        let mut dot = 0.0;
        let mut na = 0.0;
        let mut nb = 0.0;
        for i in 0..n {
            dot += a[i] * b[i];
            na += a[i] * a[i];
            nb += b[i] * b[i];
        }
        if na <= 1e-18 || nb <= 1e-18 {
            return 0.0;
        }
        (dot / (na.sqrt() * nb.sqrt())).clamp(-1.0, 1.0)
    }

    fn row_vec(x: &Matrix, i: usize) -> Vector {
        Vector::from_iter((0..x.ncols()).map(|j| x.get(i, j).max(0.0)))
    }

    fn assign(&self, v: &Vector) -> Option<(usize, f64)> {
        let mut best: Option<(usize, f64)> = None;
        for (i, (c, _)) in self.micros.iter().enumerate() {
            let sim = Self::cosine(c, v);
            if best.map(|(_, s)| sim > s).unwrap_or(true) {
                best = Some((i, sim));
            }
        }
        best
    }
}

impl PartialFit for TextClust {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let rad = if self.radius.is_finite() && self.radius > 0.0 {
            self.radius
        } else {
            0.5
        };
        let before = self.n_seen;
        let mut spawned = 0u64;
        let mut merged = 0u64;
        for i in 0..x.nrows() {
            let v = Self::row_vec(x, i);
            if v.as_slice().iter().all(|z| *z <= 0.0) {
                continue;
            }
            match self.assign(&v) {
                Some((idx, sim)) if 1.0 - sim <= rad => {
                    let w = self.micros[idx].1;
                    let nw = w + 1.0;
                    for j in 0..v.len().min(self.micros[idx].0.len()) {
                        self.micros[idx].0[j] =
                            (w * self.micros[idx].0[j] + v[j]) / nw;
                    }
                    self.micros[idx].1 = nw;
                    merged += 1;
                }
                _ => {
                    self.micros.push((v, 1.0));
                    spawned += 1;
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(spawned as f64);
        q.information_gain = Some((spawned + merged) as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "TextClust micros={} spawned={spawned} merged={merged}",
            self.micros.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "text micro-cluster update",
                "cosine merge of bag-of-words rows into TF centroids",
                format!("n={before} micros"),
                format!("n={} micros={}", self.n_seen, self.micros.len()),
            ),
        )
    }
}

impl Predict for TextClust {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.micros.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let v = Self::row_vec(x, i);
            self.assign(&v).map(|(j, _)| j as f64).unwrap_or(0.0)
        }));
        ctx.finish(out)
    }
}

/// DBSTREAM-style fading density micro-clusters (river `cluster.DBSTREAM`).
///
/// Radius / micro count are not identification `p`.
#[derive(Clone, Debug)]
pub struct Dbstream {
    /// Merge radius.
    pub radius: f64,
    /// Per-step weight decay.
    pub lambda: f64,
    micros: Vec<(Vector, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for Dbstream {
    fn default() -> Self {
        Self {
            radius: 1.0,
            lambda: 0.99,
            micros: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Dbstream {
    /// DBSTREAM with merge radius `radius`.
    pub fn new(radius: f64) -> Self {
        Self {
            radius,
            ..Self::default()
        }
    }
}

impl PartialFit for Dbstream {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let rad = if self.radius.is_finite() && self.radius > 0.0 {
            self.radius
        } else {
            1.0
        };
        let lam = self.lambda.clamp(1e-6, 1.0);
        let before = self.n_seen;
        let mut merged = 0u64;
        let mut spawned = 0u64;
        for i in 0..x.nrows() {
            for mc in self.micros.iter_mut() {
                mc.1 *= lam;
            }
            self.micros.retain(|mc| mc.1 >= 1e-3);
            let row = x.row(i);
            let mut best = None;
            let mut best_d = f64::INFINITY;
            for (c, (ctr, _)) in self.micros.iter().enumerate() {
                let mut d = 0.0;
                for j in 0..row.len().min(ctr.len()) {
                    let z = row[j] - ctr[j];
                    d += z * z;
                }
                d = d.sqrt();
                if d < best_d {
                    best_d = d;
                    best = Some(c);
                }
            }
            if let Some(c) = best {
                if best_d <= rad {
                    let w = self.micros[c].1;
                    let nw = w + 1.0;
                    for j in 0..self.micros[c].0.len().min(row.len()) {
                        self.micros[c].0[j] = (w * self.micros[c].0[j] + row[j]) / nw;
                    }
                    self.micros[c].1 = nw;
                    merged += 1;
                    continue;
                }
            }
            self.micros.push((row, 1.0));
            spawned += 1;
        }
        // Shared-density merge of overlapping micros.
        let mut i = 0;
        while i < self.micros.len() {
            let mut j = i + 1;
            while j < self.micros.len() {
                let mut d = 0.0;
                let (a, b) = (&self.micros[i].0, &self.micros[j].0);
                for k in 0..a.len().min(b.len()) {
                    let z = a[k] - b[k];
                    d += z * z;
                }
                if d.sqrt() <= 2.0 * rad {
                    let wa = self.micros[i].1;
                    let wb = self.micros[j].1;
                    let nw = wa + wb;
                    for k in 0..self.micros[i].0.len().min(self.micros[j].0.len()) {
                        self.micros[i].0[k] =
                            (wa * self.micros[i].0[k] + wb * self.micros[j].0[k]) / nw.max(1e-12);
                    }
                    self.micros[i].1 = nw;
                    self.micros.remove(j);
                    merged += 1;
                } else {
                    j += 1;
                }
            }
            i += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.micros.iter().map(|m| m.1).sum();
        q.parameter_delta_norm = Some(spawned as f64);
        q.information_gain = Some((merged + spawned) as f64);
        q.still_identified = !self.micros.is_empty();
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "DBSTREAM micros={} spawned={spawned} merged={merged}",
            self.micros.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "DBSTREAM micro-cluster update",
                "fading centroids plus shared-density merge",
                format!("n={before}"),
                format!("n={} micros={}", self.n_seen, self.micros.len()),
            ),
        )
    }
}

impl Predict for Dbstream {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.micros.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let row = x.row(i);
            let mut best = 0usize;
            let mut best_d = f64::INFINITY;
            for (c, (ctr, _)) in self.micros.iter().enumerate() {
                let mut d = 0.0;
                for j in 0..row.len().min(ctr.len()) {
                    let z = row[j] - ctr[j];
                    d += z * z;
                }
                if d < best_d {
                    best_d = d;
                    best = c;
                }
            }
            best as f64
        }));
        ctx.finish(out)
    }
}

/// Mini-batch MLP regressor (river `neural_net.MLPRegressor`).
///
/// Hidden width is not identification `p`.
#[derive(Clone, Debug)]
pub struct MiniBatchMlp {
    inner: crate::neural::MLPRegressor,
}

impl Default for MiniBatchMlp {
    fn default() -> Self {
        let mut inner = crate::neural::MLPRegressor::new();
        inner.hidden = 4;
        inner.learning_rate = 0.05;
        inner.max_iter = 1;
        inner.seed = 3;
        Self { inner }
    }
}

impl MiniBatchMlp {
    /// MLP with `hidden` ReLU units.
    pub fn new(hidden: usize) -> Self {
        let mut s = Self::default();
        s.inner.hidden = hidden.max(1);
        s
    }
}

impl PartialFit for MiniBatchMlp {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for MiniBatchMlp {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Factorization-machine recommender (river `reco` / `facto.FM`).
///
/// Factor count is not identification `p`.
#[derive(Clone, Debug)]
pub struct FmReco {
    inner: FactorizationMachine,
}

impl Default for FmReco {
    fn default() -> Self {
        Self {
            inner: FactorizationMachine::new(2),
        }
    }
}

impl FmReco {
    /// FM recommender with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            inner: FactorizationMachine::new(n_factors),
        }
    }
}

impl PartialFit for FmReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for FmReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// River `metrics.LogCosh` — online \(\log\cosh\) residual loss.
#[derive(Clone, Debug, Default)]
pub struct OnlineLogCosh {
    acc: f64,
    n: u64,
    updates: u64,
}

impl OnlineLogCosh {
    /// Empty log-cosh metric.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean log-cosh, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.acc / self.n as f64
        }
    }
}

impl PartialFit for OnlineLogCosh {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        metric_partial(
            session,
            "log_cosh",
            x,
            y,
            self.updates,
            self.n,
            |pred, truth| {
                let e = pred - truth;
                let ae = e.abs();
                // `log(cosh(e))` = `|e| + log(1 + exp(-2|e|)) - log(2)`.
                let lch = ae + ((-2.0 * ae).exp() + 1.0).ln() - 2.0_f64.ln();
                self.acc += lch;
                self.n += 1;
                self.updates += 1;
                self.score()
            },
        )
    }
}

/// Extremely Fast Decision Tree regressor (`river.tree.HoeffdingAdaptiveTreeRegressor` / EFDT).
///
/// Same sufficient statistics as [`HoeffdingRegressor`], with a smaller
/// `min_samples` and larger `tau` so splits fire earlier.
#[derive(Clone, Debug)]
pub struct EfdtRegressor {
    inner: HoeffdingRegressor,
}

impl Default for EfdtRegressor {
    fn default() -> Self {
        Self {
            inner: HoeffdingRegressor {
                min_samples: 5,
                tau: 0.15,
                ..HoeffdingRegressor::default()
            },
        }
    }
}

impl EfdtRegressor {
    /// Eager Hoeffding regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for EfdtRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let q = self.inner.partial_fit(x, y, session)?;
        let mut ctx = FitCtx::with_session(session.child("efdt_regressor"));
        ctx.push(
            Issue::builder(IssueCode::JitterInjected)
                .severity(Severity::Advisory)
                .message(
                    "EFDT-regressor uses an eager Hoeffding bound; it does not re-evaluate existing splits",
                )
                .build(),
        );
        ctx.finish(q.value)
    }
}

impl Predict for EfdtRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// River stacking classifier: two perceptrons plus a meta perceptron.
///
/// Member count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineStacking {
    a: Perceptron,
    b: Perceptron,
    meta: Perceptron,
    n_seen: u64,
    updates: u64,
}

impl OnlineStacking {
    /// Empty stacked perceptrons.
    pub fn new() -> Self {
        Self::default()
    }

    fn score_one(p: &Perceptron, x: &Matrix, i: usize) -> f64 {
        let mut s = 0.0;
        let w = p.coef();
        for j in 0..x.ncols().min(w.len()) {
            s += w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for OnlineStacking {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("online_stacking"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        let z = Matrix::from_fn(x.nrows(), 2, |i, j| {
            if j == 0 {
                Self::score_one(&self.a, x, i)
            } else {
                Self::score_one(&self.b, x, i)
            }
        });
        self.a.partial_fit(x, Some(y), session)?;
        self.b.partial_fit(x, Some(y), session)?;
        self.meta.partial_fit(&z, Some(y), session)?;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("stacked perceptrons; n={}", self.n_seen);
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("online stacking has seen fewer than 2 rows")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "updated two base perceptrons and the meta-learner",
                "river stacking trains bases then a meta-learner on their scores",
                "previous stacked state",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

/// ADWIN-resetting kNN (`river.neighbors.KNNADWINClassifier`).
///
/// Neighborhood size `k` is not identification `p`.
#[derive(Clone, Debug)]
pub struct AdwinKnn {
    knn: KnnClassifier,
    adwin: Adwin,
    n_seen: u64,
    updates: u64,
}

impl Default for AdwinKnn {
    fn default() -> Self {
        Self {
            knn: KnnClassifier::new(3),
            adwin: Adwin::default(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl AdwinKnn {
    /// kNN with ADWIN window reset.
    pub fn new(k: usize) -> Self {
        Self {
            knn: KnnClassifier::new(k.max(1)),
            ..Self::default()
        }
    }
}

impl PartialFit for AdwinKnn {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("adwin_knn"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let mut drifted = false;
        if !self.knn.xs.is_empty() {
            match self.knn.predict(x, session) {
                Ok(pred) => {
                    for i in 0..x.nrows().min(y.len()).min(pred.value.len()) {
                        let err = if (pred.value[i] - y[i]).abs() > 0.5 {
                            1.0
                        } else {
                            0.0
                        };
                        match self.adwin.update(err, session) {
                            Ok(q) => {
                                if matches!(q.value, DriftDecision::Drift { .. }) {
                                    drifted = true;
                                }
                            }
                            Err(e) => {
                                if e.primary().code == IssueCode::ConceptDriftDetected {
                                    drifted = true;
                                }
                            }
                        }
                    }
                }
                Err(_) => {}
            }
        }
        if drifted {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message("ADWIN detected a change; kNN memory was cleared")
                    .build(),
            );
            self.knn.xs.clear();
            self.knn.ys.clear();
        }
        self.knn.partial_fit(x, Some(y), session)?;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.knn.xs.len() as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.knn.xs.len() >= self.knn.k;
        q.warmup = self.knn.xs.len() < self.knn.k;
        q.explanation = if drifted {
            "ADWIN reset the kNN window after a detected change".into()
        } else {
            format!("stored neighbors; window={}", self.knn.xs.len())
        };
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                if drifted {
                    "cleared then stored the new neighborhood"
                } else {
                    "appended labeled neighbors"
                },
                "ADWIN-kNN forgets neighbors when the 0-1 loss stream changes",
                "previous neighbor window",
                format!("window={}", self.knn.xs.len()),
            ),
        )
    }
}

fn mix_hash(item: f64, salt: u64) -> u64 {
    let mut h = item.to_bits() ^ salt.wrapping_mul(0x9E37_79B9_7F4A_7C15);
    h ^= h >> 30;
    h = h.wrapping_mul(0xBF58_476D_1CE4_E5B9);
    h ^= h >> 27;
    h = h.wrapping_mul(0x94D0_49BB_1331_11EB);
    h ^= h >> 31;
    h
}

fn row_hash(x: &Matrix, i: usize, salt: u64) -> u64 {
    let mut h = salt;
    for j in 0..x.ncols() {
        h ^= mix_hash(x.get(i, j), (j as u64).wrapping_add(1));
    }
    h
}

/// Count–Min sketch (river `sketch.CountMinSketch`).
///
/// Depth / width are sketch sizes, not identification `p`.
#[derive(Clone, Debug)]
pub struct CountMinSketch {
    /// Hash rows.
    pub depth: usize,
    /// Buckets per row.
    pub width: usize,
    table: Vec<Vec<u64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for CountMinSketch {
    fn default() -> Self {
        Self::new(3, 32)
    }
}

impl CountMinSketch {
    /// `depth` × `width` sketch.
    pub fn new(depth: usize, width: usize) -> Self {
        let d = depth.max(1);
        let w = width.max(2);
        Self {
            depth: d,
            width: w,
            table: vec![vec![0; w]; d],
            n_seen: 0,
            updates: 0,
        }
    }

    fn add(&mut self, key: u64) {
        for r in 0..self.depth {
            let j = (mix_hash(key as f64, r as u64 + 1) as usize) % self.width;
            self.table[r][j] = self.table[r][j].saturating_add(1);
        }
    }

    /// Conservative count for a hashed key.
    pub fn estimate(&self, key: u64) -> u64 {
        let mut m = u64::MAX;
        for r in 0..self.depth {
            let j = (mix_hash(key as f64, r as u64 + 1) as usize) % self.width;
            m = m.min(self.table[r][j]);
        }
        if m == u64::MAX {
            0
        } else {
            m
        }
    }
}

impl PartialFit for CountMinSketch {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("count_min_sketch"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.n_seen;
        for i in 0..x.nrows() {
            self.add(row_hash(x, i, 1));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.explanation = format!("Count-Min ingested {} rows", x.nrows());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("added {} hashed rows to the Count-Min table", x.nrows()),
                "river.sketch.CountMinSketch stores conservative frequency estimates",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

/// Bloom filter (river `sketch.BloomFilter`).
///
/// Bit / hash counts are sketch sizes, not identification `p`.
#[derive(Clone, Debug)]
pub struct BloomFilter {
    /// Bit capacity.
    pub n_bits: usize,
    /// Independent hashes.
    pub n_hashes: usize,
    words: Vec<u64>,
    n_seen: u64,
    updates: u64,
}

impl Default for BloomFilter {
    fn default() -> Self {
        Self::new(256, 3)
    }
}

impl BloomFilter {
    /// `n_bits` filter with `n_hashes` probes.
    pub fn new(n_bits: usize, n_hashes: usize) -> Self {
        let bits = n_bits.max(64);
        Self {
            n_bits: bits,
            n_hashes: n_hashes.max(1),
            words: vec![0; (bits + 63) / 64],
            n_seen: 0,
            updates: 0,
        }
    }

    fn set_bit(&mut self, i: usize) {
        let w = i / 64;
        let b = i % 64;
        if let Some(word) = self.words.get_mut(w) {
            *word |= 1u64 << b;
        }
    }

    fn get_bit(&self, i: usize) -> bool {
        let w = i / 64;
        let b = i % 64;
        self.words
            .get(w)
            .map(|word| (*word >> b) & 1 == 1)
            .unwrap_or(false)
    }

    fn add(&mut self, key: u64) {
        for h in 0..self.n_hashes {
            let idx = (mix_hash(key as f64, h as u64 + 11) as usize) % self.n_bits;
            self.set_bit(idx);
        }
    }

    /// Whether `key` may have been inserted.
    pub fn maybe_contains(&self, key: u64) -> bool {
        (0..self.n_hashes).all(|h| {
            let idx = (mix_hash(key as f64, h as u64 + 11) as usize) % self.n_bits;
            self.get_bit(idx)
        })
    }
}

impl PartialFit for BloomFilter {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("bloom_filter"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.n_seen;
        for i in 0..x.nrows() {
            self.add(row_hash(x, i, 3));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = true;
        q.explanation = format!("Bloom filter ingested {} rows", x.nrows());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("set bits for {} hashed rows", x.nrows()),
                "river.sketch.BloomFilter records membership with a false-positive rate",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

/// HyperLogLog cardinality sketch (river `sketch.HyperLogLog`).
///
/// Register count \(2^{p}\) is a sketch size, not identification `p`.
#[derive(Clone, Debug)]
pub struct HyperLogLog {
    /// Precision bits (`4..=16`).
    pub precision: usize,
    registers: Vec<u8>,
    n_seen: u64,
    updates: u64,
}

impl Default for HyperLogLog {
    fn default() -> Self {
        Self::new(6)
    }
}

impl HyperLogLog {
    /// HyperLogLog with `precision` index bits.
    pub fn new(precision: usize) -> Self {
        let p = precision.clamp(4, 16);
        Self {
            precision: p,
            registers: vec![0; 1usize << p],
            n_seen: 0,
            updates: 0,
        }
    }

    fn add(&mut self, key: u64) {
        let h = mix_hash(key as f64, 0x0111_u64);
        let nreg = self.registers.len();
        let idx = ((h >> (64 - self.precision)) as usize).min(nreg.saturating_sub(1));
        let w = h << self.precision;
        let rho = (w.leading_zeros() + 1).min(64 - self.precision as u32 + 1) as u8;
        if let Some(reg) = self.registers.get_mut(idx) {
            *reg = (*reg).max(rho);
        }
    }

    /// Cardinality estimate.
    pub fn estimate(&self) -> f64 {
        let m = self.registers.len() as f64;
        let mut s = 0.0;
        let mut zeros = 0usize;
        for &r in &self.registers {
            s += 2.0_f64.powi(-(r as i32));
            if r == 0 {
                zeros += 1;
            }
        }
        let alpha = match self.registers.len() {
            16 => 0.673,
            32 => 0.697,
            64 => 0.709,
            _ => 0.7213 / (1.0 + 1.079 / m),
        };
        let e = alpha * m * m / s.max(1e-12);
        if e <= 2.5 * m && zeros > 0 {
            m * (m / zeros as f64).ln()
        } else {
            e
        }
    }
}

impl PartialFit for HyperLogLog {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("hyperloglog"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.estimate();
        for i in 0..x.nrows() {
            self.add(row_hash(x, i, 7));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.estimate();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = after;
        q.information_gain = Some((after - before).abs());
        q.still_identified = true;
        q.explanation = format!("HLL cardinality ≈ {after:.4e}");
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("updated HyperLogLog registers ({:.4e} → {after:.4e})", before),
                "river.sketch.HyperLogLog estimates distinct hashed rows",
                format!("card≈{before:.4e}"),
                format!("card≈{after:.4e}"),
            ),
        )
    }
}

/// Online majority vote of two perceptrons (river `ensemble.VotingClassifier`).
///
/// Member count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineVoting {
    a: Perceptron,
    b: Perceptron,
    n_seen: u64,
    updates: u64,
}

impl OnlineVoting {
    /// Empty voting ensemble.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineVoting {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("online_voting"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        self.a.partial_fit(x, Some(y), session)?;
        self.b.partial_fit(x, Some(y), session)?;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("voting perceptrons; n={}", self.n_seen);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "updated both voting members",
                "river VotingClassifier trains members independently and averages signs",
                "previous vote state",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

/// Heavy-hitter sketch (river `sketch.HeavyHitters`).
///
/// Capacity `k` is a sketch size, not identification `p`.
#[derive(Clone, Debug)]
pub struct HeavyHitters {
    /// Maximum stored keys.
    pub k: usize,
    counts: HashMap<u64, u64>,
    n_seen: u64,
    updates: u64,
}

impl Default for HeavyHitters {
    fn default() -> Self {
        Self::new(8)
    }
}

impl HeavyHitters {
    /// Keep the `k` most frequent hashed rows.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            counts: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }

    /// Current top keys (hash, count), highest count first.
    pub fn top(&self) -> Vec<(u64, u64)> {
        let mut v: Vec<(u64, u64)> = self.counts.iter().map(|(&k, &c)| (k, c)).collect();
        v.sort_by(|a, b| b.1.cmp(&a.1).then(a.0.cmp(&b.0)));
        v.truncate(self.k);
        v
    }
}

impl PartialFit for HeavyHitters {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("heavy_hitters"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.n_seen;
        for i in 0..x.nrows() {
            let key = row_hash(x, i, 13);
            *self.counts.entry(key).or_insert(0) += 1;
        }
        if self.counts.len() > self.k.saturating_mul(4).max(self.k) {
            let keep: Vec<u64> = self.top().into_iter().map(|(k, _)| k).collect();
            self.counts.retain(|k, _| keep.contains(k));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.counts.is_empty();
        q.explanation = format!("heavy hitters stored {} keys", self.counts.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("updated frequency table ({before} → {} rows)", self.n_seen),
                "river.sketch.HeavyHitters keeps the most frequent hashed rows",
                format!("n={before}"),
                format!("n={} keys={}", self.n_seen, self.counts.len()),
            ),
        )
    }
}

/// Online one-vs-rest perceptrons (river `multiclass.OneVsRestClassifier`).
///
/// Class count is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineOneVsRest {
    members: HashMap<i64, Perceptron>,
    n_seen: u64,
    updates: u64,
}

impl OnlineOneVsRest {
    /// Empty OvR ensemble.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineOneVsRest {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("online_ovr"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        let mut labels: Vec<i64> = y
            .as_slice()
            .iter()
            .map(|v| v.round() as i64)
            .collect();
        for lab in self.members.keys().copied() {
            labels.push(lab);
        }
        labels.sort_unstable();
        labels.dedup();
        for lab in labels {
            let yi = Vector::from_iter((0..y.len()).map(|i| {
                if (y[i].round() as i64) == lab {
                    1.0
                } else {
                    0.0
                }
            }));
            self.members
                .entry(lab)
                .or_default()
                .partial_fit(x, Some(&yi), session)?;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.members.len() >= 2;
        q.warmup = self.members.len() < 2;
        q.explanation = format!("OvR members={}", self.members.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("updated {} one-vs-rest perceptrons", self.members.len()),
                "river OneVsRest trains one binary member per observed label",
                "previous OvR state",
                format!("n_seen={} members={}", self.n_seen, self.members.len()),
            ),
        )
    }
}

/// Distinct hashed-row set (river `sketch.Set`).
///
/// Stored cardinality is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct SketchSet {
    keys: HashSet<u64>,
    n_seen: u64,
    updates: u64,
}

impl SketchSet {
    /// Empty set sketch.
    pub fn new() -> Self {
        Self::default()
    }

    /// Distinct hashed keys seen so far.
    pub fn cardinality(&self) -> usize {
        self.keys.len()
    }
}

impl PartialFit for SketchSet {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("sketch_set"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.keys.len();
        for i in 0..x.nrows() {
            self.keys.insert(row_hash(x, i, 17));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.keys.len() as f64;
        q.information_gain = Some((self.keys.len() - before) as f64);
        q.still_identified = true;
        q.explanation = format!("sketch set cardinality={}", self.keys.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("set grew {before} → {}", self.keys.len()),
                "river.sketch.Set stores distinct hashed rows exactly",
                format!("card={before}"),
                format!("card={}", self.keys.len()),
            ),
        )
    }
}

/// Online output-code ensemble (river `multiclass.OutputCodeClassifier`).
///
/// Code-bit count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineOutputCode {
    bits: Vec<Perceptron>,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineOutputCode {
    fn default() -> Self {
        Self {
            bits: vec![Perceptron::new(), Perceptron::new(), Perceptron::new()],
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineOutputCode {
    /// Three-bit output code.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineOutputCode {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("online_output_code"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        for (b, member) in self.bits.iter_mut().enumerate() {
            let code = Vector::from_iter((0..y.len()).map(|i| {
                let lab = y[i].round() as i64;
                if ((lab >> (b % 8)) & 1) == 1 {
                    1.0
                } else {
                    0.0
                }
            }));
            member.partial_fit(x, Some(&code), session)?;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 1;
        q.explanation = format!("output-code bits={} n={}", self.bits.len(), self.n_seen);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("updated {} ECOC bits", self.bits.len()),
                "river OutputCode trains one binary member per code bit",
                "previous ECOC state",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

fn online_linear_dim(fit_intercept: bool, p: usize) -> usize {
    p + if fit_intercept { 1 } else { 0 }
}

fn row_aug(x: &Matrix, i: usize, fit_intercept: bool) -> Vector {
    let p = online_linear_dim(fit_intercept, x.ncols());
    let mut v = Vector::zeros(p);
    if fit_intercept {
        v[0] = 1.0;
        for j in 0..x.ncols() {
            v[j + 1] = x.get(i, j);
        }
    } else {
        for j in 0..x.ncols() {
            v[j] = x.get(i, j);
        }
    }
    v
}

/// AdaGrad linear regressor (river `optim.AdaGrad` + `linear_model.LinearRegression`).
#[derive(Clone, Debug)]
pub struct AdaGradRegressor {
    /// Global step \(\eta\).
    pub learning_rate: f64,
    /// Diagonal floor \(\varepsilon\).
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    g2: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaGradRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.5,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            g2: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaGradRegressor {
    /// Default AdaGrad regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdaGradRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.g2 = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.5
        };
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.g2[j] += g * g;
                let den = self.g2[j].sqrt() + eps;
                self.coef[j] -= eta * g / den;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdaGrad linear weights",
            "diagonal accumulator √(Σ g²) rescales each coordinate",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AdaGradRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Adam linear regressor (river `optim.Adam`).
#[derive(Clone, Debug)]
pub struct AdamRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdamRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdamRegressor {
    /// Default Adam regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdamRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                self.coef[j] -= eta * mhat / (vhat.sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Adam linear weights",
            "bias-corrected first and second moments rescale the residual gradient",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AdamRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// RMSProp linear regressor (river `optim.RMSProp`).
#[derive(Clone, Debug)]
pub struct RmsPropRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Second-moment decay \(\rho\).
    pub rho: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    acc: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for RmsPropRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            rho: 0.9,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            acc: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl RmsPropRegressor {
    /// Default RMSProp regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for RmsPropRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.acc = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let rho = self.rho.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.acc[j] = rho * self.acc[j] + (1.0 - rho) * g * g;
                self.coef[j] -= eta * g / (self.acc[j].sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "RMSProp linear weights",
            "decaying second-moment accumulator rescales the residual gradient",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for RmsPropRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Streaming local outlier factor on a sliding window (river `anomaly.LocalOutlierFactor`).
///
/// Neighbor count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineLof {
    /// Neighbors used for the k-distance.
    pub k: usize,
    /// Window capacity.
    pub window: usize,
    xs: Vec<Vec<f64>>,
    last_score: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineLof {
    fn default() -> Self {
        Self {
            k: 3,
            window: 32,
            xs: Vec::new(),
            last_score: f64::NAN,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineLof {
    /// LOF memory with `k` neighbors.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }

    fn score_row(&self, row: &[f64]) -> f64 {
        if self.xs.len() <= self.k {
            return f64::NAN;
        }
        let mut dists: Vec<f64> = self
            .xs
            .iter()
            .map(|o| {
                let mut s = 0.0;
                let p = row.len().min(o.len());
                for j in 0..p {
                    let d = row[j] - o[j];
                    s += d * d;
                }
                s.sqrt()
            })
            .collect();
        dists.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = self.k.min(dists.len().saturating_sub(1));
        if kk == 0 {
            f64::NAN
        } else {
            dists[kk]
        }
    }
}

impl PartialFit for OnlineLof {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen > 0 {
            if let Some(first) = self.xs.first() {
                if first.len() != x.ncols() {
                    ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                    return finish_explain(
                        ctx,
                        reject_explain(
                            self.updates,
                            x.nrows(),
                            self.n_seen,
                            "feature space changed",
                        ),
                    );
                }
            }
        }
        let before = self.last_score;
        for i in 0..x.nrows() {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            self.last_score = self.score_row(&row);
            self.xs.push(row);
            while self.xs.len() > self.window.max(1) {
                self.xs.remove(0);
            }
            self.n_seen += 1;
        }
        if self.xs.len() <= self.k {
            ctx.push(
                Issue::builder(IssueCode::WindowTooShort)
                    .message(format!(
                        "OnlineLof window={} ≤ k={}; k-distance is undefined",
                        self.xs.len(),
                        self.k
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.last_score;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.xs.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.xs.len() > self.k;
        q.warmup = self.xs.len() <= self.k;
        q.explanation = format!("OnlineLof k-distance={after:.6e} window={}", self.xs.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sliding-window k-distance",
                "each new row is scored against the retained window, then appended",
                format!("score={before:.6e}"),
                format!("score={after:.6e}"),
            ),
        )
    }
}

impl Predict for OnlineLof {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.xs.len() <= self.k {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            self.score_row(&row)
        })))
    }
}

/// Streaming Beta (river `proba.Beta`) updated from a binary label stream.
#[derive(Clone, Debug)]
pub struct OnlineBeta {
    alpha: f64,
    beta: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineBeta {
    fn default() -> Self {
        Self {
            alpha: 1.0,
            beta: 1.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineBeta {
    /// Uniform Beta(1, 1) prior.
    pub fn new() -> Self {
        Self::default()
    }

    /// Posterior mean \(\alpha/(\alpha+\beta)\).
    pub fn score(&self) -> f64 {
        let z = self.alpha + self.beta;
        if z <= 0.0 {
            f64::NAN
        } else {
            self.alpha / z
        }
    }
}

impl PartialFit for OnlineBeta {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            if y[i] >= 0.5 {
                self.alpha += 1.0;
            } else {
                self.beta += 1.0;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        if self.alpha <= 0.0 || self.beta <= 0.0 {
            ctx.push(
                Issue::builder(IssueCode::DegenerateDistribution)
                    .message("OnlineBeta posterior hit a zero concentration")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "OnlineBeta α={:.3} β={:.3} mean={after:.6e}",
            self.alpha, self.beta
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Beta concentrations",
                "successes increment α and failures increment β",
                format!("mean={before:.6e}"),
                format!("mean={after:.6e}"),
            ),
        )
    }
}

/// Streaming Gamma via method of moments (river `proba.Gamma`).
#[derive(Clone, Debug, Default)]
pub struct OnlineGamma {
    n: f64,
    mean: f64,
    m2: f64,
    updates: u64,
}

impl OnlineGamma {
    /// Empty Gamma accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Moment shape \(\mu^2 / s^2\), or NaN during warmup.
    pub fn shape(&self) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let var = self.m2 / (self.n - 1.0);
        if var <= 1e-18 {
            f64::NAN
        } else {
            (self.mean * self.mean) / var
        }
    }
}

impl PartialFit for OnlineGamma {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.shape();
        let mut nonpos = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v <= 0.0 {
                nonpos += 1;
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
        }
        if nonpos > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineGamma skipped {nonpos} non-positive observations"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.shape();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0 && after.is_finite();
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineGamma shape={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Gamma moment shape",
                "Welford mean/variance of positive column-0 values",
                format!("shape={before:.6e}"),
                format!("shape={after:.6e}"),
            ),
        )
    }
}

/// Keep a named subset of columns (river `compose.Select`).
///
/// Column count is not identification `p`.
#[derive(Clone, Debug)]
pub struct ColumnSelect {
    /// Columns to keep.
    pub cols: Vec<usize>,
    n_seen: u64,
    updates: u64,
}

impl ColumnSelect {
    /// Keep `cols`.
    pub fn new(cols: Vec<usize>) -> Self {
        Self {
            cols,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl PartialFit for ColumnSelect {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if self.cols.iter().any(|&j| j >= x.ncols()) {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ColumnSelect index is out of range for this batch")
                    .build(),
            );
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 0 && !self.cols.is_empty();
        q.warmup = self.n_seen == 0;
        q.explanation = format!("ColumnSelect kept {} columns", self.cols.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "column mask",
                "stateless projection; partial_fit only records stream length",
                format!("n={}", self.n_seen.saturating_sub(x.nrows() as u64)),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for ColumnSelect {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let cols: Vec<usize> = self
            .cols
            .iter()
            .copied()
            .filter(|&j| j < x.ncols())
            .collect();
        if cols.is_empty() {
            ctx.push(
                Issue::builder(IssueCode::EmptyMatrix)
                    .message("ColumnSelect kept no in-range columns")
                    .build(),
            );
            return ctx.finish(Matrix::zeros(x.nrows(), 0));
        }
        ctx.finish(Matrix::from_fn(x.nrows(), cols.len(), |i, j| {
            x.get(i, cols[j])
        }))
    }
}

/// Drop a named subset of columns (river `compose.Discard`).
///
/// Column count is not identification `p`.
#[derive(Clone, Debug)]
pub struct ColumnDiscard {
    /// Columns to drop.
    pub cols: Vec<usize>,
    n_seen: u64,
    updates: u64,
}

impl ColumnDiscard {
    /// Drop `cols`.
    pub fn new(cols: Vec<usize>) -> Self {
        Self {
            cols,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl PartialFit for ColumnDiscard {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(0.0);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen > 0;
        q.warmup = self.n_seen == 0;
        q.explanation = format!("ColumnDiscard drops {} columns", self.cols.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "column drop mask",
                "stateless projection; partial_fit only records stream length",
                format!("n={}", self.n_seen.saturating_sub(x.nrows() as u64)),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Transform for ColumnDiscard {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        let keep: Vec<usize> = (0..x.ncols()).filter(|j| !self.cols.contains(j)).collect();
        if keep.is_empty() {
            ctx.push(
                Issue::builder(IssueCode::EmptyMatrix)
                    .message("ColumnDiscard dropped every column")
                    .build(),
            );
            return ctx.finish(Matrix::zeros(x.nrows(), 0));
        }
        ctx.finish(Matrix::from_fn(x.nrows(), keep.len(), |i, j| {
            x.get(i, keep[j])
        }))
    }
}

/// FTRL-Proximal linear regressor (river `optim.FTRLProximal`).
#[derive(Clone, Debug)]
pub struct FtrlRegressor {
    /// Learning-rate scale \(\alpha > 0\).
    pub alpha: f64,
    /// Learning-rate offset \(\beta > 0\).
    pub beta: f64,
    /// ℓ1.
    pub l1: f64,
    /// ℓ2.
    pub l2: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    z: Vector,
    n: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FtrlRegressor {
    fn default() -> Self {
        Self {
            alpha: 0.5,
            beta: 1.0,
            l1: 0.0,
            l2: 0.0,
            fit_intercept: true,
            z: Vector::zeros(0),
            n: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FtrlRegressor {
    /// Default FTRL regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn weights(&self) -> Vector {
        let dim = self.z.len();
        let mut w = Vector::zeros(dim);
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 {
            self.alpha
        } else {
            0.5
        };
        let beta = if self.beta.is_finite() && self.beta > 0.0 {
            self.beta
        } else {
            1.0
        };
        for j in 0..dim {
            if self.z[j].abs() <= self.l1 {
                w[j] = 0.0;
            } else {
                let sign = if self.z[j] < 0.0 { -1.0 } else { 1.0 };
                w[j] =
                    -(self.z[j] - sign * self.l1) / ((beta + self.n[j].sqrt()) / alpha + self.l2);
            }
        }
        w
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let w = self.weights();
        let n = z.len().min(w.len());
        let mut s = 0.0;
        for j in 0..n {
            s += w[j] * z[j];
        }
        s
    }
}

impl PartialFit for FtrlRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.z = Vector::zeros(dim);
            self.n = Vector::zeros(dim);
            self.initialized = true;
        } else if self.z.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.weights();
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 {
            self.alpha
        } else {
            0.5
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let xa = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            let w = self.weights();
            for j in 0..dim {
                let g = err * xa[j];
                let sigma = (self.n[j] + g * g).sqrt() / alpha - self.n[j].sqrt() / alpha;
                self.z[j] += g - sigma * w[j];
                self.n[j] += g * g;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after_w = self.weights();
        let delta = after_w.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "FTRL-Proximal weights",
            "accumulated z and n define a proximal closed form at each row",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for FtrlRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// FTRL-Proximal logistic classifier (river `optim.FTRL` / Follow-the-Regularized-Leader).
///
/// Gradient is \((\sigma(w^\top x)-y)x\). Feature count is identification
/// width, not a substitute `p` from class count.
#[derive(Clone, Debug)]
pub struct FtrlClassifier {
    /// Learning-rate scale \(\alpha > 0\).
    pub alpha: f64,
    /// Learning-rate offset \(\beta > 0\).
    pub beta: f64,
    /// ℓ1.
    pub l1: f64,
    /// ℓ2.
    pub l2: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    z: Vector,
    n: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FtrlClassifier {
    fn default() -> Self {
        Self {
            alpha: 0.5,
            beta: 1.0,
            l1: 0.0,
            l2: 0.0,
            fit_intercept: true,
            z: Vector::zeros(0),
            n: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FtrlClassifier {
    /// Default FTRL classifier.
    pub fn new() -> Self {
        Self::default()
    }

    fn weights(&self) -> Vector {
        let dim = self.z.len();
        let mut w = Vector::zeros(dim);
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 {
            self.alpha
        } else {
            0.5
        };
        let beta = if self.beta.is_finite() && self.beta > 0.0 {
            self.beta
        } else {
            1.0
        };
        for j in 0..dim {
            if self.z[j].abs() <= self.l1 {
                w[j] = 0.0;
            } else {
                let sign = if self.z[j] < 0.0 { -1.0 } else { 1.0 };
                w[j] =
                    -(self.z[j] - sign * self.l1) / ((beta + self.n[j].sqrt()) / alpha + self.l2);
            }
        }
        w
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let w = self.weights();
        let n = z.len().min(w.len());
        let mut s = 0.0_f64;
        for j in 0..n {
            s += w[j] * z[j];
        }
        sigmoid(s)
    }
}

impl PartialFit for FtrlClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.z = Vector::zeros(dim);
            self.n = Vector::zeros(dim);
            self.initialized = true;
        } else if self.z.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.weights();
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 {
            self.alpha
        } else {
            0.5
        };
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yi = if y[i] > 0.5 { 1.0 } else { 0.0 };
            let xa = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i).clamp(1e-12, 1.0 - 1e-12);
            loss_before += -(yi * pred.ln() + (1.0 - yi) * (1.0 - pred).ln());
            let w = self.weights();
            let gbase = pred - yi;
            for j in 0..dim {
                let g = gbase * xa[j];
                let sigma = (self.n[j] + g * g).sqrt() / alpha - self.n[j].sqrt() / alpha;
                self.z[j] += g - sigma * w[j];
                self.n[j] += g * g;
            }
            let after = self.predict_row(x, i).clamp(1e-12, 1.0 - 1e-12);
            loss_after += -(yi * after.ln() + (1.0 - yi) * (1.0 - after).ln());
            self.n_seen += 1;
        }
        self.updates += 1;
        let after_w = self.weights();
        let delta = after_w.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "FTRL-Proximal logistic weights",
            "log-loss gradient; class count is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for FtrlClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::filled(x.nrows(), 0.5));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// AdaDelta linear regressor (river `optim.AdaDelta`).
#[derive(Clone, Debug)]
pub struct AdaDeltaRegressor {
    /// Second-moment decay \(\rho\).
    pub rho: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    eg2: Vector,
    edx2: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaDeltaRegressor {
    fn default() -> Self {
        Self {
            rho: 0.95,
            eps: 1e-6,
            fit_intercept: true,
            coef: Vector::zeros(0),
            eg2: Vector::zeros(0),
            edx2: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaDeltaRegressor {
    /// Default AdaDelta regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdaDeltaRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.eg2 = Vector::zeros(dim);
            self.edx2 = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let rho = self.rho.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-6
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.eg2[j] = rho * self.eg2[j] + (1.0 - rho) * g * g;
                let dx = -((self.edx2[j] + eps).sqrt() / (self.eg2[j] + eps).sqrt()) * g;
                self.coef[j] += dx;
                self.edx2[j] = rho * self.edx2[j] + (1.0 - rho) * dx * dx;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdaDelta linear weights",
            "RMS of updates over RMS of gradients removes a global learning rate",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AdaDeltaRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Heavy-ball momentum linear regressor (river `optim.Momentum`).
#[derive(Clone, Debug)]
pub struct MomentumRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Momentum \(\mu \in [0, 1)\).
    pub mu: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    vel: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for MomentumRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            mu: 0.9,
            fit_intercept: true,
            coef: Vector::zeros(0),
            vel: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl MomentumRegressor {
    /// Default momentum regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for MomentumRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.vel = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mu = self.mu.clamp(0.0, 0.999);
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.vel[j] = mu * self.vel[j] - eta * g;
                self.coef[j] += self.vel[j];
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "momentum linear weights",
            "velocity carries a fraction of the previous residual step",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for MomentumRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Streaming Poisson rate (river `proba.Poisson`).
#[derive(Clone, Debug, Default)]
pub struct OnlinePoisson {
    n: f64,
    mean: f64,
    updates: u64,
}

impl OnlinePoisson {
    /// Empty Poisson accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current rate \(\lambda\), or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.n <= 0.0 {
            f64::NAN
        } else {
            self.mean
        }
    }
}

impl PartialFit for OnlinePoisson {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.score();
        let mut neg = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v < 0.0 {
                neg += 1;
                continue;
            }
            self.n += 1.0;
            self.mean += (v - self.mean) / self.n;
        }
        if neg > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!("OnlinePoisson skipped {neg} negative counts"))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.score();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1.0;
        q.warmup = self.n < 1.0;
        q.explanation = format!("OnlinePoisson λ={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Poisson rate",
                "running mean of non-negative column-0 counts",
                format!("lambda={before:.6e}"),
                format!("lambda={after:.6e}"),
            ),
        )
    }
}

/// Nesterov accelerated gradient linear regressor (river `optim.NesterovMomentum`).
#[derive(Clone, Debug)]
pub struct NesterovRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Momentum \(\mu\).
    pub mu: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    vel: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for NesterovRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            mu: 0.9,
            fit_intercept: true,
            coef: Vector::zeros(0),
            vel: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl NesterovRegressor {
    /// Default Nesterov regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_with(&self, x: &Matrix, i: usize, w: &Vector) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(w.len());
        let mut s = 0.0;
        for j in 0..n {
            s += w[j] * z[j];
        }
        s
    }
}

impl PartialFit for NesterovRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.vel = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mu = self.mu.clamp(0.0, 0.999);
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let look = Vector::from_iter((0..dim).map(|j| self.coef[j] + mu * self.vel[j]));
            let pred = self.predict_with(x, i, &look);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.vel[j] = mu * self.vel[j] - eta * g;
                self.coef[j] += self.vel[j];
            }
            let after = self.predict_with(x, i, &self.coef);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Nesterov linear weights",
            "the residual gradient is evaluated at the look-ahead point w+μv",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for NesterovRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_with(x, i, &self.coef)),
        ))
    }
}

/// Streaming Dirichlet concentrations (river `proba.Multinomial` / Dirichlet).
///
/// Category count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineDirichlet {
    alpha: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineDirichlet {
    fn default() -> Self {
        Self {
            alpha: vec![1.0, 1.0],
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineDirichlet {
    /// Symmetric Dirichlet prior over `k` categories.
    pub fn new(k: usize) -> Self {
        Self {
            alpha: vec![1.0; k.max(2)],
            n_seen: 0,
            updates: 0,
        }
    }

    /// Posterior mean of category 1, or NaN when empty.
    pub fn score(&self) -> f64 {
        let z: f64 = self.alpha.iter().sum();
        if z <= 0.0 || self.alpha.len() < 2 {
            f64::NAN
        } else {
            self.alpha[1] / z
        }
    }
}

impl PartialFit for OnlineDirichlet {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.score();
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let k = y[i].round().max(0.0) as usize;
            while self.alpha.len() <= k {
                self.alpha.push(1.0);
            }
            self.alpha[k] += 1.0;
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 1;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineDirichlet K={} mean1={after:.6e}", self.alpha.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Dirichlet concentrations",
                "integer labels increment the matching concentration",
                format!("mean1={before:.6e}"),
                format!("mean1={after:.6e}"),
            ),
        )
    }
}

/// Implicit-feedback matrix factorization (river `reco.BiasedMF` implicit).
///
/// Factor count is not identification `p`. `X` is `[user, item]`.
#[derive(Clone, Debug)]
pub struct ImplicitMf {
    inner: FunkMf,
    /// Confidence scale on the implicit residual.
    pub confidence: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for ImplicitMf {
    fn default() -> Self {
        Self {
            inner: FunkMf::new(2),
            confidence: 1.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ImplicitMf {
    /// Implicit MF with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            inner: FunkMf::new(n_factors),
            ..Self::default()
        }
    }
}

impl PartialFit for ImplicitMf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let c = if self.confidence.is_finite() && self.confidence > 0.0 {
            self.confidence
        } else {
            1.0
        };
        let yw = Vector::from_iter((0..y.len()).map(|i| {
            let r = y[i];
            if r.is_finite() && r > 0.0 {
                r * (1.0 + c)
            } else {
                0.0
            }
        }));
        let _ = self
            .inner
            .partial_fit(x, Some(&yw), &session.child("implicit"));
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(x.nrows() as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("ImplicitMf confidence={c:.3} on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "implicit MF factors",
                "positive feedback is up-weighted and passed to FunkMF SGD",
                "previous factors",
                "updated factors",
            ),
        )
    }
}

/// Bayesian Personalized Ranking (river `reco.BPR`).
///
/// Factor count is not identification `p`. `X` is `[user, item]`; each row is
/// a positive pair. A negative item is sampled uniformly from items seen so far.
#[derive(Clone, Debug)]
pub struct Bpr {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// ℓ2 on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    rng: Rng,
    n_seen: u64,
    updates: u64,
}

impl Default for Bpr {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            rng: Rng::new(17),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Bpr {
    /// BPR with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for Bpr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BPR needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for r in 0..x.nrows() {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let n_items = self.qi.len();
            if n_items < 2 {
                continue;
            }
            let mut j = self.rng.below(n_items);
            if j == i {
                j = (j + 1) % n_items;
            }
            let xui = self.score(u, i) - self.score(u, j);
            let sig = sigmoid(xui);
            let err = 1.0 - sig;
            for f in 0..self.n_factors {
                let pu = self.pu[u][f];
                let qi = self.qi[i][f];
                let qj = self.qi[j][f];
                self.pu[u][f] += lr * (err * (qi - qj) - l2 * pu);
                self.qi[i][f] += lr * (err * pu - l2 * qi);
                self.qi[j][f] += lr * (-err * pu - l2 * qj);
                dsum += (self.pu[u][f] - pu).abs()
                    + (self.qi[i][f] - qi).abs()
                    + (self.qi[j][f] - qj).abs();
            }
            pairs += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("BPR has fewer than two items; no negative was sampled")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "BPR pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "BPR pairwise ranking step",
                "sigmoid(s_ui-s_uj) SGD; factor count is not identification p",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for Bpr {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BPR predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        }));
        ctx.finish(out)
    }
}

/// Item-item kNN recommender (river `reco.ItemKNN`).
///
/// `X` is `[user, item]`, `y` is the rating. Neighbourhood size is not
/// identification `p`. Similarity is cosine of item rating vectors over users.
#[derive(Clone, Debug)]
pub struct ItemKnn {
    /// Neighbours kept per query item. Not identification `p`.
    pub k: usize,
    ratings: HashMap<(u64, u64), f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for ItemKnn {
    fn default() -> Self {
        Self {
            k: 8,
            ratings: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ItemKnn {
    /// Empty item-item neighbourhood.
    pub fn new() -> Self {
        Self::default()
    }

    fn item_vec(&self, item: u64) -> HashMap<u64, f64> {
        let mut v = HashMap::new();
        for (&(u, i), &r) in &self.ratings {
            if i == item {
                v.insert(u, r);
            }
        }
        v
    }

    fn user_items(&self, user: u64) -> Vec<(u64, f64)> {
        self.ratings
            .iter()
            .filter_map(|(&(u, i), &r)| if u == user { Some((i, r)) } else { None })
            .collect()
    }

    fn pred(&self, user: u64, item: u64) -> f64 {
        let target = self.item_vec(item);
        let rated = self.user_items(user);
        if rated.is_empty() {
            return self
                .ratings
                .values()
                .copied()
                .sum::<f64>()
                / (self.ratings.len().max(1) as f64);
        }
        let mut scored: Vec<(f64, f64)> = Vec::new();
        for (j, rj) in rated {
            if j == item {
                continue;
            }
            let other = self.item_vec(j);
            let sim = cosine_sparse(&target, &other);
            if sim.abs() > 1e-15 {
                scored.push((sim, rj));
            }
        }
        scored.sort_by(|a, b| b.0.abs().partial_cmp(&a.0.abs()).unwrap_or(std::cmp::Ordering::Equal));
        let keep = self.k.max(1);
        let mut num = 0.0_f64;
        let mut den = 0.0_f64;
        for (sim, rj) in scored.into_iter().take(keep) {
            num += sim * rj;
            den += sim.abs();
        }
        if den > 1e-15 {
            num / den
        } else {
            self.ratings.values().copied().sum::<f64>() / (self.ratings.len().max(1) as f64)
        }
    }
}

fn cosine_sparse(a: &HashMap<u64, f64>, b: &HashMap<u64, f64>) -> f64 {
    let mut dot = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for (&u, &ra) in a {
        na += ra * ra;
        if let Some(&rb) = b.get(&u) {
            dot += ra * rb;
        }
    }
    for &rb in b.values() {
        nb += rb * rb;
    }
    let den = na.sqrt() * nb.sqrt();
    if den <= 1e-15 {
        0.0
    } else {
        dot / den
    }
}

impl PartialFit for ItemKnn {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ItemKNN needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let mut stored = 0u64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.ratings.insert((u, i), y[r]);
            stored += 1;
        }
        self.n_seen += stored;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(stored as f64);
        q.information_gain = Some(stored as f64);
        q.still_identified = self.ratings.len() >= 2;
        q.warmup = self.ratings.len() < 2;
        q.explanation = format!(
            "ItemKNN stored={stored} cells={} k={}",
            self.ratings.len(),
            self.k
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("ItemKNN has fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "item-item rating table update",
                "cosine neighbourhood; neighbour count is not identification p",
                "previous user-item cells",
                format!("cells={}", self.ratings.len()),
            ),
        )
    }
}

impl Predict for ItemKnn {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ItemKNN predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// User-user kNN recommender (river `reco.UserKNN`).
///
/// `X` is `[user, item]`, `y` is the rating. Neighbourhood size is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct UserKnn {
    /// Neighbours kept per query user. Not identification `p`.
    pub k: usize,
    ratings: HashMap<(u64, u64), f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for UserKnn {
    fn default() -> Self {
        Self {
            k: 8,
            ratings: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl UserKnn {
    /// Empty user-user neighbourhood.
    pub fn new() -> Self {
        Self::default()
    }

    fn user_vec(&self, user: u64) -> HashMap<u64, f64> {
        let mut v = HashMap::new();
        for (&(u, i), &r) in &self.ratings {
            if u == user {
                v.insert(i, r);
            }
        }
        v
    }

    fn item_raters(&self, item: u64) -> Vec<(u64, f64)> {
        self.ratings
            .iter()
            .filter_map(|(&(u, i), &r)| if i == item { Some((u, r)) } else { None })
            .collect()
    }

    fn pred(&self, user: u64, item: u64) -> f64 {
        let target = self.user_vec(user);
        let raters = self.item_raters(item);
        if raters.is_empty() {
            return self
                .ratings
                .values()
                .copied()
                .sum::<f64>()
                / (self.ratings.len().max(1) as f64);
        }
        let mut scored: Vec<(f64, f64)> = Vec::new();
        for (v, rv) in raters {
            if v == user {
                continue;
            }
            let other = self.user_vec(v);
            let sim = cosine_sparse(&target, &other);
            if sim.abs() > 1e-15 {
                scored.push((sim, rv));
            }
        }
        scored.sort_by(|a, b| b.0.abs().partial_cmp(&a.0.abs()).unwrap_or(std::cmp::Ordering::Equal));
        let keep = self.k.max(1);
        let mut num = 0.0_f64;
        let mut den = 0.0_f64;
        for (sim, rv) in scored.into_iter().take(keep) {
            num += sim * rv;
            den += sim.abs();
        }
        if den > 1e-15 {
            num / den
        } else {
            self.ratings.values().copied().sum::<f64>() / (self.ratings.len().max(1) as f64)
        }
    }
}

impl PartialFit for UserKnn {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("UserKNN needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let mut stored = 0u64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.ratings.insert((u, i), y[r]);
            stored += 1;
        }
        self.n_seen += stored;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(stored as f64);
        q.information_gain = Some(stored as f64);
        q.still_identified = self.ratings.len() >= 2;
        q.warmup = self.ratings.len() < 2;
        q.explanation = format!(
            "UserKNN stored={stored} cells={} k={}",
            self.ratings.len(),
            self.k
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("UserKNN has fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "user-user rating table update",
                "cosine neighbourhood; neighbour count is not identification p",
                "previous user-item cells",
                format!("cells={}", self.ratings.len()),
            ),
        )
    }
}

impl Predict for UserKnn {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("UserKNN predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// SVD++ / implicit-feedback MF (river `reco.SVDPP`).
///
/// \( \hat y_{ui}=\mu+b_u+b_i+q_i^\top\bigl(p_u+|N(u)|^{-1/2}\sum_{j\in N(u)}y_j\bigr) \).
/// Factor count is not identification `p`. `X` is `[user, item]`.
#[derive(Clone, Debug)]
pub struct SvdPp {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// ℓ2 on factors and biases.
    pub l2: f64,
    mu: f64,
    bu: Vec<f64>,
    bi: Vec<f64>,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    yj: Vec<Vec<f64>>,
    nu: Vec<Vec<usize>>,
    n_seen: u64,
    updates: u64,
}

impl Default for SvdPp {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            mu: 0.0,
            bu: Vec::new(),
            bi: Vec::new(),
            pu: Vec::new(),
            qi: Vec::new(),
            yj: Vec::new(),
            nu: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SvdPp {
    /// SVD++ with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.bu.len() <= u {
            self.bu.push(0.0);
            self.pu.push(vec![0.01; k]);
            self.nu.push(Vec::new());
        }
        while self.bi.len() <= i {
            self.bi.push(0.0);
            self.qi.push(vec![0.01; k]);
            self.yj.push(vec![0.01; k]);
        }
        if !self.nu[u].contains(&i) {
            self.nu[u].push(i);
        }
    }

    fn implicit(&self, u: usize) -> Vec<f64> {
        let k = self.n_factors.max(1);
        let mut acc = vec![0.0_f64; k];
        if u >= self.nu.len() {
            return acc;
        }
        let n = self.nu[u].len();
        if n == 0 {
            return acc;
        }
        let scale = 1.0 / (n as f64).sqrt();
        for &j in &self.nu[u] {
            if j < self.yj.len() {
                for f in 0..k.min(self.yj[j].len()) {
                    acc[f] += self.yj[j][f] * scale;
                }
            }
        }
        acc
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = self.mu;
        if u < self.bu.len() {
            s += self.bu[u];
        }
        if i < self.bi.len() {
            s += self.bi[i];
        }
        let imp = self.implicit(u);
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.qi[i][f] * (self.pu[u][f] + imp.get(f).copied().unwrap_or(0.0));
            }
        }
        s
    }
}

impl PartialFit for SvdPp {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SVD++ needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut sse = 0.0_f64;
        let mut dsum = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let pred = self.pred(u, i);
            let e = y[r] - pred;
            sse += e * e;
            let n_imp = self.nu[u].len().max(1);
            let scale = 1.0 / (n_imp as f64).sqrt();
            let imp = self.implicit(u);
            self.mu += lr * e;
            self.bu[u] += lr * (e - l2 * self.bu[u]);
            self.bi[i] += lr * (e - l2 * self.bi[i]);
            for f in 0..self.n_factors {
                let pu = self.pu[u][f];
                let qi = self.qi[i][f];
                let im = imp.get(f).copied().unwrap_or(0.0);
                self.pu[u][f] += lr * (e * qi - l2 * pu);
                self.qi[i][f] += lr * (e * (pu + im) - l2 * qi);
                dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                let items = self.nu[u].clone();
                for j in items {
                    let yold = self.yj[j][f];
                    self.yj[j][f] += lr * (e * qi * scale - l2 * yold);
                    dsum += (self.yj[j][f] - yold).abs();
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 8;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.loss_before = Some(sse);
        q.loss_after = Some(sse);
        q.information_gain = Some(dsum);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "SVD++ SGD ||Δ||₁={dsum:.4e} SSE={sse:.4e} users={} items={}",
            self.bu.len(),
            self.bi.len()
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SVD++ is still warming up")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SVD++ biased MF + implicit item set",
                "SGD on μ, b, p, q, y; factor count is not identification p",
                "pre-batch factors",
                format!("μ={:.4e} SSE={sse:.4e}", self.mu),
            ),
        )
    }
}

impl Predict for SvdPp {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SVD++ predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.mu));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// Slope One item-item recommender (river `reco.SlopeOne`).
///
/// `X` is `[user, item]`, `y` is the rating. Deviation tables are not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct SlopeOne {
    ratings: HashMap<(u64, u64), f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for SlopeOne {
    fn default() -> Self {
        Self {
            ratings: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SlopeOne {
    /// Empty Slope One table.
    pub fn new() -> Self {
        Self::default()
    }

    fn pred(&self, user: u64, item: u64) -> f64 {
        let user_rs: Vec<(u64, f64)> = self
            .ratings
            .iter()
            .filter_map(|(&(u, i), &r)| if u == user && i != item { Some((i, r)) } else { None })
            .collect();
        if user_rs.is_empty() {
            return self.ratings.values().copied().sum::<f64>()
                / (self.ratings.len().max(1) as f64);
        }
        let mut num = 0.0_f64;
        let mut den = 0.0_f64;
        for (j, rj) in user_rs {
            let mut dsum = 0.0_f64;
            let mut dn = 0.0_f64;
            for (&(v, i), &rvi) in &self.ratings {
                if i != item {
                    continue;
                }
                if let Some(&rvj) = self.ratings.get(&(v, j)) {
                    dsum += rvi - rvj;
                    dn += 1.0;
                }
            }
            if dn > 0.0 {
                num += rj + dsum / dn;
                den += 1.0;
            }
        }
        if den > 0.0 {
            num / den
        } else {
            self.ratings.values().copied().sum::<f64>() / (self.ratings.len().max(1) as f64)
        }
    }
}

impl PartialFit for SlopeOne {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SlopeOne needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let mut stored = 0u64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.ratings.insert((u, i), y[r]);
            stored += 1;
        }
        self.n_seen += stored;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(stored as f64);
        q.information_gain = Some(stored as f64);
        q.still_identified = self.ratings.len() >= 2;
        q.warmup = self.ratings.len() < 2;
        q.explanation = format!("SlopeOne stored={stored} cells={}", self.ratings.len());
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SlopeOne has fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Slope One rating-table update",
                "pairwise item deviations; item count is not identification p",
                "previous cells",
                format!("cells={}", self.ratings.len()),
            ),
        )
    }
}

impl Predict for SlopeOne {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SlopeOne predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// Surprise / river `NormalPredictor`: rating mean (not a draw).
///
/// Predicts \(\mu\); the stored variance is only for the explanation.
#[derive(Clone, Debug)]
pub struct NormalPredictor {
    mean: f64,
    m2: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for NormalPredictor {
    fn default() -> Self {
        Self {
            mean: 0.0,
            m2: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl NormalPredictor {
    /// Empty Gaussian rating model.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for NormalPredictor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let before = self.mean;
        for i in 0..y.len() {
            if !y[i].is_finite() {
                continue;
            }
            self.n_seen += 1;
            let d = y[i] - self.mean;
            self.mean += d / self.n_seen as f64;
            self.m2 += d * (y[i] - self.mean);
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.mean - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("NormalPredictor μ={:.4e} n={}", self.mean, self.n_seen);
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("NormalPredictor has fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Gaussian rating mean",
                "Welford μ; predict returns μ, not a Normal draw",
                format!("μ={before:.4e}"),
                format!("μ={:.4e}", self.mean),
            ),
        )
    }
}

impl Predict for NormalPredictor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::filled(x.nrows(), self.mean))
    }
}

/// Surprise `BaselineOnly`: \(\mu+b_u+b_i\) SGD, no latent factors.
///
/// `X` is `[user, item]`. Factor count is not identification `p` (there are none).
#[derive(Clone, Debug)]
pub struct BaselineOnly {
    /// SGD step.
    pub learning_rate: f64,
    /// ℓ2 on biases.
    pub l2: f64,
    mu: f64,
    bu: Vec<f64>,
    bi: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for BaselineOnly {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            l2: 0.01,
            mu: 0.0,
            bu: Vec::new(),
            bi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl BaselineOnly {
    /// Empty user/item bias model.
    pub fn new() -> Self {
        Self::default()
    }

    fn grow(&mut self, u: usize, i: usize) {
        while self.bu.len() <= u {
            self.bu.push(0.0);
        }
        while self.bi.len() <= i {
            self.bi.push(0.0);
        }
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = self.mu;
        if u < self.bu.len() {
            s += self.bu[u];
        }
        if i < self.bi.len() {
            s += self.bi[i];
        }
        s
    }
}

impl PartialFit for BaselineOnly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BaselineOnly needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut sse = 0.0_f64;
        let mut dsum = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let pred = self.pred(u, i);
            let e = y[r] - pred;
            sse += e * e;
            self.mu += lr * e;
            let bu = self.bu[u];
            let bi = self.bi[i];
            self.bu[u] += lr * (e - l2 * bu);
            self.bi[i] += lr * (e - l2 * bi);
            dsum += (self.bu[u] - bu).abs() + (self.bi[i] - bi).abs();
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 4;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.loss_after = Some(sse);
        q.information_gain = Some(dsum);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!("BaselineOnly μ={:.4e} SSE={sse:.4e}", self.mu);
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("BaselineOnly is still warming up")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SGD on μ, b_u, b_i",
                "no latent factors; user/item cardinalities are not p",
                "pre-batch biases",
                format!("μ={:.4e}", self.mu),
            ),
        )
    }
}

impl Predict for BaselineOnly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("BaselineOnly predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.mu));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// Hash co-clustering recommender (surprise `CoClustering` lite).
///
/// User/item cluster counts are not identification `p`. Clusters are
/// `user % k` / `item % k`; cell means are Welford-updated.
#[derive(Clone, Debug)]
pub struct CoClustering {
    /// User clusters. Not identification `p`.
    pub n_user_clusters: usize,
    /// Item clusters. Not identification `p`.
    pub n_item_clusters: usize,
    cells: HashMap<(u64, u64), (f64, u64)>,
    global: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for CoClustering {
    fn default() -> Self {
        Self {
            n_user_clusters: 4,
            n_item_clusters: 4,
            cells: HashMap::new(),
            global: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl CoClustering {
    /// Default 4×4 hash co-clustering.
    pub fn new() -> Self {
        Self::default()
    }

    fn pred(&self, user: u64, item: u64) -> f64 {
        let ku = self.n_user_clusters.max(1) as u64;
        let ki = self.n_item_clusters.max(1) as u64;
        let uc = user % ku;
        let ic = item % ki;
        self.cells
            .get(&(uc, ic))
            .map(|(m, _)| *m)
            .unwrap_or(self.global)
    }
}

impl PartialFit for CoClustering {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("CoClustering needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let ku = self.n_user_clusters.max(1) as u64;
        let ki = self.n_item_clusters.max(1) as u64;
        let mut moved = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as u64 % ku;
            let i = x.get(r, 1).max(0.0).round() as u64 % ki;
            let e = self.cells.entry((u, i)).or_insert((0.0, 0));
            e.1 += 1;
            let before = e.0;
            e.0 += (y[r] - e.0) / e.1 as f64;
            moved += (e.0 - before).abs();
            self.n_seen += 1;
            self.global += (y[r] - self.global) / self.n_seen as f64;
        }
        self.updates += 1;
        ctx.push(
            Issue::builder(IssueCode::CausalClaimUnidentified)
                .severity(Severity::Advisory)
                .message("CoClustering is hash clustering, not Bregman co-clustering")
                .compromise(NumericalCompromise::new(
                    "Bregman co-clustering of the rating matrix",
                    "user%k / item%k cell means",
                    "cluster assignments ignore rating structure",
                    "read as a hashed block mean, not surprise CoClustering",
                ))
                .build(),
        );
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "CoClustering cells={} μ={:.4e}",
            self.cells.len(),
            self.global
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("CoClustering has fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "hash co-cluster cell-mean update",
                "cluster counts are not identification p",
                "previous cell means",
                format!("cells={}", self.cells.len()),
            ),
        )
    }
}

impl Predict for CoClustering {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("CoClustering predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.global));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as u64;
            self.pred(u, i)
        }));
        ctx.finish(out)
    }
}

/// Streaming Ben-Haim / Tom-Tov histogram (river `sketch.Histogram`).
///
/// Bin count is not identification `p`. Closest adjacent bins are merged when
/// the capacity is exceeded.
#[derive(Clone, Debug)]
pub struct OnlineHistogram {
    /// Maximum stored bins. Not identification `p`.
    pub max_bins: usize,
    bins: Vec<(f64, f64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineHistogram {
    fn default() -> Self {
        Self::new(8)
    }
}

impl OnlineHistogram {
    /// Empty histogram with at most `max_bins` centroids.
    pub fn new(max_bins: usize) -> Self {
        Self {
            max_bins: max_bins.max(1),
            bins: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }

    /// Current `(center, count)` bins, left to right.
    pub fn bins(&self) -> &[(f64, f64)] {
        &self.bins
    }

    fn insert(&mut self, v: f64) {
        if !v.is_finite() {
            return;
        }
        if let Some(i) = self
            .bins
            .iter()
            .position(|(c, _)| (*c - v).abs() <= 1e-15)
        {
            self.bins[i].1 += 1.0;
        } else {
            self.bins.push((v, 1.0));
            self.bins
                .sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        }
        while self.bins.len() > self.max_bins {
            let mut best = 0usize;
            let mut best_d = f64::INFINITY;
            for i in 0..self.bins.len().saturating_sub(1) {
                let d = (self.bins[i + 1].0 - self.bins[i].0).abs();
                if d < best_d {
                    best_d = d;
                    best = i;
                }
            }
            let (c1, n1) = self.bins[best];
            let (c2, n2) = self.bins[best + 1];
            let n = n1 + n2;
            let c = if n > 0.0 { (c1 * n1 + c2 * n2) / n } else { c1 };
            self.bins[best] = (c, n);
            self.bins.remove(best + 1);
        }
    }
}

impl PartialFit for OnlineHistogram {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let before = self.bins.len();
        for i in 0..x.nrows() {
            self.insert(x.get(i, 0));
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let after = self.bins.len();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = !self.bins.is_empty();
        q.warmup = self.n_seen < 2;
        q.explanation = format!("OnlineHistogram bins={after}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("OnlineHistogram has fewer than two observations")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Ben-Haim/Tom-Tov bin update",
                "bin count is not identification p",
                format!("bins={before}"),
                format!("bins={after} n={}", self.n_seen),
            ),
        )
    }
}

/// Streaming Weibull via Gumbel moments on \(\log x\) (river-style `proba`).
///
/// Shape \(\pi/(s\sqrt{6})\) from the log-scale sample sd. Shape is not
/// identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineWeibull {
    n: f64,
    mean_log: f64,
    m2_log: f64,
    updates: u64,
}

impl OnlineWeibull {
    /// Empty Weibull accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Gumbel-moment shape, or NaN during warmup.
    pub fn shape(&self) -> f64 {
        if self.n < 2.0 {
            return f64::NAN;
        }
        let var = self.m2_log / (self.n - 1.0);
        let s = var.max(0.0).sqrt().max(1e-6);
        (std::f64::consts::PI / (s * 6.0_f64.sqrt())).clamp(0.2, 5.0)
    }
}

impl PartialFit for OnlineWeibull {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.shape();
        let mut nonpos = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v <= 0.0 {
                nonpos += 1;
                continue;
            }
            let z = v.ln();
            self.n += 1.0;
            let d = z - self.mean_log;
            self.mean_log += d / self.n;
            self.m2_log += d * (z - self.mean_log);
        }
        if nonpos > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineWeibull skipped {nonpos} non-positive observations"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.shape();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0 && after.is_finite();
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlineWeibull shape={after:.6e}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("OnlineWeibull needs two positive observations")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Weibull Gumbel-moment shape",
                "Welford mean/variance of log(column-0) positives",
                format!("shape={before:.6e}"),
                format!("shape={after:.6e}"),
            ),
        )
    }
}

/// Streaming exponential rate \(1/\bar x\) on positive column 0.
///
/// Rate is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineExponential {
    n: f64,
    sum: f64,
    updates: u64,
}

impl OnlineExponential {
    /// Empty exponential accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// MLE rate, or NaN during warmup.
    pub fn rate(&self) -> f64 {
        if self.n < 1.0 || self.sum <= 0.0 {
            f64::NAN
        } else {
            self.n / self.sum
        }
    }
}

impl PartialFit for OnlineExponential {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.rate();
        let mut nonpos = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v <= 0.0 {
                nonpos += 1;
                continue;
            }
            self.n += 1.0;
            self.sum += v;
        }
        if nonpos > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineExponential skipped {nonpos} non-positive observations"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.rate();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1.0 && after.is_finite();
        q.warmup = self.n < 1.0;
        q.explanation = format!("OnlineExponential rate={after:.6e}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("OnlineExponential needs one positive observation")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "exponential rate 1/mean",
                "positive column-0 values only",
                format!("rate={before:.6e}"),
                format!("rate={after:.6e}"),
            ),
        )
    }
}

/// Streaming log-normal via Welford on \(\log x\) (river-style `proba`).
///
/// Log-mean is not identification `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlineLogNormal {
    n: f64,
    mean_log: f64,
    m2_log: f64,
    updates: u64,
}

impl OnlineLogNormal {
    /// Empty log-normal accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Log-scale mean, or NaN during warmup.
    pub fn mu(&self) -> f64 {
        if self.n < 1.0 {
            f64::NAN
        } else {
            self.mean_log
        }
    }
}

impl PartialFit for OnlineLogNormal {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.mu();
        let mut nonpos = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v <= 0.0 {
                nonpos += 1;
                continue;
            }
            let z = v.ln();
            self.n += 1.0;
            let d = z - self.mean_log;
            self.mean_log += d / self.n;
            self.m2_log += d * (z - self.mean_log);
        }
        if nonpos > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineLogNormal skipped {nonpos} non-positive observations"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.mu();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1.0 && after.is_finite();
        q.warmup = self.n < 1.0;
        q.explanation = format!("OnlineLogNormal μ={after:.6e}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("OnlineLogNormal needs one positive observation")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-normal Welford on log x",
                "positive column-0 values only",
                format!("mu={before:.6e}"),
                format!("mu={after:.6e}"),
            ),
        )
    }
}

/// Streaming Pareto Hill estimator on positive column 0 (river-style `proba`).
///
/// \(\hat\alpha = n / \sum\log(x_i/\hat x_{\min})\). Shape is not identification
/// `p`.
#[derive(Clone, Debug, Default)]
pub struct OnlinePareto {
    n: f64,
    sum_log: f64,
    xmin: f64,
    updates: u64,
}

impl OnlinePareto {
    /// Empty Pareto accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Hill shape, or NaN during warmup.
    pub fn alpha(&self) -> f64 {
        if self.n < 1.0 || !self.xmin.is_finite() || self.xmin <= 0.0 {
            return f64::NAN;
        }
        let den = self.sum_log - self.n * self.xmin.ln();
        if den <= 1e-18 {
            f64::NAN
        } else {
            self.n / den
        }
    }
}

impl PartialFit for OnlinePareto {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.alpha();
        let mut nonpos = 0u64;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            if v <= 0.0 {
                nonpos += 1;
                continue;
            }
            if self.n <= 0.0 || v < self.xmin {
                self.xmin = v;
            }
            self.n += 1.0;
            self.sum_log += v.ln();
        }
        if nonpos > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlinePareto skipped {nonpos} non-positive observations"
                    ))
                    .build(),
            );
        }
        self.updates += 1;
        let after = self.alpha();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0 && after.is_finite();
        q.warmup = self.n < 2.0;
        q.explanation = format!("OnlinePareto α={after:.6e}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("OnlinePareto needs two positive observations")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Pareto Hill shape",
                "running xmin and sum of log positives; shape is not p",
                format!("alpha={before:.6e}"),
                format!("alpha={after:.6e}"),
            ),
        )
    }
}

/// AdaMax linear regressor (river `optim.AdaMax`).
///
/// Infinity-norm second moment: \(u \leftarrow \max(\beta_2 u, |g|)\).
#[derive(Clone, Debug)]
pub struct AdaMaxRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Infinity-norm decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    u: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaMaxRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            u: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaMaxRegressor {
    /// Default AdaMax regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdaMaxRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.u = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                let ug: f64 = g.abs();
                self.u[j] = (b2 * self.u[j]).max(ug);
                let mhat = self.m[j] / corr1.max(1e-18);
                self.coef[j] -= eta * mhat / (self.u[j] + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdaMax linear weights",
            "the first moment is bias-corrected and scaled by the running ∞-norm of g",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AdaMaxRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// AMSGrad linear regressor (river `optim.AMSGrad`).
///
/// Keeps the running maximum of the bias-corrected second moment.
#[derive(Clone, Debug)]
pub struct AMSGradRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    vhat_max: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AMSGradRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            vhat_max: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AMSGradRegressor {
    /// Default AMSGrad regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AMSGradRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.vhat_max = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                if vhat > self.vhat_max[j] {
                    self.vhat_max[j] = vhat;
                }
                self.coef[j] -= eta * mhat / (self.vhat_max[j].sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AMSGrad linear weights",
            "the second-moment maximum stops the Adam step from shrinking after a rare large g",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AMSGradRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Streaming quantile of column 0 (river `stats.Quantile`).
///
/// Window length is not identification `p`. Distinct from [`RollingQuantile`]
/// by exposing an explicit sliding capacity.
#[derive(Clone, Debug)]
pub struct OnlineQuantile {
    /// Quantile in \((0, 1)\).
    pub q: f64,
    /// Sliding window capacity (not a parameter count).
    pub window: usize,
    buf: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for OnlineQuantile {
    fn default() -> Self {
        Self {
            q: 0.5,
            window: 32,
            buf: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OnlineQuantile {
    /// Quantile `q` over a sliding window of `window` observations.
    pub fn new(q: f64, window: usize) -> Self {
        Self {
            q,
            window: window.max(1),
            ..Self::default()
        }
    }

    /// Current order-statistic quantile, or NaN when empty.
    pub fn score(&self) -> f64 {
        if self.buf.is_empty() {
            return f64::NAN;
        }
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            0.5
        };
        let mut xs = self.buf.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let idx = (qv * (xs.len() - 1) as f64).round() as usize;
        xs[idx.min(xs.len() - 1)]
    }
}

impl PartialFit for OnlineQuantile {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.q.is_finite() || self.q <= 0.0 || self.q >= 1.0 {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "OnlineQuantile.q={} is not in (0,1); using 0.5",
                        self.q
                    ))
                    .build(),
            );
            self.q = 0.5;
        }
        let cap = self.window.max(1);
        let before = self.score();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.buf.push(v);
            if self.buf.len() > cap {
                self.buf.remove(0);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.score();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.buf.len() as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.buf.len() >= 2;
        q.warmup = self.buf.len() < 2;
        q.explanation = format!(
            "OnlineQuantile q={:.3} value={after:.6e} n={}",
            self.q,
            self.buf.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming quantile",
                "order statistic of a sliding window of column-0 values; the window length is not p",
                format!("q={before:.6e}"),
                format!("q={after:.6e}"),
            ),
        )
    }
}

/// SLIM-lite item-item recommender (river `reco.SLIM`).
///
/// `X` is `[user, item]`, `y` is the rating. The last item per user is kept
/// and SGD updates \(W[\mathrm{prev},\mathrm{item}]\). Item count is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct SlimReco {
    /// SGD step.
    pub learning_rate: f64,
    last: HashMap<i64, usize>,
    w: HashMap<(usize, usize), f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for SlimReco {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            last: HashMap::new(),
            w: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SlimReco {
    /// Empty SLIM-lite recommender.
    pub fn new() -> Self {
        Self::default()
    }

    fn pred(&self, prev: usize, item: usize) -> f64 {
        self.w.get(&(prev, item)).copied().unwrap_or(0.0)
    }
}

impl PartialFit for SlimReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("SlimReco needs [user, item] columns")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need two columns"),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut sse = 0.0;
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let user = x.get(i, 0).round() as i64;
            let item = x.get(i, 1).round().max(0.0) as usize;
            if let Some(&prev) = self.last.get(&user) {
                let before = self.pred(prev, item);
                let err = before - y[i];
                sse += err * err;
                let nxt = before - eta * err;
                self.w.insert((prev, item), nxt);
                moved += (nxt - before).abs();
            }
            self.last.insert(user, item);
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved.max(x.nrows() as f64));
        q.loss_after = Some(sse);
        q.still_identified = self.w.len() >= 1;
        q.warmup = self.w.is_empty();
        q.explanation = format!(
            "SlimReco {} item-item weights, ||ΔW||={moved:.6e}",
            self.w.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("SlimReco has not yet seen a repeat user")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SLIM item-item weights",
                "SGD on W[prev_item, item] for the last item of each user",
                "previous sparse W",
                format!("{} pairs", self.w.len()),
            ),
        )
    }
}

/// AdaBound linear regressor (river `optim.AdaBound`).
///
/// Adam steps are clipped into a shrinking band around a final learning rate.
#[derive(Clone, Debug)]
pub struct AdaBoundRegressor {
    /// Initial step \(\eta\).
    pub learning_rate: f64,
    /// Final learning-rate centre.
    pub final_lr: f64,
    /// Band shrink rate.
    pub gamma: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaBoundRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            final_lr: 0.01,
            gamma: 0.001,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaBoundRegressor {
    /// Default AdaBound regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdaBoundRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let final_lr = if self.final_lr.is_finite() && self.final_lr > 0.0 {
            self.final_lr
        } else {
            0.01
        };
        let gamma = if self.gamma.is_finite() && self.gamma > 0.0 {
            self.gamma
        } else {
            0.001
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            let lower = final_lr * (1.0 - 1.0 / (gamma * self.t + 1.0));
            let upper = final_lr * (1.0 + 1.0 / (gamma * self.t));
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                let mut step = eta * mhat / (vhat.sqrt() + eps);
                if step.abs() < lower {
                    step = lower.copysign(step);
                } else if step.abs() > upper {
                    step = upper.copysign(step);
                }
                self.coef[j] -= step;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdaBound linear weights",
            "Adam steps are clipped into a shrinking band around the final learning rate",
        );
        finish_explain(ctx, expl)
    }
}

/// Batch Newton linear regressor (river `optim.Newton`).
///
/// One Newton step on a quadratic loss is the Gram solve of the current
/// mini-batch. Feature count is not identification `p`.
#[derive(Clone, Debug)]
pub struct NewtonRegressor {
    /// Ridge floor on the batch Gram.
    pub alpha: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for NewtonRegressor {
    fn default() -> Self {
        Self {
            alpha: 1e-6,
            fit_intercept: true,
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl NewtonRegressor {
    /// Default Newton regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for NewtonRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if self.initialized && self.coef.len() != dim {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("NewtonRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.initialized = true;
        }
        let n = x.nrows().min(y.len());
        let mut loss_before = 0.0;
        for i in 0..n {
            if y[i].is_finite() {
                let e = self.predict_row(x, i) - y[i];
                loss_before += e * e;
            }
        }
        let design = Matrix::from_fn(n, dim, |i, j| {
            let z = row_aug(x, i, self.fit_intercept);
            z.as_slice().get(j).copied().unwrap_or(0.0)
        });
        let yb = Vector::from_iter((0..n).map(|i| y[i]));
        let alpha = if self.alpha.is_finite() && self.alpha >= 0.0 {
            self.alpha
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!("Newton α={} is invalid; using 1e-6", self.alpha))
                    .build(),
            );
            1e-6
        };
        let mut scratch = signlred::Report::new("newton", "batch");
        let before = self.coef.clone();
        if let Some(beta) = ridge_solve(&mut scratch, &design, &yb, alpha, &ctx.policy) {
            self.coef = beta;
        }
        for issue in scratch.issues() {
            if matches!(
                issue.code,
                IssueCode::ResidualTooLarge
                    | IssueCode::NearSingular
                    | IssueCode::R2IsOne
                    | IssueCode::RankZero
                    | IssueCode::PerfectCollinearity
                    | IssueCode::InvalidWeight
                    | IssueCode::UnderdeterminedSystem
                    | IssueCode::CholeskyFailed
                    | IssueCode::NonFiniteOutput
                    | IssueCode::DimensionMismatch
                    | IssueCode::JitterInjected
            ) {
                continue;
            }
            ctx.push(issue.clone());
        }
        let mut loss_after = 0.0;
        let mut used = 0u64;
        for i in 0..n {
            if !y[i].is_finite() {
                continue;
            }
            let e = self.predict_row(x, i) - y[i];
            loss_after += e * e;
            used += 1;
        }
        self.n_seen += used;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Newton linear weights",
            "one Gram solve of the current mini-batch (exact Newton on squared loss)",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for NewtonRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Normalised rating recommender (river `reco.Norm`).
///
/// `X` is `[user, item]`, `y` is the rating. Running mean / std of ratings
/// are maintained globally and per item. Item count is not identification `p`.
#[derive(Clone, Debug)]
pub struct NormReco {
    mean: f64,
    m2: f64,
    n_seen: u64,
    updates: u64,
    item_mean: HashMap<usize, f64>,
    item_n: HashMap<usize, u64>,
}

impl Default for NormReco {
    fn default() -> Self {
        Self {
            mean: 0.0,
            m2: 0.0,
            n_seen: 0,
            updates: 0,
            item_mean: HashMap::new(),
            item_n: HashMap::new(),
        }
    }
}

impl NormReco {
    /// Empty Norm recommender.
    pub fn new() -> Self {
        Self::default()
    }

    fn std(&self) -> f64 {
        if self.n_seen < 2 {
            return 0.0;
        }
        (self.m2 / (self.n_seen as f64 - 1.0)).max(0.0).sqrt()
    }

    fn pred_item(&self, item: usize) -> f64 {
        self.item_mean.get(&item).copied().unwrap_or(self.mean)
    }
}

impl PartialFit for NormReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("NormReco needs [user, item] columns")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need two columns"),
            );
        }
        let before = self.mean;
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let item = x.get(i, 1).round().max(0.0) as usize;
            self.n_seen += 1;
            let d = y[i] - self.mean;
            self.mean += d / self.n_seen as f64;
            self.m2 += d * (y[i] - self.mean);
            let n_i = self.item_n.get(&item).copied().unwrap_or(0) + 1;
            let m_i = self.item_mean.get(&item).copied().unwrap_or(0.0);
            let nxt = m_i + (y[i] - m_i) / n_i as f64;
            moved += (nxt - m_i).abs();
            self.item_n.insert(item, n_i);
            self.item_mean.insert(item, nxt);
        }
        self.updates += 1;
        let sd = self.std();
        if self.n_seen >= 2 && sd <= 1e-18 {
            ctx.push(
                Issue::builder(IssueCode::DegenerateDistribution)
                    .message("NormReco rating std vanished")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.mean - before).abs().max(moved));
        q.information_gain = Some((self.mean - before).abs().max(moved));
        q.still_identified = self.n_seen >= 2 && sd > 1e-18;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "NormReco μ={:.6e} σ={:.6e} on {} items",
            self.mean,
            sd,
            self.item_mean.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("NormReco has seen fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "normalised rating mean/std",
                "Welford μ,σ plus per-item means; item count is not p",
                format!("μ={before:.6e}"),
                format!("μ={:.6e} items={}", self.mean, self.item_mean.len()),
            ),
        )
    }
}

impl Predict for NormReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        if x.ncols() < 2 {
            return ctx.finish(Vector::filled(x.nrows(), self.mean));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let item = x.get(i, 1).round().max(0.0) as usize;
            self.pred_item(item)
        })))
    }
}

/// Nadam linear regressor (river `optim.Nadam`).
///
/// Nesterov look-ahead on the Adam first moment.
#[derive(Clone, Debug)]
pub struct NadamRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for NadamRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl NadamRegressor {
    /// Default Nadam regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for NadamRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("NadamRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                let nest = b1 * mhat + (1.0 - b1) * g / corr1.max(1e-18);
                self.coef[j] -= eta * nest / (vhat.sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Nadam linear weights",
            "Nesterov look-ahead on the bias-corrected Adam first moment",
        );
        finish_explain(ctx, expl)
    }
}

/// AdamW linear regressor (river `optim.Adam` with decoupled weight decay).
#[derive(Clone, Debug)]
pub struct AdamWRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Decoupled L2 decay.
    pub weight_decay: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdamWRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            weight_decay: 1e-4,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdamWRegressor {
    /// Default AdamW regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdamWRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(0);
            self.v = Vector::zeros(0);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("AdamWRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let wd = if self.weight_decay.is_finite() && self.weight_decay >= 0.0 {
            self.weight_decay
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message(format!(
                        "AdamW weight_decay={} is invalid; using 0",
                        self.weight_decay
                    ))
                    .build(),
            );
            0.0
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                self.coef[j] -= eta * mhat / (vhat.sqrt() + eps);
                if j > 0 || !self.fit_intercept {
                    self.coef[j] *= 1.0 - eta * wd;
                }
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdamW linear weights",
            "Adam step plus decoupled weight decay (intercept is not decayed)",
        );
        finish_explain(ctx, expl)
    }
}

/// Hogwild!-style SGD linear regressor (river `optim.SGD` / Hogwild).
///
/// Feature count is not identification `p`.
#[derive(Clone, Debug)]
pub struct HogwildRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HogwildRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            fit_intercept: true,
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HogwildRegressor {
    /// Default Hogwild regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for HogwildRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("HogwildRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim.min(z.len()) {
                self.coef[j] -= eta * err * z[j];
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Hogwild linear weights",
            "unlocked SGD steps; each row writes θ immediately",
        );
        finish_explain(ctx, expl)
    }
}

/// Content-based recommender (river `reco.Content`).
///
/// `X` is `[user, item, …content]`, `y` is the rating. Item count is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct ContentReco {
    /// SGD step on content weights.
    pub learning_rate: f64,
    user_mean: HashMap<i64, (f64, u64)>,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for ContentReco {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            user_mean: HashMap::new(),
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl ContentReco {
    /// Empty content recommender.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ContentReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("ContentReco needs [user, item, …content]")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need two columns"),
            );
        }
        let p = x.ncols().saturating_sub(2);
        if !self.initialized {
            self.coef = Vector::zeros(p.max(1));
            self.initialized = true;
        } else if self.coef.len() != p.max(1) {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("ContentReco content dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "content dim changed"),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut moved = 0.0;
        let mut sse = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let user = x.get(i, 0).round() as i64;
            let (um, un) = self.user_mean.get(&user).copied().unwrap_or((0.0, 0));
            let mut pred = um;
            for j in 0..p.min(self.coef.len()) {
                pred += self.coef[j] * x.get(i, j + 2);
            }
            let err = pred - y[i];
            sse += err * err;
            for j in 0..p.min(self.coef.len()) {
                let step = eta * err * x.get(i, j + 2);
                self.coef[j] -= step;
                moved += step.abs();
            }
            let nxt_n = un + 1;
            let nxt_m = um + (y[i] - um) / nxt_n as f64;
            self.user_mean.insert(user, (nxt_m, nxt_n));
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved.max(x.nrows() as f64));
        q.loss_after = Some(sse);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "ContentReco {} users, ||Δw||={moved:.6e}",
            self.user_mean.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("ContentReco has seen fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "user mean + content SGD",
                "residual of the user mean is fit on columns 2..; item count is not p",
                "previous user means / content weights",
                format!("{} users", self.user_mean.len()),
            ),
        )
    }
}

/// Group-wise target mean encoder (river `feature_extraction.TargetAgg`).
///
/// Group count is not identification `p`.
#[derive(Clone, Debug)]
pub struct TargetAgg {
    means: HashMap<i64, (f64, u64)>,
    n_seen: u64,
    updates: u64,
}

impl Default for TargetAgg {
    fn default() -> Self {
        Self {
            means: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl TargetAgg {
    /// Empty target aggregator.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for TargetAgg {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        if x.ncols() < 1 {
            ctx.push(
                Issue::builder(IssueCode::EmptyMatrix)
                    .severity(Severity::Warning)
                    .message("TargetAgg needs a group column")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no group column"),
            );
        }
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let g = x.get(i, 0).round() as i64;
            let (m, n) = self.means.get(&g).copied().unwrap_or((0.0, 0));
            let nxt_n = n + 1;
            let nxt = m + (y[i] - m) / nxt_n as f64;
            moved += (nxt - m).abs();
            self.means.insert(g, (nxt, nxt_n));
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved);
        q.still_identified = !self.means.is_empty();
        q.warmup = self.means.is_empty();
        q.explanation = format!("TargetAgg {} groups, ||Δμ||={moved:.6e}", self.means.len());
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("TargetAgg has no groups yet")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "group target means",
                "Welford mean of y keyed by X[:,0]; group count is not p",
                "previous group means",
                format!("{} groups", self.means.len()),
            ),
        )
    }
}

/// Higher-order factorization machine (river `reco.HoFM`).
///
/// Factor count is not identification `p`. A design with fewer than three
/// columns has no 3-way term.
#[derive(Clone, Debug)]
pub struct HoFm {
    /// Pairwise factor width.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    v: Matrix,
    t3: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HoFm {
    fn default() -> Self {
        Self {
            n_factors: 2,
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            v: Matrix::zeros(0, 0),
            t3: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HoFm {
    /// HoFM with `k` pairwise factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        let k = self.v.ncols();
        if k > 0 && self.v.nrows() > 0 {
            for f in 0..k {
                let mut sum_vx = 0.0;
                let mut sum_v2x2 = 0.0;
                for j in 0..p.min(self.v.nrows()) {
                    let vjf = self.v.get(j, f);
                    let xj = x.get(i, j);
                    sum_vx += vjf * xj;
                    sum_v2x2 += vjf * vjf * xj * xj;
                }
                s += 0.5 * (sum_vx * sum_vx - sum_v2x2);
            }
        }
        if p >= 3 {
            s += self.t3 * x.get(i, 0) * x.get(i, 1) * x.get(i, 2);
        }
        s
    }
}

impl PartialFit for HoFm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        let p = x.ncols();
        let k = self.n_factors.max(1);
        if !self.initialized {
            self.w = Vector::zeros(p.max(1));
            self.v = Matrix::from_fn(p.max(1), k, |_, _| 0.01);
            self.initialized = true;
        } else if self.w.len() != p.max(1) {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("HoFm feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut sse = 0.0;
        let mut moved = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            sse += err * err;
            self.w0 -= eta * err;
            moved += (eta * err).abs();
            let kf = k.min(self.v.ncols());
            let mut sum_f = vec![0.0; kf];
            for f in 0..kf {
                for j in 0..p.min(self.v.nrows()) {
                    sum_f[f] += self.v.get(j, f) * x.get(i, j);
                }
            }
            for j in 0..p.min(self.w.len()) {
                let xj = x.get(i, j);
                let step = eta * err * xj;
                self.w[j] -= step;
                moved += step.abs();
                if j >= self.v.nrows() {
                    continue;
                }
                for f in 0..kf {
                    let vjf = self.v.get(j, f);
                    let grad = err * xj * (sum_f[f] - vjf * xj);
                    let nxt = vjf - eta * grad;
                    self.v.set(j, f, nxt);
                    moved += (eta * grad).abs();
                }
            }
            if p >= 3 {
                let trip = x.get(i, 0) * x.get(i, 1) * x.get(i, 2);
                let step = eta * err * trip;
                self.t3 -= step;
                moved += step.abs();
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved.max(x.nrows() as f64));
        q.loss_after = Some(sse);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("HoFM ||Δ||={moved:.6e} sse={sse:.6e}");
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("HoFm has seen fewer than two rows")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "linear + pairwise FM + optional 3-way term",
                "SGD on w, V and the first-three-column product; factor count is not p",
                "previous HoFM weights",
                format!("n_seen={}", self.n_seen),
            ),
        )
    }
}

impl Predict for AdaBoundRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.predict_row(x, i)),
        ))
    }
}

/// Yogi linear regressor (river `optim.Yogi`).
///
/// Second-moment updates subtract a signed increment so \(v\) can decrease.
#[derive(Clone, Debug)]
pub struct YogiRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for YogiRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl YogiRegressor {
    /// Default Yogi regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for YogiRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                let g2 = g * g;
                self.v[j] -= (1.0 - b2) * (self.v[j] - g2).signum() * g2;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j].abs() / corr2.max(1e-18);
                self.coef[j] -= eta * mhat / (vhat.sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Yogi linear weights",
            "Adam first moment with a signed second-moment correction",
        );
        finish_explain(ctx, expl)
    }
}

/// Lion linear regressor (river `optim.Lion`).
///
/// Sign of an interpolated momentum; no second-moment state.
#[derive(Clone, Debug)]
pub struct LionRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Momentum mix.
    pub beta1: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LionRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LionRegressor {
    /// Default Lion regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for LionRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                let update = b1 * self.m[j] + (1.0 - b1) * g;
                self.coef[j] -= eta * update.signum();
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Lion linear weights",
            "sign of interpolated momentum; no second-moment state",
        );
        finish_explain(ctx, expl)
    }
}

/// Adafactor linear regressor (river `optim.Adafactor`).
///
/// For a 1-d parameter the factored second moment reduces to RMSProp with a
/// relative step \(\max(1,\mathrm{rms}(w))\).
#[derive(Clone, Debug)]
pub struct AdafactorRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    v: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdafactorRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            v: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdafactorRegressor {
    /// Default Adafactor regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdafactorRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        let rms_w = {
            let s: f64 = self.coef.as_slice().iter().map(|w| w * w).sum();
            (s / dim.max(1) as f64).sqrt().max(1.0)
        };
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                let g = err * z[j];
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                self.coef[j] -= eta * rms_w * g / (self.v[j].sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Adafactor linear weights",
            "RMSProp second moment with a relative rms(w) step; factor count is not p",
        );
        finish_explain(ctx, expl)
    }
}

/// AdaBelief linear regressor (river `optim.AdaBelief`).
///
/// Second moment tracks \((g-m)^2\) rather than \(g^2\).
#[derive(Clone, Debug)]
pub struct AdaBeliefRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdaBeliefRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdaBeliefRegressor {
    /// Default AdaBelief regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for AdaBeliefRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                let belief = g - self.m[j];
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * belief * belief;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                self.coef[j] -= eta * mhat / (vhat.sqrt() + eps);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "AdaBelief linear weights",
            "Adam step with a second moment on (g − m)",
        );
        finish_explain(ctx, expl)
    }
}

/// Fast Hoeffding Drift Detection (river `drift.FHDDM`).
///
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct Fhddm {
    /// Confidence \(\delta\).
    pub delta: f64,
    /// Sliding window of 0/1 errors.
    pub window: usize,
    buf: Vec<f64>,
    p_max: f64,
    n: u64,
    updates: u64,
}

impl Default for Fhddm {
    fn default() -> Self {
        Self {
            delta: 1e-6,
            window: 32,
            buf: Vec::new(),
            p_max: 0.0,
            n: 0,
            updates: 0,
        }
    }
}

impl Fhddm {
    /// Fresh FHDDM detector.
    pub fn new() -> Self {
        Self::default()
    }

    fn bound(n: usize, delta: f64) -> f64 {
        if n == 0 {
            return f64::INFINITY;
        }
        (0.5 / n as f64 * (1.0 / delta.max(1e-16)).ln()).sqrt()
    }

    /// Update with a 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        let w = self.window.max(2);
        self.buf.push(e);
        if self.buf.len() > w {
            self.buf.remove(0);
        }
        self.n += 1;
        let p = self.buf.iter().sum::<f64>() / self.buf.len() as f64;
        if p > self.p_max {
            self.p_max = p;
        }
        let eps = Self::bound(self.buf.len(), self.delta);
        let stat = self.p_max - p;
        let decision = if self.buf.len() >= w && stat > eps {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("FHDDM drift: p_max-p={stat:.4} > ε={eps:.4}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.buf.clear();
            self.p_max = 0.0;
            DriftDecision::Drift { statistic: stat }
        } else {
            DriftDecision::Stable
        };
        ctx.finish(decision)
    }
}

impl PartialFit for Fhddm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let before = self.p_max;
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("fhddm")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.p_max);
        q.still_identified = self.buf.len() >= self.window.max(2);
        q.warmup = self.buf.len() < self.window.max(2);
        q.explanation = format!(
            "FHDDM p_max {before:.4}→{:.4}, n={}, drift_cuts={fired}",
            self.p_max, self.n
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed error rate and Hoeffding bound",
                "FHDDM comparison of p_max to the current window mean",
                format!("p_max={before:.4}"),
                format!("p_max={:.4} n={}", self.p_max, self.n),
            ),
        )
    }
}

/// Expanding-window mean (river `stats.Mean` / `utils.Rolling` with infinite window).
#[derive(Clone, Debug, Default)]
pub struct ExpandingMean {
    n: f64,
    mean: f64,
    updates: u64,
}

impl ExpandingMean {
    /// Empty accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current mean, or NaN when empty.
    pub fn mean(&self) -> f64 {
        if self.n <= 0.0 {
            f64::NAN
        } else {
            self.mean
        }
    }
}

impl PartialFit for ExpandingMean {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.mean();
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            self.mean += (v - self.mean) / self.n;
        }
        self.updates += 1;
        let after = self.mean();
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            0.0
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1.0;
        q.warmup = self.n < 1.0;
        q.explanation = format!("ExpandingMean μ={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "expanding arithmetic mean",
                "Welford update on column 0; count is not p",
                format!("mean={before:.6e}"),
                format!("mean={after:.6e}"),
            ),
        )
    }
}

/// Running absolute maximum (river `stats.AbsMax`).
#[derive(Clone, Debug)]
pub struct AbsMax {
    max: f64,
    n: u64,
    updates: u64,
}

impl Default for AbsMax {
    fn default() -> Self {
        Self {
            max: 0.0,
            n: 0,
            updates: 0,
        }
    }
}

impl AbsMax {
    /// Empty accumulator.
    pub fn new() -> Self {
        Self::default()
    }

    /// Current \(|x|_{\max}\).
    pub fn value(&self) -> f64 {
        if self.n == 0 {
            f64::NAN
        } else {
            self.max
        }
    }
}

impl PartialFit for AbsMax {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.value();
        for i in 0..x.nrows() {
            let v = x.get(i, 0).abs();
            if !v.is_finite() {
                continue;
            }
            if self.n == 0 || v > self.max {
                self.max = v;
            }
            self.n += 1;
        }
        self.updates += 1;
        let after = self.value();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.parameter_delta_norm = Some(if before.is_finite() && after.is_finite() {
            (after - before).abs()
        } else {
            after.abs()
        });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 1;
        q.warmup = self.n < 1;
        q.explanation = format!("AbsMax={after:.6e}");
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running absolute maximum",
                "column-0 |x| fold; the extremum is not p",
                format!("absmax={before:.6e}"),
                format!("absmax={after:.6e}"),
            ),
        )
    }
}

/// Stacked FHDDM (river `drift.FHDDMS`) with a short and a long window.
///
/// Window lengths are not identification `p`.
#[derive(Clone, Debug)]
pub struct Fhddms {
    short: Fhddm,
    long: Fhddm,
    n: u64,
    updates: u64,
}

impl Default for Fhddms {
    fn default() -> Self {
        Self {
            short: Fhddm {
                window: 16,
                ..Fhddm::default()
            },
            long: Fhddm {
                window: 32,
                ..Fhddm::default()
            },
            n: 0,
            updates: 0,
        }
    }
}

impl Fhddms {
    /// Fresh stacked FHDDM.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for Fhddms {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let mut fired = 0.0;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if let Ok(q) = self.short.update(v, &session.child("fhddms-s")) {
                if matches!(q.value, DriftDecision::Drift { .. }) {
                    fired += 1.0;
                }
            }
            if let Ok(q) = self.long.update(v, &session.child("fhddms-l")) {
                if matches!(q.value, DriftDecision::Drift { .. }) {
                    fired += 1.0;
                }
            }
            self.n += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n);
        q.effective_sample_size = self.n as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.short.p_max.max(self.long.p_max));
        q.still_identified = self.n >= 16;
        q.warmup = self.n < 16;
        q.explanation = format!("FHDDMS drift_cuts={fired} n={}", self.n);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "short and long FHDDM windows",
                "OR of two Hoeffding error-rate detectors; window length is not p",
                "previous stacked bounds",
                format!("n={}", self.n),
            ),
        )
    }
}

/// Polyak-averaged SGD (river `optim.Averager`).
///
/// Coefficient count is not identification `p`.
#[derive(Clone, Debug)]
pub struct AveragerRegressor {
    /// Instantaneous step \(\eta\).
    pub learning_rate: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    avg: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AveragerRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            fit_intercept: true,
            coef: Vector::zeros(0),
            avg: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AveragerRegressor {
    /// Default Polyak averager.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.avg.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.avg[j] * z[j];
        }
        s
    }
}

impl PartialFit for AveragerRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.avg = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.avg.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            for j in 0..dim {
                self.coef[j] -= eta * err * z[j];
            }
            self.n_seen += 1;
            let w = 1.0 / self.n_seen as f64;
            for j in 0..dim {
                self.avg[j] += w * (self.coef[j] - self.avg[j]);
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
        }
        self.updates += 1;
        let delta = self.avg.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Polyak-averaged linear weights",
            "SGD path plus a running mean of the iterate",
        );
        finish_explain(ctx, expl)
    }
}

/// Online Bayesian linear regression (river `linear_model.BayesianLinearRegression`).
///
/// Ridge-prior Gram solve on the accumulated batch; feature count is not
/// identification `p`. Scratch Cholesky is not used — the Gram is diagonally
/// loaded.
#[derive(Clone, Debug)]
pub struct BayesianLinearRegression {
    /// Prior precision \(\lambda\).
    pub alpha: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    xtx: Option<Mat<f64>>,
    xty: Vector,
    coef: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for BayesianLinearRegression {
    fn default() -> Self {
        Self {
            alpha: 1.0,
            fit_intercept: true,
            xtx: None,
            xty: Vector::zeros(0),
            coef: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl BayesianLinearRegression {
    /// Unit-precision prior.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for BayesianLinearRegression {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.xtx = Some(Mat::<f64>::zeros(dim, dim));
            self.xty = Vector::zeros(dim);
            self.coef = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.coef.clone();
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        let lam = if self.alpha.is_finite() && self.alpha >= 0.0 {
            self.alpha
        } else {
            1.0
        };
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            loss_before += (pred - y[i]) * (pred - y[i]);
        }
        {
            let xtx = self.xtx.as_mut().expect("initialized Gram");
            for i in 0..x.nrows().min(y.len()) {
                if !y[i].is_finite() {
                    continue;
                }
                let z = row_aug(x, i, self.fit_intercept);
                for a in 0..dim {
                    self.xty[a] += z[a] * y[i];
                    for b in 0..dim {
                        xtx[(a, b)] += z[a] * z[b];
                    }
                }
                self.n_seen += 1;
            }
            let mut g = Matrix::zeros(dim, dim);
            for a in 0..dim {
                for b in 0..dim {
                    let mut v = xtx[(a, b)];
                    if a == b {
                        v += lam;
                    }
                    g.set(a, b, v);
                }
            }
            let mut scratch = signlred::Report::new("bayeslin", "gram");
            if let Some(sol) = ridge_solve(&mut scratch, &g, &self.xty, 0.0, &ctx.policy) {
                self.coef = sol;
            } else {
                ctx.push(
                    Issue::builder(IssueCode::DidNotConverge)
                        .message("BayesianLinearRegression Gram solve failed")
                        .build(),
                );
            }
        }
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "Bayesian linear posterior mean",
            "ridge-prior normal equations on the accumulated Gram",
        );
        finish_explain(ctx, expl)
    }
}

/// Standard absolute deviation scorer (river `anomaly.StandardAbsoluteDeviation`).
#[derive(Clone, Debug, Default)]
pub struct StandardAbsoluteDeviation {
    n: f64,
    mean: f64,
    m2: f64,
    last: f64,
    updates: u64,
}

impl StandardAbsoluteDeviation {
    /// Empty scorer.
    pub fn new() -> Self {
        Self::default()
    }

    /// Latest \(|x-\bar x|/s\).
    pub fn score(&self) -> f64 {
        self.last
    }
}

impl PartialFit for StandardAbsoluteDeviation {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let before = self.last;
        for i in 0..x.nrows() {
            let v = x.get(i, 0);
            if !v.is_finite() {
                continue;
            }
            self.n += 1.0;
            let d = v - self.mean;
            self.mean += d / self.n;
            self.m2 += d * (v - self.mean);
            let sd = if self.n >= 2.0 {
                (self.m2 / (self.n - 1.0)).max(0.0).sqrt()
            } else {
                0.0
            };
            self.last = if sd > 1e-12 {
                (v - self.mean).abs() / sd
            } else {
                0.0
            };
        }
        self.updates += 1;
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some((self.last - before).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("SAD score={:.6e}", self.last);
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("StandardAbsoluteDeviation needs two observations")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "standard absolute deviation score",
                "Welford |x-mean|/std on column 0; the score is not p",
                format!("sad={before:.6e}"),
                format!("sad={:.6e}", self.last),
            ),
        )
    }
}

/// Hedge / multiplicative-weights regressor (river `optim.Hedge`).
///
/// Two experts (expanding mean and last observation). Expert count is not `p`.
#[derive(Clone, Debug)]
pub struct HedgeRegressor {
    /// Mix rate \(\eta\).
    pub learning_rate: f64,
    w_mean: f64,
    w_last: f64,
    mean: f64,
    last: f64,
    n: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for HedgeRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.5,
            w_mean: 1.0,
            w_last: 1.0,
            mean: 0.0,
            last: 0.0,
            n: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl HedgeRegressor {
    /// Default two-expert Hedge.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HedgeRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.5
        };
        let before = if self.w_mean + self.w_last > 0.0 {
            (self.w_mean * self.mean + self.w_last * self.last) / (self.w_mean + self.w_last)
        } else {
            0.0
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = if self.w_mean + self.w_last > 0.0 {
                (self.w_mean * self.mean + self.w_last * self.last) / (self.w_mean + self.w_last)
            } else {
                0.0
            };
            loss_before += (pred - y[i]) * (pred - y[i]);
            let e_mean = (self.mean - y[i]).abs();
            let e_last = (self.last - y[i]).abs();
            self.w_mean *= (-eta * e_mean).exp();
            self.w_last *= (-eta * e_last).exp();
            let s = self.w_mean + self.w_last;
            if s > 0.0 {
                self.w_mean /= s;
                self.w_last /= s;
            } else {
                self.w_mean = 0.5;
                self.w_last = 0.5;
            }
            self.n += 1.0;
            self.mean += (y[i] - self.mean) / self.n;
            self.last = y[i];
            let after = (self.w_mean * self.mean + self.w_last * self.last)
                / (self.w_mean + self.w_last).max(1e-18);
            loss_after += (after - y[i]) * (after - y[i]);
            self.n_seen += 1;
        }
        self.updates += 1;
        let dummy = Vector::from_iter([self.w_mean, self.w_last]);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &dummy.sub(&Vector::from_iter([0.5, 0.5])),
            loss_before,
            loss_after,
            self.n_seen >= 2,
            self.n_seen < 2,
            "Hedge expert weights",
            "multiplicative weights on expanding-mean and last-value experts",
        );
        let _ = before;
        finish_explain(ctx, expl)
    }
}

/// Named alias of [`AdwinKnn`] (river `neighbors.KNNADWINClassifier`).
#[derive(Clone, Debug)]
pub struct KnnAdwin {
    inner: AdwinKnn,
}

impl Default for KnnAdwin {
    fn default() -> Self {
        Self {
            inner: AdwinKnn::default(),
        }
    }
}

impl KnnAdwin {
    /// kNN + ADWIN with neighbourhood `k`.
    pub fn new(k: usize) -> Self {
        Self {
            inner: AdwinKnn::new(k),
        }
    }
}

impl PartialFit for KnnAdwin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

/// Self-adjusting memory kNN (river `neighbors.SAMKNNClassifier`).
///
/// STM window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct SamKnn {
    /// Neighbourhood size.
    pub k: usize,
    /// Short-term memory length.
    pub window: usize,
    stm_x: Vec<Vec<f64>>,
    stm_y: Vec<i64>,
    ltm_x: Vec<Vec<f64>>,
    ltm_y: Vec<i64>,
    n_seen: u64,
    updates: u64,
}

impl Default for SamKnn {
    fn default() -> Self {
        Self {
            k: 3,
            window: 8,
            stm_x: Vec::new(),
            stm_y: Vec::new(),
            ltm_x: Vec::new(),
            ltm_y: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SamKnn {
    /// STM of length `window` and neighbourhood `k`.
    pub fn new(k: usize, window: usize) -> Self {
        Self {
            k: k.max(1),
            window: window.max(2),
            ..Self::default()
        }
    }

    fn vote(xs: &[Vec<f64>], ys: &[i64], row: &Matrix, i: usize, k: usize) -> Option<i64> {
        if xs.is_empty() {
            return None;
        }
        let mut dist: Vec<(f64, i64)> = xs
            .iter()
            .zip(ys.iter())
            .map(|(z, &lab)| {
                let mut s = 0.0;
                for j in 0..row.ncols().min(z.len()) {
                    let d = row.get(i, j) - z[j];
                    s += d * d;
                }
                (s, lab)
            })
            .collect();
        dist.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let take = k.max(1).min(dist.len());
        let mut votes = std::collections::BTreeMap::<i64, usize>::new();
        for (_, lab) in dist.into_iter().take(take) {
            *votes.entry(lab).or_insert(0) += 1;
        }
        votes
            .into_iter()
            .max_by(|a, b| a.1.cmp(&b.1).then(b.0.cmp(&a.0)))
            .map(|(lab, _)| lab)
    }
}

impl PartialFit for SamKnn {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.stm_x.len() + self.ltm_x.len();
        let w = self.window.max(2);
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            let lab = y[i].round() as i64;
            self.stm_x.push(row);
            self.stm_y.push(lab);
            if self.stm_x.len() > w {
                if let Some(old) = self.stm_x.first().cloned() {
                    self.ltm_x.push(old);
                    self.ltm_y.push(self.stm_y[0]);
                }
                self.stm_x.remove(0);
                self.stm_y.remove(0);
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self.stm_x.len() + self.ltm_x.len();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((after - before) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = after >= self.k;
        q.warmup = after < self.k;
        q.explanation = format!("SAM-kNN STM={} LTM={}", self.stm_x.len(), self.ltm_x.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "short-term and long-term kNN memories",
                "STM is a sliding window; overflow is promoted to LTM; k is not p",
                format!("mem={before}"),
                format!("mem={after}"),
            ),
        )
    }
}

impl Predict for SamKnn {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.stm_x.is_empty() && self.ltm_x.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let stm = Self::vote(&self.stm_x, &self.stm_y, x, i, self.k);
            let ltm = Self::vote(&self.ltm_x, &self.ltm_y, x, i, self.k);
            match (stm, ltm) {
                (Some(a), Some(b)) if a == b => a as f64,
                (Some(a), Some(_)) => a as f64,
                (Some(a), None) | (None, Some(a)) => a as f64,
                _ => 0.0,
            }
        }));
        ctx.finish(out)
    }
}

/// Streaming one-class SVM (river `anomaly.SVM`).
///
/// Feature count is not identification `p` for a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineOneClassSvm {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// Fraction of outliers \(\nu\).
    pub nu: f64,
    w: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineOneClassSvm {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            nu: 0.1,
            w: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineOneClassSvm {
    /// Default one-class stream.
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let n = x.ncols().min(self.w.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for OnlineOneClassSvm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let dim = x.ncols();
        if !self.initialized {
            self.w = Vector::zeros(dim);
            self.initialized = true;
        } else if self.w.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.w.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let nu = self.nu.clamp(1e-3, 0.999);
        for i in 0..x.nrows() {
            let score = self.score_row(x, i);
            if score < 1.0 {
                for j in 0..dim {
                    self.w[j] += eta * (x.get(i, j) - nu * self.w[j]);
                }
            } else {
                for j in 0..dim {
                    self.w[j] -= eta * nu * self.w[j];
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.w.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            0.0,
            0.0,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "one-class SVM weights",
            "Pegasos-style ν-SVM on the incoming rows; ν is not p",
        );
        finish_explain(ctx, expl)
    }
}

/// Field-weighted factorization machine (river `facto.FwFM`).
///
/// Factor / field counts are not identification `p`.
#[derive(Clone, Debug)]
pub struct FwFmRegressor {
    /// Latent width.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    v: Matrix,
    r: Matrix,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FwFmRegressor {
    fn default() -> Self {
        Self {
            n_factors: 2,
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            v: Matrix::zeros(0, 0),
            r: Matrix::zeros(0, 0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FwFmRegressor {
    /// FwFM with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        let k = self.v.ncols();
        if k == 0 || p < 2 {
            return s;
        }
        for a in 0..p {
            for b in (a + 1)..p {
                let mut dot = 0.0;
                for f in 0..k {
                    dot += self.v.get(a, f) * self.v.get(b, f);
                }
                let rw = if a < self.r.nrows() && b < self.r.ncols() {
                    self.r.get(a, b)
                } else {
                    1.0
                };
                s += rw * dot * x.get(i, a) * x.get(i, b);
            }
        }
        s
    }
}

impl PartialFit for FwFmRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols();
        let k = self.n_factors.max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.v = Matrix::from_fn(p, k, |i, f| 0.01 * ((i + f + 1) as f64).sin());
            self.r = Matrix::from_fn(p, p, |a, b| if a == b { 0.0 } else { 1.0 });
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.w.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.w0 -= eta * err;
            for j in 0..p {
                self.w[j] -= eta * err * x.get(i, j);
            }
            for a in 0..p {
                for b in (a + 1)..p {
                    let xa = x.get(i, a);
                    let xb = x.get(i, b);
                    let mut dot = 0.0;
                    for f in 0..k {
                        dot += self.v.get(a, f) * self.v.get(b, f);
                    }
                    let rw = self.r.get(a, b);
                    self.r.set(a, b, rw - eta * err * dot * xa * xb);
                    self.r.set(b, a, self.r.get(a, b));
                    for f in 0..k {
                        let va = self.v.get(a, f);
                        let vb = self.v.get(b, f);
                        self.v.set(a, f, va - eta * err * rw * vb * xa * xb);
                        self.v.set(b, f, vb - eta * err * rw * va * xa * xb);
                    }
                }
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.w.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= 2,
            self.n_seen < 2,
            "FwFM linear + field-pair weights",
            "SGD on w, V and a symmetric field-interaction matrix; k is not p",
        );
        finish_explain(ctx, expl)
    }
}

/// Online multivariate Gaussian (river `proba.MultivariateGaussian`).
///
/// Dimension is not identification `p` for a 1-row stream.
#[derive(Clone, Debug)]
pub struct MultivariateGaussian {
    n: f64,
    mean: Vector,
    m2: Matrix,
    updates: u64,
    initialized: bool,
}

impl Default for MultivariateGaussian {
    fn default() -> Self {
        Self {
            n: 0.0,
            mean: Vector::zeros(0),
            m2: Matrix::zeros(0, 0),
            updates: 0,
            initialized: false,
        }
    }
}

impl MultivariateGaussian {
    /// Empty accumulator.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for MultivariateGaussian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let p = x.ncols();
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.m2 = Matrix::zeros(p, p);
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n as u64, "feature space changed"),
            );
        }
        let before = self.mean.clone();
        for i in 0..x.nrows() {
            self.n += 1.0;
            let mut delta = Vector::zeros(p);
            for j in 0..p {
                delta[j] = x.get(i, j) - self.mean[j];
                self.mean[j] += delta[j] / self.n;
            }
            for a in 0..p {
                for b in 0..p {
                    let db = x.get(i, b) - self.mean[b];
                    self.m2.set(a, b, self.m2.get(a, b) + delta[a] * db);
                }
            }
        }
        self.updates += 1;
        let dmean = self.mean.sub(&before);
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(dmean.norm());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("MultivariateGaussian n={:.0}", self.n);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "running mean and scatter",
                "Welford multivariate update; dimension is not identification p",
                format!("||μ||={:.6e}", before.norm()),
                format!("||μ||={:.6e}", self.mean.norm()),
            ),
        )
    }
}

/// Online covariance scaling (river `preprocessing.ScaleCovariances` / LDA prep).
///
/// Column count is not identification `p` for a 1-row stream.
#[derive(Clone, Debug)]
pub struct ScaleCovariances {
    n: f64,
    mean: Vector,
    var: Vector,
    updates: u64,
    initialized: bool,
}

impl Default for ScaleCovariances {
    fn default() -> Self {
        Self {
            n: 0.0,
            mean: Vector::zeros(0),
            var: Vector::zeros(0),
            updates: 0,
            initialized: false,
        }
    }
}

impl ScaleCovariances {
    /// Empty scaler.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ScaleCovariances {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let p = x.ncols();
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.var = Vector::filled(p, 1.0);
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n as u64, "feature space changed"),
            );
        }
        let before = self.var.clone();
        for i in 0..x.nrows() {
            self.n += 1.0;
            for j in 0..p {
                let d = x.get(i, j) - self.mean[j];
                self.mean[j] += d / self.n;
                let d2 = x.get(i, j) - self.mean[j];
                self.var[j] += (d * d2 - self.var[j]) / self.n;
            }
        }
        self.updates += 1;
        let delta = self.var.sub(&before);
        let mut q =
            IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n as u64);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        q.explanation = format!("ScaleCovariances n={:.0}", self.n);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "per-column variance used as a whitening scale",
                "Welford variances; a later transform divides by sqrt(var)",
                format!("||σ²||={:.6e}", before.norm()),
                format!("||σ²||={:.6e}", self.var.norm()),
            ),
        )
    }
}

impl Transform for ScaleCovariances {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(x.clone());
        }
        let z = Matrix::from_fn(x.nrows(), x.ncols(), |i, j| {
            let s = if j < self.var.len() {
                self.var[j].abs().sqrt().max(1e-12)
            } else {
                1.0
            };
            let m = if j < self.mean.len() { self.mean[j] } else { 0.0 };
            (x.get(i, j) - m) / s
        });
        ctx.finish(z)
    }
}

/// Named alias of [`AdaptiveModelRules`] (river `rules.VeryFastDecisionRulesClassifier`).
#[derive(Clone, Debug)]
pub struct VeryFastDecisionRules {
    inner: AdaptiveModelRules,
}

impl Default for VeryFastDecisionRules {
    fn default() -> Self {
        Self {
            inner: AdaptiveModelRules::default(),
        }
    }
}

impl VeryFastDecisionRules {
    /// Default VFDR wrapper.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for VeryFastDecisionRules {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

/// Supervised neural matrix factorisation (river `facto.SNMF`).
///
/// Factor count is not identification `p`. Needs `[user, item]` columns.
#[derive(Clone, Debug)]
pub struct Snmf {
    /// Embedding width.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    users: HashMap<i64, Vector>,
    items: HashMap<i64, Vector>,
    bu: HashMap<i64, f64>,
    bi: HashMap<i64, f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Snmf {
    fn default() -> Self {
        Self {
            n_factors: 2,
            learning_rate: 0.05,
            w0: 0.0,
            users: HashMap::new(),
            items: HashMap::new(),
            bu: HashMap::new(),
            bi: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Snmf {
    /// SNMF with `k` latent factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn score(&self, user: i64, item: i64) -> f64 {
        let k = self.n_factors.max(1);
        let mut dot = 0.0_f64;
        if let (Some(eu), Some(ei)) = (self.users.get(&user), self.items.get(&item)) {
            for f in 0..k.min(eu.len()).min(ei.len()) {
                dot += eu[f] * ei[f];
            }
        }
        self.w0
            + self.bu.get(&user).copied().unwrap_or(0.0)
            + self.bi.get(&item).copied().unwrap_or(0.0)
            + dot.max(0.0)
    }
}

impl PartialFit for Snmf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("Snmf needs [user, item] columns")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need two columns"),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("Snmf learning_rate was non-positive; using 0.05")
                    .build(),
            );
            0.05
        };
        let k = self.n_factors.max(1);
        let mut sse = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let user = x.get(i, 0).round() as i64;
            let item = x.get(i, 1).round() as i64;
            let mut eu = self
                .users
                .remove(&user)
                .unwrap_or_else(|| Vector::filled(k, 0.01));
            let mut ei = self
                .items
                .remove(&item)
                .unwrap_or_else(|| Vector::filled(k, 0.01));
            if eu.len() != k {
                eu = Vector::filled(k, 0.01);
            }
            if ei.len() != k {
                ei = Vector::filled(k, 0.01);
            }
            let mut dot = 0.0_f64;
            for f in 0..k {
                dot += eu[f] * ei[f];
            }
            let bu = self.bu.get(&user).copied().unwrap_or(0.0);
            let bi = self.bi.get(&item).copied().unwrap_or(0.0);
            let yhat = self.w0 + bu + bi + dot.max(0.0);
            let err = yhat - y[i];
            sse += err * err;
            let gact = if dot > 0.0 { 1.0 } else { 0.0 };
            self.w0 -= eta * err;
            self.bu.insert(user, bu - eta * err);
            self.bi.insert(item, bi - eta * err);
            for f in 0..k {
                let du = err * gact * ei[f];
                let di = err * gact * eu[f];
                eu[f] -= eta * du;
                ei[f] -= eta * di;
                moved += du.abs() + di.abs();
            }
            self.users.insert(user, eu);
            self.items.insert(item, ei);
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved.max(x.nrows() as f64));
        q.loss_after = Some(sse);
        q.still_identified = !self.users.is_empty() && !self.items.is_empty();
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "SNMF {} users × {} items, ||Δ||={moved:.6e}",
            self.users.len(),
            self.items.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("Snmf has seen fewer than two ratings")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "supervised neural matrix factorisation",
                "ReLU of user·item plus biases; k is not p",
                "previous embeddings",
                format!("{}×{}", self.users.len(), self.items.len()),
            ),
        )
    }
}

impl Predict for Snmf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("Snmf predict needs [user, item]")
                    .build(),
            );
            return ctx.finish(Vector::filled(x.nrows(), self.w0));
        }
        if self.users.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            self.score(x.get(i, 0).round() as i64, x.get(i, 1).round() as i64)
        })))
    }
}

/// Content-based recommender: hashed item features averaged into a user profile.
///
/// Hash width is not identification `p`. Needs `[user, item]` columns.
#[derive(Clone, Debug)]
pub struct ContentRecommender {
    profiles: HashMap<i64, Vector>,
    counts: HashMap<i64, f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for ContentRecommender {
    fn default() -> Self {
        Self {
            profiles: HashMap::new(),
            counts: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ContentRecommender {
    /// Empty content recommender.
    pub fn new() -> Self {
        Self::default()
    }

    fn item_feat(item: f64) -> Vector {
        Vector::from_iter((0..8).map(|d| {
            let h = mix_hash(item, d as u64);
            if h % 2 == 0 { 1.0 } else { -1.0 }
        }))
    }

    fn score(&self, user: i64, item: f64) -> f64 {
        let Some(p) = self.profiles.get(&user) else {
            return 0.0;
        };
        let z = Self::item_feat(item);
        let mut s = 0.0_f64;
        for j in 0..p.len().min(z.len()) {
            s += p[j] * z[j];
        }
        s
    }
}

impl PartialFit for ContentRecommender {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no ratings"),
            );
        };
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("ContentRecommender needs [user, item] columns")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need two columns"),
            );
        }
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let user = x.get(i, 0).round() as i64;
            let feat = Self::item_feat(x.get(i, 1));
            let n0 = self.counts.get(&user).copied().unwrap_or(0.0);
            let mut prof = self
                .profiles
                .remove(&user)
                .unwrap_or_else(|| Vector::zeros(8));
            if prof.len() != 8 {
                prof = Vector::zeros(8);
            }
            let n1 = n0 + 1.0;
            for j in 0..8 {
                let nxt = (prof[j] * n0 + y[i] * feat[j]) / n1;
                moved += (nxt - prof[j]).abs();
                prof[j] = nxt;
            }
            self.profiles.insert(user, prof);
            self.counts.insert(user, n1);
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(moved);
        q.information_gain = Some(moved.max(x.nrows() as f64));
        q.still_identified = !self.profiles.is_empty();
        q.warmup = self.profiles.is_empty();
        q.explanation = format!(
            "ContentRecommender {} user profiles, ||Δ||={moved:.6e}",
            self.profiles.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "content user profiles",
                "running mean of rating × hashed item features; hash width is not p",
                "previous profiles",
                format!("{} users", self.profiles.len()),
            ),
        )
    }
}

impl Predict for ContentRecommender {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .severity(Severity::Warning)
                    .message("ContentRecommender predict needs [user, item]")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        if self.profiles.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| self.score(x.get(i, 0).round() as i64, x.get(i, 1))),
        ))
    }
}

/// SGD with inverse-scaling step \(\eta_0 / t^{p}\) (river `optim.schedulers.InverseScaling`).
///
/// The exponent is not identification `p`.
#[derive(Clone, Debug)]
pub struct InverseScalingRegressor {
    /// Base step \(\eta_0\).
    pub eta0: f64,
    /// Decay exponent.
    pub power: f64,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for InverseScalingRegressor {
    fn default() -> Self {
        Self {
            eta0: 0.1,
            power: 0.5,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl InverseScalingRegressor {
    /// Inverse-scaling SGD with \(\eta_0\) and exponent `power`.
    pub fn new(eta0: f64, power: f64) -> Self {
        Self {
            eta0,
            power,
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for InverseScalingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("InverseScalingRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta0 = if self.eta0.is_finite() && self.eta0 > 0.0 {
            self.eta0
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("InverseScalingRegressor eta0 was non-positive; using 0.1")
                    .build(),
            );
            0.1
        };
        let pow = if self.power.is_finite() && self.power >= 0.0 {
            self.power
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("InverseScalingRegressor power was invalid; using 0.5")
                    .build(),
            );
            0.5
        };
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let t = self.n_seen.max(1) as f64;
            let eta = eta0 / t.powf(pow).max(1e-12);
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            let e1 = after - y[i];
            sse1 += e1 * e1;
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "inverse-scaling SGD",
            "η = η0 / t^power; the exponent is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for InverseScalingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with a cosine-annealed step (river `optim.schedulers.CosineAnnealing`).
///
/// Period length is not identification `p`.
#[derive(Clone, Debug)]
pub struct CosineAnnealingRegressor {
    /// Peak step.
    pub eta_max: f64,
    /// Floor step.
    pub eta_min: f64,
    /// Annealing period.
    pub period: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for CosineAnnealingRegressor {
    fn default() -> Self {
        Self {
            eta_max: 0.1,
            eta_min: 0.001,
            period: 8,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl CosineAnnealingRegressor {
    /// Cosine-annealed SGD with period `period`.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for CosineAnnealingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("CosineAnnealingRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let t_max = if self.eta_max.is_finite() && self.eta_max > 0.0 {
            self.eta_max
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("CosineAnnealingRegressor eta_max invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let t_min = if self.eta_min.is_finite() && self.eta_min >= 0.0 {
            self.eta_min.min(t_max)
        } else {
            0.001_f64.min(t_max)
        };
        let period = self.period.max(1);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let phase = ((self.n_seen - 1) as usize % period) as f64 / period as f64;
            let eta = t_min + 0.5 * (t_max - t_min) * (1.0 + (std::f64::consts::PI * phase).cos());
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            let e1 = after - y[i];
            sse1 += e1 * e1;
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "cosine-annealed SGD",
            "η follows a cosine over `period`; period is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for CosineAnnealingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// Polynomial factorisation machine (river `facto` 2-way polynomial FM).
///
/// Pair count is not identification `p`. A 1-column design has no pair term.
#[derive(Clone, Debug)]
pub struct PolynomialFm {
    /// SGD step.
    pub learning_rate: f64,
    w0: f64,
    w: Vector,
    pair: HashMap<(usize, usize), f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PolynomialFm {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            w0: 0.0,
            w: Vector::zeros(0),
            pair: HashMap::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PolynomialFm {
    /// Empty polynomial FM.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = x.ncols().min(self.w.len());
        let mut s = self.w0;
        for j in 0..p {
            s += self.w[j] * x.get(i, j);
        }
        for j in 0..p {
            for k in (j + 1)..p {
                let wjk = self.pair.get(&(j, k)).copied().unwrap_or(0.0);
                s += wjk * x.get(i, j) * x.get(i, k);
            }
        }
        s
    }
}

impl PartialFit for PolynomialFm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("PolynomialFm feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("PolynomialFm learning_rate was non-positive; using 0.05")
                    .build(),
            );
            0.05
        };
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.w0 -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let pp = x.ncols().min(self.w.len());
            for j in 0..pp {
                for k in (j + 1)..pp {
                    let xjk = x.get(i, j) * x.get(i, k);
                    let cur = self.pair.get(&(j, k)).copied().unwrap_or(0.0);
                    let nxt = cur - eta * err * xjk;
                    moved += (nxt - cur).abs();
                    self.pair.insert((j, k), nxt);
                }
            }
            let after = self.predict_row(x, i);
            let e1 = after - y[i];
            sse1 += e1 * e1;
            self.n_seen += 1;
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "polynomial FM",
            "SGD on 1 + linear + pairwise x_j x_k; pair count is not p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for PolynomialFm {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with linear decay \(\eta_0(1-t/T)\) (river `optim.schedulers.Linear`).
///
/// Period \(T\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct LinearDecayRegressor {
    /// Base step.
    pub eta0: f64,
    /// Decay period.
    pub period: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LinearDecayRegressor {
    fn default() -> Self {
        Self {
            eta0: 0.1,
            period: 12,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LinearDecayRegressor {
    /// Linear-decay SGD with period `period`.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for LinearDecayRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("LinearDecayRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta0 = if self.eta0.is_finite() && self.eta0 > 0.0 {
            self.eta0
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("LinearDecayRegressor eta0 invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let period = self.period.max(1);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let t = ((self.n_seen - 1) as usize % period) as f64 / period as f64;
            let eta = eta0 * (1.0 - t).max(0.01);
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "linear-decay SGD",
            "η = η0 (1 − t/T); period is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for LinearDecayRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with a triangular cyclic step (river `optim.schedulers.Cyclic`).
///
/// Period length is not identification `p`.
#[derive(Clone, Debug)]
pub struct CyclicLrRegressor {
    /// Peak step.
    pub eta_max: f64,
    /// Floor step.
    pub eta_min: f64,
    /// Cycle length.
    pub period: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for CyclicLrRegressor {
    fn default() -> Self {
        Self {
            eta_max: 0.1,
            eta_min: 0.01,
            period: 8,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl CyclicLrRegressor {
    /// Cyclic triangular SGD with period `period`.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for CyclicLrRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("CyclicLrRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let t_max = if self.eta_max.is_finite() && self.eta_max > 0.0 {
            self.eta_max
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("CyclicLrRegressor eta_max invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let t_min = if self.eta_min.is_finite() && self.eta_min >= 0.0 {
            self.eta_min.min(t_max)
        } else {
            0.01_f64.min(t_max)
        };
        let period = self.period.max(1);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let phase = ((self.n_seen - 1) as usize % period) as f64 / period as f64;
            let tri = 1.0 - (2.0 * phase - 1.0).abs();
            let eta = t_min + (t_max - t_min) * tri;
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "cyclic triangular SGD",
            "η follows a triangle over `period`; period is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for CyclicLrRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with polynomial decay \(\eta_0(1-t/T)^{p}\) (river `optim.schedulers.PolynomialDecay`).
///
/// Period and exponent are not identification `p`.
#[derive(Clone, Debug)]
pub struct PolynomialDecayRegressor {
    /// Base step.
    pub eta0: f64,
    /// Decay exponent.
    pub power: f64,
    /// Decay period \(T\).
    pub period: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PolynomialDecayRegressor {
    fn default() -> Self {
        Self {
            eta0: 0.1,
            power: 0.5,
            period: 12,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PolynomialDecayRegressor {
    /// Polynomial-decay SGD with period `period`.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for PolynomialDecayRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("PolynomialDecayRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta0 = if self.eta0.is_finite() && self.eta0 > 0.0 {
            self.eta0
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("PolynomialDecayRegressor eta0 invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let power = if self.power.is_finite() && self.power >= 0.0 {
            self.power
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("PolynomialDecayRegressor power invalid; using 0.5")
                    .build(),
            );
            0.5
        };
        let period = self.period.max(1);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let t = ((self.n_seen - 1) as usize % period) as f64 / period as f64;
            let eta = eta0 * (1.0 - t).max(0.01).powf(power);
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "polynomial-decay SGD",
            "η = η0 (1 − t/T)^power; period is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for PolynomialDecayRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with cosine warm restarts (river / SGDR `CosineAnnealingWarmRestarts`).
///
/// Restart lengths are not identification `p`.
#[derive(Clone, Debug)]
pub struct WarmRestartsRegressor {
    /// Peak step.
    pub eta_max: f64,
    /// Floor step.
    pub eta_min: f64,
    /// First cycle length.
    pub t0: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WarmRestartsRegressor {
    fn default() -> Self {
        Self {
            eta_max: 0.1,
            eta_min: 0.01,
            t0: 4,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WarmRestartsRegressor {
    /// Warm-restart SGD with first cycle `t0`.
    pub fn new(t0: usize) -> Self {
        Self {
            t0: t0.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for WarmRestartsRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("WarmRestartsRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let t_max = if self.eta_max.is_finite() && self.eta_max > 0.0 {
            self.eta_max
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("WarmRestartsRegressor eta_max invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let t_min = if self.eta_min.is_finite() && self.eta_min >= 0.0 {
            self.eta_min.min(t_max)
        } else {
            0.01_f64.min(t_max)
        };
        let t0 = self.t0.max(1) as u64;
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let mut consumed = 0u64;
            let mut tcur = t0;
            let n = self.n_seen;
            while n > consumed + tcur {
                consumed += tcur;
                tcur = tcur.saturating_mul(2).max(1);
            }
            let pos = n - 1 - consumed;
            let phase = pos as f64 / tcur as f64;
            let eta = t_min + 0.5 * (t_max - t_min) * (1.0 + (std::f64::consts::PI * phase).cos());
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "cosine warm-restart SGD",
            "η follows SGDR; restart lengths are not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for WarmRestartsRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// SGD with a one-cycle step (river / Smith 1cycle).
///
/// Cycle length is not identification `p`.
#[derive(Clone, Debug)]
pub struct OneCycleLrRegressor {
    /// Peak step.
    pub eta_max: f64,
    /// Floor step.
    pub eta_min: f64,
    /// Cycle length.
    pub period: usize,
    w: Vector,
    b: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OneCycleLrRegressor {
    fn default() -> Self {
        Self {
            eta_max: 0.1,
            eta_min: 0.01,
            period: 12,
            w: Vector::zeros(0),
            b: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OneCycleLrRegressor {
    /// One-cycle SGD with period `period`.
    pub fn new(period: usize) -> Self {
        Self {
            period: period.max(2),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b;
        for j in 0..x.ncols().min(self.w.len()) {
            s += self.w[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for OneCycleLrRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.w.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("OneCycleLrRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let t_max = if self.eta_max.is_finite() && self.eta_max > 0.0 {
            self.eta_max
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("OneCycleLrRegressor eta_max invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let t_min = if self.eta_min.is_finite() && self.eta_min >= 0.0 {
            self.eta_min.min(t_max)
        } else {
            0.01_f64.min(t_max)
        };
        let period = self.period.max(2);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err = yhat - y[i];
            sse0 += err * err;
            self.n_seen += 1;
            let t = ((self.n_seen - 1) as usize % period) as f64 / period as f64;
            let eta = if t < 0.3 {
                t_min + (t_max - t_min) * (t / 0.3)
            } else {
                t_max + (t_min - t_max) * ((t - 0.3) / 0.7)
            };
            self.b -= eta * err;
            for j in 0..x.ncols().min(self.w.len()) {
                let d = eta * err * x.get(i, j);
                self.w[j] -= d;
                moved += d.abs();
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "one-cycle SGD",
            "η rises then falls over `period`; period is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OneCycleLrRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// Rectified Adam (river `optim.RAdam`).
#[derive(Clone, Debug)]
pub struct RAdamRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for RAdamRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl RAdamRegressor {
    /// Default RAdam regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for RAdamRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("RAdamRegressor learning_rate invalid; using 0.05")
                    .build(),
            );
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        let rho_inf = 2.0 / (1.0 - b2).max(1e-12) - 1.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            let rho_t = rho_inf - 2.0 * self.t * b2.powf(self.t) / (1.0 - b2.powf(self.t)).max(1e-18);
            let rect = if rho_t > 4.0 {
                let num = (rho_t - 4.0) * (rho_t - 2.0) * rho_inf;
                let den = (rho_inf - 4.0) * (rho_inf - 2.0) * rho_t;
                (num / den.max(1e-18)).max(0.0).sqrt()
            } else {
                1.0
            };
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                if rho_t > 4.0 {
                    let vhat = self.v[j] / corr2.max(1e-18);
                    self.coef[j] -= eta * rect * mhat / (vhat.sqrt() + eps);
                } else {
                    self.coef[j] -= eta * mhat;
                }
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "RAdam linear weights",
            "rectified second-moment warmup; β is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for RAdamRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// LAMB (You et al.) trust-ratio Adam (river `optim.LAMB`).
#[derive(Clone, Debug)]
pub struct LambRegressor {
    /// Step \(\eta\).
    pub learning_rate: f64,
    /// First-moment decay.
    pub beta1: f64,
    /// Second-moment decay.
    pub beta2: f64,
    /// Diagonal floor.
    pub eps: f64,
    /// Prepend an intercept column.
    pub fit_intercept: bool,
    coef: Vector,
    m: Vector,
    v: Vector,
    t: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LambRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            beta1: 0.9,
            beta2: 0.999,
            eps: 1e-8,
            fit_intercept: true,
            coef: Vector::zeros(0),
            m: Vector::zeros(0),
            v: Vector::zeros(0),
            t: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LambRegressor {
    /// Default LAMB regressor.
    pub fn new() -> Self {
        Self::default()
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = row_aug(x, i, self.fit_intercept);
        let n = z.len().min(self.coef.len());
        let mut s = 0.0;
        for j in 0..n {
            s += self.coef[j] * z[j];
        }
        s
    }
}

impl PartialFit for LambRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let dim = online_linear_dim(self.fit_intercept, x.ncols());
        if !self.initialized {
            self.coef = Vector::zeros(dim);
            self.m = Vector::zeros(dim);
            self.v = Vector::zeros(dim);
            self.initialized = true;
        } else if self.coef.len() != dim {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let before = self.coef.clone();
        let eta = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("LambRegressor learning_rate invalid; using 0.05")
                    .build(),
            );
            0.05
        };
        let b1 = self.beta1.clamp(0.0, 0.999);
        let b2 = self.beta2.clamp(0.0, 0.999);
        let eps = if self.eps.is_finite() && self.eps > 0.0 {
            self.eps
        } else {
            1e-8
        };
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = row_aug(x, i, self.fit_intercept);
            let pred = self.predict_row(x, i);
            let err = pred - y[i];
            loss_before += err * err;
            self.t += 1.0;
            let corr1 = 1.0 - b1.powf(self.t);
            let corr2 = 1.0 - b2.powf(self.t);
            let mut u = Vector::zeros(dim);
            for j in 0..dim {
                let g = err * z[j];
                self.m[j] = b1 * self.m[j] + (1.0 - b1) * g;
                self.v[j] = b2 * self.v[j] + (1.0 - b2) * g * g;
                let mhat = self.m[j] / corr1.max(1e-18);
                let vhat = self.v[j] / corr2.max(1e-18);
                u[j] = mhat / (vhat.sqrt() + eps);
            }
            let wn = self.coef.norm();
            let un = u.norm();
            let trust = if wn > 1e-12 && un > 1e-12 {
                wn / un
            } else {
                1.0
            };
            for j in 0..dim {
                self.coef[j] -= eta * trust * u[j];
            }
            let after = self.predict_row(x, i);
            let ea = after - y[i];
            loss_after += ea * ea;
            self.n_seen += 1;
        }
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen >= dim as u64,
            self.n_seen < 4,
            "LAMB linear weights",
            "Adam direction scaled by ||w||/||u||; the trust ratio is not p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for LambRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// Named Hoeffding tree classifier (river `tree.HoeffdingTreeClassifier`).
#[derive(Clone, Debug)]
pub struct HoeffdingTreeClassifier {
    inner: HoeffdingTree,
}

impl Default for HoeffdingTreeClassifier {
    fn default() -> Self {
        Self {
            inner: HoeffdingTree::default(),
        }
    }
}

impl HoeffdingTreeClassifier {
    /// Default VFDT classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HoeffdingTreeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for HoeffdingTreeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named adaptive random forest classifier (river `ensemble.AdaptiveRandomForestClassifier`).
#[derive(Clone, Debug)]
pub struct AdaptiveRandomForestClassifier {
    inner: AdaptiveRandomForest,
}

impl Default for AdaptiveRandomForestClassifier {
    fn default() -> Self {
        Self {
            inner: AdaptiveRandomForest::default(),
        }
    }
}

impl AdaptiveRandomForestClassifier {
    /// Default ARF classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for AdaptiveRandomForestClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AdaptiveRandomForestClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Lookahead SGD (Zhang et al. / river `optim.Lookahead`).
///
/// Sync period is not identification `p`.
#[derive(Clone, Debug)]
pub struct LookaheadRegressor {
    /// Inner step.
    pub eta: f64,
    /// Slow-weight blend \(\alpha\).
    pub alpha: f64,
    /// Sync every `k` rows.
    pub k: usize,
    fast: Vector,
    slow: Vector,
    b_fast: f64,
    b_slow: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LookaheadRegressor {
    fn default() -> Self {
        Self {
            eta: 0.1,
            alpha: 0.5,
            k: 3,
            fast: Vector::zeros(0),
            slow: Vector::zeros(0),
            b_fast: 0.0,
            b_slow: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LookaheadRegressor {
    /// Lookahead with sync period `k`.
    pub fn new(k: usize) -> Self {
        Self {
            k: k.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b_slow;
        for j in 0..x.ncols().min(self.slow.len()) {
            s += self.slow[j] * x.get(i, j);
        }
        s
    }

    fn predict_fast(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.b_fast;
        for j in 0..x.ncols().min(self.fast.len()) {
            s += self.fast[j] * x.get(i, j);
        }
        s
    }
}

impl PartialFit for LookaheadRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no target"),
            );
        };
        let p = x.ncols().max(1);
        if !self.initialized {
            self.fast = Vector::zeros(p);
            self.slow = Vector::zeros(p);
            self.initialized = true;
        } else if self.fast.len() != p {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message("LookaheadRegressor feature dim changed")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "feature space changed",
                ),
            );
        }
        let eta = if self.eta.is_finite() && self.eta > 0.0 {
            self.eta
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("LookaheadRegressor eta invalid; using 0.1")
                    .build(),
            );
            0.1
        };
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 && self.alpha <= 1.0 {
            self.alpha
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("LookaheadRegressor alpha invalid; using 0.5")
                    .build(),
            );
            0.5
        };
        let k = self.k.max(1);
        let mut sse0 = 0.0_f64;
        let mut sse1 = 0.0_f64;
        let mut moved = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yhat = self.predict_row(x, i);
            let err_slow = yhat - y[i];
            sse0 += err_slow * err_slow;
            let err = self.predict_fast(x, i) - y[i];
            self.b_fast -= eta * err;
            for j in 0..x.ncols().min(self.fast.len()) {
                let d = eta * err * x.get(i, j);
                self.fast[j] -= d;
                moved += d.abs();
            }
            self.n_seen += 1;
            if self.n_seen % k as u64 == 0 {
                self.b_slow = (1.0 - alpha) * self.b_slow + alpha * self.b_fast;
                for j in 0..self.slow.len().min(self.fast.len()) {
                    self.slow[j] = (1.0 - alpha) * self.slow[j] + alpha * self.fast[j];
                }
            }
            let after = self.predict_row(x, i);
            sse1 += (after - y[i]) * (after - y[i]);
        }
        self.updates += 1;
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &Vector::filled(1, moved),
            sse0,
            sse1,
            self.n_seen >= 2,
            self.n_seen < 2,
            "lookahead SGD",
            "slow weights track fast SGD every k rows; k is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for LookaheadRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.predict_row(x, i))))
    }
}

/// Named ALMA classifier (river `linear_model.ALMAClassifier`).
#[derive(Clone, Debug)]
pub struct AlmaClassifier {
    inner: Alma,
}

impl Default for AlmaClassifier {
    fn default() -> Self {
        Self {
            inner: Alma::default(),
        }
    }
}

impl AlmaClassifier {
    /// Default ALMA classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for AlmaClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AlmaClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named HAT classifier (river `tree.HoeffdingAdaptiveTreeClassifier`).
#[derive(Clone, Debug)]
pub struct HoeffdingAdaptiveTreeClassifier {
    inner: HoeffdingAdaptiveTree,
}

impl Default for HoeffdingAdaptiveTreeClassifier {
    fn default() -> Self {
        Self {
            inner: HoeffdingAdaptiveTree::default(),
        }
    }
}

impl HoeffdingAdaptiveTreeClassifier {
    /// Default HAT classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HoeffdingAdaptiveTreeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for HoeffdingAdaptiveTreeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named EFDT classifier (river `tree.ExtremelyFastDecisionTreeClassifier`).
#[derive(Clone, Debug)]
pub struct ExtremelyFastDecisionTreeClassifier {
    inner: ExtremelyFastDecisionTree,
}

impl Default for ExtremelyFastDecisionTreeClassifier {
    fn default() -> Self {
        Self {
            inner: ExtremelyFastDecisionTree::default(),
        }
    }
}

impl ExtremelyFastDecisionTreeClassifier {
    /// Default EFDT classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ExtremelyFastDecisionTreeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for ExtremelyFastDecisionTreeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named SRP classifier (river `ensemble.SRPClassifier`).
#[derive(Clone, Debug)]
pub struct StreamingRandomPatchesClassifier {
    inner: StreamingRandomPatches,
}

impl Default for StreamingRandomPatchesClassifier {
    fn default() -> Self {
        Self {
            inner: StreamingRandomPatches::default(),
        }
    }
}

impl StreamingRandomPatchesClassifier {
    /// Default SRP classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for StreamingRandomPatchesClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for StreamingRandomPatchesClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named leveraging-bagging classifier (river `ensemble.LeveragingBaggingClassifier`).
#[derive(Clone, Debug)]
pub struct LeveragingBaggingClassifier {
    inner: LeveragingBagging,
}

impl Default for LeveragingBaggingClassifier {
    fn default() -> Self {
        Self {
            inner: LeveragingBagging::default(),
        }
    }
}

impl LeveragingBaggingClassifier {
    /// Default leveraging-bagging classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for LeveragingBaggingClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for LeveragingBaggingClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named ADWIN bagging classifier (river `ensemble.ADWINBaggingClassifier`).
#[derive(Clone, Debug)]
pub struct AdwinBaggingClassifier {
    inner: AdwinBagging,
}

impl Default for AdwinBaggingClassifier {
    fn default() -> Self {
        Self {
            inner: AdwinBagging::default(),
        }
    }
}

impl AdwinBaggingClassifier {
    /// Default ADWIN-bagging classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for AdwinBaggingClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AdwinBaggingClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named softmax classifier (river `linear_model.SoftmaxRegression`).
#[derive(Clone, Debug, Default)]
pub struct SoftmaxClassifier {
    inner: SoftmaxRegression,
}

impl SoftmaxClassifier {
    /// Empty softmax classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for SoftmaxClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for SoftmaxClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named Hoeffding tree regressor (river `tree.HoeffdingTreeRegressor`).
#[derive(Clone, Debug, Default)]
pub struct HoeffdingTreeRegressor {
    inner: HoeffdingRegressor,
}

impl HoeffdingTreeRegressor {
    /// Default Hoeffding regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HoeffdingTreeRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for HoeffdingTreeRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named ARF regressor (river `forest.ARFRegressor`).
#[derive(Clone, Debug)]
pub struct ARFRegressor {
    inner: AdaptiveRandomForestRegressor,
}

impl Default for ARFRegressor {
    fn default() -> Self {
        Self {
            inner: AdaptiveRandomForestRegressor::default(),
        }
    }
}

impl ARFRegressor {
    /// Forest with `n_estimators` RLS leaves.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            inner: AdaptiveRandomForestRegressor::new(n_estimators),
        }
    }
}

impl PartialFit for ARFRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for ARFRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named SRP regressor (river `ensemble.SRPRegressor`).
#[derive(Clone, Debug)]
pub struct StreamingRandomPatchesRegressor {
    inner: SrpRegressor,
}

impl Default for StreamingRandomPatchesRegressor {
    fn default() -> Self {
        Self {
            inner: SrpRegressor::default(),
        }
    }
}

impl StreamingRandomPatchesRegressor {
    /// Forest with `n_estimators` patch trees.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            inner: SrpRegressor::new(n_estimators),
        }
    }
}

impl PartialFit for StreamingRandomPatchesRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for StreamingRandomPatchesRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Eager Hoeffding regressor (river `tree.ExtremelyFastDecisionTreeRegressor` lite).
///
/// Split-threshold / sample-count are not identification `p`.
#[derive(Clone, Debug)]
pub struct ExtremelyFastDecisionTreeRegressor {
    tree: HoeffdingRegressor,
}

impl Default for ExtremelyFastDecisionTreeRegressor {
    fn default() -> Self {
        let mut tree = HoeffdingRegressor::new();
        tree.min_samples = 5;
        tree.tau = 0.15;
        Self { tree }
    }
}

impl ExtremelyFastDecisionTreeRegressor {
    /// Eager VFDT regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for ExtremelyFastDecisionTreeRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let q = self.tree.partial_fit(x, y, session)?;
        let mut ctx = FitCtx::with_session(session.child("efdtr"));
        ctx.push(
            Issue::builder(IssueCode::JitterInjected)
                .severity(Severity::Advisory)
                .message("EFDT-regressor uses an eager Hoeffding bound; it does not re-evaluate splits")
                .build(),
        );
        ctx.finish(q.value)
    }
}

impl Predict for ExtremelyFastDecisionTreeRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.tree.predict(x, session)
    }
}

/// Leveraging bagging of Hoeffding regressors (river `ensemble.LeveragingBaggingRegressor`).
///
/// Estimator count is not identification `p`.
#[derive(Clone, Debug)]
pub struct LeveragingBaggingRegressor {
    /// Number of trees. Not identification `p`.
    pub n_estimators: usize,
    /// Poisson mean of the leverage weights. Not identification `p`.
    pub w: f64,
    trees: Vec<HoeffdingRegressor>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for LeveragingBaggingRegressor {
    fn default() -> Self {
        Self {
            n_estimators: 3,
            w: 6.0,
            trees: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(29),
        }
    }
}

impl LeveragingBaggingRegressor {
    /// Forest with `n_estimators` Hoeffding regressors.
    pub fn new(n_estimators: usize) -> Self {
        Self {
            n_estimators: n_estimators.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for LeveragingBaggingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            self.trees = (0..self.n_estimators.max(1))
                .map(|_| HoeffdingRegressor::new())
                .collect();
            self.initialized = true;
        }
        let mut n_updates = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            let xi = Matrix::from_fn(1, x.ncols(), |_, j| x.get(i, j));
            let yi = Vector::from_slice(&[y[i]]);
            for t in 0..self.trees.len() {
                let k = self.rng.poisson(self.w.max(0.0));
                n_updates += k;
                for _ in 0..k {
                    let _ = self.trees[t].partial_fit(&xi, Some(&yi), &session.child("lbagr"));
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates - 1, x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_updates as f64);
        q.information_gain = Some(n_updates as f64);
        q.still_identified = self.n_seen >= 15;
        q.warmup = self.n_seen < 15;
        q.explanation = format!(
            "LeveragingBaggingRegressor: {} trees, {n_updates} Poisson-weighted updates",
            self.trees.len()
        );
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("leveraging bagging regressor still warming up")
                    .build(),
            );
        }
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_updates} Poisson(w={}) regressor updates", self.w),
                "each row is replayed k~Poisson(w) times into every Hoeffding regressor",
                "pre-batch forest",
                "post-batch forest",
            ),
        )
    }
}

impl Predict for LeveragingBaggingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.trees.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.trees.len() as f64;
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0_f64;
            for t in &self.trees {
                s += t.predict_one(x, i);
            }
            s / n_t.max(1.0)
        }));
        ctx.finish(out)
    }
}

/// ADWIN bagging of Hoeffding regressors (river `ensemble.ADWINBaggingRegressor`).
///
/// Member count is not identification `p`.
#[derive(Clone, Debug)]
pub struct AdwinBaggingRegressor {
    /// Members. Not identification `p`.
    pub n_models: usize,
    models: Vec<HoeffdingRegressor>,
    detectors: Vec<Adwin>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for AdwinBaggingRegressor {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            detectors: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(31),
        }
    }
}

impl AdwinBaggingRegressor {
    /// Bag of `n_models` Hoeffding regressors.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for AdwinBaggingRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.models.is_empty() {
            self.models = (0..self.n_models.max(1))
                .map(|_| HoeffdingRegressor::new())
                .collect();
            self.detectors = (0..self.models.len()).map(|_| Adwin::new(0.002)).collect();
        }
        let before = self.n_seen;
        let mut resets = 0u64;
        for m in 0..self.models.len() {
            let mut rows: Vec<usize> = Vec::new();
            for i in 0..x.nrows().min(y.len()) {
                let k = self.rng.poisson(1.0) as usize;
                for _ in 0..k {
                    rows.push(i);
                }
            }
            if rows.is_empty() && x.nrows() > 0 {
                rows.push(0);
            }
            if rows.is_empty() {
                continue;
            }
            let xb = Matrix::from_fn(rows.len(), x.ncols(), |r, c| x.get(rows[r], c));
            let yb = Vector::from_iter(rows.iter().map(|&i| y[i]));
            let _ = self.models[m].partial_fit(&xb, Some(&yb), &session.child(format!("abagr_{m}")));
            let pred = match self.models[m].predict(&xb, &session.child(format!("abagrp_{m}"))) {
                Ok(q) => q.value,
                Err(_) => Vector::zeros(xb.nrows()),
            };
            let mut err = 0.0_f64;
            for i in 0..yb.len().min(pred.len()) {
                err += (pred[i] - yb[i]).abs();
            }
            err /= yb.len().max(1) as f64;
            if let Ok(q) = self.detectors[m].update(err, &session.child(format!("adwinr_{m}"))) {
                if matches!(q.value, DriftDecision::Drift { .. }) {
                    self.models[m] = HoeffdingRegressor::new();
                    self.detectors[m].reset();
                    resets += 1;
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(resets as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AdwinBaggingRegressor {} trees, resets={resets}",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ADWIN bagging regressor update",
                "Poisson bootstrap plus per-member ADWIN reset",
                format!("n={before}"),
                format!("n={} resets={resets}", self.n_seen),
            ),
        )
    }
}

impl Predict for AdwinBaggingRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.models.len() as f64;
        let out = Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0_f64;
            for t in &self.models {
                s += t.predict_one(x, i);
            }
            s / n_t.max(1.0)
        }));
        ctx.finish(out)
    }
}

/// Named HAT regressor (river `tree.HoeffdingAdaptiveTreeRegressor`).
#[derive(Clone, Debug, Default)]
pub struct HATRegressor {
    inner: HoeffdingAdaptiveTreeRegressor,
}

impl HATRegressor {
    /// Default HAT regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for HATRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for HATRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named streaming AdaBoost (river `ensemble.AdaBoostClassifier`).
#[derive(Clone, Debug, Default)]
pub struct AdaBoostClassifier {
    inner: OnlineAdaBoost,
}

impl AdaBoostClassifier {
    /// Default online AdaBoost.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for AdaBoostClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AdaBoostClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named streaming bagging (river `ensemble.BaggingClassifier`).
#[derive(Clone, Debug, Default)]
pub struct BaggingClassifier {
    inner: OnlineBagging,
}

impl BaggingClassifier {
    /// Default online bagging.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for BaggingClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for BaggingClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Poisson bag of Mondrian trees (river `forest.MondrianForestClassifier`).
///
/// Member count is not identification `p`. Each tree is the existing
/// [`MondrianTree`]; Poisson(\(w\)) replay diversifies the random cut.
#[derive(Clone, Debug)]
pub struct MondrianForest {
    /// Trees. Not identification `p`.
    pub n_trees: usize,
    /// Poisson leverage.
    pub w: f64,
    trees: Vec<MondrianTree>,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for MondrianForest {
    fn default() -> Self {
        Self {
            n_trees: 3,
            w: 1.0,
            trees: Vec::new(),
            n_seen: 0,
            updates: 0,
            rng: Rng::new(0x4d07_f04e),
        }
    }
}

impl MondrianForest {
    /// Forest of `n_trees` Mondrian classifiers.
    pub fn new(n_trees: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for MondrianForest {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.trees.is_empty() {
            self.trees = (0..self.n_trees.max(1))
                .map(|_| MondrianTree::new())
                .collect();
        }
        let before = self.n_seen;
        let mut n_updates = 0u64;
        let w = if self.w.is_finite() && self.w > 0.0 {
            self.w
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("MondrianForest w is not a positive finite; using 1")
                    .build(),
            );
            1.0
        };
        for (m, tree) in self.trees.iter_mut().enumerate() {
            let k = self.rng.poisson(w) as usize;
            for _ in 0..k.max(1) {
                let _ = tree.partial_fit(x, Some(y), &session.child(format!("mf_{m}")));
                n_updates += 1;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_updates as f64);
        q.information_gain = Some(n_updates as f64);
        q.still_identified = self.n_seen >= 8;
        q.warmup = self.n_seen < 8;
        q.explanation = format!(
            "MondrianForest: {} trees, {n_updates} Poisson-weighted updates",
            self.trees.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("{n_updates} Poisson(w={w}) Mondrian updates"),
                "each tree is a range-tracking Mondrian stump; Poisson replay diversifies the cut",
                format!("n={before}"),
                format!("n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for MondrianForest {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.trees.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.trees.len() as f64;
        let mut acc = vec![0.0; x.nrows()];
        for (m, tree) in self.trees.iter().enumerate() {
            match tree.predict(x, &session.child(format!("mfp_{m}"))) {
                Ok(q) => {
                    for i in 0..x.nrows().min(q.value.len()) {
                        acc[i] += q.value[i];
                    }
                }
                Err(_) => {}
            }
        }
        ctx.finish(Vector::from_iter(acc.iter().map(|v| {
            if *v / n_t.max(1.0) >= 0.5 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Named Mondrian forest classifier.
#[derive(Clone, Debug)]
pub struct MondrianForestClassifier {
    inner: MondrianForest,
}

impl Default for MondrianForestClassifier {
    fn default() -> Self {
        Self {
            inner: MondrianForest::default(),
        }
    }
}

impl MondrianForestClassifier {
    /// Forest of `n_trees` Mondrian classifiers.
    pub fn new(n_trees: usize) -> Self {
        Self {
            inner: MondrianForest::new(n_trees),
        }
    }
}

impl PartialFit for MondrianForestClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for MondrianForestClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

#[derive(Clone, Debug)]
struct MondrianRegTree {
    mins: Vector,
    maxs: Vector,
    split_j: Option<usize>,
    threshold: f64,
    leaf_sum: f64,
    leaf_n: f64,
    left_sum: f64,
    left_n: f64,
    right_sum: f64,
    right_n: f64,
    n_seen: u64,
}

impl MondrianRegTree {
    fn new() -> Self {
        Self {
            mins: Vector::zeros(0),
            maxs: Vector::zeros(0),
            split_j: None,
            threshold: 0.0,
            leaf_sum: 0.0,
            leaf_n: 0.0,
            left_sum: 0.0,
            left_n: 0.0,
            right_sum: 0.0,
            right_n: 0.0,
            n_seen: 0,
        }
    }

    fn mean(sum: f64, n: f64) -> f64 {
        if n > 0.0 {
            sum / n
        } else {
            0.0
        }
    }

    fn update(&mut self, x: &Matrix, i: usize, yi: f64, rng: &mut Rng) {
        if !yi.is_finite() {
            return;
        }
        if self.mins.is_empty() && x.ncols() > 0 {
            self.mins = Vector::from_iter((0..x.ncols()).map(|j| x.get(i, j)));
            self.maxs = self.mins.clone();
        }
        for j in 0..x.ncols().min(self.mins.len()) {
            let v = x.get(i, j);
            if v < self.mins[j] {
                self.mins[j] = v;
            }
            if v > self.maxs[j] {
                self.maxs[j] = v;
            }
        }
        if let Some(j) = self.split_j {
            if x.get(i, j) <= self.threshold {
                self.left_sum += yi;
                self.left_n += 1.0;
            } else {
                self.right_sum += yi;
                self.right_n += 1.0;
            }
        } else {
            self.leaf_sum += yi;
            self.leaf_n += 1.0;
        }
        self.n_seen += 1;
        if self.split_j.is_none() && self.n_seen >= 8 && !self.mins.is_empty() {
            let mut cand = Vec::new();
            for j in 0..self.mins.len() {
                if self.maxs[j] - self.mins[j] > 1e-12 {
                    cand.push(j);
                }
            }
            if !cand.is_empty() {
                let j = cand[rng.below(cand.len())];
                let span = self.maxs[j] - self.mins[j];
                self.threshold = self.mins[j] + rng.uniform() * span;
                self.split_j = Some(j);
                self.left_sum = self.leaf_sum;
                self.left_n = self.leaf_n;
                self.right_sum = 0.0;
                self.right_n = 0.0;
            }
        }
    }

    fn predict_one(&self, x: &Matrix, i: usize) -> f64 {
        match self.split_j {
            Some(j) if j < x.ncols() => {
                if x.get(i, j) <= self.threshold {
                    Self::mean(self.left_sum, self.left_n)
                } else {
                    Self::mean(self.right_sum, self.right_n)
                }
            }
            _ => Self::mean(self.leaf_sum, self.leaf_n),
        }
    }
}

/// Poisson bag of Mondrian regressor trees (river `forest.MondrianForestRegressor`).
///
/// Member count is not identification `p`. Distinct from [`AmfRegressor`]
/// (widest-range stump) by using a delayed random cut and Poisson bagging.
#[derive(Clone, Debug)]
pub struct MondrianForestRegressor {
    /// Trees. Not identification `p`.
    pub n_trees: usize,
    trees: Vec<MondrianRegTree>,
    detector: Adwin,
    n_seen: u64,
    updates: u64,
    resets: u64,
    rng: Rng,
}

impl Default for MondrianForestRegressor {
    fn default() -> Self {
        Self {
            n_trees: 3,
            trees: Vec::new(),
            detector: Adwin::new(0.002),
            n_seen: 0,
            updates: 0,
            resets: 0,
            rng: Rng::new(0x4d07_a4f),
        }
    }
}

impl MondrianForestRegressor {
    /// Forest of `n_trees` Mondrian regressors.
    pub fn new(n_trees: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for MondrianForestRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if self.trees.is_empty() {
            self.trees = (0..self.n_trees.max(1))
                .map(|_| MondrianRegTree::new())
                .collect();
        } else if !self.trees[0].mins.is_empty() && self.trees[0].mins.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.n_seen;
        let mut n_updates = 0u64;
        for tree in &mut self.trees {
            for i in 0..x.nrows().min(y.len()) {
                let k = self.rng.poisson(1.0) as usize;
                for _ in 0..k.max(1) {
                    tree.update(x, i, y[i], &mut self.rng);
                    n_updates += 1;
                }
            }
        }
        let mut mae = 0.0_f64;
        let mut n_mae = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let mut s = 0.0;
            for t in &self.trees {
                s += t.predict_one(x, i);
            }
            mae += (s / self.trees.len().max(1) as f64 - y[i]).abs();
            n_mae += 1.0;
        }
        if n_mae > 0.0 {
            if let Ok(d) = self.detector.update(mae / n_mae, &session.child("amf_adwin")) {
                if matches!(d.value, DriftDecision::Drift { .. }) {
                    self.trees = (0..self.trees.len())
                        .map(|_| MondrianRegTree::new())
                        .collect();
                    self.detector.reset();
                    self.resets += 1;
                    ctx.push(
                        Issue::builder(IssueCode::ConceptDriftDetected)
                            .message("MondrianForestRegressor reset after an ADWIN cut on MAE")
                            .build(),
                    );
                }
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(n_updates as f64 + self.resets as f64);
        q.information_gain = Some(n_updates as f64);
        q.still_identified = self.n_seen >= 8;
        q.warmup = self.n_seen < 8;
        q.explanation = format!(
            "MondrianForestRegressor {} trees, updates={n_updates} resets={}",
            self.trees.len(),
            self.resets
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Mondrian regressor forest update",
                "Poisson bag of range-tracking mean trees; ADWIN resets on MAE",
                format!("n={before}"),
                format!("n={} resets={}", self.n_seen, self.resets),
            ),
        )
    }
}

impl Predict for MondrianForestRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.trees.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let n_t = self.trees.len() as f64;
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0;
            for t in &self.trees {
                s += t.predict_one(x, i);
            }
            s / n_t.max(1.0)
        })))
    }
}

/// Named aggregated Mondrian forest regressor (river `forest.AMFRegressor`).
#[derive(Clone, Debug, Default)]
pub struct AMFRegressor {
    inner: AmfRegressor,
}

impl AMFRegressor {
    /// AMF with `n_trees` Mondrian stumps.
    pub fn new(n_trees: usize) -> Self {
        Self {
            inner: AmfRegressor::new(n_trees),
        }
    }
}

impl PartialFit for AMFRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AMFRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

#[derive(Clone, Debug)]
enum SgtNode {
    Leaf(SgtLeaf),
    Split {
        feature: usize,
        threshold: f64,
        left: Box<SgtNode>,
        right: Box<SgtNode>,
    },
}

#[derive(Clone, Debug)]
struct SgtLeaf {
    pred: f64,
    g: f64,
    h: f64,
    n: f64,
    bins: Vec<SgtBin>,
}

#[derive(Clone, Debug, Default)]
struct SgtBin {
    gx: Gauss,
    lg: f64,
    lh: f64,
    rg: f64,
    rh: f64,
}

impl SgtLeaf {
    fn new(p: usize, pred: f64) -> Self {
        Self {
            pred,
            g: 0.0,
            h: 0.0,
            n: 0.0,
            bins: vec![SgtBin::default(); p],
        }
    }
}

fn sgt_gain(gl: f64, hl: f64, gr: f64, hr: f64, g: f64, h: f64, lambda: f64) -> f64 {
    let sl = gl * gl / (hl + lambda).max(1e-12);
    let sr = gr * gr / (hr + lambda).max(1e-12);
    let sp = g * g / (h + lambda).max(1e-12);
    0.5 * (sl + sr - sp)
}

fn sgt_push(leaf: &mut SgtLeaf, x: &Matrix, i: usize, gi: f64, hi: f64) {
    leaf.g += gi;
    leaf.h += hi;
    leaf.n += 1.0;
    for j in 0..x.ncols().min(leaf.bins.len()) {
        let xv = x.get(i, j);
        let t = if leaf.bins[j].gx.n < 1.0 {
            xv
        } else {
            leaf.bins[j].gx.mean
        };
        if xv <= t {
            leaf.bins[j].lg += gi;
            leaf.bins[j].lh += hi;
        } else {
            leaf.bins[j].rg += gi;
            leaf.bins[j].rh += hi;
        }
        leaf.bins[j].gx.push(xv);
    }
}

fn sgt_maybe_split(leaf: &SgtLeaf, min_samples: usize, lambda: f64, tau: f64) -> Option<(usize, f64, f64)> {
    if (leaf.n as usize) < min_samples {
        return None;
    }
    let mut best = (0usize, 0.0, -1.0);
    for (j, bin) in leaf.bins.iter().enumerate() {
        if bin.lh < 1e-8 || bin.rh < 1e-8 {
            continue;
        }
        let gain = sgt_gain(bin.lg, bin.lh, bin.rg, bin.rh, leaf.g, leaf.h, lambda);
        if gain > best.2 {
            best = (j, bin.gx.mean, gain);
        }
    }
    if best.2 > tau {
        Some((best.0, best.1, best.2))
    } else {
        None
    }
}

fn sgt_update(
    node: &mut SgtNode,
    x: &Matrix,
    i: usize,
    gi: f64,
    hi: f64,
    min_samples: usize,
    lambda: f64,
    tau: f64,
    lr: f64,
) -> Option<f64> {
    match node {
        SgtNode::Split {
            feature,
            threshold,
            left,
            right,
        } => {
            let v = if *feature < x.ncols() {
                x.get(i, *feature)
            } else {
                0.0
            };
            if v <= *threshold {
                sgt_update(left, x, i, gi, hi, min_samples, lambda, tau, lr)
            } else {
                sgt_update(right, x, i, gi, hi, min_samples, lambda, tau, lr)
            }
        }
        SgtNode::Leaf(leaf) => {
            sgt_push(leaf, x, i, gi, hi);
            leaf.pred -= lr * gi / hi.max(1e-8);
            if let Some((j, t, gain)) = sgt_maybe_split(leaf, min_samples, lambda, tau) {
                let pred = leaf.pred;
                let p = leaf.bins.len();
                *node = SgtNode::Split {
                    feature: j,
                    threshold: t,
                    left: Box::new(SgtNode::Leaf(SgtLeaf::new(p, pred))),
                    right: Box::new(SgtNode::Leaf(SgtLeaf::new(p, pred))),
                };
                Some(gain)
            } else {
                None
            }
        }
    }
}

fn sgt_predict(node: &SgtNode, x: &Matrix, i: usize) -> f64 {
    match node {
        SgtNode::Leaf(l) => l.pred,
        SgtNode::Split {
            feature,
            threshold,
            left,
            right,
        } => {
            let v = if *feature < x.ncols() {
                x.get(i, *feature)
            } else {
                0.0
            };
            if v <= *threshold {
                sgt_predict(left, x, i)
            } else {
                sgt_predict(right, x, i)
            }
        }
    }
}

/// Streaming gradient tree regressor (river `tree.SGTRegressor`).
///
/// Leaves accumulate squared-error gradient / Hessian bins and split on
/// XGBoost-style gain. Split / bin counts are not identification `p`.
/// Computationally distinct from the single-leaf [`SgtRegressor`].
#[derive(Clone, Debug)]
pub struct StreamingGradientTreeRegressor {
    /// Newton / SGD step on the leaf score.
    pub learning_rate: f64,
    /// L2 on the Hessian denominator.
    pub lambda: f64,
    /// Minimum observations before a split. Not identification `p`.
    pub min_samples: usize,
    /// Minimum gain to split.
    pub tau: f64,
    root: SgtNode,
    n_seen: u64,
    updates: u64,
    n_features: usize,
    initialized: bool,
    last_gain: f64,
}

impl Default for StreamingGradientTreeRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            lambda: 1.0,
            min_samples: 8,
            tau: 1e-4,
            root: SgtNode::Leaf(SgtLeaf::new(0, 0.0)),
            n_seen: 0,
            updates: 0,
            n_features: 0,
            initialized: false,
            last_gain: 0.0,
        }
    }
}

impl StreamingGradientTreeRegressor {
    /// Default streaming gradient tree.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for StreamingGradientTreeRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "SGT needs y"),
            );
        };
        if self.initialized && self.n_features != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        if !self.initialized {
            self.n_features = x.ncols();
            self.root = SgtNode::Leaf(SgtLeaf::new(x.ncols(), 0.0));
            self.initialized = true;
        }
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("StreamingGradientTreeRegressor learning_rate is not a positive finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let before = self.n_seen;
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let mut splits = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let pred = sgt_predict(&self.root, x, i);
            let e0 = pred - y[i];
            loss_before += e0 * e0;
            let gi = pred - y[i];
            let hi = 1.0;
            if sgt_update(
                &mut self.root,
                x,
                i,
                gi,
                hi,
                self.min_samples.max(2),
                self.lambda.max(1e-8),
                self.tau.max(0.0),
                lr,
            )
            .is_some()
            {
                splits += 1;
            }
            let pred1 = sgt_predict(&self.root, x, i);
            let e1 = pred1 - y[i];
            loss_after += e1 * e1;
            self.n_seen += 1;
        }
        self.updates += 1;
        self.last_gain = splits as f64;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(splits as f64);
        q.information_gain = Some((loss_before - loss_after).abs());
        q.loss_before = Some(loss_before);
        q.loss_after = Some(loss_after);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 8;
        q.explanation = format!("StreamingGradientTreeRegressor splits={splits} n={}", self.n_seen);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SGT squared-error split / leaf Newton",
                "leaves accumulate g=pred−y, h=1 and split on ½(G²/(H+λ)) gain",
                format!("n={before}"),
                format!("n={} splits={splits}", self.n_seen),
            ),
        )
    }
}

impl Predict for StreamingGradientTreeRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter(
            (0..x.nrows()).map(|i| sgt_predict(&self.root, x, i)),
        ))
    }
}

/// Streaming gradient tree classifier (river `tree.SGTClassifier`).
///
/// Log-loss gradient \(p-y\) and Hessian \(p(1-p)\) drive the same split
/// machinery as [`StreamingGradientTreeRegressor`]. Distinct from the
/// single-logit [`SgtClassifier`].
#[derive(Clone, Debug)]
pub struct StreamingGradientTreeClassifier {
    /// Newton step on the leaf logit.
    pub learning_rate: f64,
    /// L2 on the Hessian denominator.
    pub lambda: f64,
    /// Minimum observations before a split. Not identification `p`.
    pub min_samples: usize,
    /// Minimum gain to split.
    pub tau: f64,
    root: SgtNode,
    n_seen: u64,
    updates: u64,
    n_features: usize,
    initialized: bool,
}

impl Default for StreamingGradientTreeClassifier {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            lambda: 1.0,
            min_samples: 8,
            tau: 1e-4,
            root: SgtNode::Leaf(SgtLeaf::new(0, 0.0)),
            n_seen: 0,
            updates: 0,
            n_features: 0,
            initialized: false,
        }
    }
}

impl StreamingGradientTreeClassifier {
    /// Default streaming gradient tree classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for StreamingGradientTreeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "SGT needs labels"),
            );
        };
        if self.initialized && self.n_features != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        if !self.initialized {
            self.n_features = x.ncols();
            self.root = SgtNode::Leaf(SgtLeaf::new(x.ncols(), 0.0));
            self.initialized = true;
        }
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("StreamingGradientTreeClassifier learning_rate is not a positive finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let before = self.n_seen;
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let mut splits = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let yi = if y[i] >= 0.5 { 1.0 } else { 0.0 };
            let logit = sgt_predict(&self.root, x, i);
            let p0 = 1.0 / (1.0 + (-logit).exp());
            loss_before += -(yi * p0.max(1e-15).ln() + (1.0 - yi) * (1.0 - p0).max(1e-15).ln());
            let gi = p0 - yi;
            let hi = (p0 * (1.0 - p0)).max(1e-8);
            if sgt_update(
                &mut self.root,
                x,
                i,
                gi,
                hi,
                self.min_samples.max(2),
                self.lambda.max(1e-8),
                self.tau.max(0.0),
                lr,
            )
            .is_some()
            {
                splits += 1;
            }
            let logit1 = sgt_predict(&self.root, x, i);
            let p1 = 1.0 / (1.0 + (-logit1).exp());
            loss_after += -(yi * p1.max(1e-15).ln() + (1.0 - yi) * (1.0 - p1).max(1e-15).ln());
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(splits as f64);
        q.information_gain = Some((loss_before - loss_after).abs());
        q.loss_before = Some(loss_before);
        q.loss_after = Some(loss_after);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 8;
        q.explanation = format!("StreamingGradientTreeClassifier splits={splits} n={}", self.n_seen);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SGT log-loss split / leaf Newton",
                "leaves accumulate g=p−y, h=p(1−p) and split on ½(G²/(H+λ)) gain",
                format!("n={before}"),
                format!("n={} splits={splits}", self.n_seen),
            ),
        )
    }
}

impl Predict for StreamingGradientTreeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let p = 1.0 / (1.0 + (-sgt_predict(&self.root, x, i)).exp());
            if p >= 0.5 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Named passive-aggressive classifier (river `linear_model.PAClassifier`).
#[derive(Clone, Debug, Default)]
pub struct PaClassifier {
    inner: PassiveAggressive,
}

impl PaClassifier {
    /// Default PA classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for PaClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for PaClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Named aggregated Mondrian forest classifier (river `forest.AMFClassifier`).
#[derive(Clone, Debug, Default)]
pub struct AMFClassifier {
    inner: AmfClassifier,
}

impl AMFClassifier {
    /// AMF with `n_trees` Mondrian stumps.
    pub fn new(n_trees: usize) -> Self {
        Self {
            inner: AmfClassifier::new(n_trees),
        }
    }
}

impl PartialFit for AMFClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for AMFClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

/// Passive-aggressive PA-I regressor (river `linear_model.PARegressor`).
///
/// \(\varepsilon\)-insensitive residual; the step is capped at `c`.
/// \(\varepsilon\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct PaRegressor {
    /// Aggressiveness `C`.
    pub c: f64,
    /// Insensitive tube. Not identification `p`.
    pub epsilon: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PaRegressor {
    fn default() -> Self {
        Self {
            c: 1.0,
            epsilon: 0.1,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PaRegressor {
    /// PA-I regressor with aggressiveness `c`.
    pub fn new(c: f64) -> Self {
        Self {
            c,
            ..Self::default()
        }
    }
}

impl PartialFit for PaRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let eps = if self.epsilon.is_finite() && self.epsilon >= 0.0 {
            self.epsilon
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("PaRegressor epsilon is not a non-negative finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let cap = if self.c.is_finite() && self.c > 0.0 {
            self.c
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("PaRegressor C is not a positive finite; using 1")
                    .build(),
            );
            1.0
        };
        let before = self.coef.clone();
        let mut pinball = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let mut s = self.intercept;
            let mut n2 = 1.0_f64;
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                s += self.coef[j] * v;
                n2 += v * v;
            }
            let r = y[i] - s;
            let loss = (r.abs() - eps).max(0.0);
            pinball += loss;
            if loss > 0.0 && n2 > 0.0 {
                let tau = (loss / n2).min(cap);
                let sgn = if r >= 0.0 { 1.0 } else { -1.0 };
                for j in 0..x.ncols() {
                    self.coef[j] += tau * sgn * x.get(i, j);
                }
                self.intercept += tau * sgn;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            x.nrows(),
            self.n_seen,
            &delta,
            pinball / x.nrows().max(1) as f64,
            0.0,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "PA-I regressor",
            "ε-insensitive residual with step capped at C",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for PaRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Running majority-class dummy (river `dummy.NoChangeClassifier` sibling).
#[derive(Clone, Debug, Default)]
pub struct MajorityClass {
    n_pos: u64,
    n_neg: u64,
    n_seen: u64,
    updates: u64,
}

impl MajorityClass {
    /// Empty majority tracker.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for MajorityClass {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.n_pos;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            if y[i] >= 0.5 {
                self.n_pos += 1;
            } else {
                self.n_neg += 1;
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_pos as f64 - before as f64).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!("MajorityClass pos={} neg={}", self.n_pos, self.n_neg);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "majority counts",
                "the predicted class is the more frequent label so far",
                format!("pos={before}"),
                format!("pos={}", self.n_pos),
            ),
        )
    }
}

impl Predict for MajorityClass {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if self.n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let lab = if self.n_pos > self.n_neg { 1.0 } else { 0.0 };
        ctx.finish(Vector::filled(x.nrows(), lab))
    }
}

/// Predict the last observed label (river `dummy.NoChangeClassifier`).
#[derive(Clone, Debug, Default)]
pub struct NoChangeClassifier {
    last: Option<f64>,
    n_seen: u64,
    updates: u64,
}

impl NoChangeClassifier {
    /// Empty last-label tracker.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for NoChangeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.last;
        for i in 0..x.nrows().min(y.len()) {
            if y[i].is_finite() {
                self.last = Some(if y[i] >= 0.5 { 1.0 } else { 0.0 });
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(if before != self.last { 1.0 } else { 0.0 });
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.last.is_some();
        q.warmup = self.last.is_none();
        q.explanation = format!("NoChangeClassifier last={:?}", self.last);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "last label",
                "the next prediction repeats the most recent observed class",
                format!("{before:?}"),
                format!("{:?}", self.last),
            ),
        )
    }
}

impl Predict for NoChangeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        match self.last {
            Some(lab) => ctx.finish(Vector::filled(x.nrows(), lab)),
            None => {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                ctx.finish(Vector::zeros(x.nrows()))
            }
        }
    }
}

/// Predict the last observed target (river `dummy.NoChangeRegressor` / last-value).
#[derive(Clone, Debug, Default)]
pub struct LastValueRegressor {
    last: Option<f64>,
    n_seen: u64,
    updates: u64,
}

impl LastValueRegressor {
    /// Empty last-value tracker.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for LastValueRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        let before = self.last;
        for i in 0..x.nrows().min(y.len()) {
            if y[i].is_finite() {
                self.last = Some(y[i]);
                self.n_seen += 1;
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.last.unwrap_or(0.0) - before.unwrap_or(0.0)).abs());
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.last.is_some();
        q.warmup = self.last.is_none();
        q.explanation = format!("LastValueRegressor last={:?}", self.last);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "last target",
                "the next prediction repeats the most recent observed y",
                format!("{before:?}"),
                format!("{:?}", self.last),
            ),
        )
    }
}

impl Predict for LastValueRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        match self.last {
            Some(v) => ctx.finish(Vector::filled(x.nrows(), v)),
            None => {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                ctx.finish(Vector::zeros(x.nrows()))
            }
        }
    }
}

/// Named prior / empirical-class dummy (river `dummy.Prior`).
#[derive(Clone, Debug, Default)]
pub struct PriorClassifier {
    inner: MajorityClass,
}

impl PriorClassifier {
    /// Empty prior tracker.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for PriorClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        self.inner.partial_fit(x, y, session)
    }
}

impl Predict for PriorClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.inner.predict(x, session)
    }
}

#[derive(Clone, Debug)]
struct LogitLeaf {
    theta: Vector,
    n: u64,
}

impl LogitLeaf {
    fn new(p: usize) -> Self {
        Self {
            theta: Vector::zeros(p),
            n: 0,
        }
    }

    fn predict(&self, z: &Vector) -> f64 {
        let mut s = 0.0;
        for j in 0..self.theta.len().min(z.len()) {
            s += self.theta[j] * z[j];
        }
        sigmoid(s.clamp(-20.0, 20.0))
    }

    fn update(&mut self, z: &Vector, y: f64, lr: f64) -> f64 {
        if self.theta.len() != z.len() {
            *self = Self::new(z.len());
        }
        let p_hat = self.predict(z);
        let t = as01(y);
        let err = p_hat - t;
        let mut dn = 0.0;
        for j in 0..self.theta.len().min(z.len()) {
            let d = lr * err * z[j];
            self.theta[j] -= d;
            dn += d * d;
        }
        self.n += 1;
        dn.sqrt()
    }
}

fn binary_gini(n: f64, pos: f64) -> f64 {
    if n <= 0.0 {
        return 0.0;
    }
    let p = (pos / n).clamp(0.0, 1.0);
    1.0 - p * p - (1.0 - p) * (1.0 - p)
}

/// Adaptive model rules for classification (river `rules.AMRulesClassifier`).
///
/// Hoeffding split on **Gini** of the label, logistic-SGD leaves. Distinct
/// from [`AdaptiveModelRules`] (SSE + RLS regression) and
/// [`VeryFastDecisionRules`] (named wrapper of the regressor).
#[derive(Clone, Debug)]
pub struct AMRulesClassifier {
    /// SGD step.
    pub learning_rate: f64,
    /// Hoeffding \(\delta\).
    pub delta: f64,
    p: usize,
    split: Option<(usize, f64)>,
    default: LogitLeaf,
    left: Option<LogitLeaf>,
    right: Option<LogitLeaf>,
    feat_mean: Vector,
    feat_n: u64,
    parent_n: u64,
    parent_pos: f64,
    left_n: Vector,
    left_pos: Vector,
    right_n: Vector,
    right_pos: Vector,
    updates: u64,
    n_seen: u64,
}

impl Default for AMRulesClassifier {
    fn default() -> Self {
        Self {
            learning_rate: 0.1,
            delta: 0.01,
            p: 0,
            split: None,
            default: LogitLeaf::new(0),
            left: None,
            right: None,
            feat_mean: Vector::zeros(0),
            feat_n: 0,
            parent_n: 0,
            parent_pos: 0.0,
            left_n: Vector::zeros(0),
            left_pos: Vector::zeros(0),
            right_n: Vector::zeros(0),
            right_pos: Vector::zeros(0),
            updates: 0,
            n_seen: 0,
        }
    }
}

impl AMRulesClassifier {
    /// AMRules classifier with SGD step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }

    fn ensure(&mut self, p_x: usize) {
        let p = p_x + 1;
        if self.p == p {
            return;
        }
        self.p = p;
        self.default = LogitLeaf::new(p);
        self.feat_mean = Vector::zeros(p_x);
        self.left_n = Vector::zeros(p_x);
        self.left_pos = Vector::zeros(p_x);
        self.right_n = Vector::zeros(p_x);
        self.right_pos = Vector::zeros(p_x);
    }

    fn row(x: &Matrix, i: usize) -> Vector {
        let mut z = Vector::zeros(x.ncols() + 1);
        z[0] = 1.0;
        for j in 0..x.ncols() {
            z[j + 1] = x.get(i, j);
        }
        z
    }

    fn route(&mut self, x: &Matrix, i: usize) -> &mut LogitLeaf {
        match self.split {
            Some((j, t)) if x.get(i, j) <= t => {
                if self.left.is_none() {
                    self.left = Some(LogitLeaf::new(self.p));
                }
                self.left.as_mut().unwrap()
            }
            Some((_j, _)) => {
                if self.right.is_none() {
                    self.right = Some(LogitLeaf::new(self.p));
                }
                self.right.as_mut().unwrap()
            }
            None => &mut self.default,
        }
    }

    fn maybe_split(&mut self, ctx: &mut FitCtx) -> Option<(usize, f64, f64)> {
        if self.split.is_some() || self.feat_mean.is_empty() || self.parent_n < 20 {
            return None;
        }
        let eps = ((1.0 / self.delta.max(1e-12)).ln() / (2.0 * self.parent_n as f64)).sqrt();
        let parent = binary_gini(self.parent_n as f64, self.parent_pos);
        let mut best = None;
        let mut best_gain = 0.0;
        let mut second = 0.0;
        for j in 0..self.feat_mean.len() {
            let nl = self.left_n[j];
            let nr = self.right_n[j];
            if nl < 5.0 || nr < 5.0 {
                continue;
            }
            let gl = binary_gini(nl, self.left_pos[j]);
            let gr = binary_gini(nr, self.right_pos[j]);
            let gain = parent - (nl * gl + nr * gr) / (nl + nr);
            if gain > best_gain {
                second = best_gain;
                best_gain = gain;
                best = Some((j, self.feat_mean[j], gain));
            } else if gain > second {
                second = gain;
            }
        }
        if let Some((j, t, gain)) = best {
            if gain - second > eps && gain > 0.0 {
                self.split = Some((j, t));
                self.left = Some(LogitLeaf::new(self.p));
                self.right = Some(LogitLeaf::new(self.p));
                ctx.push(
                    Issue::builder(IssueCode::ConceptDriftDetected)
                        .severity(Severity::Advisory)
                        .message(format!(
                            "AMRulesClassifier split on feature {j} at {t:.6e} (gain={gain:.6e}, ε={eps:.6e})"
                        ))
                        .build(),
                );
                return Some((j, t, gain));
            }
        }
        None
    }

    fn predict_row(&self, x: &Matrix, i: usize) -> f64 {
        let z = Self::row(x, i);
        match self.split {
            Some((j, t)) if x.get(i, j) <= t => {
                self.left.as_ref().map(|l| l.predict(&z)).unwrap_or(0.5)
            }
            Some(_) => self.right.as_ref().map(|l| l.predict(&z)).unwrap_or(0.5),
            None => self.default.predict(&z),
        }
    }
}

impl PartialFit for AMRulesClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            if self.n_seen == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, 0, self.n_seen, "AMRulesClassifier needs a target"),
            );
        };
        self.ensure(x.ncols());
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("AMRulesClassifier learning_rate is not a positive finite; using 0.1")
                    .build(),
            );
            0.1
        };
        let mut dsum = 0.0;
        let mut loss_before = 0.0;
        let mut loss_after = 0.0;
        let mut leaf_hits = [0u64; 3];
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            let z = Self::row(x, i);
            let pred_b = self.predict_row(x, i);
            let t = as01(y[i]);
            loss_before += -(t * (pred_b + 1e-12).ln() + (1.0 - t) * (1.0 - pred_b + 1e-12).ln());
            let which = match self.split {
                Some((j, th)) if x.get(i, j) <= th => 1,
                Some(_) => 2,
                None => 0,
            };
            leaf_hits[which] += 1;
            let dn = self.route(x, i).update(&z, y[i], lr);
            dsum += dn;
            let pred_a = self.predict_row(x, i);
            loss_after += -(t * (pred_a + 1e-12).ln() + (1.0 - t) * (1.0 - pred_a + 1e-12).ln());
            if self.split.is_none() {
                self.parent_n += 1;
                self.parent_pos += t;
                self.feat_n += 1;
                for j in 0..x.ncols() {
                    let v = x.get(i, j);
                    self.feat_mean[j] += (v - self.feat_mean[j]) / self.feat_n as f64;
                    if v <= self.feat_mean[j] {
                        self.left_n[j] += 1.0;
                        self.left_pos[j] += t;
                    } else {
                        self.right_n[j] += 1.0;
                        self.right_pos[j] += t;
                    }
                }
            }
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let split = self.maybe_split(&mut ctx);
        let identified = self.n_seen as usize > self.p.max(1);
        let warmup = self.n_seen < 10;
        let what = if let Some((j, t, g)) = split {
            format!("split on x[{j}]≤{t:.4e} gain={g:.4e}; leaf hits {leaf_hits:?}")
        } else if self.split.is_some() {
            format!("updated split logistic leaves; hits {leaf_hits:?}")
        } else {
            format!("updated default logistic leaf; hits {leaf_hits:?}")
        };
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &Vector::from_slice(&[dsum]),
            loss_before,
            loss_after,
            identified,
            warmup,
            &what,
            "logistic-SGD leaf update; a Hoeffding Gini test may grow a stump",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for AMRulesClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.p == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            if self.predict_row(x, i) >= 0.5 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Online pinball / quantile regressor (river `optim` + linear quantile).
///
/// SGD on \(\rho_\tau(y-x^\top\theta)\). Distinct from [`PaRegressor`]
/// (ε-insensitive) and [`OnlineQuantile`] (univariate tracker, no \(X\)).
/// \(\tau\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineQuantileRegressor {
    /// Quantile level in \((0,1)\).
    pub tau: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineQuantileRegressor {
    fn default() -> Self {
        Self {
            tau: 0.5,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineQuantileRegressor {
    /// Pinball regressor at quantile `tau`.
    pub fn new(tau: f64) -> Self {
        Self {
            tau,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineQuantileRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            if self.n_seen == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        if !self.initialized {
            if x.ncols() == 0 {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
                );
            }
            self.coef = Vector::zeros(x.ncols());
            self.initialized = true;
        } else if self.coef.len() != x.ncols() {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let tau = if self.tau.is_finite() && self.tau > 0.0 && self.tau < 1.0 {
            self.tau
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("OnlineQuantileRegressor tau is not in (0,1); using 0.5")
                    .build(),
            );
            0.5
        };
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            ctx.push(
                Issue::builder(IssueCode::InvalidWeight)
                    .severity(Severity::Warning)
                    .message("OnlineQuantileRegressor learning_rate is not a positive finite; using 0.05")
                    .build(),
            );
            0.05
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut s = self.intercept;
            for j in 0..x.ncols() {
                s += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - s;
            loss_before += if r >= 0.0 { tau * r } else { (tau - 1.0) * r };
            let g = if r >= 0.0 { -tau } else { 1.0 - tau };
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut sa = self.intercept;
            for j in 0..x.ncols() {
                sa += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - sa;
            loss_after += if ra >= 0.0 {
                tau * ra
            } else {
                (tau - 1.0) * ra
            };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online pinball regressor",
            "SGD on ρ_τ(y − xθ); τ is a quantile, not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineQuantileRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

fn online_glm_prepare<'a>(
    ctx: &mut FitCtx,
    x: &'a Matrix,
    y: Option<&'a Vector>,
    initialized: bool,
    n_seen: u64,
    coef: &mut Vector,
    learning_rate: f64,
    name: &str,
) -> Option<(&'a Vector, f64)> {
    inspect_online_xy(ctx, x, y);
    let Some(y) = y else {
        ctx.push(Issue::builder(IssueCode::MissingTarget).build());
        if n_seen == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
        }
        return None;
    };
    if !initialized {
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return None;
        }
        *coef = Vector::zeros(x.ncols());
    } else if coef.len() != x.ncols() {
        ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
        return None;
    }
    let lr = if learning_rate.is_finite() && learning_rate > 0.0 {
        learning_rate
    } else {
        ctx.push(
            Issue::builder(IssueCode::InvalidWeight)
                .severity(Severity::Warning)
                .message(format!("{name} learning_rate is not a positive finite; using 0.05"))
                .build(),
        );
        0.05
    };
    Some((y, lr))
}

/// Online Poisson GLM regressor (river `linear_model` + Poisson).
///
/// Log-link SGD on \(\exp(\eta)-y\eta\). Distinct from [`OnlinePoisson`]
/// (univariate count tracker, no \(X\)).
#[derive(Clone, Debug)]
pub struct OnlinePoissonRegressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlinePoissonRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlinePoissonRegressor {
    /// Poisson GLM with step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlinePoissonRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlinePoissonRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Poisson GLM update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() || y[i] < 0.0 {
                if y[i].is_finite() && y[i] < 0.0 {
                    ctx.push(
                        Issue::builder(IssueCode::NonPositiveSeries)
                            .severity(Severity::Warning)
                            .message("OnlinePoissonRegressor skipped a negative count")
                            .build(),
                    );
                }
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            let mu = eta.exp();
            loss_before += mu - y[i] * eta;
            let g = mu - y[i];
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ea = ea.clamp(-20.0, 20.0);
            loss_after += ea.exp() - y[i] * ea;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Poisson GLM",
            "SGD on exp(η) − yη; count mean, not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlinePoissonRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            eta.clamp(-20.0, 20.0).exp()
        })))
    }
}

/// Online Gamma GLM regressor (river Gamma + log link).
///
/// SGD on \(y e^{-\eta}+\eta\). Distinct from [`OnlineGamma`] (univariate
/// tracker) and [`OnlinePoissonRegressor`] (count mean).
#[derive(Clone, Debug)]
pub struct OnlineGammaRegressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineGammaRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineGammaRegressor {
    /// Gamma GLM with step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineGammaRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineGammaRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Gamma GLM update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() || y[i] <= 0.0 {
                if y[i].is_finite() && y[i] <= 0.0 {
                    ctx.push(
                        Issue::builder(IssueCode::NonPositiveSeries)
                            .severity(Severity::Warning)
                            .message("OnlineGammaRegressor skipped a non-positive response")
                            .build(),
                    );
                }
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            let mu = eta.exp();
            loss_before += y[i] / mu + eta;
            let g = 1.0 - y[i] / mu;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ea = ea.clamp(-20.0, 20.0);
            loss_after += y[i] / ea.exp() + ea;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Gamma GLM",
            "SGD on y exp(−η) + η; mean is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineGammaRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            eta.clamp(-20.0, 20.0).exp()
        })))
    }
}

/// Online Tweedie GLM regressor (compound Poisson–Gamma, log link).
///
/// Power \(p\) is not identification `p`. Distinct from
/// [`OnlineTweedieDeviance`] (metric) and [`OnlinePoissonRegressor`] (\(p=1\)).
#[derive(Clone, Debug)]
pub struct OnlineTweedieRegressor {
    /// Tweedie power in \((1,2)\). Not identification `p`.
    pub power: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineTweedieRegressor {
    fn default() -> Self {
        Self {
            power: 1.5,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineTweedieRegressor {
    /// Tweedie GLM with variance power `power`.
    pub fn new(power: f64) -> Self {
        Self {
            power,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineTweedieRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineTweedieRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Tweedie GLM update"),
            );
        };
        self.initialized = true;
        let pwr = if self.power.is_finite() && self.power > 1.0 && self.power < 2.0 {
            self.power
        } else {
            1.5
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() || y[i] < 0.0 {
                if y[i].is_finite() && y[i] < 0.0 {
                    ctx.push(
                        Issue::builder(IssueCode::NonPositiveSeries)
                            .severity(Severity::Warning)
                            .message("OnlineTweedieRegressor skipped a negative response")
                            .build(),
                    );
                }
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            let mu = eta.exp();
            let inv = 1.0 - pwr;
            loss_before += mu.powf(inv) / inv - y[i] * mu.powf(-pwr) / (-pwr);
            let g = (mu - y[i]) * mu.powf(1.0 - pwr);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ea = ea.clamp(-20.0, 20.0);
            let mua = ea.exp();
            loss_after += mua.powf(inv) / inv - y[i] * mua.powf(-pwr) / (-pwr);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Tweedie GLM",
            "SGD on the Tweedie unit deviance; power is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineTweedieRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            eta.clamp(-20.0, 20.0).exp()
        })))
    }
}

/// Online Huber regressor (river `optim` + Huber).
///
/// SGD on the Huber piecewise loss. Distinct from [`OnlineHuber`] (rolling
/// metric) and [`PaRegressor`] (ε-insensitive).
#[derive(Clone, Debug)]
pub struct OnlineHuberRegressor {
    /// Huber threshold \(\delta>0\). Not identification `p`.
    pub delta: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineHuberRegressor {
    fn default() -> Self {
        Self {
            delta: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineHuberRegressor {
    /// Huber regressor with threshold `delta`.
    pub fn new(delta: f64) -> Self {
        Self {
            delta,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineHuberRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineHuberRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Huber update"),
            );
        };
        self.initialized = true;
        let delta = if self.delta.is_finite() && self.delta > 0.0 {
            self.delta
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = r.abs();
            loss_before += if ar <= delta {
                0.5 * r * r
            } else {
                delta * (ar - 0.5 * delta)
            };
            let g = if ar <= delta {
                -r
            } else if r >= 0.0 {
                -delta
            } else {
                delta
            };
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let ara = ra.abs();
            loss_after += if ara <= delta {
                0.5 * ra * ra
            } else {
                delta * (ara - 0.5 * delta)
            };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta_c = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta_c,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Huber regressor",
            "SGD on the Huber piecewise loss; δ is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineHuberRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online negative-binomial GLM (NB2 log link).
///
/// Dispersion \(r\) is not identification `p`. Distinct from
/// [`OnlinePoissonRegressor`] (equidispersion) — this is an overdispersed
/// regression head on \(X\).
#[derive(Clone, Debug)]
pub struct OnlineNegBinRegressor {
    /// NB2 dispersion \(r>0\). Not identification `p`.
    pub r: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineNegBinRegressor {
    fn default() -> Self {
        Self {
            r: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineNegBinRegressor {
    /// NB2 GLM with dispersion `r`.
    pub fn new(r: f64) -> Self {
        Self {
            r,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineNegBinRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineNegBinRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected NB2 GLM update"),
            );
        };
        self.initialized = true;
        let r = if self.r.is_finite() && self.r > 0.0 {
            self.r
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() || y[i] < 0.0 {
                if y[i].is_finite() && y[i] < 0.0 {
                    ctx.push(
                        Issue::builder(IssueCode::NonPositiveSeries)
                            .severity(Severity::Warning)
                            .message("OnlineNegBinRegressor skipped a negative count")
                            .build(),
                    );
                }
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            let mu = eta.exp();
            loss_before += -y[i] * eta + (y[i] + r) * (mu + r).ln();
            let g = (mu - y[i]) / (1.0 + mu / r);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ea = ea.clamp(-20.0, 20.0);
            loss_after += -y[i] * ea + (y[i] + r) * (ea.exp() + r).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online NB2 GLM",
            "SGD on the NB2 log-link score; r is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineNegBinRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            eta.clamp(-20.0, 20.0).exp()
        })))
    }
}

/// Online Beta GLM (logit quasi-likelihood on \((0,1)\)).
///
/// Rows with \(y\notin(0,1)\) are skipped with a warning and do not abort.
/// Distinct from [`OnlineBeta`] (univariate tracker) — this is a
/// fractional-response GLM.
#[derive(Clone, Debug)]
pub struct OnlineBetaRegressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineBetaRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineBetaRegressor {
    /// Beta GLM with step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineBetaRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineBetaRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Beta GLM update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() || y[i] <= 0.0 || y[i] >= 1.0 {
                if y[i].is_finite() && (y[i] <= 0.0 || y[i] >= 1.0) {
                    ctx.push(
                        Issue::builder(IssueCode::NonPositiveSeries)
                            .severity(Severity::Warning)
                            .message("OnlineBetaRegressor skipped a response outside (0, 1)")
                            .build(),
                    );
                }
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            let mu = 1.0 / (1.0 + (-eta).exp());
            let mu = mu.clamp(1e-8, 1.0 - 1e-8);
            loss_before += -(y[i] * mu.ln() + (1.0 - y[i]) * (1.0 - mu).ln());
            let g = mu - y[i];
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ea = ea.clamp(-20.0, 20.0);
            let mua = (1.0 / (1.0 + (-ea).exp())).clamp(1e-8, 1.0 - 1e-8);
            loss_after += -(y[i] * mua.ln() + (1.0 - y[i]) * (1.0 - mua).ln());
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Beta GLM",
            "logit quasi-likelihood on (0,1); skipped rows warn and do not abort",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineBetaRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut eta = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                eta += self.coef[j] * x.get(i, j);
            }
            let eta = eta.clamp(-20.0, 20.0);
            1.0 / (1.0 + (-eta).exp())
        })))
    }
}

/// Online log-cosh regressor (smooth L1).
///
/// SGD on \(\log\cosh(y-\eta)\). Distinct from [`OnlineHuberRegressor`]
/// (piecewise) and [`OnlineQuantileRegressor`] (pinball).
#[derive(Clone, Debug)]
pub struct OnlineLogCoshRegressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineLogCoshRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineLogCoshRegressor {
    /// Log-cosh regressor with step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineLogCoshRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineLogCoshRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected log-cosh update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = (y[i] - eta).clamp(-20.0, 20.0);
            loss_before += r.cosh().ln();
            let g = -r.tanh();
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = (y[i] - ea).clamp(-20.0, 20.0);
            loss_after += ra.cosh().ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online log-cosh regressor",
            "SGD on log cosh(y − η); the loss is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineLogCoshRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Cauchy regressor (river robust loss).
///
/// SGD on \(\log(1+(r/c)^2)\). Scale \(c\) is not identification `p`. Distinct
/// from [`OnlineHuberRegressor`] (piecewise \(L_2/L_1\)) and
/// [`OnlineLogCoshRegressor`] (log-cosh).
#[derive(Clone, Debug)]
pub struct OnlineCauchyRegressor {
    /// Cauchy scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineCauchyRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineCauchyRegressor {
    /// Cauchy regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineCauchyRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineCauchyRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Cauchy update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let c2 = c * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = (y[i] - eta).clamp(-1.0e6, 1.0e6);
            loss_before += (1.0 + (r * r) / c2).ln();
            let g = -2.0 * r / (c2 + r * r);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = (y[i] - ea).clamp(-1.0e6, 1.0e6);
            loss_after += (1.0 + (ra * ra) / c2).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Cauchy regressor",
            "SGD on log(1+(r/c)^2); the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineCauchyRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Tukey biweight regressor (river robust loss).
///
/// SGD on Tukey's \(\rho\). Cutoff \(c\) is not identification `p`. Distinct
/// from [`OnlineHuberRegressor`] (hard \(L_1\) tail) and
/// [`OnlineCauchyRegressor`] (no hard zero).
#[derive(Clone, Debug)]
pub struct OnlineTukeyRegressor {
    /// Biweight cutoff \(c>0\). Not identification `p`.
    pub cutoff: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineTukeyRegressor {
    fn default() -> Self {
        Self {
            cutoff: 4.685,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineTukeyRegressor {
    /// Tukey regressor with cutoff `cutoff`.
    pub fn new(cutoff: f64) -> Self {
        Self {
            cutoff,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineTukeyRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineTukeyRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Tukey update"),
            );
        };
        self.initialized = true;
        let c = if self.cutoff.is_finite() && self.cutoff > 0.0 {
            self.cutoff
        } else {
            4.685
        };
        let c2 = c * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let u = r / c;
            let rho = if r.abs() <= c {
                let t = 1.0 - u * u;
                (c2 / 6.0) * (1.0 - t * t * t)
            } else {
                c2 / 6.0
            };
            loss_before += rho;
            let g = if r.abs() <= c {
                let t = 1.0 - u * u;
                -r * t * t
            } else {
                0.0
            };
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let ua = ra / c;
            loss_after += if ra.abs() <= c {
                let t = 1.0 - ua * ua;
                (c2 / 6.0) * (1.0 - t * t * t)
            } else {
                c2 / 6.0
            };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Tukey regressor",
            "SGD on Tukey's biweight; the cutoff is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineTukeyRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Laplace / \(L_1\) regressor (river MAE SGD).
///
/// SGD on \(|y-\eta|\). Distinct from [`OnlineHuberRegressor`] (quadratic
/// bowl) and [`OnlineQuantileRegressor`] (asymmetric pinball).
#[derive(Clone, Debug)]
pub struct OnlineLaplaceRegressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineLaplaceRegressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineLaplaceRegressor {
    /// Laplace regressor with step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineLaplaceRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineLaplaceRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Laplace update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            loss_before += r.abs();
            let g = if r > 0.0 {
                -1.0
            } else if r < 0.0 {
                1.0
            } else {
                0.0
            };
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += (y[i] - ea).abs();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Laplace regressor",
            "SGD on |y − η|; the L1 loss is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineLaplaceRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Student-\(t\) regressor (river robust loss).
///
/// SGD on \(\frac{\nu+1}{2}\log(1+r^2/\nu)\). Degrees of freedom \(\nu\) are
/// not identification `p`. Distinct from [`OnlineCauchyRegressor`]
/// (\(\nu=1\) scale form) and [`OnlineHuberRegressor`] (piecewise).
#[derive(Clone, Debug)]
pub struct OnlineStudentTRegressor {
    /// Degrees of freedom \(\nu>0\). Not identification `p`.
    pub df: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineStudentTRegressor {
    fn default() -> Self {
        Self {
            df: 4.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineStudentTRegressor {
    /// Student-\(t\) regressor with `df` degrees of freedom.
    pub fn new(df: f64) -> Self {
        Self {
            df,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineStudentTRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineStudentTRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Student-t update"),
            );
        };
        self.initialized = true;
        let nu = if self.df.is_finite() && self.df > 0.0 {
            self.df
        } else {
            4.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = (y[i] - eta).clamp(-1.0e6, 1.0e6);
            loss_before += 0.5 * (nu + 1.0) * (1.0 + r * r / nu).ln();
            let g = -(nu + 1.0) * r / (nu + r * r);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = (y[i] - ea).clamp(-1.0e6, 1.0e6);
            loss_after += 0.5 * (nu + 1.0) * (1.0 + ra * ra / nu).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Student-t regressor",
            "SGD on the Student-t loss; ν is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineStudentTRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Fair regressor (river Fair loss).
///
/// SGD on \(c|r|-c^2\log(1+|r|/c)\). Scale \(c\) is not identification `p`.
/// Distinct from [`OnlineHuberRegressor`] (hard \(L_1\) tail) and
/// [`OnlineTukeyRegressor`] (hard zero).
#[derive(Clone, Debug)]
pub struct OnlineFairRegressor {
    /// Fair scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineFairRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineFairRegressor {
    /// Fair regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineFairRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineFairRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Fair update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = r.abs();
            loss_before += c * ar - c * c * (1.0 + ar / c).ln();
            let g = -r / (1.0 + ar / c);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let ara = ra.abs();
            loss_after += c * ara - c * c * (1.0 + ara / c).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Fair regressor",
            "SGD on the Fair loss; the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineFairRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Welsch / Leclerc regressor (river robust loss).
///
/// SGD on \(\frac{c^2}{2}(1-\exp(-(r/c)^2))\). Scale \(c\) is not
/// identification `p`. Distinct from [`OnlineTukeyRegressor`] (hard zero)
/// and [`OnlineCauchyRegressor`] (log tails).
#[derive(Clone, Debug)]
pub struct OnlineWelschRegressor {
    /// Welsch scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineWelschRegressor {
    fn default() -> Self {
        Self {
            scale: 2.985,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineWelschRegressor {
    /// Welsch regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineWelschRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineWelschRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Welsch update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            2.985
        };
        let c2 = c * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let u = (-(r * r) / c2).exp();
            loss_before += 0.5 * c2 * (1.0 - u);
            let g = -r * u;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            loss_after += 0.5 * c2 * (1.0 - (-(ra * ra) / c2).exp());
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Welsch regressor",
            "SGD on the Welsch/Leclerc loss; the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineWelschRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online expectile regressor (river asymmetric \(L_2\)).
///
/// SGD on \(\tau r_+^2+(1-\tau)r_-^2\). Level \(\tau\) is not identification
/// `p`. Distinct from [`OnlineQuantileRegressor`] (pinball) and
/// [`OnlineHuberRegressor`] (piecewise \(L_2/L_1\)).
#[derive(Clone, Debug)]
pub struct OnlineExpectileRegressor {
    /// Expectile level in \((0,1)\). Not identification `p`.
    pub tau: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineExpectileRegressor {
    fn default() -> Self {
        Self {
            tau: 0.25,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineExpectileRegressor {
    /// Expectile regressor at level `tau`.
    pub fn new(tau: f64) -> Self {
        Self {
            tau,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineExpectileRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineExpectileRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected expectile update"),
            );
        };
        self.initialized = true;
        let tau = if self.tau.is_finite() && self.tau > 0.0 && self.tau < 1.0 {
            self.tau
        } else {
            0.25
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let w = if r >= 0.0 { tau } else { 1.0 - tau };
            loss_before += w * r * r;
            let g = -2.0 * w * r;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let wa = if ra >= 0.0 { tau } else { 1.0 - tau };
            loss_after += wa * ra * ra;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online expectile regressor",
            "SGD on the asymmetric L2 expectile; τ is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineExpectileRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Hampel redescending regressor (river robust loss).
///
/// Three-part \(\psi\) with cutoffs \(a<b<c\), none of which are identification
/// `p`. Distinct from [`OnlineHuberRegressor`] (no hard zero) and
/// [`OnlineTukeyRegressor`] (quartic bowl).
#[derive(Clone, Debug)]
pub struct OnlineHampelRegressor {
    /// Inner Huber radius. Not identification `p`.
    pub a: f64,
    /// Linear-to-descend hinge. Not identification `p`.
    pub b: f64,
    /// Hard-reject radius. Not identification `p`.
    pub c: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineHampelRegressor {
    fn default() -> Self {
        Self {
            a: 1.5,
            b: 3.5,
            c: 8.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineHampelRegressor {
    /// Hampel regressor with inner cutoff `a`.
    pub fn new(a: f64) -> Self {
        Self {
            a,
            ..Self::default()
        }
    }
}

fn hampel_psi(r: f64, a: f64, b: f64, c: f64) -> (f64, f64) {
    let ar = r.abs();
    let sign = if r >= 0.0 { 1.0 } else { -1.0 };
    if ar <= a {
        (0.5 * r * r, r)
    } else if ar <= b {
        (a * ar - 0.5 * a * a, sign * a)
    } else if ar < c {
        let t = (c - ar) / (c - b).max(1e-8);
        let rho = a * b - 0.5 * a * a + 0.5 * a * (c - b) * (1.0 - t * t);
        (rho, sign * a * t)
    } else {
        let rho = a * b - 0.5 * a * a + 0.5 * a * (c - b);
        (rho, 0.0)
    }
}

impl PartialFit for OnlineHampelRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineHampelRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Hampel update"),
            );
        };
        self.initialized = true;
        let a = if self.a.is_finite() && self.a > 0.0 {
            self.a
        } else {
            1.5
        };
        let b = if self.b.is_finite() && self.b > a {
            self.b
        } else {
            a + 2.0
        };
        let c = if self.c.is_finite() && self.c > b {
            self.c
        } else {
            b + 4.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let (rho, psi) = hampel_psi(r, a, b, c);
            loss_before += rho;
            let g = -psi;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += hampel_psi(y[i] - ea, a, b, c).0;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Hampel regressor",
            "SGD on the three-part Hampel ψ; a,b,c are not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineHampelRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Andrews sine-wave regressor (river robust loss).
///
/// \(\psi(r)=\sin(r/c)\) for \(|r|\le\pi c\), else 0. Scale \(c\) is not
/// identification `p`. Distinct from [`OnlineHampelRegressor`] (linear
/// middle) and [`OnlineTukeyRegressor`] (quartic).
#[derive(Clone, Debug)]
pub struct OnlineAndrewsRegressor {
    /// Andrews scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineAndrewsRegressor {
    fn default() -> Self {
        Self {
            scale: 1.339,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineAndrewsRegressor {
    /// Andrews regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineAndrewsRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineAndrewsRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Andrews update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.339
        };
        let lim = std::f64::consts::PI * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let rho = if r.abs() <= lim {
                c * c * (1.0 - (r / c).cos())
            } else {
                2.0 * c * c
            };
            loss_before += rho;
            let psi = if r.abs() <= lim { (r / c).sin() } else { 0.0 };
            let g = -psi;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            loss_after += if ra.abs() <= lim {
                c * c * (1.0 - (ra / c).cos())
            } else {
                2.0 * c * c
            };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Andrews regressor",
            "SGD on the Andrews sine wave; the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineAndrewsRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Geman–McClure regressor (redescending \(r^2/(c^2+r^2)\)).
///
/// Scale \(c\) is not identification `p`. Distinct from [`OnlineWelschRegressor`]
/// (Gaussian-like) and [`OnlineTukeyRegressor`] (hard cutoff).
#[derive(Clone, Debug)]
pub struct OnlineGemanMcClureRegressor {
    /// Geman–McClure scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineGemanMcClureRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineGemanMcClureRegressor {
    /// Geman–McClure regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineGemanMcClureRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineGemanMcClureRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Geman-McClure update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let c2 = c * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let den = c2 + r * r;
            loss_before += (r * r) / den;
            let g = -2.0 * c2 * r / (den * den);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            loss_after += (ra * ra) / (c2 + ra * ra);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Geman-McClure regressor",
            "SGD on r^2/(c^2+r^2); the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineGemanMcClureRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Ramsay \(E_a\) regressor (exponentially tailed influence).
///
/// Radius \(a\) is not identification `p`. Distinct from [`OnlineHuberRegressor`]
/// (linear tails) and [`OnlineHampelRegressor`] (three-part \(\psi\)).
#[derive(Clone, Debug)]
pub struct OnlineRamsayRegressor {
    /// Ramsay decay \(a>0\). Not identification `p`.
    pub a: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineRamsayRegressor {
    fn default() -> Self {
        Self {
            a: 0.3,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineRamsayRegressor {
    /// Ramsay regressor with decay `a`.
    pub fn new(a: f64) -> Self {
        Self {
            a,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineRamsayRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineRamsayRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Ramsay update"),
            );
        };
        self.initialized = true;
        let a = if self.a.is_finite() && self.a > 0.0 {
            self.a
        } else {
            0.3
        };
        let a2 = (a * a).max(1e-12);
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = a * r.abs();
            let e = (-ar).exp();
            loss_before += (1.0 - (1.0 + ar) * e) / a2;
            let g = -r * e;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let ara = a * ra.abs();
            loss_after += (1.0 - (1.0 + ara) * (-ara).exp()) / a2;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Ramsay Ea regressor",
            "SGD on the Ramsay Ea loss; a is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineRamsayRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online \(L_p\) regressor (power loss with \(p \neq 1,2\)).
///
/// Power is not identification `p`. Distinct from [`OnlineLaplaceRegressor`]
/// (\(p=1\)) and ordinary least squares (\(p=2\)).
#[derive(Clone, Debug)]
pub struct OnlineLpRegressor {
    /// Loss power \(p>0\). Not identification `p`.
    pub power: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineLpRegressor {
    fn default() -> Self {
        Self {
            power: 1.5,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineLpRegressor {
    /// \(L_p\) regressor with loss power `power`.
    pub fn new(power: f64) -> Self {
        Self {
            power,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineLpRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineLpRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Lp update"),
            );
        };
        self.initialized = true;
        let pwr = if self.power.is_finite() && self.power > 0.0 {
            self.power
        } else {
            1.5
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = r.abs().max(1e-12);
            loss_before += ar.powf(pwr) / pwr;
            let sign = if r >= 0.0 { 1.0 } else { -1.0 };
            let g = -sign * ar.powf(pwr - 1.0);
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = (y[i] - ea).abs().max(1e-12);
            loss_after += ra.powf(pwr) / pwr;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Lp regressor",
            "SGD on |r|^p / p; the power is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineLpRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Charbonnier / pseudo-Huber regressor (\(\sqrt{c^2+r^2}-c\)).
///
/// Scale \(c\) is not identification `p`. Distinct from [`OnlineHuberRegressor`]
/// (piecewise) and [`OnlineLogCoshRegressor`] (log-cosh).
#[derive(Clone, Debug)]
pub struct OnlineCharbonnierRegressor {
    /// Charbonnier scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineCharbonnierRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineCharbonnierRegressor {
    /// Charbonnier regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineCharbonnierRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineCharbonnierRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Charbonnier update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let c2 = c * c;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let nrm = (c2 + r * r).sqrt();
            loss_before += nrm - c;
            let g = -r / nrm;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            loss_after += (c2 + ra * ra).sqrt() - c;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Charbonnier regressor",
            "SGD on sqrt(c^2+r^2)-c; the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineCharbonnierRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online tanh-influence regressor (bounded \(\psi=c\tanh(r/c)\)).
///
/// Scale \(c\) is not identification `p`. Distinct from [`OnlineAndrewsRegressor`]
/// (sine, hard zero) and [`OnlineHuberRegressor`] (linear then clip).
#[derive(Clone, Debug)]
pub struct OnlineTanhRegressor {
    /// Tanh scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineTanhRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineTanhRegressor {
    /// Tanh regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineTanhRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineTanhRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected tanh update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let u = r / c;
            loss_before += c * c * u.cosh().ln();
            let g = -c * u.tanh();
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ua = (y[i] - ea) / c;
            loss_after += c * c * ua.cosh().ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online tanh regressor",
            "SGD on c^2 log cosh(r/c); the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineTanhRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online arctan-influence regressor (\(\psi=c\arctan(r/c)\)).
///
/// Scale \(c\) is not identification `p`. Distinct from [`OnlineCauchyRegressor`]
/// (score \(r/(c^2+r^2)\)) and [`OnlineTanhRegressor`] (exponential tails).
#[derive(Clone, Debug)]
pub struct OnlineAtanRegressor {
    /// Arctan scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineAtanRegressor {
    fn default() -> Self {
        Self {
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineAtanRegressor {
    /// Arctan regressor with scale `scale`.
    pub fn new(scale: f64) -> Self {
        Self {
            scale,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineAtanRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineAtanRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected arctan update"),
            );
        };
        self.initialized = true;
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let u = r / c;
            loss_before += c * r * u.atan() - 0.5 * c * c * (1.0 + u * u).ln();
            let g = -c * u.atan();
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ra = y[i] - ea;
            let ua = ra / c;
            loss_after += c * ra * ua.atan() - 0.5 * c * c * (1.0 + ua * ua).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online arctan regressor",
            "SGD on the arctan loss; the scale is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineAtanRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online \(\varepsilon\)-insensitive regressor (SVM dead-zone).
///
/// \(\varepsilon\) is not identification `p`. Distinct from [`OnlineHuberRegressor`]
/// (quadratic bowl) and [`OnlineLaplaceRegressor`] (no dead-zone).
#[derive(Clone, Debug)]
pub struct OnlineEpsilonInsensitiveRegressor {
    /// Dead-zone radius. Not identification `p`.
    pub epsilon: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineEpsilonInsensitiveRegressor {
    fn default() -> Self {
        Self {
            epsilon: 0.1,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineEpsilonInsensitiveRegressor {
    /// \(\varepsilon\)-insensitive regressor.
    pub fn new(epsilon: f64) -> Self {
        Self {
            epsilon,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineEpsilonInsensitiveRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineEpsilonInsensitiveRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected eps-insensitive update"),
            );
        };
        self.initialized = true;
        let eps = if self.epsilon.is_finite() && self.epsilon >= 0.0 {
            self.epsilon
        } else {
            0.1
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = r.abs();
            loss_before += (ar - eps).max(0.0);
            let g = if ar <= eps {
                0.0
            } else if r > 0.0 {
                -1.0
            } else {
                1.0
            };
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += ((y[i] - ea).abs() - eps).max(0.0);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online epsilon-insensitive regressor",
            "SGD on the SVM dead-zone loss; ε is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineEpsilonInsensitiveRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online Barron robust regressor (one-parameter robust family).
///
/// Shape \(\alpha\) is not identification `p`. Distinct from [`OnlineCharbonnierRegressor`]
/// (\(\alpha=1\)) and [`OnlineGemanMcClureRegressor`] (\(\alpha=-2\)).
#[derive(Clone, Debug)]
pub struct OnlineBarronRegressor {
    /// Barron shape. Not identification `p`.
    pub alpha: f64,
    /// Scale \(c>0\). Not identification `p`.
    pub scale: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineBarronRegressor {
    fn default() -> Self {
        Self {
            alpha: 1.0,
            scale: 1.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineBarronRegressor {
    /// Barron regressor with shape `alpha`.
    pub fn new(alpha: f64) -> Self {
        Self {
            alpha,
            ..Self::default()
        }
    }
}

fn barron_rho_grad(r: f64, alpha: f64, c: f64) -> (f64, f64) {
    let c2 = (c * c).max(1e-12);
    let u = (r * r) / c2;
    if (alpha - 2.0).abs() < 1e-8 {
        return (0.5 * u, -r / c2);
    }
    if alpha.abs() < 1e-8 {
        let den = 1.0 + 0.5 * u;
        return (den.ln(), -r / (c2 * den));
    }
    let beta = (alpha - 2.0).abs();
    let inner = u / beta + 1.0;
    let rho = (beta / alpha) * (inner.powf(0.5 * alpha) - 1.0);
    let g = -(r / c2) * inner.powf(0.5 * alpha - 1.0);
    (rho, g)
}

impl PartialFit for OnlineBarronRegressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineBarronRegressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Barron update"),
            );
        };
        self.initialized = true;
        let alpha = if self.alpha.is_finite() { self.alpha } else { 1.0 };
        let c = if self.scale.is_finite() && self.scale > 0.0 {
            self.scale
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let (rho, g) = barron_rho_grad(r, alpha, c);
            loss_before += rho;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += barron_rho_grad(y[i] - ea, alpha, c).0;
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Barron regressor",
            "SGD on the Barron robust family; α is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineBarronRegressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Lightweight online detector of anomalies (river `anomaly.LODA`).
///
/// Projection count is not identification `p`. Distinct from [`HalfSpaceTrees`]
/// (axis-aligned splits) and [`GaussianScorer`] (per-feature moments).
#[derive(Clone, Debug)]
pub struct Loda {
    /// Random projections. Not identification `p`.
    pub n_projections: usize,
    /// Histogram bins. Not identification `p`.
    pub n_bins: usize,
    weights: Vec<Vector>,
    lo: Vector,
    hi: Vector,
    counts: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Loda {
    fn default() -> Self {
        Self {
            n_projections: 4,
            n_bins: 8,
            weights: Vec::new(),
            lo: Vector::zeros(0),
            hi: Vector::zeros(0),
            counts: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Loda {
    /// LODA with `n_projections` histograms.
    pub fn new(n_projections: usize) -> Self {
        Self {
            n_projections: n_projections.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for Loda {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let k = self.n_projections.max(1);
        let nb = self.n_bins.max(2);
        if !self.initialized {
            let mut rng = Rng::new(0x10da);
            self.weights = (0..k)
                .map(|_| {
                    Vector::from_iter((0..x.ncols()).map(|_| rng.standard_normal()))
                })
                .collect();
            self.lo = Vector::filled(k, f64::INFINITY);
            self.hi = Vector::filled(k, f64::NEG_INFINITY);
            self.counts = (0..k).map(|_| Vector::zeros(nb)).collect();
            self.initialized = true;
        } else if self.weights.as_slice().first().map(|w| w.len()) != Some(x.ncols()) {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            for j in 0..k {
                let mut z = 0.0_f64;
                for c in 0..x.ncols() {
                    z += self.weights[j][c] * x.get(i, c);
                }
                if !z.is_finite() {
                    continue;
                }
                if z < self.lo[j] {
                    self.lo[j] = z;
                }
                if z > self.hi[j] {
                    self.hi[j] = z;
                }
                let span = self.hi[j] - self.lo[j];
                let bin = if span <= 1e-12 {
                    0
                } else {
                    let u = ((z - self.lo[j]) / span * (nb as f64 - 1e-9)).floor() as usize;
                    u.min(nb - 1)
                };
                self.counts[j][bin] += 1.0;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("LODA updated {k} projection histograms on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "projection histograms",
                "LODA incrementally bins random 1-d projections",
                "previous bin counts",
                "updated bin counts",
            ),
        )
    }
}

impl Predict for Loda {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let k = self.weights.len();
        let nb = self.n_bins.max(2);
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut acc = 0.0_f64;
            let mut ok = 0usize;
            for j in 0..k {
                let mut z = 0.0_f64;
                for c in 0..x.ncols().min(self.weights[j].len()) {
                    z += self.weights[j][c] * x.get(i, c);
                }
                let span = self.hi[j] - self.lo[j];
                let bin = if span <= 1e-12 {
                    0
                } else {
                    let u = ((z - self.lo[j]) / span * (nb as f64 - 1e-9)).floor() as usize;
                    u.min(nb - 1)
                };
                let tot: f64 = self.counts[j].as_slice().iter().sum();
                let p = if tot > 0.0 {
                    (self.counts[j][bin] / tot).max(1e-12)
                } else {
                    1.0
                };
                acc -= p.ln();
                ok += 1;
            }
            if ok == 0 {
                0.0
            } else {
                acc / ok as f64
            }
        })))
    }
}

/// Online Soft-L1 regressor \(\rho=\ln(1+|r|)\).
///
/// Influence \(\psi(r)=\mathrm{sign}(r)/(1+|r|)\) is strictly between L1 and
/// L2. Distinct from [`OnlineLaplaceRegressor`] (\(\rho=|r|\)) and
/// [`OnlineCharbonnierRegressor`] (\(\sqrt{r^2+c^2}-c\)).
#[derive(Clone, Debug)]
pub struct OnlineSoftL1Regressor {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineSoftL1Regressor {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineSoftL1Regressor {
    /// Soft-L1 SGD regressor.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineSoftL1Regressor {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineSoftL1Regressor",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected Soft-L1 update"),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let r = y[i] - eta;
            let ar = r.abs();
            let rho = (1.0 + ar).ln();
            let g = if ar < 1e-15 {
                0.0
            } else {
                -r.signum() / (1.0 + ar)
            };
            loss_before += rho;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += (1.0 + (y[i] - ea).abs()).ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online Soft-L1 regressor",
            "SGD on ln(1+|r|); distinct from Laplace and Charbonnier",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineSoftL1Regressor {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online squared-hinge linear classifier.
///
/// \(\ell=\max(0,1-y\eta)^2\) on labels mapped to \(\{\pm 1\}\). Distinct from
/// [`LogisticRegression`] (log-loss) and [`OnlineSvm`] (hinge / Pegasos).
#[derive(Clone, Debug)]
pub struct OnlineSquaredHingeClassifier {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineSquaredHingeClassifier {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineSquaredHingeClassifier {
    /// Squared-hinge SGD classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineSquaredHingeClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineSquaredHingeClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "rejected squared-hinge update",
                ),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let m = 1.0 - ys * eta;
            let (rho, g) = if m > 0.0 {
                (m * m, -2.0 * ys * m)
            } else {
                (0.0, 0.0)
            };
            loss_before += rho;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let ma = 1.0 - ys * ea;
            loss_after += if ma > 0.0 { ma * ma } else { 0.0 };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online squared-hinge classifier",
            "SGD on max(0,1-yη)²; distinct from log-loss and linear hinge",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineSquaredHingeClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online binary focal-loss classifier (Lin et al.).
///
/// Focusing \(\gamma\) is not identification `p`. Distinct from
/// [`LogisticRegression`] (\(\gamma=0\)) and [`OnlineSquaredHingeClassifier`].
#[derive(Clone, Debug)]
pub struct OnlineFocalClassifier {
    /// Focusing parameter. Not identification `p`.
    pub gamma: f64,
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineFocalClassifier {
    fn default() -> Self {
        Self {
            gamma: 2.0,
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineFocalClassifier {
    /// Focal classifier with focusing `gamma`.
    pub fn new(gamma: f64) -> Self {
        Self {
            gamma,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineFocalClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineFocalClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected focal update"),
            );
        };
        self.initialized = true;
        let gamma = if self.gamma.is_finite() && self.gamma >= 0.0 {
            self.gamma
        } else {
            2.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let z = ys * eta;
            let p = (1.0 / (1.0 + (-z).exp())).clamp(1e-12, 1.0 - 1e-12);
            let om = 1.0 - p;
            let rho = -(om.powf(gamma)) * p.ln();
            let dldz = gamma * p * om.powf(gamma) * p.ln() - om.powf(gamma + 1.0);
            let g = dldz * ys;
            loss_before += rho;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let pa = (1.0 / (1.0 + (-ys * ea).exp())).clamp(1e-12, 1.0 - 1e-12);
            loss_after += -((1.0 - pa).powf(gamma)) * pa.ln();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online focal classifier",
            "SGD on −(1−p)^γ log p; γ is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineFocalClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Online modified-Huber classifier (Zhang / sklearn `modified_huber`).
///
/// Distinct from [`OnlineSquaredHingeClassifier`] (no linear far-tail) and
/// [`OnlineHuberRegressor`] (regression residual).
#[derive(Clone, Debug)]
pub struct OnlineModifiedHuberClassifier {
    /// SGD step.
    pub learning_rate: f64,
    coef: Vector,
    intercept: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineModifiedHuberClassifier {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            coef: Vector::zeros(0),
            intercept: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineModifiedHuberClassifier {
    /// Modified-Huber SGD classifier.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineModifiedHuberClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, lr)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            self.learning_rate,
            "OnlineModifiedHuberClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "rejected modified-Huber update",
                ),
            );
        };
        self.initialized = true;
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            for j in 0..x.ncols() {
                eta += self.coef[j] * x.get(i, j);
            }
            let z = ys * eta;
            let (rho, g) = if z >= 1.0 {
                (0.0, 0.0)
            } else if z >= -1.0 {
                let m = 1.0 - z;
                (m * m, -2.0 * m * ys)
            } else {
                (-4.0 * z, -4.0 * ys)
            };
            loss_before += rho;
            self.intercept -= lr * g;
            for j in 0..x.ncols() {
                self.coef[j] -= lr * g * x.get(i, j);
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let za = ys * ea;
            loss_after += if za >= 1.0 {
                0.0
            } else if za >= -1.0 {
                let m = 1.0 - za;
                m * m
            } else {
                -4.0 * za
            };
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online modified-Huber classifier",
            "SGD on Zhang modified Huber; linear far-tail, quadratic near-margin",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineModifiedHuberClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Adaptive Regularization of Weights classifier (Crammer et al. AROW).
///
/// Diagonal covariance \(r\) is not identification `p`. Distinct from
/// [`PassiveAggressive`] (no covariance) and [`Perceptron`] (no margin
/// confidence).
#[derive(Clone, Debug)]
pub struct OnlineArowClassifier {
    /// Confidence regularizer. Not identification `p`.
    pub r: f64,
    coef: Vector,
    intercept: f64,
    sigma: Vector,
    intercept_var: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineArowClassifier {
    fn default() -> Self {
        Self {
            r: 1.0,
            coef: Vector::zeros(0),
            intercept: 0.0,
            sigma: Vector::zeros(0),
            intercept_var: 1.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineArowClassifier {
    /// AROW classifier with confidence `r`.
    pub fn new(r: f64) -> Self {
        Self {
            r,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineArowClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, _)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            0.05,
            "OnlineArowClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected AROW update"),
            );
        };
        if !self.initialized {
            self.sigma = Vector::filled(x.ncols(), 1.0);
            self.intercept_var = 1.0;
        } else if self.sigma.len() != x.ncols() {
            self.sigma = Vector::filled(x.ncols(), 1.0);
        }
        self.initialized = true;
        let r = if self.r.is_finite() && self.r > 0.0 {
            self.r
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            let mut v = self.intercept_var;
            for j in 0..x.ncols() {
                let xj = x.get(i, j);
                eta += self.coef[j] * xj;
                v += self.sigma[j] * xj * xj;
            }
            let m = ys * eta;
            let hinge = (1.0 - m).max(0.0);
            loss_before += hinge;
            if hinge > 0.0 {
                let beta = 1.0 / (v + r).max(1e-8);
                let alpha = hinge * beta;
                self.intercept += alpha * ys * self.intercept_var;
                self.intercept_var = (self.intercept_var - beta * self.intercept_var * self.intercept_var)
                    .max(1e-8);
                for j in 0..x.ncols() {
                    let xj = x.get(i, j);
                    self.coef[j] += alpha * ys * self.sigma[j] * xj;
                    let sj = self.sigma[j];
                    self.sigma[j] = (sj - beta * (sj * xj) * (sj * xj)).max(1e-8);
                }
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += (1.0 - ys * ea).max(0.0);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online AROW classifier",
            "confidence-weighted margin update; r is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineArowClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Soft Confidence-Weighted classifier (Wang, Crammer, Vucetic).
///
/// Probability \(\varphi\) is not identification `p`. Distinct from
/// [`OnlineArowClassifier`] (hard margin) and [`PassiveAggressive`].
#[derive(Clone, Debug)]
pub struct OnlineScwClassifier {
    /// Target probability. Not identification `p`.
    pub phi: f64,
    /// Aggressiveness cap. Not identification `p`.
    pub c: f64,
    coef: Vector,
    intercept: f64,
    sigma: Vector,
    intercept_var: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineScwClassifier {
    fn default() -> Self {
        Self {
            phi: 0.7,
            c: 1.0,
            coef: Vector::zeros(0),
            intercept: 0.0,
            sigma: Vector::zeros(0),
            intercept_var: 1.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineScwClassifier {
    /// SCW classifier with target probability `phi`.
    pub fn new(phi: f64) -> Self {
        Self {
            phi,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineScwClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, _)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            0.05,
            "OnlineScwClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected SCW update"),
            );
        };
        if !self.initialized {
            self.sigma = Vector::filled(x.ncols(), 1.0);
            self.intercept_var = 1.0;
        } else if self.sigma.len() != x.ncols() {
            self.sigma = Vector::filled(x.ncols(), 1.0);
        }
        self.initialized = true;
        let phi = if self.phi.is_finite() && self.phi > 0.0 {
            self.phi.clamp(0.05, 0.95)
        } else {
            0.7
        };
        let cap = if self.c.is_finite() && self.c > 0.0 {
            self.c
        } else {
            1.0
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            let mut v = self.intercept_var;
            for j in 0..x.ncols() {
                let xj = x.get(i, j);
                eta += self.coef[j] * xj;
                v += self.sigma[j] * xj * xj;
            }
            let m = ys * eta;
            let hinge = (1.0 - m).max(0.0);
            loss_before += hinge;
            if hinge > 0.0 {
                let alpha = (hinge / (v + 1.0 / (2.0 * phi)).max(1e-8)).clamp(0.0, cap);
                let beta = alpha / (1.0 + 2.0 * phi * v * alpha).max(1e-8);
                self.intercept += alpha * ys * self.intercept_var;
                self.intercept_var = (self.intercept_var
                    - beta * self.intercept_var * self.intercept_var)
                    .max(1e-8);
                for j in 0..x.ncols() {
                    let xj = x.get(i, j);
                    self.coef[j] += alpha * ys * self.sigma[j] * xj;
                    let sj = self.sigma[j];
                    self.sigma[j] = (sj - beta * (sj * xj) * (sj * xj)).max(1e-8);
                }
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            loss_after += (1.0 - ys * ea).max(0.0);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online SCW classifier",
            "soft confidence-weighted margin; φ is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineScwClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Confidence-Weighted classifier (Crammer, Dredze, Pereira).
///
/// Probability \(\varphi\) is not identification `p`. Distinct from
/// [`OnlineArowClassifier`] (no \(\varphi\sqrt{v}\) constraint) and
/// [`OnlineScwClassifier`] (soft cap).
#[derive(Clone, Debug)]
pub struct OnlineCwClassifier {
    /// Target probability. Not identification `p`.
    pub phi: f64,
    coef: Vector,
    intercept: f64,
    sigma: Vector,
    intercept_var: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineCwClassifier {
    fn default() -> Self {
        Self {
            phi: 0.7,
            coef: Vector::zeros(0),
            intercept: 0.0,
            sigma: Vector::zeros(0),
            intercept_var: 1.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineCwClassifier {
    /// CW classifier with target probability `phi`.
    pub fn new(phi: f64) -> Self {
        Self {
            phi,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineCwClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some((y, _)) = online_glm_prepare(
            &mut ctx,
            x,
            y,
            self.initialized,
            self.n_seen,
            &mut self.coef,
            0.05,
            "OnlineCwClassifier",
        ) else {
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "rejected CW update"),
            );
        };
        if !self.initialized {
            self.sigma = Vector::filled(x.ncols(), 1.0);
            self.intercept_var = 1.0;
        } else if self.sigma.len() != x.ncols() {
            self.sigma = Vector::filled(x.ncols(), 1.0);
        }
        self.initialized = true;
        let phi = if self.phi.is_finite() && self.phi > 0.0 {
            self.phi.clamp(0.05, 0.95)
        } else {
            0.7
        };
        let before = self.coef.clone();
        let mut loss_before = 0.0_f64;
        let mut loss_after = 0.0_f64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = if y[i] >= 0.5 { 1.0 } else { -1.0 };
            let mut eta = self.intercept;
            let mut v = self.intercept_var;
            for j in 0..x.ncols() {
                let xj = x.get(i, j);
                eta += self.coef[j] * xj;
                v += self.sigma[j] * xj * xj;
            }
            let m = ys * eta;
            let gap = phi * v.sqrt() - m;
            loss_before += gap.max(0.0);
            if gap > 0.0 {
                let den = v.max(1e-8) * (1.0 + phi * v.sqrt() * 0.5);
                let alpha = (gap / den).max(0.0);
                let beta = 2.0 * alpha * phi / (1.0 + 2.0 * alpha * phi * v).max(1e-8);
                self.intercept += alpha * ys * self.intercept_var;
                self.intercept_var = (self.intercept_var
                    - beta * self.intercept_var * self.intercept_var)
                    .max(1e-8);
                for j in 0..x.ncols() {
                    let xj = x.get(i, j);
                    self.coef[j] += alpha * ys * self.sigma[j] * xj;
                    let sj = self.sigma[j];
                    self.sigma[j] = (sj - beta * (sj * xj) * (sj * xj)).max(1e-8);
                }
            }
            let mut ea = self.intercept;
            for j in 0..x.ncols() {
                ea += self.coef[j] * x.get(i, j);
            }
            let va = self.intercept_var
                + (0..x.ncols())
                    .map(|j| self.sigma[j] * x.get(i, j) * x.get(i, j))
                    .sum::<f64>();
            loss_after += (phi * va.max(0.0).sqrt() - ys * ea).max(0.0);
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let delta = self.coef.sub(&before);
        let expl = pack_linear_explain(
            &mut ctx,
            self.updates,
            n_rows,
            self.n_seen,
            &delta,
            loss_before,
            loss_after,
            self.n_seen as usize > x.ncols(),
            self.n_seen < 5,
            "online CW classifier",
            "confidence-weighted update with φ√v margin; φ is not identification p",
        );
        finish_explain(ctx, expl)
    }
}

impl Predict for OnlineCwClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = self.intercept;
            for j in 0..x.ncols().min(self.coef.len()) {
                s += self.coef[j] * x.get(i, j);
            }
            s
        })))
    }
}

/// Streaming power-method / Oja PCA (river `decomposition.PCA`).
///
/// Distinct from [`crate::decompose::IncrementalPca`] (SKL / sequential
/// Karhunen–Loève SVD on stacked batches). Component count is not
/// identification `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlinePca {
    /// Axes to track. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// Base Oja step; effective rate is `lr / (n_seen + 1)`.
    pub learning_rate: f64,
    mean: Vector,
    components: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlinePca {
    fn default() -> Self {
        Self {
            n_components: 1,
            learning_rate: 1.0,
            mean: Vector::zeros(0),
            components: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlinePca {
    /// Track `n_components` Oja axes.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlinePca {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.components = (0..k)
                .map(|c| {
                    let mut w = Vector::zeros(p);
                    w[c % p] = 1.0;
                    w
                })
                .collect();
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            1.0
        };
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            for j in 0..p {
                self.mean[j] += (x.get(i, j) - self.mean[j]) / n;
            }
            let mut residual = Vector::from_iter((0..p).map(|j| x.get(i, j) - self.mean[j]));
            let lr = lr0 / n;
            for c in 0..self.components.len() {
                let y = self.components[c].dot(&residual);
                for j in 0..p {
                    self.components[c][j] += lr * y * residual[j];
                }
                for prev in 0..c {
                    let proj = self.components[c].dot(&self.components[prev]);
                    for j in 0..p {
                        self.components[c][j] -= proj * self.components[prev][j];
                    }
                }
                let nrm = self.components[c].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        self.components[c][j] /= nrm;
                    }
                }
                let y2 = self.components[c].dot(&residual);
                for j in 0..p {
                    residual[j] -= y2 * self.components[c][j];
                }
            }
        }
        self.updates += 1;
        let after = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < (p as u64).saturating_add(2);
        q.explanation = format!(
            "OnlinePca Oja-updated {k} axes on {} rows; ||Δw0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Oja principal axes",
                "streaming power-method PCA; distinct from SKL IncrementalPca",
                "previous axes",
                "updated axes",
            ),
        )
    }
}

impl Transform for OnlinePca {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.components.len();
        let p = self.mean.len();
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut s = 0.0_f64;
            for j in 0..x.ncols().min(p) {
                s += self.components[c][j] * (x.get(i, j) - self.mean[j]);
            }
            s
        }))
    }
}

/// One DWM linear expert (Rosenblatt weights; not a public [`Perceptron`]).
#[derive(Clone, Debug)]
struct DwmExpert {
    coef: Vector,
    intercept: f64,
}

impl DwmExpert {
    fn new(p: usize) -> Self {
        Self {
            coef: Vector::zeros(p),
            intercept: 0.0,
        }
    }

    fn score_pm(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.intercept;
        for j in 0..x.ncols().min(self.coef.len()) {
            s += self.coef[j] * x.get(i, j);
        }
        if s >= 0.0 {
            1.0
        } else {
            -1.0
        }
    }

    fn update(&mut self, x: &Matrix, i: usize, ys: f64) {
        if self.coef.len() != x.ncols() {
            *self = Self::new(x.ncols());
        }
        let mut s = self.intercept;
        for j in 0..x.ncols() {
            s += self.coef[j] * x.get(i, j);
        }
        if ys * s <= 0.0 {
            for j in 0..x.ncols() {
                self.coef[j] += ys * x.get(i, j);
            }
            self.intercept += ys;
        }
    }
}

/// Dynamic Weighted Majority classifier (Kolter & Maloof; river `ensemble.DWMClassifier`).
///
/// Experts are born on ensemble mistakes and die when their weight falls below
/// `θ`. Distinct from [`BoleClassifier`] (fixed pool, train-on-mistake only)
/// and [`HedgeRegressor`] (no expert birth/death). `β` and `θ` are not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct DwmClassifier {
    /// Multiplicative penalty on an expert mistake.
    pub beta: f64,
    /// Removal threshold.
    pub theta: f64,
    /// Cap on the expert pool. Not identification `p`.
    pub max_experts: usize,
    experts: Vec<DwmExpert>,
    weights: Vec<f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for DwmClassifier {
    fn default() -> Self {
        Self {
            beta: 0.5,
            theta: 0.01,
            max_experts: 8,
            experts: Vec::new(),
            weights: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl DwmClassifier {
    /// DWM with mistake penalty `beta`.
    pub fn new(beta: f64) -> Self {
        Self {
            beta,
            ..Self::default()
        }
    }

    fn vote_pm(&self, x: &Matrix, i: usize) -> f64 {
        let mut acc = 0.0_f64;
        let mut wsum = 0.0_f64;
        for (e, w) in self.experts.iter().zip(self.weights.iter()) {
            acc += *w * e.score_pm(x, i);
            wsum += *w;
        }
        if wsum <= 0.0 {
            0.0
        } else if acc >= 0.0 {
            1.0
        } else {
            -1.0
        }
    }

    fn prune_and_normalize(&mut self) {
        let th = if self.theta.is_finite() && self.theta > 0.0 {
            self.theta
        } else {
            0.01
        };
        if self.experts.len() > 1 {
            let keep: Vec<usize> = self
                .weights
                .iter()
                .enumerate()
                .filter(|(_, w)| **w >= th)
                .map(|(i, _)| i)
                .collect();
            if !keep.is_empty() {
                self.experts = keep.iter().map(|&i| self.experts[i].clone()).collect();
                self.weights = keep.iter().map(|&i| self.weights[i]).collect();
            }
        }
        let wsum: f64 = self.weights.iter().sum();
        if wsum > 1e-15 {
            for w in self.weights.iter_mut() {
                *w /= wsum;
            }
        }
    }
}

impl PartialFit for DwmClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "DWM needs a target"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if self.initialized {
            if self
                .experts
                .as_slice()
                .first()
                .map(|e| e.coef.len())
                != Some(x.ncols())
            {
                ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
                );
            }
        } else {
            self.experts.push(DwmExpert::new(x.ncols()));
            self.weights.push(1.0);
            self.initialized = true;
        }
        let beta = if self.beta.is_finite() && self.beta > 0.0 && self.beta < 1.0 {
            self.beta
        } else {
            0.5
        };
        let cap = self.max_experts.max(1);
        let mut births = 0u64;
        let mut mistakes = 0u64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = as_pm(y[i]);
            let vote = self.vote_pm(x, i);
            if vote != ys {
                mistakes += 1;
            }
            for (e, w) in self.experts.iter().zip(self.weights.iter_mut()) {
                if e.score_pm(x, i) != ys {
                    *w *= beta;
                }
            }
            for e in self.experts.iter_mut() {
                e.update(x, i, ys);
            }
            if vote != ys && self.experts.len() < cap {
                let mut newborn = DwmExpert::new(x.ncols());
                newborn.update(x, i, ys);
                self.experts.push(newborn);
                self.weights.push(1.0);
                births += 1;
            }
            self.prune_and_normalize();
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), n_rows, self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(births as f64 + mistakes as f64);
        q.information_gain = Some((mistakes + births) as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "DWM {mistakes} ensemble mistakes, {births} births, {} experts remain",
            self.experts.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "DWM expert pool",
                "dynamic weighted majority with birth on mistakes and death below θ",
                "previous expert weights",
                "updated expert weights",
            ),
        )
    }
}

impl Predict for DwmClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            if self.vote_pm(x, i) >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Streaming natural-gradient ICA (Amari; online Infomax).
///
/// Distinct from [`crate::decompose::FastIca`] (batch fixed-point Newton) and
/// [`OnlinePca`] (second-order Oja axes). Component count is not identification
/// `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineIca {
    /// Independent components. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// Base natural-gradient step.
    pub learning_rate: f64,
    mean: Vector,
    unmix: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineIca {
    fn default() -> Self {
        Self {
            n_components: 1,
            learning_rate: 0.05,
            mean: Vector::zeros(0),
            unmix: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineIca {
    /// Track `n_components` independent directions.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineIca {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.unmix = (0..k)
                .map(|c| {
                    let mut w = Vector::zeros(p);
                    w[c % p] = 1.0;
                    w
                })
                .collect();
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .unmix
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            for j in 0..p {
                self.mean[j] += (x.get(i, j) - self.mean[j]) / n;
            }
            let xc = Vector::from_iter((0..p).map(|j| x.get(i, j) - self.mean[j]));
            let y = Vector::from_iter(self.unmix.iter().map(|w| w.dot(&xc)));
            let g = Vector::from_iter((0..y.len()).map(|c| y[c].tanh()));
            let lr = lr0 / n.sqrt();
            let kk = self.unmix.len();
            let mut gy = vec![vec![0.0_f64; kk]; kk];
            for a in 0..kk {
                for b in 0..kk {
                    gy[a][b] = g[a] * y[b];
                }
            }
            let mut new_w = self.unmix.clone();
            for a in 0..kk {
                for j in 0..p {
                    let mut acc = 0.0_f64;
                    for b in 0..kk {
                        let igr = if a == b { 1.0 } else { 0.0 };
                        acc += (igr - gy[a][b]) * self.unmix[b][j];
                    }
                    new_w[a][j] += lr * acc;
                }
            }
            for a in 0..kk {
                for prev in 0..a {
                    let proj = new_w[a].dot(&new_w[prev]);
                    for j in 0..p {
                        new_w[a][j] -= proj * new_w[prev][j];
                    }
                }
                let nrm = new_w[a].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        new_w[a][j] /= nrm;
                    }
                }
            }
            self.unmix = new_w;
        }
        self.updates += 1;
        let after = self
            .unmix
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < (p as u64).saturating_add(2);
        q.explanation = format!(
            "OnlineIca Amari-updated {k} unmixing rows on {} rows; ||Δw0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Amari unmixing",
                "streaming natural-gradient ICA; distinct from batch FastICA",
                "previous W",
                "updated W",
            ),
        )
    }
}

impl Transform for OnlineIca {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.unmix.len();
        let p = self.mean.len();
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut s = 0.0_f64;
            for j in 0..x.ncols().min(p) {
                s += self.unmix[c][j] * (x.get(i, j) - self.mean[j]);
            }
            s
        }))
    }
}

/// One Learn++.NSE linear expert.
#[derive(Clone, Debug)]
struct NseExpert {
    coef: Vector,
    intercept: f64,
    err: f64,
}

impl NseExpert {
    fn new(p: usize) -> Self {
        Self {
            coef: Vector::zeros(p),
            intercept: 0.0,
            err: 0.5,
        }
    }

    fn score_pm(&self, x: &Matrix, i: usize) -> f64 {
        let mut s = self.intercept;
        for j in 0..x.ncols().min(self.coef.len()) {
            s += self.coef[j] * x.get(i, j);
        }
        if s >= 0.0 {
            1.0
        } else {
            -1.0
        }
    }

    fn update(&mut self, x: &Matrix, i: usize, ys: f64) {
        if self.coef.len() != x.ncols() {
            let err = self.err;
            *self = Self::new(x.ncols());
            self.err = err;
        }
        let mut s = self.intercept;
        for j in 0..x.ncols() {
            s += self.coef[j] * x.get(i, j);
        }
        if ys * s <= 0.0 {
            for j in 0..x.ncols() {
                self.coef[j] += ys * x.get(i, j);
            }
            self.intercept += ys;
        }
    }
}

/// Learn++.NSE classifier (Ditzler & Polikar; river `ensemble.LearnNseClassifier`).
///
/// Experts are born when the ensemble is wrong; weights are sigmoid functions
/// of an EMA error (no death). Distinct from [`DwmClassifier`] (multiplicative
/// decay and removal) and [`BoleClassifier`] (fixed pool). `β` is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct LearnNseClassifier {
    /// Sigmoid slope on the error.
    pub beta: f64,
    /// Cap on the expert pool. Not identification `p`.
    pub max_experts: usize,
    experts: Vec<NseExpert>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LearnNseClassifier {
    fn default() -> Self {
        Self {
            beta: 1.0,
            max_experts: 8,
            experts: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LearnNseClassifier {
    /// Learn++.NSE with sigmoid slope `beta`.
    pub fn new(beta: f64) -> Self {
        Self {
            beta,
            ..Self::default()
        }
    }

    fn vote_pm(&self, x: &Matrix, i: usize) -> f64 {
        let beta = if self.beta.is_finite() && self.beta > 0.0 {
            self.beta
        } else {
            1.0
        };
        let mut acc = 0.0_f64;
        let mut wsum = 0.0_f64;
        for e in &self.experts {
            let w = 1.0 / (1.0 + (beta * (2.0 * e.err - 1.0)).exp());
            acc += w * e.score_pm(x, i);
            wsum += w;
        }
        if wsum <= 0.0 {
            0.0
        } else if acc >= 0.0 {
            1.0
        } else {
            -1.0
        }
    }
}

impl PartialFit for LearnNseClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "LearnNSE needs a target"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if self.initialized {
            if self
                .experts
                .as_slice()
                .first()
                .map(|e| e.coef.len())
                != Some(x.ncols())
            {
                ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
                );
            }
        } else {
            self.experts.push(NseExpert::new(x.ncols()));
            self.initialized = true;
        }
        let cap = self.max_experts.max(1);
        let mut births = 0u64;
        let mut mistakes = 0u64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = as_pm(y[i]);
            let vote = self.vote_pm(x, i);
            if vote != ys {
                mistakes += 1;
                if self.experts.len() < cap {
                    self.experts.push(NseExpert::new(x.ncols()));
                    births += 1;
                }
            }
            for e in self.experts.iter_mut() {
                let wrong = e.score_pm(x, i) != ys;
                e.err = 0.9 * e.err + 0.1 * if wrong { 1.0 } else { 0.0 };
                e.update(x, i, ys);
            }
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), n_rows, self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(births as f64 + mistakes as f64);
        q.information_gain = Some((mistakes + births) as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "LearnNSE {mistakes} ensemble mistakes, {births} births, {} experts remain",
            self.experts.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Learn++.NSE pool",
                "sigmoid-weighted experts born on ensemble error; no death",
                "previous expert errors",
                "updated expert errors",
            ),
        )
    }
}

impl Predict for LearnNseClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            if self.vote_pm(x, i) >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Additive Expert Ensemble classifier (Kolter & Maloof; river `ensemble.AdditiveExpertEnsemble`).
///
/// Experts are born on ensemble mistakes and never die; weights decay by `β`
/// but are not pruned. Distinct from [`DwmClassifier`] (threshold death) and
/// [`LearnNseClassifier`] (sigmoid EMA weights). `β` is not identification `p`.
#[derive(Clone, Debug)]
pub struct AdditiveExpertClassifier {
    /// Multiplicative penalty on an expert mistake.
    pub beta: f64,
    /// Cap on the expert pool. Not identification `p`.
    pub max_experts: usize,
    experts: Vec<DwmExpert>,
    weights: Vec<f64>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdditiveExpertClassifier {
    fn default() -> Self {
        Self {
            beta: 0.5,
            max_experts: 8,
            experts: Vec::new(),
            weights: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdditiveExpertClassifier {
    /// Additive experts with mistake penalty `beta`.
    pub fn new(beta: f64) -> Self {
        Self {
            beta,
            ..Self::default()
        }
    }

    fn vote_pm(&self, x: &Matrix, i: usize) -> f64 {
        let mut acc = 0.0_f64;
        let mut wsum = 0.0_f64;
        for (e, w) in self.experts.iter().zip(self.weights.iter()) {
            acc += *w * e.score_pm(x, i);
            wsum += *w;
        }
        if wsum <= 0.0 {
            0.0
        } else if acc >= 0.0 {
            1.0
        } else {
            -1.0
        }
    }
}

impl PartialFit for AdditiveExpertClassifier {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(
                    self.updates,
                    x.nrows(),
                    self.n_seen,
                    "AdditiveExpert needs a target",
                ),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        if self.initialized {
            if self
                .experts
                .as_slice()
                .first()
                .map(|e| e.coef.len())
                != Some(x.ncols())
            {
                ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
                );
            }
        } else {
            self.experts.push(DwmExpert::new(x.ncols()));
            self.weights.push(1.0);
            self.initialized = true;
        }
        let beta = if self.beta.is_finite() && self.beta > 0.0 && self.beta < 1.0 {
            self.beta
        } else {
            0.5
        };
        let cap = self.max_experts.max(1);
        let mut births = 0u64;
        let mut mistakes = 0u64;
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let ys = as_pm(y[i]);
            let vote = self.vote_pm(x, i);
            if vote != ys {
                mistakes += 1;
            }
            for (e, w) in self.experts.iter().zip(self.weights.iter_mut()) {
                if e.score_pm(x, i) != ys {
                    *w *= beta;
                }
            }
            for e in self.experts.iter_mut() {
                e.update(x, i, ys);
            }
            if vote != ys && self.experts.len() < cap {
                let mut newborn = DwmExpert::new(x.ncols());
                newborn.update(x, i, ys);
                self.experts.push(newborn);
                self.weights.push(1.0);
                births += 1;
            }
            let wsum: f64 = self.weights.iter().sum();
            if wsum > 1e-15 {
                for w in self.weights.iter_mut() {
                    *w /= wsum;
                }
            }
        }
        self.n_seen += n_rows as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), n_rows, self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(births as f64 + mistakes as f64);
        q.information_gain = Some((mistakes + births) as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "AdditiveExpert {mistakes} ensemble mistakes, {births} births, {} experts remain",
            self.experts.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "additive expert pool",
                "weights decay on mistakes; experts are born and never die",
                "previous expert weights",
                "updated expert weights",
            ),
        )
    }
}

impl Predict for AdditiveExpertClassifier {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            if self.vote_pm(x, i) >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Streaming multiplicative NMF (Lee–Seung rank-1 residual updates).
///
/// Distinct from [`crate::decompose::MiniBatchNmf`] (batched MU on stacked
/// blocks). Component count is not identification `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineNmf {
    /// Nonnegative components. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// Additive step on the residual outer product.
    pub learning_rate: f64,
    w: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineNmf {
    fn default() -> Self {
        Self {
            n_components: 1,
            learning_rate: 0.1,
            w: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineNmf {
    /// Track `n_components` nonnegative basis vectors.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineNmf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        let mut n_neg = 0usize;
        for i in 0..x.nrows() {
            for j in 0..p {
                if x.get(i, j).is_finite() && x.get(i, j) < 0.0 {
                    n_neg += 1;
                }
            }
        }
        if n_neg > 0 {
            ctx.push(
                Issue::builder(IssueCode::NonPositiveSeries)
                    .severity(Severity::Warning)
                    .message(format!("OnlineNmf clipped {n_neg} negative entries"))
                    .build(),
            );
        }
        if !self.initialized {
            self.w = (0..k)
                .map(|c| {
                    let mut col = Vector::filled(p, 0.1);
                    col[c % p] = 1.0;
                    col
                })
                .collect();
            self.initialized = true;
        } else if self.w.as_slice().first().map(|c| c.len()) != Some(p) {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .w
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.1
        };
        for i in 0..x.nrows() {
            let xi = Vector::from_iter((0..p).map(|j| x.get(i, j).max(0.0)));
            let h = Vector::from_iter(self.w.iter().map(|wc| {
                let den = wc.dot(wc).max(1e-8);
                (wc.dot(&xi) / den).max(0.0)
            }));
            let recon = Vector::from_iter((0..p).map(|j| {
                let mut s = 0.0_f64;
                for c in 0..self.w.len() {
                    s += self.w[c][j] * h[c];
                }
                s
            }));
            for c in 0..self.w.len() {
                for j in 0..p {
                    self.w[c][j] = (self.w[c][j] + lr * (xi[j] - recon[j]) * h[c]).max(0.0);
                }
                let nrm = self.w[c].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        self.w[c][j] /= nrm;
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self
            .w
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineNmf residual-updated {k} nonnegative axes on {} rows; ||Δw0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "nonnegative basis",
                "streaming NMF; distinct from mini-batch MU IncrementalNMF",
                "previous W",
                "updated W",
            ),
        )
    }
}

impl Transform for OnlineNmf {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.w.len();
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut num = 0.0_f64;
            let mut den = 0.0_f64;
            for j in 0..x.ncols().min(self.w[c].len()) {
                let xj = x.get(i, j).max(0.0);
                num += self.w[c][j] * xj;
                den += self.w[c][j] * self.w[c][j];
            }
            num / den.max(1e-8)
        }))
    }
}

/// Streaming Oja CCA on a column-split two-view stream.
///
/// The first `⌈p/2⌉` columns are view X, the rest view Y. Distinct from
/// [`crate::decompose::Cca`] (batch SVD of the whitened cross-covariance).
/// The single canonical pair is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineCca {
    /// Oja step.
    pub learning_rate: f64,
    mean_x: Vector,
    mean_y: Vector,
    u: Vector,
    v: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineCca {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            mean_x: Vector::zeros(0),
            mean_y: Vector::zeros(0),
            u: Vector::zeros(0),
            v: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineCca {
    /// Streaming CCA with Oja step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

fn split_views(p: usize) -> (usize, usize) {
    let px = (p + 1) / 2;
    let py = p - px;
    if py == 0 {
        (p, p)
    } else {
        (px, py)
    }
}

impl PartialFit for OnlineCca {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let (px, py) = split_views(p);
        if !self.initialized {
            self.mean_x = Vector::zeros(px);
            self.mean_y = Vector::zeros(py);
            self.u = Vector::filled(px, 0.0);
            self.u[0] = 1.0;
            self.v = Vector::filled(py, 0.0);
            self.v[0] = 1.0;
            self.initialized = true;
        } else if self.u.len() != px || self.v.len() != py {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.u.clone();
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let same = py == p && px == p;
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            for j in 0..px {
                self.mean_x[j] += (x.get(i, j) - self.mean_x[j]) / n;
            }
            for j in 0..py {
                let src = if same { j } else { px + j };
                self.mean_y[j] += (x.get(i, src) - self.mean_y[j]) / n;
            }
            let xc = Vector::from_iter((0..px).map(|j| x.get(i, j) - self.mean_x[j]));
            let yc = Vector::from_iter((0..py).map(|j| {
                let src = if same { j } else { px + j };
                x.get(i, src) - self.mean_y[j]
            }));
            let yu = self.u.dot(&xc);
            let yv = self.v.dot(&yc);
            let lr = lr0 / n.sqrt();
            for j in 0..px {
                self.u[j] += lr * (yv * xc[j] - yu * self.u[j]);
            }
            for j in 0..py {
                self.v[j] += lr * (yu * yc[j] - yv * self.v[j]);
            }
            let nu = self.u.norm();
            if nu > 1e-12 {
                for j in 0..px {
                    self.u[j] /= nu;
                }
            }
            let nv = self.v.norm();
            if nv > 1e-12 {
                for j in 0..py {
                    self.v[j] /= nv;
                }
            }
        }
        self.updates += 1;
        let delta = self.u.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineCca Oja-updated a canonical pair on {} rows; ||Δu||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "canonical directions",
                "streaming two-view Oja CCA; distinct from batch SVD CCA",
                "previous u",
                "updated u",
            ),
        )
    }
}

impl Transform for OnlineCca {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), 2));
        }
        let px = self.u.len();
        let py = self.v.len();
        let same = px == x.ncols() && py == x.ncols();
        ctx.finish(Matrix::from_fn(x.nrows(), 2, |i, c| {
            if c == 0 {
                let mut s = 0.0_f64;
                for j in 0..px.min(x.ncols()) {
                    s += self.u[j] * (x.get(i, j) - self.mean_x[j]);
                }
                s
            } else {
                let mut s = 0.0_f64;
                for j in 0..py {
                    let src = if same { j } else { px + j };
                    if src < x.ncols() {
                        s += self.v[j] * (x.get(i, src) - self.mean_y[j]);
                    }
                }
                s
            }
        }))
    }
}

/// Streaming one-pass factor analysis (Oja loadings + residual uniqueness).
///
/// Distinct from [`crate::decompose::FactorAnalysis`] (batch SVD / EM) and
/// [`OnlinePca`] (no unique-variance \(\psi\)). Factor count is not
/// identification `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineFa {
    /// Latent factors. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// Base Oja step; effective rate is `lr / (n_seen + 1)`.
    pub learning_rate: f64,
    mean: Vector,
    loadings: Vec<Vector>,
    psi: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineFa {
    fn default() -> Self {
        Self {
            n_components: 1,
            learning_rate: 1.0,
            mean: Vector::zeros(0),
            loadings: Vec::new(),
            psi: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineFa {
    /// Track `n_components` factors with residual uniqueness.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineFa {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.psi = Vector::filled(p, 1.0);
            self.loadings = (0..k)
                .map(|c| {
                    let mut w = Vector::zeros(p);
                    w[c % p] = 1.0;
                    w
                })
                .collect();
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .loadings
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            1.0
        };
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            for j in 0..p {
                self.mean[j] += (x.get(i, j) - self.mean[j]) / n;
            }
            let mut residual = Vector::from_iter((0..p).map(|j| x.get(i, j) - self.mean[j]));
            let lr = lr0 / n;
            for c in 0..self.loadings.len() {
                let y = self.loadings[c].dot(&residual);
                for j in 0..p {
                    self.loadings[c][j] += lr * y * residual[j];
                }
                for prev in 0..c {
                    let proj = self.loadings[c].dot(&self.loadings[prev]);
                    for j in 0..p {
                        self.loadings[c][j] -= proj * self.loadings[prev][j];
                    }
                }
                let nrm = self.loadings[c].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        self.loadings[c][j] /= nrm;
                    }
                }
                let y2 = self.loadings[c].dot(&residual);
                for j in 0..p {
                    residual[j] -= y2 * self.loadings[c][j];
                }
            }
            for j in 0..p {
                self.psi[j] += (residual[j] * residual[j] - self.psi[j]) / n;
                if self.psi[j] < 1e-6 {
                    self.psi[j] = 1e-6;
                }
            }
        }
        self.updates += 1;
        let after = self
            .loadings
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < (p as u64).saturating_add(2);
        q.explanation = format!(
            "OnlineFa residual-updated {k} factors on {} rows; ||Δλ0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "streaming factor loadings",
                "Oja Λ plus residual uniqueness; distinct from batch FA and OnlinePca",
                "previous loadings",
                "updated loadings",
            ),
        )
    }
}

impl Transform for OnlineFa {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.loadings.len();
        let p = self.mean.len();
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut num = 0.0_f64;
            let mut den = 1.0_f64;
            for j in 0..x.ncols().min(p) {
                let psi = self.psi[j].max(1e-6);
                let z = x.get(i, j) - self.mean[j];
                num += self.loadings[c][j] * z / psi;
                den += self.loadings[c][j] * self.loadings[c][j] / psi;
            }
            num / den
        }))
    }
}

/// Streaming one-component NIPALS PLS on a column-split two-view stream.
///
/// The first `⌈p/2⌉` columns are view X, the rest view Y. Distinct from
/// [`crate::linear_model::PlsRegression`] (batch NIPALS) and [`OnlineCca`]
/// (Oja decay on the weights). The single latent pair is not identification
/// `p`.
#[derive(Clone, Debug)]
pub struct OnlinePls {
    /// NIPALS step.
    pub learning_rate: f64,
    mean_x: Vector,
    mean_y: Vector,
    u: Vector,
    v: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlinePls {
    fn default() -> Self {
        Self {
            learning_rate: 0.05,
            mean_x: Vector::zeros(0),
            mean_y: Vector::zeros(0),
            u: Vector::zeros(0),
            v: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlinePls {
    /// Streaming PLS with NIPALS step `learning_rate`.
    pub fn new(learning_rate: f64) -> Self {
        Self {
            learning_rate,
            ..Self::default()
        }
    }
}

impl PartialFit for OnlinePls {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let (px, py) = split_views(p);
        if !self.initialized {
            self.mean_x = Vector::zeros(px);
            self.mean_y = Vector::zeros(py);
            self.u = Vector::filled(px, 0.0);
            self.u[0] = 1.0;
            self.v = Vector::filled(py, 0.0);
            self.v[0] = 1.0;
            self.initialized = true;
        } else if self.u.len() != px || self.v.len() != py {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.u.clone();
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.05
        };
        let same = py == p && px == p;
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            for j in 0..px {
                self.mean_x[j] += (x.get(i, j) - self.mean_x[j]) / n;
            }
            for j in 0..py {
                let src = if same { j } else { px + j };
                self.mean_y[j] += (x.get(i, src) - self.mean_y[j]) / n;
            }
            let xc = Vector::from_iter((0..px).map(|j| x.get(i, j) - self.mean_x[j]));
            let yc = Vector::from_iter((0..py).map(|j| {
                let src = if same { j } else { px + j };
                x.get(i, src) - self.mean_y[j]
            }));
            let yu = self.u.dot(&xc);
            let yv = self.v.dot(&yc);
            let lr = lr0 / n.sqrt();
            for j in 0..px {
                self.u[j] += lr * yv * xc[j];
            }
            for j in 0..py {
                self.v[j] += lr * yu * yc[j];
            }
            let nu = self.u.norm();
            if nu > 1e-12 {
                for j in 0..px {
                    self.u[j] /= nu;
                }
            }
            let nv = self.v.norm();
            if nv > 1e-12 {
                for j in 0..py {
                    self.v[j] /= nv;
                }
            }
        }
        self.updates += 1;
        let delta = self.u.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlinePls NIPALS-updated a latent pair on {} rows; ||Δu||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "PLS directions",
                "streaming two-view NIPALS; distinct from batch PLS and OnlineCca",
                "previous u",
                "updated u",
            ),
        )
    }
}

impl Transform for OnlinePls {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), 2));
        }
        let px = self.u.len();
        let py = self.v.len();
        let same = px == x.ncols() && py == x.ncols();
        ctx.finish(Matrix::from_fn(x.nrows(), 2, |i, c| {
            if c == 0 {
                let mut s = 0.0_f64;
                for j in 0..px.min(x.ncols()) {
                    s += self.u[j] * (x.get(i, j) - self.mean_x[j]);
                }
                s
            } else {
                let mut s = 0.0_f64;
                for j in 0..py {
                    let src = if same { j } else { px + j };
                    if src < x.ncols() {
                        s += self.v[j] * (x.get(i, src) - self.mean_y[j]);
                    }
                }
                s
            }
        }))
    }
}

/// Streaming diagonal Fisher LDA (two-class).
///
/// Distinct from [`crate::discriminant::LinearDiscriminantAnalysis`] (batch
/// pooled covariance) and [`OnlineGaussianNb`] (class-conditional diagonals
/// without a shared scatter). The direction is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineFda {
    mean0: Vector,
    mean1: Vector,
    var: Vector,
    n0: u64,
    n1: u64,
    w: Vector,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineFda {
    fn default() -> Self {
        Self {
            mean0: Vector::zeros(0),
            mean1: Vector::zeros(0),
            var: Vector::zeros(0),
            n0: 0,
            n1: 0,
            w: Vector::zeros(0),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineFda {
    /// Streaming two-class Fisher discriminant.
    pub fn new() -> Self {
        Self::default()
    }
}

impl PartialFit for OnlineFda {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no labels"),
            );
        };
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.mean0 = Vector::zeros(p);
            self.mean1 = Vector::zeros(p);
            self.var = Vector::filled(p, 1.0);
            self.w = Vector::zeros(p);
            self.initialized = true;
        } else if self.mean0.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.w.clone();
        let n_rows = x.nrows().min(y.len());
        for i in 0..n_rows {
            if !y[i].is_finite() {
                continue;
            }
            let pos = y[i] > 0.5;
            if pos {
                self.n1 += 1;
                let n = self.n1 as f64;
                for j in 0..p {
                    self.mean1[j] += (x.get(i, j) - self.mean1[j]) / n;
                }
            } else {
                self.n0 += 1;
                let n = self.n0 as f64;
                for j in 0..p {
                    self.mean0[j] += (x.get(i, j) - self.mean0[j]) / n;
                }
            }
            self.n_seen += 1;
            let n = self.n_seen as f64;
            let mu = if pos { &self.mean1 } else { &self.mean0 };
            for j in 0..p {
                let e = x.get(i, j) - mu[j];
                self.var[j] += (e * e - self.var[j]) / n;
                if self.var[j] < 1e-6 {
                    self.var[j] = 1e-6;
                }
            }
        }
        for j in 0..p {
            self.w[j] = (self.mean1[j] - self.mean0[j]) / self.var[j].max(1e-6);
        }
        self.updates += 1;
        let delta = self.w.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n0 >= 1 && self.n1 >= 1;
        q.warmup = self.n_seen < (p as u64).saturating_add(2);
        q.explanation = format!(
            "OnlineFda updated a diagonal Fisher direction on {} rows; ||Δw||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Fisher direction",
                "streaming diagonal LDA; distinct from batch pooled-covariance LDA",
                "previous w",
                "updated w",
            ),
        )
    }
}

impl Predict for OnlineFda {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let p = self.w.len();
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut s = 0.0_f64;
            for j in 0..x.ncols().min(p) {
                let mid = 0.5 * (self.mean0[j] + self.mean1[j]);
                s += self.w[j] * (x.get(i, j) - mid);
            }
            if s >= 0.0 {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// Streaming dictionary learning (ISTA sparse codes + residual atom update).
///
/// Distinct from [`crate::decompose::MiniBatchDictionaryLearning`] (batched
/// MU on stacked blocks) and [`OnlineNmf`] (nonnegative, no soft-threshold).
/// Atom count is not identification `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineDictionaryLearning {
    /// Dictionary atoms. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// ISTA threshold.
    pub l1: f64,
    /// Residual step.
    pub learning_rate: f64,
    atoms: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineDictionaryLearning {
    fn default() -> Self {
        Self {
            n_components: 1,
            l1: 0.1,
            learning_rate: 0.1,
            atoms: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineDictionaryLearning {
    /// Track `n_components` unit-norm atoms.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineDictionaryLearning {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        if !self.initialized {
            self.atoms = (0..k)
                .map(|c| {
                    let mut col = Vector::zeros(p);
                    col[c % p] = 1.0;
                    col
                })
                .collect();
            self.initialized = true;
        } else if self.atoms.as_slice().first().map(|c| c.len()) != Some(p) {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .atoms
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            0.1
        };
        let lam = if self.l1.is_finite() && self.l1 >= 0.0 {
            self.l1
        } else {
            0.1
        };
        for i in 0..x.nrows() {
            let xi = Vector::from_iter((0..p).map(|j| x.get(i, j)));
            let h = Vector::from_iter(self.atoms.iter().map(|atom| {
                let raw = atom.dot(&xi);
                let mag = (raw.abs() - lam).max(0.0);
                if raw >= 0.0 {
                    mag
                } else {
                    -mag
                }
            }));
            let recon = Vector::from_iter((0..p).map(|j| {
                let mut s = 0.0_f64;
                for c in 0..self.atoms.len() {
                    s += self.atoms[c][j] * h[c];
                }
                s
            }));
            for c in 0..self.atoms.len() {
                for j in 0..p {
                    self.atoms[c][j] += lr * (xi[j] - recon[j]) * h[c];
                }
                let nrm = self.atoms[c].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        self.atoms[c][j] /= nrm;
                    }
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let after = self
            .atoms
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineDictionaryLearning ISTA-updated {k} atoms on {} rows; ||Δd0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "dictionary atoms",
                "streaming ISTA dictionary; distinct from mini-batch MU and OnlineNmf",
                "previous D",
                "updated D",
            ),
        )
    }
}

impl Transform for OnlineDictionaryLearning {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.atoms.len();
        let lam = if self.l1.is_finite() && self.l1 >= 0.0 {
            self.l1
        } else {
            0.1
        };
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut raw = 0.0_f64;
            for j in 0..x.ncols().min(self.atoms[c].len()) {
                raw += self.atoms[c][j] * x.get(i, j);
            }
            let mag = (raw.abs() - lam).max(0.0);
            if raw >= 0.0 {
                mag
            } else {
                -mag
            }
        }))
    }
}

/// Incremental alternating least squares matrix factorisation.
///
/// One-observation ridge updates of \(p_u\) then \(q_i\). Distinct from
/// [`FunkMf`] (SGD) and [`BiasedMf`]. Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct AlsReco {
    /// Latent rank.
    pub n_factors: usize,
    /// Ridge on the rank-1 Gram.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for AlsReco {
    fn default() -> Self {
        Self {
            n_factors: 4,
            l2: 0.1,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl AlsReco {
    /// ALS reco with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for AlsReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("AlsReco needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let l2 = if self.l2.is_finite() && self.l2 >= 0.0 {
            self.l2
        } else {
            0.1
        };
        let mut sse = 0.0_f64;
        let mut dsum = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let pred = self.pred(u, i);
            let e = y[r] - pred;
            sse += e * e;
            let mut qn = 0.0_f64;
            for f in 0..self.n_factors {
                qn += self.qi[i][f] * self.qi[i][f];
            }
            let den_u = (qn + l2).max(1e-8);
            for f in 0..self.n_factors {
                let old = self.pu[u][f];
                self.pu[u][f] = y[r] * self.qi[i][f] / den_u;
                dsum += (self.pu[u][f] - old).abs();
            }
            let mut pn = 0.0_f64;
            for f in 0..self.n_factors {
                pn += self.pu[u][f] * self.pu[u][f];
            }
            let den_i = (pn + l2).max(1e-8);
            for f in 0..self.n_factors {
                let old = self.qi[i][f];
                self.qi[i][f] = y[r] * self.pu[u][f] / den_i;
                dsum += (self.qi[i][f] - old).abs();
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 8;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.loss_before = Some(sse);
        q.loss_after = Some(sse);
        q.information_gain = Some(dsum.max(x.nrows() as f64));
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "AlsReco rank-1 ridge ALS on {} ratings; ||ΔP,Q||₁={:.6e}",
            x.nrows(),
            dsum
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("AlsReco is still warming up")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("ALS on {} ratings", x.nrows()),
                "one-observation ridge ALS; distinct from FunkMF SGD",
                "previous factors",
                "updated factors",
            ),
        )
    }
}

/// WARP ranking recommender (Weston et al.): sample negatives until a
/// margin violation, then weight the pairwise step by the estimated rank
/// \(\lfloor n_{\mathrm{items}}/N\rfloor\).
///
/// Distinct from [`Bpr`] (one unweighted negative). Rank is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WarpReco {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    rng: Rng,
    n_seen: u64,
    updates: u64,
}

impl Default for WarpReco {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            rng: Rng::new(19),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl WarpReco {
    /// WARP reco with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for WarpReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, y);
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("WarpReco needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for r in 0..x.nrows() {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let n_items = self.qi.len();
            if n_items < 2 {
                continue;
            }
            let s_ui = self.score(u, i);
            let cap = n_items.min(32);
            let mut n_try = 0usize;
            let mut j = i;
            let mut viol = false;
            while n_try < cap {
                n_try += 1;
                j = self.rng.below(n_items);
                if j == i {
                    j = (j + 1) % n_items;
                }
                if self.score(u, j) + 1.0 >= s_ui {
                    viol = true;
                    break;
                }
            }
            if !viol {
                continue;
            }
            let rank = ((n_items as f64) / (n_try as f64)).floor().max(1.0);
            let wgt = (rank + 1.0).ln();
            let xui = s_ui - self.score(u, j);
            let sig = sigmoid(xui);
            let err = wgt * (1.0 - sig);
            for f in 0..self.n_factors {
                let pu = self.pu[u][f];
                let qi = self.qi[i][f];
                let qj = self.qi[j][f];
                self.pu[u][f] += lr * (err * (qi - qj) - l2 * pu);
                self.qi[i][f] += lr * (err * pu - l2 * qi);
                self.qi[j][f] += lr * (-err * pu - l2 * qj);
                dsum += (self.pu[u][f] - pu).abs()
                    + (self.qi[i][f] - qi).abs()
                    + (self.qi[j][f] - qj).abs();
            }
            pairs += 1;
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("WarpReco found no margin-violating negatives")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "WarpReco pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "WARP rank-weighted pairwise step",
                "sample-until-violation SGD; distinct from unweighted BPR",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for WarpReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("WarpReco predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let out = Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        }));
        ctx.finish(out)
    }
}

/// Uncentered streaming Oja / power-method SVD.
///
/// Distinct from [`OnlinePca`] (centered) and
/// [`crate::decompose::IncrementalPca`] (SKL sequential KL). Component count
/// is not identification `p` on a 1-row stream.
#[derive(Clone, Debug)]
pub struct OnlineSvd {
    /// Singular vectors to track. Not identification `p` for a streaming update.
    pub n_components: usize,
    /// Base Oja step; effective rate is `lr / (n_seen + 1)`.
    pub learning_rate: f64,
    components: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineSvd {
    fn default() -> Self {
        Self {
            n_components: 1,
            learning_rate: 1.0,
            components: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineSvd {
    /// Track `n_components` uncentered singular vectors.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineSvd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let k = self.n_components.max(1).min(p);
        if !self.initialized {
            self.components = (0..k)
                .map(|c| {
                    let mut w = Vector::zeros(p);
                    w[c % p] = 1.0;
                    w
                })
                .collect();
            self.initialized = true;
        } else if self.components.as_slice().first().map(|c| c.len()) != Some(p) {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            1.0
        };
        for i in 0..x.nrows() {
            self.n_seen += 1;
            let n = self.n_seen as f64;
            let mut residual = Vector::from_iter((0..p).map(|j| x.get(i, j)));
            let lr = lr0 / n;
            for c in 0..self.components.len() {
                let y = self.components[c].dot(&residual);
                for j in 0..p {
                    self.components[c][j] += lr * y * residual[j];
                }
                for prev in 0..c {
                    let proj = self.components[c].dot(&self.components[prev]);
                    for j in 0..p {
                        self.components[c][j] -= proj * self.components[prev][j];
                    }
                }
                let nrm = self.components[c].norm();
                if nrm > 1e-12 {
                    for j in 0..p {
                        self.components[c][j] /= nrm;
                    }
                }
                let y2 = self.components[c].dot(&residual);
                for j in 0..p {
                    residual[j] -= y2 * self.components[c][j];
                }
            }
        }
        self.updates += 1;
        let after = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(p));
        let delta = after.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(delta.norm());
        q.information_gain = Some(delta.norm().max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < (p as u64).saturating_add(2);
        q.explanation = format!(
            "OnlineSvd Oja-updated {k} uncentered axes on {} rows; ||Δw0||={:.6e}",
            x.nrows(),
            delta.norm()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "uncentered Oja singular vectors",
                "streaming power-method SVD; distinct from centered OnlinePca",
                "previous axes",
                "updated axes",
            ),
        )
    }
}

impl Transform for OnlineSvd {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.components.len();
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let mut s = 0.0_f64;
            for j in 0..x.ncols().min(self.components[c].len()) {
                s += self.components[c][j] * x.get(i, j);
            }
            s
        }))
    }
}

fn invert_ridge_gram(g: &[Vec<f64>], lam: f64) -> Option<Vec<Vec<f64>>> {
    let n = g.len();
    if n == 0 {
        return Some(Vec::new());
    }
    let mut a = vec![vec![0.0_f64; 2 * n]; n];
    for i in 0..n {
        for j in 0..n {
            a[i][j] = g[i][j];
        }
        a[i][i] += lam;
        a[i][n + i] = 1.0;
    }
    for col in 0..n {
        let mut piv = col;
        for r in (col + 1)..n {
            if a[r][col].abs() > a[piv][col].abs() {
                piv = r;
            }
        }
        if a[piv][col].abs() < 1e-14 {
            return None;
        }
        if piv != col {
            a.swap(piv, col);
        }
        let den = a[col][col];
        for j in 0..(2 * n) {
            a[col][j] /= den;
        }
        for r in 0..n {
            if r == col {
                continue;
            }
            let f = a[r][col];
            for j in 0..(2 * n) {
                a[r][j] -= f * a[col][j];
            }
        }
    }
    Some((0..n).map(|i| a[i][n..].to_vec()).collect())
}

/// EASE^R item-item recommender (Steck): \(B_{ij}=-P^{-1}_{ij}/P^{-1}_{ii}\)
/// with \(P=G+\lambda I\) and a zero diagonal.
///
/// Distinct from [`SlimReco`] (L1 sequential weights) and [`ItemKnn`]
/// (cosine neighbourhood). Item count is not identification `p`.
#[derive(Clone, Debug)]
pub struct EaseReco {
    /// Ridge on the item Gram.
    pub l2: f64,
    ratings: HashMap<(u64, usize), f64>,
    gram: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for EaseReco {
    fn default() -> Self {
        Self {
            l2: 0.5,
            ratings: HashMap::new(),
            gram: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl EaseReco {
    /// Empty EASE recommender.
    pub fn new() -> Self {
        Self::default()
    }

    fn grow(&mut self, i: usize) {
        while self.gram.len() <= i {
            let n = self.gram.len();
            for row in self.gram.iter_mut() {
                row.push(0.0);
            }
            self.gram.push(vec![0.0; n + 1]);
        }
    }

    fn score(&self, user: u64, item: usize) -> f64 {
        let lam = if self.l2.is_finite() && self.l2 >= 0.0 {
            self.l2
        } else {
            0.5
        };
        let Some(inv) = invert_ridge_gram(&self.gram, lam.max(1e-6)) else {
            return 0.0;
        };
        if item >= inv.len() {
            return 0.0;
        }
        let dii = inv[item][item];
        if dii.abs() < 1e-14 {
            return 0.0;
        }
        let mut s = 0.0_f64;
        for (&(u, j), &rj) in &self.ratings {
            if u != user || j == item || j >= inv[item].len() {
                continue;
            }
            s += rj * (-inv[item][j] / dii);
        }
        s
    }
}

impl PartialFit for EaseReco {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("EaseReco needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let mut dsum = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let user = x.get(r, 0).max(0.0).round() as u64;
            let item = x.get(r, 1).max(0.0).round() as usize;
            self.grow(item);
            let old = self.ratings.get(&(user, item)).copied();
            if let Some(old) = old {
                self.gram[item][item] -= old * old;
                let others: Vec<(usize, f64)> = self
                    .ratings
                    .iter()
                    .filter_map(|(&(u, j), &rj)| {
                        if u == user && j != item {
                            Some((j, rj))
                        } else {
                            None
                        }
                    })
                    .collect();
                for (j, rj) in others {
                    self.grow(j);
                    self.gram[item][j] -= old * rj;
                    self.gram[j][item] -= old * rj;
                }
            }
            self.ratings.insert((user, item), y[r]);
            self.gram[item][item] += y[r] * y[r];
            let others: Vec<(usize, f64)> = self
                .ratings
                .iter()
                .filter_map(|(&(u, j), &rj)| {
                    if u == user && j != item {
                        Some((j, rj))
                    } else {
                        None
                    }
                })
                .collect();
            for (j, rj) in others {
                self.grow(j);
                self.gram[item][j] += y[r] * rj;
                self.gram[j][item] += y[r] * rj;
            }
            dsum += y[r].abs();
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 4;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(dsum.max(x.nrows() as f64));
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "EaseReco updated item Gram on {} ratings; items={}",
            x.nrows(),
            self.gram.len()
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("EaseReco is still warming up")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "EASE item-item Gram",
                "Steck closed-form ridge inverse; distinct from SLIM and ItemKNN",
                "previous G",
                "updated G",
            ),
        )
    }
}

impl Predict for EaseReco {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("EaseReco predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as u64;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

fn rbf_landmarks(x: &Vector, landmarks: &[Vector], bw: f64) -> Vector {
    Vector::from_iter(landmarks.iter().map(|lm| {
        let n = x.len().min(lm.len());
        let mut s = 0.0_f64;
        for j in 0..n {
            let d = x[j] - lm[j];
            s += d * d;
        }
        (-s / (2.0 * bw * bw).max(1e-8)).exp()
    }))
}

/// Streaming kernel PCA (RBF landmarks + Oja in feature space).
///
/// Distinct from [`OnlinePca`] (linear) and [`crate::kernel_pca::KernelPca`]
/// (batch eigendecomposition). Landmark count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineKernelPca {
    /// Kernel-space axes / landmarks. Not identification `p` on a stream.
    pub n_components: usize,
    /// RBF bandwidth.
    pub bandwidth: f64,
    /// Base Oja step.
    pub learning_rate: f64,
    landmarks: Vec<Vector>,
    components: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineKernelPca {
    fn default() -> Self {
        Self {
            n_components: 1,
            bandwidth: 1.0,
            learning_rate: 1.0,
            landmarks: Vec::new(),
            components: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineKernelPca {
    /// Track `n_components` kernel axes.
    pub fn new(n_components: usize) -> Self {
        Self {
            n_components: n_components.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OnlineKernelPca {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if self.initialized {
            if self.landmarks.as_slice().first().map(|c| c.len()) != Some(p) {
                ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
                return finish_explain(
                    ctx,
                    reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
                );
            }
        }
        let k = self.n_components.max(1);
        let bw = if self.bandwidth.is_finite() && self.bandwidth > 0.0 {
            self.bandwidth
        } else {
            1.0
        };
        let lr0 = if self.learning_rate.is_finite() && self.learning_rate > 0.0 {
            self.learning_rate
        } else {
            1.0
        };
        let before = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(k));
        for i in 0..x.nrows() {
            let xi = Vector::from_iter((0..p).map(|j| x.get(i, j)));
            if self.landmarks.len() < k {
                self.landmarks.push(xi.clone());
                self.components = (0..self.landmarks.len())
                    .map(|c| {
                        let mut w = Vector::zeros(self.landmarks.len());
                        w[c] = 1.0;
                        w
                    })
                    .collect();
                self.initialized = true;
            }
            if self.landmarks.is_empty() {
                continue;
            }
            let phi = rbf_landmarks(&xi, &self.landmarks, bw);
            if self.components.as_slice().first().map(|c| c.len()) != Some(phi.len()) {
                self.components = (0..self.landmarks.len().min(k))
                    .map(|c| {
                        let mut w = Vector::zeros(phi.len());
                        w[c % phi.len()] = 1.0;
                        w
                    })
                    .collect();
            }
            self.n_seen += 1;
            let n = self.n_seen as f64;
            let lr = lr0 / n;
            let mut residual = phi;
            for c in 0..self.components.len() {
                let y = self.components[c].dot(&residual);
                for j in 0..residual.len() {
                    self.components[c][j] += lr * y * residual[j];
                }
                for prev in 0..c {
                    let proj = self.components[c].dot(&self.components[prev]);
                    for j in 0..self.components[c].len() {
                        self.components[c][j] -= proj * self.components[prev][j];
                    }
                }
                let nrm = self.components[c].norm();
                if nrm > 1e-12 {
                    for j in 0..self.components[c].len() {
                        self.components[c][j] /= nrm;
                    }
                }
                let y2 = self.components[c].dot(&residual);
                for j in 0..residual.len() {
                    residual[j] -= y2 * self.components[c][j];
                }
            }
        }
        self.updates += 1;
        let after = self
            .components
            .as_slice()
            .first()
            .cloned()
            .unwrap_or_else(|| Vector::zeros(k));
        let alen = after.len().min(before.len());
        let mut dn = 0.0_f64;
        for j in 0..alen {
            let d = after[j] - before[j];
            dn += d * d;
        }
        let dn = dn.sqrt();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dn);
        q.information_gain = Some(dn.max(x.nrows() as f64));
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "OnlineKernelPca Oja-updated {} RBF landmarks on {} rows; ||Δw0||={:.6e}",
            self.landmarks.len(),
            x.nrows(),
            dn
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RBF-landmark kernel axes",
                "streaming kernel Oja; distinct from linear OnlinePca and batch KernelPca",
                "previous axes",
                "updated axes",
            ),
        )
    }
}

impl Transform for OnlineKernelPca {
    fn transform(&self, x: &Matrix, session: &Session) -> Result<Qualified<Matrix>> {
        let mut ctx = FitCtx::with_session(session.child("transform"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized || self.landmarks.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Matrix::zeros(x.nrows(), self.n_components.max(1)));
        }
        let k = self.components.len();
        let bw = if self.bandwidth.is_finite() && self.bandwidth > 0.0 {
            self.bandwidth
        } else {
            1.0
        };
        ctx.finish(Matrix::from_fn(x.nrows(), k, |i, c| {
            let xi = Vector::from_iter((0..x.ncols()).map(|j| x.get(i, j)));
            let phi = rbf_landmarks(&xi, &self.landmarks, bw);
            let mut s = 0.0_f64;
            for j in 0..phi.len().min(self.components[c].len()) {
                s += self.components[c][j] * phi[j];
            }
            s
        }))
    }
}

/// Isolation Forest + ADWIN (river `anomaly.IForestASD`).
///
/// Scores isolation paths, then resets the forest when ADWIN cuts the
/// score window. Distinct from [`HalfSpaceTrees`] (no reset) and batch
/// [`crate::anomaly::IsolationForest`]. Tree count is not identification `p`.
#[derive(Clone, Debug)]
pub struct IForestAsd {
    /// Isolation trees. Not identification `p` on a stream.
    pub n_trees: usize,
    /// Maximum isolation depth.
    pub max_depth: usize,
    forest: HalfSpaceTrees,
    adwin: Adwin,
    warmed: bool,
    resets: u64,
    n_seen: u64,
    updates: u64,
}

impl Default for IForestAsd {
    fn default() -> Self {
        Self {
            n_trees: 8,
            max_depth: 6,
            forest: HalfSpaceTrees::new(8, 6),
            adwin: Adwin::new(0.002),
            warmed: false,
            resets: 0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl IForestAsd {
    /// IForestASD with `n_trees` isolation trees.
    pub fn new(n_trees: usize) -> Self {
        let n_trees = n_trees.max(1);
        Self {
            n_trees,
            forest: HalfSpaceTrees::new(n_trees, 6),
            ..Self::default()
        }
    }
}

impl PartialFit for IForestAsd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        let mut drifted = false;
        if self.warmed {
            let scores = self.forest.predict(x, &session.child("score"))?;
            for i in 0..scores.value.len() {
                let dec = self
                    .adwin
                    .update(scores.value[i], &session.child(format!("adwin{i}")))?;
                if matches!(dec.value, DriftDecision::Drift { .. }) {
                    drifted = true;
                }
            }
        }
        if drifted {
            self.forest = HalfSpaceTrees::new(self.n_trees.max(1), self.max_depth.max(1));
            self.adwin.reset();
            self.warmed = false;
            self.resets += 1;
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .severity(Severity::Warning)
                    .message(format!("IForestAsd reset the forest (resets={})", self.resets))
                    .build(),
            );
        }
        self.forest
            .partial_fit(x, None, &session.child("forest"))?;
        self.warmed = true;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(self.resets as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 4;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "IForestAsd isolation+ADWIN on {} rows; resets={}",
            x.nrows(),
            self.resets
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "isolation forest with ADWIN reset",
                "river IForestASD; distinct from HalfSpaceTrees (no reset)",
                "previous forest",
                "updated or reset forest",
            ),
        )
    }
}

impl Predict for IForestAsd {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        self.forest.predict(x, session)
    }
}

fn softmax_scores(xs: &[f64]) -> Vec<f64> {
    let m = xs.iter().copied().fold(f64::NEG_INFINITY, f64::max);
    if !m.is_finite() {
        let n = xs.len().max(1) as f64;
        return vec![1.0 / n; xs.len()];
    }
    let mut e: Vec<f64> = xs.iter().map(|v| (v - m).exp()).collect();
    let s: f64 = e.iter().sum();
    let s = s.max(1e-12);
    for v in e.iter_mut() {
        *v /= s;
    }
    e
}

/// ListNet listwise ranking (softmax cross-entropy on a user's items).
///
/// Distinct from [`Bpr`] (one pairwise negative) and [`WarpReco`]
/// (sample-until-violation). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct ListNet {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for ListNet {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ListNet {
    /// ListNet with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for ListNet {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ListNet needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut lists = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let scores: Vec<f64> = items.iter().map(|&(i, _)| self.score(u, i)).collect();
            let rel: Vec<f64> = items.iter().map(|&(_, r)| r).collect();
            let p = softmax_scores(&scores);
            let q = softmax_scores(&rel);
            for t in 0..items.len() {
                let err = p[t] - q[t];
                let i = items[t].0;
                for f in 0..self.n_factors {
                    let pu = self.pu[u][f];
                    let qi = self.qi[i][f];
                    self.pu[u][f] -= lr * (err * qi + l2 * pu);
                    self.qi[i][f] -= lr * (err * pu + l2 * qi);
                    dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                }
            }
            lists += 1;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if lists == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("ListNet needs two items per user in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(lists as f64);
        q.still_identified = lists > 0;
        q.warmup = lists == 0;
        q.explanation = format!(
            "ListNet lists={lists} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ListNet listwise softmax step",
                "softmax CE on a user's items; distinct from pairwise BPR/WARP",
                "pre-batch factors",
                format!("lists={lists} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for ListNet {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ListNet predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// ListMLE (Xia et al.): Plackett–Luce likelihood of the relevance order.
///
/// Distinct from [`ListNet`] (top-1 softmax CE) and [`LambdaRank`]
/// (λ-weighted pairwise). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct ListMle {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for ListMle {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ListMle {
    /// ListMLE with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for ListMle {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ListMLE needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut lists = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let mut order: Vec<usize> = (0..items.len()).collect();
            order.sort_by(|a, b| {
                items[*b]
                    .1
                    .partial_cmp(&items[*a].1)
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
            for start in 0..order.len() {
                let rest = &order[start..];
                let scores: Vec<f64> = rest.iter().map(|&t| self.score(u, items[t].0)).collect();
                let p = softmax_scores(&scores);
                for (j, &t) in rest.iter().enumerate() {
                    let err = p[j] - if j == 0 { 1.0 } else { 0.0 };
                    let i = items[t].0;
                    for f in 0..self.n_factors {
                        let pu = self.pu[u][f];
                        let qi = self.qi[i][f];
                        self.pu[u][f] -= lr * (err * qi + l2 * pu);
                        self.qi[i][f] -= lr * (err * pu + l2 * qi);
                        dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                    }
                }
            }
            lists += 1;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if lists == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("ListMLE needs two items per user in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(lists as f64);
        q.still_identified = lists > 0;
        q.warmup = lists == 0;
        q.explanation = format!(
            "ListMLE lists={lists} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ListMLE Plackett–Luce step",
                "likelihood of the relevance permutation; not ListNet top-1 CE",
                "pre-batch factors",
                format!("lists={lists} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for ListMle {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ListMLE predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

fn softrank_phi(z: f64) -> f64 {
    const INV_SQRT_2PI: f64 = 0.398_942_280_401_432_7;
    INV_SQRT_2PI * (-0.5 * z * z).exp()
}

/// SoftRank (Taylor et al.): Gaussian-smoothed rank MSE.
///
/// \(\hat r_i=1+\sum_{j\neq i}\Phi((s_j-s_i)/\sigma)\). Distinct from
/// [`ListNet`] (top-1 softmax CE) and [`ListMle`] (Plackett–Luce). Bandwidth
/// \(\sigma\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct SoftRank {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    /// Rank-smoothing bandwidth. Not identification `p`.
    pub sigma: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for SoftRank {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            sigma: 0.5,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SoftRank {
    /// SoftRank with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for SoftRank {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SoftRank needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let sigma = if self.sigma.is_finite() && self.sigma > 0.0 {
            self.sigma
        } else {
            0.5
        };
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut lists = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let n = items.len();
            let scores: Vec<f64> = items.iter().map(|&(ii, _)| self.score(u, ii)).collect();
            let mut hat = vec![1.0_f64; n];
            let mut phi = vec![vec![0.0_f64; n]; n];
            for ii in 0..n {
                for jj in 0..n {
                    if ii == jj {
                        continue;
                    }
                    let z = (scores[jj] - scores[ii]) / sigma;
                    phi[jj][ii] = softrank_phi(z);
                    hat[ii] += norm_cdf(z);
                }
            }
            let mut rank = vec![1.0_f64; n];
            for ii in 0..n {
                for jj in 0..n {
                    if ii == jj {
                        continue;
                    }
                    let dy = items[jj].1 - items[ii].1;
                    if dy > 1e-15 {
                        rank[ii] += 1.0;
                    } else if dy.abs() <= 1e-15 {
                        rank[ii] += 0.5;
                    }
                }
            }
            let mut gs = vec![0.0_f64; n];
            for kk in 0..n {
                let mut sum_phi = 0.0_f64;
                for jj in 0..n {
                    if jj != kk {
                        sum_phi += phi[jj][kk];
                    }
                }
                let mut acc = -(hat[kk] - rank[kk]) * sum_phi;
                for ii in 0..n {
                    if ii != kk {
                        acc += (hat[ii] - rank[ii]) * phi[kk][ii];
                    }
                }
                gs[kk] = 2.0 / sigma * acc;
            }
            for t in 0..n {
                let i = items[t].0;
                let err = gs[t];
                for f in 0..self.n_factors {
                    let pu = self.pu[u][f];
                    let qi = self.qi[i][f];
                    self.pu[u][f] -= lr * (err * qi + l2 * pu);
                    self.qi[i][f] -= lr * (err * pu + l2 * qi);
                    dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                }
            }
            lists += 1;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if lists == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("SoftRank needs two items per user in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(lists as f64);
        q.still_identified = lists > 0;
        q.warmup = lists == 0;
        q.explanation = format!(
            "SoftRank lists={lists} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SoftRank Gaussian-smoothed rank step",
                "MSE of Φ-smoothed ranks vs relevance ranks; not ListNet CE or ListMLE",
                "pre-batch factors",
                format!("lists={lists} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for SoftRank {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SoftRank predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// ApproxNDCG (Qin / SoftNDCG): sigmoid-smoothed DCG on latent scores.
///
/// \(\hat r_i=1+\sum_{j\neq i}\sigma((s_j-s_i)/\tau)\). Distinct from
/// [`SoftRank`] (Gaussian rank MSE) and [`LambdaRank`] (λ-pairwise).
/// Temperature \(\tau\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct ApproxNdcg {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    /// Rank-smoothing temperature. Not identification `p`.
    pub tau: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for ApproxNdcg {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            tau: 0.5,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl ApproxNdcg {
    /// ApproxNDCG with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for ApproxNdcg {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ApproxNDCG needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let tau = if self.tau.is_finite() && self.tau > 0.0 {
            self.tau
        } else {
            0.5
        };
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut lists = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let n = items.len();
            let scores: Vec<f64> = items.iter().map(|&(ii, _)| self.score(u, ii)).collect();
            let gains: Vec<f64> = items
                .iter()
                .map(|&(_, rel)| 2.0_f64.powf(rel.max(0.0).min(20.0)) - 1.0)
                .collect();
            let mut hat = vec![1.0_f64; n];
            let mut dsig = vec![vec![0.0_f64; n]; n];
            for ii in 0..n {
                for jj in 0..n {
                    if ii == jj {
                        continue;
                    }
                    let z = (scores[jj] - scores[ii]) / tau;
                    let sg = sigmoid(z);
                    dsig[jj][ii] = sg * (1.0 - sg) / tau;
                    hat[ii] += sg;
                }
            }
            let mut ddcg_dr = vec![0.0_f64; n];
            for ii in 0..n {
                let one_r = 1.0 + hat[ii];
                let ln = one_r.ln().max(1e-12);
                ddcg_dr[ii] = -gains[ii] * std::f64::consts::LN_2 / (one_r * ln * ln);
            }
            let mut gs = vec![0.0_f64; n];
            for kk in 0..n {
                let mut acc = 0.0_f64;
                let mut sum_d = 0.0_f64;
                for jj in 0..n {
                    if jj != kk {
                        sum_d += dsig[jj][kk];
                    }
                }
                acc += ddcg_dr[kk] * (-sum_d);
                for ii in 0..n {
                    if ii != kk {
                        acc += ddcg_dr[ii] * dsig[kk][ii];
                    }
                }
                gs[kk] = acc;
            }
            for t in 0..n {
                let i = items[t].0;
                let err = -gs[t];
                for f in 0..self.n_factors {
                    let pu = self.pu[u][f];
                    let qi = self.qi[i][f];
                    self.pu[u][f] -= lr * (err * qi + l2 * pu);
                    self.qi[i][f] -= lr * (err * pu + l2 * qi);
                    dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                }
            }
            lists += 1;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if lists == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("ApproxNDCG needs two items per user in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(lists as f64);
        q.still_identified = lists > 0;
        q.warmup = lists == 0;
        q.explanation = format!(
            "ApproxNDCG lists={lists} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ApproxNDCG sigmoid-smoothed DCG step",
                "ascent on 1/log2(1+soft-rank); not SoftRank MSE or LambdaRank pairs",
                "pre-batch factors",
                format!("lists={lists} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for ApproxNdcg {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("ApproxNDCG predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// RankCosine (Cao et al.): cosine loss between scores and relevance.
///
/// Distinct from [`ListNet`] (top-1 CE) and [`SoftRank`] (Gaussian rank MSE).
#[derive(Clone, Debug)]
pub struct RankCosine {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for RankCosine {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RankCosine {
    /// RankCosine with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for RankCosine {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankCosine needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r].max(0.0)));
        }
        let mut dsum = 0.0_f64;
        let mut lists = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let scores: Vec<f64> = items.iter().map(|&(ii, _)| self.score(u, ii)).collect();
            let mut dot = 0.0_f64;
            let mut ns = 0.0_f64;
            let mut ny = 0.0_f64;
            for t in 0..items.len() {
                dot += scores[t] * items[t].1;
                ns += scores[t] * scores[t];
                ny += items[t].1 * items[t].1;
            }
            let den = (ns.sqrt() * ny.sqrt()).max(1e-12);
            // ∂/∂s_t of ⟨s,y⟩/(|s||y|) = y_t/den - ⟨s,y⟩ s_t / (|s|³ |y|)
            let nsn = ns.sqrt().max(1e-12);
            let nyn = ny.sqrt().max(1e-12);
            for t in 0..items.len() {
                let i = items[t].0;
                let gs = items[t].1 / den - dot * scores[t] / (nsn * nsn * nsn * nyn);
                let err = -gs;
                for f in 0..self.n_factors {
                    let pu = self.pu[u][f];
                    let qi = self.qi[i][f];
                    self.pu[u][f] -= lr * (err * qi + l2 * pu);
                    self.qi[i][f] -= lr * (err * pu + l2 * qi);
                    dsum += (self.pu[u][f] - pu).abs() + (self.qi[i][f] - qi).abs();
                }
            }
            lists += 1;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if lists == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("RankCosine needs two items per user in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(lists as f64);
        q.still_identified = lists > 0;
        q.warmup = lists == 0;
        q.explanation = format!(
            "RankCosine lists={lists} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RankCosine listwise step",
                "ascent on cosine(s, relevance); not ListNet CE or SoftRank MSE",
                "pre-batch factors",
                format!("lists={lists} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for RankCosine {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankCosine predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// RankSVM pairwise ranking (hinge on \(s_i-s_j\) for \(y_i>y_j\)).
///
/// Distinct from [`RankNet`] (logistic pair loss) and [`Bpr`] (one unweighted
/// negative). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct RankSvm {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for RankSvm {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RankSvm {
    /// RankSVM with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for RankSvm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankSvm needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let ia = items[a].0;
                    let ib = items[b].0;
                    let margin = self.score(u, ia) - self.score(u, ib);
                    if margin >= 1.0 {
                        continue;
                    }
                    let err = -1.0_f64;
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] -= lr * (err * (qia - qib) + l2 * pua);
                        self.qi[ia][f] -= lr * (err * pua + l2 * qia);
                        self.qi[ib][f] -= lr * (-err * pua + l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("RankSvm needs a hinge-active pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "RankSvm pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RankSvm pairwise hinge step",
                "hinge on s_i-s_j; distinct from RankNet logistic and BPR/WARP",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for RankSvm {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankSvm predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// AdaRank (Xu & Li): query-reweighted exponential pairwise ranking.
///
/// Distinct from [`RankNet`] (unweighted logistic), [`RankSvm`] (hinge), and
/// [`LambdaMart`] (λ-tree boosting). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct AdaRank {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    query_w: HashMap<usize, f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for AdaRank {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            query_w: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl AdaRank {
    /// AdaRank with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for AdaRank {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("AdaRank needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let mut inv = 0.0_f64;
            let mut tot = 0.0_f64;
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    tot += 1.0;
                    if self.score(u, items[a].0) <= self.score(u, items[b].0) {
                        inv += 1.0;
                    }
                }
            }
            if tot < 1.0 {
                continue;
            }
            let err = (inv / tot).clamp(1e-6, 1.0 - 1e-6);
            let w = self.query_w.entry(u).or_insert(1.0);
            *w = (*w * (err / (1.0 - err)).sqrt()).clamp(1e-3, 40.0);
            let qw = *w;
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let ia = items[a].0;
                    let ib = items[b].0;
                    let margin = self.score(u, ia) - self.score(u, ib);
                    let soft = (-margin).exp().min(20.0);
                    let errg = -qw * soft;
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] -= lr * (errg * (qia - qib) + l2 * pua);
                        self.qi[ia][f] -= lr * (errg * pua + l2 * qia);
                        self.qi[ib][f] -= lr * (-errg * pua + l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("AdaRank needs a comparable pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "AdaRank pairs={pairs} ||Δ||₁={dsum:.4e} queries={}",
            self.query_w.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "AdaRank query-reweighted step",
                "exponential pair loss with query weights; not RankNet/RankSvm/LambdaMart",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for AdaRank {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("AdaRank predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// Coordinate ascent ranking (RankLib-style item-coordinate logistic steps).
///
/// Updates one item factor at a time. Distinct from [`AdaRank`] (query
/// reweighting), [`RankNet`] (joint pair SGD), and [`ListNet`]. Rank is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct CoordinateAscent {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for CoordinateAscent {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl CoordinateAscent {
    /// Coordinate ascent with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for CoordinateAscent {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("CoordinateAscent needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut steps = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            for t in 0..items.len() {
                let it = items[t].0;
                let yt = items[t].1;
                let mut gs = 0.0_f64;
                for j in 0..items.len() {
                    if j == t || items[j].1 == yt {
                        continue;
                    }
                    let ij = items[j].0;
                    let diff = self.score(u, it) - self.score(u, ij);
                    let p = 1.0 / (1.0 + (-diff).exp());
                    if yt > items[j].1 {
                        gs += p - 1.0;
                    } else {
                        gs += p;
                    }
                }
                for f in 0..self.n_factors {
                    let qit = self.qi[it][f];
                    let pu = self.pu[u][f];
                    self.qi[it][f] -= lr * (gs * pu + l2 * qit);
                    dsum += (self.qi[it][f] - qit).abs();
                }
                steps += 1;
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if steps == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("CoordinateAscent needs two items per user")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(steps as f64);
        q.still_identified = steps > 0;
        q.warmup = steps == 0;
        q.explanation = format!(
            "CoordinateAscent steps={steps} ||Δ||₁={dsum:.4e} items={}",
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "item-coordinate ranking step",
                "updates qi only; distinct from joint RankNet SGD and AdaRank",
                "pre-batch item factors",
                format!("steps={steps} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for CoordinateAscent {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("CoordinateAscent predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// RankBoost (Freund et al.): pair-weighted exponential ranking.
///
/// Distinct from [`AdaRank`] (query-level weights) and [`RankSvm`] (hinge).
/// Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct RankBoost {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    pair_w: HashMap<(usize, usize, usize), f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for RankBoost {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            pair_w: HashMap::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RankBoost {
    /// RankBoost with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for RankBoost {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankBoost needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let ia = items[a].0;
                    let ib = items[b].0;
                    let key = (u, ia, ib);
                    let margin = self.score(u, ia) - self.score(u, ib);
                    let w = self.pair_w.entry(key).or_insert(1.0);
                    *w = (*w * (-margin).exp().min(8.0)).clamp(1e-3, 40.0);
                    let pw = *w;
                    let errg = -pw * (-margin).exp().min(8.0);
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] -= lr * (errg * (qia - qib) + l2 * pua);
                        self.qi[ia][f] -= lr * (errg * pua + l2 * qia);
                        self.qi[ib][f] -= lr * (-errg * pua + l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("RankBoost needs a comparable pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "RankBoost pairs={pairs} ||Δ||₁={dsum:.4e} weights={}",
            self.pair_w.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RankBoost pair-weighted step",
                "exponential pair weights; not AdaRank query weights or RankSvm hinge",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for RankBoost {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankBoost predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// RankNet pairwise ranking (logistic loss on \(s_i-s_j\) for \(y_i>y_j\)).
///
/// Distinct from [`ListNet`] (listwise softmax CE), [`Bpr`] (one unweighted
/// negative), and [`WarpReco`] (sample-until-violation). Rank is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct RankNet {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for RankNet {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl RankNet {
    /// RankNet with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for RankNet {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankNet needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let ia = items[a].0;
                    let ib = items[b].0;
                    let p = sigmoid(self.score(u, ia) - self.score(u, ib));
                    let err = p - 1.0;
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] -= lr * (err * (qia - qib) + l2 * pua);
                        self.qi[ia][f] -= lr * (err * pua + l2 * qia);
                        self.qi[ib][f] -= lr * (-err * pua + l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("RankNet needs a comparable pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "RankNet pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RankNet pairwise logistic step",
                "logistic on s_i-s_j; distinct from listwise ListNet and BPR/WARP",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for RankNet {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("RankNet predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// SoftImpute (Mazumder–Hastie–Tibshirani): fill missing ratings, thin SVD,
/// soft-threshold singular values, reconstruct.
///
/// Distinct from [`AlsReco`] (ridge ALS), [`EaseReco`] (Gram inverse), and
/// [`FunkMf`] (SGD). Nuclear-norm λ is not identification `p`.
#[derive(Clone, Debug)]
pub struct SoftImpute {
    /// Soft-threshold on singular values.
    pub lambda: f64,
    observed: HashMap<(usize, usize), f64>,
    recon: Vec<Vec<f64>>,
    n_users: usize,
    n_items: usize,
    n_seen: u64,
    updates: u64,
}

impl Default for SoftImpute {
    fn default() -> Self {
        Self {
            lambda: 0.1,
            observed: HashMap::new(),
            recon: Vec::new(),
            n_users: 0,
            n_items: 0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl SoftImpute {
    /// SoftImpute with default nuclear-norm threshold.
    pub fn new() -> Self {
        Self::default()
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        if u < self.recon.len() && i < self.recon[u].len() {
            self.recon[u][i]
        } else {
            0.0
        }
    }
}

impl PartialFit for SoftImpute {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SoftImpute needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.observed.insert((u, i), y[r]);
            self.n_users = self.n_users.max(u + 1);
            self.n_items = self.n_items.max(i + 1);
        }
        if self.n_users == 0 || self.n_items == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty ratings"),
            );
        }
        let nu = self.n_users;
        let ni = self.n_items;
        if self.recon.len() != nu || self.recon.as_slice().first().map(|r| r.len()) != Some(ni) {
            self.recon = vec![vec![0.0; ni]; nu];
        }
        let filled = Matrix::from_fn(nu, ni, |u, i| {
            if let Some(&r) = self.observed.get(&(u, i)) {
                r
            } else {
                self.recon[u][i]
            }
        });
        let mut scratch = Report::new("SoftImpute", "svd");
        let mut nkeep = 0u64;
        let mut dsum = 0.0_f64;
        if let Some(svd) = thin_svd(&mut scratch, &filled, &ctx.policy) {
            let lam = if self.lambda.is_finite() && self.lambda >= 0.0 {
                self.lambda
            } else {
                0.1
            };
            let r = svd.singular_values.len();
            let mut news = vec![0.0_f64; r];
            for k in 0..r {
                let sk: f64 = svd.singular_values[k];
                news[k] = (sk - lam).max(0.0);
                if news[k] > 0.0 {
                    nkeep += 1;
                }
            }
            let mut next = vec![vec![0.0_f64; ni]; nu];
            for u in 0..nu {
                for i in 0..ni {
                    let mut s = 0.0_f64;
                    for c in 0..r {
                        s += svd.u[(u, c)] * news[c] * svd.v[(i, c)];
                    }
                    next[u][i] = s;
                    dsum += (s - self.recon[u][i]).abs();
                }
            }
            self.recon = next;
        } else {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .severity(Severity::Warning)
                    .message("SoftImpute skipped a non-converged SVD")
                    .build(),
            );
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = nkeep == 0;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(nkeep as f64);
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "SoftImpute nuclear SVD on {nu}×{ni}; kept={nkeep} ||Δ||₁={dsum:.4e}"
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SoftImpute soft-threshold SVD",
                "fill-SVD-shrink; distinct from ALS, EASE, and FunkMF",
                "previous reconstruction",
                format!("kept={nkeep} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for SoftImpute {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("SoftImpute predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// Weighted regularized MF (Hu–Koren–Volinsky implicit ALS).
///
/// Confidence \(c=1+\alpha r\) multiplies a binary preference. Distinct from
/// [`AlsReco`] (explicit ridge on the raw rating). Rank is not identification
/// `p`.
#[derive(Clone, Debug)]
pub struct Wrmf {
    /// Latent rank.
    pub n_factors: usize,
    /// Confidence slope.
    pub alpha: f64,
    /// Ridge on the rank-1 Gram.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for Wrmf {
    fn default() -> Self {
        Self {
            n_factors: 4,
            alpha: 20.0,
            l2: 0.1,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Wrmf {
    /// WRMF with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn pred(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for Wrmf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("Wrmf needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let l2 = if self.l2.is_finite() && self.l2 >= 0.0 {
            self.l2
        } else {
            0.1
        };
        let alpha = if self.alpha.is_finite() && self.alpha >= 0.0 {
            self.alpha
        } else {
            20.0
        };
        let mut sse = 0.0_f64;
        let mut dsum = 0.0_f64;
        for r in 0..x.nrows().min(y.len()) {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            let rating = if y[r].is_finite() { y[r].max(0.0) } else { 0.0 };
            let pref = if rating > 0.0 { 1.0 } else { 0.0 };
            let conf = 1.0 + alpha * rating;
            let pred = self.pred(u, i);
            let e = pref - pred;
            sse += conf * e * e;
            let mut qn = 0.0_f64;
            for f in 0..self.n_factors {
                qn += self.qi[i][f] * self.qi[i][f];
            }
            let den_u = (conf * qn + l2).max(1e-8);
            for f in 0..self.n_factors {
                let old = self.pu[u][f];
                self.pu[u][f] = conf * pref * self.qi[i][f] / den_u;
                dsum += (self.pu[u][f] - old).abs();
            }
            let mut pn = 0.0_f64;
            for f in 0..self.n_factors {
                pn += self.pu[u][f] * self.pu[u][f];
            }
            let den_i = (conf * pn + l2).max(1e-8);
            for f in 0..self.n_factors {
                let old = self.qi[i][f];
                self.qi[i][f] = conf * pref * self.pu[u][f] / den_i;
                dsum += (self.qi[i][f] - old).abs();
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let warmup = self.n_seen < 8;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.loss_before = Some(sse);
        q.loss_after = Some(sse);
        q.information_gain = Some(dsum.max(x.nrows() as f64));
        q.still_identified = !warmup;
        q.warmup = warmup;
        q.explanation = format!(
            "Wrmf implicit ALS on {} ratings; ||ΔP,Q||₁={:.6e}",
            x.nrows(),
            dsum
        );
        if warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .incremental(q.clone())
                    .message("Wrmf is still warming up")
                    .build(),
            );
        }
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                format!("WRMF on {} ratings", x.nrows()),
                "confidence-weighted implicit ALS; distinct from explicit AlsReco",
                "previous factors",
                "updated factors",
            ),
        )
    }
}

impl Predict for Wrmf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("Wrmf predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.pred(u, i)
        })))
    }
}

fn lambdarank_pair_weight(rel_a: f64, rel_b: f64, rank_a: f64, rank_b: f64, idcg: f64) -> f64 {
    if idcg <= 1e-12 {
        return 1.0;
    }
    let gain_a = 2.0_f64.powf(rel_a.max(0.0).min(20.0)) - 1.0;
    let gain_b = 2.0_f64.powf(rel_b.max(0.0).min(20.0)) - 1.0;
    let da = 1.0 / (rank_a + 1.0).log2().max(1e-6);
    let db = 1.0 / (rank_b + 1.0).log2().max(1e-6);
    ((gain_a - gain_b).abs() * (da - db).abs() / idcg).max(1e-6)
}

/// LambdaRank: RankNet logistic pairs weighted by \(|\Delta\mathrm{NDCG}|\).
///
/// Distinct from [`RankNet`] (unit pair weight) and [`ListNet`] (listwise
/// softmax). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct LambdaRank {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for LambdaRank {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl LambdaRank {
    /// LambdaRank with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for LambdaRank {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("LambdaRank needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.len() < 2 {
                continue;
            }
            let scores: Vec<f64> = items.iter().map(|&(i, _)| self.score(u, i)).collect();
            let mut order: Vec<usize> = (0..items.len()).collect();
            order.sort_by(|&a, &b| {
                scores[b]
                    .partial_cmp(&scores[a])
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
            let mut rank = vec![1.0_f64; items.len()];
            for (r, &t) in order.iter().enumerate() {
                rank[t] = (r + 1) as f64;
            }
            let mut rels: Vec<f64> = items.iter().map(|&(_, rel)| rel.max(0.0)).collect();
            rels.sort_by(|a, b| b.partial_cmp(a).unwrap_or(std::cmp::Ordering::Equal));
            let mut idcg = 0.0_f64;
            for (r, &g) in rels.iter().enumerate() {
                idcg += (2.0_f64.powf(g.min(20.0)) - 1.0) / ((r + 2) as f64).log2();
            }
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let ia = items[a].0;
                    let ib = items[b].0;
                    let w = lambdarank_pair_weight(
                        items[a].1,
                        items[b].1,
                        rank[a],
                        rank[b],
                        idcg,
                    );
                    let p = sigmoid(self.score(u, ia) - self.score(u, ib));
                    let err = (p - 1.0) * w;
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] -= lr * (err * (qia - qib) + l2 * pua);
                        self.qi[ia][f] -= lr * (err * pua + l2 * qia);
                        self.qi[ib][f] -= lr * (-err * pua + l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("LambdaRank needs a comparable pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "LambdaRank pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "LambdaRank |ΔNDCG|-weighted pairwise step",
                "RankNet logistic times NDCG swap weight; distinct from RankNet and ListNet",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for LambdaRank {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("LambdaRank predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

fn rrcf_codisp(points: &[Vec<f64>], q: &[f64], rng: &mut Rng, max_depth: usize) -> f64 {
    let p = q.len();
    if p == 0 {
        return 0.0;
    }
    let mut pts: Vec<Vec<f64>> = points.to_vec();
    let mut depth = 0usize;
    loop {
        if pts.is_empty() || depth >= max_depth {
            return pts.len() as f64 + 1.0;
        }
        let mut los = vec![0.0_f64; p];
        let mut his = vec![0.0_f64; p];
        let mut ranges = vec![0.0_f64; p];
        for j in 0..p {
            let qj: f64 = q[j];
            let mut lo = qj;
            let mut hi = qj;
            for pt in &pts {
                lo = lo.min(pt[j]);
                hi = hi.max(pt[j]);
            }
            los[j] = lo;
            his[j] = hi;
            ranges[j] = (hi - lo).max(0.0);
        }
        let tot: f64 = ranges.iter().sum();
        if tot < 1e-15 {
            return pts.len() as f64 + 1.0;
        }
        let u = rng.uniform() * tot;
        let mut acc = 0.0_f64;
        let mut dim = 0usize;
        for j in 0..p {
            acc += ranges[j];
            if u <= acc {
                dim = j;
                break;
            }
        }
        let cut = rng.uniform_range(los[dim], his[dim]);
        let qleft = q[dim] < cut;
        let mut next = Vec::new();
        let mut sibling = 0usize;
        for pt in pts {
            if pt[dim] < cut {
                if qleft {
                    next.push(pt);
                } else {
                    sibling += 1;
                }
            } else if qleft {
                sibling += 1;
            } else {
                next.push(pt);
            }
        }
        if sibling == next.len() + sibling && next.is_empty() {
            return (sibling as f64) / (depth as f64 + 1.0);
        }
        if sibling > 0 && next.len() < sibling {
            return (sibling as f64) / (depth as f64 + 1.0);
        }
        pts = next;
        depth += 1;
    }
}

/// Robust Random Cut Forest (Guha et al.): dimension sampled \(\propto\)
/// range, score is collusive displacement.
///
/// Distinct from [`HalfSpaceTrees`] (uniform axis) and [`IForestAsd`]
/// (isolation + ADWIN). Tree count is not identification `p`.
#[derive(Clone, Debug)]
pub struct Rrcf {
    /// Trees in the forest.
    pub n_trees: usize,
    /// Reservoir cap per tree.
    pub max_size: usize,
    /// Cut depth cap.
    pub max_depth: usize,
    reservoirs: Vec<Vec<Vec<f64>>>,
    rng: Rng,
    warmed: bool,
    n_seen: u64,
    updates: u64,
}

impl Default for Rrcf {
    fn default() -> Self {
        Self {
            n_trees: 8,
            max_size: 32,
            max_depth: 8,
            reservoirs: Vec::new(),
            rng: Rng::new(17),
            warmed: false,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Rrcf {
    /// RRCF with `n_trees` random-cut trees.
    pub fn new(n_trees: usize) -> Self {
        Self {
            n_trees: n_trees.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for Rrcf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if self.reservoirs.len() != self.n_trees.max(1) {
            self.reservoirs = vec![Vec::new(); self.n_trees.max(1)];
        }
        let cap = self.max_size.max(4);
        let mut dsum = 0.0_f64;
        for i in 0..x.nrows() {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            for res in &mut self.reservoirs {
                if res.len() < cap {
                    res.push(row.clone());
                    dsum += 1.0;
                } else {
                    let k = self.rng.below(res.len());
                    res[k] = row.clone();
                    dsum += 1.0;
                }
            }
        }
        self.warmed = true;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 4;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "Rrcf range-weighted cuts on {} rows; trees={}",
            x.nrows(),
            self.reservoirs.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "robust random cut forest step",
                "dimension ∝ range; CoDisp; distinct from HalfSpaceTrees",
                "previous reservoirs",
                "updated reservoirs",
            ),
        )
    }
}

impl Predict for Rrcf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.warmed {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut rng = self.rng.clone();
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            let mut s = 0.0_f64;
            let n = self.reservoirs.len().max(1) as f64;
            for res in &self.reservoirs {
                s += rrcf_codisp(res, &row, &mut rng, self.max_depth.max(1));
            }
            s / n
        })))
    }
}

fn xstream_dot(row: &[f64], dir: &[f64]) -> f64 {
    let mut s = 0.0_f64;
    let n = row.len().min(dir.len());
    for i in 0..n {
        s += row[i] * dir[i];
    }
    s
}

/// xStream (Manzoor–Lamba–Akoglu): half-space chains, not axis-aligned isolation.
///
/// Chain count is not identification `p`. Distinct from [`HalfSpaceTrees`]
/// (uniform axis) and [`Loda`] (projection histograms without chaining).
#[derive(Clone, Debug)]
pub struct XStream {
    /// Number of half-space chains.
    pub n_chains: usize,
    /// Cuts per chain.
    pub depth: usize,
    dirs: Vec<Vec<Vec<f64>>>,
    cuts: Vec<Vec<f64>>,
    counts: Vec<HashMap<u32, u64>>,
    n_tot: Vec<u64>,
    warmed: bool,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for XStream {
    fn default() -> Self {
        Self {
            n_chains: 8,
            depth: 8,
            dirs: Vec::new(),
            cuts: Vec::new(),
            counts: Vec::new(),
            n_tot: Vec::new(),
            warmed: false,
            n_seen: 0,
            updates: 0,
            rng: Rng::new(23),
        }
    }
}

impl XStream {
    /// xStream with `n_chains` half-space chains.
    pub fn new(n_chains: usize) -> Self {
        Self {
            n_chains: n_chains.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        let c = self.n_chains.max(1);
        let d = self.depth.max(1).min(16);
        if self.dirs.len() == c && self.dirs.first().map(|ch| ch.len()) == Some(d) {
            if self.dirs.first().and_then(|ch| ch.first()).map(|v| v.len()) == Some(p) {
                return;
            }
        }
        self.dirs = Vec::with_capacity(c);
        self.cuts = Vec::with_capacity(c);
        self.counts = vec![HashMap::new(); c];
        self.n_tot = vec![0; c];
        for _ in 0..c {
            let mut chain = Vec::with_capacity(d);
            let mut cuts = Vec::with_capacity(d);
            for _ in 0..d {
                let mut dir: Vec<f64> = (0..p).map(|_| self.rng.standard_normal()).collect();
                let nrm: f64 = dir.iter().map(|v| v * v).sum::<f64>().sqrt().max(1e-12);
                for v in &mut dir {
                    *v /= nrm;
                }
                chain.push(dir);
                cuts.push(self.rng.uniform_range(-1.0, 1.0));
            }
            self.dirs.push(chain);
            self.cuts.push(cuts);
        }
    }

    fn path_of(&self, chain: usize, row: &[f64]) -> u32 {
        let mut path = 0u32;
        if chain >= self.dirs.len() {
            return path;
        }
        for (l, dir) in self.dirs[chain].iter().enumerate() {
            let cut = self.cuts[chain].get(l).copied().unwrap_or(0.0);
            if xstream_dot(row, dir) > cut {
                path |= 1u32 << l;
            }
        }
        path
    }
}

impl PartialFit for XStream {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        self.ensure(x.ncols());
        let mut dsum = 0.0_f64;
        for i in 0..x.nrows() {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            for c in 0..self.dirs.len() {
                let path = self.path_of(c, &row);
                *self.counts[c].entry(path).or_insert(0) += 1;
                self.n_tot[c] += 1;
                dsum += 1.0;
            }
        }
        self.warmed = true;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "XStream half-space chains on {} rows; chains={}",
            x.nrows(),
            self.dirs.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "xStream half-space chain step",
                "random projections chained into bins; distinct from HalfSpaceTrees and Loda",
                "previous bin counts",
                "updated bin counts",
            ),
        )
    }
}

impl Predict for XStream {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.warmed {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let nc = self.dirs.len().max(1) as f64;
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            let mut s = 0.0_f64;
            for c in 0..self.dirs.len() {
                let path = self.path_of(c, &row);
                let cnt = self.counts[c].get(&path).copied().unwrap_or(0) as f64;
                let n = self.n_tot.get(c).copied().unwrap_or(0) as f64;
                s += -((cnt + 1.0) / (n + 2.0)).ln();
            }
            s / nc
        })))
    }
}

fn kitnet_recon(w: &[f64], x: &[f64]) -> (f64, f64) {
    let p = w.len().min(x.len());
    let mut dot = 0.0_f64;
    for j in 0..p {
        dot += w[j] * x[j];
    }
    let h = dot.tanh();
    let mut sse = 0.0_f64;
    for j in 0..p {
        let e = h * w[j] - x[j];
        sse += e * e;
    }
    (h, sse.sqrt())
}

fn kitnet_sgd(w: &mut [f64], x: &[f64], lr: f64) -> f64 {
    let p = w.len().min(x.len());
    let (h, rmse) = kitnet_recon(w, x);
    let dh = 1.0 - h * h;
    let mut werr = 0.0_f64;
    for j in 0..p {
        werr += w[j] * (h * w[j] - x[j]);
    }
    for j in 0..p {
        let e = h * w[j] - x[j];
        w[j] -= lr * (e * h + werr * dh * x[j]);
    }
    rmse
}

/// KitNET (Mirsky et al.): ensemble of tiny autoencoders plus an output AE.
///
/// Ensemble size is not identification `p`. Distinct from [`Loda`] (histograms)
/// and [`OnlineIsolationForest`].
#[derive(Clone, Debug)]
pub struct KitNet {
    /// Number of ensemble autoencoders.
    pub n_ae: usize,
    /// SGD step.
    pub learning_rate: f64,
    ensemble: Vec<Vec<f64>>,
    output: Vec<f64>,
    p: usize,
    warmed: bool,
    n_seen: u64,
    updates: u64,
    rng: Rng,
}

impl Default for KitNet {
    fn default() -> Self {
        Self {
            n_ae: 4,
            learning_rate: 0.05,
            ensemble: Vec::new(),
            output: Vec::new(),
            p: 0,
            warmed: false,
            n_seen: 0,
            updates: 0,
            rng: Rng::new(29),
        }
    }
}

impl KitNet {
    /// KitNET with `n_ae` ensemble autoencoders.
    pub fn new(n_ae: usize) -> Self {
        Self {
            n_ae: n_ae.max(1),
            ..Self::default()
        }
    }

    fn ensure(&mut self, p: usize) {
        if self.p == p && self.ensemble.len() == self.n_ae.max(1) {
            return;
        }
        self.p = p;
        let k = self.n_ae.max(1);
        self.ensemble = (0..k)
            .map(|_| (0..p).map(|_| 0.05 * self.rng.standard_normal()).collect())
            .collect();
        self.output = (0..k).map(|_| 0.05 * self.rng.standard_normal()).collect();
    }
}

impl PartialFit for KitNet {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if self.p != 0 && self.p != x.ncols() {
            ctx.push(
                Issue::builder(IssueCode::FeatureSpaceChangedOnline)
                    .severity(Severity::Warning)
                    .message(format!("KitNet saw {} columns; model has {}", x.ncols(), self.p))
                    .build(),
            );
        }
        self.ensure(x.ncols());
        let lr = self.learning_rate.max(1e-6);
        let mut dsum = 0.0_f64;
        for i in 0..x.nrows() {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            let mut rmses = vec![0.0_f64; self.ensemble.len()];
            for (a, w) in self.ensemble.iter_mut().enumerate() {
                let before = w.iter().map(|v| v.abs()).sum::<f64>();
                rmses[a] = kitnet_sgd(w, &row, lr);
                dsum += (w.iter().map(|v| v.abs()).sum::<f64>() - before).abs();
            }
            kitnet_sgd(&mut self.output, &rmses, lr);
        }
        self.warmed = true;
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "KitNet ensemble AEs on {} rows; n_ae={}",
            x.nrows(),
            self.ensemble.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "KitNET autoencoder ensemble step",
                "tiny tied AEs plus output AE; distinct from Loda and isolation forests",
                "previous AE weights",
                "updated AE weights",
            ),
        )
    }
}

impl Predict for KitNet {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.warmed {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            let rmses: Vec<f64> = self
                .ensemble
                .iter()
                .map(|w| kitnet_recon(w, &row).1)
                .collect();
            kitnet_recon(&self.output, &rmses).1
        })))
    }
}

#[derive(Clone, Debug)]
struct AxgbStump {
    feat: usize,
    thr: f64,
    left: f64,
    right: f64,
}

fn axgb_stump_pred(st: &AxgbStump, row: &[f64]) -> f64 {
    let v = if st.feat < row.len() { row[st.feat] } else { 0.0 };
    if v < st.thr {
        st.left
    } else {
        st.right
    }
}

fn axgb_fit_stump(xs: &[Vec<f64>], res: &[f64], rng: &mut Rng) -> AxgbStump {
    let n = xs.len();
    let p = xs.get(0).map(|r| r.len()).unwrap_or(1).max(1);
    let mut best = AxgbStump {
        feat: 0,
        thr: 0.0,
        left: 0.0,
        right: 0.0,
    };
    let mut best_sse = f64::INFINITY;
    if n == 0 {
        return best;
    }
    for _ in 0..8 {
        let feat = rng.below(p);
        let i0 = rng.below(n);
        let thr = xs
            .get(i0)
            .and_then(|r| r.get(feat))
            .copied()
            .unwrap_or(0.0);
        let mut sl = 0.0_f64;
        let mut sr = 0.0_f64;
        let mut nl = 0.0_f64;
        let mut nr = 0.0_f64;
        for i in 0..n {
            let v = xs[i].get(feat).copied().unwrap_or(0.0);
            if v < thr {
                sl += res[i];
                nl += 1.0;
            } else {
                sr += res[i];
                nr += 1.0;
            }
        }
        let left = if nl > 0.0 { sl / nl } else { 0.0 };
        let right = if nr > 0.0 { sr / nr } else { 0.0 };
        let mut sse = 0.0_f64;
        for i in 0..n {
            let v = xs[i].get(feat).copied().unwrap_or(0.0);
            let pred = if v < thr { left } else { right };
            let e = res[i] - pred;
            sse += e * e;
        }
        if sse < best_sse {
            best_sse = sse;
            best = AxgbStump {
                feat,
                thr,
                left,
                right,
            };
        }
    }
    best
}

/// Adaptive XGBoost (Gomes–Read–Bifet / river `AdaptiveXGBoost`): sliding-window
/// residual stumps, not Hoeffding or Mondrian trees.
///
/// Round count is not identification `p`. Distinct from
/// [`AdaptiveRandomForest`] and [`AmfClassifier`].
#[derive(Clone, Debug)]
pub struct AdaptiveXgboost {
    /// Boosting rounds kept in the windowed ensemble.
    pub n_rounds: usize,
    /// Sliding window cap.
    pub window: usize,
    xs: Vec<Vec<f64>>,
    ys: Vec<f64>,
    trees: Vec<AxgbStump>,
    rng: Rng,
    n_seen: u64,
    updates: u64,
}

impl Default for AdaptiveXgboost {
    fn default() -> Self {
        Self {
            n_rounds: 4,
            window: 64,
            xs: Vec::new(),
            ys: Vec::new(),
            trees: Vec::new(),
            rng: Rng::new(31),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl AdaptiveXgboost {
    /// Adaptive XGBoost with `n_rounds` residual stumps.
    pub fn new(n_rounds: usize) -> Self {
        Self {
            n_rounds: n_rounds.max(1),
            ..Self::default()
        }
    }

    fn predict_row(&self, row: &[f64]) -> f64 {
        let mut s = 0.0_f64;
        for st in &self.trees {
            s += axgb_stump_pred(st, row);
        }
        s
    }
}

impl PartialFit for AdaptiveXgboost {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        let cap = self.window.max(8);
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            self.xs
                .push((0..x.ncols()).map(|j| x.get(i, j)).collect());
            self.ys.push(y[i]);
            if self.xs.len() > cap {
                self.xs.remove(0);
                self.ys.remove(0);
            }
        }
        let n = self.xs.len();
        let mut pred = vec![0.0_f64; n];
        let mean = if n == 0 {
            0.0
        } else {
            self.ys.iter().sum::<f64>() / n as f64
        };
        for p in &mut pred {
            *p = mean;
        }
        self.trees.clear();
        let rounds = self.n_rounds.max(1);
        for _ in 0..rounds {
            let res: Vec<f64> = self.ys.iter().zip(pred.iter()).map(|(yy, pp)| yy - pp).collect();
            let stump = axgb_fit_stump(&self.xs, &res, &mut self.rng);
            for i in 0..n {
                pred[i] += axgb_stump_pred(&stump, &self.xs[i]);
            }
            self.trees.push(stump);
        }
        let mut sse = 0.0_f64;
        for i in 0..n {
            let e = self.ys[i] - pred[i];
            sse += e * e;
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(sse.sqrt());
        q.information_gain = Some(n as f64);
        q.still_identified = n >= 2;
        q.warmup = n < 2;
        q.explanation = format!(
            "AdaptiveXgboost window={n} rounds={} sse={sse:.4e}",
            self.trees.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "AdaptiveXGBoost windowed residual-stump step",
                "sliding-window GBDT stumps; distinct from ARF and AMF",
                "previous ensemble",
                format!("rounds={} sse={sse:.4e}", self.trees.len()),
            ),
        )
    }
}

impl Predict for AdaptiveXgboost {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let ctx = FitCtx::with_session(session.child("predict"));
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let row: Vec<f64> = (0..x.ncols()).map(|j| x.get(i, j)).collect();
            self.predict_row(&row)
        })))
    }
}

/// LambdaMART: MART stumps on user/item ids, gradients weighted by \(|\Delta\mathrm{NDCG}|\).
///
/// Distinct from [`LambdaRank`] (bilinear factors) and [`AdaptiveXgboost`]
/// (squared residual, not ranking). Round count is not identification `p`.
#[derive(Clone, Debug)]
pub struct LambdaMart {
    /// Boosting rounds.
    pub n_rounds: usize,
    /// Shrinkage.
    pub learning_rate: f64,
    trees: Vec<AxgbStump>,
    rng: Rng,
    n_seen: u64,
    updates: u64,
}

impl Default for LambdaMart {
    fn default() -> Self {
        Self {
            n_rounds: 4,
            learning_rate: 0.1,
            trees: Vec::new(),
            rng: Rng::new(41),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl LambdaMart {
    /// LambdaMART with `n_rounds` ranking stumps.
    pub fn new(n_rounds: usize) -> Self {
        Self {
            n_rounds: n_rounds.max(1),
            ..Self::default()
        }
    }

    fn score_ui(&self, u: f64, i: f64) -> f64 {
        let row = [u, i];
        let mut s = 0.0_f64;
        for st in &self.trees {
            s += axgb_stump_pred(st, &row);
        }
        s
    }
}

impl PartialFit for LambdaMart {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("LambdaMart needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut xs: Vec<Vec<f64>> = Vec::new();
        let mut res: Vec<f64> = Vec::new();
        let mut pairs = 0u64;
        for (u, items) in &by_user {
            if items.len() < 2 {
                continue;
            }
            let scores: Vec<f64> = items
                .iter()
                .map(|&(i, _)| self.score_ui(*u as f64, i as f64))
                .collect();
            let mut order: Vec<usize> = (0..items.len()).collect();
            order.sort_by(|&a, &b| {
                scores[b]
                    .partial_cmp(&scores[a])
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
            let mut rank = vec![1.0_f64; items.len()];
            for (r, &t) in order.iter().enumerate() {
                rank[t] = (r + 1) as f64;
            }
            let mut rels: Vec<f64> = items.iter().map(|&(_, rel)| rel.max(0.0)).collect();
            rels.sort_by(|a, b| b.partial_cmp(a).unwrap_or(std::cmp::Ordering::Equal));
            let mut idcg = 0.0_f64;
            for (r, &g) in rels.iter().enumerate() {
                idcg += (2.0_f64.powf(g.min(20.0)) - 1.0) / ((r + 2) as f64).log2();
            }
            let mut lam = vec![0.0_f64; items.len()];
            for a in 0..items.len() {
                for b in 0..items.len() {
                    if items[a].1 <= items[b].1 {
                        continue;
                    }
                    let w = lambdarank_pair_weight(
                        items[a].1,
                        items[b].1,
                        rank[a],
                        rank[b],
                        idcg,
                    );
                    let pab = sigmoid(scores[a] - scores[b]);
                    let g = (pab - 1.0) * w;
                    lam[a] += g;
                    lam[b] -= g;
                    pairs += 1;
                }
            }
            for (t, item) in items.iter().enumerate() {
                xs.push(vec![*u as f64, item.0 as f64]);
                res.push(-lam[t]);
            }
        }
        let mut dsum = 0.0_f64;
        if !xs.is_empty() {
            let rounds = self.n_rounds.max(1);
            for _ in 0..rounds {
                let stump = axgb_fit_stump(&xs, &res, &mut self.rng);
                for i in 0..xs.len() {
                    let pred = axgb_stump_pred(&stump, &xs[i]);
                    res[i] -= lr * pred;
                    dsum += (lr * pred).abs();
                }
                self.trees.push(stump);
            }
            if self.trees.len() > 16 {
                let drop = self.trees.len() - 16;
                self.trees.drain(0..drop);
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("LambdaMart needs a comparable pair in the batch")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "LambdaMart pairs={pairs} trees={} ||Δ||₁={dsum:.4e}",
            self.trees.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "LambdaMART |ΔNDCG| stump step",
                "ranking MART on user/item ids; distinct from LambdaRank bilinear factors",
                "pre-batch ensemble",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for LambdaMart {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("LambdaMart predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            self.score_ui(x.get(r, 0), x.get(r, 1))
        })))
    }
}

/// Oza online boosting: Poisson(\(\lambda\)) replay with \(\lambda\) updated by
/// each member's running error.
///
/// Distinct from [`AdaBoostClassifier`] / [`OnlineAdaBoost`] (batch vote
/// reweight, no Poisson replay). Member count is not identification `p`.
#[derive(Clone, Debug)]
pub struct OzaBoost {
    /// Committee size.
    pub n_models: usize,
    models: Vec<Perceptron>,
    err: Vec<f64>,
    seen: Vec<f64>,
    rng: Rng,
    n_seen: u64,
    updates: u64,
}

impl Default for OzaBoost {
    fn default() -> Self {
        Self {
            n_models: 3,
            models: Vec::new(),
            err: Vec::new(),
            seen: Vec::new(),
            rng: Rng::new(43),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl OzaBoost {
    /// Oza boost with `n_models` perceptrons.
    pub fn new(n_models: usize) -> Self {
        Self {
            n_models: n_models.max(1),
            ..Self::default()
        }
    }
}

impl PartialFit for OzaBoost {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        let m = self.n_models.max(1);
        if self.models.len() != m {
            self.models = vec![Perceptron::new(); m];
            self.err = vec![0.0; m];
            self.seen = vec![0.0; m];
        }
        let mut dsum = 0.0_f64;
        let mut replays = 0u64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let row = Matrix::from_fn(1, x.ncols(), |_, j| x.get(i, j));
            let yi = Vector::from_iter(std::iter::once(y[i]));
            let mut lam = 1.0_f64;
            for t in 0..m {
                let k = self.rng.poisson(lam.min(20.0));
                for _ in 0..k {
                    let _ = self.models[t].partial_fit(
                        &row,
                        Some(&yi),
                        &session.child(format!("oza_{t}")),
                    );
                    replays += 1;
                    dsum += 1.0;
                }
                let pred = self
                    .models[t]
                    .predict(&row, &session.child(format!("ozap_{t}")))
                    .map(|q| q.value.as_slice().first().copied().unwrap_or(0.0))
                    .unwrap_or(0.0);
                let hit = (pred - y[i]).abs() < 0.5;
                self.seen[t] += 1.0;
                if !hit {
                    self.err[t] += 1.0;
                }
                let e = (self.err[t] / self.seen[t].max(1.0)).clamp(1e-3, 1.0 - 1e-3);
                if hit {
                    lam *= 1.0 / (2.0 * (1.0 - e));
                } else {
                    lam *= 1.0 / (2.0 * e);
                }
                lam = lam.clamp(1e-3, 20.0);
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(replays as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 2;
        q.explanation = format!(
            "OzaBoost Poisson replay={replays} members={}",
            self.models.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Oza online boosting step",
                "Poisson(λ) replay with error-driven λ; distinct from OnlineAdaBoost vote weights",
                "pre-batch perceptrons",
                format!("replays={replays}"),
            ),
        )
    }
}

impl Predict for OzaBoost {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if self.models.is_empty() {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let mut acc = vec![0.0_f64; x.nrows()];
        for (t, model) in self.models.iter().enumerate() {
            if let Ok(pq) = model.predict(x, &session.child(format!("ozap_{t}"))) {
                for i in 0..x.nrows().min(pq.value.len()) {
                    acc[i] += if pq.value[i] >= 0.5 { 1.0 } else { -1.0 };
                }
            }
        }
        ctx.finish(Vector::from_iter(acc.iter().map(|v| if *v >= 0.0 { 1.0 } else { 0.0 })))
    }
}

/// CLiMF (Shi et al.): reciprocal-rank logistic on relevant pairs.
///
/// Distinct from [`Bpr`] (one unweighted negative) and [`LambdaRank`]
/// (\(|\Delta\mathrm{NDCG}|\)). Rank is not identification `p`.
#[derive(Clone, Debug)]
pub struct Climf {
    /// Latent rank.
    pub n_factors: usize,
    /// SGD step.
    pub learning_rate: f64,
    /// \(\ell_2\) on factors.
    pub l2: f64,
    pu: Vec<Vec<f64>>,
    qi: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
}

impl Default for Climf {
    fn default() -> Self {
        Self {
            n_factors: 4,
            learning_rate: 0.05,
            l2: 0.01,
            pu: Vec::new(),
            qi: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Climf {
    /// CLiMF with `k` factors.
    pub fn new(n_factors: usize) -> Self {
        Self {
            n_factors: n_factors.max(1),
            ..Self::default()
        }
    }

    fn grow(&mut self, u: usize, i: usize) {
        let k = self.n_factors.max(1);
        while self.pu.len() <= u {
            self.pu.push(vec![0.01; k]);
        }
        while self.qi.len() <= i {
            self.qi.push(vec![0.01; k]);
        }
    }

    fn score(&self, u: usize, i: usize) -> f64 {
        let mut s = 0.0_f64;
        if u < self.pu.len() && i < self.qi.len() {
            for f in 0..self.n_factors.min(self.pu[u].len()).min(self.qi[i].len()) {
                s += self.pu[u][f] * self.qi[i][f];
            }
        }
        s
    }
}

impl PartialFit for Climf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("Climf needs two columns (user, item)")
                    .build(),
            );
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "need user/item"),
            );
        }
        let lr = self.learning_rate.max(1e-6);
        let l2 = self.l2.max(0.0);
        let mut by_user: HashMap<usize, Vec<(usize, f64)>> = HashMap::new();
        for r in 0..x.nrows().min(y.len()) {
            if !y[r].is_finite() || y[r] <= 0.0 {
                continue;
            }
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.grow(u, i);
            by_user.entry(u).or_default().push((i, y[r]));
        }
        let mut dsum = 0.0_f64;
        let mut pairs = 0u64;
        for (u, items) in by_user {
            if items.is_empty() {
                continue;
            }
            let scores: Vec<f64> = items.iter().map(|&(i, _)| self.score(u, i)).collect();
            for a in 0..items.len() {
                let ia = items[a].0;
                let sa = scores[a];
                let g1 = 1.0 - sigmoid(sa);
                for f in 0..self.n_factors {
                    let pua = self.pu[u][f];
                    let qia = self.qi[ia][f];
                    self.pu[u][f] += lr * (g1 * qia - l2 * pua);
                    self.qi[ia][f] += lr * (g1 * pua - l2 * qia);
                    dsum += (self.pu[u][f] - pua).abs() + (self.qi[ia][f] - qia).abs();
                }
                for b in 0..items.len() {
                    if a == b {
                        continue;
                    }
                    let ib = items[b].0;
                    let sb = scores[b];
                    let gij = 1.0 - sigmoid(sa - sb);
                    let gji = 1.0 - sigmoid(sb - sa);
                    let g = gij - gji;
                    for f in 0..self.n_factors {
                        let pua = self.pu[u][f];
                        let qia = self.qi[ia][f];
                        let qib = self.qi[ib][f];
                        self.pu[u][f] += lr * (g * (qia - qib) - l2 * pua);
                        self.qi[ia][f] += lr * (g * pua - l2 * qia);
                        self.qi[ib][f] += lr * (-g * pua - l2 * qib);
                        dsum += (self.pu[u][f] - pua).abs()
                            + (self.qi[ia][f] - qia).abs()
                            + (self.qi[ib][f] - qib).abs();
                    }
                    pairs += 1;
                }
            }
        }
        self.n_seen += x.nrows().min(y.len()) as u64;
        self.updates += 1;
        if pairs == 0 {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("Climf needs two relevant items for a user")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(pairs as f64);
        q.still_identified = pairs > 0;
        q.warmup = pairs == 0;
        q.explanation = format!(
            "Climf pairs={pairs} ||Δ||₁={dsum:.4e} users={} items={}",
            self.pu.len(),
            self.qi.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "CLiMF reciprocal-rank logistic step",
                "smoothed RR among relevant items; distinct from BPR and LambdaRank",
                "pre-batch factors",
                format!("pairs={pairs} dsum={dsum:.4e}"),
            ),
        )
    }
}

impl Predict for Climf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if x.ncols() < 2 {
            ctx.push(
                Issue::builder(IssueCode::DimensionMismatch)
                    .message("Climf predict needs user/item columns")
                    .build(),
            );
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|r| {
            let u = x.get(r, 0).max(0.0).round() as usize;
            let i = x.get(r, 1).max(0.0).round() as usize;
            self.score(u, i)
        })))
    }
}

/// Online sequential extreme learning machine (Liang et al. OS-ELM).
///
/// Random hidden layer, recursive least squares on the output. Hidden width
/// is not identification `p`. Distinct from [`MiniBatchMlp`] (SGD) and
/// [`LinearRegression`] (no hidden map).
#[derive(Clone, Debug)]
pub struct OsElm {
    /// Hidden units. Not identification `p`.
    pub n_hidden: usize,
    /// Initial inverse-Gram scale.
    pub p0: f64,
    w: Vec<Vec<f64>>,
    bias: Vec<f64>,
    beta: Vector,
    p_mat: Option<Mat<f64>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OsElm {
    fn default() -> Self {
        Self {
            n_hidden: 4,
            p0: 100.0,
            w: Vec::new(),
            bias: Vec::new(),
            beta: Vector::zeros(0),
            p_mat: None,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OsElm {
    /// OS-ELM with `n_hidden` tanh units.
    pub fn new(n_hidden: usize) -> Self {
        Self {
            n_hidden: n_hidden.max(1),
            ..Self::default()
        }
    }

    fn hidden(&self, x: &Matrix, i: usize) -> Vector {
        Vector::from_iter((0..self.n_hidden.max(1)).map(|h| {
            let mut s = self.bias.get(h).copied().unwrap_or(0.0);
            if h < self.w.len() {
                for j in 0..x.ncols().min(self.w[h].len()) {
                    s += self.w[h][j] * x.get(i, j);
                }
            }
            s.tanh()
        }))
    }

    fn init(&mut self, p: usize) {
        let h = self.n_hidden.max(1);
        let mut rng = Rng::new(0x0e17);
        self.w = (0..h)
            .map(|_| (0..p).map(|_| 0.5 * rng.standard_normal()).collect())
            .collect();
        self.bias = (0..h).map(|_| 0.25 * rng.standard_normal()).collect();
        self.beta = Vector::zeros(h);
        let p0 = if self.p0.is_finite() && self.p0 > 0.0 {
            self.p0
        } else {
            100.0
        };
        self.p_mat = Some(Mat::<f64>::from_fn(h, h, |r, c| {
            if r == c {
                p0
            } else {
                0.0
            }
        }));
        self.initialized = true;
    }
}

impl PartialFit for OsElm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        let Some(y) = y else {
            ctx.push(Issue::builder(IssueCode::MissingTarget).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "missing y"),
            );
        };
        inspect_online_xy(&mut ctx, x, Some(y));
        if x.ncols() == 0 || x.nrows() == 0 {
            if !self.initialized {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if !self.initialized {
            self.init(x.ncols());
        } else if self.w.as_slice().first().map(|r| r.len()) != Some(x.ncols()) {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let mut dsum = 0.0_f64;
        for i in 0..x.nrows().min(y.len()) {
            if !y[i].is_finite() {
                continue;
            }
            let z = self.hidden(x, i);
            let p = match self.p_mat.as_mut() {
                Some(p) => p,
                None => break,
            };
            let mut pz = Vector::zeros(z.len());
            for r in 0..z.len() {
                let mut s = 0.0;
                for c in 0..z.len() {
                    s += p[(r, c)] * z[c];
                }
                pz[r] = s;
            }
            let denom = 1.0 + z.dot(&pz);
            if !denom.is_finite() || denom.abs() < 1e-18 {
                ctx.push(
                    Issue::builder(IssueCode::NearSingular)
                        .severity(Severity::Warning)
                        .message("OsElm gain denominator vanished")
                        .build(),
                );
                continue;
            }
            let pred = z.dot(&self.beta);
            let err = y[i] - pred;
            for r in 0..z.len() {
                let g = pz[r] / denom;
                let before = self.beta[r];
                self.beta[r] += g * err;
                dsum += (self.beta[r] - before).abs();
                for c in 0..z.len() {
                    p[(r, c)] -= g * pz[c];
                }
            }
            self.n_seen += 1;
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(dsum);
        q.information_gain = Some(dsum);
        q.still_identified = self.n_seen >= self.n_hidden.max(1) as u64;
        q.warmup = self.n_seen < self.n_hidden.max(1) as u64;
        q.explanation = format!(
            "OsElm hidden={} ||Δβ||₁={dsum:.4e}",
            self.n_hidden
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "OS-ELM recursive output step",
                "fixed random tanh hidden layer; RLS on β, not backprop",
                "pre-batch β",
                format!("dsum={dsum:.4e} n={}", self.n_seen),
            ),
        )
    }
}

impl Predict for OsElm {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let z = self.hidden(x, i);
            z.dot(&self.beta)
        })))
    }
}

/// Streaming peaks-over-threshold anomaly detector (Siffer et al. SPOT).
///
/// GPD tail on excesses above an init quantile. Distinct from
/// [`QuantileFilter`] (order statistic only) and [`Loda`] (projections).
/// Init length is not identification `p`.
#[derive(Clone, Debug)]
pub struct SpotAnomaly {
    /// Tail probability \(q\in(0,1)\).
    pub q: f64,
    /// Warm-up length. Not identification `p`.
    pub n_init: usize,
    init_buf: Vec<f64>,
    t0: f64,
    excesses: Vec<f64>,
    n_seen: u64,
    n_alarms: u64,
    updates: u64,
    warmed: bool,
}

impl Default for SpotAnomaly {
    fn default() -> Self {
        Self {
            q: 1e-3,
            n_init: 8,
            init_buf: Vec::new(),
            t0: f64::NAN,
            excesses: Vec::new(),
            n_seen: 0,
            n_alarms: 0,
            updates: 0,
            warmed: false,
        }
    }
}

impl SpotAnomaly {
    /// SPOT with tail level `q`.
    pub fn new(q: f64) -> Self {
        Self {
            q,
            ..Self::default()
        }
    }

    fn row_score(x: &Matrix, i: usize) -> f64 {
        if x.ncols() == 1 {
            x.get(i, 0)
        } else {
            let mut s = 0.0_f64;
            for j in 0..x.ncols() {
                let v = x.get(i, j);
                if v.is_finite() {
                    s += v * v;
                }
            }
            s.sqrt()
        }
    }

    fn finish_init(&mut self) {
        if self.init_buf.is_empty() {
            return;
        }
        let mut xs = self.init_buf.clone();
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let idx = ((xs.len() - 1) as f64 * 0.9).round() as usize;
        self.t0 = xs[idx.min(xs.len() - 1)];
        self.excesses = self
            .init_buf
            .iter()
            .filter_map(|v| {
                if *v > self.t0 {
                    Some(*v - self.t0)
                } else {
                    None
                }
            })
            .collect();
        self.warmed = true;
    }

    fn gpd_threshold(&self) -> f64 {
        if !self.t0.is_finite() {
            return f64::NAN;
        }
        if self.excesses.len() < 2 {
            return self.t0;
        }
        let n = self.excesses.len() as f64;
        let mean = self.excesses.iter().sum::<f64>() / n;
        let var = self
            .excesses
            .iter()
            .map(|e| {
                let d = *e - mean;
                d * d
            })
            .sum::<f64>()
            / n;
        if !mean.is_finite() || mean <= 0.0 {
            return self.t0;
        }
        let mut xi = 0.5 * (1.0 - mean * mean / var.max(1e-12));
        xi = xi.clamp(-0.4, 0.4);
        let sig = (mean * (1.0 - xi)).max(1e-6);
        let ntot = self.n_seen.max(1) as f64;
        let npeak = self.excesses.len().max(1) as f64;
        let qv = if self.q.is_finite() && self.q > 0.0 && self.q < 1.0 {
            self.q
        } else {
            1e-3
        };
        let r = (qv * ntot / npeak).clamp(1e-9, 1.0);
        if xi.abs() < 1e-6 {
            self.t0 - sig * r.ln()
        } else {
            self.t0 + sig / xi * (r.powf(-xi) - 1.0)
        }
    }
}

impl PartialFit for SpotAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 || x.nrows() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        let need = self.n_init.max(4);
        let mut alarms = 0u64;
        for i in 0..x.nrows() {
            let v = Self::row_score(x, i);
            if !v.is_finite() {
                continue;
            }
            self.n_seen += 1;
            if !self.warmed {
                self.init_buf.push(v);
                if self.init_buf.len() >= need {
                    self.finish_init();
                }
                continue;
            }
            let zq = self.gpd_threshold();
            if v > zq {
                alarms += 1;
                self.n_alarms += 1;
            } else if v > self.t0 {
                self.excesses.push(v - self.t0);
                if self.excesses.len() > 256 {
                    self.excesses.remove(0);
                }
            }
        }
        self.updates += 1;
        if !self.warmed {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message(format!(
                        "SpotAnomaly warming {} / {need}",
                        self.init_buf.len()
                    ))
                    .build(),
            );
        }
        let zq = self.gpd_threshold();
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some(alarms as f64);
        q.information_gain = Some(self.excesses.len() as f64);
        q.still_identified = self.warmed && self.excesses.len() >= 2;
        q.warmup = !self.warmed;
        q.explanation = format!(
            "SPOT alarms={alarms} z_q={:.4e} peaks={}",
            if zq.is_finite() { zq } else { 0.0 },
            self.excesses.len()
        );
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "SPOT GPD tail step",
                "peaks over init quantile; distinct from QuantileFilter",
                "pre-batch excesses",
                format!("alarms={alarms} zq={zq:.4e}"),
            ),
        )
    }
}

impl Predict for SpotAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.warmed {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let zq = self.gpd_threshold();
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let v = Self::row_score(x, i);
            if v.is_finite() && zq.is_finite() && v > zq {
                1.0
            } else {
                0.0
            }
        })))
    }
}

/// McDiarmid drift detector (river `drift.MDDM_A`).
///
/// Sliding-window mean gap vs McDiarmid bound. Distinct from [`Hddm`]
/// (Hoeffding on a running error rate) and [`Kswin`] (KS). Window length is
/// not identification `p`.
#[derive(Clone, Debug)]
pub struct Mddm {
    /// Window length. Not identification `p`.
    pub window_size: usize,
    /// Recent half used for the gap.
    pub stat_size: usize,
    /// Type-I level \(\delta\).
    pub delta: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
}

impl Default for Mddm {
    fn default() -> Self {
        Self {
            window_size: 16,
            stat_size: 6,
            delta: 0.01,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Mddm {
    /// MDDM with window `window_size`.
    pub fn new(window_size: usize) -> Self {
        Self {
            window_size: window_size.max(4),
            ..Self::default()
        }
    }

    /// Consume one scalar.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if x.is_finite() {
            self.window.push(x);
        }
        if self.window.len() > self.window_size {
            let drop = self.window.len() - self.window_size;
            self.window.drain(0..drop);
        }
        self.n_seen += 1;
        let r = self.stat_size.min(self.window.len() / 2).max(1);
        if self.window.len() < r * 2 {
            return ctx.finish(DriftDecision::Stable);
        }
        let n = self.window.len();
        let recent = &self.window[n - r..];
        let past = &self.window[..n - r];
        let mu_r = recent.iter().sum::<f64>() / recent.len() as f64;
        let mu_p = past.iter().sum::<f64>() / past.len() as f64;
        let mut lo = f64::INFINITY;
        let mut hi = f64::NEG_INFINITY;
        for v in self.window.iter() {
            if *v < lo {
                lo = *v;
            }
            if *v > hi {
                hi = *v;
            }
        }
        let range = (hi - lo).max(1e-6);
        let del = if self.delta.is_finite() && self.delta > 0.0 && self.delta < 1.0 {
            self.delta
        } else {
            0.01
        };
        let eps = range
            * ((-0.5 * del.ln()) * (1.0 / past.len() as f64 + 1.0 / recent.len() as f64)).sqrt();
        let stat = (mu_r - mu_p).abs();
        if stat > eps {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("MDDM |Δμ|={stat:.4e} > ε={eps:.4e}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.window.clear();
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for Mddm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        let mut last = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("mddm")) {
                Ok(q) => {
                    if let DriftDecision::Drift { statistic } = q.value {
                        fired += 1.0;
                        last = statistic;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(last);
        q.still_identified = self.window.len() >= 4;
        q.warmup = self.window.len() < 4;
        q.explanation = format!("MDDM window={} drift_cuts={fired}", self.window.len());
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "McDiarmid window-mean gap",
                "range-scaled McDiarmid ε; distinct from HDDM Hoeffding",
                "pre-batch window",
                format!("fired={fired} n={}", self.n_seen),
            ),
        )
    }
}

/// Classic CUSUM on a stream mean (not Page–Hinkley).
///
/// \(S^\pm\) reset at 0; drift when \(|S|>h\). Distinct from [`PageHinkley`]
/// (fading min/max of a single cumulative). Threshold is not identification
/// `p`.
#[derive(Clone, Debug)]
pub struct CusumDrift {
    /// Allowance \(k\).
    pub k: f64,
    /// Decision threshold \(h\).
    pub h: f64,
    mean: f64,
    s_pos: f64,
    s_neg: f64,
    n_seen: u64,
    updates: u64,
}

impl Default for CusumDrift {
    fn default() -> Self {
        Self {
            k: 0.5,
            h: 8.0,
            mean: 0.0,
            s_pos: 0.0,
            s_neg: 0.0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl CusumDrift {
    /// CUSUM with allowance `k` and threshold `h`.
    pub fn new(k: f64, h: f64) -> Self {
        Self {
            k,
            h,
            ..Self::default()
        }
    }

    /// Consume one scalar.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if !x.is_finite() {
            return ctx.finish(DriftDecision::Stable);
        }
        self.n_seen += 1;
        self.mean += (x - self.mean) / self.n_seen as f64;
        let kk = if self.k.is_finite() && self.k > 0.0 {
            self.k
        } else {
            0.5
        };
        let hh = if self.h.is_finite() && self.h > 0.0 {
            self.h
        } else {
            8.0
        };
        self.s_pos = (self.s_pos + x - self.mean - kk).max(0.0);
        self.s_neg = (self.s_neg + x - self.mean + kk).min(0.0);
        let stat = self.s_pos.max(-self.s_neg);
        if stat > hh {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("CUSUM S={stat:.4e} > h={hh:.4e}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.s_pos = 0.0;
            self.s_neg = 0.0;
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for CusumDrift {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("cusum")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.s_pos.max(-self.s_neg));
        q.still_identified = self.n_seen >= 4;
        q.warmup = self.n_seen < 4;
        q.explanation = format!(
            "CUSUM S+={:.4e} S-={:.4e} cuts={fired}",
            self.s_pos, self.s_neg
        );
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "two-sided CUSUM step",
                "reset-to-zero CUSUM; distinct from Page–Hinkley fading extrema",
                "pre-batch S",
                format!("fired={fired} n={}", self.n_seen),
            ),
        )
    }
}

/// Streaming Mahalanobis anomaly score.
///
/// Welford mean and scatter, ridge-stabilized quadratic form. Distinct from
/// [`GaussianScorer`] (per-coordinate z) and [`MultivariateGaussian`]
/// (accumulator only). Dimension is not identification `p`.
#[derive(Clone, Debug)]
pub struct OnlineMahalanobis {
    mean: Vector,
    m2: Matrix,
    n: f64,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for OnlineMahalanobis {
    fn default() -> Self {
        Self {
            mean: Vector::zeros(0),
            m2: Matrix::zeros(0, 0),
            n: 0.0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl OnlineMahalanobis {
    /// Empty Mahalanobis scorer.
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.mean.len();
        if p == 0 || self.n < 2.0 {
            return 0.0;
        }
        let mut d = vec![0.0_f64; p];
        for j in 0..p {
            d[j] = x.get(i, j) - self.mean[j];
        }
        if p == 1 {
            let v = (self.m2.get(0, 0) / self.n + 1e-4).max(1e-8);
            return d[0] * d[0] / v;
        }
        if p == 2 {
            let a = self.m2.get(0, 0) / self.n + 1e-4;
            let b = self.m2.get(0, 1) / self.n;
            let c = self.m2.get(1, 1) / self.n + 1e-4;
            let det = a * c - b * b;
            if det.abs() < 1e-12 {
                return d[0] * d[0] / a + d[1] * d[1] / c;
            }
            return (c * d[0] * d[0] - 2.0 * b * d[0] * d[1] + a * d[1] * d[1]) / det;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let v = (self.m2.get(j, j) / self.n + 1e-4).max(1e-8);
            s += d[j] * d[j] / v;
        }
        s
    }
}

impl PartialFit for OnlineMahalanobis {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let p = x.ncols();
        if p == 0 || x.nrows() == 0 {
            if !self.initialized {
                ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            }
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "empty"),
            );
        }
        if !self.initialized {
            self.mean = Vector::zeros(p);
            self.m2 = Matrix::zeros(p, p);
            self.initialized = true;
        } else if self.mean.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before = self.mean.clone();
        let mut dsum = 0.0_f64;
        for i in 0..x.nrows() {
            self.n += 1.0;
            self.n_seen += 1;
            let mut delta = Vector::zeros(p);
            for j in 0..p {
                delta[j] = x.get(i, j) - self.mean[j];
                self.mean[j] += delta[j] / self.n;
            }
            for a in 0..p {
                for b in 0..p {
                    let db = x.get(i, b) - self.mean[b];
                    self.m2.set(a, b, self.m2.get(a, b) + delta[a] * db);
                }
            }
            dsum += self.score_row(x, i);
        }
        self.updates += 1;
        let dmean = self.mean.sub(&before);
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n;
        q.parameter_delta_norm = Some(dmean.norm());
        q.information_gain = Some(dsum);
        q.still_identified = self.n >= 2.0;
        q.warmup = self.n < 2.0;
        if q.warmup {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("OnlineMahalanobis needs two rows for a covariance")
                    .build(),
            );
        }
        q.explanation = format!("OnlineMahalanobis n={:.0} Σd²={dsum:.4e}", self.n);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Mahalanobis scatter step",
                "Welford mean/scatter then (x-μ)ᵀΣ⁺(x-μ); not per-axis z-scores",
                format!("||μ||={:.6e}", before.norm()),
                format!("||μ||={:.6e} n={:.0}", self.mean.norm(), self.n),
            ),
        )
    }
}

impl Predict for OnlineMahalanobis {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        if !self.initialized || self.n < 2.0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// EWMA control chart (Roberts; river-style drift on a smoothed mean).
///
/// \(z_t=λ x_t+(1-λ)z_{t-1}\); limits use \(\sigma\sqrt{λ/(2-λ)}\). Distinct
/// from [`CusumDrift`] and [`EwMean`] (no limits). \(λ\) is not identification
/// `p`.
#[derive(Clone, Debug)]
pub struct EwmaChart {
    /// Smoothing \(λ\in(0,1]\).
    pub lambda: f64,
    /// Limit width \(L\).
    pub l: f64,
    z: f64,
    mu0: f64,
    var: f64,
    n_seen: u64,
    updates: u64,
    warmed: bool,
}

impl Default for EwmaChart {
    fn default() -> Self {
        Self {
            lambda: 0.2,
            l: 3.0,
            z: 0.0,
            mu0: 0.0,
            var: 1.0,
            n_seen: 0,
            updates: 0,
            warmed: false,
        }
    }
}

impl EwmaChart {
    /// Chart with smoothing `lambda`.
    pub fn new(lambda: f64) -> Self {
        Self {
            lambda,
            ..Self::default()
        }
    }

    /// Consume one scalar.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if !x.is_finite() {
            return ctx.finish(DriftDecision::Stable);
        }
        self.n_seen += 1;
        if !self.warmed {
            let n = self.n_seen as f64;
            self.mu0 += (x - self.mu0) / n;
            let d = x - self.mu0;
            self.var += (d * d - self.var) / n;
            self.z = self.mu0;
            if self.n_seen >= 4 {
                self.warmed = true;
            }
            return ctx.finish(DriftDecision::Stable);
        }
        let lam = if self.lambda.is_finite() && self.lambda > 0.0 && self.lambda <= 1.0 {
            self.lambda
        } else {
            0.2
        };
        self.z = lam * x + (1.0 - lam) * self.z;
        let sig = self.var.max(1e-8).sqrt();
        let half = self.l.max(1e-3) * sig * (lam / (2.0 - lam)).sqrt();
        let stat = (self.z - self.mu0).abs();
        if stat > half {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("EWMA |z-μ|={stat:.4e} > Lσ√(λ/(2-λ))={half:.4e}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for EwmaChart {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("ewma")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        if !self.warmed {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("EwmaChart is estimating μ₀,σ")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some((self.z - self.mu0).abs());
        q.still_identified = self.warmed;
        q.warmup = !self.warmed;
        q.explanation = format!("EWMA z={:.4e} cuts={fired}", self.z);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "EWMA control-chart step",
                "Roberts chart on a smoothed mean; distinct from CUSUM and EwMean",
                "pre-batch z",
                format!("z={:.4e} fired={fired}", self.z),
            ),
        )
    }
}

/// Nishida–Yamauchi STEPD (two-window two-proportion z-test).
///
/// Binarizes the stream at the current window median and compares the older
/// half to the recent half. Distinct from [`Mddm`] (McDiarmid mean gap),
/// [`Ddm`] (p+s), [`Hddm`] (Hoeffding), and [`Kswin`] (KS). Window length is
/// not identification `p`.
#[derive(Clone, Debug)]
pub struct Stepd {
    /// Length of each comparison window. Not identification `p`.
    pub window_size: usize,
    /// Two-sided type-I level.
    pub alpha: f64,
    window: Vec<f64>,
    n_seen: u64,
    updates: u64,
    last_z: f64,
}

impl Default for Stepd {
    fn default() -> Self {
        Self {
            window_size: 8,
            alpha: 0.05,
            window: Vec::new(),
            n_seen: 0,
            updates: 0,
            last_z: 0.0,
        }
    }
}

impl Stepd {
    /// STEPD with per-window length `window`.
    pub fn new(window: usize) -> Self {
        Self {
            window_size: window.max(2),
            ..Self::default()
        }
    }

    /// Consume one scalar.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if x.is_finite() {
            self.window.push(x);
        }
        let cap = self.window_size.saturating_mul(2);
        if self.window.len() > cap {
            let drop = self.window.len() - cap;
            self.window.drain(0..drop);
        }
        self.n_seen += 1;
        let w = self.window_size.max(2);
        if self.window.len() < w * 2 {
            return ctx.finish(DriftDecision::Stable);
        }
        let n = self.window.len();
        let past = &self.window[..w];
        let recent = &self.window[n - w..];
        let mut sorted: Vec<f64> = self.window.iter().copied().collect();
        sorted.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let mid = sorted.len() / 2;
        let med = if sorted.len() % 2 == 1 {
            sorted[mid]
        } else {
            0.5 * (sorted[mid - 1] + sorted[mid])
        };
        let mut s1 = 0.0_f64;
        let mut s2 = 0.0_f64;
        for v in past.iter() {
            if *v > med {
                s1 += 1.0;
            }
        }
        for v in recent.iter() {
            if *v > med {
                s2 += 1.0;
            }
        }
        let n1 = past.len() as f64;
        let n2 = recent.len() as f64;
        let p1 = s1 / n1;
        let p2 = s2 / n2;
        let ph = (s1 + s2) / (n1 + n2);
        let var = ph * (1.0 - ph) * (1.0 / n1 + 1.0 / n2);
        let stat = if var > 1e-18 {
            (p1 - p2).abs() / var.sqrt()
        } else {
            0.0
        };
        self.last_z = stat;
        let pval = 2.0 * (1.0 - norm_cdf(stat));
        let alpha = if self.alpha.is_finite() && self.alpha > 0.0 && self.alpha < 1.0 {
            self.alpha
        } else {
            0.05
        };
        if pval < alpha {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("STEPD |z|={stat:.4e} p={pval:.4e} < α={alpha:.4e}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for Stepd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("stepd")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let warmed = self.window.len() >= self.window_size.saturating_mul(2);
        if !warmed {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("STEPD needs two full windows")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.last_z);
        q.still_identified = warmed;
        q.warmup = !warmed;
        q.explanation = format!("STEPD |z|={:.4e} cuts={fired}", self.last_z);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "STEPD two-proportion step",
                "Nishida–Yamauchi z-test on median-binarized halves; not McDiarmid or KS",
                "pre-batch windows",
                format!("z={:.4e} fired={fired}", self.last_z),
            ),
        )
    }
}

/// EWMA concept-drift detector (Ross, Adams, Tasoulis, Hand; ECDD).
///
/// Bernoulli EWMA with time-varying limits
/// \(L\sigma\sqrt{\lambda/(2-\lambda)\,(1-(1-\lambda)^{2t})}\). Distinct from
/// [`EwmaChart`] (Roberts on a continuous mean) and [`Ddm`] (\(p+s\)).
/// \(\lambda\) is not identification `p`.
#[derive(Clone, Debug)]
pub struct Ecdd {
    /// Smoothing \(\lambda\in(0,1]\).
    pub lambda: f64,
    /// Limit width \(L\).
    pub l: f64,
    z: f64,
    p0: f64,
    n_seen: u64,
    t_chart: u64,
    updates: u64,
    warmed: bool,
}

impl Default for Ecdd {
    fn default() -> Self {
        Self {
            lambda: 0.2,
            l: 3.0,
            z: 0.0,
            p0: 0.0,
            n_seen: 0,
            t_chart: 0,
            updates: 0,
            warmed: false,
        }
    }
}

impl Ecdd {
    /// ECDD with smoothing `lambda`.
    pub fn new(lambda: f64) -> Self {
        Self {
            lambda,
            ..Self::default()
        }
    }

    /// Consume one error indicator (thresholded at 0.5).
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if !x.is_finite() {
            return ctx.finish(DriftDecision::Stable);
        }
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n_seen += 1;
        if !self.warmed {
            let n = self.n_seen as f64;
            self.p0 += (e - self.p0) / n;
            self.z = self.p0;
            if self.n_seen >= 8 {
                self.warmed = true;
            }
            return ctx.finish(DriftDecision::Stable);
        }
        let lam = if self.lambda.is_finite() && self.lambda > 0.0 && self.lambda <= 1.0 {
            self.lambda
        } else {
            0.2
        };
        self.t_chart += 1;
        self.z = lam * e + (1.0 - lam) * self.z;
        let t = self.t_chart as f64;
        let fade = 1.0 - (1.0 - lam).powf(2.0 * t);
        let p = self.p0.clamp(1e-6, 1.0 - 1e-6);
        let half = self.l.max(1e-3) * (p * (1.0 - p) * (lam / (2.0 - lam)) * fade.max(0.0)).sqrt();
        let stat = (self.z - self.p0).abs();
        if stat > half {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("ECDD |z-p₀|={stat:.4e} > Lσ_t={half:.4e}"))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for Ecdd {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("ecdd")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        if !self.warmed {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("ECDD is estimating the in-control error rate")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some((self.z - self.p0).abs());
        q.still_identified = self.warmed;
        q.warmup = !self.warmed;
        q.explanation = format!("ECDD z={:.4e} cuts={fired}", self.z);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "ECDD Bernoulli-EWMA step",
                "Ross time-varying limits on a 0/1 stream; not Roberts on a continuous mean",
                "pre-batch z",
                format!("z={:.4e} fired={fired}", self.z),
            ),
        )
    }
}

/// Reactive drift detection (Barros et al.; RDDM).
///
/// DDM-style \(p+s\) with warning 1.773 and drift 2.258, plus a forced
/// detection after a long warning streak. Distinct from [`Ddm`] (2/3
/// thresholds, no streak) and [`Eddm`]. Thresholds are not identification
/// `p`.
#[derive(Clone, Debug)]
pub struct Rddm {
    /// Minimum instances before a decision.
    pub min_instances: u64,
    /// Warning-streak force length.
    pub max_warnings: u64,
    n: u64,
    p: f64,
    p_min: f64,
    s_min: f64,
    warn_streak: u64,
    n_seen: u64,
    updates: u64,
}

impl Default for Rddm {
    fn default() -> Self {
        Self {
            min_instances: 30,
            max_warnings: 30,
            n: 0,
            p: 0.0,
            p_min: f64::INFINITY,
            s_min: f64::INFINITY,
            warn_streak: 0,
            n_seen: 0,
            updates: 0,
        }
    }
}

impl Rddm {
    /// RDDM with `min_instances`.
    pub fn new(min_instances: u64) -> Self {
        Self {
            min_instances: min_instances.max(8),
            ..Self::default()
        }
    }

    /// Consume one 0/1 error indicator.
    pub fn update(&mut self, x: f64, session: &Session) -> Result<Qualified<DriftDecision>> {
        let mut ctx = FitCtx::with_session(session.child("update"));
        if !x.is_finite() {
            return ctx.finish(DriftDecision::Stable);
        }
        let e = if x > 0.5 { 1.0 } else { 0.0 };
        self.n += 1;
        self.n_seen += 1;
        self.p += (e - self.p) / self.n as f64;
        let s = (self.p * (1.0 - self.p) / self.n as f64).max(0.0).sqrt();
        if self.p + s < self.p_min + self.s_min {
            self.p_min = self.p;
            self.s_min = s;
        }
        let stat = self.p + s;
        let ready = self.n >= self.min_instances;
        let drift = ready && stat > self.p_min + 2.258 * self.s_min;
        let warn = ready && stat > self.p_min + 1.773 * self.s_min;
        if warn {
            self.warn_streak += 1;
        } else {
            self.warn_streak = 0;
        }
        let forced = self.warn_streak >= self.max_warnings;
        if drift || forced {
            ctx.push(
                Issue::builder(IssueCode::ConceptDriftDetected)
                    .message(format!("RDDM p+s={stat:.4e} streak={}", self.warn_streak))
                    .metric("drift_statistic", stat)
                    .build(),
            );
            self.n = 0;
            self.p = 0.0;
            self.p_min = f64::INFINITY;
            self.s_min = f64::INFINITY;
            self.warn_streak = 0;
            return ctx.finish(DriftDecision::Drift { statistic: stat });
        }
        if warn {
            return ctx.finish(DriftDecision::Warning { statistic: stat });
        }
        ctx.finish(DriftDecision::Stable)
    }
}

impl PartialFit for Rddm {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        let mut fired = 0.0_f64;
        for i in 0..x.nrows() {
            match self.update(x.get(i, 0), &session.child("rddm")) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired += 1.0;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired += 1.0;
                    }
                }
            }
        }
        self.updates += 1;
        let warmed = self.n_seen >= self.min_instances;
        if !warmed {
            ctx.push(
                Issue::builder(IssueCode::WarmupIncomplete)
                    .message("RDDM needs min_instances before a decision")
                    .build(),
            );
        }
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.information_gain = Some(fired);
        q.drift_statistic = Some(self.p + (self.p * (1.0 - self.p) / self.n.max(1) as f64).sqrt());
        q.still_identified = warmed;
        q.warmup = !warmed;
        q.explanation = format!("RDDM p={:.4e} cuts={fired}", self.p);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "RDDM reactive p+s step",
                "Barros 1.773/2.258 levels plus warning streak; not DDM 2/3",
                "pre-batch p",
                format!("p={:.4e} fired={fired}", self.p),
            ),
        )
    }
}

/// Histogram-based outlier score (Goldstein & Dengel): axis-aligned bins.
///
/// Score \(-\sum_j\log p_j(x_j)\). Distinct from [`Loda`] (random 1-d
/// projections). Bin count is not identification `p`.
#[derive(Clone, Debug)]
pub struct Hbos {
    /// Histogram bins. Not identification `p`.
    pub n_bins: usize,
    lo: Vector,
    hi: Vector,
    counts: Vec<Vector>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Hbos {
    fn default() -> Self {
        Self {
            n_bins: 8,
            lo: Vector::zeros(0),
            hi: Vector::zeros(0),
            counts: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Hbos {
    /// HBOS with `n_bins` per feature.
    pub fn new(n_bins: usize) -> Self {
        Self {
            n_bins: n_bins.max(2),
            ..Self::default()
        }
    }
}

impl PartialFit for Hbos {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        let nb = self.n_bins.max(2);
        if !self.initialized {
            self.lo = Vector::filled(p, f64::INFINITY);
            self.hi = Vector::filled(p, f64::NEG_INFINITY);
            self.counts = (0..p).map(|_| Vector::zeros(nb)).collect();
            self.initialized = true;
        } else if self.lo.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    continue;
                }
                if z < self.lo[j] {
                    self.lo[j] = z;
                }
                if z > self.hi[j] {
                    self.hi[j] = z;
                }
                let span = self.hi[j] - self.lo[j];
                let bin = if span <= 1e-12 {
                    0
                } else {
                    let u = ((z - self.lo[j]) / span * (nb as f64 - 1e-9)).floor() as usize;
                    u.min(nb - 1)
                };
                self.counts[j][bin] += 1.0;
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("HBOS updated {p} axis-aligned histograms on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "axis-aligned histograms",
                "HBOS incrementally bins each feature; not LODA projections",
                "previous bin counts",
                "updated bin counts",
            ),
        )
    }
}

impl Predict for Hbos {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        let p = self.lo.len();
        let nb = self.n_bins.max(2);
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| {
            let mut acc = 0.0_f64;
            let mut ok = 0usize;
            for j in 0..p.min(x.ncols()) {
                let z = x.get(i, j);
                if !z.is_finite() {
                    continue;
                }
                let span = self.hi[j] - self.lo[j];
                let bin = if span <= 1e-12 {
                    0
                } else {
                    let u = ((z - self.lo[j]) / span * (nb as f64 - 1e-9)).floor() as usize;
                    u.min(nb - 1)
                };
                let tot: f64 = self.counts[j].as_slice().iter().sum();
                let pr = if tot > 0.0 {
                    (self.counts[j][bin] / tot).max(1e-12)
                } else {
                    1.0
                };
                acc -= pr.ln();
                ok += 1;
            }
            if ok == 0 {
                0.0
            } else {
                acc
            }
        })))
    }
}

/// Empirical-CDF outlier detector (Li et al. ECOD).
///
/// Score \(-\sum_j\log(2\min\{F_j,1-F_j\})\). Distinct from [`Hbos`]
/// (histogram density) and [`Loda`] (random projections).
#[derive(Clone, Debug)]
pub struct Ecod {
    samples: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Ecod {
    fn default() -> Self {
        Self {
            samples: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Ecod {
    /// Empty ECOD detector.
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.samples.len().min(x.ncols());
        let mut acc = 0.0_f64;
        let mut ok = 0usize;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() || self.samples[j].is_empty() {
                continue;
            }
            let n = self.samples[j].len() as f64;
            let mut r = 0.0_f64;
            for &v in &self.samples[j] {
                if v <= z {
                    r += 1.0;
                }
            }
            let f = (r / n).clamp(1e-6, 1.0 - 1e-6);
            acc -= (2.0 * f.min(1.0 - f)).ln();
            ok += 1;
        }
        if ok == 0 {
            0.0
        } else {
            acc
        }
    }
}

impl PartialFit for Ecod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.samples = vec![Vec::new(); p];
            self.initialized = true;
        } else if self.samples.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    continue;
                }
                if self.samples[j].len() >= 512 {
                    self.samples[j].remove(0);
                }
                self.samples[j].push(z);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("ECOD updated {p} empirical CDFs on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "empirical tail scores",
                "ECOD scores 2 min(F,1-F) per feature; not HBOS bins",
                "previous order statistics",
                "updated order statistics",
            ),
        )
    }
}

impl Predict for Ecod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Copula-based outlier detector (Li et al. COPOD).
///
/// Score \(-\log C_n(x)-\log C_n^{surv}(x)\) from the empirical copula.
/// Distinct from [`Ecod`] (sum of univariate tails) and [`Hbos`].
#[derive(Clone, Debug)]
pub struct Copod {
    samples: Vec<Vec<f64>>,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Copod {
    fn default() -> Self {
        Self {
            samples: Vec::new(),
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Copod {
    /// Empty COPOD detector.
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.samples.len().min(x.ncols());
        if p == 0 {
            return 0.0;
        }
        let n = self.samples[0].len();
        if n == 0 {
            return 0.0;
        }
        let mut left = 0.0_f64;
        let mut right = 0.0_f64;
        for t in 0..n {
            let mut le = true;
            let mut ge = true;
            for j in 0..p {
                if t >= self.samples[j].len() {
                    le = false;
                    ge = false;
                    break;
                }
                let z = x.get(i, j);
                if !z.is_finite() {
                    le = false;
                    ge = false;
                    break;
                }
                if self.samples[j][t] > z {
                    le = false;
                }
                if self.samples[j][t] < z {
                    ge = false;
                }
            }
            if le {
                left += 1.0;
            }
            if ge {
                right += 1.0;
            }
        }
        let nf = n as f64;
        let cl = (left / nf).clamp(1e-6, 1.0);
        let cr = (right / nf).clamp(1e-6, 1.0);
        -cl.ln() - cr.ln()
    }
}

impl PartialFit for Copod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.samples = vec![Vec::new(); p];
            self.initialized = true;
        } else if self.samples.len() != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    continue;
                }
                if self.samples[j].len() >= 512 {
                    self.samples[j].remove(0);
                }
                self.samples[j].push(z);
            }
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.n_seen >= 2;
        q.warmup = self.n_seen < 4;
        q.explanation = format!("COPOD updated empirical copula on {} rows", x.nrows());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "empirical copula tails",
                "COPOD uses joint C_n and survival C_n; not ECOD univariate tails",
                "previous sample cloud",
                "updated sample cloud",
            ),
        )
    }
}

impl Predict for Copod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Angle-based outlier detector (Kriegel, Schubert, Zimek ABOD).
///
/// Score is the variance of pairwise cosines of vectors from the query to a
/// reservoir of stored points. Distinct from [`Hbos`] (histograms), [`Ecod`]
/// (univariate tails), [`Copod`] (empirical copula), and [`Loda`].
#[derive(Clone, Debug)]
pub struct Abod {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Abod {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Abod {
    /// Empty ABOD detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.ncols.min(x.ncols());
        if p == 0 || self.rows.len() < 3 {
            return 0.0;
        }
        let n = self.rows.len();
        let mut cosines: Vec<f64> = Vec::new();
        for a in 0..n {
            if a >= self.rows.len() || self.rows[a].len() < p {
                continue;
            }
            for b in (a + 1)..n {
                if b >= self.rows.len() || self.rows[b].len() < p {
                    continue;
                }
                let mut dot = 0.0_f64;
                let mut na = 0.0_f64;
                let mut nb = 0.0_f64;
                let mut ok = true;
                for j in 0..p {
                    let z = x.get(i, j);
                    if !z.is_finite() {
                        ok = false;
                        break;
                    }
                    let ua = self.rows[a][j] - z;
                    let ub = self.rows[b][j] - z;
                    dot += ua * ub;
                    na += ua * ua;
                    nb += ub * ub;
                }
                if !ok {
                    continue;
                }
                let den = na.sqrt() * nb.sqrt();
                if den < 1e-18 {
                    continue;
                }
                cosines.push((dot / den).clamp(-1.0, 1.0));
            }
        }
        if cosines.len() < 2 {
            return 0.0;
        }
        let nf = cosines.len() as f64;
        let mut mean = 0.0_f64;
        for &c in &cosines {
            mean += c;
        }
        mean /= nf;
        let mut var = 0.0_f64;
        for &c in &cosines {
            let d = c - mean;
            var += d * d;
        }
        var / nf
    }
}

impl PartialFit for Abod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("ABOD reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "angle variance scores",
                "ABOD is variance of pairwise cosines; not COPOD/ECOD/HBOS",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Abod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Isolation nearest-neighbour ensemble (Bandaragoda et al. INNE).
///
/// Each reservoir point is a hypersphere of radius equal to its nearest
/// neighbour; the score is the mean \(d(x,c)/r_c\). Distinct from [`Abod`]
/// (angle variance), [`Loda`], and [`OnlineIsolationForest`].
#[derive(Clone, Debug)]
pub struct Inne {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Inne {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Inne {
    /// Empty INNE detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn nn_radius(&self, idx: usize) -> f64 {
        let p = self.ncols;
        if idx >= self.rows.len() || self.rows[idx].len() < p {
            return 1.0;
        }
        let mut best = f64::INFINITY;
        for (t, other) in self.rows.iter().enumerate() {
            if t == idx || other.len() < p {
                continue;
            }
            let mut s = 0.0_f64;
            for j in 0..p {
                let d = self.rows[idx][j] - other[j];
                s += d * d;
            }
            if s < best {
                best = s;
            }
        }
        if !best.is_finite() {
            1.0
        } else {
            best.sqrt().max(1e-8)
        }
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.ncols.min(x.ncols());
        if p == 0 || self.rows.len() < 2 {
            return 0.0;
        }
        let mut acc = 0.0_f64;
        let mut n = 0.0_f64;
        for c in 0..self.rows.len() {
            if self.rows[c].len() < p {
                continue;
            }
            let mut s = 0.0_f64;
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                let d = z - self.rows[c][j];
                s += d * d;
            }
            if !ok {
                continue;
            }
            let r = self.nn_radius(c);
            acc += s.sqrt() / r;
            n += 1.0;
        }
        if n < 1.0 {
            0.0
        } else {
            acc / n
        }
    }
}

impl PartialFit for Inne {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("INNE reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "isolation hyperspheres",
                "INNE scores d(x,c)/r_NN(c); not ABOD angles or iForest splits",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Inne {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Subspace outlier detection (Kriegel, Kröger, Schubert, Zimek SOD).
///
/// Score is the mean 1-NN distance in random axis-aligned subspaces of the
/// reservoir. Distinct from [`Inne`] (full-space hyperspheres) and [`Abod`].
#[derive(Clone, Debug)]
pub struct Sod {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Sod {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Sod {
    /// Empty SOD detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.ncols.min(x.ncols());
        if p == 0 || self.rows.len() < 2 {
            return 0.0;
        }
        let nsub = p.min(4);
        let mut acc = 0.0_f64;
        let mut n = 0.0_f64;
        for s in 0..nsub {
            let j = s % p;
            let z = x.get(i, j);
            if !z.is_finite() {
                continue;
            }
            let mut best = f64::INFINITY;
            for row in &self.rows {
                if j >= row.len() {
                    continue;
                }
                let d = (z - row[j]).abs();
                if d < best {
                    best = d;
                }
            }
            if best.is_finite() {
                acc += best;
                n += 1.0;
            }
        }
        if n < 1.0 {
            0.0
        } else {
            acc / n
        }
    }
}

impl PartialFit for Sod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("SOD reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "subspace 1-NN scores",
                "SOD averages 1-NN in axis subspaces; not INNE or ABOD",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Sod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// LOCI multi-granularity deviation factor (Papadimitriou et al.).
///
/// Score is \(1-n_r(x)/\overline{n_r}(\mathcal N_r(x))\). Distinct from
/// [`Sod`] (subspace 1-NN), [`Inne`], and [`Abod`].
#[derive(Clone, Debug)]
pub struct Loci {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Loci {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Loci {
    /// Empty LOCI detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        let p = self.ncols;
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn count_ball(&self, idx: usize, r: f64) -> f64 {
        let mut n = 0.0_f64;
        for t in 0..self.rows.len() {
            if self.dist_stored(idx, t) <= r {
                n += 1.0;
            }
        }
        n
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| self.dist_row(x, i, row))
            .filter(|d| d.is_finite())
            .collect();
        if ds.len() < 2 {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let mid = ds.len() / 2;
        let r = ds[mid].max(1e-6);
        let mut nq = 0.0_f64;
        let mut neigh: Vec<usize> = Vec::new();
        for (t, row) in self.rows.iter().enumerate() {
            let d = self.dist_row(x, i, row);
            if d <= r {
                nq += 1.0;
                neigh.push(t);
            }
        }
        if neigh.is_empty() {
            return 0.0;
        }
        let mut mean_n = 0.0_f64;
        for &t in &neigh {
            mean_n += self.count_ball(t, r);
        }
        mean_n /= neigh.len() as f64;
        if mean_n < 1e-8 {
            0.0
        } else {
            (1.0 - nq / mean_n).max(0.0)
        }
    }
}

impl PartialFit for Loci {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("LOCI reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "MDEF scores",
                "LOCI is 1-n_r/mean n_r of neighbours; not SOD/INNE/ABOD",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Loci {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Cluster-based local outlier factor (He–Wang–Deng CBLOF).
///
/// Score is Euclidean distance to the nearest large-cluster centre on a
/// two-means reservoir. Distinct from [`Sod`] (subspace 1-NN), [`Inne`], and
/// [`Loci`] (MDEF).
#[derive(Clone, Debug)]
pub struct Cblof {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    c0: Vec<f64>,
    c1: Vec<f64>,
    n0: f64,
    n1: f64,
}

impl Default for Cblof {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
            c0: Vec::new(),
            c1: Vec::new(),
            n0: 0.0,
            n1: 0.0,
        }
    }
}

impl Cblof {
    /// Empty CBLOF detector (reservoir cap 64, two centres).
    pub fn new() -> Self {
        Self::default()
    }

    fn euclid(a: &[f64], b: &[f64]) -> f64 {
        let p = a.len().min(b.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = a[j] - b[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_row(&self, x: &Matrix, i: usize, center: &[f64]) -> f64 {
        if center.is_empty() {
            return f64::INFINITY;
        }
        let p = self.ncols.min(x.ncols()).min(center.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - center[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn refit_centers(&mut self) {
        if self.rows.len() < 2 || self.ncols == 0 {
            return;
        }
        let p = self.ncols;
        let mut c0 = self.rows[0].clone();
        let last = self.rows.len() - 1;
        let mut c1 = self.rows[last].clone();
        if c0.len() < p || c1.len() < p {
            return;
        }
        let mut n0 = 0.0_f64;
        let mut n1 = 0.0_f64;
        for _ in 0..5 {
            let mut s0 = vec![0.0_f64; p];
            let mut s1 = vec![0.0_f64; p];
            n0 = 0.0;
            n1 = 0.0;
            for row in &self.rows {
                if row.len() < p {
                    continue;
                }
                if Self::euclid(row, &c0) <= Self::euclid(row, &c1) {
                    for j in 0..p {
                        s0[j] += row[j];
                    }
                    n0 += 1.0;
                } else {
                    for j in 0..p {
                        s1[j] += row[j];
                    }
                    n1 += 1.0;
                }
            }
            if n0 > 0.0 {
                for j in 0..p {
                    c0[j] = s0[j] / n0;
                }
            }
            if n1 > 0.0 {
                for j in 0..p {
                    c1[j] = s1[j] / n1;
                }
            }
        }
        self.c0 = c0;
        self.c1 = c1;
        self.n0 = n0;
        self.n1 = n1;
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 2 || self.ncols == 0 || self.c0.is_empty() {
            return 0.0;
        }
        let d0 = self.dist_row(x, i, &self.c0);
        let d1 = self.dist_row(x, i, &self.c1);
        if !d0.is_finite() && !d1.is_finite() {
            return 0.0;
        }
        if (self.n0 - self.n1).abs() < 1e-12 {
            return d0.min(d1);
        }
        if self.n0 > self.n1 {
            d0
        } else {
            d1
        }
    }
}

impl PartialFit for Cblof {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.refit_centers();
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("CBLOF reservoir n={} large n0={:.0} n1={:.0}", self.rows.len(), self.n0, self.n1);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "large-cluster distances",
                "CBLOF scores distance to the nearest large 2-means centre; not SOD/INNE/LOCI",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Cblof {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Mean \(k\)-NN distance anomaly scorer on a reservoir.
///
/// Score is the mean Euclidean distance to \(k=\min(5,n-1)\) neighbours.
/// Distinct from [`Sod`] (axis-aligned 1-NN) and [`Loci`] (MDEF).
#[derive(Clone, Debug)]
pub struct KnnAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KnnAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KnnAnomaly {
    /// Empty \(k\)-NN anomaly detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 2 || self.ncols == 0 {
            return 0.0;
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| self.dist_row(x, i, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for KnnAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("k-NN anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "mean k-NN distances",
                "KnnAnomaly is mean Euclidean k-NN in the full space; not SOD/LOCI",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KnnAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Connectivity-based outlier factor (Tang–Chen–Fu–Cheung COF).
///
/// Score is \(\mathrm{acn}(x)/\overline{\mathrm{acn}}(\mathcal N_k(x))\) from
/// set-chaining distances. Distinct from [`OnlineLof`] (k-distance),
/// [`KnnAnomaly`] (mean k-NN), and [`Loci`] (MDEF).
#[derive(Clone, Debug)]
pub struct Cof {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Cof {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Cof {
    /// Empty COF detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn knn_indices(&self, x: &Matrix, i: usize, k: usize) -> Vec<usize> {
        let mut ds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        ds.into_iter().take(k).map(|(_, t)| t).collect()
    }

    fn dist_to_seed(&self, cand: usize, query: Option<(&Matrix, usize)>, stored: Option<usize>) -> f64 {
        let mut d = f64::INFINITY;
        if let Some((xm, ri)) = query {
            d = d.min(self.dist_row(xm, ri, &self.rows[cand]));
        }
        if let Some(si) = stored {
            d = d.min(self.dist_stored(si, cand));
        }
        d
    }

    fn acn_from_chain(
        &self,
        query: Option<(&Matrix, usize)>,
        stored: Option<usize>,
        neigh: &[usize],
    ) -> f64 {
        let k = neigh.len();
        if k == 0 {
            return 0.0;
        }
        let mut remaining: Vec<usize> = neigh.to_vec();
        let mut in_set: Vec<usize> = Vec::new();
        let mut weighted = 0.0_f64;
        for step in 0..k {
            let mut best_d = f64::INFINITY;
            let mut best_idx = 0usize;
            let mut best_pos = 0usize;
            for (pos, &cand) in remaining.iter().enumerate() {
                let mut d = self.dist_to_seed(cand, query, stored);
                for &s in &in_set {
                    d = d.min(self.dist_stored(s, cand));
                }
                if d < best_d {
                    best_d = d;
                    best_idx = cand;
                    best_pos = pos;
                }
            }
            if !best_d.is_finite() {
                break;
            }
            weighted += (k - step) as f64 * best_d;
            in_set.push(best_idx);
            remaining.remove(best_pos);
        }
        2.0 * weighted / (k as f64 * (k as f64 + 1.0))
    }

    fn acn_query(&self, x: &Matrix, i: usize, neigh: &[usize]) -> f64 {
        self.acn_from_chain(Some((x, i)), None, neigh)
    }

    fn acn_stored(&self, idx: usize, neigh: &[usize]) -> f64 {
        self.acn_from_chain(None, Some(idx), neigh)
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let neigh = self.knn_indices(x, i, k);
        if neigh.is_empty() {
            return 0.0;
        }
        let acn_x = self.acn_query(x, i, &neigh);
        let mut mean = 0.0_f64;
        let mut n = 0.0_f64;
        for &t in &neigh {
            let mut ds: Vec<(f64, usize)> = (0..self.rows.len())
                .filter(|&u| u != t)
                .map(|u| (self.dist_stored(t, u), u))
                .filter(|(d, _)| d.is_finite())
                .collect();
            ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
            let tn: Vec<usize> = ds.into_iter().take(k).map(|(_, u)| u).collect();
            if tn.is_empty() {
                continue;
            }
            mean += self.acn_stored(t, &tn);
            n += 1.0;
        }
        if n < 1.0 || mean < 1e-18 {
            0.0
        } else {
            acn_x / (mean / n)
        }
    }
}

impl PartialFit for Cof {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("COF reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "chaining acn ratios",
                "COF is acn(x)/mean acn of neighbours; not LOF/kNN/LOCI",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Cof {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Influenced outlierness (Jin–Tung–Han–Wang INFLO).
///
/// Score is mean density on the influence space \(k\mathrm{NN}\cup RN_k\)
/// over own density. Distinct from [`OnlineLof`] (k-distance) and [`Cof`].
#[derive(Clone, Debug)]
pub struct Inflo {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Inflo {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Inflo {
    /// Empty INFLO detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn k_distance_stored(&self, idx: usize, k: usize) -> f64 {
        let mut ds: Vec<f64> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| self.dist_stored(idx, t))
            .filter(|d| d.is_finite())
            .collect();
        if ds.is_empty() {
            return f64::INFINITY;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        ds[(k.min(ds.len()) - 1).min(ds.len() - 1)]
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut qds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if qds.len() < k {
            return 0.0;
        }
        qds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let kd_q = qds[k - 1].0.max(1e-12);
        let mut infl: Vec<usize> = qds.iter().take(k).map(|(_, t)| *t).collect();
        for t in 0..self.rows.len() {
            let d = self.dist_row(x, i, &self.rows[t]);
            if d.is_finite() && d <= self.k_distance_stored(t, k) && !infl.contains(&t) {
                infl.push(t);
            }
        }
        if infl.is_empty() {
            return 0.0;
        }
        let mut mean_den = 0.0_f64;
        for &t in &infl {
            mean_den += 1.0 / self.k_distance_stored(t, k).max(1e-12);
        }
        mean_den /= infl.len() as f64;
        mean_den * kd_q
    }
}

impl PartialFit for Inflo {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("INFLO reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "influence-space density ratios",
                "INFLO is mean density of kNN∪RN over own density; not LOF/COF",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Inflo {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Local outlier probability (Kriegel–Kröger–Schubert–Zimek LoOP).
///
/// Score is \(\max(0,\mathrm{erf}(\mathrm{PLOF}/(n\mathrm{PLOF}\sqrt{2})))\).
/// Distinct from [`OnlineLof`] (k-distance), [`Cof`] (chaining), and
/// [`Inflo`] (influence space).
#[derive(Clone, Debug)]
pub struct Loop {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Loop {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Loop {
    /// Empty LoOP detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn pdist_stored(&self, idx: usize, k: usize) -> f64 {
        let mut ds: Vec<f64> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| self.dist_stored(idx, t))
            .filter(|d| d.is_finite())
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = k.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t] * ds[t];
        }
        (s / kk as f64).sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut qds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if qds.len() < k {
            return 0.0;
        }
        qds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let mut p2 = 0.0_f64;
        for t in 0..k {
            p2 += qds[t].0 * qds[t].0;
        }
        let pdist_q = (p2 / k as f64).sqrt();
        let mut mean_nb = 0.0_f64;
        for t in 0..k {
            mean_nb += self.pdist_stored(qds[t].1, k);
        }
        mean_nb /= k as f64;
        if mean_nb < 1e-12 {
            return 0.0;
        }
        let plof = pdist_q / mean_nb - 1.0;
        let mut ss = 0.0_f64;
        let mut n = 0.0_f64;
        for t in 0..self.rows.len() {
            let pd = self.pdist_stored(t, k);
            let mut nb = 0.0_f64;
            let mut ds: Vec<(f64, usize)> = (0..self.rows.len())
                .filter(|&u| u != t)
                .map(|u| (self.dist_stored(t, u), u))
                .filter(|(d, _)| d.is_finite())
                .collect();
            if ds.len() < k {
                continue;
            }
            ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
            for u in 0..k {
                nb += self.pdist_stored(ds[u].1, k);
            }
            nb /= k as f64;
            if nb < 1e-12 {
                continue;
            }
            let p = pd / nb - 1.0;
            ss += p * p;
            n += 1.0;
        }
        let nplof = if n > 0.0 {
            (3.0 * (ss / n).sqrt()).max(1e-8)
        } else {
            1.0
        };
        crate::special::erf(plof / (nplof * std::f64::consts::SQRT_2)).max(0.0)
    }
}

impl PartialFit for Loop {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("LoOP reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "local outlier probabilities",
                "LoOP is erf of PLOF/nPLOF; not LOF/COF/INFLO",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Loop {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Local distance-based outlier factor (Zhang–Hutter–Jin LDOF).
///
/// Score is mean \(k\)-NN distance over mean pairwise distance among the
/// neighbours. Distinct from [`KnnAnomaly`] (numerator only) and [`Cof`].
#[derive(Clone, Debug)]
pub struct Ldof {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Ldof {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Ldof {
    /// Empty LDOF detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(2);
        let mut qds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if qds.len() < k {
            return 0.0;
        }
        qds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let mut dknn = 0.0_f64;
        let neigh: Vec<usize> = qds.iter().take(k).map(|(d, t)| {
            dknn += *d;
            *t
        }).collect();
        dknn /= k as f64;
        let mut pair = 0.0_f64;
        let mut np = 0.0_f64;
        for a in 0..neigh.len() {
            for b in (a + 1)..neigh.len() {
                let d = self.dist_stored(neigh[a], neigh[b]);
                if d.is_finite() {
                    pair += d;
                    np += 1.0;
                }
            }
        }
        if np < 1.0 || pair < 1e-18 {
            0.0
        } else {
            dknn / (pair / np)
        }
    }
}

impl PartialFit for Ldof {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("LDOF reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "LDOF ratios",
                "LDOF is mean k-NN over mean pairwise neighbour distance; not kNN/COF",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Ldof {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// ODIN reverse \(k\)-NN in-degree outlier score (Hautamäki et al.).
///
/// Score is \((k-\min(\mathrm{in}\text{-}\mathrm{degree},k))/k\). Distinct from
/// [`Inflo`] (density ratio) and [`KnnAnomaly`] (mean distance).
#[derive(Clone, Debug)]
pub struct Odin {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Odin {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Odin {
    /// Empty ODIN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn k_distance_stored(&self, idx: usize, k: usize) -> f64 {
        let mut ds: Vec<f64> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| self.dist_stored(idx, t))
            .filter(|d| d.is_finite())
            .collect();
        if ds.is_empty() {
            return f64::INFINITY;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        ds[(k.min(ds.len()) - 1).min(ds.len() - 1)]
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut indeg = 0.0_f64;
        for t in 0..self.rows.len() {
            let d = self.dist_row(x, i, &self.rows[t]);
            if d.is_finite() && d <= self.k_distance_stored(t, k) {
                indeg += 1.0;
            }
        }
        (k as f64 - indeg.min(k as f64)).max(0.0) / k as f64
    }
}

impl PartialFit for Odin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("ODIN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "reverse k-NN in-degree scores",
                "ODIN is (k-in-degree)/k; not INFLO/kNN",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Odin {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Gaussian kernel-density anomaly scorer on a reservoir.
///
/// Score is \(-\log\) of the mean RBF kernel. Distinct from [`GaussianScorer`]
/// (one Gaussian) and [`Hbos`] (axis histograms).
#[derive(Clone, Debug)]
pub struct KdeAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    bandwidth: f64,
}

impl Default for KdeAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
            bandwidth: 1.0,
        }
    }
}

impl KdeAnomaly {
    /// Empty KDE anomaly detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist2_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s
    }

    fn refit_bandwidth(&mut self) {
        if self.rows.len() < 2 || self.ncols == 0 {
            return;
        }
        let n = self.rows.len() as f64;
        let mut mean = 0.0_f64;
        let mut m2 = 0.0_f64;
        let mut c = 0.0_f64;
        for row in &self.rows {
            if row.is_empty() {
                continue;
            }
            let z = row[0];
            c += 1.0;
            let d = z - mean;
            mean += d / c;
            m2 += d * (z - mean);
        }
        let sd = if c > 1.0 { (m2 / (c - 1.0)).sqrt() } else { 1.0 };
        self.bandwidth = (1.06 * sd.max(1e-6) * n.powf(-0.2)).clamp(1e-3, 20.0);
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.is_empty() || self.ncols == 0 {
            return 0.0;
        }
        let h2 = (2.0 * self.bandwidth * self.bandwidth).max(1e-12);
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for row in &self.rows {
            let d2 = self.dist2_row(x, i, row);
            if d2.is_finite() {
                s += (-d2 / h2).exp();
                n += 1.0;
            }
        }
        if n < 1.0 || s <= 1e-18 {
            20.0
        } else {
            -(s / n).ln()
        }
    }
}

impl PartialFit for KdeAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.refit_bandwidth();
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("KDE anomaly reservoir n={} h={:.6e}", self.rows.len(), self.bandwidth);
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "KDE surprise scores",
                "KdeAnomaly is -log mean RBF kernel; not GaussianScorer/HBOS",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KdeAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Relative density-based outlier score (Tang–He RDOS).
///
/// Score is mean Gaussian-KDE density on \(k\mathrm{NN}\cup RN_k\) over own
/// KDE. Distinct from [`Inflo`] (\(1/k\)-distance density) and
/// [`KdeAnomaly`] (\(-\log\) global KDE).
#[derive(Clone, Debug)]
pub struct Rdos {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Rdos {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Rdos {
    /// Empty RDOS detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn k_distance_stored(&self, idx: usize, k: usize) -> f64 {
        let mut ds: Vec<f64> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| self.dist_stored(idx, t))
            .filter(|d| d.is_finite())
            .collect();
        if ds.is_empty() {
            return f64::INFINITY;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        ds[(k.min(ds.len()) - 1).min(ds.len() - 1)]
    }

    fn bandwidth(&self, k: usize) -> f64 {
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for t in 0..self.rows.len() {
            let d = self.k_distance_stored(t, k);
            if d.is_finite() && d > 1e-15 {
                s += d;
                n += 1.0;
            }
        }
        if n < 1.0 {
            1.0
        } else {
            (s / n).clamp(1e-3, 20.0)
        }
    }

    fn kde_query(&self, x: &Matrix, i: usize, h2: f64) -> f64 {
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for row in &self.rows {
            let d = self.dist_row(x, i, row);
            if d.is_finite() {
                s += (-d * d / h2).exp();
                n += 1.0;
            }
        }
        if n < 1.0 {
            0.0
        } else {
            s / n
        }
    }

    fn kde_stored(&self, idx: usize, h2: f64) -> f64 {
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for t in 0..self.rows.len() {
            if t == idx {
                continue;
            }
            let d = self.dist_stored(idx, t);
            if d.is_finite() {
                s += (-d * d / h2).exp();
                n += 1.0;
            }
        }
        if n < 1.0 {
            0.0
        } else {
            s / n
        }
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut qds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if qds.len() < k {
            return 0.0;
        }
        qds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let mut infl: Vec<usize> = qds.iter().take(k).map(|(_, t)| *t).collect();
        for t in 0..self.rows.len() {
            let d = self.dist_row(x, i, &self.rows[t]);
            if d.is_finite() && d <= self.k_distance_stored(t, k) && !infl.contains(&t) {
                infl.push(t);
            }
        }
        if infl.is_empty() {
            return 0.0;
        }
        let h = self.bandwidth(k);
        let h2 = (2.0 * h * h).max(1e-12);
        let own = self.kde_query(x, i, h2).max(1e-18);
        let mut mean = 0.0_f64;
        for &t in &infl {
            mean += self.kde_stored(t, h2);
        }
        mean / (infl.len() as f64 * own)
    }
}

impl PartialFit for Rdos {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("RDOS reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "relative KDE scores on kNN∪RN",
                "RDOS is mean KDE of influence space over own KDE; not INFLO/KdeAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Rdos {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Local density factor (Latecki–Lazarevic–Pokrajac LDF).
///
/// Score is mean variable-bandwidth KDE of the \(k\)-NN over own KDE, with
/// \(h(y)=k\)-distance of \(y\). Distinct from [`KdeAnomaly`] (global
/// Silverman \(h\)) and [`Rdos`] (fixed \(h\), reverse neighbours).
#[derive(Clone, Debug)]
pub struct Ldf {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Ldf {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Ldf {
    /// Empty LDF detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn k_distance_stored(&self, idx: usize, k: usize) -> f64 {
        let mut ds: Vec<f64> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| self.dist_stored(idx, t))
            .filter(|d| d.is_finite())
            .collect();
        if ds.is_empty() {
            return f64::INFINITY;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        ds[(k.min(ds.len()) - 1).min(ds.len() - 1)]
    }

    fn knn_stored(&self, idx: usize, k: usize) -> Vec<usize> {
        let mut ds: Vec<(f64, usize)> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| (self.dist_stored(idx, t), t))
            .filter(|(d, _)| d.is_finite())
            .collect();
        ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        ds.into_iter().take(k).map(|(_, t)| t).collect()
    }

    fn rho_from(&self, ds: &[(f64, usize)], k: usize) -> f64 {
        if ds.is_empty() {
            return 0.0;
        }
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for &(d, t) in ds {
            let h = self.k_distance_stored(t, k).max(1e-12);
            s += (-d * d / (2.0 * h * h)).exp() / h;
            n += 1.0;
        }
        if n < 1.0 {
            0.0
        } else {
            s / n
        }
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut qds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if qds.len() < k {
            return 0.0;
        }
        qds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let neigh: Vec<(f64, usize)> = qds.into_iter().take(k).collect();
        let own = self.rho_from(&neigh, k).max(1e-18);
        let mut mean = 0.0_f64;
        let mut n = 0.0_f64;
        for &(_, t) in &neigh {
            let tn = self.knn_stored(t, k);
            let tds: Vec<(f64, usize)> = tn.iter().map(|&u| (self.dist_stored(t, u), u)).collect();
            mean += self.rho_from(&tds, k);
            n += 1.0;
        }
        if n < 1.0 {
            0.0
        } else {
            mean / (n * own)
        }
    }
}

impl PartialFit for Ldf {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("LDF reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "variable-bandwidth local density ratios",
                "LDF uses k-distance kernels on kNN; not RDOS/KdeAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Ldf {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Neighbourhood outlier factor on the natural-neighbour graph.
///
/// \(k\) is the smallest integer such that every reservoir point has
/// in-degree \(\ge 1\). Score is \((k-\mathrm{in\text{-}degree})/k\).
/// Distinct from [`Odin`] (fixed \(k=5\)).
#[derive(Clone, Debug)]
pub struct Nof {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Nof {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Nof {
    /// Empty NOF detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn dist_stored(&self, a: usize, b: usize) -> f64 {
        if a >= self.rows.len() || b >= self.rows.len() {
            return f64::INFINITY;
        }
        let p = self.ncols;
        if self.rows[a].len() < p || self.rows[b].len() < p {
            return f64::INFINITY;
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let d = self.rows[a][j] - self.rows[b][j];
            s += d * d;
        }
        s.sqrt()
    }

    fn knn_stored(&self, idx: usize, k: usize) -> Vec<usize> {
        let mut ds: Vec<(f64, usize)> = (0..self.rows.len())
            .filter(|&t| t != idx)
            .map(|t| (self.dist_stored(idx, t), t))
            .filter(|(d, _)| d.is_finite())
            .collect();
        ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        ds.into_iter().take(k).map(|(_, t)| t).collect()
    }

    fn natural_k(&self) -> usize {
        let n = self.rows.len();
        if n < 2 {
            return 1;
        }
        for k in 1..n {
            let mut indeg = vec![0usize; n];
            for i in 0..n {
                for t in self.knn_stored(i, k) {
                    if t < n {
                        indeg[t] += 1;
                    }
                }
            }
            if indeg.iter().all(|&d| d >= 1) {
                return k;
            }
        }
        n - 1
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = self.natural_k().max(1);
        let mut indeg = 0.0_f64;
        for t in 0..self.rows.len() {
            let neigh = self.knn_stored(t, k);
            let d = self.dist_row(x, i, &self.rows[t]);
            if !d.is_finite() {
                continue;
            }
            let farthest = neigh
                .iter()
                .map(|&u| self.dist_stored(t, u))
                .fold(0.0_f64, f64::max);
            if d <= farthest {
                indeg += 1.0;
            }
        }
        (k as f64 - indeg.min(k as f64)).max(0.0) / k as f64
    }
}

impl PartialFit for Nof {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("NOF reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "natural-neighbour in-degree scores",
                "NOF uses adaptive natural k; not ODIN fixed-k",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Nof {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Fast angle-based outlier detection (kNN-restricted ABOD).
///
/// Variance of pairwise cosines is computed only among the query's \(k\)
/// nearest reservoir points. Distinct from [`Abod`] (all pairs).
#[derive(Clone, Debug)]
pub struct FastAbod {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for FastAbod {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl FastAbod {
    /// Empty FastABOD detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let p = self.ncols.min(x.ncols());
        if p == 0 || self.rows.len() < 3 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(2);
        let mut ds: Vec<(f64, usize)> = self
            .rows
            .iter()
            .enumerate()
            .map(|(t, row)| (self.dist_row(x, i, row), t))
            .filter(|(d, _)| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.len() < 2 {
            return 0.0;
        }
        ds.sort_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        let neigh: Vec<usize> = ds.into_iter().take(k).map(|(_, t)| t).collect();
        let mut cosines: Vec<f64> = Vec::new();
        for (ia, &a) in neigh.iter().enumerate() {
            if a >= self.rows.len() || self.rows[a].len() < p {
                continue;
            }
            for &b in neigh.iter().skip(ia + 1) {
                if b >= self.rows.len() || self.rows[b].len() < p {
                    continue;
                }
                let mut dot = 0.0_f64;
                let mut na = 0.0_f64;
                let mut nb = 0.0_f64;
                let mut ok = true;
                for j in 0..p {
                    let z = x.get(i, j);
                    if !z.is_finite() {
                        ok = false;
                        break;
                    }
                    let ua = self.rows[a][j] - z;
                    let ub = self.rows[b][j] - z;
                    dot += ua * ub;
                    na += ua * ua;
                    nb += ub * ub;
                }
                if !ok {
                    continue;
                }
                let den = na.sqrt() * nb.sqrt();
                if den < 1e-18 {
                    continue;
                }
                cosines.push((dot / den).clamp(-1.0, 1.0));
            }
        }
        if cosines.len() < 2 {
            return 0.0;
        }
        let nf = cosines.len() as f64;
        let mut mean = 0.0_f64;
        for &c in &cosines {
            mean += c;
        }
        mean /= nf;
        let mut var = 0.0_f64;
        for &c in &cosines {
            let d = c - mean;
            var += d * d;
        }
        var / nf
    }
}

impl PartialFit for FastAbod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("FastABOD reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "kNN-restricted angle variance scores",
                "FastABOD is ABOD on kNN pairs; not all-pairs Abod",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for FastAbod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Rank-1 PCA reconstruction anomaly on a reservoir.
///
/// When width \(\ge 2\), the score is \(\|(I-vv^\top)(x-\mu)\|^2\) after a
/// power-method first axis. In one dimension it is \((x-\mu)^2\). Distinct
/// from [`OnlinePca`] (Oja transform) and [`OnlineMahalanobis`].
#[derive(Clone, Debug)]
pub struct PcaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PcaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PcaAnomaly {
    /// Empty PCA anomaly detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn mean(&self) -> Vec<f64> {
        let p = self.ncols;
        let mut m = vec![0.0_f64; p];
        let mut n = 0.0_f64;
        for row in &self.rows {
            if row.len() < p {
                continue;
            }
            n += 1.0;
            for j in 0..p {
                m[j] += row[j];
            }
        }
        if n > 0.0 {
            for mj in &mut m {
                *mj /= n;
            }
        }
        m
    }

    fn first_axis(&self, mean: &[f64]) -> Vec<f64> {
        let p = self.ncols;
        let mut v = vec![0.0_f64; p];
        if p == 0 {
            return v;
        }
        v[0] = 1.0;
        for _ in 0..12 {
            let mut w = vec![0.0_f64; p];
            for row in &self.rows {
                if row.len() < p {
                    continue;
                }
                let mut proj = 0.0_f64;
                for j in 0..p {
                    proj += (row[j] - mean[j]) * v[j];
                }
                for j in 0..p {
                    w[j] += proj * (row[j] - mean[j]);
                }
            }
            let mut nrm = 0.0_f64;
            for wj in &w {
                nrm += wj * wj;
            }
            nrm = nrm.sqrt();
            if nrm < 1e-18 {
                break;
            }
            for j in 0..p {
                v[j] = w[j] / nrm;
            }
        }
        v
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 2 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mean = self.mean();
        if p == 1 {
            let z = x.get(i, 0);
            if !z.is_finite() {
                return 0.0;
            }
            let d = z - mean[0];
            return d * d;
        }
        let v = self.first_axis(&mean);
        let mut centered = vec![0.0_f64; p];
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            centered[j] = z - mean[j];
        }
        let mut proj = 0.0_f64;
        for j in 0..p {
            proj += centered[j] * v[j];
        }
        let mut s = 0.0_f64;
        for j in 0..p {
            let r = centered[j] - proj * v[j];
            s += r * r;
        }
        s
    }
}

impl PartialFit for PcaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 2;
        q.warmup = self.rows.len() < 2;
        q.explanation = format!("PCA anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "rank-1 PCA residual scores",
                "PcaAnomaly is reconstruction error; not OnlinePca/Mahalanobis",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for PcaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Inverse-distance weighted \(k\)-NN anomaly (harmonic mean of \(k\)-distances).
///
/// Distinct from [`KnnAnomaly`] (arithmetic mean) and [`Ldof`] (pairwise
/// neighbour scale).
#[derive(Clone, Debug)]
pub struct Knnw {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for Knnw {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl Knnw {
    /// Empty weighted \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| self.dist_row(x, i, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.len() < k {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let mut inv = 0.0_f64;
        for d in ds.iter().take(k) {
            inv += 1.0 / d;
        }
        if inv < 1e-18 {
            0.0
        } else {
            k as f64 / inv
        }
    }
}

impl PartialFit for Knnw {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("KNNW reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "harmonic-mean k-NN distances",
                "KNNW is the harmonic mean of k distances; not KnnAnomaly/LDOF",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Knnw {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn rod_median(xs: &mut [f64]) -> f64 {
    if xs.is_empty() {
        return 0.0;
    }
    xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
    let n = xs.len();
    if n % 2 == 1 {
        xs[n / 2]
    } else {
        0.5 * (xs[n / 2 - 1] + xs[n / 2])
    }
}

fn rod_mad(xs: &[f64], med: f64) -> f64 {
    let mut d: Vec<f64> = xs.iter().map(|v| (v - med).abs()).collect();
    rod_median(&mut d).max(1e-12)
}

/// Rotation-based outlier detection (random 1-D projections, max MAD z-score).
///
/// Distinct from [`GaussianScorer`] (mean/sd on raw coordinates) and
/// [`PcaAnomaly`] (first-PC residual).
#[derive(Clone, Debug)]
pub struct Rod {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
    rng: Rng,
}

impl Default for Rod {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
            rng: Rng::new(41),
        }
    }
}

impl Rod {
    /// Empty ROD detector (reservoir cap 64, eight random projections).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        if p == 0 {
            return 0.0;
        }
        let mut rng = self.rng.clone();
        let n_proj = 8usize;
        let mut worst = 0.0_f64;
        for _ in 0..n_proj {
            let mut dir = vec![0.0_f64; p];
            let mut nrm = 0.0_f64;
            for v in dir.iter_mut() {
                *v = rng.standard_normal();
                nrm += *v * *v;
            }
            let nrm = nrm.sqrt().max(1e-12);
            for v in dir.iter_mut() {
                *v /= nrm;
            }
            let mut projs: Vec<f64> = self
                .rows
                .iter()
                .map(|row| {
                    let mut s = 0.0_f64;
                    for j in 0..p.min(row.len()) {
                        s += row[j] * dir[j];
                    }
                    s
                })
                .collect();
            let med = rod_median(&mut projs);
            let mad = rod_mad(&projs, med);
            let mut q = 0.0_f64;
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                q += z * dir[j];
            }
            if !ok {
                continue;
            }
            let z = (q - med).abs() / mad;
            if z > worst {
                worst = z;
            }
        }
        worst
    }
}

impl PartialFit for Rod {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("ROD reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "rotation-based outlier detection",
                "ROD is max MAD z-score over random 1-D projections; not GaussianScorer/PCA",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for Rod {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn gmm_logpdf(y: f64, mu: f64, var: f64) -> f64 {
    let v = var.max(1e-12);
    -0.5 * ((y - mu) * (y - mu) / v + v.ln() + std::f64::consts::LN_2 + std::f64::consts::PI.ln())
}

/// Two-component 1-D GMM anomaly scorer on the first coordinate.
///
/// Distinct from [`GaussianScorer`] (single Gaussian) and [`PcaAnomaly`].
#[derive(Clone, Debug)]
pub struct GmmAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for GmmAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl GmmAnomaly {
    /// Empty two-component GMM detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 4 || self.ncols == 0 {
            return 0.0;
        }
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs: Vec<f64> = self
            .rows
            .iter()
            .filter_map(|row| row.get(0).copied())
            .filter(|v| v.is_finite())
            .collect();
        if xs.len() < 4 {
            return 0.0;
        }
        let mut sorted = xs.clone();
        sorted.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let n = sorted.len();
        let mut mu1 = sorted[n / 4];
        let mut mu2 = sorted[(3 * n) / 4];
        if (mu2 - mu1).abs() < 1e-9 {
            mu2 = mu1 + 1.0;
        }
        let mut var1 = 1.0_f64;
        let mut var2 = 1.0_f64;
        let mut w1 = 0.5_f64;
        let mut w2 = 0.5_f64;
        for _ in 0..8 {
            let mut sw1 = 0.0_f64;
            let mut sw2 = 0.0_f64;
            let mut sy1 = 0.0_f64;
            let mut sy2 = 0.0_f64;
            let mut sy21 = 0.0_f64;
            let mut sy22 = 0.0_f64;
            for &z in &xs {
                let l1 = (w1.max(1e-12)).ln() + gmm_logpdf(z, mu1, var1);
                let l2 = (w2.max(1e-12)).ln() + gmm_logpdf(z, mu2, var2);
                let m = l1.max(l2);
                let e1 = (l1 - m).exp();
                let e2 = (l2 - m).exp();
                let den = (e1 + e2).max(1e-18);
                let r1 = e1 / den;
                let r2 = e2 / den;
                sw1 += r1;
                sw2 += r2;
                sy1 += r1 * z;
                sy2 += r2 * z;
                sy21 += r1 * z * z;
                sy22 += r2 * z * z;
            }
            let tot = (sw1 + sw2).max(1e-12);
            w1 = (sw1 / tot).clamp(0.05, 0.95);
            w2 = 1.0 - w1;
            if sw1 > 1e-8 {
                mu1 = sy1 / sw1;
                var1 = (sy21 / sw1 - mu1 * mu1).max(1e-6);
            }
            if sw2 > 1e-8 {
                mu2 = sy2 / sw2;
                var2 = (sy22 / sw2 - mu2 * mu2).max(1e-6);
            }
        }
        let l1 = w1.max(1e-12).ln() + gmm_logpdf(y, mu1, var1);
        let l2 = w2.max(1e-12).ln() + gmm_logpdf(y, mu2, var2);
        let m = l1.max(l2);
        let dens = m.exp() * ((l1 - m).exp() + (l2 - m).exp());
        -dens.max(1e-300).ln()
    }
}

impl PartialFit for GmmAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("GMM-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "two-component 1-D GMM anomaly",
                "GmmAnomaly is a mixture density score; not GaussianScorer",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for GmmAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn reservoir_first_coords(rows: &[Vec<f64>]) -> Vec<f64> {
    rows.iter()
        .filter_map(|row| row.get(0).copied())
        .filter(|v| v.is_finite())
        .collect()
}

/// Interquartile-range anomaly scorer on the first coordinate.
///
/// Score is \(|x-m|/\mathrm{IQR}\). Distinct from [`Rod`] (MAD on random
/// projections) and [`GaussianScorer`] (mean/sd).
#[derive(Clone, Debug)]
pub struct IqrAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for IqrAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl IqrAnomaly {
    /// Empty IQR detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() || self.rows.len() < 4 {
            return 0.0;
        }
        let mut xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        xs.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let n = xs.len();
        let q1 = xs[n / 4];
        let q3 = xs[(3 * n) / 4];
        let med = rod_median(&mut xs);
        let iqr = (q3 - q1).max(1e-12);
        (y - med).abs() / iqr
    }
}

impl PartialFit for IqrAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("IQR-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "IQR fence anomaly",
                "IqrAnomaly is |x-median|/IQR; not Rod/GaussianScorer",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for IqrAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn student_t_logpdf(y: f64, loc: f64, scale: f64, nu: f64) -> f64 {
    if !y.is_finite() || !loc.is_finite() || scale <= 0.0 || nu <= 0.0 {
        return f64::NEG_INFINITY;
    }
    let z = (y - loc) / scale;
    crate::special::ln_gamma((nu + 1.0) * 0.5)
        - crate::special::ln_gamma(nu * 0.5)
        - 0.5 * (nu * std::f64::consts::PI).ln()
        - scale.ln()
        - 0.5 * (nu + 1.0) * (1.0 + z * z / nu).ln()
}

/// Student-\(t\) density anomaly scorer (median/MAD, \(\nu=4\)).
///
/// Distinct from [`GaussianScorer`] (normal mean/sd) and [`IqrAnomaly`].
#[derive(Clone, Debug)]
pub struct StudentTAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for StudentTAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl StudentTAnomaly {
    /// Empty Student-\(t\) detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() || self.rows.len() < 4 {
            return 0.0;
        }
        let mut xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        let med = rod_median(&mut xs);
        let mad = rod_mad(&xs, med);
        -student_t_logpdf(y, med, mad, 4.0)
    }
}

impl PartialFit for StudentTAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("Student-t anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Student-t density anomaly",
                "StudentTAnomaly is -log t_4(median, MAD); not GaussianScorer",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for StudentTAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Median \(k\)-NN anomaly (median of \(k\) distances).
///
/// Distinct from [`KnnAnomaly`] (arithmetic mean) and [`Knnw`] (harmonic mean).
#[derive(Clone, Debug)]
pub struct KnnMedian {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KnnMedian {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KnnMedian {
    /// Empty median \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| self.dist_row(x, i, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.len() < k {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let take = &ds[..k];
        if take.len() % 2 == 1 {
            take[take.len() / 2]
        } else {
            0.5 * (take[take.len() / 2 - 1] + take[take.len() / 2])
        }
    }
}

impl PartialFit for KnnMedian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("kNN-median reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "median k-NN distances",
                "KnnMedian is the median of k distances; not KnnAnomaly/KNNW",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KnnMedian {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Empirical-quantile tail anomaly on the first coordinate.
///
/// Score is \(-\log\min(\hat F(x),1-\hat F(x))\). Distinct from [`IqrAnomaly`]
/// (IQR fence) and [`Hbos`] (histogram bins).
#[derive(Clone, Debug)]
pub struct QuantileAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for QuantileAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl QuantileAnomaly {
    /// Empty empirical-quantile detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() || self.rows.len() < 4 {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        let n = xs.len() as f64;
        let cdf = xs.iter().filter(|v| **v <= y).count() as f64 / n;
        let tail = cdf.min(1.0 - cdf).max(1.0 / (n + 1.0));
        -tail.ln()
    }
}

impl PartialFit for QuantileAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("quantile-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "empirical quantile tail anomaly",
                "QuantileAnomaly is -log min(F,1-F); not IQR/HBOS",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for QuantileAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// \(k\)-th nearest-neighbour anomaly (Ramaswamy \(k\)-distance).
///
/// Distinct from [`KnnAnomaly`] (mean), [`Knnw`] (harmonic mean), and
/// [`KnnMedian`] (median).
#[derive(Clone, Debug)]
pub struct KnnKth {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KnnKth {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KnnKth {
    /// Empty \(k\)-distance detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn dist_row(&self, x: &Matrix, i: usize, row: &[f64]) -> f64 {
        let p = self.ncols.min(x.ncols()).min(row.len());
        let mut s = 0.0_f64;
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return f64::INFINITY;
            }
            let d = z - row[j];
            s += d * d;
        }
        s.sqrt()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let k = 5.min(self.rows.len().saturating_sub(1)).max(1);
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| self.dist_row(x, i, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.len() < k {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        ds[k - 1]
    }
}

impl PartialFit for KnnKth {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("kNN-kth reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "k-th neighbour distance",
                "KnnKth is the k-distance; not KnnAnomaly/KNNW/KnnMedian",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KnnKth {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn euclid_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let d = a[j] - b[j];
        s += d * d;
    }
    s.sqrt()
}

/// Energy-distance anomaly of a query against the reservoir.
///
/// Score is \(2\mathrm{E}\|x-Y\|-\mathrm{E}\|Y-Y'\|\). Distinct from
/// [`KnnAnomaly`] (mean of \(k\) neighbours only).
#[derive(Clone, Debug)]
pub struct EnergyAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for EnergyAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl EnergyAnomaly {
    /// Empty energy-distance detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let n = self.rows.len();
        let mut mean_xy = 0.0_f64;
        for row in &self.rows {
            mean_xy += euclid_rows(&qrow, row);
        }
        mean_xy /= n as f64;
        let mut mean_yy = 0.0_f64;
        let mut pairs = 0.0_f64;
        for a in 0..n {
            for b in (a + 1)..n {
                mean_yy += euclid_rows(&self.rows[a], &self.rows[b]);
                pairs += 1.0;
            }
        }
        if pairs > 0.0 {
            mean_yy /= pairs;
        }
        (2.0 * mean_xy - mean_yy).max(0.0)
    }
}

impl PartialFit for EnergyAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("energy-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "energy distance anomaly",
                "EnergyAnomaly is 2E||x-Y||-E||Y-Y'||; not k-NN mean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for EnergyAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed z-score on the first coordinate (last 8 reservoir points).
///
/// Distinct from [`GaussianScorer`] (global mean/sd) and [`IqrAnomaly`]
/// (full-reservoir IQR).
#[derive(Clone, Debug)]
pub struct WindowZscore {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowZscore {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowZscore {
    /// Empty windowed z-score detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let n = win.len() as f64;
        let mean = win.iter().copied().sum::<f64>() / n;
        let mut var = 0.0_f64;
        for v in win {
            let d = *v - mean;
            var += d * d;
        }
        let sd = (var / n).sqrt().max(1e-12);
        ((y - mean) / sd).abs()
    }
}

impl PartialFit for WindowZscore {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-zscore reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed z-score anomaly",
                "WindowZscore uses the last 8 first-coords; not GaussianScorer",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowZscore {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Sum-of-squared \(k\)-NN distances anomaly.
///
/// Score is \(\sum_{i=1}^{k} d_{(i)}^2\). Distinct from [`KnnAnomaly`] (mean of
/// \(d\)), [`KnnKth`] (\(k\)-distance), [`KnnMedian`], and [`Knnw`].
#[derive(Clone, Debug)]
pub struct SsdAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for SsdAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl SsdAnomaly {
    /// Empty sum-of-squared \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| euclid_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t] * ds[t];
        }
        s
    }
}

impl PartialFit for SsdAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("ssd-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "sum of squared k-NN distances",
                "SsdAnomaly is Σ d_{(i)}^2; not KnnAnomaly mean of d",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for SsdAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed MAD z-score on the first coordinate (last 8 reservoir points).
///
/// Distinct from [`WindowZscore`] (mean/sd) and [`Rod`] (full-reservoir
/// random projections).
#[derive(Clone, Debug)]
pub struct WindowMad {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowMad {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowMad {
    /// Empty windowed MAD detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let mut win = xs[start..].to_vec();
        let med = rod_median(&mut win);
        let mad = rod_mad(&win, med);
        (y - med).abs() / mad
    }
}

impl PartialFit for WindowMad {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-mad reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed MAD z-score",
                "WindowMad uses last-8 MAD; not WindowZscore or Rod",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowMad {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn chebyshev_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut m = 0.0_f64;
    for j in 0..p {
        m = m.max((a[j] - b[j]).abs());
    }
    m
}

/// Chebyshev (\(\ell_\infty\)) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \(\max_j|x_j-y_j|\) distances.
/// Distinct from [`KnnAnomaly`] (Euclidean mean).
#[derive(Clone, Debug)]
pub struct LinfAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LinfAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LinfAnomaly {
    /// Empty Chebyshev \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| chebyshev_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LinfAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("linf-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Chebyshev k-NN anomaly",
                "LinfAnomaly is mean L∞ k-NN; not KnnAnomaly Euclidean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LinfAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed IQR anomaly on the first coordinate (last 8 reservoir points).
///
/// Distinct from [`IqrAnomaly`] (full reservoir) and [`WindowMad`] (MAD, not
/// IQR).
#[derive(Clone, Debug)]
pub struct WindowIqr {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowIqr {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowIqr {
    /// Empty windowed IQR detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let mut win = xs[start..].to_vec();
        if win.len() < 4 {
            return 0.0;
        }
        win.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let n = win.len();
        let q1 = win[n / 4];
        let q3 = win[(3 * n) / 4];
        let med = rod_median(&mut win);
        let iqr = (q3 - q1).max(1e-12);
        (y - med).abs() / iqr
    }
}

impl PartialFit for WindowIqr {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("window-iqr reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed IQR anomaly",
                "WindowIqr uses last-8 IQR; not IqrAnomaly or WindowMad",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowIqr {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn manhattan_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        s += (a[j] - b[j]).abs();
    }
    s
}

/// Manhattan (\(\ell_1\)) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \(\sum_j|x_j-y_j|\) distances.
/// Distinct from [`KnnAnomaly`] (Euclidean) and [`LinfAnomaly`] (Chebyshev).
#[derive(Clone, Debug)]
pub struct ManhattanAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for ManhattanAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl ManhattanAnomaly {
    /// Empty Manhattan \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| manhattan_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for ManhattanAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("manhattan-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Manhattan k-NN anomaly",
                "ManhattanAnomaly is mean L1 k-NN; not KnnAnomaly or LinfAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for ManhattanAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed midrange anomaly on the first coordinate (last 8 reservoir points).
///
/// Score is \(|x-m|/(h-\ell)\) with \(m=(\ell+h)/2\). Distinct from
/// [`WindowIqr`] (quartiles) and [`WindowMad`] (MAD).
#[derive(Clone, Debug)]
pub struct WindowRange {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowRange {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowRange {
    /// Empty windowed range detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut lo = f64::INFINITY;
        let mut hi = f64::NEG_INFINITY;
        for v in win {
            if *v < lo {
                lo = *v;
            }
            if *v > hi {
                hi = *v;
            }
        }
        if !lo.is_finite() || !hi.is_finite() {
            return 0.0;
        }
        let mid = 0.5 * (lo + hi);
        let span = (hi - lo).max(1e-12);
        (y - mid).abs() / span
    }
}

impl PartialFit for WindowRange {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-range reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed midrange anomaly",
                "WindowRange uses last-8 min-max; not WindowIqr or WindowMad",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowRange {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn minkowski3_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let d = (a[j] - b[j]).abs();
        s += d * d * d;
    }
    s.cbrt()
}

/// Minkowski \(p=3\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \((\sum_j|x_j-y_j|^3)^{1/3}\)
/// distances. Distinct from [`KnnAnomaly`] (\(p=2\)), [`ManhattanAnomaly`]
/// (\(p=1\)), and [`LinfAnomaly`] (\(p=\infty\)).
#[derive(Clone, Debug)]
pub struct MinkowskiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for MinkowskiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl MinkowskiAnomaly {
    /// Empty Minkowski-\(p=3\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| minkowski3_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for MinkowskiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("minkowski3-anomaly reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Minkowski p=3 k-NN anomaly",
                "MinkowskiAnomaly is mean L3 k-NN; not Knn/Manhattan/Linf",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for MinkowskiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 coefficient-of-variation anomaly.
///
/// Score is \(|x-\bar x_{w}|/(|\bar x_{w}|+\varepsilon)\) on the trailing
/// eight first-coordinates. Distinct from [`WindowZscore`] (divides by
/// local SD) and [`WindowRange`] (min-max span).
#[derive(Clone, Debug)]
pub struct WindowCv {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowCv {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowCv {
    /// Empty windowed CV detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for v in win {
            s += *v;
            n += 1.0;
        }
        if n < 1.0 {
            return 0.0;
        }
        let mean = s / n;
        (y - mean).abs() / mean.abs().max(1e-12)
    }
}

impl PartialFit for WindowCv {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-cv reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed CV anomaly",
                "WindowCv uses last-8 relative deviation; not WindowZscore",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowCv {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Geometric-mean \(k\)-NN anomaly.
///
/// Score is \(\exp(\frac1k\sum_{t=1}^{k}\ln d_{(t)})\) of the \(k\) smallest
/// Euclidean distances. Distinct from [`KnnAnomaly`] (arithmetic mean),
/// [`SsdAnomaly`] (sum of squares), and [`EnergyAnomaly`].
#[derive(Clone, Debug)]
pub struct GeometricAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for GeometricAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl GeometricAnomaly {
    /// Empty geometric-mean \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| euclid_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut sln = 0.0_f64;
        for t in 0..kk {
            sln += ds[t].ln();
        }
        (sln / kk as f64).exp()
    }
}

impl PartialFit for GeometricAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("geometric-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "geometric-mean k-NN anomaly",
                "GeometricAnomaly is exp-mean-log k-NN; not Knn/Ssd/Energy",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for GeometricAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 RMS anomaly.
///
/// Score is \(|x|/\mathrm{rms}(w)\) on the trailing eight first-coordinates.
/// Distinct from [`WindowZscore`] (centred SD) and [`WindowCv`] (relative
/// to the local mean).
#[derive(Clone, Debug)]
pub struct WindowRms {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowRms {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowRms {
    /// Empty windowed RMS detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut ss = 0.0_f64;
        let mut n = 0.0_f64;
        for v in win {
            ss += *v * *v;
            n += 1.0;
        }
        if n < 1.0 {
            return 0.0;
        }
        let rms = (ss / n).sqrt();
        y.abs() / rms.max(1e-12)
    }
}

impl PartialFit for WindowRms {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-rms reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed RMS anomaly",
                "WindowRms uses last-8 RMS; not WindowZscore or WindowCv",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowRms {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Harmonic-mean \(k\)-NN anomaly.
///
/// Score is \(k/\sum_t 1/d_{(t)}\) of the \(k\) smallest Euclidean
/// distances. Distinct from [`KnnAnomaly`] (arithmetic) and
/// [`GeometricAnomaly`] (exp-mean-log).
#[derive(Clone, Debug)]
pub struct HarmonicAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HarmonicAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HarmonicAnomaly {
    /// Empty harmonic-mean \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| euclid_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut inv = 0.0_f64;
        for t in 0..kk {
            inv += 1.0 / ds[t];
        }
        if inv <= 1e-18 {
            return 0.0;
        }
        kk as f64 / inv
    }
}

impl PartialFit for HarmonicAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("harmonic-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "harmonic-mean k-NN anomaly",
                "HarmonicAnomaly is k/sum(1/d); not Knn or GeometricAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for HarmonicAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 trimmed-mean z-score anomaly.
///
/// Drops the lowest and highest of the trailing eight first-coordinates,
/// then scores \(|x-\bar x_{\mathrm{trim}}|/s_{\mathrm{trim}}\). Distinct
/// from [`WindowZscore`] (no trim) and [`WindowIqr`] (quartile scale).
#[derive(Clone, Debug)]
pub struct WindowTrimmed {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowTrimmed {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowTrimmed {
    /// Empty windowed trimmed-mean detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let mut win: Vec<f64> = xs[start..].to_vec();
        win.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let drop = 1usize;
        if win.len() <= 2 * drop {
            return 0.0;
        }
        let sl = &win[drop..win.len() - drop];
        let n = sl.len() as f64;
        let mut s = 0.0_f64;
        for v in sl {
            s += *v;
        }
        let mean = s / n;
        let mut ss = 0.0_f64;
        for v in sl {
            let d = *v - mean;
            ss += d * d;
        }
        let sd = (ss / n).sqrt().max(1e-12);
        (y - mean).abs() / sd
    }
}

impl PartialFit for WindowTrimmed {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("window-trimmed reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed trimmed-mean anomaly",
                "WindowTrimmed uses last-8 trimmed z; not WindowZscore or WindowIqr",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowTrimmed {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn canberra_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let den = a[j].abs() + b[j].abs();
        if den > 1e-18 {
            s += (a[j] - b[j]).abs() / den;
        }
    }
    s
}

/// Canberra \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \(\sum_j|x_j-y_j|/(|x_j|+|y_j|)\)
/// distances. Distinct from [`ManhattanAnomaly`] (raw \(\ell_1\)) and
/// [`MinkowskiAnomaly`].
#[derive(Clone, Debug)]
pub struct CanberraAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for CanberraAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl CanberraAnomaly {
    /// Empty Canberra \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| canberra_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for CanberraAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("canberra-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Canberra k-NN anomaly",
                "CanberraAnomaly is mean Canberra k-NN; not Manhattan or Minkowski",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for CanberraAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 cubed z-score anomaly.
///
/// Score is \(|(x-\bar x_w)/s_w|^3\) on the trailing eight first-coordinates.
/// Distinct from [`WindowZscore`] (unsquared) and [`WindowTrimmed`]
/// (trimmed location).
#[derive(Clone, Debug)]
pub struct WindowSkew {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowSkew {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowSkew {
    /// Empty windowed cubed-z detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for v in win {
            s += *v;
            n += 1.0;
        }
        if n < 1.0 {
            return 0.0;
        }
        let mean = s / n;
        let mut ss = 0.0_f64;
        for v in win {
            let d = *v - mean;
            ss += d * d;
        }
        let sd = (ss / n).sqrt().max(1e-12);
        let z = (y - mean) / sd;
        z.abs().powi(3)
    }
}

impl PartialFit for WindowSkew {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-skew reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed cubed-z anomaly",
                "WindowSkew uses last-8 |z|^3; not WindowZscore or WindowTrimmed",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowSkew {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn lorentzian_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        s += (1.0 + (a[j] - b[j]).abs()).ln();
    }
    s
}

/// Lorentzian \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \(\sum_j\ln(1+|x_j-y_j|)\)
/// distances. Distinct from [`ManhattanAnomaly`] and [`CanberraAnomaly`].
#[derive(Clone, Debug)]
pub struct LorentzianAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LorentzianAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LorentzianAnomaly {
    /// Empty Lorentzian \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| lorentzian_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LorentzianAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("lorentzian-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Lorentzian k-NN anomaly",
                "LorentzianAnomaly is mean log1p-L1 k-NN; not Manhattan or Canberra",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LorentzianAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 fourth-power z-score anomaly.
///
/// Score is \(|(x-\bar x_w)/s_w|^4\) on the trailing eight first-coordinates.
/// Distinct from [`WindowSkew`] (\(|z|^3\)) and [`WindowZscore`].
#[derive(Clone, Debug)]
pub struct WindowKurt {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowKurt {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowKurt {
    /// Empty windowed kurtosis detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut s = 0.0_f64;
        let mut n = 0.0_f64;
        for v in win {
            s += *v;
            n += 1.0;
        }
        if n < 1.0 {
            return 0.0;
        }
        let mean = s / n;
        let mut ss = 0.0_f64;
        for v in win {
            let d = *v - mean;
            ss += d * d;
        }
        let sd = (ss / n).sqrt().max(1e-12);
        let z = (y - mean) / sd;
        z.abs().powi(4)
    }
}

impl PartialFit for WindowKurt {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-kurt reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed |z|^4 anomaly",
                "WindowKurt uses last-8 |z|^4; not WindowSkew or WindowZscore",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowKurt {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn hassanat_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let lo = a[j].abs().min(b[j].abs());
        let hi = a[j].abs().max(b[j].abs());
        if hi > 1e-18 {
            s += 1.0 - (1.0 + lo) / (1.0 + hi);
        }
    }
    s
}

/// Hassanat \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(\sum_j(1-(1+\min|x_j|,|y_j|)/(1+\max|x_j|,|y_j|))\) distances.
/// Distinct from [`CanberraAnomaly`] and [`LorentzianAnomaly`].
#[derive(Clone, Debug)]
pub struct HassanatAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HassanatAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HassanatAnomaly {
    /// Empty Hassanat \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| hassanat_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for HassanatAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("hassanat-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Hassanat k-NN anomaly",
                "HassanatAnomaly is mean Hassanat k-NN; not Canberra or Lorentzian",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for HassanatAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 peak-ratio anomaly.
///
/// Score is \(|x|/\max_i|w_i|\) on the trailing eight first-coordinates.
/// Distinct from [`WindowRange`] (midrange over signed span) and [`WindowRms`].
#[derive(Clone, Debug)]
pub struct WindowPeak {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowPeak {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowPeak {
    /// Empty windowed peak-ratio detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut peak = 0.0_f64;
        for v in win {
            let a = v.abs();
            if a > peak {
                peak = a;
            }
        }
        y.abs() / peak.max(1e-12)
    }
}

impl PartialFit for WindowPeak {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-peak reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed peak-ratio anomaly",
                "WindowPeak uses last-8 |x|/max|w|; not WindowRange or WindowRms",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowPeak {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn angular_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        num += a[j] * b[j];
        na += a[j] * a[j];
        nb += b[j] * b[j];
    }
    let den = na.sqrt() * nb.sqrt();
    if den < 1e-18 {
        return if na < 1e-18 && nb < 1e-18 { 0.0 } else { 0.5 };
    }
    (num / den).clamp(-1.0, 1.0).acos() / std::f64::consts::PI
}

/// Angular \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest \(\arccos(\hat x\cdot\hat y)/\pi\)
/// distances. Distinct from [`HassanatAnomaly`] and [`LorentzianAnomaly`].
#[derive(Clone, Debug)]
pub struct AngularAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AngularAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AngularAnomaly {
    /// Empty angular \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| angular_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for AngularAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("angular-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "angular k-NN anomaly",
                "AngularAnomaly is mean angular k-NN; not Hassanat or Lorentzian",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for AngularAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 linear-trend residual anomaly.
///
/// Score is \(|x-(a+bt_{\mathrm{next}})|/s\) after OLS on the trailing eight
/// first-coordinates. Distinct from [`WindowZscore`] (no slope) and
/// [`WindowTrimmed`].
#[derive(Clone, Debug)]
pub struct WindowSlope {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowSlope {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowSlope {
    /// Empty windowed slope-residual detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let n = win.len() as f64;
        let mut st = 0.0_f64;
        let mut sy = 0.0_f64;
        let mut stt = 0.0_f64;
        let mut sty = 0.0_f64;
        for (k, v) in win.iter().enumerate() {
            let t = k as f64;
            st += t;
            sy += *v;
            stt += t * t;
            sty += t * *v;
        }
        let den = n * stt - st * st;
        if den.abs() < 1e-12 {
            return 0.0;
        }
        let b = (n * sty - st * sy) / den;
        let a = (sy - b * st) / n;
        let mut ss = 0.0_f64;
        for (k, v) in win.iter().enumerate() {
            let r = *v - (a + b * k as f64);
            ss += r * r;
        }
        let sd = (ss / n).sqrt().max(1e-12);
        (y - (a + b * n)).abs() / sd
    }
}

impl PartialFit for WindowSlope {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-slope reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed slope-residual anomaly",
                "WindowSlope uses last-8 OLS residual; not WindowZscore or WindowTrimmed",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowSlope {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn dice_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        num += a[j] * b[j];
        na += a[j] * a[j];
        nb += b[j] * b[j];
    }
    let den = na + nb;
    if den < 1e-18 {
        return 0.0;
    }
    (1.0 - 2.0 * num / den).max(0.0)
}

/// Dice \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(1-2\langle x,y\rangle/(\|x\|^2+\|y\|^2)\) distances.
/// Distinct from [`AngularAnomaly`] and [`HassanatAnomaly`].
#[derive(Clone, Debug)]
pub struct DiceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for DiceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl DiceAnomaly {
    /// Empty Dice \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| dice_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for DiceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("dice-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Dice k-NN anomaly",
                "DiceAnomaly is mean Dice k-NN; not Angular or Hassanat",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for DiceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 Gini-scaled residual anomaly.
///
/// Score is \(|x-\bar x_w|/(|\bar x_w|G_w+\varepsilon)\) where \(G_w\) is the
/// Gini of the trailing eight absolute first-coordinates.
/// Distinct from [`WindowCv`] (uses \(s/\bar x\)) and [`WindowMad`].
#[derive(Clone, Debug)]
pub struct WindowGini {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowGini {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowGini {
    /// Empty windowed Gini detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut absv: Vec<f64> = win.iter().map(|v| v.abs()).collect();
        absv.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let n = absv.len() as f64;
        let mut wsum = 0.0_f64;
        let mut s = 0.0_f64;
        for (k, v) in absv.iter().enumerate() {
            wsum += *v;
            s += (k as f64 + 1.0) * *v;
        }
        if wsum < 1e-18 {
            return 0.0;
        }
        let gini = (2.0 * s / (n * wsum) - (n + 1.0) / n).max(0.0);
        let mut mean = 0.0_f64;
        for v in win {
            mean += *v;
        }
        mean /= n;
        (y - mean).abs() / (mean.abs() * gini.max(1e-3) + 1e-12)
    }
}

impl PartialFit for WindowGini {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-gini reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Gini residual anomaly",
                "WindowGini uses last-8 Gini scale; not WindowCv or WindowMad",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowGini {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn tanimoto_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        num += a[j] * b[j];
        na += a[j] * a[j];
        nb += b[j] * b[j];
    }
    let den = na + nb - num;
    if den.abs() < 1e-18 {
        return 0.0;
    }
    (1.0 - num / den).max(0.0)
}

/// Tanimoto \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(1-\langle x,y\rangle/(\|x\|^2+\|y\|^2-\langle x,y\rangle)\) distances.
/// Distinct from [`DiceAnomaly`] and [`AngularAnomaly`].
#[derive(Clone, Debug)]
pub struct TanimotoAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for TanimotoAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl TanimotoAnomaly {
    /// Empty Tanimoto \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| tanimoto_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for TanimotoAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("tanimoto-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Tanimoto k-NN anomaly",
                "TanimotoAnomaly is mean Tanimoto k-NN; not Dice or Angular",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for TanimotoAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 harmonic-mean ratio anomaly.
///
/// Score is \(|x|/H_w\) where \(H_w\) is the harmonic mean of the trailing
/// eight absolute first-coordinates. Distinct from [`WindowRms`] (quadratic
/// mean) and [`HarmonicAnomaly`] (harmonic-mean \(k\)-NN).
#[derive(Clone, Debug)]
pub struct WindowHarmonic {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowHarmonic {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowHarmonic {
    /// Empty windowed harmonic-mean detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut inv = 0.0_f64;
        let mut n = 0.0_f64;
        for v in win {
            inv += 1.0 / v.abs().max(1e-12);
            n += 1.0;
        }
        if n < 1.0 {
            return 0.0;
        }
        let hmean = n / inv.max(1e-12);
        y.abs() / hmean.max(1e-12)
    }
}

impl PartialFit for WindowHarmonic {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-harmonic reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed harmonic-mean ratio anomaly",
                "WindowHarmonic uses last-8 |x|/H; not WindowRms or HarmonicAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowHarmonic {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn clark_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let den = a[j].abs() + b[j].abs();
        if den > 1e-18 {
            let r = (a[j] - b[j]) / den;
            s += r * r;
        }
    }
    s.sqrt()
}

/// Clark \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(\sqrt{\sum((x_j-y_j)/(|x_j|+|y_j|))^2}\) distances.
/// Distinct from [`CanberraAnomaly`] and [`TanimotoAnomaly`].
#[derive(Clone, Debug)]
pub struct ClarkAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for ClarkAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl ClarkAnomaly {
    /// Empty Clark \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| clark_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for ClarkAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("clark-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Clark k-NN anomaly",
                "ClarkAnomaly is mean Clark k-NN; not Canberra or Tanimoto",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for ClarkAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 median-ratio anomaly.
///
/// Score is \(|x|/M_w\) where \(M_w\) is the median of the trailing eight
/// absolute first-coordinates. Distinct from [`WindowMad`] (deviation from
/// the median) and [`OnlineMedian`] (global running median).
#[derive(Clone, Debug)]
pub struct WindowMedian {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowMedian {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowMedian {
    /// Empty windowed median detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut absv: Vec<f64> = win.iter().map(|v| v.abs()).collect();
        absv.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let mid = absv.len() / 2;
        let med = if absv.len() % 2 == 0 {
            0.5 * (absv[mid - 1] + absv[mid])
        } else {
            absv[mid]
        };
        y.abs() / med.max(1e-12)
    }
}

impl PartialFit for WindowMedian {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-median reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed median-ratio anomaly",
                "WindowMedian uses last-8 |x|/M; not WindowMad or OnlineMedian",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowMedian {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn wave_hedges_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let ma = a[j].abs().max(b[j].abs());
        if ma > 1e-18 {
            s += (a[j] - b[j]).abs() / ma;
        }
    }
    s
}

/// Wave Hedges \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(\sum |x_j-y_j|/\max(|x_j|,|y_j|)\) distances.
/// Distinct from [`CanberraAnomaly`] and [`GeometricAnomaly`].
#[derive(Clone, Debug)]
pub struct WaveHedgesAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WaveHedgesAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WaveHedgesAnomaly {
    /// Empty Wave Hedges \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| wave_hedges_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for WaveHedgesAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("wave-hedges-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Wave Hedges k-NN anomaly",
                "WaveHedgesAnomaly is mean Wave-Hedges k-NN; not Canberra or Geometric",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WaveHedgesAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 lag-1 residual anomaly.
///
/// Score is \(|x_t-x_{t-1}|/\bar{|x|}\) on the trailing eight first-coordinates.
/// Distinct from [`WindowSlope`] (window regression slope) and [`WindowMad`].
#[derive(Clone, Debug)]
pub struct WindowLag1 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag1 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag1 {
    /// Empty windowed lag-1 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let prev = win[win.len() - 1];
        (y - prev).abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag1 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-lag1 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed lag-1 residual anomaly",
                "WindowLag1 uses last-8 |x_t-x_{t-1}|; not WindowSlope or WindowMad",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag1 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn kulczynski_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        num += (a[j] - b[j]).abs();
        den += a[j].abs().min(b[j].abs());
    }
    if den < 1e-18 {
        return 0.0;
    }
    num / den
}

/// Kulczynski \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(\sum|x_j-y_j|/\sum\min(|x_j|,|y_j|)\) distances.
/// Distinct from [`CanberraAnomaly`] and [`WaveHedgesAnomaly`].
#[derive(Clone, Debug)]
pub struct KulczynskiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KulczynskiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KulczynskiAnomaly {
    /// Empty Kulczynski \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| kulczynski_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for KulczynskiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("kulczynski-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Kulczynski k-NN anomaly",
                "KulczynskiAnomaly is mean Kulczynski k-NN; not Canberra or WaveHedges",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KulczynskiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 lag-2 residual anomaly.
///
/// Score is \(|x_t-2x_{t-1}+x_{t-2}|/\bar{|x|}\) on the trailing eight
/// first-coordinates. Distinct from [`WindowLag1`] (first difference) and
/// [`WindowSlope`] (window regression slope).
#[derive(Clone, Debug)]
pub struct WindowLag2 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag2 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag2 {
    /// Empty windowed lag-2 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 2 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let prev = win[win.len() - 1];
        let prev2 = win[win.len() - 2];
        (y - 2.0 * prev + prev2).abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag2 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-lag2 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed lag-2 residual anomaly",
                "WindowLag2 uses last-8 second difference; not WindowLag1 or WindowSlope",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag2 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn ruzicka_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut nmin = 0.0_f64;
    let mut nmax = 0.0_f64;
    for j in 0..p {
        nmin += a[j].abs().min(b[j].abs());
        nmax += a[j].abs().max(b[j].abs());
    }
    if nmax < 1e-18 {
        return 0.0;
    }
    1.0 - nmin / nmax
}

/// Ružička \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest
/// \(1-\sum\min(|x_j|,|y_j|)/\sum\max(|x_j|,|y_j|)\) distances.
/// Distinct from [`KulczynskiAnomaly`] and [`TanimotoAnomaly`].
#[derive(Clone, Debug)]
pub struct RuzickaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for RuzickaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl RuzickaAnomaly {
    /// Empty Ružička \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| ruzicka_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for RuzickaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("ruzicka-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Ruzicka k-NN anomaly",
                "RuzickaAnomaly is mean Ruzicka k-NN; not Kulczynski or Tanimoto",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for RuzickaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 min-ratio anomaly.
///
/// Score is \(|x|/m_w\) where \(m_w\) is the minimum of the trailing eight
/// absolute first-coordinates. Distinct from [`WindowPeak`] (max ratio),
/// [`WindowMedian`], and [`WindowRange`].
#[derive(Clone, Debug)]
pub struct WindowMin {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowMin {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowMin {
    /// Empty windowed min-ratio detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut mn = f64::INFINITY;
        for v in win {
            let a = v.abs();
            if a < mn {
                mn = a;
            }
        }
        y.abs() / mn.max(1e-12)
    }
}

impl PartialFit for WindowMin {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-min reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed min-ratio anomaly",
                "WindowMin uses last-8 |x|/min; not WindowPeak, WindowMedian, or WindowRange",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowMin {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn taneja_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let am = 0.5 * (u + v);
        let gm = (u * v).sqrt().max(1e-18);
        s += am * (am / gm).ln();
    }
    s.max(0.0)
}

/// Taneja \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Taneja distances
/// \(\sum\frac{|x|+|y|}{2}\log\frac{|x|+|y|}{2\sqrt{|x||y|}}\).
/// Distinct from [`taneja_distance`] (which \(\ell_1\)-normalises first) and
/// [`RuzickaAnomaly`].
#[derive(Clone, Debug)]
pub struct TanejaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for TanejaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl TanejaAnomaly {
    /// Empty Taneja \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| taneja_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for TanejaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("taneja-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Taneja k-NN anomaly",
                "TanejaAnomaly is raw Taneja k-NN; not L1 Taneja or Ruzicka",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for TanejaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 mean-absolute-deviation anomaly.
///
/// Score is \(|x-\bar x_w|/\mathrm{MAD}_1\) on the trailing eight first-coordinates,
/// where \(\mathrm{MAD}_1\) is the mean absolute deviation from the window mean.
/// Distinct from [`WindowMad`] (median / MAD) and [`WindowZscore`] (mean / SD).
#[derive(Clone, Debug)]
pub struct WindowAbsdev {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowAbsdev {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowAbsdev {
    /// Empty windowed mean-absolute-deviation detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let n = win.len() as f64;
        let mut mean = 0.0_f64;
        for v in win {
            mean += *v;
        }
        mean /= n;
        let mut mad1 = 0.0_f64;
        for v in win {
            mad1 += (*v - mean).abs();
        }
        mad1 /= n;
        (y - mean).abs() / mad1.max(1e-12)
    }
}

impl PartialFit for WindowAbsdev {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-absdev reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed mean-absolute-deviation anomaly",
                "WindowAbsdev uses last-8 |x-mean|/MAD1; not WindowMad or WindowZscore",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowAbsdev {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn kumar_johnson_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d2 = u * u - v * v;
        s += d2 * d2 / (2.0 * (u * v).powf(1.5));
    }
    s.max(0.0)
}

/// Kumar–Johnson \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Kumar–Johnson distances
/// \(\sum(x^2-y^2)^2/(2(|x||y|)^{3/2})\).
/// Distinct from the \(\ell_1\)-normalised Kumar–Johnson metric and
/// [`TanejaAnomaly`].
#[derive(Clone, Debug)]
pub struct KumarJohnsonAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KumarJohnsonAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KumarJohnsonAnomaly {
    /// Empty Kumar–Johnson \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| kumar_johnson_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for KumarJohnsonAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("kumar-johnson-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Kumar-Johnson k-NN anomaly",
                "KumarJohnsonAnomaly is raw Kumar-Johnson k-NN; not L1 Kumar-Johnson or Taneja",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KumarJohnsonAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 third-difference anomaly.
///
/// Score is the backward third difference \(|x-3x_{t-1}+3x_{t-2}-x_{t-3}|\)
/// over the trailing-window mean absolute level.
/// Distinct from [`WindowLag1`] (first difference) and [`WindowLag2`]
/// (second difference).
#[derive(Clone, Debug)]
pub struct WindowLag3 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag3 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag3 {
    /// Empty windowed lag-3 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 4 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 3 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let prev = win[n - 1];
        let prev2 = win[n - 2];
        let prev3 = win[n - 3];
        (y - 3.0 * prev + 3.0 * prev2 - prev3).abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag3 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 4;
        q.warmup = self.rows.len() < 4;
        q.explanation = format!("window-lag3 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed third-difference anomaly",
                "WindowLag3 uses last-8 third difference; not WindowLag1 or WindowLag2",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag3 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn additive_symmetric_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d = u - v;
        s += d * d / (u + v);
    }
    s.max(0.0)
}

/// Additive-symmetric \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw additive-symmetric distances
/// \(\sum(x-y)^2/(|x|+|y|)\).
/// Distinct from the \(\ell_1\)-normalised additive-symmetric metric and
/// [`KumarJohnsonAnomaly`].
#[derive(Clone, Debug)]
pub struct AdditiveSymmetricAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for AdditiveSymmetricAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl AdditiveSymmetricAnomaly {
    /// Empty additive-symmetric \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| additive_symmetric_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for AdditiveSymmetricAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("additive-symmetric-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "additive-symmetric k-NN anomaly",
                "AdditiveSymmetricAnomaly is raw additive-symmetric k-NN; not L1 additive-symmetric",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for AdditiveSymmetricAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 geometric-mean residual anomaly.
///
/// Score is \(|x|/\mathrm{GM}\) on the trailing eight first-coordinates,
/// where \(\mathrm{GM}\) is the geometric mean of absolute levels.
/// Distinct from [`WindowHarmonic`] (harmonic mean) and [`WindowRms`]
/// (quadratic mean).
#[derive(Clone, Debug)]
pub struct WindowGeom {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowGeom {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowGeom {
    /// Empty windowed geometric-mean detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut log_sum = 0.0_f64;
        for v in win {
            log_sum += v.abs().max(1e-18).ln();
        }
        let gm = (log_sum / win.len() as f64).exp();
        y.abs() / gm.max(1e-12)
    }
}

impl PartialFit for WindowGeom {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-geom reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed geometric-mean anomaly",
                "WindowGeom uses last-8 |x|/GM; not WindowHarmonic or WindowRms",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowGeom {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn vicis_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d = u - v;
        let m = u.min(v);
        s += d * d / (m * m);
    }
    s.max(0.0)
}

/// Vicis-symmetric \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Vicis-symmetric distances
/// \(\sum(x-y)^2/\min(|x|,|y|)^2\).
/// Distinct from the \(\ell_1\)-normalised Vicis metric and
/// [`AdditiveSymmetricAnomaly`].
#[derive(Clone, Debug)]
pub struct VicisAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for VicisAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl VicisAnomaly {
    /// Empty Vicis-symmetric \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| vicis_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for VicisAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("vicis-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Vicis-symmetric k-NN anomaly",
                "VicisAnomaly is raw Vicis k-NN; not L1 Vicis or additive-symmetric",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for VicisAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 variance residual anomaly.
///
/// Score is \(|x-\bar x_w|/s^2_w\) on the trailing eight first-coordinates.
/// Distinct from [`WindowZscore`] (\(|x-\bar x|/s\)) and [`WindowAbsdev`]
/// (\(|x-\bar x|/\mathrm{MAD}_1\)).
#[derive(Clone, Debug)]
pub struct WindowVar {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowVar {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowVar {
    /// Empty windowed variance detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let n = win.len() as f64;
        let mut mean = 0.0_f64;
        for v in win {
            mean += *v;
        }
        mean /= n;
        let mut var = 0.0_f64;
        for v in win {
            let d = *v - mean;
            var += d * d;
        }
        var /= n;
        (y - mean).abs() / var.max(1e-12)
    }
}

impl PartialFit for WindowVar {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-var reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed variance-residual anomaly",
                "WindowVar uses last-8 |x-mean|/var; not WindowZscore or WindowAbsdev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowVar {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn topsoe_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let m = u + v;
        s += u * (2.0 * u / m).ln() + v * (2.0 * v / m).ln();
    }
    s.max(0.0)
}

/// Topsøe \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Topsøe distances
/// \(\sum[x\ln(2x/(x+y))+y\ln(2y/(x+y))]\).
/// Distinct from the \(\ell_1\)-normalised Topsøe metric and
/// [`VicisAnomaly`].
#[derive(Clone, Debug)]
pub struct TopsoeAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for TopsoeAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl TopsoeAnomaly {
    /// Empty Topsøe \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| topsoe_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for TopsoeAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("topsoe-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Topsøe k-NN anomaly",
                "TopsoeAnomaly is raw Topsøe k-NN; not L1 Topsøe or Vicis",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for TopsoeAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 fourth-difference anomaly.
///
/// Score is the backward fourth difference
/// \(|x-4x_{t-1}+6x_{t-2}-4x_{t-3}+x_{t-4}|\) over the trailing-window
/// mean absolute level.
/// Distinct from [`WindowLag3`] (third difference) and [`WindowLag2`].
#[derive(Clone, Debug)]
pub struct WindowLag4 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag4 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag4 {
    /// Empty windowed lag-4 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 5 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 4 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        (y - 4.0 * p1 + 6.0 * p2 - 4.0 * p3 + p4).abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag4 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 5;
        q.warmup = self.rows.len() < 5;
        q.explanation = format!("window-lag4 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fourth-difference anomaly",
                "WindowLag4 uses last-8 fourth difference; not WindowLag3 or WindowLag2",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag4 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn jeffreys_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        s += (u - v) * (u / v).ln();
    }
    s.max(0.0)
}

/// Jeffreys \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Jeffreys distances
/// \(\sum(x-y)\ln(x/y)\).
/// Distinct from the \(\ell_1\)-normalised Jeffreys metric and
/// [`TopsoeAnomaly`].
#[derive(Clone, Debug)]
pub struct JeffreysAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for JeffreysAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl JeffreysAnomaly {
    /// Empty Jeffreys \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| jeffreys_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for JeffreysAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("jeffreys-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Jeffreys k-NN anomaly",
                "JeffreysAnomaly is raw Jeffreys k-NN; not L1 Jeffreys or Topsøe",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for JeffreysAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 fifth-difference anomaly.
///
/// Score is the backward fifth difference
/// \(|x-5x_{t-1}+10x_{t-2}-10x_{t-3}+5x_{t-4}-x_{t-5}|\) over the
/// trailing-window mean absolute level.
/// Distinct from [`WindowLag4`] (fourth difference) and [`WindowLag3`].
#[derive(Clone, Debug)]
pub struct WindowLag5 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag5 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag5 {
    /// Empty windowed lag-5 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 6 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 5 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        (y - 5.0 * p1 + 10.0 * p2 - 10.0 * p3 + 5.0 * p4 - p5).abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag5 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 6;
        q.warmup = self.rows.len() < 6;
        q.explanation = format!("window-lag5 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifth-difference anomaly",
                "WindowLag5 uses last-8 fifth difference; not WindowLag4 or WindowLag3",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag5 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn k_divergence_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        s += u * (2.0 * u / (u + v)).ln();
    }
    s.max(0.0)
}

/// \(K\)-divergence \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw \(K\)-divergences
/// \(\sum x\ln(2x/(x+y))\).
/// Distinct from the \(\ell_1\)-normalised \(K\)-divergence and
/// [`JeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct KDivergenceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for KDivergenceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl KDivergenceAnomaly {
    /// Empty \(K\)-divergence \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| k_divergence_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for KDivergenceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("k-divergence-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "K-divergence k-NN anomaly",
                "KDivergenceAnomaly is raw K-divergence k-NN; not L1 K-divergence or Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for KDivergenceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 max-deviation residual anomaly.
///
/// Score is \(|x-\bar x_w|/\max_i|x_i-\bar x_w|\) on the trailing eight
/// first-coordinates. Distinct from [`WindowAbsdev`] (MAD1) and
/// [`WindowPeak`] (max absolute level).
#[derive(Clone, Debug)]
pub struct WindowMaxdev {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowMaxdev {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowMaxdev {
    /// Empty windowed max-deviation detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let n = win.len() as f64;
        let mut mean = 0.0_f64;
        for v in win {
            mean += *v;
        }
        mean /= n;
        let mut mx = 0.0_f64;
        for v in win {
            let d = (*v - mean).abs();
            if d > mx {
                mx = d;
            }
        }
        (y - mean).abs() / mx.max(1e-12)
    }
}

impl PartialFit for WindowMaxdev {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-maxdev reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed max-deviation anomaly",
                "WindowMaxdev uses last-8 |x-mean|/maxdev; not WindowAbsdev or WindowPeak",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowMaxdev {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn hellinger_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().sqrt();
        let v = b[j].abs().sqrt();
        let d = u - v;
        s += d * d;
    }
    (0.5 * s).max(0.0).sqrt()
}

/// Hellinger \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Hellinger distances
/// \(\sqrt{\tfrac12\sum(\sqrt{|x|}-\sqrt{|y|})^2}\).
/// Distinct from the \(\ell_1\)-normalised Hellinger metric and
/// [`KDivergenceAnomaly`].
#[derive(Clone, Debug)]
pub struct HellingerAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for HellingerAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl HellingerAnomaly {
    /// Empty Hellinger \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| hellinger_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for HellingerAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("hellinger-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Hellinger k-NN anomaly",
                "HellingerAnomaly is raw Hellinger k-NN; not L1 Hellinger or K-divergence",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for HellingerAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 Shannon-entropy anomaly.
///
/// Score is \\(-\\sum p\\ln p\\) on the trailing eight first-coordinates after
/// \\(\\ell_1\\) normalisation of absolute levels.
/// Distinct from [`WindowGini`] (Gini) and [`WindowCv`] (coefficient of variation).
#[derive(Clone, Debug)]
pub struct WindowEntropy {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowEntropy {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowEntropy {
    /// Empty windowed entropy detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 3 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        let mut s = y.abs();
        for v in win {
            s += v.abs();
        }
        if s < 1e-18 {
            return 0.0;
        }
        let mut h = 0.0_f64;
        let py = (y.abs() / s).max(1e-18);
        h -= py * py.ln();
        for v in win {
            let p = (v.abs() / s).max(1e-18);
            h -= p * p.ln();
        }
        h.max(0.0)
    }
}

impl PartialFit for WindowEntropy {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("window-entropy reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed Shannon-entropy anomaly",
                "WindowEntropy uses last-8 Shannon entropy; not WindowGini or WindowCv",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowEntropy {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn jensen_shannon_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let m = 0.5 * (u + v);
        s += 0.5 * u * (u / m).ln() + 0.5 * v * (v / m).ln();
    }
    s.max(0.0)
}

/// Jensen–Shannon \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw (unsquared) Jensen–Shannon
/// divergences. Distinct from the \\(\\ell_1\\)-normalised JS *distance*
/// and [`HellingerAnomaly`].
#[derive(Clone, Debug)]
pub struct JensenShannonAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for JensenShannonAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl JensenShannonAnomaly {
    /// Empty Jensen–Shannon \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| jensen_shannon_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for JensenShannonAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("jensen-shannon-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Jensen-Shannon k-NN anomaly",
                "JensenShannonAnomaly is raw unsquared JS k-NN; not L1 JS distance or Hellinger",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for JensenShannonAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 sixth-difference residual anomaly.
///
/// Score is \(|\Delta^6 x|/\overline{|x|}\) on the trailing eight
/// first-coordinates (binomial \(1,-6,15,-20,15,-6,1\)).
/// Distinct from [`WindowLag5`] (fifth difference) and [`WindowLag4`].
#[derive(Clone, Debug)]
pub struct WindowLag6 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag6 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag6 {
    /// Empty windowed lag-6 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 7 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 6 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        (y - 6.0 * p1 + 15.0 * p2 - 20.0 * p3 + 15.0 * p4 - 6.0 * p5 + p6).abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag6 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 7;
        q.warmup = self.rows.len() < 7;
        q.explanation = format!("window-lag6 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixth-difference anomaly",
                "WindowLag6 uses last-8 sixth difference; not WindowLag5 or WindowLag4",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag6 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn squared_chord_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().sqrt();
        let v = b[j].abs().sqrt();
        let d = u - v;
        s += d * d;
    }
    s.max(0.0)
}

/// Squared-chord \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw squared-chord distances
/// \(\sum(\sqrt{|x|}-\sqrt{|y|})^2\) without \(\ell_1\) normalisation.
/// Distinct from the \(\ell_1\)-normalised squared-chord metric and
/// [`HellingerAnomaly`] (square-root of half the same sum).
#[derive(Clone, Debug)]
pub struct SquaredChordAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for SquaredChordAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl SquaredChordAnomaly {
    /// Empty squared-chord \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| squared_chord_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for SquaredChordAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("squared-chord-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "squared-chord k-NN anomaly",
                "SquaredChordAnomaly is raw squared-chord k-NN; not L1 squared-chord or Hellinger",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for SquaredChordAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 seventh-difference residual anomaly.
///
/// Score is \(|\Delta^7 x|/\overline{|x|}\) on the trailing eight
/// first-coordinates (binomial \(1,-7,21,-35,35,-21,7,-1\)).
/// Distinct from [`WindowLag6`] (sixth difference) and [`WindowLag5`].
#[derive(Clone, Debug)]
pub struct WindowLag7 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag7 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag7 {
    /// Empty windowed lag-7 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 8 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 7 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        (y - 7.0 * p1 + 21.0 * p2 - 35.0 * p3 + 35.0 * p4 - 21.0 * p5 + 7.0 * p6 - p7).abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag7 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 8;
        q.warmup = self.rows.len() < 8;
        q.explanation = format!("window-lag7 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventh-difference anomaly",
                "WindowLag7 uses last-8 seventh difference; not WindowLag6 or WindowLag5",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag7 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn pearson_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs();
        let v = b[j].abs();
        let den = (u + v).max(1e-18);
        let d = u - v;
        s += d * d / den;
    }
    s.max(0.0)
}

/// Pearson \(\chi^2\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Pearson \(\chi^2\) distances
/// \(\sum(x-y)^2/(x+y)\) without \(\ell_1\) normalisation.
/// Distinct from the \(\ell_1\)-normalised Pearson \(\chi^2\) metric and
/// [`SquaredChordAnomaly`].
#[derive(Clone, Debug)]
pub struct PearsonChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for PearsonChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl PearsonChiAnomaly {
    /// Empty Pearson \(\chi^2\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| pearson_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for PearsonChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("pearson-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Pearson chi-squared k-NN anomaly",
                "PearsonChiAnomaly is raw Pearson chi-squared k-NN; not L1 Pearson-chi or squared-chord",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for PearsonChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-8 eighth-difference residual anomaly.
///
/// Score is \(|\Delta^7 x|/\overline{|x|}\) on the trailing eight
/// first-coordinates (binomial \(1,-8,28,-56,70,-56,28,-8,1\)).
/// Distinct from [`WindowLag7`] (sixth difference) and [`WindowLag5`].
#[derive(Clone, Debug)]
pub struct WindowLag8 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag8 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag8 {
    /// Empty windowed lag-8 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 8 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(8);
        let win = &xs[start..];
        if win.len() < 8 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        (y - 8.0 * p1 + 28.0 * p2 - 56.0 * p3 + 70.0 * p4 - 56.0 * p5 + 28.0 * p6 - 8.0 * p7 + p8).abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag8 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 8;
        q.warmup = self.rows.len() < 8;
        q.explanation = format!("window-lag8 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighth-difference anomaly",
                "WindowLag8 uses last-8 eighth difference; not WindowLag7 or WindowLag6",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag8 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn neyman_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs();
        let d = u - v;
        s += d * d / u;
    }
    s.max(0.0)
}

/// Pearson \(\chi^2\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Pearson \(\chi^2\) distances
/// \(\sum(x-y)^2/(x+y)\) without \(\ell_1\) normalisation.
/// Distinct from the \(\ell_1\)-normalised Pearson \(\chi^2\) metric and
/// [`SquaredChordAnomaly`].
#[derive(Clone, Debug)]
pub struct NeymanChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for NeymanChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl NeymanChiAnomaly {
    /// Empty Neyman \(\chi^2\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| neyman_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for NeymanChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("neyman-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Neyman chi-squared k-NN anomaly",
                "NeymanChiAnomaly is raw Pearson chi-squared k-NN; not L1 Pearson-chi or squared-chord",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for NeymanChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-9 ninth-difference residual anomaly.
///
/// Score is \(|\Delta^9 x|/\overline{|x|}\) on the trailing nine
/// first-coordinates (binomial \(1,-9,36,-84,126,-126,84,-36,9,-1\)).
/// Distinct from [`WindowLag8`] (eighth difference) and [`WindowLag7`].
#[derive(Clone, Debug)]
pub struct WindowLag9 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag9 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag9 {
    /// Empty windowed lag-9 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 9 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(9);
        let win = &xs[start..];
        if win.len() < 9 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        (y - 9.0 * p1 + 36.0 * p2 - 84.0 * p3 + 126.0 * p4 - 126.0 * p5 + 84.0 * p6
            - 36.0 * p7 + 9.0 * p8 - p9)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag9 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 9;
        q.warmup = self.rows.len() < 9;
        q.explanation = format!("window-lag9 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninth-difference anomaly",
                "WindowLag9 uses last-9 ninth difference; not WindowLag8 or WindowLag7",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag9 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn symmetric_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d = u - v;
        s += d * d * (u + v) / (u * v);
    }
    s.max(0.0)
}

/// Symmetric \(\chi^2\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw symmetric \(\chi^2\) distances
/// \(\sum(x-y)^2(x+y)/(xy)\) without \(\ell_1\) normalisation.
/// Distinct from [`PearsonChiAnomaly`] (denominator \(x+y\)) and
/// [`NeymanChiAnomaly`] (denominator \(x\)).
#[derive(Clone, Debug)]
pub struct SymmetricChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for SymmetricChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl SymmetricChiAnomaly {
    /// Empty symmetric \(\chi^2\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| symmetric_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for SymmetricChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("symmetric-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "symmetric chi-squared k-NN anomaly",
                "SymmetricChiAnomaly is raw symmetric chi-squared k-NN; not Pearson-chi or Neyman-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for SymmetricChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Last-10 tenth-difference residual anomaly.
///
/// Score is \(|\Delta^{10} x|/\overline{|x|}\) on the trailing ten
/// first-coordinates (binomial \(1,-10,45,-120,210,-252,210,-120,45,-10,1\)).
/// Distinct from [`WindowLag9`] (ninth difference) and [`WindowLag8`].
#[derive(Clone, Debug)]
pub struct WindowLag10 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag10 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag10 {
    /// Empty windowed lag-10 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 10 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(10);
        let win = &xs[start..];
        if win.len() < 10 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        (y - 10.0 * p1 + 45.0 * p2 - 120.0 * p3 + 210.0 * p4 - 252.0 * p5 + 210.0 * p6
            - 120.0 * p7 + 45.0 * p8 - 10.0 * p9 + p10)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag10 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 10;
        q.warmup = self.rows.len() < 10;
        q.explanation = format!("window-lag10 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed tenth-difference anomaly",
                "WindowLag10 uses last-10 tenth difference; not WindowLag9 or WindowLag8",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag10 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn squared_hellinger_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().sqrt();
        let v = b[j].abs().sqrt();
        let d = u - v;
        s += d * d;
    }
    (0.5 * s).max(0.0)
}

/// Squared-Hellinger \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw squared-Hellinger distances
/// \(\tfrac12\sum(\sqrt{|x|}-\sqrt{|y|})^2\) without the outer square root
/// and without \(\ell_1\) normalisation.
/// Distinct from [`HellingerAnomaly`] (square-root form) and
/// [`SquaredChordAnomaly`].
#[derive(Clone, Debug)]
pub struct SquaredHellingerAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for SquaredHellingerAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl SquaredHellingerAnomaly {
    /// Empty squared-Hellinger \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| squared_hellinger_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for SquaredHellingerAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("squared-hellinger-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "squared Hellinger k-NN anomaly",
                "SquaredHellingerAnomaly is raw squared Hellinger k-NN; not Hellinger or squared-chord",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for SquaredHellingerAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Last-11 eleventh-difference residual anomaly.
///
/// Score is \(|\Delta^{11} x|/\overline{|x|}\) on the trailing eleven
/// first-coordinates (binomial \(1,-11,55,-165,330,-462,462,-330,165,-55,11,-1\)).
/// Distinct from [`WindowLag10`] (tenth difference) and [`WindowLag9`].
#[derive(Clone, Debug)]
pub struct WindowLag11 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag11 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag11 {
    /// Empty windowed lag-11 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 11 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(11);
        let win = &xs[start..];
        if win.len() < 11 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        (y - 11.0 * p1 + 55.0 * p2 - 165.0 * p3 + 330.0 * p4 - 462.0 * p5 + 462.0 * p6
            - 330.0 * p7 + 165.0 * p8 - 55.0 * p9 + 11.0 * p10 - p11)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag11 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 11;
        q.warmup = self.rows.len() < 11;
        q.explanation = format!("window-lag11 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eleventh-difference anomaly",
                "WindowLag11 uses last-11 eleventh difference; not WindowLag10 or WindowLag9",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag11 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn max_symmetric_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut sx = 0.0_f64;
    let mut sy = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d = u - v;
        let d2 = d * d;
        sx += d2 / u;
        sy += d2 / v;
    }
    sx.max(sy).max(0.0)
}

/// Max-symmetric \(\chi^2\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw max-symmetric \(\chi^2\)
/// distances \(\max(\sum(x-y)^2/x,\sum(x-y)^2/y)\) without \(\ell_1\)
/// normalisation. Distinct from [`NeymanChiAnomaly`] (denominator \(x\) only)
/// and [`SymmetricChiAnomaly`].
#[derive(Clone, Debug)]
pub struct MaxSymmetricChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for MaxSymmetricChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl MaxSymmetricChiAnomaly {
    /// Empty max-symmetric \(\chi^2\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| max_symmetric_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for MaxSymmetricChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("max-symmetric-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "max-symmetric chi-squared k-NN anomaly",
                "MaxSymmetricChiAnomaly is raw max-symmetric chi-squared k-NN; not Neyman-chi or symmetric-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for MaxSymmetricChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Last-12 twelfth-difference residual anomaly.
///
/// Score is \(|\Delta^{12} x|/\overline{|x|}\) on the trailing twelve
/// first-coordinates (binomial \(1,-12,66,-220,495,-792,924,-792,495,-220,66,-12,1\)).
/// Distinct from [`WindowLag11`] (eleventh difference) and [`WindowLag10`].
#[derive(Clone, Debug)]
pub struct WindowLag12 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag12 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag12 {
    /// Empty windowed lag-12 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 12 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(12);
        let win = &xs[start..];
        if win.len() < 12 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        (y - 12.0 * p1 + 66.0 * p2 - 220.0 * p3 + 495.0 * p4 - 792.0 * p5 + 924.0 * p6
            - 792.0 * p7 + 495.0 * p8 - 220.0 * p9 + 66.0 * p10 - 12.0 * p11 + p12)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag12 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 12;
        q.warmup = self.rows.len() < 12;
        q.explanation = format!("window-lag12 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twelfth-difference anomaly",
                "WindowLag12 uses last-12 twelfth difference; not WindowLag11 or WindowLag10",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag12 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn min_symmetric_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut sx = 0.0_f64;
    let mut sy = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let d = u - v;
        let d2 = d * d;
        sx += d2 / u;
        sy += d2 / v;
    }
    sx.min(sy).max(0.0)
}

/// Min-symmetric \(\chi^2\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw min-symmetric \(\chi^2\)
/// distances \(\min(\sum(x-y)^2/x,\sum(x-y)^2/y)\) without \(\ell_1\)
/// normalisation. Distinct from [`MaxSymmetricChiAnomaly`] and
/// [`NeymanChiAnomaly`].
#[derive(Clone, Debug)]
pub struct MinSymmetricChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for MinSymmetricChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl MinSymmetricChiAnomaly {
    /// Empty min-symmetric \(\chi^2\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| min_symmetric_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for MinSymmetricChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("min-symmetric-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "min-symmetric chi-squared k-NN anomaly",
                "MinSymmetricChiAnomaly is raw min-symmetric chi-squared k-NN; not max-symmetric or Neyman-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for MinSymmetricChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Last-13 thirteenth-difference residual anomaly.
///
/// Score is \(|\Delta^{13} x|/\overline{|x|}\) on the trailing thirteen
/// first-coordinates (binomial \(1,-13,78,-286,715,-1287,1716,-1716,1287,-715,286,-78,13,-1\)).
/// Distinct from [`WindowLag12`] (twelfth difference) and [`WindowLag11`].
#[derive(Clone, Debug)]
pub struct WindowLag13 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag13 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag13 {
    /// Empty windowed lag-13 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 13 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(13);
        let win = &xs[start..];
        if win.len() < 13 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        (y - 13.0 * p1 + 78.0 * p2 - 286.0 * p3 + 715.0 * p4 - 1287.0 * p5 + 1716.0 * p6
            - 1716.0 * p7 + 1287.0 * p8 - 715.0 * p9 + 286.0 * p10 - 78.0 * p11 + 13.0 * p12
            - p13)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag13 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 13;
        q.warmup = self.rows.len() < 13;
        q.explanation = format!("window-lag13 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirteenth-difference anomaly",
                "WindowLag13 uses last-13 thirteenth difference; not WindowLag12 or WindowLag11",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag13 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn jensen_difference_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let m = 0.5 * (u + v);
        s += m * m.ln() - 0.5 * (u * u.ln() + v * v.ln());
    }
    s.max(0.0)
}

/// Jensen-difference \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Jensen differences
/// \(\sum[\tfrac{x+y}{2}\ln\tfrac{x+y}{2}-\tfrac12(x\ln x+y\ln y)]\) without
/// \(\ell_1\) normalisation or the outer Jensen–Shannon square root.
/// Distinct from [`JensenShannonAnomaly`].
#[derive(Clone, Debug)]
pub struct JensenDifferenceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for JensenDifferenceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl JensenDifferenceAnomaly {
    /// Empty Jensen-difference \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| jensen_difference_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for JensenDifferenceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("jensen-difference-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Jensen-difference k-NN anomaly",
                "JensenDifferenceAnomaly is raw Jensen difference k-NN; not Jensen-Shannon",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for JensenDifferenceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{14} x|/\overline{|x|}\) on the trailing fourteen
/// first-coordinates (binomial \(1,-14,91,-364,1001,-2002,3003,-3432,3003,-2002,1001,-364,91,-14,1\)).
/// Distinct from [`WindowLag13`] (thirteenth difference) and [`WindowLag12`].
#[derive(Clone, Debug)]
pub struct WindowLag14 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag14 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag14 {
    /// Empty windowed lag-14 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 14 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(14);
        let win = &xs[start..];
        if win.len() < 14 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        (y - 14.0 * p1 + 91.0 * p2 - 364.0 * p3 + 1001.0 * p4 - 2002.0 * p5 + 3003.0 * p6
            - 3432.0 * p7 + 3003.0 * p8 - 2002.0 * p9 + 1001.0 * p10 - 364.0 * p11 + 91.0 * p12
            - 14.0 * p13 + p14)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag14 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 14;
        q.warmup = self.rows.len() < 14;
        q.explanation = format!("window-lag14 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fourteenth-difference anomaly",
                "WindowLag14 uses last-14 fourteenth difference; not WindowLag13 or WindowLag12",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag14 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn itakura_saito_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        let r = u / v;
        s += r - r.ln() - 1.0;
    }
    s.max(0.0)
}

/// Itakura–Saito \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Itakura–Saito divergences
/// \(\sum(x/y-\ln(x/y)-1)\) without \(\ell_1\) normalisation.
/// Distinct from [`JeffreysAnomaly`] (symmetrised KL after \(\ell_1\)).
#[derive(Clone, Debug)]
pub struct ItakuraSaitoAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for ItakuraSaitoAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl ItakuraSaitoAnomaly {
    /// Empty Itakura–Saito \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| itakura_saito_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for ItakuraSaitoAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("itakura-saito-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Itakura-Saito k-NN anomaly",
                "ItakuraSaitoAnomaly is raw Itakura-Saito k-NN; not Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for ItakuraSaitoAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{15} x|/\overline{|x|}\) on the trailing fifteen
/// first-coordinates (binomial \(1,-15,105,-455,1365,-3003,5005,-6435,6435,-5005,3003,-1365,455,-105,15,-1\)).
/// Distinct from [`WindowLag14`] (fourteenth difference) and [`WindowLag13`].
#[derive(Clone, Debug)]
pub struct WindowLag15 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag15 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag15 {
    /// Empty windowed lag-15 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 15 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(15);
        let win = &xs[start..];
        if win.len() < 15 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        (y - 15.0 * p1 + 105.0 * p2 - 455.0 * p3 + 1365.0 * p4 - 3003.0 * p5 + 5005.0 * p6
            - 6435.0 * p7 + 6435.0 * p8 - 5005.0 * p9 + 3003.0 * p10 - 1365.0 * p11 + 455.0 * p12
            - 105.0 * p13 + 15.0 * p14 - p15)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag15 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 15;
        q.warmup = self.rows.len() < 15;
        q.explanation = format!("window-lag15 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifteenth-difference anomaly",
                "WindowLag15 uses last-15 fifteenth difference; not WindowLag14 or WindowLag13",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag15 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn triangular_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs();
        let v = b[j].abs();
        let den = (u + v).max(1e-18);
        let d = u - v;
        s += d * d / den;
    }
    s.max(0.0)
}

/// Triangular-discrimination \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw triangular discriminations
/// \(\sum(x-y)^2/(x+y)\) without \(\ell_1\) normalisation.
/// Distinct from [`PearsonChiAnomaly`] (denominator \(x\)) and [`SymmetricChiAnomaly`].
#[derive(Clone, Debug)]
pub struct TriangularAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for TriangularAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl TriangularAnomaly {
    /// Empty triangular-discrimination \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| triangular_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for TriangularAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("triangular-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "triangular-discrimination k-NN anomaly",
                "TriangularAnomaly is raw triangular discrimination k-NN; not Pearson-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for TriangularAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{16} x|/\overline{|x|}\) on the trailing sixteen
/// first-coordinates (binomial \(1,-16,120,-560,1820,-4368,8008,-11440,12870,-11440,8008,-4368,1820,-560,120,-16,1\)).
/// Distinct from [`WindowLag15`] (fifteenth difference) and [`WindowLag14`].
#[derive(Clone, Debug)]
pub struct WindowLag16 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag16 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag16 {
    /// Empty windowed lag-16 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 16 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(16);
        let win = &xs[start..];
        if win.len() < 16 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        (y - 16.0 * p1 + 120.0 * p2 - 560.0 * p3 + 1820.0 * p4 - 4368.0 * p5 + 8008.0 * p6
            - 11440.0 * p7 + 12870.0 * p8 - 11440.0 * p9 + 8008.0 * p10 - 4368.0 * p11
            + 1820.0 * p12 - 560.0 * p13 + 120.0 * p14 - 16.0 * p15 + p16)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag16 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 16;
        q.warmup = self.rows.len() < 16;
        q.explanation = format!("window-lag16 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixteenth-difference anomaly",
                "WindowLag16 uses last-16 sixteenth difference; not WindowLag15 or WindowLag14",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag16 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn idivergence_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18);
        let v = b[j].abs().max(1e-18);
        s += u * (u / v).ln() - u + v;
    }
    s.max(0.0)
}

/// I-divergence \(k\)-NN anomaly (generalised Kullback–Leibler).
///
/// Score is the mean of the \(k\) smallest raw I-divergences
/// \(\sum(x\ln(x/y)-x+y)\) without \(\ell_1\) normalisation.
/// Distinct from [`ItakuraSaitoAnomaly`] and [`JeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct IDivergenceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for IDivergenceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl IDivergenceAnomaly {
    /// Empty I-divergence \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| idivergence_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for IDivergenceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("i-divergence-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "I-divergence k-NN anomaly",
                "IDivergenceAnomaly is raw generalised KL k-NN; not Itakura-Saito",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for IDivergenceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{17} x|/\overline{|x|}\) on the trailing seventeen
/// first-coordinates (binomial \(1,-17,136,-680,2380,-6188,12376,-19448,24310,-24310,19448,-12376,6188,-2380,680,-136,17,-1\)).
/// Distinct from [`WindowLag16`] (sixteenth difference) and [`WindowLag15`].
#[derive(Clone, Debug)]
pub struct WindowLag17 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag17 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag17 {
    /// Empty windowed lag-17 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 17 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(17);
        let win = &xs[start..];
        if win.len() < 17 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        (y - 17.0 * p1 + 136.0 * p2 - 680.0 * p3 + 2380.0 * p4 - 6188.0 * p5 + 12376.0 * p6
            - 19448.0 * p7 + 24310.0 * p8 - 24310.0 * p9 + 19448.0 * p10 - 12376.0 * p11
            + 6188.0 * p12 - 2380.0 * p13 + 680.0 * p14 - 136.0 * p15 + 17.0 * p16 - p17)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag17 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 17;
        q.warmup = self.rows.len() < 17;
        q.explanation = format!("window-lag17 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventeenth-difference anomaly",
                "WindowLag17 uses last-17 seventeenth difference; not WindowLag16 or WindowLag15",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag17 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_euclidean_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        let d = u - v;
        s += d * d;
    }
    s.max(0.0)
}

/// Log-Euclidean \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Euclidean squares
/// \(\sum(\ln|x|-\ln|y|)^2\) without \(\ell_1\) normalisation.
/// Distinct from [`ManhattanAnomaly`] and [`IDivergenceAnomaly`].
#[derive(Clone, Debug)]
pub struct LogEuclideanAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogEuclideanAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogEuclideanAnomaly {
    /// Empty log-Euclidean \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_euclidean_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogEuclideanAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-euclidean-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Euclidean k-NN anomaly",
                "LogEuclideanAnomaly is raw log-Euclidean k-NN; not Manhattan",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogEuclideanAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{18} x|/\overline{|x|}\) on the trailing eighteen
/// first-coordinates (binomial \(1,-18,153,-816,3060,-8568,18564,-31824,43758,-48620,43758,-31824,18564,-8568,3060,-816,153,-18,1\)).
/// Distinct from [`WindowLag17`] (seventeenth difference) and [`WindowLag16`].
#[derive(Clone, Debug)]
pub struct WindowLag18 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag18 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag18 {
    /// Empty windowed lag-18 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 18 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(18);
        let win = &xs[start..];
        if win.len() < 18 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        (y - 18.0 * p1 + 153.0 * p2 - 816.0 * p3 + 3060.0 * p4 - 8568.0 * p5 + 18564.0 * p6
            - 31824.0 * p7 + 43758.0 * p8 - 48620.0 * p9 + 43758.0 * p10 - 31824.0 * p11
            + 18564.0 * p12 - 8568.0 * p13 + 3060.0 * p14 - 816.0 * p15 + 153.0 * p16
            - 18.0 * p17 + p18)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag18 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 18;
        q.warmup = self.rows.len() < 18;
        q.explanation = format!("window-lag18 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighteenth-difference anomaly",
                "WindowLag18 uses last-18 eighteenth difference; not WindowLag17 or WindowLag16",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag18 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn bray_curtis_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs();
        let v = b[j].abs();
        num += (u - v).abs();
        den += u + v;
    }
    if den < 1e-18 {
        return 0.0;
    }
    (num / den).max(0.0)
}

/// Bray–Curtis \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw Bray–Curtis dissimilarities
/// \(\sum|x-y|/\sum(x+y)\) without \(\ell_1\) normalisation.
/// Distinct from [`ManhattanAnomaly`] (no denominator) and Whittaker after \(\ell_1\).
#[derive(Clone, Debug)]
pub struct BrayCurtisAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for BrayCurtisAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl BrayCurtisAnomaly {
    /// Empty Bray–Curtis \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| bray_curtis_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for BrayCurtisAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("bray-curtis-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "Bray-Curtis k-NN anomaly",
                "BrayCurtisAnomaly is raw Bray-Curtis k-NN; not Manhattan or Whittaker-L1",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for BrayCurtisAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{19} x|/\overline{|x|}\) on the trailing nineteen
/// first-coordinates (binomial \(1,-19,171,-969,3876,-11628,27132,-50388,75582,-92378,92378,-75582,50388,-27132,11628,-3876,969,-171,19,-1\)).
/// Distinct from [`WindowLag18`] (eighteenth difference) and [`WindowLag17`].
#[derive(Clone, Debug)]
pub struct WindowLag19 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag19 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag19 {
    /// Empty windowed lag-19 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 19 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(19);
        let win = &xs[start..];
        if win.len() < 19 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        (y - 19.0 * p1 + 171.0 * p2 - 969.0 * p3 + 3876.0 * p4 - 11628.0 * p5 + 27132.0 * p6
            - 50388.0 * p7 + 75582.0 * p8 - 92378.0 * p9 + 92378.0 * p10 - 75582.0 * p11
            + 50388.0 * p12 - 27132.0 * p13 + 11628.0 * p14 - 3876.0 * p15 + 969.0 * p16
            - 171.0 * p17 + 19.0 * p18 - p19)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag19 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 19;
        q.warmup = self.rows.len() < 19;
        q.explanation = format!("window-lag19 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed nineteenth-difference anomaly",
                "WindowLag19 uses last-19 nineteenth difference; not WindowLag18 or WindowLag17",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag19 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_chebyshev_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut m = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        let d = (u - v).abs();
        if d > m {
            m = d;
        }
    }
    m
}

/// Log-Chebyshev \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Chebyshev distances
/// \(\max|\ln|x|-\ln|y||\) without \(\ell_1\) normalisation.
/// Distinct from [`LinfAnomaly`] (raw max gap) and [`LogEuclideanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogChebyshevAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogChebyshevAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogChebyshevAnomaly {
    /// Empty log-Chebyshev \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_chebyshev_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogChebyshevAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-chebyshev-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Chebyshev k-NN anomaly",
                "LogChebyshevAnomaly is raw log-Chebyshev k-NN; not Linf or log-Euclidean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogChebyshevAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{20} x|/\overline{|x|}\) on the trailing twenty
/// first-coordinates (binomial \(1,-20,190,-1140,4845,-15504,38760,-77520,125970,-167960,184756,-167960,125970,-77520,38760,-15504,4845,-1140,190,-20,1\)).
/// Distinct from [`WindowLag19`] (nineteenth difference) and [`WindowLag18`].
#[derive(Clone, Debug)]
pub struct WindowLag20 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag20 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag20 {
    /// Empty windowed lag-20 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 20 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(20);
        let win = &xs[start..];
        if win.len() < 20 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        (y - 20.0 * p1 + 190.0 * p2 - 1140.0 * p3 + 4845.0 * p4 - 15504.0 * p5 + 38760.0 * p6
            - 77520.0 * p7 + 125970.0 * p8 - 167960.0 * p9 + 184756.0 * p10 - 167960.0 * p11
            + 125970.0 * p12 - 77520.0 * p13 + 38760.0 * p14 - 15504.0 * p15 + 4845.0 * p16
            - 1140.0 * p17 + 190.0 * p18 - 20.0 * p19 + p20)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag20 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 20;
        q.warmup = self.rows.len() < 20;
        q.explanation = format!("window-lag20 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twentieth-difference anomaly",
                "WindowLag20 uses last-20 twentieth difference; not WindowLag19 or WindowLag18",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag20 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_manhattan_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        s += (u - v).abs();
    }
    s
}

/// Log-Manhattan \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Manhattan distances
/// \(\sum|\ln|x|-\ln|y||\) without \(\ell_1\) normalisation.
/// Distinct from [`LogEuclideanAnomaly`] (squared log gaps) and [`LogChebyshevAnomaly`].
#[derive(Clone, Debug)]
pub struct LogManhattanAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogManhattanAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogManhattanAnomaly {
    /// Empty log-Manhattan \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_manhattan_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogManhattanAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-manhattan-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Manhattan k-NN anomaly",
                "LogManhattanAnomaly is raw log-Manhattan k-NN; not log-Euclidean or log-Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogManhattanAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{21} x|/\overline{|x|}\) on the trailing twenty-one
/// first-coordinates (binomial \(1,-21,210,-1330,5985,-20349,54264,-116280,203490,-293930,352716,-352716,293930,-203490,116280,-54264,20349,-5985,1330,-210,21,-1\)).
/// Distinct from [`WindowLag20`] (twentieth difference) and [`WindowLag19`].
#[derive(Clone, Debug)]
pub struct WindowLag21 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag21 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag21 {
    /// Empty windowed lag-21 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 21 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(21);
        let win = &xs[start..];
        if win.len() < 21 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        (y - 21.0 * p1 + 210.0 * p2 - 1330.0 * p3 + 5985.0 * p4 - 20349.0 * p5 + 54264.0 * p6
            - 116280.0 * p7 + 203490.0 * p8 - 293930.0 * p9 + 352716.0 * p10 - 352716.0 * p11
            + 293930.0 * p12 - 203490.0 * p13 + 116280.0 * p14 - 54264.0 * p15 + 20349.0 * p16
            - 5985.0 * p17 + 1330.0 * p18 - 210.0 * p19 + 21.0 * p20 - p21)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag21 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 21;
        q.warmup = self.rows.len() < 21;
        q.explanation = format!("window-lag21 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-first-difference anomaly",
                "WindowLag21 uses last-21 twenty-first difference; not WindowLag20 or WindowLag19",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag21 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_canberra_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        s += (u - v).abs() / (u.abs() + v.abs()).max(1e-18);
    }
    s
}

/// Log-Canberra \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Canberra distances
/// \(\sum|\ln|x|-\ln|y||/(|\ln|x||+|\ln|y||)\) without \(\ell_1\) normalisation.
/// Distinct from [`CanberraAnomaly`] (raw levels) and [`LogManhattanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogCanberraAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogCanberraAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogCanberraAnomaly {
    /// Empty log-Canberra \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_canberra_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogCanberraAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-canberra-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Canberra k-NN anomaly",
                "LogCanberraAnomaly is raw log-Canberra k-NN; not Canberra or log-Manhattan",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogCanberraAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{22} x|/\overline{|x|}\) on the trailing twenty-two
/// first-coordinates (binomial \(1,-22,231,-1540,7315,-26334,74613,-170544,319770,-497420,646646,-705432,646646,-497420,319770,-170544,74613,-26334,7315,-1540,231,-22,1\)).
/// Distinct from [`WindowLag21`] (twenty-first difference) and [`WindowLag20`].
#[derive(Clone, Debug)]
pub struct WindowLag22 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag22 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag22 {
    /// Empty windowed lag-22 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 22 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(22);
        let win = &xs[start..];
        if win.len() < 22 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        (y - 22.0 * p1 + 231.0 * p2 - 1540.0 * p3 + 7315.0 * p4 - 26334.0 * p5 + 74613.0 * p6
            - 170544.0 * p7 + 319770.0 * p8 - 497420.0 * p9 + 646646.0 * p10 - 705432.0 * p11
            + 646646.0 * p12 - 497420.0 * p13 + 319770.0 * p14 - 170544.0 * p15 + 74613.0 * p16
            - 26334.0 * p17 + 7315.0 * p18 - 1540.0 * p19 + 231.0 * p20 - 22.0 * p21 + p22)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag22 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 22;
        q.warmup = self.rows.len() < 22;
        q.explanation = format!("window-lag22 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-second-difference anomaly",
                "WindowLag22 uses last-22 twenty-second difference; not WindowLag21 or WindowLag20",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag22 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_lorentzian_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        s += (1.0 + (u - v).abs()).ln();
    }
    s
}

/// Log-Lorentzian \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Lorentzian distances
/// \(\sum\ln(1+|\ln|x|-\ln|y||)\) without \(\ell_1\) normalisation.
/// Distinct from [`LorentzianAnomaly`] (raw gaps) and [`LogManhattanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogLorentzianAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogLorentzianAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogLorentzianAnomaly {
    /// Empty log-Lorentzian \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_lorentzian_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogLorentzianAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-lorentzian-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Lorentzian k-NN anomaly",
                "LogLorentzianAnomaly is raw log-Lorentzian k-NN; not Lorentzian or log-Manhattan",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogLorentzianAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{23} x|/\overline{|x|}\) on the trailing twenty-three
/// first-coordinates (binomial \(1,-23,253,-1771,8855,-33649,100947,-245157,490314,-817190,1144066,-1352078,1352078,-1144066,817190,-490314,245157,-100947,33649,-8855,1771,-253,23,-1\)).
/// Distinct from [`WindowLag22`] (twenty-second difference) and [`WindowLag21`].
#[derive(Clone, Debug)]
pub struct WindowLag23 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag23 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag23 {
    /// Empty windowed lag-23 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 23 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(23);
        let win = &xs[start..];
        if win.len() < 23 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        (y - 23.0 * p1 + 253.0 * p2 - 1771.0 * p3 + 8855.0 * p4 - 33649.0 * p5 + 100947.0 * p6 - 245157.0 * p7
            + 490314.0 * p8 - 817190.0 * p9 + 1144066.0 * p10 - 1352078.0 * p11 + 1352078.0 * p12
            - 1144066.0 * p13 + 817190.0 * p14 - 490314.0 * p15 + 245157.0 * p16 - 100947.0 * p17
            + 33649.0 * p18 - 8855.0 * p19 + 1771.0 * p20 - 253.0 * p21 + 23.0 * p22 - p23)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag23 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 23;
        q.warmup = self.rows.len() < 23;
        q.explanation = format!("window-lag23 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-third-difference anomaly",
                "WindowLag23 uses last-23 twenty-third difference; not WindowLag22 or WindowLag21",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag23 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_wave_hedges_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        s += (u - v).abs() / u.abs().max(v.abs()).max(1e-18);
    }
    s
}

/// Log-Wave-Hedges \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Wave-Hedges distances
/// \(\sum|\ln|x|-\ln|y||/\max(|\ln|x||,|\ln|y||)\) without \(\ell_1\) normalisation.
/// Distinct from [`WaveHedgesAnomaly`] (raw levels) and [`LogCanberraAnomaly`].
#[derive(Clone, Debug)]
pub struct LogWaveHedgesAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogWaveHedgesAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogWaveHedgesAnomaly {
    /// Empty log-Wave-Hedges \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_wave_hedges_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogWaveHedgesAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-wave-hedges-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Wave-Hedges k-NN anomaly",
                "LogWaveHedgesAnomaly is raw log-Wave-Hedges k-NN; not Wave-Hedges or log-Canberra",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogWaveHedgesAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{24} x|/\overline{|x|}\) on the trailing twenty-four
/// first-coordinates (binomial \(1,-24,276,-2024,10626,-42504,134596,-346104,735471,-1307504,1961256,-2496144,2704156,-2496144,1961256,-1307504,735471,-346104,134596,-42504,10626,-2024,276,-24,1\)).
/// Distinct from [`WindowLag23`] (twenty-third difference) and [`WindowLag22`].
#[derive(Clone, Debug)]
pub struct WindowLag24 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag24 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag24 {
    /// Empty windowed lag-24 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 24 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(24);
        let win = &xs[start..];
        if win.len() < 24 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        (y - 24.0 * p1 + 276.0 * p2 - 2024.0 * p3 + 10626.0 * p4 - 42504.0 * p5 + 134596.0 * p6 - 346104.0 * p7 + 735471.0 * p8 - 1307504.0 * p9 + 1961256.0 * p10 - 2496144.0 * p11 + 2704156.0 * p12 - 2496144.0 * p13 + 1961256.0 * p14 - 1307504.0 * p15 + 735471.0 * p16 - 346104.0 * p17 + 134596.0 * p18 - 42504.0 * p19 + 10626.0 * p20 - 2024.0 * p21 + 276.0 * p22 - 24.0 * p23 + p24)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag24 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 24;
        q.warmup = self.rows.len() < 24;
        q.explanation = format!("window-lag24 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-fourth-difference anomaly",
                "WindowLag24 uses last-24 twenty-fourth difference; not WindowLag23 or WindowLag22",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag24 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_hassanat_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        let lo = u.min(v);
        let hi = u.max(v);
        if hi > 1e-18 {
            s += 1.0 - (1.0 + lo) / (1.0 + hi);
        }
    }
    s
}

/// Log-Hassanat \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Hassanat distances
/// \(\sum(1-(1+\min|\ln|x||,|\ln|y||)/(1+\max|\ln|x||,|\ln|y||))\) without \(\ell_1\)
/// normalisation. Distinct from [`HassanatAnomaly`] (raw levels) and [`LogCanberraAnomaly`].
#[derive(Clone, Debug)]
pub struct LogHassanatAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogHassanatAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogHassanatAnomaly {
    /// Empty log-Hassanat \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_hassanat_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogHassanatAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-hassanat-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Hassanat k-NN anomaly",
                "LogHassanatAnomaly is raw log-Hassanat k-NN; not Hassanat or log-Canberra",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogHassanatAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{25} x|/\overline{|x|}\) on the trailing twenty-five
/// first-coordinates (binomial \(1,-25,300,-2300,12650,-53130,177100,-480700,1081575,-2042975,3268760,-4457400,5200300,-5200300,4457400,-3268760,2042975,-1081575,480700,-177100,53130,-12650,2300,-300,25,-1\)).
/// Distinct from [`WindowLag24`] (twenty-fourth difference) and [`WindowLag23`].
#[derive(Clone, Debug)]
pub struct WindowLag25 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag25 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag25 {
    /// Empty windowed lag-25 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 25 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(25);
        let win = &xs[start..];
        if win.len() < 25 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        (y - 25.0 * p1 + 300.0 * p2 - 2300.0 * p3 + 12650.0 * p4 - 53130.0 * p5 + 177100.0 * p6 - 480700.0 * p7 + 1081575.0 * p8 - 2042975.0 * p9 + 3268760.0 * p10 - 4457400.0 * p11 + 5200300.0 * p12 - 5200300.0 * p13 + 4457400.0 * p14 - 3268760.0 * p15 + 2042975.0 * p16 - 1081575.0 * p17 + 480700.0 * p18 - 177100.0 * p19 + 53130.0 * p20 - 12650.0 * p21 + 2300.0 * p22 - 300.0 * p23 + 25.0 * p24 - p25)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag25 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 25;
        q.warmup = self.rows.len() < 25;
        q.explanation = format!("window-lag25 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-fifth-difference anomaly",
                "WindowLag25 uses last-25 twenty-fifth difference; not WindowLag24 or WindowLag23",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag25 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_dice_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        num += u * v;
        na += u * u;
        nb += v * v;
    }
    let den = na + nb;
    if den < 1e-18 {
        return 0.0;
    }
    (1.0 - 2.0 * num / den).max(0.0)
}

/// Log-Dice \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Dice distances
/// \(1-2\langle\ln|x|,\ln|y|\rangle/(\|\ln|x|\|^2+\|\ln|y|\|^2)\) without \(\ell_1\)
/// normalisation. Distinct from [`DiceAnomaly`] (raw levels) and [`LogEuclideanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogDiceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogDiceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogDiceAnomaly {
    /// Empty log-Dice \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_dice_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogDiceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-dice-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Dice k-NN anomaly",
                "LogDiceAnomaly is raw log-Dice k-NN; not Dice or log-Euclidean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogDiceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{26} x|/\overline{|x|}\) on the trailing twenty-six
/// first-coordinates (binomial \(1,-26,325,-2600,14950,-65780,230230,-657800,1562275,-3124550,5311735,-7726160,9657700,-10400600,9657700,-7726160,5311735,-3124550,1562275,-657800,230230,-65780,14950,-2600,325,-26,1\)).
/// Distinct from [`WindowLag25`] (twenty-fifth difference) and [`WindowLag24`].
#[derive(Clone, Debug)]
pub struct WindowLag26 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag26 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag26 {
    /// Empty windowed lag-26 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 26 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(26);
        let win = &xs[start..];
        if win.len() < 26 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        (y - 26.0 * p1 + 325.0 * p2 - 2600.0 * p3 + 14950.0 * p4 - 65780.0 * p5 + 230230.0 * p6 - 657800.0 * p7 + 1562275.0 * p8 - 3124550.0 * p9 + 5311735.0 * p10 - 7726160.0 * p11 + 9657700.0 * p12 - 10400600.0 * p13 + 9657700.0 * p14 - 7726160.0 * p15 + 5311735.0 * p16 - 3124550.0 * p17 + 1562275.0 * p18 - 657800.0 * p19 + 230230.0 * p20 - 65780.0 * p21 + 14950.0 * p22 - 2600.0 * p23 + 325.0 * p24 - 26.0 * p25 + p26)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag26 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 26;
        q.warmup = self.rows.len() < 26;
        q.explanation = format!("window-lag26 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-sixth-difference anomaly",
                "WindowLag26 uses last-26 twenty-sixth difference; not WindowLag25 or WindowLag24",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag26 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_tanimoto_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln();
        let v = b[j].abs().max(1e-18).ln();
        num += u * v;
        na += u * u;
        nb += v * v;
    }
    let den = na + nb - num;
    if den.abs() < 1e-18 {
        return 0.0;
    }
    (1.0 - num / den).max(0.0)
}

/// Log-Tanimoto \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Tanimoto distances
/// \(1-\langle\ln|x|,\ln|y|\rangle/(\|\ln|x|\|^2+\|\ln|y|\|^2-\langle\ln|x|,\ln|y|\rangle)\)
/// without \(\ell_1\) normalisation. Distinct from [`TanimotoAnomaly`] (raw levels)
/// and [`LogDiceAnomaly`].
#[derive(Clone, Debug)]
pub struct LogTanimotoAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogTanimotoAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogTanimotoAnomaly {
    /// Empty log-Tanimoto \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_tanimoto_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogTanimotoAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-tanimoto-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Tanimoto k-NN anomaly",
                "LogTanimotoAnomaly is raw log-Tanimoto k-NN; not Tanimoto or log-Dice",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogTanimotoAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

/// Score is \(|\Delta^{27} x|/\overline{|x|}\) on the trailing twenty-seven
/// first-coordinates (binomial \(1,-27,351,-2925,17550,-80730,296010,-888030,2220075,-4686825,8436285,-13037895,17383860,-20058300,20058300,-17383860,13037895,-8436285,4686825,-2220075,888030,-296010,80730,-17550,2925,-351,27,-1\)).
/// Distinct from [`WindowLag26`] (twenty-sixth difference) and [`WindowLag25`].
#[derive(Clone, Debug)]
pub struct WindowLag27 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag27 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag27 {
    /// Empty windowed lag-27 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 27 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(27);
        let win = &xs[start..];
        if win.len() < 27 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        (y - 27.0 * p1 + 351.0 * p2 - 2925.0 * p3 + 17550.0 * p4 - 80730.0 * p5
            + 296010.0 * p6 - 888030.0 * p7 + 2220075.0 * p8 - 4686825.0 * p9
            + 8436285.0 * p10 - 13037895.0 * p11 + 17383860.0 * p12 - 20058300.0 * p13
            + 20058300.0 * p14 - 17383860.0 * p15 + 13037895.0 * p16 - 8436285.0 * p17
            + 4686825.0 * p18 - 2220075.0 * p19 + 888030.0 * p20 - 296010.0 * p21
            + 80730.0 * p22 - 17550.0 * p23 + 2925.0 * p24 - 351.0 * p25 + 27.0 * p26
            - p27)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag27 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 27;
        q.warmup = self.rows.len() < 27;
        q.explanation = format!("window-lag27 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-seventh-difference anomaly",
                "WindowLag27 uses last-27 twenty-seventh difference; not WindowLag26 or WindowLag25",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag27 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_ruzicka_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut nmin = 0.0_f64;
    let mut nmax = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        nmin += u.min(v);
        nmax += u.max(v);
    }
    if nmax < 1e-18 {
        return 0.0;
    }
    1.0 - nmin / nmax
}

/// Log-Ružička \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Ružička distances
/// \(1-\sum\min(|\ln|x||,|\ln|y||)/\sum\max(|\ln|x||,|\ln|y||)\)
/// without \(\ell_1\) normalisation. Distinct from [`RuzickaAnomaly`] (raw levels)
/// and [`LogTanimotoAnomaly`].
#[derive(Clone, Debug)]
pub struct LogRuzickaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogRuzickaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogRuzickaAnomaly {
    /// Empty log-Ružička \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_ruzicka_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogRuzickaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-ruzicka-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Ružička k-NN anomaly",
                "LogRuzickaAnomaly is raw log-Ružička k-NN; not Ružička or log-Tanimoto",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogRuzickaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{28} x|/\overline{|x|}\) on the trailing twenty-eight
/// first-coordinates (binomial \(1,-28,378,-3276,20475,-98280,376740,-1184040,3108105,-6906900,13123110,-21474180,30421755,-37442160,40116600,-37442160,30421755,-21474180,13123110,-6906900,3108105,-1184040,376740,-98280,20475,-3276,378,-28,1\)).
/// Distinct from [`WindowLag27`] (twenty-seventh difference) and [`WindowLag26`].
#[derive(Clone, Debug)]
pub struct WindowLag28 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag28 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag28 {
    /// Empty windowed lag-28 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 28 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(28);
        let win = &xs[start..];
        if win.len() < 28 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        (y - 28.0 * p1 + 378.0 * p2 - 3276.0 * p3 + 20475.0 * p4 - 98280.0 * p5
            + 376740.0 * p6 - 1184040.0 * p7 + 3108105.0 * p8 - 6906900.0 * p9
            + 13123110.0 * p10 - 21474180.0 * p11 + 30421755.0 * p12 - 37442160.0 * p13
            + 40116600.0 * p14 - 37442160.0 * p15 + 30421755.0 * p16 - 21474180.0 * p17
            + 13123110.0 * p18 - 6906900.0 * p19 + 3108105.0 * p20 - 1184040.0 * p21
            + 376740.0 * p22 - 98280.0 * p23 + 20475.0 * p24 - 3276.0 * p25 + 378.0 * p26
            - 28.0 * p27 + p28)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag28 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 28;
        q.warmup = self.rows.len() < 28;
        q.explanation = format!("window-lag28 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-eighth-difference anomaly",
                "WindowLag28 uses last-28 twenty-eighth difference; not WindowLag27 or WindowLag26",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag28 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_kulczynski_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        num += (u - v).abs();
        den += u.min(v);
    }
    if den < 1e-18 {
        return 0.0;
    }
    num / den
}

/// Log-Kulczynski \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Kulczynski distances
/// \(\sum||\ln|x||-\ln|y|||/\sum\min(|\ln|x||,|\ln|y||)\)
/// without \(\ell_1\) normalisation. Distinct from [`KulczynskiAnomaly`] (raw levels)
/// and [`LogRuzickaAnomaly`].
#[derive(Clone, Debug)]
pub struct LogKulczynskiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogKulczynskiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogKulczynskiAnomaly {
    /// Empty log-Kulczynski \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_kulczynski_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogKulczynskiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-kulczynski-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Kulczynski k-NN anomaly",
                "LogKulczynskiAnomaly is raw log-Kulczynski k-NN; not Kulczynski or log-Ružička",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogKulczynskiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{29} x|/\overline{|x|}\) on the trailing twenty-nine
/// first-coordinates (binomial \(1,-29,406,-3654,23751,-118755,475020,-1560780,4292145,-10015005,20030010,-34597290,51895935,-67863915,77558760,-77558760,67863915,-51895935,34597290,-20030010,10015005,-4292145,1560780,-475020,118755,-23751,3654,-406,29,-1\)).
/// Distinct from [`WindowLag28`] (twenty-eighth difference) and [`WindowLag27`].
#[derive(Clone, Debug)]
pub struct WindowLag29 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag29 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag29 {
    /// Empty windowed lag-29 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 29 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(29);
        let win = &xs[start..];
        if win.len() < 29 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        (y - 29.0 * p1 + 406.0 * p2 - 3654.0 * p3 + 23751.0 * p4 - 118755.0 * p5
            + 475020.0 * p6 - 1560780.0 * p7 + 4292145.0 * p8 - 10015005.0 * p9
            + 20030010.0 * p10 - 34597290.0 * p11 + 51895935.0 * p12 - 67863915.0 * p13
            + 77558760.0 * p14 - 77558760.0 * p15 + 67863915.0 * p16 - 51895935.0 * p17
            + 34597290.0 * p18 - 20030010.0 * p19 + 10015005.0 * p20 - 4292145.0 * p21
            + 1560780.0 * p22 - 475020.0 * p23 + 118755.0 * p24 - 23751.0 * p25 + 3654.0 * p26
            - 406.0 * p27 + 29.0 * p28 - p29)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag29 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 29;
        q.warmup = self.rows.len() < 29;
        q.explanation = format!("window-lag29 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed twenty-ninth-difference anomaly",
                "WindowLag29 uses last-29 twenty-ninth difference; not WindowLag28 or WindowLag27",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag29 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_clark_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        let den = u + v;
        if den > 1e-18 {
            let r = (u - v) / den;
            s += r * r;
        }
    }
    s.sqrt()
}

/// Log-Clark \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Clark distances
/// \(\sqrt{\sum((|\ln|x||-\ln|y||)/(|\ln|x||+|\ln|y||))^2}\)
/// without \(\ell_1\) normalisation. Distinct from [`ClarkAnomaly`] (raw levels)
/// and [`LogKulczynskiAnomaly`].
#[derive(Clone, Debug)]
pub struct LogClarkAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogClarkAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogClarkAnomaly {
    /// Empty log-Clark \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_clark_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogClarkAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-clark-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Clark k-NN anomaly",
                "LogClarkAnomaly is raw log-Clark k-NN; not Clark or log-Kulczynski",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogClarkAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{30} x|/\overline{|x|}\) on the trailing thirty
/// first-coordinates (binomial \(1,-30,435,-4060,27405,-142506,593775,-2035800,5852925,-14307150,30045015,-54627300,86493225,-119759850,145422675,-155117520,145422675,-119759850,86493225,-54627300,30045015,-14307150,5852925,-2035800,593775,-142506,27405,-4060,435,-30,1\)).
/// Distinct from [`WindowLag29`] (twenty-ninth difference) and [`WindowLag28`].
#[derive(Clone, Debug)]
pub struct WindowLag30 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag30 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag30 {
    /// Empty windowed lag-30 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 30 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(30);
        let win = &xs[start..];
        if win.len() < 30 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        (y - 30.0 * p1 + 435.0 * p2 - 4060.0 * p3 + 27405.0 * p4 - 142506.0 * p5
            + 593775.0 * p6 - 2035800.0 * p7 + 5852925.0 * p8 - 14307150.0 * p9
            + 30045015.0 * p10 - 54627300.0 * p11 + 86493225.0 * p12 - 119759850.0 * p13
            + 145422675.0 * p14 - 155117520.0 * p15 + 145422675.0 * p16 - 119759850.0 * p17
            + 86493225.0 * p18 - 54627300.0 * p19 + 30045015.0 * p20 - 14307150.0 * p21
            + 5852925.0 * p22 - 2035800.0 * p23 + 593775.0 * p24 - 142506.0 * p25 + 27405.0 * p26
            - 4060.0 * p27 + 435.0 * p28 - 30.0 * p29 + p30)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag30 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 30;
        q.warmup = self.rows.len() < 30;
        q.explanation = format!("window-lag30 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirtieth-difference anomaly",
                "WindowLag30 uses last-30 thirtieth difference; not WindowLag29 or WindowLag28",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag30 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_bray_curtis_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        num += (u - v).abs();
        den += u + v;
    }
    if den < 1e-18 {
        return 0.0;
    }
    (num / den).max(0.0)
}

/// Log-Bray–Curtis \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Bray–Curtis dissimilarities
/// \(\sum||\ln|x||-\ln|y|||/\sum(|\ln|x||+|\ln|y||)\)
/// without \(\ell_1\) normalisation. Distinct from [`BrayCurtisAnomaly`] (raw levels)
/// and [`LogClarkAnomaly`].
#[derive(Clone, Debug)]
pub struct LogBrayCurtisAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogBrayCurtisAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogBrayCurtisAnomaly {
    /// Empty log-Bray–Curtis \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_bray_curtis_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogBrayCurtisAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-bray-curtis-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Bray–Curtis k-NN anomaly",
                "LogBrayCurtisAnomaly is raw log-Bray–Curtis k-NN; not Bray–Curtis or log-Clark",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogBrayCurtisAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is \(|\Delta^{31} x|/\overline{|x|}\) on the trailing thirty-one
/// first-coordinates (binomial \(1,-31,465,-4495,31465,-169911,736281,-2629575,7888725,-20160075,44352165,-84672315,141120525,-206253075,265182525,-300540195,300540195,-265182525,206253075,-141120525,84672315,-44352165,20160075,-7888725,2629575,-736281,169911,-31465,4495,-465,31,-1\)).
/// Distinct from [`WindowLag30`] (thirtieth difference) and [`WindowLag29`].
#[derive(Clone, Debug)]
pub struct WindowLag31 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag31 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag31 {
    /// Empty windowed lag-31 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 31 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(31);
        let win = &xs[start..];
        if win.len() < 31 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        (y - 31.0 * p1 + 465.0 * p2 - 4495.0 * p3 + 31465.0 * p4 - 169911.0 * p5 + 736281.0 * p6 - 2629575.0 * p7 + 7888725.0 * p8 - 20160075.0 * p9 + 44352165.0 * p10 - 84672315.0 * p11 + 141120525.0 * p12 - 206253075.0 * p13 + 265182525.0 * p14 - 300540195.0 * p15 + 300540195.0 * p16 - 265182525.0 * p17 + 206253075.0 * p18 - 141120525.0 * p19 + 84672315.0 * p20 - 44352165.0 * p21 + 20160075.0 * p22 - 7888725.0 * p23 + 2629575.0 * p24 - 736281.0 * p25 + 169911.0 * p26 - 31465.0 * p27 + 4495.0 * p28 - 465.0 * p29 + 31.0 * p30 - p31)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag31 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 31;
        q.warmup = self.rows.len() < 31;
        q.explanation = format!("window-lag31 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-first-difference anomaly",
                "WindowLag31 uses last-31 thirty-first difference; not WindowLag30 or WindowLag29",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag31 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_taneja_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let am = 0.5 * (u + v);
        let gm = (u * v).sqrt().max(1e-18);
        s += am * (am / gm).ln();
    }
    s.max(0.0)
}

/// Log-Taneja \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest raw log-Taneja distances
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from [`TanejaAnomaly`]
/// (raw levels) and [`LogBrayCurtisAnomaly`].
#[derive(Clone, Debug)]
pub struct LogTanejaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogTanejaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogTanejaAnomaly {
    /// Empty log-Taneja \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_taneja_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogTanejaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-taneja-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Taneja k-NN anomaly",
                "LogTanejaAnomaly is raw log-Taneja k-NN; not Taneja or log-Bray–Curtis",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogTanejaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is $|\Delta^{32} x|/\overline{|x|}$ on the trailing thirty-two
/// first-coordinates (binomial thirty-second difference).
/// Distinct from [`WindowLag31`] (thirty-first difference) and [`WindowLag30`].
#[derive(Clone, Debug)]
pub struct WindowLag32 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag32 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag32 {
    /// Empty windowed lag-32 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 32 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(32);
        let win = &xs[start..];
        if win.len() < 32 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        (y - 32.0 * p1 + 496.0 * p2 - 4960.0 * p3 + 35960.0 * p4 - 201376.0 * p5 + 906192.0 * p6 - 3365856.0 * p7 + 10518300.0 * p8 - 28048800.0 * p9 + 64512240.0 * p10 - 129024480.0 * p11 + 225792840.0 * p12 - 347373600.0 * p13 + 471435600.0 * p14 - 565722720.0 * p15 + 601080390.0 * p16 - 565722720.0 * p17 + 471435600.0 * p18 - 347373600.0 * p19 + 225792840.0 * p20 - 129024480.0 * p21 + 64512240.0 * p22 - 28048800.0 * p23 + 10518300.0 * p24 - 3365856.0 * p25 + 906192.0 * p26 - 201376.0 * p27 + 35960.0 * p28 - 4960.0 * p29 + 496.0 * p30 - 32.0 * p31 + 1.0 * p32)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag32 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 32;
        q.warmup = self.rows.len() < 32;
        q.explanation = format!("window-lag32 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-second-difference anomaly",
                "WindowLag32 uses last-32 thirty-second difference; not WindowLag31 or WindowLag30",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag32 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_motyka_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        num += u.max(v);
        den += u + v;
    }
    if den < 1e-18 {
        0.0
    } else {
        (num / den).max(0.0)
    }
}

/// Log-Motyka $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Motyka distances
/// $\sum\max(|\ln|x||,|\ln|y||)/\sum(|\ln|x||+|\ln|y||)$ without $\ell_1$
/// normalisation. Distinct from [`LogBrayCurtisAnomaly`] (absolute-difference
/// form) and [`LogRuzickaAnomaly`].
#[derive(Clone, Debug)]
pub struct LogMotykaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMotykaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMotykaAnomaly {
    /// Empty log-Motyka $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_motyka_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMotykaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-motyka-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Motyka k-NN anomaly",
                "LogMotykaAnomaly is raw log-Motyka k-NN; not log-Bray–Curtis or log-Ružička",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMotykaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is $|\Delta^{33} x|/\overline{|x|}$ on the trailing thirty-three
/// first-coordinates (binomial thirty-third difference).
/// Distinct from [`WindowLag32`] (thirty-second difference) and [`WindowLag31`].
#[derive(Clone, Debug)]
pub struct WindowLag33 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag33 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag33 {
    /// Empty windowed lag-33 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 33 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(33);
        let win = &xs[start..];
        if win.len() < 33 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        (y - 33.0 * p1 + 528.0 * p2 - 5456.0 * p3 + 40920.0 * p4 - 237336.0 * p5 + 1107568.0 * p6 - 4272048.0 * p7 + 13884156.0 * p8 - 38567100.0 * p9 + 92561040.0 * p10 - 193536720.0 * p11 + 354817320.0 * p12 - 573166440.0 * p13 + 818809200.0 * p14 - 1037158320.0 * p15 + 1166803110.0 * p16 - 1166803110.0 * p17 + 1037158320.0 * p18 - 818809200.0 * p19 + 573166440.0 * p20 - 354817320.0 * p21 + 193536720.0 * p22 - 92561040.0 * p23 + 38567100.0 * p24 - 13884156.0 * p25 + 4272048.0 * p26 - 1107568.0 * p27 + 237336.0 * p28 - 40920.0 * p29 + 5456.0 * p30 - 528.0 * p31 + 33.0 * p32 - 1.0 * p33)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag33 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 33;
        q.warmup = self.rows.len() < 33;
        q.explanation = format!("window-lag33 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-third-difference anomaly",
                "WindowLag33 uses last-33 thirty-third difference; not WindowLag32 or WindowLag31",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag33 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_hellinger_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().sqrt();
        let v = b[j].abs().max(1e-18).ln().abs().sqrt();
        let d = u - v;
        s += d * d;
    }
    (0.5 * s).max(0.0).sqrt()
}

/// Log-Hellinger $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Hellinger distances on
/// $|\ln|x||$ without $\ell_1$ normalisation. Distinct from [`HellingerAnomaly`]
/// (raw levels) and [`LogMotykaAnomaly`].
#[derive(Clone, Debug)]
pub struct LogHellingerAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogHellingerAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogHellingerAnomaly {
    /// Empty log-Hellinger $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_hellinger_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogHellingerAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-hellinger-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Hellinger k-NN anomaly",
                "LogHellingerAnomaly is raw log-Hellinger k-NN; not Hellinger or log-Motyka",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogHellingerAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is $|\Delta^{34} x|/\overline{|x|}$ on the trailing thirty-four
/// first-coordinates (binomial thirty-fourth difference).
/// Distinct from [`WindowLag33`] (thirty-third difference) and [`WindowLag32`].
#[derive(Clone, Debug)]
pub struct WindowLag34 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag34 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag34 {
    /// Empty windowed lag-34 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 34 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(34);
        let win = &xs[start..];
        if win.len() < 34 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        (y - 34.0 * p1 + 561.0 * p2 - 5984.0 * p3 + 46376.0 * p4 - 278256.0 * p5 + 1344904.0 * p6 - 5379616.0 * p7 + 18156204.0 * p8 - 52451256.0 * p9 + 131128140.0 * p10 - 286097760.0 * p11 + 548354040.0 * p12 - 927983760.0 * p13 + 1391975640.0 * p14 - 1855967520.0 * p15 + 2203961430.0 * p16 - 2333606220.0 * p17 + 2203961430.0 * p18 - 1855967520.0 * p19 + 1391975640.0 * p20 - 927983760.0 * p21 + 548354040.0 * p22 - 286097760.0 * p23 + 131128140.0 * p24 - 52451256.0 * p25 + 18156204.0 * p26 - 5379616.0 * p27 + 1344904.0 * p28 - 278256.0 * p29 + 46376.0 * p30 - 5984.0 * p31 + 561.0 * p32 - 34.0 * p33 + 1.0 * p34)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag34 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 34;
        q.warmup = self.rows.len() < 34;
        q.explanation = format!("window-lag34 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-fourth-difference anomaly",
                "WindowLag34 uses last-34 thirty-fourth difference; not WindowLag33 or WindowLag32",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag34 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_fidelity_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        s += (u * v).sqrt();
    }
    (p as f64 - s).max(0.0)
}

/// Log-fidelity $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest fidelity complements
/// $p-\sum\sqrt{|\ln|x||\,|\ln|y||}$ without $\ell_1$ normalisation.
/// Distinct from [`LogHellingerAnomaly`] and [`LogMotykaAnomaly`].
#[derive(Clone, Debug)]
pub struct LogFidelityAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogFidelityAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogFidelityAnomaly {
    /// Empty log-fidelity $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_fidelity_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogFidelityAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-fidelity-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-fidelity k-NN anomaly",
                "LogFidelityAnomaly is raw log-fidelity k-NN; not log-Hellinger or log-Motyka",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogFidelityAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is $|\Delta^{35} x|/\overline{|x|}$ on the trailing thirty-five
/// first-coordinates (binomial thirty-fifth difference).
/// Distinct from [`WindowLag34`] (thirty-fourth difference) and [`WindowLag33`].
#[derive(Clone, Debug)]
pub struct WindowLag35 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag35 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag35 {
    /// Empty windowed lag-35 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 35 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(35);
        let win = &xs[start..];
        if win.len() < 35 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        (y - 35.0 * p1 + 595.0 * p2 - 6545.0 * p3 + 52360.0 * p4 - 324632.0 * p5 + 1623160.0 * p6 - 6724520.0 * p7 + 23535820.0 * p8 - 70607460.0 * p9 + 183579396.0 * p10 - 417225900.0 * p11 + 834451800.0 * p12 - 1476337800.0 * p13 + 2319959400.0 * p14 - 3247943160.0 * p15 + 4059928950.0 * p16 - 4537567650.0 * p17 + 4537567650.0 * p18 - 4059928950.0 * p19 + 3247943160.0 * p20 - 2319959400.0 * p21 + 1476337800.0 * p22 - 834451800.0 * p23 + 417225900.0 * p24 - 183579396.0 * p25 + 70607460.0 * p26 - 23535820.0 * p27 + 6724520.0 * p28 - 1623160.0 * p29 + 324632.0 * p30 - 52360.0 * p31 + 6545.0 * p32 - 595.0 * p33 + 35.0 * p34 - 1.0 * p35)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag35 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 35;
        q.warmup = self.rows.len() < 35;
        q.explanation = format!("window-lag35 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-fifth-difference anomaly",
                "WindowLag35 uses last-35 thirty-fifth difference; not WindowLag34 or WindowLag33",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag35 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_pearson_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs();
        let v = b[j].abs().max(1e-18).ln().abs();
        let den = (u + v).max(1e-18);
        let d = u - v;
        s += d * d / den;
    }
    s.max(0.0)
}

/// Log-Pearson $\chi^2$ $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Pearson $\chi^2$ distances
/// $\sum(|\ln|x||-|\ln|y||)^2 / (|\ln|x||+|\ln|y||)$ without $\ell_1$
/// normalisation. Distinct from [`PearsonChiAnomaly`] (raw levels) and
/// [`LogFidelityAnomaly`].
#[derive(Clone, Debug)]
pub struct LogPearsonChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogPearsonChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogPearsonChiAnomaly {
    /// Empty log-Pearson $\chi^2$ $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_pearson_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogPearsonChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-pearson-chi-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Pearson chi-squared k-NN anomaly",
                "LogPearsonChiAnomaly is raw log-Pearson chi-squared k-NN; not Pearson-chi or log-fidelity",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogPearsonChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Score is $|\Delta^{36} x|/\overline{|x|}$ on the trailing thirty-six
/// first-coordinates (binomial thirty-sixth difference).
/// Distinct from [`WindowLag35`] (thirty-fifth difference) and [`WindowLag34`].
#[derive(Clone, Debug)]
pub struct WindowLag36 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag36 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag36 {
    /// Empty windowed lag-36 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 36 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(36);
        let win = &xs[start..];
        if win.len() < 36 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        (y - 36.0 * p1 + 630.0 * p2 - 7140.0 * p3 + 58905.0 * p4 - 376992.0 * p5 + 1947792.0 * p6 - 8347680.0 * p7 + 30260340.0 * p8 - 94143280.0 * p9 + 254186856.0 * p10 - 600805296.0 * p11 + 1251677700.0 * p12 - 2310789600.0 * p13 + 3796297200.0 * p14 - 5567902560.0 * p15 + 7307872110.0 * p16 - 8597496600.0 * p17 + 9075135300.0 * p18 - 8597496600.0 * p19 + 7307872110.0 * p20 - 5567902560.0 * p21 + 3796297200.0 * p22 - 2310789600.0 * p23 + 1251677700.0 * p24 - 600805296.0 * p25 + 254186856.0 * p26 - 94143280.0 * p27 + 30260340.0 * p28 - 8347680.0 * p29 + 1947792.0 * p30 - 376992.0 * p31 + 58905.0 * p32 - 7140.0 * p33 + 630.0 * p34 - 36.0 * p35 + 1.0 * p36)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag36 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 36;
        q.warmup = self.rows.len() < 36;
        q.explanation = format!("window-lag36 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-sixth-difference anomaly",
                "WindowLag36 uses last-36 thirty-sixth difference; not WindowLag35 or WindowLag34",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag36 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_js_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let m = 0.5 * (u + v);
        s += 0.5 * (u * (u / m).ln() + v * (v / m).ln());
    }
    s.max(0.0)
}

/// Log–Jensen–Shannon $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest JS divergences on $|\ln|x||$
/// without $\ell_1$ normalisation. Distinct from [`JensenShannonAnomaly`]
/// (raw levels) and [`LogPearsonChiAnomaly`].
#[derive(Clone, Debug)]
pub struct LogJensenShannonAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogJensenShannonAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogJensenShannonAnomaly {
    /// Empty log-JS $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_js_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogJensenShannonAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-js-knn reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Jensen-Shannon k-NN anomaly",
                "LogJensenShannonAnomaly is raw log-JS k-NN; not Jensen-Shannon or log-Pearson-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogJensenShannonAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Windowed thirty-seventh-difference anomaly.
///
/// Score is $|\Delta^{37}|$ of the first coordinate over the last 37
/// reservoir points, scaled by the window mean absolute level. Distinct from
/// [`WindowLag36`] and [`WindowLag35`].
#[derive(Clone, Debug)]
pub struct WindowLag37 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag37 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag37 {
    /// Empty windowed lag-37 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 37 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(37);
        let win = &xs[start..];
        if win.len() < 37 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        (y - 37.0 * p1 + 666.0 * p2 - 7770.0 * p3 + 66045.0 * p4 - 435897.0 * p5 + 2324784.0 * p6 - 10295472.0 * p7 + 38608020.0 * p8 - 124403620.0 * p9 + 348330136.0 * p10 - 854992152.0 * p11 + 1852482996.0 * p12 - 3562467300.0 * p13 + 6107086800.0 * p14 - 9364199760.0 * p15 + 12875774670.0 * p16 - 15905368710.0 * p17 + 17672631900.0 * p18 - 17672631900.0 * p19 + 15905368710.0 * p20 - 12875774670.0 * p21 + 9364199760.0 * p22 - 6107086800.0 * p23 + 3562467300.0 * p24 - 1852482996.0 * p25 + 854992152.0 * p26 - 348330136.0 * p27 + 124403620.0 * p28 - 38608020.0 * p29 + 10295472.0 * p30 - 2324784.0 * p31 + 435897.0 * p32 - 66045.0 * p33 + 7770.0 * p34 - 666.0 * p35 + 37.0 * p36 - 1.0 * p37)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag37 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 37;
        q.warmup = self.rows.len() < 37;
        q.explanation = format!("window-lag37 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-seventh-difference anomaly",
                "WindowLag37 uses last-37 thirty-seventh difference; not WindowLag36 or WindowLag35",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag37 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_jeffreys_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v) * (u / v).ln();
    }
    s.max(0.0)
}

/// Log–Jeffreys $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Jeffreys divergences
/// $\sum(u-v)\ln(u/v)$ on $|\ln|x||$ without $\ell_1$ normalisation.
/// Distinct from [`JeffreysAnomaly`] (raw levels) and [`LogJensenShannonAnomaly`].
#[derive(Clone, Debug)]
pub struct LogJeffreysAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogJeffreysAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogJeffreysAnomaly {
    /// Empty log-Jeffreys $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_jeffreys_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogJeffreysAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Jeffreys k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Jeffreys k-NN anomaly",
                "LogJeffreysAnomaly is raw log-Jeffreys k-NN; not Jeffreys or log-JS",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogJeffreysAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed thirty-eighth-difference anomaly.
///
/// Score is $|\Delta^{38}|$ of the first coordinate over the last 38
/// reservoir points, scaled by the window mean absolute level. Distinct from
/// [`WindowLag37`] and [`WindowLag36`].
#[derive(Clone, Debug)]
pub struct WindowLag38 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag38 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag38 {
    /// Empty windowed lag-38 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 38 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(38);
        let win = &xs[start..];
        if win.len() < 38 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        (y - 38.0 * p1 + 703.0 * p2 - 8436.0 * p3 + 73815.0 * p4 - 501942.0 * p5 + 2760681.0 * p6 - 12620256.0 * p7 + 48903492.0 * p8 - 163011640.0 * p9 + 472733756.0 * p10 - 1203322288.0 * p11 + 2707475148.0 * p12 - 5414950296.0 * p13 + 9669554100.0 * p14 - 15471286560.0 * p15 + 22239974430.0 * p16 - 28781143380.0 * p17 + 33578000610.0 * p18 - 35345263800.0 * p19 + 33578000610.0 * p20 - 28781143380.0 * p21 + 22239974430.0 * p22 - 15471286560.0 * p23 + 9669554100.0 * p24 - 5414950296.0 * p25 + 2707475148.0 * p26 - 1203322288.0 * p27 + 472733756.0 * p28 - 163011640.0 * p29 + 48903492.0 * p30 - 12620256.0 * p31 + 2760681.0 * p32 - 501942.0 * p33 + 73815.0 * p34 - 8436.0 * p35 + 703.0 * p36 - 38.0 * p37 + 1.0 * p38)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag38 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 38;
        q.warmup = self.rows.len() < 38;
        q.explanation = format!("window-lag38 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-eighth-difference anomaly",
                "WindowLag38 uses last-38 thirty-eighth difference; not WindowLag37 or WindowLag36",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag38 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_kumar_hassebrook_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        num += u * v;
        den += u * u + v * v - u * v;
    }
    if den < 1e-18 {
        return 0.0;
    }
    (1.0 - num / den).max(0.0)
}

/// Log–Kumar–Hassebrook $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Kumar–Hassebrook distances
/// $1-\sum uv/\sum(u^2+v^2-uv)$ on $|\ln|x||$ without $\ell_1$
/// normalisation. Distinct from [`LogTanimotoAnomaly`] and [`LogJeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct LogKumarHassebrookAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogKumarHassebrookAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogKumarHassebrookAnomaly {
    /// Empty log-Kumar–Hassebrook $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_kumar_hassebrook_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogKumarHassebrookAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Kumar-Hassebrook k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Kumar-Hassebrook k-NN anomaly",
                "LogKumarHassebrookAnomaly is raw log-Kumar-Hassebrook k-NN; not Tanimoto or log-Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogKumarHassebrookAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed thirty-ninth-difference anomaly.
///
/// Score is $|\Delta^{39}|$ of the first coordinate over the last 39
/// reservoir points, scaled by the window mean absolute level. Distinct from
/// [`WindowLag38`] and [`WindowLag37`].
#[derive(Clone, Debug)]
pub struct WindowLag39 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag39 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag39 {
    /// Empty windowed lag-39 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 39 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(39);
        let win = &xs[start..];
        if win.len() < 39 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        (y - 39.0 * p1 + 741.0 * p2 - 9139.0 * p3 + 82251.0 * p4 - 575757.0 * p5 + 3262623.0 * p6 - 15380937.0 * p7 + 61523748.0 * p8 - 211915132.0 * p9 + 635745396.0 * p10 - 1676056044.0 * p11 + 3910797436.0 * p12 - 8122425444.0 * p13 + 15084504396.0 * p14 - 25140840660.0 * p15 + 37711260990.0 * p16 - 51021117810.0 * p17 + 62359143990.0 * p18 - 68923264410.0 * p19 + 68923264410.0 * p20 - 62359143990.0 * p21 + 51021117810.0 * p22 - 37711260990.0 * p23 + 25140840660.0 * p24 - 15084504396.0 * p25 + 8122425444.0 * p26 - 3910797436.0 * p27 + 1676056044.0 * p28 - 635745396.0 * p29 + 211915132.0 * p30 - 61523748.0 * p31 + 15380937.0 * p32 - 3262623.0 * p33 + 575757.0 * p34 - 82251.0 * p35 + 9139.0 * p36 - 741.0 * p37 + 39.0 * p38 - 1.0 * p39)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag39 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 39;
        q.warmup = self.rows.len() < 39;
        q.explanation = format!("window-lag39 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed thirty-ninth-difference anomaly",
                "WindowLag39 uses last-39 thirty-ninth difference; not WindowLag38 or WindowLag37",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag39 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_additive_symmetric_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        s += d * d * (u + v) / (u * v);
    }
    s.max(0.0)
}

/// Log–additive-symmetric $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest additive-symmetric divergences
/// $\sum(u-v)^2(u+v)/(uv)$ on $|\ln|x||$ without $\ell_1$ normalisation.
/// Distinct from [`AdditiveSymmetricAnomaly`] (raw levels, $(u-v)^2/(u+v)$)
/// and [`LogJeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct LogAdditiveSymmetricAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogAdditiveSymmetricAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogAdditiveSymmetricAnomaly {
    /// Empty log-additive-symmetric $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_additive_symmetric_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogAdditiveSymmetricAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-additive-symmetric k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-additive-symmetric k-NN anomaly",
                "LogAdditiveSymmetricAnomaly is raw log additive-symmetric k-NN; not AdditiveSymmetric or log-Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogAdditiveSymmetricAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed fortieth-difference anomaly.
///
/// Score is $|\Delta^{40}|$ of the first coordinate over the last 40
/// reservoir points, scaled by the window mean absolute level. Distinct from
/// [`WindowLag39`] and [`WindowLag38`].
#[derive(Clone, Debug)]
pub struct WindowLag40 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag40 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag40 {
    /// Empty windowed lag-40 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 40 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(40);
        let win = &xs[start..];
        if win.len() < 40 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        (y - 40.0 * p1 + 780.0 * p2 - 9880.0 * p3 + 91390.0 * p4 - 658008.0 * p5 + 3838380.0 * p6 - 18643560.0 * p7 + 76904685.0 * p8 - 273438880.0 * p9 + 847660528.0 * p10 - 2311801440.0 * p11 + 5586853480.0 * p12 - 12033222880.0 * p13 + 23206929840.0 * p14 - 40225345056.0 * p15 + 62852101650.0 * p16 - 88732378800.0 * p17 + 113380261800.0 * p18 - 131282408400.0 * p19 + 137846528820.0 * p20 - 131282408400.0 * p21 + 113380261800.0 * p22 - 88732378800.0 * p23 + 62852101650.0 * p24 - 40225345056.0 * p25 + 23206929840.0 * p26 - 12033222880.0 * p27 + 5586853480.0 * p28 - 2311801440.0 * p29 + 847660528.0 * p30 - 273438880.0 * p31 + 76904685.0 * p32 - 18643560.0 * p33 + 3838380.0 * p34 - 658008.0 * p35 + 91390.0 * p36 - 9880.0 * p37 + 780.0 * p38 - 40.0 * p39 + 1.0 * p40)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag40 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 40;
        q.warmup = self.rows.len() < 40;
        q.explanation = format!("window-lag40 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fortieth-difference anomaly",
                "WindowLag40 uses last-40 fortieth difference; not WindowLag39 or WindowLag38",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag40 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_k_divergence_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += u * (2.0 * u / (u + v)).ln();
    }
    s.max(0.0)
}

/// Log–K-divergence $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest K-divergences
/// $\sum u\ln(2u/(u+v))$ on $|\ln|x||$ without $\ell_1$ normalisation.
/// Distinct from [`KDivergenceAnomaly`] (raw levels) and [`LogJeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct LogKDivergenceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogKDivergenceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogKDivergenceAnomaly {
    /// Empty log-K-divergence $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_k_divergence_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogKDivergenceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-K-divergence k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-K-divergence k-NN anomaly",
                "LogKDivergenceAnomaly is raw log-K-divergence k-NN; not K-divergence or log-Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogKDivergenceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Windowed forty-first-difference anomaly.
///
/// Score is $|\Delta^{41}|$ of the first coordinate over the last 41
/// reservoir points, scaled by the window mean absolute level. Distinct from
/// [`WindowLag40`] and [`WindowLag39`].
#[derive(Clone, Debug)]
pub struct WindowLag41 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag41 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag41 {
    /// Empty windowed lag-41 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 41 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(41);
        let win = &xs[start..];
        if win.len() < 41 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        (y - 41.0 * p1 + 820.0 * p2 - 10660.0 * p3 + 101270.0 * p4 - 749398.0 * p5 + 4496388.0 * p6 - 22481940.0 * p7 + 95548245.0 * p8 - 350343565.0 * p9 + 1121099408.0 * p10 - 3159461968.0 * p11 + 7898654920.0 * p12 - 17620076360.0 * p13 + 35240152720.0 * p14 - 63432274896.0 * p15 + 103077446706.0 * p16 - 151584480450.0 * p17 + 202112640600.0 * p18 - 244662670200.0 * p19 + 269128937220.0 * p20 - 269128937220.0 * p21 + 244662670200.0 * p22 - 202112640600.0 * p23 + 151584480450.0 * p24 - 103077446706.0 * p25 + 63432274896.0 * p26 - 35240152720.0 * p27 + 17620076360.0 * p28 - 7898654920.0 * p29 + 3159461968.0 * p30 - 1121099408.0 * p31 + 350343565.0 * p32 - 95548245.0 * p33 + 22481940.0 * p34 - 4496388.0 * p35 + 749398.0 * p36 - 101270.0 * p37 + 10660.0 * p38 - 820.0 * p39 + 41.0 * p40 - 1.0 * p41)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag41 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 41;
        q.warmup = self.rows.len() < 41;
        q.explanation = format!("window-lag41 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-first-difference anomaly",
                "WindowLag41 uses last-41 forty-first difference; not WindowLag40 or WindowLag39",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag41 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_cosine_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut dot = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        dot += u * v;
        na += u * u;
        nb += v * v;
    }
    let den = na.sqrt() * nb.sqrt();
    if den < 1e-18 {
        return 0.0;
    }
    (1.0 - dot / den).max(0.0)
}

/// Log-cosine $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest cosine distances
/// $1-\langle u,v\rangle/(\|u\|\|v\|)$ on $|\ln|x||$ without $\ell_1$
/// normalisation. Distinct from [`LogEuclideanAnomaly`] and [`LogJeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct LogCosineAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogCosineAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogCosineAnomaly {
    /// Empty log-cosine $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_cosine_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogCosineAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-cosine k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-cosine k-NN anomaly",
                "LogCosineAnomaly is raw log-cosine k-NN; not Euclidean or log-Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogCosineAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-42 windowed forty-second-difference anomaly.
///
/// Distinct from [`WindowLag41`] and [`WindowLag40`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag42 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag42 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag42 {
    /// Empty windowed lag-42 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 42 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(42);
        let win = &xs[start..];
        if win.len() < 42 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        (y - 42.0 * p1 + 861.0 * p2 - 11480.0 * p3 + 111930.0 * p4 - 850668.0 * p5 + 5245786.0 * p6 - 26978328.0 * p7 + 118030185.0 * p8 - 445891810.0 * p9 + 1471442973.0 * p10 - 4280561376.0 * p11 + 11058116888.0 * p12 - 25518731280.0 * p13 + 52860229080.0 * p14 - 98672427616.0 * p15 + 166509721602.0 * p16 - 254661927156.0 * p17 + 353697121050.0 * p18 - 446775310800.0 * p19 + 513791607420.0 * p20 - 538257874440.0 * p21 + 513791607420.0 * p22 - 446775310800.0 * p23 + 353697121050.0 * p24 - 254661927156.0 * p25 + 166509721602.0 * p26 - 98672427616.0 * p27 + 52860229080.0 * p28 - 25518731280.0 * p29 + 11058116888.0 * p30 - 4280561376.0 * p31 + 1471442973.0 * p32 - 445891810.0 * p33 + 118030185.0 * p34 - 26978328.0 * p35 + 5245786.0 * p36 - 850668.0 * p37 + 111930.0 * p38 - 11480.0 * p39 + 861.0 * p40 - 42.0 * p41 + 1.0 * p42)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag42 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 42;
        q.warmup = self.rows.len() < 42;
        q.explanation = format!("window-lag42 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-second-difference anomaly",
                "WindowLag42 uses last-42 forty-second difference; not WindowLag41 or WindowLag40",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag42 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_jaccard_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut num = 0.0_f64;
    let mut den = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        num += d * d;
        den += u * u + v * v;
    }
    if den < 1e-18 {
        return 0.0;
    }
    (num / den).max(0.0)
}

/// Log-Jaccard $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest real-valued Jaccard distances
/// $\sum(u-v)^2/\sum(u^2+v^2)$ on $|\ln|x||$ without $\ell_1$
/// normalisation and without the Tanimoto $-uv$ denominator term. Distinct from
/// [`LogTanimotoAnomaly`] and [`LogKumarHassebrookAnomaly`].
#[derive(Clone, Debug)]
pub struct LogJaccardAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogJaccardAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogJaccardAnomaly {
    /// Empty log-Jaccard $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_jaccard_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogJaccardAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-jaccard k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-jaccard k-NN anomaly",
                "LogJaccardAnomaly is raw log-Jaccard k-NN; not Tanimoto or log-Kumar-Hassebrook",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogJaccardAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-43 windowed forty-third-difference anomaly.
///
/// Distinct from [`WindowLag42`] and [`WindowLag41`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag43 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag43 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag43 {
    /// Empty windowed lag-43 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 43 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(43);
        let win = &xs[start..];
        if win.len() < 43 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        (y - 43.0 * p1 + 903.0 * p2 - 12341.0 * p3 + 123410.0 * p4 - 962598.0 * p5 + 6096454.0 * p6 - 32224114.0 * p7 + 145008513.0 * p8 - 563921995.0 * p9 + 1917334783.0 * p10 - 5752004349.0 * p11 + 15338678264.0 * p12 - 36576848168.0 * p13 + 78378960360.0 * p14 - 151532656696.0 * p15 + 265182149218.0 * p16 - 421171648758.0 * p17 + 608359048206.0 * p18 - 800472431850.0 * p19 + 960566918220.0 * p20 - 1052049481860.0 * p21 + 1052049481860.0 * p22 - 960566918220.0 * p23 + 800472431850.0 * p24 - 608359048206.0 * p25 + 421171648758.0 * p26 - 265182149218.0 * p27 + 151532656696.0 * p28 - 78378960360.0 * p29 + 36576848168.0 * p30 - 15338678264.0 * p31 + 5752004349.0 * p32 - 1917334783.0 * p33 + 563921995.0 * p34 - 145008513.0 * p35 + 32224114.0 * p36 - 6096454.0 * p37 + 962598.0 * p38 - 123410.0 * p39 + 12341.0 * p40 - 903.0 * p41 + 43.0 * p42 - 1.0 * p43)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag43 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 43;
        q.warmup = self.rows.len() < 43;
        q.explanation = format!("window-lag43 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-third-difference anomaly",
                "WindowLag43 uses last-43 forty-third difference; not WindowLag42 or WindowLag41",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag43 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_divergence_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let den = (u + v).max(1e-18);
        let d = u - v;
        s += 2.0 * d * d / (den * den);
    }
    s.max(0.0)
}

/// Log-divergence $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest triangular-divergence distances
/// $2\sum(u-v)^2/(u+v)^2$ on $|\ln|x||$ without $\ell_1$ normalisation.
/// Distinct from [`LogClarkAnomaly`] (root-sum of squared relative gaps) and
/// [`LogPearsonChiAnomaly`].
#[derive(Clone, Debug)]
pub struct LogDivergenceAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogDivergenceAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogDivergenceAnomaly {
    /// Empty log-divergence $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_divergence_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogDivergenceAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-divergence k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-divergence k-NN anomaly",
                "LogDivergenceAnomaly is raw log-triangular-divergence k-NN; not Clark or log-Pearson-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogDivergenceAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-44 windowed forty-fourth-difference anomaly.
///
/// Distinct from [`WindowLag43`] and [`WindowLag42`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag44 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag44 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag44 {
    /// Empty windowed lag-44 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 44 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(44);
        let win = &xs[start..];
        if win.len() < 44 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        (y - 44.0 * p1 + 946.0 * p2 - 13244.0 * p3 + 135751.0 * p4 - 1086008.0 * p5 + 7059052.0 * p6 - 38320568.0 * p7 + 177232627.0 * p8 - 708930508.0 * p9 + 2481256778.0 * p10 - 7669339132.0 * p11 + 21090682613.0 * p12 - 51915526432.0 * p13 + 114955808528.0 * p14 - 229911617056.0 * p15 + 416714805914.0 * p16 - 686353797976.0 * p17 + 1029530696964.0 * p18 - 1408831480056.0 * p19 + 1761039350070.0 * p20 - 2012616400080.0 * p21 + 2104098963720.0 * p22 - 2012616400080.0 * p23 + 1761039350070.0 * p24 - 1408831480056.0 * p25 + 1029530696964.0 * p26 - 686353797976.0 * p27 + 416714805914.0 * p28 - 229911617056.0 * p29 + 114955808528.0 * p30 - 51915526432.0 * p31 + 21090682613.0 * p32 - 7669339132.0 * p33 + 2481256778.0 * p34 - 708930508.0 * p35 + 177232627.0 * p36 - 38320568.0 * p37 + 7059052.0 * p38 - 1086008.0 * p39 + 135751.0 * p40 - 13244.0 * p41 + 946.0 * p42 - 44.0 * p43 + 1.0 * p44)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag44 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 44;
        q.warmup = self.rows.len() < 44;
        q.explanation = format!("window-lag44 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-fourth-difference anomaly",
                "WindowLag44 uses last-44 forty-fourth difference; not WindowLag43 or WindowLag42",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag44 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_czekanowski_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut smin = 0.0_f64;
    let mut ssum = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        smin += if u < v { u } else { v };
        ssum += u + v;
    }
    if ssum < 1e-18 {
        return 0.0;
    }
    (1.0 - 2.0 * smin / ssum).max(0.0)
}

/// Log-Czekanowski $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Czekanowski distances
/// $1-2\sum\min(u,v)/\sum(u+v)$ on $|\ln|x||$ without $\ell_1$
/// normalisation. Distinct from [`LogDiceAnomaly`] (inner-product form) and
/// [`LogRuzickaAnomaly`] ($\sum\min/\sum\max$).
#[derive(Clone, Debug)]
pub struct LogCzekanowskiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogCzekanowskiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogCzekanowskiAnomaly {
    /// Empty log-Czekanowski $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_czekanowski_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogCzekanowskiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-czekanowski k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-czekanowski k-NN anomaly",
                "LogCzekanowskiAnomaly is raw log-Czekanowski k-NN; not Dice or log-Ruzicka",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogCzekanowskiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-45 windowed forty-fifth-difference anomaly.
///
/// Distinct from [`WindowLag44`] and [`WindowLag43`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag45 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag45 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag45 {
    /// Empty windowed lag-45 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 45 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(45);
        let win = &xs[start..];
        if win.len() < 45 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        (y - 45.0 * p1 + 990.0 * p2 - 14190.0 * p3 + 148995.0 * p4 - 1221759.0 * p5 + 8145060.0 * p6 - 45379620.0 * p7 + 215553195.0 * p8 - 886163135.0 * p9 + 3190187286.0 * p10 - 10150595910.0 * p11 + 28760021745.0 * p12 - 73006209045.0 * p13 + 166871334960.0 * p14 - 344867425584.0 * p15 + 646626422970.0 * p16 - 1103068603890.0 * p17 + 1715884494940.0 * p18 - 2438362177020.0 * p19 + 3169870830126.0 * p20 - 3773655750150.0 * p21 + 4116715363800.0 * p22 - 4116715363800.0 * p23 + 3773655750150.0 * p24 - 3169870830126.0 * p25 + 2438362177020.0 * p26 - 1715884494940.0 * p27 + 1103068603890.0 * p28 - 646626422970.0 * p29 + 344867425584.0 * p30 - 166871334960.0 * p31 + 73006209045.0 * p32 - 28760021745.0 * p33 + 10150595910.0 * p34 - 3190187286.0 * p35 + 886163135.0 * p36 - 215553195.0 * p37 + 45379620.0 * p38 - 8145060.0 * p39 + 1221759.0 * p40 - 148995.0 * p41 + 14190.0 * p42 - 990.0 * p43 + 45.0 * p44 - 1.0 * p45)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag45 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 45;
        q.warmup = self.rows.len() < 45;
        q.explanation = format!("window-lag45 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-fifth-difference anomaly",
                "WindowLag45 uses last-45 forty-fifth difference; not WindowLag44 or WindowLag43",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag45 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_kumar_johnson_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let uv = (u * v).max(1e-18);
        let num = (u * u - v * v).powi(2);
        s += num / (2.0 * uv.powf(1.5));
    }
    s.max(0.0)
}

/// Log–Kumar–Johnson $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Kumar–Johnson distances
/// $\sum(u^2-v^2)^2/(2(uv)^{3/2})$ on $|\ln|x||$ without $\ell_1$
/// normalisation. Distinct from [`LogKumarHassebrookAnomaly`] and
/// [`LogJeffreysAnomaly`].
#[derive(Clone, Debug)]
pub struct LogKumarJohnsonAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogKumarJohnsonAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogKumarJohnsonAnomaly {
    /// Empty log-Kumar–Johnson $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_kumar_johnson_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogKumarJohnsonAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-kumar-johnson k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-kumar-johnson k-NN anomaly",
                "LogKumarJohnsonAnomaly is raw log-Kumar-Johnson k-NN; not Kumar-Hassebrook or log-Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogKumarJohnsonAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-46 windowed forty-sixth-difference anomaly.
///
/// Distinct from [`WindowLag45`] and [`WindowLag44`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag46 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag46 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag46 {
    /// Empty windowed lag-46 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 46 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(46);
        let win = &xs[start..];
        if win.len() < 46 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        (y - 46.0 * p1 + 1035.0 * p2 - 15180.0 * p3 + 163185.0 * p4 - 1370754.0 * p5 + 9366819.0 * p6 - 53524680.0 * p7 + 260932815.0 * p8 - 1101716330.0 * p9 + 4076350421.0 * p10 - 13340783196.0 * p11 + 38910617655.0 * p12 - 101766230790.0 * p13 + 239877544005.0 * p14 - 511738760544.0 * p15 + 991493848554.0 * p16 - 1749695026860.0 * p17 + 2818953098830.0 * p18 - 4154246671960.0 * p19 + 5608233007146.0 * p20 - 6943526580276.0 * p21 + 7890371113950.0 * p22 - 8233430727600.0 * p23 + 7890371113950.0 * p24 - 6943526580276.0 * p25 + 5608233007146.0 * p26 - 4154246671960.0 * p27 + 2818953098830.0 * p28 - 1749695026860.0 * p29 + 991493848554.0 * p30 - 511738760544.0 * p31 + 239877544005.0 * p32 - 101766230790.0 * p33 + 38910617655.0 * p34 - 13340783196.0 * p35 + 4076350421.0 * p36 - 1101716330.0 * p37 + 260932815.0 * p38 - 53524680.0 * p39 + 9366819.0 * p40 - 1370754.0 * p41 + 163185.0 * p42 - 15180.0 * p43 + 1035.0 * p44 - 46.0 * p45 + 1.0 * p46)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag46 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 46;
        q.warmup = self.rows.len() < 46;
        q.explanation = format!("window-lag46 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-sixth-difference anomaly",
                "WindowLag46 uses last-46 forty-sixth difference; not WindowLag45 or WindowLag44",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag46 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_average_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut l1 = 0.0_f64;
    let mut l2 = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        l1 += d.abs();
        l2 += d * d;
    }
    0.5 * (l1 + l2.sqrt())
}

/// Log-average $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest average $\ell_1$/$\ell_2$ distances
/// $(\sum|u-v|+\sqrt{\sum(u-v)^2})/2$ on $|\ln|x||$ without $\ell_1$
/// normalisation. Distinct from [`LogManhattanAnomaly`] and [`LogEuclideanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogAverageAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogAverageAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogAverageAnomaly {
    /// Empty log-average $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_average_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogAverageAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-average k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-average k-NN anomaly",
                "LogAverageAnomaly is raw log-average L1/L2 k-NN; not Manhattan or log-Euclidean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogAverageAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-47 windowed forty-seventh-difference anomaly.
///
/// Distinct from [`WindowLag46`] and [`WindowLag45`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag47 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag47 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag47 {
    /// Empty windowed lag-47 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 47 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(47);
        let win = &xs[start..];
        if win.len() < 47 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        (y - 47.0 * p1 + 1081.0 * p2 - 16215.0 * p3 + 178365.0 * p4 - 1533939.0 * p5 + 10737573.0 * p6 - 62891499.0 * p7 + 314457495.0 * p8 - 1362649145.0 * p9 + 5178066751.0 * p10 - 17417133617.0 * p11 + 52251400851.0 * p12 - 140676848445.0 * p13 + 341643774795.0 * p14 - 751616304549.0 * p15 + 1503232609098.0 * p16 - 2741188875414.0 * p17 + 4568648125690.0 * p18 - 6973199770790.0 * p19 + 9762479679106.0 * p20 - 12551759587422.0 * p21 + 14833897694226.0 * p22 - 16123801841550.0 * p23 + 16123801841550.0 * p24 - 14833897694226.0 * p25 + 12551759587422.0 * p26 - 9762479679106.0 * p27 + 6973199770790.0 * p28 - 4568648125690.0 * p29 + 2741188875414.0 * p30 - 1503232609098.0 * p31 + 751616304549.0 * p32 - 341643774795.0 * p33 + 140676848445.0 * p34 - 52251400851.0 * p35 + 17417133617.0 * p36 - 5178066751.0 * p37 + 1362649145.0 * p38 - 314457495.0 * p39 + 62891499.0 * p40 - 10737573.0 * p41 + 1533939.0 * p42 - 178365.0 * p43 + 16215.0 * p44 - 1081.0 * p45 + 47.0 * p46 - 1.0 * p47)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag47 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 47;
        q.warmup = self.rows.len() < 47;
        q.explanation = format!("window-lag47 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-seventh-difference anomaly",
                "WindowLag47 uses last-47 forty-seventh difference; not WindowLag46 or WindowLag45",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag47 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_neyman_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        s += (d * d) / u;
    }
    s
}

/// Log-Neyman $\chi^2$ $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Neyman distances $\sum(u-v)^2/u$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogPearsonChiAnomaly`] ($\sum(u-v)^2/(u+v)$) and [`LogDivergenceAnomaly`].
#[derive(Clone, Debug)]
pub struct LogNeymanAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogNeymanAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogNeymanAnomaly {
    /// Empty log-Neyman $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_neyman_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogNeymanAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Neyman k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Neyman chi-squared k-NN anomaly",
                "LogNeymanAnomaly is raw log-Neyman k-NN; not Pearson chi-squared or log-divergence",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogNeymanAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-48 windowed forty-eighth-difference anomaly.
///
/// Distinct from [`WindowLag47`] and [`WindowLag46`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag48 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag48 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag48 {
    /// Empty windowed lag-48 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 48 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(48);
        let win = &xs[start..];
        if win.len() < 48 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        let p48 = win[n - 48];
        (y - 48.0 * p1 + 1128.0 * p2 - 17296.0 * p3 + 194580.0 * p4 - 1712304.0 * p5 + 12271512.0 * p6 - 73629072.0 * p7 + 377348994.0 * p8 - 1677106640.0 * p9 + 6540715896.0 * p10 - 22595200368.0 * p11 + 69668534468.0 * p12 - 192928249296.0 * p13 + 482320623240.0 * p14 - 1093260079344.0 * p15 + 2254848913647.0 * p16 - 4244421484512.0 * p17 + 7309837001104.0 * p18 - 11541847896480.0 * p19 + 16735679449896.0 * p20 - 22314239266528.0 * p21 + 27385657281648.0 * p22 - 30957699535776.0 * p23 + 32247603683100.0 * p24 - 30957699535776.0 * p25 + 27385657281648.0 * p26 - 22314239266528.0 * p27 + 16735679449896.0 * p28 - 11541847896480.0 * p29 + 7309837001104.0 * p30 - 4244421484512.0 * p31 + 2254848913647.0 * p32 - 1093260079344.0 * p33 + 482320623240.0 * p34 - 192928249296.0 * p35 + 69668534468.0 * p36 - 22595200368.0 * p37 + 6540715896.0 * p38 - 1677106640.0 * p39 + 377348994.0 * p40 - 73629072.0 * p41 + 12271512.0 * p42 - 1712304.0 * p43 + 194580.0 * p44 - 17296.0 * p45 + 1128.0 * p46 - 48.0 * p47 + 1.0 * p48)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag48 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 48;
        q.warmup = self.rows.len() < 48;
        q.explanation = format!("window-lag48 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-eighth-difference anomaly",
                "WindowLag48 uses last-48 forty-eighth difference; not WindowLag47 or WindowLag46",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag48 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_vicis_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        let mn = u.min(v);
        s += (d * d) / (mn * mn);
    }
    s
}

/// Log-Vicis-symmetric $\chi^2$ $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest Vicis distances $\sum(u-v)^2/\min(u,v)^2$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogNeymanAnomaly`] ($\sum(u-v)^2/u$) and [`LogPearsonChiAnomaly`].
#[derive(Clone, Debug)]
pub struct LogVicisAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogVicisAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogVicisAnomaly {
    /// Empty log-Vicis $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_vicis_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogVicisAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Vicis k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Vicis-symmetric chi-squared k-NN anomaly",
                "LogVicisAnomaly is raw log-Vicis k-NN; not Neyman or Pearson chi-squared",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogVicisAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-49 windowed forty-ninth-difference anomaly.
///
/// Distinct from [`WindowLag48`] and [`WindowLag47`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag49 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag49 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag49 {
    /// Empty windowed lag-49 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 49 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(49);
        let win = &xs[start..];
        if win.len() < 49 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        let p48 = win[n - 48];
        let p49 = win[n - 49];
        (y - 49.0 * p1 + 1176.0 * p2 - 18424.0 * p3 + 211876.0 * p4 - 1906884.0 * p5 + 13983816.0 * p6 - 85900584.0 * p7 + 450978066.0 * p8 - 2054455634.0 * p9 + 8217822536.0 * p10 - 29135916264.0 * p11 + 92263734836.0 * p12 - 262596783764.0 * p13 + 675248872536.0 * p14 - 1575580702584.0 * p15 + 3348108992991.0 * p16 - 6499270398159.0 * p17 + 11554258485616.0 * p18 - 18851684897584.0 * p19 + 28277527346376.0 * p20 - 39049918716424.0 * p21 + 49699896548176.0 * p22 - 58343356817424.0 * p23 + 63205303218876.0 * p24 - 63205303218876.0 * p25 + 58343356817424.0 * p26 - 49699896548176.0 * p27 + 39049918716424.0 * p28 - 28277527346376.0 * p29 + 18851684897584.0 * p30 - 11554258485616.0 * p31 + 6499270398159.0 * p32 - 3348108992991.0 * p33 + 1575580702584.0 * p34 - 675248872536.0 * p35 + 262596783764.0 * p36 - 92263734836.0 * p37 + 29135916264.0 * p38 - 8217822536.0 * p39 + 2054455634.0 * p40 - 450978066.0 * p41 + 85900584.0 * p42 - 13983816.0 * p43 + 1906884.0 * p44 - 211876.0 * p45 + 18424.0 * p46 - 1176.0 * p47 + 49.0 * p48 - 1.0 * p49)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag49 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 49;
        q.warmup = self.rows.len() < 49;
        q.explanation = format!("window-lag49 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed forty-ninth-difference anomaly",
                "WindowLag49 uses last-49 forty-ninth difference; not WindowLag48 or WindowLag47",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag49 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_max_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        s += (d * d) / u.max(v);
    }
    s
}

/// Log-max-symmetric $\chi^2$ $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest distances $\sum(u-v)^2/\max(u,v)$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogNeymanAnomaly`] ($\sum(u-v)^2/u$) and [`LogVicisAnomaly`].
#[derive(Clone, Debug)]
pub struct LogMaxChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMaxChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMaxChiAnomaly {
    /// Empty log-max-chi $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_max_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMaxChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-max-chi k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-max-symmetric chi-squared k-NN anomaly",
                "LogMaxChiAnomaly is raw log-max-chi k-NN; not Neyman or Vicis",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMaxChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-50 windowed fiftieth-difference anomaly.
///
/// Distinct from [`WindowLag49`] and [`WindowLag48`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag50 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag50 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag50 {
    /// Empty windowed lag-50 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 50 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(50);
        let win = &xs[start..];
        if win.len() < 50 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        let p48 = win[n - 48];
        let p49 = win[n - 49];
        let p50 = win[n - 50];
        (y - 50.0 * p1 + 1225.0 * p2 - 19600.0 * p3 + 230300.0 * p4 - 2118760.0 * p5 + 15890700.0 * p6 - 99884400.0 * p7 + 536878650.0 * p8 - 2505433700.0 * p9 + 10272278170.0 * p10 - 37353738800.0 * p11 + 121399651100.0 * p12 - 354860518600.0 * p13 + 937845656300.0 * p14 - 2250829575120.0 * p15 + 4923689695575.0 * p16 - 9847379391150.0 * p17 + 18053528883775.0 * p18 - 30405943383200.0 * p19 + 47129212243960.0 * p20 - 67327446062800.0 * p21 + 88749815264600.0 * p22 - 108043253365600.0 * p23 + 121548660036300.0 * p24 - 126410606437752.0 * p25 + 121548660036300.0 * p26 - 108043253365600.0 * p27 + 88749815264600.0 * p28 - 67327446062800.0 * p29 + 47129212243960.0 * p30 - 30405943383200.0 * p31 + 18053528883775.0 * p32 - 9847379391150.0 * p33 + 4923689695575.0 * p34 - 2250829575120.0 * p35 + 937845656300.0 * p36 - 354860518600.0 * p37 + 121399651100.0 * p38 - 37353738800.0 * p39 + 10272278170.0 * p40 - 2505433700.0 * p41 + 536878650.0 * p42 - 99884400.0 * p43 + 15890700.0 * p44 - 2118760.0 * p45 + 230300.0 * p46 - 19600.0 * p47 + 1225.0 * p48 - 50.0 * p49 + 1.0 * p50)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag50 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 50;
        q.warmup = self.rows.len() < 50;
        q.explanation = format!("window-lag50 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fiftieth-difference anomaly",
                "WindowLag50 uses last-50 fiftieth difference; not WindowLag49 or WindowLag48",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag50 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_min_chi_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        s += (d * d) / u.min(v);
    }
    s
}

/// Log-min-symmetric $\chi^2$ $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest distances $\sum(u-v)^2/\min(u,v)$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogMaxChiAnomaly`] ($\sum(u-v)^2/\max(u,v)$) and [`LogVicisAnomaly`]
/// ($\sum(u-v)^2/\min(u,v)^2$).
#[derive(Clone, Debug)]
pub struct LogMinChiAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinChiAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinChiAnomaly {
    /// Empty log-min-chi $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_min_chi_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinChiAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-min-chi k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-min-symmetric chi-squared k-NN anomaly",
                "LogMinChiAnomaly is raw log-min-chi k-NN; not max-chi or Vicis",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinChiAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-51 windowed fifty-first-difference anomaly.
///
/// Distinct from [`WindowLag50`] and [`WindowLag49`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag51 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag51 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag51 {
    /// Empty windowed lag-51 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 51 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(51);
        let win = &xs[start..];
        if win.len() < 51 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        let p48 = win[n - 48];
        let p49 = win[n - 49];
        let p50 = win[n - 50];
        let p51 = win[n - 51];
        (y - 51.0 * p1 + 1275.0 * p2 - 20825.0 * p3 + 249900.0 * p4 - 2349060.0 * p5 + 18009460.0 * p6 - 115775100.0 * p7 + 636763050.0 * p8 - 3042312350.0 * p9 + 12777711870.0 * p10 - 47626016970.0 * p11 + 158753389900.0 * p12 - 476260169700.0 * p13 + 1292706174900.0 * p14 - 3188675231420.0 * p15 + 7174519270695.0 * p16 - 14771069086725.0 * p17 + 27900908274925.0 * p18 - 48459472266975.0 * p19 + 77535155627160.0 * p20 - 114456658306760.0 * p21 + 156077261327400.0 * p22 - 196793068630200.0 * p23 + 229591913401900.0 * p24 - 247959266474052.0 * p25 + 247959266474052.0 * p26 - 229591913401900.0 * p27 + 196793068630200.0 * p28 - 156077261327400.0 * p29 + 114456658306760.0 * p30 - 77535155627160.0 * p31 + 48459472266975.0 * p32 - 27900908274925.0 * p33 + 14771069086725.0 * p34 - 7174519270695.0 * p35 + 3188675231420.0 * p36 - 1292706174900.0 * p37 + 476260169700.0 * p38 - 158753389900.0 * p39 + 47626016970.0 * p40 - 12777711870.0 * p41 + 3042312350.0 * p42 - 636763050.0 * p43 + 115775100.0 * p44 - 18009460.0 * p45 + 2349060.0 * p46 - 249900.0 * p47 + 20825.0 * p48 - 1275.0 * p49 + 51.0 * p50 - 1.0 * p51)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag51 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 51;
        q.warmup = self.rows.len() < 51;
        q.explanation = format!("window-lag51 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-first-difference anomaly",
                "WindowLag51 uses last-51 fifty-first difference; not WindowLag50 or WindowLag49",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag51 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_bhattacharyya_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    let mut su = 0.0_f64;
    let mut sv = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u * v).sqrt();
        su += u;
        sv += v;
    }
    let den = (su * sv).sqrt().max(1e-18);
    let bc = (s / den).clamp(1e-18, 1.0);
    -bc.ln()
}

/// Log-Bhattacharyya $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest distances $-\ln(\sum\sqrt{uv}/\sqrt{\sum u\sum v})$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogHellingerAnomaly`] and [`LogFidelityAnomaly`].
#[derive(Clone, Debug)]
pub struct LogBhattacharyyaAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogBhattacharyyaAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogBhattacharyyaAnomaly {
    /// Empty log-Bhattacharyya $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_bhattacharyya_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogBhattacharyyaAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Bhattacharyya k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Bhattacharyya k-NN anomaly",
                "LogBhattacharyyaAnomaly is raw log-Bhattacharyya k-NN; not Hellinger or fidelity",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogBhattacharyyaAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-52 windowed fifty-second-difference anomaly.
///
/// Distinct from [`WindowLag51`] and [`WindowLag50`]. Window length is not
/// identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag52 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag52 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag52 {
    /// Empty windowed lag-52 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 52 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(52);
        let win = &xs[start..];
        if win.len() < 52 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let n = win.len();
        let p1 = win[n - 1];
        let p2 = win[n - 2];
        let p3 = win[n - 3];
        let p4 = win[n - 4];
        let p5 = win[n - 5];
        let p6 = win[n - 6];
        let p7 = win[n - 7];
        let p8 = win[n - 8];
        let p9 = win[n - 9];
        let p10 = win[n - 10];
        let p11 = win[n - 11];
        let p12 = win[n - 12];
        let p13 = win[n - 13];
        let p14 = win[n - 14];
        let p15 = win[n - 15];
        let p16 = win[n - 16];
        let p17 = win[n - 17];
        let p18 = win[n - 18];
        let p19 = win[n - 19];
        let p20 = win[n - 20];
        let p21 = win[n - 21];
        let p22 = win[n - 22];
        let p23 = win[n - 23];
        let p24 = win[n - 24];
        let p25 = win[n - 25];
        let p26 = win[n - 26];
        let p27 = win[n - 27];
        let p28 = win[n - 28];
        let p29 = win[n - 29];
        let p30 = win[n - 30];
        let p31 = win[n - 31];
        let p32 = win[n - 32];
        let p33 = win[n - 33];
        let p34 = win[n - 34];
        let p35 = win[n - 35];
        let p36 = win[n - 36];
        let p37 = win[n - 37];
        let p38 = win[n - 38];
        let p39 = win[n - 39];
        let p40 = win[n - 40];
        let p41 = win[n - 41];
        let p42 = win[n - 42];
        let p43 = win[n - 43];
        let p44 = win[n - 44];
        let p45 = win[n - 45];
        let p46 = win[n - 46];
        let p47 = win[n - 47];
        let p48 = win[n - 48];
        let p49 = win[n - 49];
        let p50 = win[n - 50];
        let p51 = win[n - 51];
        let p52 = win[n - 52];
        (y - 52.0 * p1 + 1326.0 * p2 - 22100.0 * p3 + 270725.0 * p4 - 2598960.0 * p5 + 20358520.0 * p6 - 133784560.0 * p7 + 752538150.0 * p8 - 3679075400.0 * p9 + 15820024220.0 * p10 - 60403728840.0 * p11 + 206379406870.0 * p12 - 635013559600.0 * p13 + 1768966344600.0 * p14 - 4481381406320.0 * p15 + 10363194502115.0 * p16 - 21945588357420.0 * p17 + 42671977361650.0 * p18 - 76360380541900.0 * p19 + 125994627894135.0 * p20 - 191991813933920.0 * p21 + 270533919634160.0 * p22 - 352870329957600.0 * p23 + 426384982032100.0 * p24 - 477551179875952.0 * p25 + 495918532948104.0 * p26 - 477551179875952.0 * p27 + 426384982032100.0 * p28 - 352870329957600.0 * p29 + 270533919634160.0 * p30 - 191991813933920.0 * p31 + 125994627894135.0 * p32 - 76360380541900.0 * p33 + 42671977361650.0 * p34 - 21945588357420.0 * p35 + 10363194502115.0 * p36 - 4481381406320.0 * p37 + 1768966344600.0 * p38 - 635013559600.0 * p39 + 206379406870.0 * p40 - 60403728840.0 * p41 + 15820024220.0 * p42 - 3679075400.0 * p43 + 752538150.0 * p44 - 133784560.0 * p45 + 20358520.0 * p46 - 2598960.0 * p47 + 270725.0 * p48 - 22100.0 * p49 + 1326.0 * p50 - 52.0 * p51 + 1.0 * p52)
            .abs()
            / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag52 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 52;
        q.warmup = self.rows.len() < 52;
        q.explanation = format!("window-lag52 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-second-difference anomaly",
                "WindowLag52 uses last-52 fifty-second difference; not WindowLag51 or WindowLag50",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag52 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_vicis_wave_hedges_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs() / u.min(v);
    }
    s
}

/// Log-Vicis–Wave-Hedges $k$-NN anomaly.
///
/// Score is the mean of the $k$ smallest distances $\sum|u-v|/\min(u,v)$
/// on $|\ln|x||$ without $\ell_1$ normalisation. Distinct from
/// [`LogWaveHedgesAnomaly`] ($\sum|u-v|/\max(u,v)$) and
/// [`LogKulczynskiAnomaly`] ($\sum|u-v|/\sum\min$).
#[derive(Clone, Debug)]
pub struct LogVicisWaveHedgesAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogVicisWaveHedgesAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogVicisWaveHedgesAnomaly {
    /// Empty log-Vicis–Wave-Hedges $k$-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_vicis_wave_hedges_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogVicisWaveHedgesAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Vicis-Wave-Hedges k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Vicis-Wave-Hedges k-NN anomaly",
                "LogVicisWaveHedgesAnomaly is raw log-Vicis-WH k-NN; not Wave-Hedges or Kulczynski",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogVicisWaveHedgesAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-53 windowed fifty-third-difference anomaly.
///
/// The 53rd backward difference is formed by successive first differences
/// (`C(53, 26)` is not an exact `f64` integer). Distinct from [`WindowLag52`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag53 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag53 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag53 {
    /// Empty windowed lag-53 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 53 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(53);
        let win = &xs[start..];
        if win.len() < 53 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..53 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag53 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 53;
        q.warmup = self.rows.len() < 53;
        q.explanation = format!("window-lag53 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-third-difference anomaly",
                "WindowLag53 uses successive first differences; not WindowLag52 or WindowLag51",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag53 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_kl_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += u * (u / v).ln();
    }
    s
}

/// Log Kullback–Leibler \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest one-sided divergences
/// \(\sum u\ln(u/v)\) on \(|\ln|x||\) without \(\ell_1\) normalisation.
/// Distinct from [`LogJeffreysAnomaly`] (symmetrised) and
/// [`LogKDivergenceAnomaly`] (\(\sum u\ln(2u/(u+v))\)).
#[derive(Clone, Debug)]
pub struct LogKLAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogKLAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogKLAnomaly {
    /// Empty log-KL \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_kl_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogKLAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-KL k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-KL k-NN anomaly",
                "LogKLAnomaly is raw one-sided log-KL k-NN; not Jeffreys or K-divergence",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogKLAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-54 windowed fifty-fourth-difference anomaly.
///
/// The 54th backward difference is formed by successive first differences
/// (`C(54, 27)` is not an exact `f64` integer). Distinct from [`WindowLag53`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag54 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag54 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag54 {
    /// Empty windowed lag-54 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 54 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(54);
        let win = &xs[start..];
        if win.len() < 54 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..54 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag54 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 54;
        q.warmup = self.rows.len() < 54;
        q.explanation = format!("window-lag54 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-fourth-difference anomaly",
                "WindowLag54 uses successive first differences; not WindowLag53 or WindowLag52",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag54 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_reverse_kl_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += v * (v / u).ln();
    }
    s
}

/// Log reverse Kullback–Leibler \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest reverse divergences
/// \(\sum v\ln(v/u)\) on \(|\ln|x||\) without \(\ell_1\) normalisation.
/// Distinct from [`LogKLAnomaly`] (forward \(\sum u\ln(u/v)\)) and
/// [`LogJeffreysAnomaly`] (the sum of both directions).
#[derive(Clone, Debug)]
pub struct LogReverseKLAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogReverseKLAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogReverseKLAnomaly {
    /// Empty log reverse-KL \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_reverse_kl_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogReverseKLAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-reverse-KL k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-reverse-KL k-NN anomaly",
                "LogReverseKLAnomaly is raw reverse log-KL k-NN; not forward KL or Jeffreys",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogReverseKLAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-55 windowed fifty-fifth-difference anomaly.
///
/// The 55th backward difference is formed by successive first differences
/// (`C(55, 27)` is not an exact `f64` integer). Distinct from [`WindowLag54`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag55 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag55 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag55 {
    /// Empty windowed lag-55 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 55 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(55);
        let win = &xs[start..];
        if win.len() < 55 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..55 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag55 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 55;
        q.warmup = self.rows.len() < 55;
        q.explanation = format!("window-lag55 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-fifth-difference anomaly",
                "WindowLag55 uses successive first differences; not WindowLag54 or WindowLag53",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag55 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_itakura_saito_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += u / v - (u / v).ln() - 1.0;
    }
    s
}

/// Log Itakura–Saito \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest divergences
/// \(\sum(u/v-\ln(u/v)-1)\) on \(|\ln|x||\) without \(\ell_1\) normalisation.
/// Distinct from [`ItakuraSaitoAnomaly`] (raw scale) and [`LogKLAnomaly`].
#[derive(Clone, Debug)]
pub struct LogItakuraSaitoAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogItakuraSaitoAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogItakuraSaitoAnomaly {
    /// Empty log Itakura–Saito \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_itakura_saito_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogItakuraSaitoAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-Itakura-Saito k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-Itakura-Saito k-NN anomaly",
                "LogItakuraSaitoAnomaly is log-scale IS k-NN; not raw Itakura-Saito or KL",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogItakuraSaitoAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-56 windowed fifty-sixth-difference anomaly.
///
/// The 56th backward difference is formed by successive first differences
/// (`C(56, 28)` is not an exact `f64` integer). Distinct from [`WindowLag55`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag56 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag56 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag56 {
    /// Empty windowed lag-56 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 56 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(56);
        let win = &xs[start..];
        if win.len() < 56 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..56 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag56 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 56;
        q.warmup = self.rows.len() < 56;
        q.explanation = format!("window-lag56 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-sixth-difference anomaly",
                "WindowLag56 uses successive first differences; not WindowLag55 or WindowLag54",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag56 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_reverse_itakura_saito_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += v / u - (v / u).ln() - 1.0;
    }
    s
}

/// Log reverse Itakura–Saito \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest reverse divergences
/// \(\sum(v/u-\ln(v/u)-1)\) on \(|\ln|x||\) without \(\ell_1\) normalisation.
/// Distinct from [`LogItakuraSaitoAnomaly`] (forward) and [`LogReverseKLAnomaly`].
#[derive(Clone, Debug)]
pub struct LogReverseItakuraSaitoAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogReverseItakuraSaitoAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogReverseItakuraSaitoAnomaly {
    /// Empty log reverse Itakura–Saito \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_reverse_itakura_saito_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogReverseItakuraSaitoAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-reverse-IS k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-reverse-Itakura-Saito k-NN anomaly",
                "LogReverseItakuraSaitoAnomaly is reverse log-IS k-NN; not forward IS or reverse KL",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogReverseItakuraSaitoAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-57 windowed fifty-seventh-difference anomaly.
///
/// The 57th backward difference is formed by successive first differences
/// (`C(57, 28)` is not an exact `f64` integer). Distinct from [`WindowLag56`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag57 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag57 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag57 {
    /// Empty windowed lag-57 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 57 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(57);
        let win = &xs[start..];
        if win.len() < 57 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..57 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag57 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 57;
        q.warmup = self.rows.len() < 57;
        q.explanation = format!("window-lag57 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-seventh-difference anomaly",
                "WindowLag57 uses successive first differences; not WindowLag56 or WindowLag55",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag57 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_angular_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut dot = 0.0_f64;
    let mut na = 0.0_f64;
    let mut nb = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        dot += u * v;
        na += u * u;
        nb += v * v;
    }
    let c = (dot / (na.sqrt().max(1e-18) * nb.sqrt().max(1e-18))).clamp(-1.0, 1.0);
    c.acos()
}

/// Log angular \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest angles
/// \(\arccos(\langle u,v\rangle/(\|u\|\|v\|))\) on \(|\ln|x||\) without
/// \(\ell_1\) normalisation. Distinct from [`LogCosineAnomaly`] (\(1-\cos\)).
#[derive(Clone, Debug)]
pub struct LogAngularAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogAngularAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogAngularAnomaly {
    /// Empty log-angular \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_angular_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogAngularAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-angular k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-angular k-NN anomaly",
                "LogAngularAnomaly is arccos cosine on |ln|; not LogCosineAnomaly",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogAngularAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-58 windowed fifty-eighth-difference anomaly.
///
/// The 58th backward difference is formed by successive first differences
/// (`C(58, 29)` is not an exact `f64` integer). Distinct from [`WindowLag57`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag58 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag58 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag58 {
    /// Empty windowed lag-58 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 58 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(58);
        let win = &xs[start..];
        if win.len() < 58 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..58 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag58 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 58;
        q.warmup = self.rows.len() < 58;
        q.explanation = format!("window-lag58 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-eighth-difference anomaly",
                "WindowLag58 uses successive first differences; not WindowLag57 or WindowLag56",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag58 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_harmonic_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = u - v;
        s += (d * d) / (u * v);
    }
    s
}

/// Log harmonic \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest divergences \(\sum(u-v)^2/(uv)\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogNeymanAnomaly`] (\(\sum(u-v)^2/u\)) and [`LogPearsonChiAnomaly`].
#[derive(Clone, Debug)]
pub struct LogHarmonicAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogHarmonicAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogHarmonicAnomaly {
    /// Empty log-harmonic \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_harmonic_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogHarmonicAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-harmonic k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-harmonic k-NN anomaly",
                "LogHarmonicAnomaly is (u-v)^2/(uv) on |ln|; not Neyman or Pearson-chi",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogHarmonicAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-59 windowed fifty-ninth-difference anomaly.
///
/// The 59th backward difference is formed by successive first differences
/// (`C(59, 29)` is not an exact `f64` integer). Distinct from [`WindowLag58`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag59 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag59 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag59 {
    /// Empty windowed lag-59 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 59 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(59);
        let win = &xs[start..];
        if win.len() < 59 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..59 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag59 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 59;
        q.warmup = self.rows.len() < 59;
        q.explanation = format!("window-lag59 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed fifty-ninth-difference anomaly",
                "WindowLag59 uses successive first differences; not WindowLag58 or WindowLag57",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag59 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_ratio_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u / v).ln().abs();
    }
    s
}

/// Log-ratio \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \(\sum|\ln(u/v)|\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogManhattanAnomaly`] (\(\sum|u-v|\)) and [`LogChebyshevAnomaly`].
#[derive(Clone, Debug)]
pub struct LogRatioAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogRatioAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogRatioAnomaly {
    /// Empty log-ratio \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_ratio_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogRatioAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-ratio k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-ratio k-NN anomaly",
                "LogRatioAnomaly is |ln(u/v)| on |ln|; not Manhattan or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogRatioAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-60 windowed sixtieth-difference anomaly.
///
/// The 60th backward difference is formed by successive first differences
/// (`C(60, 30)` is not an exact `f64` integer). Distinct from [`WindowLag59`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag60 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag60 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag60 {
    /// Empty windowed lag-60 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 60 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(60);
        let win = &xs[start..];
        if win.len() < 60 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..60 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag60 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 60;
        q.warmup = self.rows.len() < 60;
        q.explanation = format!("window-lag60 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixtieth-difference anomaly",
                "WindowLag60 uses successive first differences; not WindowLag59 or WindowLag58",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag60 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_maxmin_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += u.max(v) / u.min(v) - 1.0;
    }
    s
}

/// Log max/min \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \(\sum(\max/\min-1)\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogRatioAnomaly`] (\(\sum|\ln(u/v)|\)) and [`LogChebyshevAnomaly`].
#[derive(Clone, Debug)]
pub struct LogMaxMinAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMaxMinAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMaxMinAnomaly {
    /// Empty log max/min \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_maxmin_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMaxMinAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-maxmin k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-maxmin k-NN anomaly",
                "LogMaxMinAnomaly is max/min-1 on |ln|; not log-ratio or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMaxMinAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-61 windowed sixty-first-difference anomaly.
///
/// The 61st backward difference is formed by successive first differences
/// (`C(61, 30)` is not an exact `f64` integer). Distinct from [`WindowLag60`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag61 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag61 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag61 {
    /// Empty windowed lag-61 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 61 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(61);
        let win = &xs[start..];
        if win.len() < 61 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..61 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag61 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 61;
        q.warmup = self.rows.len() < 61;
        q.explanation = format!("window-lag61 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-first-difference anomaly",
                "WindowLag61 uses successive first differences; not WindowLag60 or WindowLag59",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag61 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_squared_ratio_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        let d = (u / v).ln();
        s += d * d;
    }
    s
}

/// Log squared-ratio \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \(\sum(\ln(u/v))^2\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogRatioAnomaly`] (\(\sum|\ln(u/v)|\)) and [`LogEuclideanAnomaly`].
#[derive(Clone, Debug)]
pub struct LogSquaredRatioAnomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogSquaredRatioAnomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogSquaredRatioAnomaly {
    /// Empty log squared-ratio \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_squared_ratio_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogSquaredRatioAnomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-squared-ratio k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-squared-ratio k-NN anomaly",
                "LogSquaredRatioAnomaly is (ln(u/v))^2 on |ln|; not |ln(u/v)| or Euclidean",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogSquaredRatioAnomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-62 windowed sixty-second-difference anomaly.
///
/// The 62nd backward difference is formed by successive first differences
/// (`C(62, 31)` is not an exact `f64` integer). Distinct from [`WindowLag61`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag62 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag62 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag62 {
    /// Empty windowed lag-62 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 62 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(62);
        let win = &xs[start..];
        if win.len() < 62 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..62 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag62 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 62;
        q.warmup = self.rows.len() < 62;
        q.explanation = format!("window-lag62 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-second-difference anomaly",
                "WindowLag62 uses successive first differences; not WindowLag61 or WindowLag60",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag62 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski3_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(3);
    }
    s.powf(1.0 / 3.0)
}

/// Log Minkowski \(p=3\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^3)^{1/3}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogEuclideanAnomaly`] (\(p=2\)), [`LogManhattanAnomaly`] (\(p=1\)),
/// and [`LogChebyshevAnomaly`] (\(p=\infty\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski3Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski3Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski3Anomaly {
    /// Empty log Minkowski-\(p=3\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski3_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski3Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski3 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=3 k-NN anomaly",
                "LogMinkowski3Anomaly is L3 on |ln|; not Euclidean, Manhattan, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski3Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-63 windowed sixty-third-difference anomaly.
///
/// The 63rd backward difference is formed by successive first differences
/// (`C(63, 31)` is not an exact `f64` integer). Distinct from [`WindowLag62`].
/// Window length is not identification `p`.
#[derive(Clone, Debug)]
pub struct WindowLag63 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag63 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag63 {
    /// Empty windowed lag-63 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 63 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(63);
        let win = &xs[start..];
        if win.len() < 63 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..63 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag63 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 63;
        q.warmup = self.rows.len() < 63;
        q.explanation = format!("window-lag63 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-third-difference anomaly",
                "WindowLag63 uses successive first differences; not WindowLag62 or WindowLag61",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag63 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski4_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(4);
    }
    s.powf(0.25)
}

/// Log Minkowski \(p=4\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^4)^{1/4}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski3Anomaly`] (\(p=3\)) and [`LogEuclideanAnomaly`] (\(p=2\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski4Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski4Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski4Anomaly {
    /// Empty log Minkowski-\(p=4\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski4_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski4Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski4 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=4 k-NN anomaly",
                "LogMinkowski4Anomaly is L4 on |ln|; not L3, Euclidean, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski4Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-64 windowed sixty-fourth-difference anomaly.
///
/// The 64th backward difference is formed by successive first differences
/// (`C(64, 32)` is not an exact `f64` integer). Distinct from [`WindowLag63`].
/// Window length is not identification `p`. Reservoir cap equals the window.
#[derive(Clone, Debug)]
pub struct WindowLag64 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag64 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag64 {
    /// Empty windowed lag-64 detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 64 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(64);
        let win = &xs[start..];
        if win.len() < 64 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..64 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag64 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 64;
        q.warmup = self.rows.len() < 64;
        q.explanation = format!("window-lag64 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-fourth-difference anomaly",
                "WindowLag64 uses successive first differences; not WindowLag63 or WindowLag62",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag64 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski5_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(5);
    }
    s.powf(0.2)
}

/// Log Minkowski \(p=5\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^5)^{1/5}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski4Anomaly`] (\(p=4\)) and [`LogMinkowski3Anomaly`] (\(p=3\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski5Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski5Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski5Anomaly {
    /// Empty log Minkowski-\(p=5\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski5_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski5Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski5 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=5 k-NN anomaly",
                "LogMinkowski5Anomaly is L5 on |ln|; not L4, L3, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski5Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-65 windowed sixty-fifth-difference anomaly.
///
/// The 65th backward difference is formed by successive first differences
/// (`C(65, 32)` is not an exact `f64` integer). Distinct from [`WindowLag64`].
/// Window length is not identification `p`. Reservoir cap is 80 so the
/// window can fill.
#[derive(Clone, Debug)]
pub struct WindowLag65 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag65 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag65 {
    /// Empty windowed lag-65 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 65 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(65);
        let win = &xs[start..];
        if win.len() < 65 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..65 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag65 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 65;
        q.warmup = self.rows.len() < 65;
        q.explanation = format!("window-lag65 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-fifth-difference anomaly",
                "WindowLag65 uses successive first differences and cap 80; not WindowLag64",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag65 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski6_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(6);
    }
    s.powf(1.0 / 6.0)
}

/// Log Minkowski \(p=6\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^6)^{1/6}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski5Anomaly`] (\(p=5\)) and [`LogMinkowski4Anomaly`] (\(p=4\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski6Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski6Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski6Anomaly {
    /// Empty log Minkowski-\(p=6\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski6_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski6Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski6 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=6 k-NN anomaly",
                "LogMinkowski6Anomaly is L6 on |ln|; not L5, L4, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski6Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-66 windowed sixty-sixth-difference anomaly.
///
/// The 66th backward difference is formed by successive first differences
/// (`C(66, 33)` is not an exact `f64` integer). Distinct from [`WindowLag65`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag66 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag66 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag66 {
    /// Empty windowed lag-66 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 66 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(66);
        let win = &xs[start..];
        if win.len() < 66 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..66 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag66 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 66;
        q.warmup = self.rows.len() < 66;
        q.explanation = format!("window-lag66 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-sixth-difference anomaly",
                "WindowLag66 uses successive first differences and cap 80; not WindowLag65",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag66 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski7_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(7);
    }
    s.powf(1.0 / 7.0)
}

/// Log Minkowski \(p=7\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^7)^{1/7}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski6Anomaly`] (\(p=6\)) and [`LogMinkowski5Anomaly`] (\(p=5\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski7Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski7Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski7Anomaly {
    /// Empty log Minkowski-\(p=7\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski7_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski7Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski7 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=7 k-NN anomaly",
                "LogMinkowski7Anomaly is L7 on |ln|; not L6, L5, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski7Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-67 windowed sixty-seventh-difference anomaly.
///
/// The 67th backward difference is formed by successive first differences
/// (`C(67, 33)` is not an exact `f64` integer). Distinct from [`WindowLag66`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag67 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag67 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag67 {
    /// Empty windowed lag-67 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 67 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(67);
        let win = &xs[start..];
        if win.len() < 67 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..67 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag67 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 67;
        q.warmup = self.rows.len() < 67;
        q.explanation = format!("window-lag67 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-seventh-difference anomaly",
                "WindowLag67 uses successive first differences and cap 80; not WindowLag66",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag67 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski8_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(8);
    }
    s.powf(0.125)
}

/// Log Minkowski \(p=8\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^8)^{1/8}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski7Anomaly`] (\(p=7\)) and [`LogMinkowski6Anomaly`] (\(p=6\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski8Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski8Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski8Anomaly {
    /// Empty log Minkowski-\(p=8\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski8_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski8Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski8 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=8 k-NN anomaly",
                "LogMinkowski8Anomaly is L8 on |ln|; not L7, L6, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski8Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-68 windowed sixty-eighth-difference anomaly.
///
/// The 68th backward difference is formed by successive first differences
/// (`C(68, 34)` is not an exact `f64` integer). Distinct from [`WindowLag67`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag68 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag68 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag68 {
    /// Empty windowed lag-68 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 68 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(68);
        let win = &xs[start..];
        if win.len() < 68 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..68 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag68 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 68;
        q.warmup = self.rows.len() < 68;
        q.explanation = format!("window-lag68 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-eighth-difference anomaly",
                "WindowLag68 uses successive first differences and cap 80; not WindowLag67",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag68 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski9_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(9);
    }
    s.powf(1.0 / 9.0)
}

/// Log Minkowski \(p=9\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^9)^{1/9}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski8Anomaly`] (\(p=8\)) and [`LogMinkowski7Anomaly`] (\(p=7\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski9Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski9Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski9Anomaly {
    /// Empty log Minkowski-\(p=9\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski9_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski9Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski9 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=9 k-NN anomaly",
                "LogMinkowski9Anomaly is L9 on |ln|; not L8, L7, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski9Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-69 windowed sixty-ninth-difference anomaly.
///
/// The 69th backward difference is formed by successive first differences
/// (`C(69, 34)` is not an exact `f64` integer). Distinct from [`WindowLag68`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag69 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag69 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag69 {
    /// Empty windowed lag-69 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 69 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(69);
        let win = &xs[start..];
        if win.len() < 69 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..69 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag69 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 69;
        q.warmup = self.rows.len() < 69;
        q.explanation = format!("window-lag69 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed sixty-ninth-difference anomaly",
                "WindowLag69 uses successive first differences and cap 80; not WindowLag68",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag69 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski10_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(10);
    }
    s.powf(0.1)
}

/// Log Minkowski \(p=10\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{10})^{1/10}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski9Anomaly`] (\(p=9\)) and [`LogMinkowski8Anomaly`] (\(p=8\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski10Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski10Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski10Anomaly {
    /// Empty log Minkowski-\(p=10\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski10_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski10Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski10 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=10 k-NN anomaly",
                "LogMinkowski10Anomaly is L10 on |ln|; not L9, L8, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski10Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-70 windowed seventieth-difference anomaly.
///
/// The 70th backward difference is formed by successive first differences
/// (`C(70, 35)` is not an exact `f64` integer). Distinct from [`WindowLag69`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag70 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag70 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag70 {
    /// Empty windowed lag-70 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 70 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(70);
        let win = &xs[start..];
        if win.len() < 70 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..70 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag70 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 70;
        q.warmup = self.rows.len() < 70;
        q.explanation = format!("window-lag70 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventieth-difference anomaly",
                "WindowLag70 uses successive first differences and cap 80; not WindowLag69",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag70 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski11_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(11);
    }
    s.powf(1.0 / 11.0)
}

/// Log Minkowski \(p=11\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{11})^{1/11}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski10Anomaly`] (\(p=10\)) and [`LogMinkowski9Anomaly`] (\(p=9\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski11Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski11Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski11Anomaly {
    /// Empty log Minkowski-\(p=11\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski11_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski11Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski11 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=11 k-NN anomaly",
                "LogMinkowski11Anomaly is L11 on |ln|; not L10, L9, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski11Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-71 windowed seventy-first-difference anomaly.
///
/// The 71st backward difference is formed by successive first differences
/// (`C(71, 35)` is not an exact `f64` integer). Distinct from [`WindowLag70`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag71 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag71 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag71 {
    /// Empty windowed lag-71 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 71 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(71);
        let win = &xs[start..];
        if win.len() < 71 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..71 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag71 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 71;
        q.warmup = self.rows.len() < 71;
        q.explanation = format!("window-lag71 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-first-difference anomaly",
                "WindowLag71 uses successive first differences and cap 80; not WindowLag70",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag71 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski12_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(12);
    }
    s.powf(1.0 / 12.0)
}

/// Log Minkowski \(p=12\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{12})^{1/12}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski11Anomaly`] (\(p=11\)) and [`LogMinkowski10Anomaly`] (\(p=10\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski12Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski12Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski12Anomaly {
    /// Empty log Minkowski-\(p=12\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski12_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski12Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski12 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=12 k-NN anomaly",
                "LogMinkowski12Anomaly is L12 on |ln|; not L11, L10, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski12Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-72 windowed seventy-second-difference anomaly.
///
/// The 72nd backward difference is formed by successive first differences
/// (`C(72, 36)` is not an exact `f64` integer). Distinct from [`WindowLag71`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag72 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag72 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag72 {
    /// Empty windowed lag-72 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 72 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(72);
        let win = &xs[start..];
        if win.len() < 72 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..72 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag72 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 72;
        q.warmup = self.rows.len() < 72;
        q.explanation = format!("window-lag72 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-second-difference anomaly",
                "WindowLag72 uses successive first differences and cap 80; not WindowLag71",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag72 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski13_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(13);
    }
    s.powf(1.0 / 13.0)
}

/// Log Minkowski \(p=13\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{13})^{1/13}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski12Anomaly`] (\(p=12\)) and [`LogMinkowski11Anomaly`] (\(p=11\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski13Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski13Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski13Anomaly {
    /// Empty log Minkowski-\(p=13\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski13_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski13Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski13 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=13 k-NN anomaly",
                "LogMinkowski13Anomaly is L13 on |ln|; not L12, L11, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski13Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-73 windowed seventy-third-difference anomaly.
///
/// The 73rd backward difference is formed by successive first differences
/// (`C(73, 36)` is not an exact `f64` integer). Distinct from [`WindowLag72`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag73 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag73 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag73 {
    /// Empty windowed lag-73 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 73 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(73);
        let win = &xs[start..];
        if win.len() < 73 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..73 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag73 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 73;
        q.warmup = self.rows.len() < 73;
        q.explanation = format!("window-lag73 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-third-difference anomaly",
                "WindowLag73 uses successive first differences and cap 80; not WindowLag72",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag73 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski14_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(14);
    }
    s.powf(1.0 / 14.0)
}

/// Log Minkowski \(p=14\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{14})^{1/14}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski13Anomaly`] (\(p=13\)) and [`LogMinkowski12Anomaly`] (\(p=12\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski14Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski14Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski14Anomaly {
    /// Empty log Minkowski-\(p=14\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski14_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski14Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski14 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=14 k-NN anomaly",
                "LogMinkowski14Anomaly is L14 on |ln|; not L13, L12, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski14Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-74 windowed seventy-fourth-difference anomaly.
///
/// The 74th backward difference is formed by successive first differences
/// (`C(74, 37)` is not an exact `f64` integer). Distinct from [`WindowLag73`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag74 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag74 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag74 {
    /// Empty windowed lag-74 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 74 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(74);
        let win = &xs[start..];
        if win.len() < 74 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..74 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag74 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 74;
        q.warmup = self.rows.len() < 74;
        q.explanation = format!("window-lag74 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-fourth-difference anomaly",
                "WindowLag74 uses successive first differences and cap 80; not WindowLag73",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag74 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski15_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(15);
    }
    s.powf(1.0 / 15.0)
}

/// Log Minkowski \(p=15\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{15})^{1/15}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski14Anomaly`] (\(p=14\)) and [`LogMinkowski13Anomaly`] (\(p=13\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski15Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski15Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski15Anomaly {
    /// Empty log Minkowski-\(p=15\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski15_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski15Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski15 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=15 k-NN anomaly",
                "LogMinkowski15Anomaly is L15 on |ln|; not L14, L13, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski15Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-75 windowed seventy-fifth-difference anomaly.
///
/// The 75th backward difference is formed by successive first differences
/// (`C(75, 37)` is not an exact `f64` integer). Distinct from [`WindowLag74`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag75 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag75 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag75 {
    /// Empty windowed lag-75 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 75 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(75);
        let win = &xs[start..];
        if win.len() < 75 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..75 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag75 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 75;
        q.warmup = self.rows.len() < 75;
        q.explanation = format!("window-lag75 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-fifth-difference anomaly",
                "WindowLag75 uses successive first differences and cap 80; not WindowLag74",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag75 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski16_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(16);
    }
    s.powf(1.0 / 16.0)
}

/// Log Minkowski \(p=16\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{16})^{1/16}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski15Anomaly`] (\(p=15\)) and [`LogMinkowski14Anomaly`] (\(p=14\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski16Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski16Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski16Anomaly {
    /// Empty log Minkowski-\(p=16\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski16_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski16Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski16 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=16 k-NN anomaly",
                "LogMinkowski16Anomaly is L16 on |ln|; not L15, L14, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski16Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-76 windowed seventy-sixth-difference anomaly.
///
/// The 76th backward difference is formed by successive first differences
/// (`C(76, 38)` is not an exact `f64` integer). Distinct from [`WindowLag75`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag76 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag76 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag76 {
    /// Empty windowed lag-76 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 76 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(76);
        let win = &xs[start..];
        if win.len() < 76 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..76 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag76 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 76;
        q.warmup = self.rows.len() < 76;
        q.explanation = format!("window-lag76 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-sixth-difference anomaly",
                "WindowLag76 uses successive first differences and cap 80; not WindowLag75",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag76 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski17_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(17);
    }
    s.powf(1.0 / 17.0)
}

/// Log Minkowski \(p=17\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{17})^{1/17}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski16Anomaly`] (\(p=16\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski17Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski17Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski17Anomaly {
    /// Empty log Minkowski-\(p=17\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski17_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski17Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski17 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=17 k-NN anomaly",
                "LogMinkowski17Anomaly is L17 on |ln|; not L16 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski17Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-77 windowed seventy-seventh-difference anomaly.
///
/// The 77th backward difference is formed by successive first differences
/// (`C(77, 38)` is not an exact `f64` integer). Distinct from [`WindowLag76`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag77 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag77 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag77 {
    /// Empty windowed lag-77 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 77 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(77);
        let win = &xs[start..];
        if win.len() < 77 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..77 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag77 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 77;
        q.warmup = self.rows.len() < 77;
        q.explanation = format!("window-lag77 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-seventh-difference anomaly",
                "WindowLag77 uses successive first differences and cap 80; not WindowLag76",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag77 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski18_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(18);
    }
    s.powf(1.0 / 18.0)
}

/// Log Minkowski \(p=18\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{18})^{1/18}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski17Anomaly`] (\(p=17\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski18Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski18Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski18Anomaly {
    /// Empty log Minkowski-\(p=18\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski18_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski18Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski18 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=18 k-NN anomaly",
                "LogMinkowski18Anomaly is L18 on |ln|; not L17 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski18Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-78 windowed seventy-eighth-difference anomaly.
///
/// The 78th backward difference is formed by successive first differences
/// (`C(78, 39)` is not an exact `f64` integer). Distinct from [`WindowLag77`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag78 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag78 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag78 {
    /// Empty windowed lag-78 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 78 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(78);
        let win = &xs[start..];
        if win.len() < 78 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..78 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag78 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 78;
        q.warmup = self.rows.len() < 78;
        q.explanation = format!("window-lag78 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-eighth-difference anomaly",
                "WindowLag78 uses successive first differences and cap 80; not WindowLag77",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag78 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski19_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(19);
    }
    s.powf(1.0 / 19.0)
}

/// Log Minkowski \(p=19\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{19})^{1/19}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski18Anomaly`] (\(p=18\)) and [`LogMinkowski17Anomaly`] (\(p=17\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski19Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski19Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski19Anomaly {
    /// Empty log Minkowski-\(p=19\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski19_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski19Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski19 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=19 k-NN anomaly",
                "LogMinkowski19Anomaly is L19 on |ln|; not L18, L17, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski19Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-79 windowed seventy-ninth-difference anomaly.
///
/// The 79th backward difference is formed by successive first differences
/// (`C(79, 39)` is not an exact `f64` integer). Distinct from [`WindowLag78`].
/// Window length is not identification `p`. Reservoir cap is 80.
#[derive(Clone, Debug)]
pub struct WindowLag79 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag79 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag79 {
    /// Empty windowed lag-79 detector (reservoir cap 80).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 79 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(79);
        let win = &xs[start..];
        if win.len() < 79 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..79 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag79 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 80 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 79;
        q.warmup = self.rows.len() < 79;
        q.explanation = format!("window-lag79 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed seventy-ninth-difference anomaly",
                "WindowLag79 uses successive first differences and cap 80; not WindowLag78",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag79 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski20_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(20);
    }
    s.powf(1.0 / 20.0)
}

/// Log Minkowski \(p=20\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{20})^{1/20}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski19Anomaly`] (\(p=19\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski20Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski20Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski20Anomaly {
    /// Empty log Minkowski-\(p=20\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski20_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski20Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski20 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=20 k-NN anomaly",
                "LogMinkowski20Anomaly is L20 on |ln|; not L19 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski20Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-80 windowed eightieth-difference anomaly.
///
/// The 80th backward difference is formed by successive first differences
/// (`C(80, 40)` is not an exact `f64` integer). Distinct from [`WindowLag79`].
/// Window length is not identification `p`. Reservoir cap is 96 so a length-80
/// window can fill.
#[derive(Clone, Debug)]
pub struct WindowLag80 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag80 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag80 {
    /// Empty windowed lag-80 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 80 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(80);
        let win = &xs[start..];
        if win.len() < 80 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..80 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag80 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 80;
        q.warmup = self.rows.len() < 80;
        q.explanation = format!("window-lag80 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eightieth-difference anomaly",
                "WindowLag80 uses successive first differences and cap 96; not WindowLag79",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag80 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski21_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(21);
    }
    s.powf(1.0 / 21.0)
}

/// Log Minkowski \(p=21\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{21})^{1/21}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski20Anomaly`] (\(p=20\)) and [`LogMinkowski19Anomaly`] (\(p=19\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski21Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski21Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski21Anomaly {
    /// Empty log Minkowski-\(p=21\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski21_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski21Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski21 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=21 k-NN anomaly",
                "LogMinkowski21Anomaly is L21 on |ln|; not L20, L19, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski21Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-81 windowed eighty-first-difference anomaly.
///
/// The 81st backward difference is formed by successive first differences
/// (`C(81, 40)` is not an exact `f64` integer). Distinct from [`WindowLag80`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag81 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag81 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag81 {
    /// Empty windowed lag-81 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 81 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(81);
        let win = &xs[start..];
        if win.len() < 81 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..81 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag81 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 81;
        q.warmup = self.rows.len() < 81;
        q.explanation = format!("window-lag81 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-first-difference anomaly",
                "WindowLag81 uses successive first differences and cap 96; not WindowLag80",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag81 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski22_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(22);
    }
    s.powf(1.0 / 22.0)
}

/// Log Minkowski \(p=22\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{22})^{1/22}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski21Anomaly`] (\(p=21\)) and [`LogMinkowski20Anomaly`] (\(p=20\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski22Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski22Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski22Anomaly {
    /// Empty log Minkowski-\(p=22\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski22_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski22Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski22 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=22 k-NN anomaly",
                "LogMinkowski22Anomaly is L22 on |ln|; not L21, L20, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski22Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-82 windowed eighty-second-difference anomaly.
///
/// The 82nd backward difference is formed by successive first differences
/// (`C(82, 41)` is not an exact `f64` integer). Distinct from [`WindowLag81`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag82 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag82 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag82 {
    /// Empty windowed lag-82 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 82 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(82);
        let win = &xs[start..];
        if win.len() < 82 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..82 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag82 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 82;
        q.warmup = self.rows.len() < 82;
        q.explanation = format!("window-lag82 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-second-difference anomaly",
                "WindowLag82 uses successive first differences and cap 96; not WindowLag81",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag82 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski23_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(23);
    }
    s.powf(1.0 / 23.0)
}

/// Log Minkowski \(p=23\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{23})^{1/23}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski22Anomaly`] (\(p=22\)) and [`LogMinkowski21Anomaly`] (\(p=21\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski23Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski23Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski23Anomaly {
    /// Empty log Minkowski-\(p=23\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski23_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski23Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski23 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=23 k-NN anomaly",
                "LogMinkowski23Anomaly is L23 on |ln|; not L22, L21, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski23Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-83 windowed eighty-third-difference anomaly.
///
/// The 83rd backward difference is formed by successive first differences
/// (`C(83, 41)` is not an exact `f64` integer). Distinct from [`WindowLag82`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag83 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag83 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag83 {
    /// Empty windowed lag-83 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 83 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(83);
        let win = &xs[start..];
        if win.len() < 83 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..83 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag83 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 83;
        q.warmup = self.rows.len() < 83;
        q.explanation = format!("window-lag83 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-third-difference anomaly",
                "WindowLag83 uses successive first differences and cap 96; not WindowLag82",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag83 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski24_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(24);
    }
    s.powf(1.0 / 24.0)
}

/// Log Minkowski \(p=24\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{24})^{1/24}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski23Anomaly`] (\(p=23\)) and [`LogMinkowski22Anomaly`] (\(p=22\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski24Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski24Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski24Anomaly {
    /// Empty log Minkowski-\(p=24\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski24_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski24Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski24 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=24 k-NN anomaly",
                "LogMinkowski24Anomaly is L24 on |ln|; not L23, L22, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski24Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-84 windowed eighty-fourth-difference anomaly.
///
/// The 84th backward difference is formed by successive first differences
/// (`C(84, 42)` is not an exact `f64` integer). Distinct from [`WindowLag83`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag84 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag84 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag84 {
    /// Empty windowed lag-84 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 84 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(84);
        let win = &xs[start..];
        if win.len() < 84 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..84 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag84 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 84;
        q.warmup = self.rows.len() < 84;
        q.explanation = format!("window-lag84 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-fourth-difference anomaly",
                "WindowLag84 uses successive first differences and cap 96; not WindowLag83",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag84 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski25_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(25);
    }
    s.powf(1.0 / 25.0)
}

/// Log Minkowski \(p=25\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{25})^{1/25}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski24Anomaly`] (\(p=24\)) and [`LogMinkowski23Anomaly`] (\(p=23\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski25Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski25Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski25Anomaly {
    /// Empty log Minkowski-\(p=25\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski25_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski25Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski25 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=25 k-NN anomaly",
                "LogMinkowski25Anomaly is L24 on |ln|; not L23, L22, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski25Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-85 windowed eighty-fifth-difference anomaly.
///
/// The 85th backward difference is formed by successive first differences
/// (`C(85, 42)` is not an exact `f64` integer). Distinct from [`WindowLag84`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag85 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag85 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag85 {
    /// Empty windowed lag-85 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 85 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(85);
        let win = &xs[start..];
        if win.len() < 85 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..85 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag85 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 85;
        q.warmup = self.rows.len() < 85;
        q.explanation = format!("window-lag85 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-fifth-difference anomaly",
                "WindowLag85 uses successive first differences and cap 96; not WindowLag84",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag85 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski26_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(26);
    }
    s.powf(1.0 / 26.0)
}

/// Log Minkowski \(p=26\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{26})^{1/26}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski25Anomaly`] (\(p=25\)) and [`LogMinkowski24Anomaly`] (\(p=24\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski26Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski26Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski26Anomaly {
    /// Empty log Minkowski-\(p=26\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski26_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski26Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski25 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=26 k-NN anomaly",
                "LogMinkowski26Anomaly is L24 on |ln|; not L23, L22, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski26Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-86 windowed eighty-sixth-difference anomaly.
///
/// The 86th backward difference is formed by successive first differences
/// (`C(86, 43)` is not an exact `f64` integer). Distinct from [`WindowLag85`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag86 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag86 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag86 {
    /// Empty windowed lag-86 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 86 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(86);
        let win = &xs[start..];
        if win.len() < 86 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..86 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag86 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 86;
        q.warmup = self.rows.len() < 86;
        q.explanation = format!("window-lag86 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-sixth-difference anomaly",
                "WindowLag86 uses successive first differences and cap 96; not WindowLag85",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag86 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski27_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(27);
    }
    s.powf(1.0 / 27.0)
}

/// Log Minkowski \(p=27\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{27})^{1/27}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski26Anomaly`] (\(p=26\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski27Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski27Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski27Anomaly {
    /// Empty log Minkowski-\(p=27\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski27_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski27Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski27 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=27 k-NN anomaly",
                "LogMinkowski27Anomaly is L27 on |ln|; not L26 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski27Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-87 windowed eighty-seventh-difference anomaly.
///
/// The 87th backward difference is formed by successive first differences
/// (`C(87, 43)` is not an exact `f64` integer). Distinct from [`WindowLag86`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag87 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag87 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag87 {
    /// Empty windowed lag-87 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 87 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(87);
        let win = &xs[start..];
        if win.len() < 87 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..87 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag87 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 87;
        q.warmup = self.rows.len() < 87;
        q.explanation = format!("window-lag87 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-seventh-difference anomaly",
                "WindowLag87 uses successive first differences and cap 96; not WindowLag86",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag87 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski28_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(28);
    }
    s.powf(1.0 / 28.0)
}

/// Log Minkowski \(p=28\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{28})^{1/28}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski27Anomaly`] (\(p=27\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski28Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski28Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski28Anomaly {
    /// Empty log Minkowski-\(p=28\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski28_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski28Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski28 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=28 k-NN anomaly",
                "LogMinkowski28Anomaly is L28 on |ln|; not L27 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski28Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-88 windowed eighty-eighth-difference anomaly.
///
/// The 88th backward difference is formed by successive first differences
/// (`C(88, 44)` is not an exact `f64` integer). Distinct from [`WindowLag87`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag88 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag88 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag88 {
    /// Empty windowed lag-88 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 88 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(88);
        let win = &xs[start..];
        if win.len() < 88 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..88 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag88 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 88;
        q.warmup = self.rows.len() < 88;
        q.explanation = format!("window-lag88 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-eighth-difference anomaly",
                "WindowLag88 uses successive first differences and cap 96; not WindowLag87",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag88 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski29_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(29);
    }
    s.powf(1.0 / 29.0)
}

/// Log Minkowski \(p=29\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{29})^{1/29}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski28Anomaly`] (\(p=28\)) and [`LogMinkowski27Anomaly`] (\(p=27\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski29Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski29Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski29Anomaly {
    /// Empty log Minkowski-\(p=29\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski29_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski29Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski29 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=29 k-NN anomaly",
                "LogMinkowski29Anomaly is L29 on |ln|; not L28, L27, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski29Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-89 windowed eighty-ninth-difference anomaly.
///
/// The 89th backward difference is formed by successive first differences
/// (`C(89, 44)` is not an exact `f64` integer). Distinct from [`WindowLag88`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag89 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag89 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag89 {
    /// Empty windowed lag-89 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 89 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(89);
        let win = &xs[start..];
        if win.len() < 89 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..89 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag89 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 89;
        q.warmup = self.rows.len() < 89;
        q.explanation = format!("window-lag89 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed eighty-ninth-difference anomaly",
                "WindowLag89 uses successive first differences and cap 96; not WindowLag88",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag89 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski30_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(30);
    }
    s.powf(1.0 / 30.0)
}

/// Log Minkowski \(p=30\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{30})^{1/30}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski29Anomaly`] (\(p=29\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski30Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski30Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski30Anomaly {
    /// Empty log Minkowski-\(p=30\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski30_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski30Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski30 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=30 k-NN anomaly",
                "LogMinkowski30Anomaly is L30 on |ln|; not L29 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski30Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-90 windowed ninetieth-difference anomaly.
///
/// The 90th backward difference is formed by successive first differences
/// (`C(90, 45)` is not an exact `f64` integer). Distinct from [`WindowLag89`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag90 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag90 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag90 {
    /// Empty windowed lag-90 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 90 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(90);
        let win = &xs[start..];
        if win.len() < 90 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..90 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag90 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 90;
        q.warmup = self.rows.len() < 90;
        q.explanation = format!("window-lag90 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninetieth-difference anomaly",
                "WindowLag90 uses successive first differences and cap 96; not WindowLag89",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag90 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski31_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(31);
    }
    s.powf(1.0 / 31.0)
}

/// Log Minkowski \(p=31\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{31})^{1/31}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski30Anomaly`] (\(p=30\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski31Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski31Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski31Anomaly {
    /// Empty log Minkowski-\(p=31\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski31_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski31Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski31 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=31 k-NN anomaly",
                "LogMinkowski31Anomaly is L31 on |ln|; not L30 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski31Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-91 windowed ninety-first-difference anomaly.
///
/// The 91st backward difference is formed by successive first differences
/// (`C(91, 45)` is not an exact `f64` integer). Distinct from [`WindowLag90`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag91 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag91 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag91 {
    /// Empty windowed lag-91 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 91 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(91);
        let win = &xs[start..];
        if win.len() < 91 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..91 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag91 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 91;
        q.warmup = self.rows.len() < 91;
        q.explanation = format!("window-lag91 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-first-difference anomaly",
                "WindowLag91 uses successive first differences and cap 96; not WindowLag90",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag91 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski32_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(32);
    }
    s.powf(1.0 / 32.0)
}

/// Log Minkowski \(p=32\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{32})^{1/32}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski31Anomaly`] (\(p=31\)) and [`LogMinkowski30Anomaly`] (\(p=30\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski32Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski32Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski32Anomaly {
    /// Empty log Minkowski-\(p=32\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski32_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski32Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski32 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=32 k-NN anomaly",
                "LogMinkowski32Anomaly is L32 on |ln|; not L31, L30, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski32Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-92 windowed ninety-second-difference anomaly.
///
/// The 92nd backward difference is formed by successive first differences
/// (`C(92, 46)` is not an exact `f64` integer). Distinct from [`WindowLag91`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag92 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag92 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag92 {
    /// Empty windowed lag-92 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 92 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(92);
        let win = &xs[start..];
        if win.len() < 92 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..92 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag92 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 92;
        q.warmup = self.rows.len() < 92;
        q.explanation = format!("window-lag92 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-second-difference anomaly",
                "WindowLag92 uses successive first differences and cap 96; not WindowLag91",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag92 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski33_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(33);
    }
    s.powf(1.0 / 33.0)
}

/// Log Minkowski \(p=33\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{33})^{1/33}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski32Anomaly`] (\(p=32\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski33Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski33Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski33Anomaly {
    /// Empty log Minkowski-\(p=33\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski33_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski33Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski33 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=33 k-NN anomaly",
                "LogMinkowski33Anomaly is L33 on |ln|; not L32 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski33Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-93 windowed ninety-third-difference anomaly.
///
/// The 93rd backward difference is formed by successive first differences
/// (`C(93, 46)` is not an exact `f64` integer). Distinct from [`WindowLag92`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag93 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag93 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag93 {
    /// Empty windowed lag-93 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 93 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(93);
        let win = &xs[start..];
        if win.len() < 93 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..93 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag93 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 93;
        q.warmup = self.rows.len() < 93;
        q.explanation = format!("window-lag93 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-third-difference anomaly",
                "WindowLag93 uses successive first differences and cap 96; not WindowLag92",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag93 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski34_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(34);
    }
    s.powf(1.0 / 34.0)
}

/// Log Minkowski \(p=34\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{34})^{1/34}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski33Anomaly`] (\(p=33\)) and [`LogMinkowski32Anomaly`] (\(p=32\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski34Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski34Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski34Anomaly {
    /// Empty log Minkowski-\(p=34\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski34_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski34Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski34 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=34 k-NN anomaly",
                "LogMinkowski34Anomaly is L34 on |ln|; not L33, L32, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski34Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-94 windowed ninety-fourth-difference anomaly.
///
/// The 94th backward difference is formed by successive first differences
/// (`C(94, 47)` is not an exact `f64` integer). Distinct from [`WindowLag93`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag94 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag94 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag94 {
    /// Empty windowed lag-94 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 94 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(94);
        let win = &xs[start..];
        if win.len() < 94 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..94 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag94 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 94;
        q.warmup = self.rows.len() < 94;
        q.explanation = format!("window-lag94 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-fourth-difference anomaly",
                "WindowLag94 uses successive first differences and cap 96; not WindowLag93",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag94 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski35_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(35);
    }
    s.powf(1.0 / 35.0)
}

/// Log Minkowski \(p=35\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{35})^{1/35}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski34Anomaly`] (\(p=34\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski35Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski35Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski35Anomaly {
    /// Empty log Minkowski-\(p=35\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski35_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski35Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski35 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=35 k-NN anomaly",
                "LogMinkowski35Anomaly is L35 on |ln|; not L34 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski35Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-95 windowed ninety-fifth-difference anomaly.
///
/// The 95th backward difference is formed by successive first differences
/// (`C(95, 47)` is not an exact `f64` integer). Distinct from [`WindowLag94`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag95 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag95 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag95 {
    /// Empty windowed lag-95 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 95 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(95);
        let win = &xs[start..];
        if win.len() < 95 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..95 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag95 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 95;
        q.warmup = self.rows.len() < 95;
        q.explanation = format!("window-lag95 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-fifth-difference anomaly",
                "WindowLag95 uses successive first differences and cap 96; not WindowLag94",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag95 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski36_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(36);
    }
    s.powf(1.0 / 36.0)
}

/// Log Minkowski \(p=36\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{36})^{1/36}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski35Anomaly`] (\(p=35\)) and [`LogMinkowski34Anomaly`] (\(p=34\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski36Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski36Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski36Anomaly {
    /// Empty log Minkowski-\(p=36\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski36_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski36Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski36 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=36 k-NN anomaly",
                "LogMinkowski36Anomaly is L36 on |ln|; not L35, L34, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski36Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-96 windowed ninety-sixth-difference anomaly.
///
/// The 96th backward difference is formed by successive first differences
/// (`C(96, 48)` is not an exact `f64` integer). Distinct from [`WindowLag95`].
/// Window length is not identification `p`. Reservoir cap is 96.
#[derive(Clone, Debug)]
pub struct WindowLag96 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag96 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag96 {
    /// Empty windowed lag-96 detector (reservoir cap 96).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 96 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(96);
        let win = &xs[start..];
        if win.len() < 96 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..96 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag96 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 96 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 96;
        q.warmup = self.rows.len() < 96;
        q.explanation = format!("window-lag96 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-sixth-difference anomaly",
                "WindowLag96 uses successive first differences and cap 96; not WindowLag95",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag96 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski37_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(37);
    }
    s.powf(1.0 / 37.0)
}

/// Log Minkowski \(p=37\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{37})^{1/37}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski36Anomaly`] (\(p=36\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski37Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski37Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski37Anomaly {
    /// Empty log Minkowski-\(p=37\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski37_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski37Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski37 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=37 k-NN anomaly",
                "LogMinkowski37Anomaly is L37 on |ln|; not L36 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski37Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-97 windowed ninety-seventh-difference anomaly.
///
/// The 97th backward difference is formed by successive first differences
/// (`C(97, 48)` is not an exact `f64` integer). Distinct from [`WindowLag96`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag97 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag97 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag97 {
    /// Empty windowed lag-97 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 97 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(97);
        let win = &xs[start..];
        if win.len() < 97 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..97 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag97 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 97;
        q.warmup = self.rows.len() < 97;
        q.explanation = format!("window-lag97 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-seventh-difference anomaly",
                "WindowLag97 uses successive first differences and cap 112; not WindowLag96",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag97 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski38_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(38);
    }
    s.powf(1.0 / 38.0)
}

/// Log Minkowski \(p=38\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{38})^{1/38}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski37Anomaly`] (\(p=37\)) and [`LogMinkowski36Anomaly`] (\(p=36\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski38Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski38Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski38Anomaly {
    /// Empty log Minkowski-\(p=38\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski38_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski38Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski38 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=38 k-NN anomaly",
                "LogMinkowski38Anomaly is L38 on |ln|; not L37, L36, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski38Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-98 windowed ninety-eighth-difference anomaly.
///
/// The 98th backward difference is formed by successive first differences
/// (`C(98, 49)` is not an exact `f64` integer). Distinct from [`WindowLag97`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag98 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag98 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag98 {
    /// Empty windowed lag-98 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 98 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(98);
        let win = &xs[start..];
        if win.len() < 98 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..98 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag98 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 98;
        q.warmup = self.rows.len() < 98;
        q.explanation = format!("window-lag98 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-eighth-difference anomaly",
                "WindowLag98 uses successive first differences and cap 112; not WindowLag97",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag98 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski39_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(39);
    }
    s.powf(1.0 / 39.0)
}

/// Log Minkowski \(p=39\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{39})^{1/39}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski38Anomaly`] (\(p=38\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski39Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski39Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski39Anomaly {
    /// Empty log Minkowski-\(p=39\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski39_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski39Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski39 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=39 k-NN anomaly",
                "LogMinkowski39Anomaly is L39 on |ln|; not L38 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski39Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-99 windowed ninety-ninth-difference anomaly.
///
/// The 99th backward difference is formed by successive first differences
/// (`C(99, 49)` is not an exact `f64` integer). Distinct from [`WindowLag98`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag99 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag99 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag99 {
    /// Empty windowed lag-99 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 99 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(99);
        let win = &xs[start..];
        if win.len() < 99 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..99 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag99 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 99;
        q.warmup = self.rows.len() < 99;
        q.explanation = format!("window-lag99 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed ninety-ninth-difference anomaly",
                "WindowLag99 uses successive first differences and cap 112; not WindowLag98",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag99 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski40_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(40);
    }
    s.powf(1.0 / 40.0)
}

/// Log Minkowski \(p=40\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{40})^{1/40}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski39Anomaly`] (\(p=39\)) and [`LogMinkowski38Anomaly`] (\(p=38\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski40Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski40Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski40Anomaly {
    /// Empty log Minkowski-\(p=40\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski40_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski40Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski40 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=40 k-NN anomaly",
                "LogMinkowski40Anomaly is L40 on |ln|; not L39, L38, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski40Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-100 windowed one-hundredth-difference anomaly.
///
/// The 100th backward difference is formed by successive first differences
/// (`C(100, 50)` is not an exact `f64` integer). Distinct from [`WindowLag99`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag100 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag100 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag100 {
    /// Empty windowed lag-100 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 100 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(100);
        let win = &xs[start..];
        if win.len() < 100 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..100 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag100 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 100;
        q.warmup = self.rows.len() < 100;
        q.explanation = format!("window-lag100 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundredth-difference anomaly",
                "WindowLag100 uses successive first differences and cap 112; not WindowLag99",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag100 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski41_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(41);
    }
    s.powf(1.0 / 41.0)
}

/// Log Minkowski \(p=41\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{41})^{1/41}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski40Anomaly`] (\(p=40\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski41Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski41Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski41Anomaly {
    /// Empty log Minkowski-\(p=41\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski41_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski41Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski41 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=41 k-NN anomaly",
                "LogMinkowski41Anomaly is L41 on |ln|; not L40 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski41Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-101 windowed one-hundred-first-difference anomaly.
///
/// The 101st backward difference is formed by successive first differences
/// (`C(101, 50)` is not an exact `f64` integer). Distinct from [`WindowLag100`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag101 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag101 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag101 {
    /// Empty windowed lag-101 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 101 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(101);
        let win = &xs[start..];
        if win.len() < 101 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..101 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag101 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 101;
        q.warmup = self.rows.len() < 101;
        q.explanation = format!("window-lag101 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-first-difference anomaly",
                "WindowLag101 uses successive first differences and cap 112; not WindowLag100",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag101 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski42_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(42);
    }
    s.powf(1.0 / 42.0)
}

/// Log Minkowski \(p=42\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{42})^{1/42}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski41Anomaly`] (\(p=41\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski42Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski42Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski42Anomaly {
    /// Empty log Minkowski-\(p=42\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski42_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski42Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski42 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=42 k-NN anomaly",
                "LogMinkowski42Anomaly is L42 on |ln|; not L41 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski42Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-102 windowed one-hundred-second-difference anomaly.
///
/// The 102nd backward difference is formed by successive first differences
/// (`C(102, 51)` is not an exact `f64` integer). Distinct from [`WindowLag101`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag102 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag102 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag102 {
    /// Empty windowed lag-102 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 102 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(102);
        let win = &xs[start..];
        if win.len() < 102 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..102 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag102 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 102;
        q.warmup = self.rows.len() < 102;
        q.explanation = format!("window-lag102 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-second-difference anomaly",
                "WindowLag102 uses successive first differences and cap 112; not WindowLag101",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag102 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski43_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(43);
    }
    s.powf(1.0 / 43.0)
}

/// Log Minkowski \(p=43\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{43})^{1/43}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski42Anomaly`] (\(p=42\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski43Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski43Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski43Anomaly {
    /// Empty log Minkowski-\(p=43\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski43_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski43Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski43 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=43 k-NN anomaly",
                "LogMinkowski43Anomaly is L43 on |ln|; not L42 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski43Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-103 windowed one-hundred-third-difference anomaly.
///
/// The 103rd backward difference is formed by successive first differences
/// (`C(103, 51)` is not an exact `f64` integer). Distinct from [`WindowLag102`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag103 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag103 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag103 {
    /// Empty windowed lag-103 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 103 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(103);
        let win = &xs[start..];
        if win.len() < 103 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..103 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag103 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 103;
        q.warmup = self.rows.len() < 103;
        q.explanation = format!("window-lag103 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-third-difference anomaly",
                "WindowLag103 uses successive first differences and cap 112; not WindowLag102",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag103 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski44_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(44);
    }
    s.powf(1.0 / 44.0)
}

/// Log Minkowski \(p=44\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{44})^{1/44}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski43Anomaly`] (\(p=43\)) and [`LogMinkowski42Anomaly`] (\(p=42\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski44Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski44Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski44Anomaly {
    /// Empty log Minkowski-\(p=44\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski44_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski44Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski44 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=44 k-NN anomaly",
                "LogMinkowski44Anomaly is L44 on |ln|; not L43, L42, or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski44Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-104 windowed one-hundred-fourth-difference anomaly.
///
/// The 104th backward difference is formed by successive first differences
/// (`C(104, 52)` is not an exact `f64` integer). Distinct from [`WindowLag103`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag104 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag104 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag104 {
    /// Empty windowed lag-104 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 104 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(104);
        let win = &xs[start..];
        if win.len() < 104 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..104 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag104 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 104;
        q.warmup = self.rows.len() < 104;
        q.explanation = format!("window-lag104 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fourth-difference anomaly",
                "WindowLag104 uses successive first differences and cap 112; not WindowLag103",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag104 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski45_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(45);
    }
    s.powf(1.0 / 45.0)
}

/// Log Minkowski \(p=45\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{45})^{1/45}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski44Anomaly`] (\(p=44\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski45Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski45Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski45Anomaly {
    /// Empty log Minkowski-\(p=45\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski45_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski45Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski45 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=45 k-NN anomaly",
                "LogMinkowski45Anomaly is L45 on |ln|; not L44 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski45Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-105 windowed one-hundred-fifth-difference anomaly.
///
/// The 105th backward difference is formed by successive first differences
/// (`C(105, 52)` is not an exact `f64` integer). Distinct from [`WindowLag104`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag105 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag105 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag105 {
    /// Empty windowed lag-105 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 105 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(105);
        let win = &xs[start..];
        if win.len() < 105 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..105 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag105 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 105;
        q.warmup = self.rows.len() < 105;
        q.explanation = format!("window-lag105 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifth-difference anomaly",
                "WindowLag105 uses successive first differences and cap 112; not WindowLag104",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag105 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski46_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(46);
    }
    s.powf(1.0 / 46.0)
}

/// Log Minkowski \(p=46\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{46})^{1/46}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski45Anomaly`] (\(p=45\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski46Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski46Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski46Anomaly {
    /// Empty log Minkowski-\(p=46\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski46_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski46Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski46 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=46 k-NN anomaly",
                "LogMinkowski46Anomaly is L46 on |ln|; not L45 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski46Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-106 windowed one-hundred-sixth-difference anomaly.
///
/// The 106th backward difference is formed by successive first differences
/// (`C(106, 53)` is not an exact `f64` integer). Distinct from [`WindowLag105`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag106 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag106 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag106 {
    /// Empty windowed lag-106 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 106 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(106);
        let win = &xs[start..];
        if win.len() < 106 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..106 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag106 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 106;
        q.warmup = self.rows.len() < 106;
        q.explanation = format!("window-lag106 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixth-difference anomaly",
                "WindowLag106 uses successive first differences and cap 112; not WindowLag105",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag106 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski47_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(47);
    }
    s.powf(1.0 / 47.0)
}

/// Log Minkowski \(p=47\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{47})^{1/47}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski46Anomaly`] (\(p=46\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski47Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski47Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski47Anomaly {
    /// Empty log Minkowski-\(p=47\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski47_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski47Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski47 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=47 k-NN anomaly",
                "LogMinkowski47Anomaly is L47 on |ln|; not L46 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski47Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-107 windowed one-hundred-seventh-difference anomaly.
///
/// The 107th backward difference is formed by successive first differences
/// (`C(107, 53)` is not an exact `f64` integer). Distinct from [`WindowLag106`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag107 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag107 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag107 {
    /// Empty windowed lag-107 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 107 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(107);
        let win = &xs[start..];
        if win.len() < 107 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..107 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag107 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 107;
        q.warmup = self.rows.len() < 107;
        q.explanation = format!("window-lag107 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventh-difference anomaly",
                "WindowLag107 uses successive first differences and cap 112; not WindowLag106",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag107 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski48_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(48);
    }
    s.powf(1.0 / 48.0)
}

/// Log Minkowski \(p=48\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{48})^{1/48}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski47Anomaly`] (\(p=47\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski48Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski48Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski48Anomaly {
    /// Empty log Minkowski-\(p=48\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski48_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski48Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski48 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=48 k-NN anomaly",
                "LogMinkowski48Anomaly is L48 on |ln|; not L47 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski48Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-108 windowed one-hundred-eighth-difference anomaly.
///
/// The 108th backward difference is formed by successive first differences
/// (`C(108, 54)` is not an exact `f64` integer). Distinct from [`WindowLag107`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag108 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag108 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag108 {
    /// Empty windowed lag-108 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 108 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(108);
        let win = &xs[start..];
        if win.len() < 108 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..108 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag108 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 108;
        q.warmup = self.rows.len() < 108;
        q.explanation = format!("window-lag108 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighth-difference anomaly",
                "WindowLag108 uses successive first differences and cap 112; not WindowLag107",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag108 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski49_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(49);
    }
    s.powf(1.0 / 49.0)
}

/// Log Minkowski \(p=49\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{49})^{1/49}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski48Anomaly`] (\(p=48\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski49Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski49Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski49Anomaly {
    /// Empty log Minkowski-\(p=49\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski49_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski49Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski49 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=49 k-NN anomaly",
                "LogMinkowski49Anomaly is L49 on |ln|; not L48 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski49Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-109 windowed one-hundred-ninth-difference anomaly.
///
/// The 109th backward difference is formed by successive first differences
/// (`C(109, 54)` is not an exact `f64` integer). Distinct from [`WindowLag108`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag109 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag109 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag109 {
    /// Empty windowed lag-109 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 109 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(109);
        let win = &xs[start..];
        if win.len() < 109 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..109 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag109 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 109;
        q.warmup = self.rows.len() < 109;
        q.explanation = format!("window-lag109 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninth-difference anomaly",
                "WindowLag109 uses successive first differences and cap 112; not WindowLag108",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag109 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski50_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(50);
    }
    s.powf(1.0 / 50.0)
}

/// Log Minkowski \(p=50\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{50})^{1/50}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski49Anomaly`] (\(p=49\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski50Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski50Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski50Anomaly {
    /// Empty log Minkowski-\(p=50\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski50_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski50Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski50 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=50 k-NN anomaly",
                "LogMinkowski50Anomaly is L50 on |ln|; not L49 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski50Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-110 windowed one-hundred-tenth-difference anomaly.
///
/// The 110th backward difference is formed by successive first differences
/// (`C(110, 55)` is not an exact `f64` integer). Distinct from [`WindowLag109`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag110 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag110 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag110 {
    /// Empty windowed lag-110 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 110 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(110);
        let win = &xs[start..];
        if win.len() < 110 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..110 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag110 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 110;
        q.warmup = self.rows.len() < 110;
        q.explanation = format!("window-lag110 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-tenth-difference anomaly",
                "WindowLag110 uses successive first differences and cap 112; not WindowLag109",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag110 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski51_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(51);
    }
    s.powf(1.0 / 51.0)
}

/// Log Minkowski \(p=51\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{51})^{1/51}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski50Anomaly`] (\(p=50\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski51Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski51Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski51Anomaly {
    /// Empty log Minkowski-\(p=51\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski51_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski51Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski51 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=51 k-NN anomaly",
                "LogMinkowski51Anomaly is L51 on |ln|; not L50 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski51Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-111 windowed one-hundred-eleventh-difference anomaly.
///
/// The 111th backward difference is formed by successive first differences
/// (`C(111, 55)` is not an exact `f64` integer). Distinct from [`WindowLag110`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag111 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag111 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag111 {
    /// Empty windowed lag-111 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 111 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(111);
        let win = &xs[start..];
        if win.len() < 111 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..111 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag111 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 111;
        q.warmup = self.rows.len() < 111;
        q.explanation = format!("window-lag111 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eleventh-difference anomaly",
                "WindowLag111 uses successive first differences and cap 112; not WindowLag110",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag111 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski52_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(52);
    }
    s.powf(1.0 / 52.0)
}

/// Log Minkowski \(p=52\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{52})^{1/52}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski51Anomaly`] (\(p=51\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski52Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski52Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski52Anomaly {
    /// Empty log Minkowski-\(p=52\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski52_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski52Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski52 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=52 k-NN anomaly",
                "LogMinkowski52Anomaly is L52 on |ln|; not L51 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski52Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-112 windowed one-hundred-twelfth-difference anomaly.
///
/// The 112th backward difference is formed by successive first differences
/// (`C(112, 56)` is not an exact `f64` integer). Distinct from [`WindowLag111`].
/// Window length is not identification `p`. Reservoir cap is 112.
#[derive(Clone, Debug)]
pub struct WindowLag112 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag112 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag112 {
    /// Empty windowed lag-112 detector (reservoir cap 112).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 112 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(112);
        let win = &xs[start..];
        if win.len() < 112 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..112 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag112 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 112 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 112;
        q.warmup = self.rows.len() < 112;
        q.explanation = format!("window-lag112 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twelfth-difference anomaly",
                "WindowLag112 uses successive first differences and cap 112; not WindowLag111",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag112 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski53_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(53);
    }
    s.powf(1.0 / 53.0)
}

/// Log Minkowski \(p=53\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{53})^{1/53}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski52Anomaly`] (\(p=52\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski53Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski53Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski53Anomaly {
    /// Empty log Minkowski-\(p=53\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski53_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski53Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski53 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=53 k-NN anomaly",
                "LogMinkowski53Anomaly is L53 on |ln|; not L52 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski53Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-113 windowed one-hundred-thirteenth-difference anomaly.
///
/// The 113th backward difference is formed by successive first differences
/// (`C(113, 56)` is not an exact `f64` integer). Distinct from [`WindowLag112`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag113 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag113 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag113 {
    /// Empty windowed lag-113 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 113 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(113);
        let win = &xs[start..];
        if win.len() < 113 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..113 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag113 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 113;
        q.warmup = self.rows.len() < 113;
        q.explanation = format!("window-lag113 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirteenth-difference anomaly",
                "WindowLag113 uses successive first differences and cap 128; not WindowLag112",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag113 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski54_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(54);
    }
    s.powf(1.0 / 54.0)
}

/// Log Minkowski \(p=54\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{54})^{1/54}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski53Anomaly`] (\(p=53\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski54Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski54Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski54Anomaly {
    /// Empty log Minkowski-\(p=54\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski54_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski54Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski54 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=54 k-NN anomaly",
                "LogMinkowski54Anomaly is L54 on |ln|; not L53 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski54Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-114 windowed one-hundred-fourteenth-difference anomaly.
///
/// The 114th backward difference is formed by successive first differences
/// (`C(114, 57)` is not an exact `f64` integer). Distinct from [`WindowLag113`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag114 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag114 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag114 {
    /// Empty windowed lag-114 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 114 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(114);
        let win = &xs[start..];
        if win.len() < 114 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..114 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag114 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 114;
        q.warmup = self.rows.len() < 114;
        q.explanation = format!("window-lag114 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fourteenth-difference anomaly",
                "WindowLag114 uses successive first differences and cap 128; not WindowLag113",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag114 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski55_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(55);
    }
    s.powf(1.0 / 55.0)
}

/// Log Minkowski \(p=55\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{55})^{1/55}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski54Anomaly`] (\(p=54\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski55Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski55Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski55Anomaly {
    /// Empty log Minkowski-\(p=55\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski55_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski55Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski55 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=55 k-NN anomaly",
                "LogMinkowski55Anomaly is L55 on |ln|; not L54 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski55Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-115 windowed one-hundred-fifteenth-difference anomaly.
///
/// The 115th backward difference is formed by successive first differences
/// (`C(115, 57)` is not an exact `f64` integer). Distinct from [`WindowLag114`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag115 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag115 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag115 {
    /// Empty windowed lag-115 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 115 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(115);
        let win = &xs[start..];
        if win.len() < 115 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..115 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag115 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 115;
        q.warmup = self.rows.len() < 115;
        q.explanation = format!("window-lag115 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifteenth-difference anomaly",
                "WindowLag115 uses successive first differences and cap 128; not WindowLag114",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag115 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski56_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(56);
    }
    s.powf(1.0 / 56.0)
}

/// Log Minkowski \(p=56\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{56})^{1/56}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski55Anomaly`] (\(p=55\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski56Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski56Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski56Anomaly {
    /// Empty log Minkowski-\(p=56\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski56_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski56Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski56 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=56 k-NN anomaly",
                "LogMinkowski56Anomaly is L56 on |ln|; not L55 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski56Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-116 windowed one-hundred-sixteenth-difference anomaly.
///
/// The 116th backward difference is formed by successive first differences
/// (`C(116, 58)` is not an exact `f64` integer). Distinct from [`WindowLag115`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag116 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag116 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag116 {
    /// Empty windowed lag-116 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 116 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(116);
        let win = &xs[start..];
        if win.len() < 116 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..116 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag116 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 116;
        q.warmup = self.rows.len() < 116;
        q.explanation = format!("window-lag116 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixteenth-difference anomaly",
                "WindowLag116 uses successive first differences and cap 128; not WindowLag115",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag116 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski57_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(57);
    }
    s.powf(1.0 / 57.0)
}

/// Log Minkowski \(p=57\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{57})^{1/57}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski56Anomaly`] (\(p=56\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski57Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski57Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski57Anomaly {
    /// Empty log Minkowski-\(p=57\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski57_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski57Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski57 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=57 k-NN anomaly",
                "LogMinkowski57Anomaly is L57 on |ln|; not L56 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski57Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-117 windowed one-hundred-seventeenth-difference anomaly.
///
/// The 117th backward difference is formed by successive first differences
/// (`C(117, 58)` is not an exact `f64` integer). Distinct from [`WindowLag116`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag117 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag117 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag117 {
    /// Empty windowed lag-117 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 117 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(117);
        let win = &xs[start..];
        if win.len() < 117 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..117 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag117 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 117;
        q.warmup = self.rows.len() < 117;
        q.explanation = format!("window-lag117 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventeenth-difference anomaly",
                "WindowLag117 uses successive first differences and cap 128; not WindowLag116",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag117 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski58_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(58);
    }
    s.powf(1.0 / 58.0)
}

/// Log Minkowski \(p=58\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{58})^{1/58}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski57Anomaly`] (\(p=57\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski58Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski58Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski58Anomaly {
    /// Empty log Minkowski-\(p=58\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski58_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski58Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski58 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=58 k-NN anomaly",
                "LogMinkowski58Anomaly is L58 on |ln|; not L57 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski58Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-118 windowed one-hundred-eighteenth-difference anomaly.
///
/// The 118th backward difference is formed by successive first differences
/// (`C(118, 59)` is not an exact `f64` integer). Distinct from [`WindowLag117`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag118 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag118 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag118 {
    /// Empty windowed lag-118 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 118 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(118);
        let win = &xs[start..];
        if win.len() < 118 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..118 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag118 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 118;
        q.warmup = self.rows.len() < 118;
        q.explanation = format!("window-lag118 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighteenth-difference anomaly",
                "WindowLag118 uses successive first differences and cap 128; not WindowLag117",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag118 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski59_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(59);
    }
    s.powf(1.0 / 59.0)
}

/// Log Minkowski \(p=59\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{59})^{1/59}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski58Anomaly`] (\(p=58\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski59Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski59Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski59Anomaly {
    /// Empty log Minkowski-\(p=59\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski59_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski59Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski59 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=59 k-NN anomaly",
                "LogMinkowski59Anomaly is L59 on |ln|; not L58 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski59Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-119 windowed one-hundred-nineteenth-difference anomaly.
///
/// The 119th backward difference is formed by successive first differences
/// (`C(119, 59)` is not an exact `f64` integer). Distinct from [`WindowLag118`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag119 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag119 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag119 {
    /// Empty windowed lag-119 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 119 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(119);
        let win = &xs[start..];
        if win.len() < 119 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..119 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag119 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 119;
        q.warmup = self.rows.len() < 119;
        q.explanation = format!("window-lag119 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-nineteenth-difference anomaly",
                "WindowLag119 uses successive first differences and cap 128; not WindowLag118",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag119 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski60_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(60);
    }
    s.powf(1.0 / 60.0)
}

/// Log Minkowski \(p=60\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{60})^{1/60}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski59Anomaly`] (\(p=59\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski60Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski60Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski60Anomaly {
    /// Empty log Minkowski-\(p=60\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski60_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski60Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski60 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=60 k-NN anomaly",
                "LogMinkowski60Anomaly is L60 on |ln|; not L59 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski60Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-120 windowed one-hundred-twentieth-difference anomaly.
///
/// The 120th backward difference is formed by successive first differences
/// (`C(120, 60)` is not an exact `f64` integer). Distinct from [`WindowLag119`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag120 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag120 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag120 {
    /// Empty windowed lag-120 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 120 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(120);
        let win = &xs[start..];
        if win.len() < 120 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..120 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag120 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 120;
        q.warmup = self.rows.len() < 120;
        q.explanation = format!("window-lag120 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twentieth-difference anomaly",
                "WindowLag120 uses successive first differences and cap 128; not WindowLag119",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag120 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski61_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(61);
    }
    s.powf(1.0 / 61.0)
}

/// Log Minkowski \(p=61\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{61})^{1/61}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski60Anomaly`] (\(p=60\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski61Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski61Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski61Anomaly {
    /// Empty log Minkowski-\(p=61\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski61_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski61Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski61 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=61 k-NN anomaly",
                "LogMinkowski61Anomaly is L61 on |ln|; not L60 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski61Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-121 windowed one-hundred-twenty-first-difference anomaly.
///
/// The 121st backward difference is formed by successive first differences
/// (`C(121, 60)` is not an exact `f64` integer). Distinct from [`WindowLag120`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag121 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag121 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag121 {
    /// Empty windowed lag-121 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 121 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(121);
        let win = &xs[start..];
        if win.len() < 121 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..121 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag121 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 121;
        q.warmup = self.rows.len() < 121;
        q.explanation = format!("window-lag121 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-first-difference anomaly",
                "WindowLag121 uses successive first differences and cap 128; not WindowLag120",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag121 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski62_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(62);
    }
    s.powf(1.0 / 62.0)
}

/// Log Minkowski \(p=62\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{62})^{1/62}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski61Anomaly`] (\(p=61\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski62Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski62Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski62Anomaly {
    /// Empty log Minkowski-\(p=62\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski62_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski62Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski62 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=62 k-NN anomaly",
                "LogMinkowski62Anomaly is L62 on |ln|; not L61 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski62Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-122 windowed one-hundred-twenty-second-difference anomaly.
///
/// The 122nd backward difference is formed by successive first differences
/// (`C(122, 61)` is not an exact `f64` integer). Distinct from [`WindowLag121`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag122 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag122 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag122 {
    /// Empty windowed lag-122 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 122 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(122);
        let win = &xs[start..];
        if win.len() < 122 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..122 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag122 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 122;
        q.warmup = self.rows.len() < 122;
        q.explanation = format!("window-lag122 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-second-difference anomaly",
                "WindowLag122 uses successive first differences and cap 128; not WindowLag121",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag122 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski63_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(63);
    }
    s.powf(1.0 / 63.0)
}

/// Log Minkowski \(p=63\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{63})^{1/63}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski62Anomaly`] (\(p=62\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski63Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski63Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski63Anomaly {
    /// Empty log Minkowski-\(p=63\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski63_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski63Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski63 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=63 k-NN anomaly",
                "LogMinkowski63Anomaly is L63 on |ln|; not L62 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski63Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-123 windowed one-hundred-twenty-third-difference anomaly.
///
/// The 123rd backward difference is formed by successive first differences
/// (`C(123, 61)` is not an exact `f64` integer). Distinct from [`WindowLag122`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag123 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag123 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag123 {
    /// Empty windowed lag-123 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 123 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(123);
        let win = &xs[start..];
        if win.len() < 123 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..123 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag123 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 123;
        q.warmup = self.rows.len() < 123;
        q.explanation = format!("window-lag123 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-third-difference anomaly",
                "WindowLag123 uses successive first differences and cap 128; not WindowLag122",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag123 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski64_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(64);
    }
    s.powf(1.0 / 64.0)
}

/// Log Minkowski \(p=64\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{64})^{1/64}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski63Anomaly`] (\(p=63\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski64Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski64Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski64Anomaly {
    /// Empty log Minkowski-\(p=64\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski64_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski64Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski64 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=64 k-NN anomaly",
                "LogMinkowski64Anomaly is L64 on |ln|; not L63 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski64Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-124 windowed one-hundred-twenty-fourth-difference anomaly.
///
/// The 124th backward difference is formed by successive first differences
/// (`C(124, 62)` is not an exact `f64` integer). Distinct from [`WindowLag123`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag124 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag124 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag124 {
    /// Empty windowed lag-124 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 124 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(124);
        let win = &xs[start..];
        if win.len() < 124 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..124 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag124 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 124;
        q.warmup = self.rows.len() < 124;
        q.explanation = format!("window-lag124 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-fourth-difference anomaly",
                "WindowLag124 uses successive first differences and cap 128; not WindowLag123",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag124 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski65_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(65);
    }
    s.powf(1.0 / 65.0)
}

/// Log Minkowski \(p=65\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{65})^{1/65}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski64Anomaly`] (\(p=64\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski65Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski65Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski65Anomaly {
    /// Empty log Minkowski-\(p=65\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski65_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski65Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski65 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=65 k-NN anomaly",
                "LogMinkowski65Anomaly is L65 on |ln|; not L64 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski65Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-125 windowed one-hundred-twenty-fifth-difference anomaly.
///
/// The 125th backward difference is formed by successive first differences
/// (`C(125, 62)` is not an exact `f64` integer). Distinct from [`WindowLag124`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag125 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag125 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag125 {
    /// Empty windowed lag-125 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 125 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(125);
        let win = &xs[start..];
        if win.len() < 125 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..125 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag125 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 125;
        q.warmup = self.rows.len() < 125;
        q.explanation = format!("window-lag125 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-fifth-difference anomaly",
                "WindowLag125 uses successive first differences and cap 128; not WindowLag124",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag125 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski66_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(66);
    }
    s.powf(1.0 / 66.0)
}

/// Log Minkowski \(p=66\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{66})^{1/66}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski65Anomaly`] (\(p=65\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski66Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski66Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski66Anomaly {
    /// Empty log Minkowski-\(p=66\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski66_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski66Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski66 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=66 k-NN anomaly",
                "LogMinkowski66Anomaly is L66 on |ln|; not L65 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski66Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-126 windowed one-hundred-twenty-sixth-difference anomaly.
///
/// The 126th backward difference is formed by successive first differences
/// (`C(126, 63)` is not an exact `f64` integer). Distinct from [`WindowLag125`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag126 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag126 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag126 {
    /// Empty windowed lag-126 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 126 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(126);
        let win = &xs[start..];
        if win.len() < 126 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..126 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag126 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 126;
        q.warmup = self.rows.len() < 126;
        q.explanation = format!("window-lag126 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-sixth-difference anomaly",
                "WindowLag126 uses successive first differences and cap 128; not WindowLag125",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag126 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski67_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(67);
    }
    s.powf(1.0 / 67.0)
}

/// Log Minkowski \(p=67\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{67})^{1/67}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski66Anomaly`] (\(p=66\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski67Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski67Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski67Anomaly {
    /// Empty log Minkowski-\(p=67\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski67_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski67Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski67 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=67 k-NN anomaly",
                "LogMinkowski67Anomaly is L67 on |ln|; not L66 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski67Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-127 windowed one-hundred-twenty-seventh-difference anomaly.
///
/// The 127th backward difference is formed by successive first differences
/// (`C(127, 63)` is not an exact `f64` integer). Distinct from [`WindowLag126`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag127 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag127 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag127 {
    /// Empty windowed lag-127 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 127 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(127);
        let win = &xs[start..];
        if win.len() < 127 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..127 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag127 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 127;
        q.warmup = self.rows.len() < 127;
        q.explanation = format!("window-lag127 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-seventh-difference anomaly",
                "WindowLag127 uses successive first differences and cap 128; not WindowLag126",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag127 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski68_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(68);
    }
    s.powf(1.0 / 68.0)
}

/// Log Minkowski \(p=68\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{68})^{1/68}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski67Anomaly`] (\(p=67\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski68Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski68Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski68Anomaly {
    /// Empty log Minkowski-\(p=68\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski68_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski68Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski68 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=68 k-NN anomaly",
                "LogMinkowski68Anomaly is L68 on |ln|; not L67 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski68Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-128 windowed one-hundred-twenty-eighth-difference anomaly.
///
/// The 128th backward difference is formed by successive first differences
/// (`C(128, 64)` is not an exact `f64` integer). Distinct from [`WindowLag127`].
/// Window length is not identification `p`. Reservoir cap is 128.
#[derive(Clone, Debug)]
pub struct WindowLag128 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag128 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag128 {
    /// Empty windowed lag-128 detector (reservoir cap 128).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 128 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(128);
        let win = &xs[start..];
        if win.len() < 128 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..128 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag128 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 128 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 128;
        q.warmup = self.rows.len() < 128;
        q.explanation = format!("window-lag128 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-eighth-difference anomaly",
                "WindowLag128 uses successive first differences and cap 128; not WindowLag127",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag128 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski69_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(69);
    }
    s.powf(1.0 / 69.0)
}

/// Log Minkowski \(p=69\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{69})^{1/69}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski68Anomaly`] (\(p=68\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski69Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski69Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski69Anomaly {
    /// Empty log Minkowski-\(p=69\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski69_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski69Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski69 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=69 k-NN anomaly",
                "LogMinkowski69Anomaly is L69 on |ln|; not L68 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski69Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-129 windowed one-hundred-twenty-ninth-difference anomaly.
///
/// The 129th backward difference is formed by successive first differences
/// (`C(129, 64)` is not an exact `f64` integer). Distinct from [`WindowLag128`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag129 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag129 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag129 {
    /// Empty windowed lag-129 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 129 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(129);
        let win = &xs[start..];
        if win.len() < 129 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..129 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag129 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 129;
        q.warmup = self.rows.len() < 129;
        q.explanation = format!("window-lag129 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-twenty-ninth-difference anomaly",
                "WindowLag129 uses successive first differences and cap 144; not WindowLag128",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag129 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski70_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(70);
    }
    s.powf(1.0 / 70.0)
}

/// Log Minkowski \(p=70\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{70})^{1/70}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski69Anomaly`] (\(p=69\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski70Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski70Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski70Anomaly {
    /// Empty log Minkowski-\(p=70\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski70_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski70Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski70 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=70 k-NN anomaly",
                "LogMinkowski70Anomaly is L70 on |ln|; not L69 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski70Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-130 windowed one-hundred-thirtieth-difference anomaly.
///
/// The 130th backward difference is formed by successive first differences
/// (`C(130, 65)` is not an exact `f64` integer). Distinct from [`WindowLag129`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag130 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag130 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag130 {
    /// Empty windowed lag-130 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 130 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(130);
        let win = &xs[start..];
        if win.len() < 130 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..130 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag130 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 130;
        q.warmup = self.rows.len() < 130;
        q.explanation = format!("window-lag130 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirtieth-difference anomaly",
                "WindowLag130 uses successive first differences and cap 144; not WindowLag129",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag130 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski71_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(71);
    }
    s.powf(1.0 / 71.0)
}

/// Log Minkowski \(p=71\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{71})^{1/71}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski70Anomaly`] (\(p=70\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski71Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski71Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski71Anomaly {
    /// Empty log Minkowski-\(p=71\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski71_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski71Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski71 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=71 k-NN anomaly",
                "LogMinkowski71Anomaly is L71 on |ln|; not L70 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski71Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-131 windowed one-hundred-thirty-first-difference anomaly.
///
/// The 131st backward difference is formed by successive first differences
/// (`C(131, 65)` is not an exact `f64` integer). Distinct from [`WindowLag130`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag131 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag131 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag131 {
    /// Empty windowed lag-131 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 131 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(131);
        let win = &xs[start..];
        if win.len() < 131 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..131 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag131 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 131;
        q.warmup = self.rows.len() < 131;
        q.explanation = format!("window-lag131 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-first-difference anomaly",
                "WindowLag131 uses successive first differences and cap 144; not WindowLag130",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag131 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski72_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(72);
    }
    s.powf(1.0 / 72.0)
}

/// Log Minkowski \(p=72\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{72})^{1/72}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski71Anomaly`] (\(p=71\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski72Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski72Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski72Anomaly {
    /// Empty log Minkowski-\(p=72\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski72_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski72Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski72 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=72 k-NN anomaly",
                "LogMinkowski72Anomaly is L72 on |ln|; not L71 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski72Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-132 windowed one-hundred-thirty-second-difference anomaly.
///
/// The 132nd backward difference is formed by successive first differences
/// (`C(132, 66)` is not an exact `f64` integer). Distinct from [`WindowLag131`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag132 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag132 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag132 {
    /// Empty windowed lag-132 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 132 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(132);
        let win = &xs[start..];
        if win.len() < 132 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..132 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag132 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 132;
        q.warmup = self.rows.len() < 132;
        q.explanation = format!("window-lag132 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-second-difference anomaly",
                "WindowLag132 uses successive first differences and cap 144; not WindowLag131",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag132 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski73_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(73);
    }
    s.powf(1.0 / 73.0)
}

/// Log Minkowski \(p=73\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{73})^{1/73}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski72Anomaly`] (\(p=72\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski73Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski73Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski73Anomaly {
    /// Empty log Minkowski-\(p=73\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski73_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski73Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski73 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=73 k-NN anomaly",
                "LogMinkowski73Anomaly is L73 on |ln|; not L72 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski73Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-133 windowed one-hundred-thirty-third-difference anomaly.
///
/// The 133rd backward difference is formed by successive first differences
/// (`C(133, 66)` is not an exact `f64` integer). Distinct from [`WindowLag132`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag133 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag133 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag133 {
    /// Empty windowed lag-133 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 133 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(133);
        let win = &xs[start..];
        if win.len() < 133 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..133 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag133 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 133;
        q.warmup = self.rows.len() < 133;
        q.explanation = format!("window-lag133 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-third-difference anomaly",
                "WindowLag133 uses successive first differences and cap 144; not WindowLag132",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag133 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski74_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(74);
    }
    s.powf(1.0 / 74.0)
}

/// Log Minkowski \(p=74\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{74})^{1/74}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski73Anomaly`] (\(p=73\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski74Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski74Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski74Anomaly {
    /// Empty log Minkowski-\(p=74\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski74_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski74Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski74 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=74 k-NN anomaly",
                "LogMinkowski74Anomaly is L74 on |ln|; not L73 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski74Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-134 windowed one-hundred-thirty-fourth-difference anomaly.
///
/// The 134th backward difference is formed by successive first differences
/// (`C(134, 67)` is not an exact `f64` integer). Distinct from [`WindowLag133`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag134 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag134 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag134 {
    /// Empty windowed lag-134 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 134 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(134);
        let win = &xs[start..];
        if win.len() < 134 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..134 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag134 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 134;
        q.warmup = self.rows.len() < 134;
        q.explanation = format!("window-lag134 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-fourth-difference anomaly",
                "WindowLag134 uses successive first differences and cap 144; not WindowLag133",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag134 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski75_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(75);
    }
    s.powf(1.0 / 75.0)
}

/// Log Minkowski \(p=75\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{75})^{1/75}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski74Anomaly`] (\(p=74\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski75Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski75Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski75Anomaly {
    /// Empty log Minkowski-\(p=75\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski75_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski75Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski75 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=75 k-NN anomaly",
                "LogMinkowski75Anomaly is L75 on |ln|; not L74 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski75Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-135 windowed one-hundred-thirty-fifth-difference anomaly.
///
/// The 135th backward difference is formed by successive first differences
/// (`C(135, 67)` is not an exact `f64` integer). Distinct from [`WindowLag134`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag135 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag135 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag135 {
    /// Empty windowed lag-135 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 135 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(135);
        let win = &xs[start..];
        if win.len() < 135 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..135 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag135 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 135;
        q.warmup = self.rows.len() < 135;
        q.explanation = format!("window-lag135 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-fifth-difference anomaly",
                "WindowLag135 uses successive first differences and cap 144; not WindowLag134",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag135 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski76_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(76);
    }
    s.powf(1.0 / 76.0)
}

/// Log Minkowski \(p=76\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{76})^{1/76}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski75Anomaly`] (\(p=75\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski76Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski76Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski76Anomaly {
    /// Empty log Minkowski-\(p=76\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski76_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski76Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski76 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=76 k-NN anomaly",
                "LogMinkowski76Anomaly is L76 on |ln|; not L75 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski76Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-136 windowed one-hundred-thirty-sixth-difference anomaly.
///
/// The 136th backward difference is formed by successive first differences
/// (`C(136, 68)` is not an exact `f64` integer). Distinct from [`WindowLag135`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag136 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag136 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag136 {
    /// Empty windowed lag-136 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 136 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(136);
        let win = &xs[start..];
        if win.len() < 136 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..136 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag136 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 136;
        q.warmup = self.rows.len() < 136;
        q.explanation = format!("window-lag136 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-sixth-difference anomaly",
                "WindowLag136 uses successive first differences and cap 144; not WindowLag135",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag136 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski77_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(77);
    }
    s.powf(1.0 / 77.0)
}

/// Log Minkowski \(p=77\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{77})^{1/77}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski76Anomaly`] (\(p=76\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski77Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski77Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski77Anomaly {
    /// Empty log Minkowski-\(p=77\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski77_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski77Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski77 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=77 k-NN anomaly",
                "LogMinkowski77Anomaly is L77 on |ln|; not L76 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski77Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-137 windowed one-hundred-thirty-seventh-difference anomaly.
///
/// The 137th backward difference is formed by successive first differences
/// (`C(137, 68)` is not an exact `f64` integer). Distinct from [`WindowLag136`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag137 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag137 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag137 {
    /// Empty windowed lag-137 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 137 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(137);
        let win = &xs[start..];
        if win.len() < 137 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..137 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag137 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 137;
        q.warmup = self.rows.len() < 137;
        q.explanation = format!("window-lag137 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-seventh-difference anomaly",
                "WindowLag137 uses successive first differences and cap 144; not WindowLag136",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag137 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski78_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(78);
    }
    s.powf(1.0 / 78.0)
}

/// Log Minkowski \(p=78\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{78})^{1/78}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski77Anomaly`] (\(p=77\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski78Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski78Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski78Anomaly {
    /// Empty log Minkowski-\(p=78\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski78_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski78Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski78 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=78 k-NN anomaly",
                "LogMinkowski78Anomaly is L78 on |ln|; not L77 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski78Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-138 windowed one-hundred-thirty-eighth-difference anomaly.
///
/// The 138th backward difference is formed by successive first differences
/// (`C(138, 69)` is not an exact `f64` integer). Distinct from [`WindowLag137`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag138 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag138 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag138 {
    /// Empty windowed lag-138 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 138 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(138);
        let win = &xs[start..];
        if win.len() < 138 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..138 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag138 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 138;
        q.warmup = self.rows.len() < 138;
        q.explanation = format!("window-lag138 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-eighth-difference anomaly",
                "WindowLag138 uses successive first differences and cap 144; not WindowLag137",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag138 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski79_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(79);
    }
    s.powf(1.0 / 79.0)
}

/// Log Minkowski \(p=79\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{79})^{1/79}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski78Anomaly`] (\(p=78\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski79Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski79Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski79Anomaly {
    /// Empty log Minkowski-\(p=79\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski79_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski79Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski79 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=79 k-NN anomaly",
                "LogMinkowski79Anomaly is L79 on |ln|; not L78 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski79Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-139 windowed one-hundred-thirty-ninth-difference anomaly.
///
/// The 139th backward difference is formed by successive first differences
/// (`C(139, 69)` is not an exact `f64` integer). Distinct from [`WindowLag138`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag139 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag139 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag139 {
    /// Empty windowed lag-139 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 139 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(139);
        let win = &xs[start..];
        if win.len() < 139 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..139 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag139 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 139;
        q.warmup = self.rows.len() < 139;
        q.explanation = format!("window-lag139 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-thirty-ninth-difference anomaly",
                "WindowLag139 uses successive first differences and cap 144; not WindowLag138",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag139 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski80_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(80);
    }
    s.powf(1.0 / 80.0)
}

/// Log Minkowski \(p=80\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{80})^{1/80}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski79Anomaly`] (\(p=79\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski80Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski80Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski80Anomaly {
    /// Empty log Minkowski-\(p=80\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski80_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski80Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski80 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=80 k-NN anomaly",
                "LogMinkowski80Anomaly is L80 on |ln|; not L79 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski80Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-140 windowed one-hundred-fortieth-difference anomaly.
///
/// The 140th backward difference is formed by successive first differences
/// (`C(140, 70)` is not an exact `f64` integer). Distinct from [`WindowLag139`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag140 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag140 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag140 {
    /// Empty windowed lag-140 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 140 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(140);
        let win = &xs[start..];
        if win.len() < 140 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..140 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag140 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 140;
        q.warmup = self.rows.len() < 140;
        q.explanation = format!("window-lag140 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fortieth-difference anomaly",
                "WindowLag140 uses successive first differences and cap 144; not WindowLag139",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag140 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski81_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(81);
    }
    s.powf(1.0 / 81.0)
}

/// Log Minkowski \(p=81\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{81})^{1/81}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski80Anomaly`] (\(p=80\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski81Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski81Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski81Anomaly {
    /// Empty log Minkowski-\(p=81\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski81_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski81Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski81 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=81 k-NN anomaly",
                "LogMinkowski81Anomaly is L81 on |ln|; not L80 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski81Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-141 windowed one-hundred-forty-first-difference anomaly.
///
/// The 141st backward difference is formed by successive first differences
/// (`C(141, 70)` is not an exact `f64` integer). Distinct from [`WindowLag140`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag141 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag141 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag141 {
    /// Empty windowed lag-141 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 141 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(141);
        let win = &xs[start..];
        if win.len() < 141 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..141 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag141 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 141;
        q.warmup = self.rows.len() < 141;
        q.explanation = format!("window-lag141 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-first-difference anomaly",
                "WindowLag141 uses successive first differences and cap 144; not WindowLag140",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag141 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski82_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(82);
    }
    s.powf(1.0 / 82.0)
}

/// Log Minkowski \(p=82\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{82})^{1/82}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski81Anomaly`] (\(p=81\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski82Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski82Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski82Anomaly {
    /// Empty log Minkowski-\(p=82\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski82_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski82Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski82 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=82 k-NN anomaly",
                "LogMinkowski82Anomaly is L82 on |ln|; not L81 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski82Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-142 windowed one-hundred-forty-second-difference anomaly.
///
/// The 142nd backward difference is formed by successive first differences
/// (`C(142, 71)` is not an exact `f64` integer). Distinct from [`WindowLag141`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag142 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag142 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag142 {
    /// Empty windowed lag-142 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 142 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(142);
        let win = &xs[start..];
        if win.len() < 142 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..142 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag142 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 142;
        q.warmup = self.rows.len() < 142;
        q.explanation = format!("window-lag142 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-second-difference anomaly",
                "WindowLag142 uses successive first differences and cap 144; not WindowLag141",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag142 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski83_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(83);
    }
    s.powf(1.0 / 83.0)
}

/// Log Minkowski \(p=83\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{83})^{1/83}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski82Anomaly`] (\(p=82\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski83Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski83Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski83Anomaly {
    /// Empty log Minkowski-\(p=83\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski83_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski83Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski83 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=83 k-NN anomaly",
                "LogMinkowski83Anomaly is L83 on |ln|; not L82 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski83Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-143 windowed one-hundred-forty-third-difference anomaly.
///
/// The 143rd backward difference is formed by successive first differences
/// (`C(143, 71)` is not an exact `f64` integer). Distinct from [`WindowLag142`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag143 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag143 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag143 {
    /// Empty windowed lag-143 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 143 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(143);
        let win = &xs[start..];
        if win.len() < 143 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..143 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag143 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 143;
        q.warmup = self.rows.len() < 143;
        q.explanation = format!("window-lag143 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-third-difference anomaly",
                "WindowLag143 uses successive first differences and cap 144; not WindowLag142",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag143 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski84_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(84);
    }
    s.powf(1.0 / 84.0)
}

/// Log Minkowski \(p=84\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{84})^{1/84}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski83Anomaly`] (\(p=83\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski84Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski84Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski84Anomaly {
    /// Empty log Minkowski-\(p=84\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski84_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski84Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski84 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=84 k-NN anomaly",
                "LogMinkowski84Anomaly is L84 on |ln|; not L83 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski84Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-144 windowed one-hundred-forty-fourth-difference anomaly.
///
/// The 144th backward difference is formed by successive first differences
/// (`C(144, 72)` is not an exact `f64` integer). Distinct from [`WindowLag143`].
/// Window length is not identification `p`. Reservoir cap is 144.
#[derive(Clone, Debug)]
pub struct WindowLag144 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag144 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag144 {
    /// Empty windowed lag-144 detector (reservoir cap 144).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 144 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(144);
        let win = &xs[start..];
        if win.len() < 144 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..144 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag144 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 144 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 144;
        q.warmup = self.rows.len() < 144;
        q.explanation = format!("window-lag144 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-fourth-difference anomaly",
                "WindowLag144 uses successive first differences and cap 144; not WindowLag143",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag144 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski85_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(85);
    }
    s.powf(1.0 / 85.0)
}

/// Log Minkowski \(p=85\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{85})^{1/85}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski84Anomaly`] (\(p=84\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski85Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski85Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski85Anomaly {
    /// Empty log Minkowski-\(p=85\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski85_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski85Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski85 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=85 k-NN anomaly",
                "LogMinkowski85Anomaly is L85 on |ln|; not L84 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski85Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-145 windowed one-hundred-forty-fifth-difference anomaly.
///
/// The 145th backward difference is formed by successive first differences
/// (`C(145, 72)` is not an exact `f64` integer). Distinct from [`WindowLag144`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag145 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag145 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag145 {
    /// Empty windowed lag-145 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 145 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(145);
        let win = &xs[start..];
        if win.len() < 145 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..145 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag145 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 145;
        q.warmup = self.rows.len() < 145;
        q.explanation = format!("window-lag145 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-fifth-difference anomaly",
                "WindowLag145 uses successive first differences and cap 160; not WindowLag144",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag145 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski86_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(86);
    }
    s.powf(1.0 / 86.0)
}

/// Log Minkowski \(p=86\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{86})^{1/86}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski85Anomaly`] (\(p=85\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski86Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski86Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski86Anomaly {
    /// Empty log Minkowski-\(p=86\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski86_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski86Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski86 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=86 k-NN anomaly",
                "LogMinkowski86Anomaly is L86 on |ln|; not L85 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski86Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-146 windowed one-hundred-forty-sixth-difference anomaly.
///
/// The 146th backward difference is formed by successive first differences
/// (`C(146, 73)` is not an exact `f64` integer). Distinct from [`WindowLag145`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag146 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag146 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag146 {
    /// Empty windowed lag-146 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 146 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(146);
        let win = &xs[start..];
        if win.len() < 146 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..146 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag146 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 146;
        q.warmup = self.rows.len() < 146;
        q.explanation = format!("window-lag146 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-sixth-difference anomaly",
                "WindowLag146 uses successive first differences and cap 160; not WindowLag145",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag146 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski87_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(87);
    }
    s.powf(1.0 / 87.0)
}

/// Log Minkowski \(p=87\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{87})^{1/87}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski86Anomaly`] (\(p=86\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski87Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski87Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski87Anomaly {
    /// Empty log Minkowski-\(p=87\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski87_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski87Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski87 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=87 k-NN anomaly",
                "LogMinkowski87Anomaly is L87 on |ln|; not L86 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski87Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-147 windowed one-hundred-forty-seventh-difference anomaly.
///
/// The 147th backward difference is formed by successive first differences
/// (`C(147, 73)` is not an exact `f64` integer). Distinct from [`WindowLag146`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag147 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag147 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag147 {
    /// Empty windowed lag-147 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 147 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(147);
        let win = &xs[start..];
        if win.len() < 147 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..147 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag147 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 147;
        q.warmup = self.rows.len() < 147;
        q.explanation = format!("window-lag147 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-seventh-difference anomaly",
                "WindowLag147 uses successive first differences and cap 160; not WindowLag146",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag147 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski88_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(88);
    }
    s.powf(1.0 / 88.0)
}

/// Log Minkowski \(p=88\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{88})^{1/88}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski87Anomaly`] (\(p=87\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski88Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski88Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski88Anomaly {
    /// Empty log Minkowski-\(p=88\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski88_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski88Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski88 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=88 k-NN anomaly",
                "LogMinkowski88Anomaly is L88 on |ln|; not L87 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski88Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-148 windowed one-hundred-forty-eighth-difference anomaly.
///
/// The 148th backward difference is formed by successive first differences
/// (`C(148, 74)` is not an exact `f64` integer). Distinct from [`WindowLag147`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag148 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag148 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag148 {
    /// Empty windowed lag-148 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 148 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(148);
        let win = &xs[start..];
        if win.len() < 148 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..148 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag148 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 148;
        q.warmup = self.rows.len() < 148;
        q.explanation = format!("window-lag148 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-eighth-difference anomaly",
                "WindowLag148 uses successive first differences and cap 160; not WindowLag147",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag148 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski89_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(89);
    }
    s.powf(1.0 / 89.0)
}

/// Log Minkowski \(p=89\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{89})^{1/89}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski88Anomaly`] (\(p=88\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski89Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski89Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski89Anomaly {
    /// Empty log Minkowski-\(p=89\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski89_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski89Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski89 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=89 k-NN anomaly",
                "LogMinkowski89Anomaly is L89 on |ln|; not L88 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski89Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-149 windowed one-hundred-forty-ninth-difference anomaly.
///
/// The 149th backward difference is formed by successive first differences
/// (`C(149, 74)` is not an exact `f64` integer). Distinct from [`WindowLag148`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag149 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag149 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag149 {
    /// Empty windowed lag-149 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 149 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(149);
        let win = &xs[start..];
        if win.len() < 149 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..149 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag149 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 149;
        q.warmup = self.rows.len() < 149;
        q.explanation = format!("window-lag149 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-forty-ninth-difference anomaly",
                "WindowLag149 uses successive first differences and cap 160; not WindowLag148",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag149 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski90_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(90);
    }
    s.powf(1.0 / 90.0)
}

/// Log Minkowski \(p=90\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{90})^{1/90}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski89Anomaly`] (\(p=89\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski90Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski90Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski90Anomaly {
    /// Empty log Minkowski-\(p=90\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski90_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski90Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski90 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=90 k-NN anomaly",
                "LogMinkowski90Anomaly is L90 on |ln|; not L89 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski90Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-150 windowed one-hundred-fiftieth-difference anomaly.
///
/// The 150th backward difference is formed by successive first differences
/// (`C(150, 75)` is not an exact `f64` integer). Distinct from [`WindowLag149`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag150 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag150 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag150 {
    /// Empty windowed lag-150 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 150 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(150);
        let win = &xs[start..];
        if win.len() < 150 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..150 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag150 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 150;
        q.warmup = self.rows.len() < 150;
        q.explanation = format!("window-lag150 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fiftieth-difference anomaly",
                "WindowLag150 uses successive first differences and cap 160; not WindowLag149",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag150 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski91_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(91);
    }
    s.powf(1.0 / 91.0)
}

/// Log Minkowski \(p=91\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{91})^{1/91}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski90Anomaly`] (\(p=90\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski91Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski91Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski91Anomaly {
    /// Empty log Minkowski-\(p=91\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski91_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski91Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski91 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=91 k-NN anomaly",
                "LogMinkowski91Anomaly is L91 on |ln|; not L90 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski91Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-151 windowed one-hundred-fifty-first-difference anomaly.
///
/// The 151st backward difference is formed by successive first differences
/// (`C(151, 75)` is not an exact `f64` integer). Distinct from [`WindowLag150`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag151 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag151 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag151 {
    /// Empty windowed lag-151 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 151 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(151);
        let win = &xs[start..];
        if win.len() < 151 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..151 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag151 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 151;
        q.warmup = self.rows.len() < 151;
        q.explanation = format!("window-lag151 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-first-difference anomaly",
                "WindowLag151 uses successive first differences and cap 160; not WindowLag150",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag151 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski92_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(92);
    }
    s.powf(1.0 / 92.0)
}

/// Log Minkowski \(p=92\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{92})^{1/92}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski91Anomaly`] (\(p=91\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski92Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski92Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski92Anomaly {
    /// Empty log Minkowski-\(p=92\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski92_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski92Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski92 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=92 k-NN anomaly",
                "LogMinkowski92Anomaly is L92 on |ln|; not L91 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski92Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-152 windowed one-hundred-fifty-second-difference anomaly.
///
/// The 152nd backward difference is formed by successive first differences
/// (`C(152, 76)` is not an exact `f64` integer). Distinct from [`WindowLag151`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag152 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag152 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag152 {
    /// Empty windowed lag-152 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 152 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(152);
        let win = &xs[start..];
        if win.len() < 152 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..152 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag152 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 152;
        q.warmup = self.rows.len() < 152;
        q.explanation = format!("window-lag152 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-second-difference anomaly",
                "WindowLag152 uses successive first differences and cap 160; not WindowLag151",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag152 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski93_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(93);
    }
    s.powf(1.0 / 93.0)
}

/// Log Minkowski \(p=93\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{93})^{1/93}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski92Anomaly`] (\(p=92\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski93Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski93Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski93Anomaly {
    /// Empty log Minkowski-\(p=93\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski93_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski93Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski93 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=93 k-NN anomaly",
                "LogMinkowski93Anomaly is L93 on |ln|; not L92 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski93Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-153 windowed one-hundred-fifty-third-difference anomaly.
///
/// The 153rd backward difference is formed by successive first differences
/// (`C(153, 76)` is not an exact `f64` integer). Distinct from [`WindowLag152`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag153 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag153 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag153 {
    /// Empty windowed lag-153 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 153 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(153);
        let win = &xs[start..];
        if win.len() < 153 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..153 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag153 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 153;
        q.warmup = self.rows.len() < 153;
        q.explanation = format!("window-lag153 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-third-difference anomaly",
                "WindowLag153 uses successive first differences and cap 160; not WindowLag152",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag153 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski94_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(94);
    }
    s.powf(1.0 / 94.0)
}

/// Log Minkowski \(p=94\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{94})^{1/94}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski93Anomaly`] (\(p=93\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski94Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski94Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski94Anomaly {
    /// Empty log Minkowski-\(p=94\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski94_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski94Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski94 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=94 k-NN anomaly",
                "LogMinkowski94Anomaly is L94 on |ln|; not L93 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski94Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-154 windowed one-hundred-fifty-fourth-difference anomaly.
///
/// The 154th backward difference is formed by successive first differences
/// (`C(154, 77)` is not an exact `f64` integer). Distinct from [`WindowLag153`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag154 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag154 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag154 {
    /// Empty windowed lag-154 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 154 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(154);
        let win = &xs[start..];
        if win.len() < 154 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..154 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag154 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 154;
        q.warmup = self.rows.len() < 154;
        q.explanation = format!("window-lag154 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-fourth-difference anomaly",
                "WindowLag154 uses successive first differences and cap 160; not WindowLag153",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag154 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski95_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(95);
    }
    s.powf(1.0 / 95.0)
}

/// Log Minkowski \(p=95\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{95})^{1/95}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski94Anomaly`] (\(p=94\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski95Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski95Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski95Anomaly {
    /// Empty log Minkowski-\(p=95\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski95_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski95Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski95 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=95 k-NN anomaly",
                "LogMinkowski95Anomaly is L95 on |ln|; not L94 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski95Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-155 windowed one-hundred-fifty-fifth-difference anomaly.
///
/// The 155th backward difference is formed by successive first differences
/// (`C(155, 77)` is not an exact `f64` integer). Distinct from [`WindowLag154`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag155 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag155 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag155 {
    /// Empty windowed lag-155 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 155 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(155);
        let win = &xs[start..];
        if win.len() < 155 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..155 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag155 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 155;
        q.warmup = self.rows.len() < 155;
        q.explanation = format!("window-lag155 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-fifth-difference anomaly",
                "WindowLag155 uses successive first differences and cap 160; not WindowLag154",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag155 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski96_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(96);
    }
    s.powf(1.0 / 96.0)
}

/// Log Minkowski \(p=96\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{96})^{1/96}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski95Anomaly`] (\(p=95\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski96Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski96Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski96Anomaly {
    /// Empty log Minkowski-\(p=96\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski96_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski96Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski96 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=96 k-NN anomaly",
                "LogMinkowski96Anomaly is L96 on |ln|; not L95 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski96Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-156 windowed one-hundred-fifty-sixth-difference anomaly.
///
/// The 156th backward difference is formed by successive first differences
/// (`C(156, 78)` is not an exact `f64` integer). Distinct from [`WindowLag155`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag156 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag156 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag156 {
    /// Empty windowed lag-156 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 156 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(156);
        let win = &xs[start..];
        if win.len() < 156 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..156 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag156 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 156;
        q.warmup = self.rows.len() < 156;
        q.explanation = format!("window-lag156 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-sixth-difference anomaly",
                "WindowLag156 uses successive first differences and cap 160; not WindowLag155",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag156 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski97_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(97);
    }
    s.powf(1.0 / 97.0)
}

/// Log Minkowski \(p=97\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{97})^{1/97}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski96Anomaly`] (\(p=96\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski97Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski97Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski97Anomaly {
    /// Empty log Minkowski-\(p=97\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski97_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski97Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski97 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=97 k-NN anomaly",
                "LogMinkowski97Anomaly is L97 on |ln|; not L96 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski97Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-157 windowed one-hundred-fifty-seventh-difference anomaly.
///
/// The 157th backward difference is formed by successive first differences
/// (`C(157, 78)` is not an exact `f64` integer). Distinct from [`WindowLag156`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag157 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag157 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag157 {
    /// Empty windowed lag-157 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 157 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(157);
        let win = &xs[start..];
        if win.len() < 157 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..157 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag157 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 157;
        q.warmup = self.rows.len() < 157;
        q.explanation = format!("window-lag157 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-seventh-difference anomaly",
                "WindowLag157 uses successive first differences and cap 160; not WindowLag156",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag157 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski98_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(98);
    }
    s.powf(1.0 / 98.0)
}

/// Log Minkowski \(p=98\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{98})^{1/98}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski97Anomaly`] (\(p=97\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski98Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski98Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski98Anomaly {
    /// Empty log Minkowski-\(p=98\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski98_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski98Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski98 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=98 k-NN anomaly",
                "LogMinkowski98Anomaly is L98 on |ln|; not L97 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski98Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-158 windowed one-hundred-fifty-eighth-difference anomaly.
///
/// The 158th backward difference is formed by successive first differences
/// (`C(158, 79)` is not an exact `f64` integer). Distinct from [`WindowLag157`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag158 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag158 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag158 {
    /// Empty windowed lag-158 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 158 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(158);
        let win = &xs[start..];
        if win.len() < 158 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..158 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag158 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 158;
        q.warmup = self.rows.len() < 158;
        q.explanation = format!("window-lag158 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-eighth-difference anomaly",
                "WindowLag158 uses successive first differences and cap 160; not WindowLag157",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag158 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski99_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(99);
    }
    s.powf(1.0 / 99.0)
}

/// Log Minkowski \(p=99\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{99})^{1/99}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski98Anomaly`] (\(p=98\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski99Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski99Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski99Anomaly {
    /// Empty log Minkowski-\(p=99\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski99_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski99Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski99 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=99 k-NN anomaly",
                "LogMinkowski99Anomaly is L99 on |ln|; not L98 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski99Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-159 windowed one-hundred-fifty-ninth-difference anomaly.
///
/// The 159th backward difference is formed by successive first differences
/// (`C(159, 79)` is not an exact `f64` integer). Distinct from [`WindowLag158`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag159 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag159 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag159 {
    /// Empty windowed lag-159 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 159 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(159);
        let win = &xs[start..];
        if win.len() < 159 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..159 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag159 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 159;
        q.warmup = self.rows.len() < 159;
        q.explanation = format!("window-lag159 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-fifty-ninth-difference anomaly",
                "WindowLag159 uses successive first differences and cap 160; not WindowLag158",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag159 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski100_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(100);
    }
    s.powf(1.0 / 100.0)
}

/// Log Minkowski \(p=100\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{100})^{1/100}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski99Anomaly`] (\(p=99\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski100Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski100Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski100Anomaly {
    /// Empty log Minkowski-\(p=100\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski100_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski100Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski100 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=100 k-NN anomaly",
                "LogMinkowski100Anomaly is L100 on |ln|; not L99 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski100Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-160 windowed one-hundred-sixtieth-difference anomaly.
///
/// The 160th backward difference is formed by successive first differences
/// (`C(160, 80)` is not an exact `f64` integer). Distinct from [`WindowLag159`].
/// Window length is not identification `p`. Reservoir cap is 160.
#[derive(Clone, Debug)]
pub struct WindowLag160 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag160 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag160 {
    /// Empty windowed lag-160 detector (reservoir cap 160).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 160 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(160);
        let win = &xs[start..];
        if win.len() < 160 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..160 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag160 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 160 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 160;
        q.warmup = self.rows.len() < 160;
        q.explanation = format!("window-lag160 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixtieth-difference anomaly",
                "WindowLag160 uses successive first differences and cap 160; not WindowLag159",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag160 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski101_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(101);
    }
    s.powf(1.0 / 101.0)
}

/// Log Minkowski \(p=101\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{101})^{1/101}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski100Anomaly`] (\(p=100\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski101Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski101Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski101Anomaly {
    /// Empty log Minkowski-\(p=101\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski101_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski101Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski101 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=101 k-NN anomaly",
                "LogMinkowski101Anomaly is L101 on |ln|; not L100 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski101Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-161 windowed one-hundred-sixty-first-difference anomaly.
///
/// The 161st backward difference is formed by successive first differences
/// (`C(161, 80)` is not an exact `f64` integer). Distinct from [`WindowLag160`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag161 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag161 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag161 {
    /// Empty windowed lag-161 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 161 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(161);
        let win = &xs[start..];
        if win.len() < 161 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..161 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag161 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 161;
        q.warmup = self.rows.len() < 161;
        q.explanation = format!("window-lag161 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-first-difference anomaly",
                "WindowLag161 uses successive first differences and cap 176; not WindowLag160",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag161 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski102_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(102);
    }
    s.powf(1.0 / 102.0)
}

/// Log Minkowski \(p=102\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{102})^{1/102}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski101Anomaly`] (\(p=101\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski102Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski102Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski102Anomaly {
    /// Empty log Minkowski-\(p=102\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski102_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski102Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski102 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=102 k-NN anomaly",
                "LogMinkowski102Anomaly is L102 on |ln|; not L101 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski102Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-162 windowed one-hundred-sixty-second-difference anomaly.
///
/// The 162nd backward difference is formed by successive first differences
/// (`C(162, 81)` is not an exact `f64` integer). Distinct from [`WindowLag161`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag162 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag162 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag162 {
    /// Empty windowed lag-162 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 162 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(162);
        let win = &xs[start..];
        if win.len() < 162 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..162 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag162 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 162;
        q.warmup = self.rows.len() < 162;
        q.explanation = format!("window-lag162 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-second-difference anomaly",
                "WindowLag162 uses successive first differences and cap 176; not WindowLag161",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag162 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski103_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(103);
    }
    s.powf(1.0 / 103.0)
}

/// Log Minkowski \(p=103\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{103})^{1/103}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski102Anomaly`] (\(p=102\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski103Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski103Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski103Anomaly {
    /// Empty log Minkowski-\(p=103\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski103_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski103Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski103 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=103 k-NN anomaly",
                "LogMinkowski103Anomaly is L103 on |ln|; not L102 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski103Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-163 windowed one-hundred-sixty-third-difference anomaly.
///
/// The 163rd backward difference is formed by successive first differences
/// (`C(163, 81)` is not an exact `f64` integer). Distinct from [`WindowLag162`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag163 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag163 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag163 {
    /// Empty windowed lag-163 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 163 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(163);
        let win = &xs[start..];
        if win.len() < 163 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..163 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag163 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 163;
        q.warmup = self.rows.len() < 163;
        q.explanation = format!("window-lag163 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-third-difference anomaly",
                "WindowLag163 uses successive first differences and cap 176; not WindowLag162",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag163 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski104_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(104);
    }
    s.powf(1.0 / 104.0)
}

/// Log Minkowski \(p=104\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{104})^{1/104}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski103Anomaly`] (\(p=103\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski104Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski104Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski104Anomaly {
    /// Empty log Minkowski-\(p=104\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski104_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski104Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski104 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=104 k-NN anomaly",
                "LogMinkowski104Anomaly is L104 on |ln|; not L103 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski104Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-164 windowed one-hundred-sixty-fourth-difference anomaly.
///
/// The 164th backward difference is formed by successive first differences
/// (`C(164, 82)` is not an exact `f64` integer). Distinct from [`WindowLag163`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag164 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag164 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag164 {
    /// Empty windowed lag-164 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 164 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(164);
        let win = &xs[start..];
        if win.len() < 164 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..164 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag164 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 164;
        q.warmup = self.rows.len() < 164;
        q.explanation = format!("window-lag164 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-fourth-difference anomaly",
                "WindowLag164 uses successive first differences and cap 176; not WindowLag163",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag164 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski105_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(105);
    }
    s.powf(1.0 / 105.0)
}

/// Log Minkowski \(p=105\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski104Anomaly`] (\(p=104\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski105Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski105Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski105Anomaly {
    /// Empty log Minkowski-\(p=105\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski105_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski105Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski105 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=105 k-NN anomaly",
                "LogMinkowski105Anomaly is L105 on |ln|; not L104 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski105Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-165 windowed one-hundred-sixty-fifth-difference anomaly.
///
/// The 165th backward difference is formed by successive first differences
/// (`C(165, 82)` is not an exact `f64` integer). Distinct from [`WindowLag164`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag165 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag165 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag165 {
    /// Empty windowed lag-165 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 165 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(165);
        let win = &xs[start..];
        if win.len() < 165 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..165 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag165 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 165;
        q.warmup = self.rows.len() < 165;
        q.explanation = format!("window-lag165 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-fifth-difference anomaly",
                "WindowLag165 uses successive first differences and cap 176; not WindowLag164",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag165 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski106_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(106);
    }
    s.powf(1.0 / 106.0)
}

/// Log Minkowski \(p=106\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{106})^{1/106}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski105Anomaly`] (\(p=105\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski106Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski106Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski106Anomaly {
    /// Empty log Minkowski-\(p=106\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski106_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski106Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski106 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=106 k-NN anomaly",
                "LogMinkowski106Anomaly is L106 on |ln|; not L105 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski106Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-166 windowed one-hundred-sixty-sixth-difference anomaly.
///
/// The 166th backward difference is formed by successive first differences
/// (`C(166, 83)` is not an exact `f64` integer). Distinct from [`WindowLag165`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag166 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag166 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag166 {
    /// Empty windowed lag-166 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 166 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(166);
        let win = &xs[start..];
        if win.len() < 166 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..166 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag166 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 166;
        q.warmup = self.rows.len() < 166;
        q.explanation = format!("window-lag166 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-sixth-difference anomaly",
                "WindowLag166 uses successive first differences and cap 176; not WindowLag165",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag166 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski107_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(107);
    }
    s.powf(1.0 / 107.0)
}

/// Log Minkowski \(p=107\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski106Anomaly`] (\(p=106\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski107Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski107Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski107Anomaly {
    /// Empty log Minkowski-\(p=107\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski107_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski107Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski107 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=107 k-NN anomaly",
                "LogMinkowski107Anomaly is L107 on |ln|; not L106 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski107Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-167 windowed one-hundred-sixty-seventh-difference anomaly.
///
/// The 167th backward difference is formed by successive first differences
/// (`C(167, 83)` is not an exact `f64` integer). Distinct from [`WindowLag166`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag167 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag167 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag167 {
    /// Empty windowed lag-167 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 167 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(167);
        let win = &xs[start..];
        if win.len() < 167 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..167 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag167 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 167;
        q.warmup = self.rows.len() < 167;
        q.explanation = format!("window-lag167 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-seventh-difference anomaly",
                "WindowLag167 uses successive first differences and cap 176; not WindowLag166",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag167 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski108_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(108);
    }
    s.powf(1.0 / 108.0)
}

/// Log Minkowski \(p=108\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{108})^{1/108}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski107Anomaly`] (\(p=107\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski108Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski108Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski108Anomaly {
    /// Empty log Minkowski-\(p=108\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski108_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski108Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski108 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=108 k-NN anomaly",
                "LogMinkowski108Anomaly is L108 on |ln|; not L107 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski108Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-168 windowed one-hundred-sixty-eighth-difference anomaly.
///
/// The 168th backward difference is formed by successive first differences
/// (`C(168, 84)` is not an exact `f64` integer). Distinct from [`WindowLag167`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag168 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag168 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag168 {
    /// Empty windowed lag-168 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 168 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(168);
        let win = &xs[start..];
        if win.len() < 168 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..168 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag168 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 168;
        q.warmup = self.rows.len() < 168;
        q.explanation = format!("window-lag168 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-eighth-difference anomaly",
                "WindowLag168 uses successive first differences and cap 176; not WindowLag167",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag168 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski109_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(109);
    }
    s.powf(1.0 / 109.0)
}

/// Log Minkowski \(p=109\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski108Anomaly`] (\(p=108\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski109Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski109Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski109Anomaly {
    /// Empty log Minkowski-\(p=109\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski109_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski109Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski109 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=109 k-NN anomaly",
                "LogMinkowski109Anomaly is L109 on |ln|; not L108 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski109Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-169 windowed one-hundred-sixty-ninth-difference anomaly.
///
/// The 169th backward difference is formed by successive first differences
/// (`C(169, 84)` is not an exact `f64` integer). Distinct from [`WindowLag168`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag169 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag169 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag169 {
    /// Empty windowed lag-169 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 169 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(169);
        let win = &xs[start..];
        if win.len() < 169 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..169 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag169 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 169;
        q.warmup = self.rows.len() < 169;
        q.explanation = format!("window-lag169 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-sixty-ninth-difference anomaly",
                "WindowLag169 uses successive first differences and cap 176; not WindowLag168",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag169 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski110_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(110);
    }
    s.powf(1.0 / 110.0)
}

/// Log Minkowski \(p=110\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{110})^{1/110}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski109Anomaly`] (\(p=109\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski110Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski110Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski110Anomaly {
    /// Empty log Minkowski-\(p=110\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski110_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski110Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski110 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=110 k-NN anomaly",
                "LogMinkowski110Anomaly is L110 on |ln|; not L109 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski110Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-170 windowed one-hundred-seventieth-difference anomaly.
///
/// The 170th backward difference is formed by successive first differences
/// (`C(170, 85)` is not an exact `f64` integer). Distinct from [`WindowLag169`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag170 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag170 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag170 {
    /// Empty windowed lag-170 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 170 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(170);
        let win = &xs[start..];
        if win.len() < 170 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..170 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag170 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 170;
        q.warmup = self.rows.len() < 170;
        q.explanation = format!("window-lag170 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventieth-difference anomaly",
                "WindowLag170 uses successive first differences and cap 176; not WindowLag169",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag170 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski111_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(111);
    }
    s.powf(1.0 / 111.0)
}

/// Log Minkowski \(p=111\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski110Anomaly`] (\(p=110\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski111Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski111Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski111Anomaly {
    /// Empty log Minkowski-\(p=111\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski111_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski111Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski111 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=111 k-NN anomaly",
                "LogMinkowski111Anomaly is L111 on |ln|; not L110 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski111Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-171 windowed one-hundred-seventy-first-difference anomaly.
///
/// The 171st backward difference is formed by successive first differences
/// (`C(171, 85)` is not an exact `f64` integer). Distinct from [`WindowLag170`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag171 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag171 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag171 {
    /// Empty windowed lag-171 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 171 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(171);
        let win = &xs[start..];
        if win.len() < 171 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..171 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag171 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 171;
        q.warmup = self.rows.len() < 171;
        q.explanation = format!("window-lag171 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-first-difference anomaly",
                "WindowLag171 uses successive first differences and cap 176; not WindowLag170",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag171 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski112_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(112);
    }
    s.powf(1.0 / 112.0)
}

/// Log Minkowski \(p=112\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{112})^{1/112}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski111Anomaly`] (\(p=111\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski112Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski112Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski112Anomaly {
    /// Empty log Minkowski-\(p=112\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski112_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski112Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski112 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=112 k-NN anomaly",
                "LogMinkowski112Anomaly is L112 on |ln|; not L111 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski112Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-172 windowed one-hundred-seventy-second-difference anomaly.
///
/// The 172nd backward difference is formed by successive first differences
/// (`C(172, 86)` is not an exact `f64` integer). Distinct from [`WindowLag171`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag172 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag172 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag172 {
    /// Empty windowed lag-172 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 172 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(172);
        let win = &xs[start..];
        if win.len() < 172 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..172 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag172 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 172;
        q.warmup = self.rows.len() < 172;
        q.explanation = format!("window-lag172 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-second-difference anomaly",
                "WindowLag172 uses successive first differences and cap 176; not WindowLag171",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag172 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski113_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(113);
    }
    s.powf(1.0 / 113.0)
}

/// Log Minkowski \(p=113\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski112Anomaly`] (\(p=112\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski113Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski113Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski113Anomaly {
    /// Empty log Minkowski-\(p=113\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski113_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski113Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski113 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=113 k-NN anomaly",
                "LogMinkowski113Anomaly is L113 on |ln|; not L112 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski113Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-173 windowed one-hundred-seventy-third-difference anomaly.
///
/// The 173rd backward difference is formed by successive first differences
/// (`C(173, 86)` is not an exact `f64` integer). Distinct from [`WindowLag172`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag173 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag173 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag173 {
    /// Empty windowed lag-173 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 173 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(173);
        let win = &xs[start..];
        if win.len() < 173 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..173 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag173 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 173;
        q.warmup = self.rows.len() < 173;
        q.explanation = format!("window-lag173 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-third-difference anomaly",
                "WindowLag173 uses successive first differences and cap 176; not WindowLag172",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag173 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski114_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(114);
    }
    s.powf(1.0 / 114.0)
}

/// Log Minkowski \(p=114\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{114})^{1/114}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski113Anomaly`] (\(p=113\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski114Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski114Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski114Anomaly {
    /// Empty log Minkowski-\(p=114\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski114_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski114Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski114 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=114 k-NN anomaly",
                "LogMinkowski114Anomaly is L114 on |ln|; not L113 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski114Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-174 windowed one-hundred-seventy-fourth-difference anomaly.
///
/// The 174th backward difference is formed by successive first differences
/// (`C(174, 87)` is not an exact `f64` integer). Distinct from [`WindowLag173`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag174 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag174 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag174 {
    /// Empty windowed lag-174 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 174 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(174);
        let win = &xs[start..];
        if win.len() < 174 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..174 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag174 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 174;
        q.warmup = self.rows.len() < 174;
        q.explanation = format!("window-lag174 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-fourth-difference anomaly",
                "WindowLag174 uses successive first differences and cap 176; not WindowLag173",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag174 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski115_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(115);
    }
    s.powf(1.0 / 115.0)
}

/// Log Minkowski \(p=115\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski114Anomaly`] (\(p=114\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski115Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski115Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski115Anomaly {
    /// Empty log Minkowski-\(p=115\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski115_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski115Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski115 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=115 k-NN anomaly",
                "LogMinkowski115Anomaly is L115 on |ln|; not L114 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski115Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-175 windowed one-hundred-seventy-fifth-difference anomaly.
///
/// The 175th backward difference is formed by successive first differences
/// (`C(175, 87)` is not an exact `f64` integer). Distinct from [`WindowLag174`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag175 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag175 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag175 {
    /// Empty windowed lag-175 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 175 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(175);
        let win = &xs[start..];
        if win.len() < 175 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..175 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag175 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 175;
        q.warmup = self.rows.len() < 175;
        q.explanation = format!("window-lag175 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-fifth-difference anomaly",
                "WindowLag175 uses successive first differences and cap 176; not WindowLag174",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag175 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski116_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(116);
    }
    s.powf(1.0 / 116.0)
}

/// Log Minkowski \(p=116\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{116})^{1/116}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski115Anomaly`] (\(p=115\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski116Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski116Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski116Anomaly {
    /// Empty log Minkowski-\(p=116\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski116_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski116Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski116 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=116 k-NN anomaly",
                "LogMinkowski116Anomaly is L116 on |ln|; not L115 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski116Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-176 windowed one-hundred-seventy-sixth-difference anomaly.
///
/// The 176th backward difference is formed by successive first differences
/// (`C(176, 88)` is not an exact `f64` integer). Distinct from [`WindowLag175`].
/// Window length is not identification `p`. Reservoir cap is 176.
#[derive(Clone, Debug)]
pub struct WindowLag176 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag176 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag176 {
    /// Empty windowed lag-176 detector (reservoir cap 176).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 176 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(176);
        let win = &xs[start..];
        if win.len() < 176 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..176 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag176 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 176 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 176;
        q.warmup = self.rows.len() < 176;
        q.explanation = format!("window-lag176 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-sixth-difference anomaly",
                "WindowLag176 uses successive first differences and cap 176; not WindowLag175",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag176 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski117_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(117);
    }
    s.powf(1.0 / 117.0)
}

/// Log Minkowski \(p=117\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{105})^{1/105}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski116Anomaly`] (\(p=116\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski117Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski117Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski117Anomaly {
    /// Empty log Minkowski-\(p=117\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski117_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski117Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski117 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=117 k-NN anomaly",
                "LogMinkowski117Anomaly is L117 on |ln|; not L116 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski117Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-177 windowed one-hundred-seventy-seventh-difference anomaly.
///
/// The 177th backward difference is formed by successive first differences
/// (`C(177, 88)` is not an exact `f64` integer). Distinct from [`WindowLag176`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag177 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag177 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag177 {
    /// Empty windowed lag-177 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 177 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(177);
        let win = &xs[start..];
        if win.len() < 177 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..177 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag177 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 177;
        q.warmup = self.rows.len() < 177;
        q.explanation = format!("window-lag177 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-seventh-difference anomaly",
                "WindowLag177 uses successive first differences and cap 192; not WindowLag176",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag177 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski118_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(118);
    }
    s.powf(1.0 / 118.0)
}

/// Log Minkowski \(p=118\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{118})^{1/118}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski117Anomaly`] (\(p=117\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski118Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski118Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski118Anomaly {
    /// Empty log Minkowski-\(p=118\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski118_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski118Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski118 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=118 k-NN anomaly",
                "LogMinkowski118Anomaly is L118 on |ln|; not L117 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski118Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-178 windowed one-hundred-seventy-eighth-difference anomaly.
///
/// The 178th backward difference is formed by successive first differences
/// (`C(178, 89)` is not an exact `f64` integer). Distinct from [`WindowLag177`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag178 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag178 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag178 {
    /// Empty windowed lag-178 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 178 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(178);
        let win = &xs[start..];
        if win.len() < 178 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..178 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag178 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 178;
        q.warmup = self.rows.len() < 178;
        q.explanation = format!("window-lag178 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-eighth-difference anomaly",
                "WindowLag178 uses successive first differences and cap 192; not WindowLag177",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag178 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski119_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(119);
    }
    s.powf(1.0 / 119.0)
}

/// Log Minkowski \(p=119\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{118})^{1/118}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski118Anomaly`] (\(p=118\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski119Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski119Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski119Anomaly {
    /// Empty log Minkowski-\(p=119\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski119_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski119Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski119 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=119 k-NN anomaly",
                "LogMinkowski119Anomaly is L119 on |ln|; not L118 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski119Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-179 windowed one-hundred-seventy-ninth-difference anomaly.
///
/// The 179th backward difference is formed by successive first differences
/// (`C(179, 89)` is not an exact `f64` integer). Distinct from [`WindowLag178`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag179 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag179 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag179 {
    /// Empty windowed lag-179 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 179 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(179);
        let win = &xs[start..];
        if win.len() < 179 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..179 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag179 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 179;
        q.warmup = self.rows.len() < 179;
        q.explanation = format!("window-lag179 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-seventy-ninth-difference anomaly",
                "WindowLag179 uses successive first differences and cap 192; not WindowLag178",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag179 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski120_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(120);
    }
    s.powf(1.0 / 120.0)
}

/// Log Minkowski \(p=120\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{120})^{1/120}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski119Anomaly`] (\(p=119\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski120Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski120Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski120Anomaly {
    /// Empty log Minkowski-\(p=120\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski120_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski120Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski120 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=120 k-NN anomaly",
                "LogMinkowski120Anomaly is L120 on |ln|; not L119 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski120Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-180 windowed one-hundred-eightieth-difference anomaly.
///
/// The 180th backward difference is formed by successive first differences
/// (`C(180, 90)` is not an exact `f64` integer). Distinct from [`WindowLag179`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag180 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag180 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag180 {
    /// Empty windowed lag-180 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 180 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(180);
        let win = &xs[start..];
        if win.len() < 180 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..180 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag180 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 180;
        q.warmup = self.rows.len() < 180;
        q.explanation = format!("window-lag180 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eightieth-difference anomaly",
                "WindowLag180 uses successive first differences and cap 192; not WindowLag179",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag180 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski121_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(121);
    }
    s.powf(1.0 / 121.0)
}

/// Log Minkowski \(p=121\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{118})^{1/118}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski120Anomaly`] (\(p=120\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski121Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski121Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski121Anomaly {
    /// Empty log Minkowski-\(p=121\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski121_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski121Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski121 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=121 k-NN anomaly",
                "LogMinkowski121Anomaly is L121 on |ln|; not L120 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski121Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}



/// Length-181 windowed one-hundred-eighty-first-difference anomaly.
///
/// The 181st backward difference is formed by successive first differences
/// (`C(181, 90)` is not an exact `f64` integer). Distinct from [`WindowLag180`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag181 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag181 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag181 {
    /// Empty windowed lag-181 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 181 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(181);
        let win = &xs[start..];
        if win.len() < 181 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..181 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag181 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 181;
        q.warmup = self.rows.len() < 181;
        q.explanation = format!("window-lag181 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-first-difference anomaly",
                "WindowLag181 uses successive first differences and cap 192; not WindowLag180",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag181 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski122_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(122);
    }
    s.powf(1.0 / 122.0)
}

/// Log Minkowski \(p=122\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{122})^{1/122}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski121Anomaly`] (\(p=121\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski122Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski122Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski122Anomaly {
    /// Empty log Minkowski-\(p=122\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski122_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski122Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski122 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=122 k-NN anomaly",
                "LogMinkowski122Anomaly is L122 on |ln|; not L121 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski122Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-182 windowed one-hundred-eighty-second-difference anomaly.
///
/// The 182nd backward difference is formed by successive first differences
/// (`C(182, 91)` is not an exact `f64` integer). Distinct from [`WindowLag181`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag182 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag182 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag182 {
    /// Empty windowed lag-182 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 182 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(182);
        let win = &xs[start..];
        if win.len() < 182 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..182 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag182 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 182;
        q.warmup = self.rows.len() < 182;
        q.explanation = format!("window-lag182 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-second-difference anomaly",
                "WindowLag182 uses successive first differences and cap 192; not WindowLag181",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag182 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski123_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(123);
    }
    s.powf(1.0 / 123.0)
}

/// Log Minkowski \(p=123\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{123})^{1/122}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski122Anomaly`] (\(p=122\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski123Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski123Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski123Anomaly {
    /// Empty log Minkowski-\(p=123\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski123_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski123Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski123 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=123 k-NN anomaly",
                "LogMinkowski123Anomaly is L123 on |ln|; not L122 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski123Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-183 windowed one-hundred-eighty-third-difference anomaly.
///
/// The 183rd backward difference is formed by successive first differences
/// (`C(183, 91)` is not an exact `f64` integer). Distinct from [`WindowLag182`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag183 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag183 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag183 {
    /// Empty windowed lag-183 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 183 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(183);
        let win = &xs[start..];
        if win.len() < 183 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..183 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag183 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 183;
        q.warmup = self.rows.len() < 183;
        q.explanation = format!("window-lag183 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-third-difference anomaly",
                "WindowLag183 uses successive first differences and cap 192; not WindowLag182",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag183 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski124_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(124);
    }
    s.powf(1.0 / 124.0)
}

/// Log Minkowski \(p=124\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{124})^{1/124}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski123Anomaly`] (\(p=123\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski124Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski124Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski124Anomaly {
    /// Empty log Minkowski-\(p=124\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski124_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski124Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski124 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=124 k-NN anomaly",
                "LogMinkowski124Anomaly is L124 on |ln|; not L123 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski124Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-184 windowed one-hundred-eighty-fourth-difference anomaly.
///
/// The 184th backward difference is formed by successive first differences
/// (`C(184, 92)` is not an exact `f64` integer). Distinct from [`WindowLag183`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag184 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag184 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag184 {
    /// Empty windowed lag-184 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 184 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(184);
        let win = &xs[start..];
        if win.len() < 184 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..184 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag184 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 184;
        q.warmup = self.rows.len() < 184;
        q.explanation = format!("window-lag184 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-fourth-difference anomaly",
                "WindowLag184 uses successive first differences and cap 192; not WindowLag183",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag184 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski125_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(125);
    }
    s.powf(1.0 / 125.0)
}

/// Log Minkowski \(p=125\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{125})^{1/124}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski124Anomaly`] (\(p=124\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski125Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski125Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski125Anomaly {
    /// Empty log Minkowski-\(p=125\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski125_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski125Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski125 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=125 k-NN anomaly",
                "LogMinkowski125Anomaly is L125 on |ln|; not L124 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski125Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-185 windowed one-hundred-eighty-fifth-difference anomaly.
///
/// The 185th backward difference is formed by successive first differences
/// (`C(185, 92)` is not an exact `f64` integer). Distinct from [`WindowLag184`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag185 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag185 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag185 {
    /// Empty windowed lag-185 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 185 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(185);
        let win = &xs[start..];
        if win.len() < 185 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..185 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag185 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 185;
        q.warmup = self.rows.len() < 185;
        q.explanation = format!("window-lag185 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-fifth-difference anomaly",
                "WindowLag185 uses successive first differences and cap 192; not WindowLag184",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag185 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski126_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(126);
    }
    s.powf(1.0 / 126.0)
}

/// Log Minkowski \(p=126\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{126})^{1/126}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski125Anomaly`] (\(p=125\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski126Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski126Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski126Anomaly {
    /// Empty log Minkowski-\(p=126\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski126_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski126Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski126 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=126 k-NN anomaly",
                "LogMinkowski126Anomaly is L126 on |ln|; not L125 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski126Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-186 windowed one-hundred-eighty-sixth-difference anomaly.
///
/// The 186th backward difference is formed by successive first differences
/// (`C(186, 93)` is not an exact `f64` integer). Distinct from [`WindowLag185`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag186 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag186 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag186 {
    /// Empty windowed lag-186 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 186 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(186);
        let win = &xs[start..];
        if win.len() < 186 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..186 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag186 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 186;
        q.warmup = self.rows.len() < 186;
        q.explanation = format!("window-lag186 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-sixth-difference anomaly",
                "WindowLag186 uses successive first differences and cap 192; not WindowLag185",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag186 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski127_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(127);
    }
    s.powf(1.0 / 127.0)
}

/// Log Minkowski \(p=127\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{127})^{1/126}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski126Anomaly`] (\(p=126\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski127Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski127Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski127Anomaly {
    /// Empty log Minkowski-\(p=127\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski127_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski127Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski127 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=127 k-NN anomaly",
                "LogMinkowski127Anomaly is L127 on |ln|; not L126 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski127Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-187 windowed one-hundred-eighty-seventh-difference anomaly.
///
/// The 187th backward difference is formed by successive first differences
/// (`C(187, 93)` is not an exact `f64` integer). Distinct from [`WindowLag186`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag187 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag187 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag187 {
    /// Empty windowed lag-187 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 187 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(187);
        let win = &xs[start..];
        if win.len() < 187 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..187 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag187 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 187;
        q.warmup = self.rows.len() < 187;
        q.explanation = format!("window-lag187 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-seventh-difference anomaly",
                "WindowLag187 uses successive first differences and cap 192; not WindowLag186",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag187 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski128_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(128);
    }
    s.powf(1.0 / 128.0)
}

/// Log Minkowski \(p=128\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{128})^{1/128}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski127Anomaly`] (\(p=127\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski128Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski128Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski128Anomaly {
    /// Empty log Minkowski-\(p=128\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski128_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski128Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski128 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=128 k-NN anomaly",
                "LogMinkowski128Anomaly is L128 on |ln|; not L127 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski128Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-188 windowed one-hundred-eighty-eighth-difference anomaly.
///
/// The 188th backward difference is formed by successive first differences
/// (`C(188, 94)` is not an exact `f64` integer). Distinct from [`WindowLag187`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag188 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag188 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag188 {
    /// Empty windowed lag-188 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 188 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(188);
        let win = &xs[start..];
        if win.len() < 188 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..188 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag188 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 188;
        q.warmup = self.rows.len() < 188;
        q.explanation = format!("window-lag188 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-eighth-difference anomaly",
                "WindowLag188 uses successive first differences and cap 192; not WindowLag187",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag188 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski129_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(129);
    }
    s.powf(1.0 / 129.0)
}

/// Log Minkowski \(p=129\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{129})^{1/128}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski128Anomaly`] (\(p=128\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski129Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski129Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski129Anomaly {
    /// Empty log Minkowski-\(p=129\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski129_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski129Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski129 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=129 k-NN anomaly",
                "LogMinkowski129Anomaly is L129 on |ln|; not L128 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski129Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-189 windowed one-hundred-eighty-ninth-difference anomaly.
///
/// The 189th backward difference is formed by successive first differences
/// (`C(189, 94)` is not an exact `f64` integer). Distinct from [`WindowLag188`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag189 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag189 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag189 {
    /// Empty windowed lag-189 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 189 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(189);
        let win = &xs[start..];
        if win.len() < 189 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..189 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag189 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 189;
        q.warmup = self.rows.len() < 189;
        q.explanation = format!("window-lag189 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-eighty-ninth-difference anomaly",
                "WindowLag189 uses successive first differences and cap 192; not WindowLag188",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag189 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski130_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(130);
    }
    s.powf(1.0 / 130.0)
}

/// Log Minkowski \(p=130\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{130})^{1/130}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski129Anomaly`] (\(p=129\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski130Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski130Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski130Anomaly {
    /// Empty log Minkowski-\(p=130\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski130_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski130Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski130 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=130 k-NN anomaly",
                "LogMinkowski130Anomaly is L130 on |ln|; not L129 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski130Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-190 windowed one-hundred-ninetieth-difference anomaly.
///
/// The 190th backward difference is formed by successive first differences
/// (`C(190, 95)` is not an exact `f64` integer). Distinct from [`WindowLag189`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag190 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag190 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag190 {
    /// Empty windowed lag-190 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 190 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(190);
        let win = &xs[start..];
        if win.len() < 190 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..190 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag190 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 190;
        q.warmup = self.rows.len() < 190;
        q.explanation = format!("window-lag190 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninetieth-difference anomaly",
                "WindowLag190 uses successive first differences and cap 192; not WindowLag189",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag190 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski131_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(131);
    }
    s.powf(1.0 / 131.0)
}

/// Log Minkowski \(p=131\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{131})^{1/130}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski130Anomaly`] (\(p=130\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski131Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski131Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski131Anomaly {
    /// Empty log Minkowski-\(p=131\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski131_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski131Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski131 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=131 k-NN anomaly",
                "LogMinkowski131Anomaly is L131 on |ln|; not L130 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski131Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-191 windowed one-hundred-ninety-first-difference anomaly.
///
/// The 191st backward difference is formed by successive first differences
/// (`C(191, 95)` is not an exact `f64` integer). Distinct from [`WindowLag190`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag191 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag191 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag191 {
    /// Empty windowed lag-191 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 191 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(191);
        let win = &xs[start..];
        if win.len() < 191 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..191 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag191 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 191;
        q.warmup = self.rows.len() < 191;
        q.explanation = format!("window-lag191 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-first-difference anomaly",
                "WindowLag191 uses successive first differences and cap 192; not WindowLag190",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag191 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski132_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(132);
    }
    s.powf(1.0 / 132.0)
}

/// Log Minkowski \(p=132\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{132})^{1/132}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski131Anomaly`] (\(p=131\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski132Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski132Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski132Anomaly {
    /// Empty log Minkowski-\(p=132\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski132_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski132Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski132 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=132 k-NN anomaly",
                "LogMinkowski132Anomaly is L132 on |ln|; not L131 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski132Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-192 windowed one-hundred-ninety-second-difference anomaly.
///
/// The 192nd backward difference is formed by successive first differences
/// (`C(192, 96)` is not an exact `f64` integer). Distinct from [`WindowLag191`].
/// Window length is not identification `p`. Reservoir cap is 192.
#[derive(Clone, Debug)]
pub struct WindowLag192 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag192 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag192 {
    /// Empty windowed lag-192 detector (reservoir cap 192).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 192 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(192);
        let win = &xs[start..];
        if win.len() < 192 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..192 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag192 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 192 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 192;
        q.warmup = self.rows.len() < 192;
        q.explanation = format!("window-lag192 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-second-difference anomaly",
                "WindowLag192 uses successive first differences and cap 192; not WindowLag191",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag192 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski133_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(133);
    }
    s.powf(1.0 / 133.0)
}

/// Log Minkowski \(p=133\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{133})^{1/132}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski132Anomaly`] (\(p=132\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski133Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski133Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski133Anomaly {
    /// Empty log Minkowski-\(p=133\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski133_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski133Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski133 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=133 k-NN anomaly",
                "LogMinkowski133Anomaly is L133 on |ln|; not L132 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski133Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-193 windowed one-hundred-ninety-third-difference anomaly.
///
/// The 193rd backward difference is formed by successive first differences
/// (`C(193, 96)` is not an exact `f64` integer). Distinct from [`WindowLag192`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag193 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag193 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag193 {
    /// Empty windowed lag-193 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 193 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(193);
        let win = &xs[start..];
        if win.len() < 193 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..193 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag193 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 193;
        q.warmup = self.rows.len() < 193;
        q.explanation = format!("window-lag193 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-third-difference anomaly",
                "WindowLag193 uses successive first differences and cap 208; not WindowLag192",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag193 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski134_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(134);
    }
    s.powf(1.0 / 134.0)
}

/// Log Minkowski \(p=134\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{134})^{1/134}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski133Anomaly`] (\(p=133\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski134Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski134Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski134Anomaly {
    /// Empty log Minkowski-\(p=134\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski134_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski134Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski134 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=134 k-NN anomaly",
                "LogMinkowski134Anomaly is L134 on |ln|; not L133 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski134Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-194 windowed one-hundred-ninety-fourth-difference anomaly.
///
/// The 194th backward difference is formed by successive first differences
/// (`C(194, 97)` is not an exact `f64` integer). Distinct from [`WindowLag193`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag194 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag194 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag194 {
    /// Empty windowed lag-194 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 194 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(194);
        let win = &xs[start..];
        if win.len() < 194 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..194 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag194 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 194;
        q.warmup = self.rows.len() < 194;
        q.explanation = format!("window-lag194 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-fourth-difference anomaly",
                "WindowLag194 uses successive first differences and cap 208; not WindowLag193",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag194 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski135_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(135);
    }
    s.powf(1.0 / 135.0)
}

/// Log Minkowski \(p=135\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{135})^{1/134}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski134Anomaly`] (\(p=134\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski135Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski135Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski135Anomaly {
    /// Empty log Minkowski-\(p=135\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski135_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski135Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski135 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=135 k-NN anomaly",
                "LogMinkowski135Anomaly is L135 on |ln|; not L134 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski135Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-195 windowed one-hundred-ninety-fifth-difference anomaly.
///
/// The 195th backward difference is formed by successive first differences
/// (`C(195, 97)` is not an exact `f64` integer). Distinct from [`WindowLag194`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag195 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag195 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag195 {
    /// Empty windowed lag-195 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 195 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(195);
        let win = &xs[start..];
        if win.len() < 195 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..195 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag195 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 195;
        q.warmup = self.rows.len() < 195;
        q.explanation = format!("window-lag195 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-fifth-difference anomaly",
                "WindowLag195 uses successive first differences and cap 208; not WindowLag194",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag195 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski136_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(136);
    }
    s.powf(1.0 / 136.0)
}

/// Log Minkowski \(p=136\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{136})^{1/136}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski135Anomaly`] (\(p=135\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski136Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski136Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski136Anomaly {
    /// Empty log Minkowski-\(p=136\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski136_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski136Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski136 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=136 k-NN anomaly",
                "LogMinkowski136Anomaly is L136 on |ln|; not L135 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski136Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-196 windowed one-hundred-ninety-sixth-difference anomaly.
///
/// The 196th backward difference is formed by successive first differences
/// (`C(196, 98)` is not an exact `f64` integer). Distinct from [`WindowLag195`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag196 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag196 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag196 {
    /// Empty windowed lag-196 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 196 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(196);
        let win = &xs[start..];
        if win.len() < 196 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..196 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag196 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 196;
        q.warmup = self.rows.len() < 196;
        q.explanation = format!("window-lag196 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-sixth-difference anomaly",
                "WindowLag196 uses successive first differences and cap 208; not WindowLag195",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag196 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski137_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(137);
    }
    s.powf(1.0 / 137.0)
}

/// Log Minkowski \(p=137\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{137})^{1/137}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski136Anomaly`] (\(p=136\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski137Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski137Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski137Anomaly {
    /// Empty log Minkowski-\(p=137\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski137_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski137Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski137 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=137 k-NN anomaly",
                "LogMinkowski137Anomaly is L137 on |ln|; not L136 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski137Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-197 windowed one-hundred-ninety-seventh-difference anomaly.
///
/// The 197th backward difference is formed by successive first differences
/// (`C(197, 98)` is not an exact `f64` integer). Distinct from [`WindowLag196`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag197 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag197 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag197 {
    /// Empty windowed lag-197 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 197 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(197);
        let win = &xs[start..];
        if win.len() < 197 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..197 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag197 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 197;
        q.warmup = self.rows.len() < 197;
        q.explanation = format!("window-lag197 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-seventh-difference anomaly",
                "WindowLag197 uses successive first differences and cap 208; not WindowLag196",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag197 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski138_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(138);
    }
    s.powf(1.0 / 138.0)
}

/// Log Minkowski \(p=138\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{138})^{1/138}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski137Anomaly`] (\(p=137\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski138Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski138Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski138Anomaly {
    /// Empty log Minkowski-\(p=138\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski138_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski138Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski138 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=138 k-NN anomaly",
                "LogMinkowski138Anomaly is L138 on |ln|; not L137 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski138Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-198 windowed one-hundred-ninety-eighth-difference anomaly.
///
/// The 198th backward difference is formed by successive first differences
/// (`C(198, 99)` is not an exact `f64` integer). Distinct from [`WindowLag197`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag198 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag198 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag198 {
    /// Empty windowed lag-198 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 198 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(198);
        let win = &xs[start..];
        if win.len() < 198 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..198 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag198 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 198;
        q.warmup = self.rows.len() < 198;
        q.explanation = format!("window-lag198 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-eighth-difference anomaly",
                "WindowLag198 uses successive first differences and cap 208; not WindowLag197",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag198 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski139_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(139);
    }
    s.powf(1.0 / 139.0)
}

/// Log Minkowski \(p=139\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{139})^{1/139}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski138Anomaly`] (\(p=138\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski139Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski139Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski139Anomaly {
    /// Empty log Minkowski-\(p=139\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski139_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski139Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski139 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=139 k-NN anomaly",
                "LogMinkowski139Anomaly is L139 on |ln|; not L138 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski139Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-199 windowed one-hundred-ninety-ninth-difference anomaly.
///
/// The 199th backward difference is formed by successive first differences
/// (`C(199, 99)` is not an exact `f64` integer). Distinct from [`WindowLag198`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag199 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag199 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag199 {
    /// Empty windowed lag-199 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 199 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(199);
        let win = &xs[start..];
        if win.len() < 199 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..199 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag199 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 199;
        q.warmup = self.rows.len() < 199;
        q.explanation = format!("window-lag199 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed one-hundred-ninety-ninth-difference anomaly",
                "WindowLag199 uses successive first differences and cap 208; not WindowLag198",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag199 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski140_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(140);
    }
    s.powf(1.0 / 140.0)
}

/// Log Minkowski \(p=140\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{140})^{1/140}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski139Anomaly`] (\(p=139\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski140Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski140Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski140Anomaly {
    /// Empty log Minkowski-\(p=140\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski140_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski140Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski140 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=140 k-NN anomaly",
                "LogMinkowski140Anomaly is L140 on |ln|; not L139 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski140Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-200 windowed two-hundredth-difference anomaly.
///
/// The 200th backward difference is formed by successive first differences
/// (`C(200, 100)` is not an exact `f64` integer). Distinct from [`WindowLag199`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag200 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag200 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag200 {
    /// Empty windowed lag-200 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 200 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(200);
        let win = &xs[start..];
        if win.len() < 200 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..200 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag200 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 200;
        q.warmup = self.rows.len() < 200;
        q.explanation = format!("window-lag200 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundredth-difference anomaly",
                "WindowLag200 uses successive first differences and cap 208; not WindowLag199",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag200 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski141_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(141);
    }
    s.powf(1.0 / 141.0)
}

/// Log Minkowski \(p=141\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{141})^{1/141}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski140Anomaly`] (\(p=140\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski141Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski141Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski141Anomaly {
    /// Empty log Minkowski-\(p=141\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski141_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski141Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski141 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=141 k-NN anomaly",
                "LogMinkowski141Anomaly is L141 on |ln|; not L140 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski141Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-201 windowed two-hundred-first-difference anomaly.
///
/// The 201st backward difference is formed by successive first differences
/// (`C(201, 100)` is not an exact `f64` integer). Distinct from [`WindowLag200`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag201 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag201 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag201 {
    /// Empty windowed lag-201 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 201 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(201);
        let win = &xs[start..];
        if win.len() < 201 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..201 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag201 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 201;
        q.warmup = self.rows.len() < 201;
        q.explanation = format!("window-lag201 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-first-difference anomaly",
                "WindowLag201 uses successive first differences and cap 208; not WindowLag200",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag201 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski142_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(142);
    }
    s.powf(1.0 / 142.0)
}

/// Log Minkowski \(p=142\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{142})^{1/142}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski141Anomaly`] (\(p=141\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski142Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski142Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski142Anomaly {
    /// Empty log Minkowski-\(p=142\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski142_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski142Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski142 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=142 k-NN anomaly",
                "LogMinkowski142Anomaly is L142 on |ln|; not L141 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski142Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-202 windowed two-hundred-second-difference anomaly.
///
/// The 202nd backward difference is formed by successive first differences
/// (`C(202, 101)` is not an exact `f64` integer). Distinct from [`WindowLag201`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag202 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag202 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag202 {
    /// Empty windowed lag-202 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 202 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(202);
        let win = &xs[start..];
        if win.len() < 202 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..202 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag202 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 202;
        q.warmup = self.rows.len() < 202;
        q.explanation = format!("window-lag202 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-second-difference anomaly",
                "WindowLag202 uses successive first differences and cap 208; not WindowLag201",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag202 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski143_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(143);
    }
    s.powf(1.0 / 143.0)
}

/// Log Minkowski \(p=143\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{143})^{1/143}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski142Anomaly`] (\(p=142\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski143Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski143Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski143Anomaly {
    /// Empty log Minkowski-\(p=143\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski143_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski143Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski143 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=143 k-NN anomaly",
                "LogMinkowski143Anomaly is L143 on |ln|; not L142 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski143Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-203 windowed two-hundred-third-difference anomaly.
///
/// The 203rd backward difference is formed by successive first differences
/// (`C(203, 101)` is not an exact `f64` integer). Distinct from [`WindowLag202`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag203 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag203 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag203 {
    /// Empty windowed lag-203 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 203 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(203);
        let win = &xs[start..];
        if win.len() < 203 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..203 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag203 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 203;
        q.warmup = self.rows.len() < 203;
        q.explanation = format!("window-lag203 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-third-difference anomaly",
                "WindowLag203 uses successive first differences and cap 208; not WindowLag202",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag203 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski144_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(144);
    }
    s.powf(1.0 / 144.0)
}

/// Log Minkowski \(p=144\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{144})^{1/144}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski143Anomaly`] (\(p=143\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski144Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski144Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski144Anomaly {
    /// Empty log Minkowski-\(p=144\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski144_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski144Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski144 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=144 k-NN anomaly",
                "LogMinkowski144Anomaly is L144 on |ln|; not L143 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski144Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-204 windowed two-hundred-fourth-difference anomaly.
///
/// The 204th backward difference is formed by successive first differences
/// (`C(204, 102)` is not an exact `f64` integer). Distinct from [`WindowLag203`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag204 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag204 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag204 {
    /// Empty windowed lag-204 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 204 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(204);
        let win = &xs[start..];
        if win.len() < 204 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..204 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag204 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 204;
        q.warmup = self.rows.len() < 204;
        q.explanation = format!("window-lag204 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-fourth-difference anomaly",
                "WindowLag204 uses successive first differences and cap 208; not WindowLag203",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag204 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski145_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(145);
    }
    s.powf(1.0 / 145.0)
}

/// Log Minkowski \(p=145\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{145})^{1/145}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski144Anomaly`] (\(p=144\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski145Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski145Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski145Anomaly {
    /// Empty log Minkowski-\(p=145\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski145_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski145Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski145 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=145 k-NN anomaly",
                "LogMinkowski145Anomaly is L145 on |ln|; not L144 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski145Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-205 windowed two-hundred-fifth-difference anomaly.
///
/// The 205th backward difference is formed by successive first differences
/// (`C(205, 102)` is not an exact `f64` integer). Distinct from [`WindowLag204`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag205 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag205 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag205 {
    /// Empty windowed lag-205 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 205 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(205);
        let win = &xs[start..];
        if win.len() < 205 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..205 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag205 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 205;
        q.warmup = self.rows.len() < 205;
        q.explanation = format!("window-lag205 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-fifth-difference anomaly",
                "WindowLag205 uses successive first differences and cap 208; not WindowLag204",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag205 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski146_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(146);
    }
    s.powf(1.0 / 146.0)
}

/// Log Minkowski \(p=146\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{146})^{1/146}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski145Anomaly`] (\(p=145\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski146Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski146Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski146Anomaly {
    /// Empty log Minkowski-\(p=146\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski146_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski146Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski146 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=146 k-NN anomaly",
                "LogMinkowski146Anomaly is L146 on |ln|; not L145 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski146Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-206 windowed two-hundred-sixth-difference anomaly.
///
/// The 206th backward difference is formed by successive first differences
/// (`C(206, 103)` is not an exact `f64` integer). Distinct from [`WindowLag205`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag206 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag206 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag206 {
    /// Empty windowed lag-206 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 206 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(206);
        let win = &xs[start..];
        if win.len() < 206 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..206 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag206 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 206;
        q.warmup = self.rows.len() < 206;
        q.explanation = format!("window-lag206 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-sixth-difference anomaly",
                "WindowLag206 uses successive first differences and cap 208; not WindowLag205",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag206 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski147_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(147);
    }
    s.powf(1.0 / 147.0)
}

/// Log Minkowski \(p=147\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{147})^{1/147}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski146Anomaly`] (\(p=146\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski147Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski147Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski147Anomaly {
    /// Empty log Minkowski-\(p=147\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski147_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski147Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski147 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=147 k-NN anomaly",
                "LogMinkowski147Anomaly is L147 on |ln|; not L146 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski147Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-207 windowed two-hundred-seventh-difference anomaly.
///
/// The 207th backward difference is formed by successive first differences
/// (`C(207, 103)` is not an exact `f64` integer). Distinct from [`WindowLag206`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag207 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag207 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag207 {
    /// Empty windowed lag-207 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 207 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(207);
        let win = &xs[start..];
        if win.len() < 207 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..207 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag207 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 207;
        q.warmup = self.rows.len() < 207;
        q.explanation = format!("window-lag207 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-seventh-difference anomaly",
                "WindowLag207 uses successive first differences and cap 208; not WindowLag206",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag207 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski148_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(148);
    }
    s.powf(1.0 / 148.0)
}

/// Log Minkowski \(p=148\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{148})^{1/148}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski147Anomaly`] (\(p=147\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski148Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski148Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski148Anomaly {
    /// Empty log Minkowski-\(p=148\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski148_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski148Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski148 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=148 k-NN anomaly",
                "LogMinkowski148Anomaly is L148 on |ln|; not L147 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski148Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-208 windowed two-hundred-eighth-difference anomaly.
///
/// The 208th backward difference is formed by successive first differences
/// (`C(208, 104)` is not an exact `f64` integer). Distinct from [`WindowLag207`].
/// Window length is not identification `p`. Reservoir cap is 208.
#[derive(Clone, Debug)]
pub struct WindowLag208 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag208 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag208 {
    /// Empty windowed lag-208 detector (reservoir cap 208).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 208 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(208);
        let win = &xs[start..];
        if win.len() < 208 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..208 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag208 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 208 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 208;
        q.warmup = self.rows.len() < 208;
        q.explanation = format!("window-lag208 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-eighth-difference anomaly",
                "WindowLag208 uses successive first differences and cap 208; not WindowLag207",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag208 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski149_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(149);
    }
    s.powf(1.0 / 149.0)
}

/// Log Minkowski \(p=149\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{149})^{1/149}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski148Anomaly`] (\(p=148\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski149Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski149Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski149Anomaly {
    /// Empty log Minkowski-\(p=149\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski149_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski149Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski149 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=149 k-NN anomaly",
                "LogMinkowski149Anomaly is L149 on |ln|; not L148 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski149Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-209 windowed two-hundred-ninth-difference anomaly.
///
/// The 209th backward difference is formed by successive first differences
/// (`C(209, 104)` is not an exact `f64` integer). Distinct from [`WindowLag208`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag209 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag209 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag209 {
    /// Empty windowed lag-209 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 209 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(209);
        let win = &xs[start..];
        if win.len() < 209 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..209 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag209 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 209;
        q.warmup = self.rows.len() < 209;
        q.explanation = format!("window-lag209 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-ninth-difference anomaly",
                "WindowLag209 uses successive first differences and cap 224; not WindowLag208",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag209 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski150_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(150);
    }
    s.powf(1.0 / 150.0)
}

/// Log Minkowski \(p=150\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{150})^{1/150}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski149Anomaly`] (\(p=149\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski150Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski150Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski150Anomaly {
    /// Empty log Minkowski-\(p=150\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski150_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski150Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski150 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=150 k-NN anomaly",
                "LogMinkowski150Anomaly is L150 on |ln|; not L149 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski150Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-210 windowed two-hundred-tenth-difference anomaly.
///
/// The 210th backward difference is formed by successive first differences
/// (`C(210, 105)` is not an exact `f64` integer). Distinct from [`WindowLag209`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag210 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag210 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag210 {
    /// Empty windowed lag-210 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 210 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(210);
        let win = &xs[start..];
        if win.len() < 210 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..210 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag210 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 210;
        q.warmup = self.rows.len() < 210;
        q.explanation = format!("window-lag210 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-tenth-difference anomaly",
                "WindowLag210 uses successive first differences and cap 224; not WindowLag209",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag210 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski151_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(151);
    }
    s.powf(1.0 / 151.0)
}

/// Log Minkowski \(p=151\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{151})^{1/151}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski150Anomaly`] (\(p=150\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski151Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski151Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski151Anomaly {
    /// Empty log Minkowski-\(p=151\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski151_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski151Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski151 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=151 k-NN anomaly",
                "LogMinkowski151Anomaly is L151 on |ln|; not L150 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski151Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-211 windowed two-hundred-eleventh-difference anomaly.
///
/// The 211th backward difference is formed by successive first differences
/// (`C(211, 105)` is not an exact `f64` integer). Distinct from [`WindowLag210`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag211 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag211 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag211 {
    /// Empty windowed lag-211 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 211 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(211);
        let win = &xs[start..];
        if win.len() < 211 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..211 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag211 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 211;
        q.warmup = self.rows.len() < 211;
        q.explanation = format!("window-lag211 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-eleventh-difference anomaly",
                "WindowLag211 uses successive first differences and cap 224; not WindowLag210",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag211 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski152_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(152);
    }
    s.powf(1.0 / 152.0)
}

/// Log Minkowski \(p=152\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{152})^{1/152}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski151Anomaly`] (\(p=151\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski152Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski152Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski152Anomaly {
    /// Empty log Minkowski-\(p=152\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski152_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski152Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski152 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=152 k-NN anomaly",
                "LogMinkowski152Anomaly is L152 on |ln|; not L151 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski152Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-212 windowed two-hundred-twelfth-difference anomaly.
///
/// The 212th backward difference is formed by successive first differences
/// (`C(212, 106)` is not an exact `f64` integer). Distinct from [`WindowLag211`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag212 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag212 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag212 {
    /// Empty windowed lag-212 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 212 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(212);
        let win = &xs[start..];
        if win.len() < 212 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..212 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag212 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 212;
        q.warmup = self.rows.len() < 212;
        q.explanation = format!("window-lag212 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twelfth-difference anomaly",
                "WindowLag212 uses successive first differences and cap 224; not WindowLag211",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag212 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski153_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(153);
    }
    s.powf(1.0 / 153.0)
}

/// Log Minkowski \(p=153\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{153})^{1/153}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski152Anomaly`] (\(p=152\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski153Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski153Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski153Anomaly {
    /// Empty log Minkowski-\(p=153\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski153_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski153Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski153 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=153 k-NN anomaly",
                "LogMinkowski153Anomaly is L153 on |ln|; not L152 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski153Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-213 windowed two-hundred-thirteenth-difference anomaly.
///
/// The 213th backward difference is formed by successive first differences
/// (`C(213, 106)` is not an exact `f64` integer). Distinct from [`WindowLag212`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag213 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag213 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag213 {
    /// Empty windowed lag-213 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 213 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(213);
        let win = &xs[start..];
        if win.len() < 213 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..213 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag213 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 213;
        q.warmup = self.rows.len() < 213;
        q.explanation = format!("window-lag213 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirteenth-difference anomaly",
                "WindowLag213 uses successive first differences and cap 224; not WindowLag212",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag213 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski154_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(154);
    }
    s.powf(1.0 / 154.0)
}

/// Log Minkowski \(p=154\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{154})^{1/154}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski153Anomaly`] (\(p=153\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski154Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski154Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski154Anomaly {
    /// Empty log Minkowski-\(p=154\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski154_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski154Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski154 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=154 k-NN anomaly",
                "LogMinkowski154Anomaly is L154 on |ln|; not L153 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski154Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-214 windowed two-hundred-fourteenth-difference anomaly.
///
/// The 214th backward difference is formed by successive first differences
/// (`C(214, 107)` is not an exact `f64` integer). Distinct from [`WindowLag213`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag214 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag214 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag214 {
    /// Empty windowed lag-214 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 214 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(214);
        let win = &xs[start..];
        if win.len() < 214 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..214 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag214 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 214;
        q.warmup = self.rows.len() < 214;
        q.explanation = format!("window-lag214 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-fourteenth-difference anomaly",
                "WindowLag214 uses successive first differences and cap 224; not WindowLag213",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag214 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski155_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(155);
    }
    s.powf(1.0 / 155.0)
}

/// Log Minkowski \(p=155\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{155})^{1/155}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski154Anomaly`] (\(p=154\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski155Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski155Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski155Anomaly {
    /// Empty log Minkowski-\(p=155\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski155_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski155Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski155 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=155 k-NN anomaly",
                "LogMinkowski155Anomaly is L155 on |ln|; not L154 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski155Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-215 windowed two-hundred-fifteenth-difference anomaly.
///
/// The 215th backward difference is formed by successive first differences
/// (`C(215, 107)` is not an exact `f64` integer). Distinct from [`WindowLag214`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag215 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag215 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag215 {
    /// Empty windowed lag-215 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 215 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(215);
        let win = &xs[start..];
        if win.len() < 215 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..215 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag215 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 215;
        q.warmup = self.rows.len() < 215;
        q.explanation = format!("window-lag215 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-fifteenth-difference anomaly",
                "WindowLag215 uses successive first differences and cap 224; not WindowLag214",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag215 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski156_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(156);
    }
    s.powf(1.0 / 156.0)
}

/// Log Minkowski \(p=156\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{156})^{1/156}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski155Anomaly`] (\(p=155\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski156Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski156Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski156Anomaly {
    /// Empty log Minkowski-\(p=156\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski156_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski156Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski156 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=156 k-NN anomaly",
                "LogMinkowski156Anomaly is L156 on |ln|; not L155 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski156Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-216 windowed two-hundred-sixteenth-difference anomaly.
///
/// The 216th backward difference is formed by successive first differences
/// (`C(216, 108)` is not an exact `f64` integer). Distinct from [`WindowLag215`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag216 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag216 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag216 {
    /// Empty windowed lag-216 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 216 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(216);
        let win = &xs[start..];
        if win.len() < 216 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..216 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag216 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 216;
        q.warmup = self.rows.len() < 216;
        q.explanation = format!("window-lag216 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-sixteenth-difference anomaly",
                "WindowLag216 uses successive first differences and cap 224; not WindowLag215",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag216 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski157_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(157);
    }
    s.powf(1.0 / 157.0)
}

/// Log Minkowski \(p=157\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{157})^{1/157}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski156Anomaly`] (\(p=156\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski157Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski157Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski157Anomaly {
    /// Empty log Minkowski-\(p=157\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski157_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski157Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski157 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=157 k-NN anomaly",
                "LogMinkowski157Anomaly is L157 on |ln|; not L156 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski157Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-217 windowed two-hundred-seventeenth-difference anomaly.
///
/// The 217th backward difference is formed by successive first differences
/// (`C(217, 108)` is not an exact `f64` integer). Distinct from [`WindowLag216`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag217 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag217 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag217 {
    /// Empty windowed lag-217 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 217 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(217);
        let win = &xs[start..];
        if win.len() < 217 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..217 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag217 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 217;
        q.warmup = self.rows.len() < 217;
        q.explanation = format!("window-lag217 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-seventeenth-difference anomaly",
                "WindowLag217 uses successive first differences and cap 224; not WindowLag216",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag217 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski158_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(158);
    }
    s.powf(1.0 / 158.0)
}

/// Log Minkowski \(p=158\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{158})^{1/158}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski157Anomaly`] (\(p=157\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski158Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski158Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski158Anomaly {
    /// Empty log Minkowski-\(p=158\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski158_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski158Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski158 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=158 k-NN anomaly",
                "LogMinkowski158Anomaly is L158 on |ln|; not L157 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski158Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-218 windowed two-hundred-eighteenth-difference anomaly.
///
/// The 218th backward difference is formed by successive first differences
/// (`C(218, 109)` is not an exact `f64` integer). Distinct from [`WindowLag217`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag218 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag218 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag218 {
    /// Empty windowed lag-218 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 218 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(218);
        let win = &xs[start..];
        if win.len() < 218 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..218 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag218 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 218;
        q.warmup = self.rows.len() < 218;
        q.explanation = format!("window-lag218 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-eighteenth-difference anomaly",
                "WindowLag218 uses successive first differences and cap 224; not WindowLag217",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag218 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski159_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(159);
    }
    s.powf(1.0 / 159.0)
}

/// Log Minkowski \(p=159\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{159})^{1/159}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski158Anomaly`] (\(p=158\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski159Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski159Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski159Anomaly {
    /// Empty log Minkowski-\(p=159\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski159_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski159Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski159 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=159 k-NN anomaly",
                "LogMinkowski159Anomaly is L159 on |ln|; not L158 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski159Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-219 windowed two-hundred-nineteenth-difference anomaly.
///
/// The 219th backward difference is formed by successive first differences
/// (`C(219, 109)` is not an exact `f64` integer). Distinct from [`WindowLag218`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag219 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag219 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag219 {
    /// Empty windowed lag-219 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 219 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(219);
        let win = &xs[start..];
        if win.len() < 219 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..219 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag219 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 219;
        q.warmup = self.rows.len() < 219;
        q.explanation = format!("window-lag219 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-nineteenth-difference anomaly",
                "WindowLag219 uses successive first differences and cap 224; not WindowLag218",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag219 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski160_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(160);
    }
    s.powf(1.0 / 160.0)
}

/// Log Minkowski \(p=160\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{160})^{1/160}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski159Anomaly`] (\(p=159\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski160Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski160Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski160Anomaly {
    /// Empty log Minkowski-\(p=160\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski160_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski160Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski160 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=160 k-NN anomaly",
                "LogMinkowski160Anomaly is L160 on |ln|; not L159 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski160Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-220 windowed two-hundred-twentieth-difference anomaly.
///
/// The 220th backward difference is formed by successive first differences
/// (`C(220, 110)` is not an exact `f64` integer). Distinct from [`WindowLag219`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag220 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag220 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag220 {
    /// Empty windowed lag-220 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 220 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(220);
        let win = &xs[start..];
        if win.len() < 220 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..220 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag220 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 220;
        q.warmup = self.rows.len() < 220;
        q.explanation = format!("window-lag220 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twentieth-difference anomaly",
                "WindowLag220 uses successive first differences and cap 224; not WindowLag219",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag220 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski161_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(161);
    }
    s.powf(1.0 / 161.0)
}

/// Log Minkowski \(p=161\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{161})^{1/161}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski160Anomaly`] (\(p=160\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski161Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski161Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski161Anomaly {
    /// Empty log Minkowski-\(p=161\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski161_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski161Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski161 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=161 k-NN anomaly",
                "LogMinkowski161Anomaly is L161 on |ln|; not L160 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski161Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-221 windowed two-hundred-twenty-first-difference anomaly.
///
/// The 221st backward difference is formed by successive first differences
/// (`C(221, 110)` is not an exact `f64` integer). Distinct from [`WindowLag220`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag221 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag221 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag221 {
    /// Empty windowed lag-221 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 221 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(221);
        let win = &xs[start..];
        if win.len() < 221 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..221 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag221 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 221;
        q.warmup = self.rows.len() < 221;
        q.explanation = format!("window-lag221 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-first-difference anomaly",
                "WindowLag221 uses successive first differences and cap 224; not WindowLag220",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag221 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski162_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(162);
    }
    s.powf(1.0 / 162.0)
}

/// Log Minkowski \(p=162\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{162})^{1/162}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski161Anomaly`] (\(p=161\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski162Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski162Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski162Anomaly {
    /// Empty log Minkowski-\(p=162\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski162_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski162Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski162 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=162 k-NN anomaly",
                "LogMinkowski162Anomaly is L162 on |ln|; not L161 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski162Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-222 windowed two-hundred-twenty-second-difference anomaly.
///
/// The 222nd backward difference is formed by successive first differences
/// (`C(222, 111)` is not an exact `f64` integer). Distinct from [`WindowLag221`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag222 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag222 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag222 {
    /// Empty windowed lag-222 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 222 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(222);
        let win = &xs[start..];
        if win.len() < 222 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..222 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag222 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 222;
        q.warmup = self.rows.len() < 222;
        q.explanation = format!("window-lag222 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-second-difference anomaly",
                "WindowLag222 uses successive first differences and cap 224; not WindowLag221",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag222 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski163_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(163);
    }
    s.powf(1.0 / 163.0)
}

/// Log Minkowski \(p=163\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{163})^{1/163}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski162Anomaly`] (\(p=162\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski163Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski163Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski163Anomaly {
    /// Empty log Minkowski-\(p=163\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski163_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski163Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski163 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=163 k-NN anomaly",
                "LogMinkowski163Anomaly is L163 on |ln|; not L162 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski163Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-223 windowed two-hundred-twenty-third-difference anomaly.
///
/// The 223rd backward difference is formed by successive first differences
/// (`C(223, 111)` is not an exact `f64` integer). Distinct from [`WindowLag222`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag223 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag223 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag223 {
    /// Empty windowed lag-223 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 223 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(223);
        let win = &xs[start..];
        if win.len() < 223 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..223 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag223 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 223;
        q.warmup = self.rows.len() < 223;
        q.explanation = format!("window-lag223 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-third-difference anomaly",
                "WindowLag223 uses successive first differences and cap 224; not WindowLag222",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag223 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski164_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(164);
    }
    s.powf(1.0 / 164.0)
}

/// Log Minkowski \(p=164\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{164})^{1/164}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski163Anomaly`] (\(p=163\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski164Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski164Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski164Anomaly {
    /// Empty log Minkowski-\(p=164\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski164_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski164Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski164 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=164 k-NN anomaly",
                "LogMinkowski164Anomaly is L164 on |ln|; not L163 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski164Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-224 windowed two-hundred-twenty-fourth-difference anomaly.
///
/// The 224th backward difference is formed by successive first differences
/// (`C(224, 112)` is not an exact `f64` integer). Distinct from [`WindowLag223`].
/// Window length is not identification `p`. Reservoir cap is 224.
#[derive(Clone, Debug)]
pub struct WindowLag224 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag224 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag224 {
    /// Empty windowed lag-224 detector (reservoir cap 224).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 224 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(224);
        let win = &xs[start..];
        if win.len() < 224 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..224 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag224 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 224 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 224;
        q.warmup = self.rows.len() < 224;
        q.explanation = format!("window-lag224 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-fourth-difference anomaly",
                "WindowLag224 uses successive first differences and cap 224; not WindowLag223",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag224 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski165_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(165);
    }
    s.powf(1.0 / 165.0)
}

/// Log Minkowski \(p=165\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{165})^{1/165}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski164Anomaly`] (\(p=164\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski165Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski165Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski165Anomaly {
    /// Empty log Minkowski-\(p=165\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski165_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski165Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski165 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=165 k-NN anomaly",
                "LogMinkowski165Anomaly is L165 on |ln|; not L164 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski165Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-225 windowed two-hundred-twenty-fifth-difference anomaly.
///
/// The 225th backward difference is formed by successive first differences
/// (`C(225, 112)` is not an exact `f64` integer). Distinct from [`WindowLag224`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag225 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag225 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag225 {
    /// Empty windowed lag-225 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 225 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(225);
        let win = &xs[start..];
        if win.len() < 225 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..225 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag225 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 225;
        q.warmup = self.rows.len() < 225;
        q.explanation = format!("window-lag225 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-fifth-difference anomaly",
                "WindowLag225 uses successive first differences and cap 240; not WindowLag224",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag225 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski166_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(166);
    }
    s.powf(1.0 / 166.0)
}

/// Log Minkowski \(p=166\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{166})^{1/166}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski165Anomaly`] (\(p=165\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski166Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski166Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski166Anomaly {
    /// Empty log Minkowski-\(p=166\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski166_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski166Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski166 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=166 k-NN anomaly",
                "LogMinkowski166Anomaly is L166 on |ln|; not L165 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski166Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-226 windowed two-hundred-twenty-sixth-difference anomaly.
///
/// The 226th backward difference is formed by successive first differences
/// (`C(226, 113)` is not an exact `f64` integer). Distinct from [`WindowLag225`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag226 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag226 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag226 {
    /// Empty windowed lag-226 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 226 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(226);
        let win = &xs[start..];
        if win.len() < 226 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..226 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag226 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 226;
        q.warmup = self.rows.len() < 226;
        q.explanation = format!("window-lag226 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-sixth-difference anomaly",
                "WindowLag226 uses successive first differences and cap 240; not WindowLag225",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag226 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski167_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(167);
    }
    s.powf(1.0 / 167.0)
}

/// Log Minkowski \(p=167\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{167})^{1/167}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski166Anomaly`] (\(p=166\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski167Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski167Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski167Anomaly {
    /// Empty log Minkowski-\(p=167\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski167_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski167Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski167 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=167 k-NN anomaly",
                "LogMinkowski167Anomaly is L167 on |ln|; not L166 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski167Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-227 windowed two-hundred-twenty-seventh-difference anomaly.
///
/// The 227th backward difference is formed by successive first differences
/// (`C(227, 113)` is not an exact `f64` integer). Distinct from [`WindowLag226`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag227 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag227 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag227 {
    /// Empty windowed lag-227 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 227 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(227);
        let win = &xs[start..];
        if win.len() < 227 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..227 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag227 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 227;
        q.warmup = self.rows.len() < 227;
        q.explanation = format!("window-lag227 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-seventh-difference anomaly",
                "WindowLag227 uses successive first differences and cap 240; not WindowLag226",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag227 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski168_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(168);
    }
    s.powf(1.0 / 168.0)
}

/// Log Minkowski \(p=168\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{168})^{1/168}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski167Anomaly`] (\(p=167\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski168Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski168Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski168Anomaly {
    /// Empty log Minkowski-\(p=168\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski168_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski168Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski168 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=168 k-NN anomaly",
                "LogMinkowski168Anomaly is L168 on |ln|; not L167 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski168Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-228 windowed two-hundred-twenty-eighth-difference anomaly.
///
/// The 228th backward difference is formed by successive first differences
/// (`C(228, 114)` is not an exact `f64` integer). Distinct from [`WindowLag227`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag228 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag228 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag228 {
    /// Empty windowed lag-228 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 228 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(228);
        let win = &xs[start..];
        if win.len() < 228 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..228 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag228 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 228;
        q.warmup = self.rows.len() < 228;
        q.explanation = format!("window-lag228 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-eighth-difference anomaly",
                "WindowLag228 uses successive first differences and cap 240; not WindowLag227",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag228 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski169_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(169);
    }
    s.powf(1.0 / 169.0)
}

/// Log Minkowski \(p=169\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{169})^{1/169}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski168Anomaly`] (\(p=168\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski169Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski169Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski169Anomaly {
    /// Empty log Minkowski-\(p=169\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski169_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski169Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski169 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=169 k-NN anomaly",
                "LogMinkowski169Anomaly is L169 on |ln|; not L168 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski169Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-229 windowed two-hundred-twenty-ninth-difference anomaly.
///
/// The 229th backward difference is formed by successive first differences
/// (`C(229, 114)` is not an exact `f64` integer). Distinct from [`WindowLag228`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag229 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag229 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag229 {
    /// Empty windowed lag-229 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 229 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(229);
        let win = &xs[start..];
        if win.len() < 229 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..229 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag229 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 229;
        q.warmup = self.rows.len() < 229;
        q.explanation = format!("window-lag229 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-twenty-ninth-difference anomaly",
                "WindowLag229 uses successive first differences and cap 240; not WindowLag228",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag229 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski170_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(170);
    }
    s.powf(1.0 / 170.0)
}

/// Log Minkowski \(p=170\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{170})^{1/170}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski169Anomaly`] (\(p=169\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski170Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski170Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski170Anomaly {
    /// Empty log Minkowski-\(p=170\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski170_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski170Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski170 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=170 k-NN anomaly",
                "LogMinkowski170Anomaly is L170 on |ln|; not L169 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski170Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-230 windowed two-hundred-thirtieth-difference anomaly.
///
/// The 230th backward difference is formed by successive first differences
/// (`C(230, 115)` is not an exact `f64` integer). Distinct from [`WindowLag229`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag230 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag230 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag230 {
    /// Empty windowed lag-230 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 230 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(230);
        let win = &xs[start..];
        if win.len() < 230 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..230 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag230 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 230;
        q.warmup = self.rows.len() < 230;
        q.explanation = format!("window-lag230 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirtieth-difference anomaly",
                "WindowLag230 uses successive first differences and cap 240; not WindowLag229",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag230 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski171_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(171);
    }
    s.powf(1.0 / 171.0)
}

/// Log Minkowski \(p=171\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{171})^{1/171}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski170Anomaly`] (\(p=170\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski171Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski171Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski171Anomaly {
    /// Empty log Minkowski-\(p=171\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski171_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski171Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski171 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=171 k-NN anomaly",
                "LogMinkowski171Anomaly is L171 on |ln|; not L170 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski171Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-231 windowed two-hundred-thirty-first-difference anomaly.
///
/// The 231st backward difference is formed by successive first differences
/// (`C(231, 115)` is not an exact `f64` integer). Distinct from [`WindowLag230`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag231 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag231 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag231 {
    /// Empty windowed lag-231 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 231 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(231);
        let win = &xs[start..];
        if win.len() < 231 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..231 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag231 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 231;
        q.warmup = self.rows.len() < 231;
        q.explanation = format!("window-lag231 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-first-difference anomaly",
                "WindowLag231 uses successive first differences and cap 240; not WindowLag230",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag231 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski172_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(172);
    }
    s.powf(1.0 / 172.0)
}

/// Log Minkowski \(p=172\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{172})^{1/172}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski171Anomaly`] (\(p=171\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski172Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski172Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski172Anomaly {
    /// Empty log Minkowski-\(p=172\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski172_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski172Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski172 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=172 k-NN anomaly",
                "LogMinkowski172Anomaly is L172 on |ln|; not L171 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski172Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-232 windowed two-hundred-thirty-second-difference anomaly.
///
/// The 232nd backward difference is formed by successive first differences
/// (`C(232, 116)` is not an exact `f64` integer). Distinct from [`WindowLag231`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag232 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag232 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag232 {
    /// Empty windowed lag-232 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 232 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(232);
        let win = &xs[start..];
        if win.len() < 232 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..232 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag232 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 232;
        q.warmup = self.rows.len() < 232;
        q.explanation = format!("window-lag232 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-second-difference anomaly",
                "WindowLag232 uses successive first differences and cap 240; not WindowLag231",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag232 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski173_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(173);
    }
    s.powf(1.0 / 173.0)
}

/// Log Minkowski \(p=173\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{173})^{1/173}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski172Anomaly`] (\(p=172\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski173Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski173Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski173Anomaly {
    /// Empty log Minkowski-\(p=173\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski173_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski173Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski173 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=173 k-NN anomaly",
                "LogMinkowski173Anomaly is L173 on |ln|; not L172 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski173Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-233 windowed two-hundred-thirty-third-difference anomaly.
///
/// The 233rd backward difference is formed by successive first differences
/// (`C(233, 116)` is not an exact `f64` integer). Distinct from [`WindowLag232`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag233 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag233 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag233 {
    /// Empty windowed lag-233 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 233 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(233);
        let win = &xs[start..];
        if win.len() < 233 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..233 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag233 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 233;
        q.warmup = self.rows.len() < 233;
        q.explanation = format!("window-lag233 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-third-difference anomaly",
                "WindowLag233 uses successive first differences and cap 240; not WindowLag232",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag233 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski174_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(174);
    }
    s.powf(1.0 / 174.0)
}

/// Log Minkowski \(p=174\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{174})^{1/174}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski173Anomaly`] (\(p=173\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski174Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski174Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski174Anomaly {
    /// Empty log Minkowski-\(p=174\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski174_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski174Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski174 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=174 k-NN anomaly",
                "LogMinkowski174Anomaly is L174 on |ln|; not L173 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski174Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-234 windowed two-hundred-thirty-fourth-difference anomaly.
///
/// The 234th backward difference is formed by successive first differences
/// (`C(234, 117)` is not an exact `f64` integer). Distinct from [`WindowLag233`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag234 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag234 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag234 {
    /// Empty windowed lag-234 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 234 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(234);
        let win = &xs[start..];
        if win.len() < 234 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..234 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag234 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 234;
        q.warmup = self.rows.len() < 234;
        q.explanation = format!("window-lag234 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-fourth-difference anomaly",
                "WindowLag234 uses successive first differences and cap 240; not WindowLag233",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag234 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski175_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(175);
    }
    s.powf(1.0 / 175.0)
}

/// Log Minkowski \(p=175\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{175})^{1/175}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski174Anomaly`] (\(p=174\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski175Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski175Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski175Anomaly {
    /// Empty log Minkowski-\(p=175\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski175_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski175Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski175 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=175 k-NN anomaly",
                "LogMinkowski175Anomaly is L175 on |ln|; not L174 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski175Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-235 windowed two-hundred-thirty-fifth-difference anomaly.
///
/// The 235th backward difference is formed by successive first differences
/// (`C(235, 117)` is not an exact `f64` integer). Distinct from [`WindowLag234`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag235 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag235 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag235 {
    /// Empty windowed lag-235 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 235 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(235);
        let win = &xs[start..];
        if win.len() < 235 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..235 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag235 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 235;
        q.warmup = self.rows.len() < 235;
        q.explanation = format!("window-lag235 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-fifth-difference anomaly",
                "WindowLag235 uses successive first differences and cap 240; not WindowLag234",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag235 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski176_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(176);
    }
    s.powf(1.0 / 176.0)
}

/// Log Minkowski \(p=176\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{176})^{1/176}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski175Anomaly`] (\(p=175\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski176Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski176Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski176Anomaly {
    /// Empty log Minkowski-\(p=176\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski176_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski176Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski176 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=176 k-NN anomaly",
                "LogMinkowski176Anomaly is L176 on |ln|; not L175 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski176Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-236 windowed two-hundred-thirty-sixth-difference anomaly.
///
/// The 236th backward difference is formed by successive first differences
/// (`C(236, 118)` is not an exact `f64` integer). Distinct from [`WindowLag235`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag236 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag236 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag236 {
    /// Empty windowed lag-236 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 236 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(236);
        let win = &xs[start..];
        if win.len() < 236 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..236 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag236 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 236;
        q.warmup = self.rows.len() < 236;
        q.explanation = format!("window-lag236 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-sixth-difference anomaly",
                "WindowLag236 uses successive first differences and cap 240; not WindowLag235",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag236 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski177_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(177);
    }
    s.powf(1.0 / 177.0)
}

/// Log Minkowski \(p=177\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{177})^{1/177}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski176Anomaly`] (\(p=176\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski177Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski177Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski177Anomaly {
    /// Empty log Minkowski-\(p=177\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski177_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski177Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski177 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=177 k-NN anomaly",
                "LogMinkowski177Anomaly is L177 on |ln|; not L176 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski177Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-237 windowed two-hundred-thirty-seventh-difference anomaly.
///
/// The 237th backward difference is formed by successive first differences
/// (`C(237, 118)` is not an exact `f64` integer). Distinct from [`WindowLag236`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag237 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag237 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag237 {
    /// Empty windowed lag-237 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 237 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(237);
        let win = &xs[start..];
        if win.len() < 237 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..237 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag237 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 237;
        q.warmup = self.rows.len() < 237;
        q.explanation = format!("window-lag237 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-seventh-difference anomaly",
                "WindowLag237 uses successive first differences and cap 240; not WindowLag236",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag237 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski178_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(178);
    }
    s.powf(1.0 / 178.0)
}

/// Log Minkowski \(p=178\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{178})^{1/178}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski177Anomaly`] (\(p=177\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski178Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski178Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski178Anomaly {
    /// Empty log Minkowski-\(p=178\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski178_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski178Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski178 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=178 k-NN anomaly",
                "LogMinkowski178Anomaly is L178 on |ln|; not L177 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski178Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}


/// Length-238 windowed two-hundred-thirty-eighth-difference anomaly.
///
/// The 238th backward difference is formed by successive first differences
/// (`C(238, 119)` is not an exact `f64` integer). Distinct from [`WindowLag237`].
/// Window length is not identification `p`. Reservoir cap is 240.
#[derive(Clone, Debug)]
pub struct WindowLag238 {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for WindowLag238 {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl WindowLag238 {
    /// Empty windowed lag-238 detector (reservoir cap 240).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        let y = x.get(i, 0);
        if !y.is_finite() {
            return 0.0;
        }
        let xs = reservoir_first_coords(&self.rows);
        if xs.len() < 238 {
            return 0.0;
        }
        let start = xs.len().saturating_sub(238);
        let win = &xs[start..];
        if win.len() < 238 {
            return 0.0;
        }
        let mut mean_abs = 0.0_f64;
        for v in win {
            mean_abs += v.abs();
        }
        mean_abs /= win.len() as f64;
        let mut cur: Vec<f64> = win.to_vec();
        cur.push(y);
        for _ in 0..238 {
            let mut nxt = Vec::with_capacity(cur.len().saturating_sub(1));
            for ii in 1..cur.len() {
                nxt.push(cur[ii] - cur[ii - 1]);
            }
            cur = nxt;
        }
        let delta = match cur.get(0) {
            Some(v) => *v,
            None => 0.0,
        };
        delta.abs() / mean_abs.max(1e-12)
    }
}

impl PartialFit for WindowLag238 {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 240 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 238;
        q.warmup = self.rows.len() < 238;
        q.explanation = format!("window-lag238 reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "windowed two-hundred-thirty-eighth-difference anomaly",
                "WindowLag238 uses successive first differences and cap 240; not WindowLag237",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for WindowLag238 {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

fn log_minkowski179_rows(a: &[f64], b: &[f64]) -> f64 {
    let p = a.len().min(b.len());
    let mut s = 0.0_f64;
    for j in 0..p {
        let u = a[j].abs().max(1e-18).ln().abs().max(1e-18);
        let v = b[j].abs().max(1e-18).ln().abs().max(1e-18);
        s += (u - v).abs().powi(179);
    }
    s.powf(1.0 / 179.0)
}

/// Log Minkowski \(p=179\) \(k\)-NN anomaly.
///
/// Score is the mean of the \(k\) smallest distances \((\sum|u-v|^{179})^{1/179}\)
/// on \(|\ln|x||\) without \(\ell_1\) normalisation. Distinct from
/// [`LogMinkowski178Anomaly`] (\(p=178\)).
#[derive(Clone, Debug)]
pub struct LogMinkowski179Anomaly {
    rows: Vec<Vec<f64>>,
    ncols: usize,
    n_seen: u64,
    updates: u64,
    initialized: bool,
}

impl Default for LogMinkowski179Anomaly {
    fn default() -> Self {
        Self {
            rows: Vec::new(),
            ncols: 0,
            n_seen: 0,
            updates: 0,
            initialized: false,
        }
    }
}

impl LogMinkowski179Anomaly {
    /// Empty log Minkowski-\(p=179\) \(k\)-NN detector (reservoir cap 64).
    pub fn new() -> Self {
        Self::default()
    }

    fn score_row(&self, x: &Matrix, i: usize) -> f64 {
        if self.rows.len() < 3 || self.ncols == 0 {
            return 0.0;
        }
        let p = self.ncols.min(x.ncols());
        let mut qrow = Vec::with_capacity(p);
        for j in 0..p {
            let z = x.get(i, j);
            if !z.is_finite() {
                return 0.0;
            }
            qrow.push(z);
        }
        let mut ds: Vec<f64> = self
            .rows
            .iter()
            .map(|row| log_minkowski179_rows(&qrow, row))
            .filter(|d| d.is_finite() && *d > 1e-15)
            .collect();
        if ds.is_empty() {
            return 0.0;
        }
        ds.sort_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal));
        let kk = 5.min(ds.len());
        let mut s = 0.0_f64;
        for t in 0..kk {
            s += ds[t];
        }
        s / kk as f64
    }
}

impl PartialFit for LogMinkowski179Anomaly {
    fn partial_fit(
        &mut self,
        x: &Matrix,
        _y: Option<&Vector>,
        session: &Session,
    ) -> Result<Qualified<IncrementalExplain>> {
        let mut ctx = FitCtx::with_session(session.child("partial_fit"));
        inspect_online_xy(&mut ctx, x, None);
        if x.ncols() == 0 {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "no features"),
            );
        }
        let p = x.ncols();
        if !self.initialized {
            self.ncols = p;
            self.initialized = true;
        } else if self.ncols != p {
            ctx.push(Issue::builder(IssueCode::FeatureSpaceChangedOnline).build());
            return finish_explain(
                ctx,
                reject_explain(self.updates, x.nrows(), self.n_seen, "feature space changed"),
            );
        }
        let before_n = self.n_seen;
        for i in 0..x.nrows() {
            let mut row = Vec::with_capacity(p);
            let mut ok = true;
            for j in 0..p {
                let z = x.get(i, j);
                if !z.is_finite() {
                    ok = false;
                    break;
                }
                row.push(z);
            }
            if !ok {
                continue;
            }
            if self.rows.len() >= 64 {
                self.rows.remove(0);
            }
            self.rows.push(row);
        }
        self.n_seen += x.nrows() as u64;
        self.updates += 1;
        let mut q = IncrementalQuality::new(self.updates.saturating_sub(1), x.nrows(), self.n_seen);
        q.effective_sample_size = self.n_seen as f64;
        q.parameter_delta_norm = Some((self.n_seen - before_n) as f64);
        q.information_gain = Some(x.nrows() as f64);
        q.still_identified = self.rows.len() >= 3;
        q.warmup = self.rows.len() < 3;
        q.explanation = format!("log-minkowski179 k-NN reservoir n={}", self.rows.len());
        flag_info(&mut ctx, &q);
        finish_explain(
            ctx,
            IncrementalExplain::from_quality(
                q,
                "log-minkowski-p=179 k-NN anomaly",
                "LogMinkowski179Anomaly is L179 on |ln|; not L178 or Chebyshev",
                "previous reservoir",
                "updated reservoir",
            ),
        )
    }
}

impl Predict for LogMinkowski179Anomaly {
    type Output = Vector;
    fn predict(&self, x: &Matrix, session: &Session) -> Result<Qualified<Vector>> {
        let mut ctx = FitCtx::with_session(session.child("predict"));
        inspect_online_xy(&mut ctx, x, None);
        if !self.initialized {
            ctx.push(Issue::builder(IssueCode::PartialFitBeforeInit).build());
            return ctx.finish(Vector::zeros(x.nrows()));
        }
        ctx.finish(Vector::from_iter((0..x.nrows()).map(|i| self.score_row(x, i))))
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::traits::{PartialFit, Predict};
    use ojizou_san::{EventKind, Session};

    fn has_incremental(session: &Session) -> bool {
        session
            .ledger()
            .events()
            .iter()
            .any(|e| e.kind == EventKind::IncrementalExplanation)
    }

    #[test]
    fn rls_recovers_slope() {
        let n = 80;
        let x = Matrix::from_fn(n, 1, |i, _| (i as f64) + 1.0);
        let y = Vector::from_iter((0..n).map(|i| 2.0 * ((i as f64) + 1.0)));
        let session = Session::new("rls", "partial_fit");
        let mut m = LinearRegression {
            forgetting_factor: 1.0,
            fit_intercept: false,
            p0: 100.0,
            ..LinearRegression::default()
        };
        let q = m.partial_fit(&x, Some(&y), &session).expect("rls");
        assert!(!q.value.narrative.is_empty());
        assert!(has_incremental(&session));
        let slope = m.coef()[0];
        assert!((slope - 2.0).abs() < 0.05, "slope={slope}");
    }

    #[test]
    fn adwin_fires_on_mean_shift() {
        let session = Session::new("adwin", "update");
        let mut a = Adwin::new(0.002);
        for _ in 0..80 {
            a.update(0.0, &session).expect("stable");
        }
        let mut fired = false;
        for _ in 0..80 {
            match a.update(10.0, &session) {
                Ok(q) => {
                    if matches!(q.value, DriftDecision::Drift { .. }) {
                        fired = true;
                    }
                }
                Err(e) => {
                    if e.primary().code == IssueCode::ConceptDriftDetected {
                        fired = true;
                    }
                }
            }
        }
        assert!(fired, "ADWIN should detect 0 → 10 mean shift");
    }

    #[test]
    fn every_partial_fit_logs_explanation() {
        let x = Matrix::from_fn(12, 1, |i, _| i as f64);
        let y = Vector::from_iter((0..12).map(|i| 2.0 * i as f64));
        let yb = Vector::from_iter((0..12).map(|i| if i % 2 == 0 { 0.0 } else { 1.0 }));

        let session = Session::new("online", "partial_fit");
        LinearRegression::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("rls");
        LogisticRegression::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("logit");
        Perceptron::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("perc");
        PassiveAggressive::new(1.0)
            .partial_fit(&x, Some(&yb), &session)
            .expect("pa");
        HoeffdingTree::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ht");
        OnlineStandardScaler::new()
            .partial_fit(&x, None, &session)
            .expect("scaler");
        StreamKMeans::new(2)
            .partial_fit(&x, None, &session)
            .expect("kmeans");
        HalfSpaceTrees::new(4, 4)
            .partial_fit(&x, None, &session)
            .expect("hst");
        OnlineAccuracy::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("acc");
        OnlineMse::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("mse");
        OnlineR2::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("r2");
        HoltWintersOnline::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("hw");
        AdaptiveModelRules::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("amrules");
        AdaptiveRandomForest::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("arf");
        AdaptiveRandomForestRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("arfr");
        SgdRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("sgd");
        crate::glm::SgdClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("sgdc");
        crate::glm::PassiveAggressiveRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("par");
        Alma::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("alma");
        Kswin::new(32)
            .partial_fit(&x, None, &session)
            .expect("kswin");
        Hddm::new().partial_fit(&x, None, &session).expect("hddm");
        Eddm::new().partial_fit(&x, None, &session).expect("eddm");
        HddmW::new().partial_fit(&x, None, &session).expect("hddmw");
        LeveragingBagging::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("lb");
        Snarimax::new(1, 0)
            .partial_fit(&x, Some(&y), &session)
            .expect("snarimax");
        DenStream::new(3.0)
            .partial_fit(&x, None, &session)
            .expect("denstream");
        HoeffdingAdaptiveTree::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("hat");
        StreamingRandomPatches::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("srp");
        let xui = Matrix::from_fn(12, 2, |i, j| {
            if j == 0 {
                (i % 4) as f64
            } else {
                (i % 3) as f64
            }
        });
        FunkMf::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("funk");
        CluStream::new(4)
            .partial_fit(&x, None, &session)
            .expect("clustream");
        ExtremelyFastDecisionTree::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("efdt");
        BiasedMf::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("biasedmf");
        KnnClassifier::new(3)
            .partial_fit(&x, Some(&yb), &session)
            .expect("knn");
        OnlineMinMaxScaler::new()
            .partial_fit(&x, None, &session)
            .expect("minmax");
        OnlineMaxAbsScaler::new()
            .partial_fit(&x, None, &session)
            .expect("maxabs");
        Baseline::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("base");
        KnnRegressor::new(3)
            .partial_fit(&x, Some(&y), &session)
            .expect("knnr");
        OnlineRobustScaler::new()
            .partial_fit(&x, None, &session)
            .expect("robust");
        SoftmaxRegression::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("smx");
        OnlineGaussianNb::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ognb");
        HoeffdingRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("htr");
        OnlineOneHotEncoder::new(4)
            .partial_fit(&x, None, &session)
            .expect("ohe");
        HoeffdingAdaptiveTreeRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("hatr");
        OnlineOrdinalEncoder::new(8)
            .partial_fit(&x, None, &session)
            .expect("ord");
        OnlineTfidf::new()
            .partial_fit(&x, None, &session)
            .expect("tfidf");
        StatImputer::new()
            .partial_fit(&x, None, &session)
            .expect("statimp");
        OnlineSelectKBest::new(1)
            .partial_fit(&x, Some(&yb), &session)
            .expect("osk");
        OnlineMae::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("mae");
        OnlineLogLoss::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ll");
        OnlineRocAuc::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("auc");
        OnlineMultinomialNb::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("mnb");
        PreviousImputer::new()
            .partial_fit(&x, None, &session)
            .expect("prev");
        AdaptiveStandardScaler::new(0.5)
            .partial_fit(&x, None, &session)
            .expect("adp");
        GaussianScorer::new()
            .partial_fit(&x, None, &session)
            .expect("gsc");
        OnlineF1::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("f1");
        OnlinePrecision::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("prec");
        OnlineRecall::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("rec");
        OnlineBernoulliNb::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("bnb");
        EwaRegressor::new(0.4)
            .partial_fit(&x, Some(&y), &session)
            .expect("ewa");
        QuantileFilter::new(0.9)
            .partial_fit(&x, None, &session)
            .expect("qf");
        OnlineRmse::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("rmse");
        OnlineCohenKappa::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("kappa");
        EwMean::new(0.6)
            .partial_fit(&x, None, &session)
            .expect("ewm");
        PolynomialExtender::new(2)
            .partial_fit(&x, None, &session)
            .expect("poly");
        TargetMeanEncoder::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("tme");
        OnlineBagging::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("obag");
        RollingQuantile::new(0.5)
            .partial_fit(&x, None, &session)
            .expect("rq");
        OnlineHamming::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("oham");
        OnlineJaccard::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ojac");
        EwVar::new(0.6)
            .partial_fit(&x, None, &session)
            .expect("ewv");
        SuccessiveHalvingClassifier::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("sh");
        SuccessiveHalvingRegressor::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("shr");
        AmfClassifier::new(3)
            .partial_fit(&x, Some(&yb), &session)
            .expect("amf");
        DbStream::new(2.0)
            .partial_fit(&x, None, &session)
            .expect("dbstream");
        OnlineBaggingRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("obagr");
        OnlineBinarizer::new(0.0)
            .partial_fit(&x, None, &session)
            .expect("obin");
        OnlineIsolationForest::new(3, 3)
            .partial_fit(&x, None, &session)
            .expect("oif");
        RollingMin::new()
            .partial_fit(&x, None, &session)
            .expect("rmin");
        RollingMax::new()
            .partial_fit(&x, None, &session)
            .expect("rmax");
        OnlineSkew::new()
            .partial_fit(&x, None, &session)
            .expect("skew");
        OnlineKurtosis::new()
            .partial_fit(&x, None, &session)
            .expect("kurt");
        OnlineEntropy::new()
            .partial_fit(&x, None, &session)
            .expect("ent");
        Periodic::new(12.0)
            .partial_fit(&x, None, &session)
            .expect("per");
        OnlineCovariance::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("ocov");
        HashingTrick::new(4)
            .partial_fit(&x, None, &session)
            .expect("hash");
        OnlineMad::new()
            .partial_fit(&x, None, &session)
            .expect("mad");
        OnlineIqr::new()
            .partial_fit(&x, None, &session)
            .expect("iqr");
        OnlinePearson::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("pearson");
        RollingMean::new()
            .partial_fit(&x, None, &session)
            .expect("rmean");
        AdwinBagging::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("abag");
        OnlineAdaBoost::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("ada");
        RandomOverSampler::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ros");
        RandomUnderSampler::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("rus");
        OnlineMean::new()
            .partial_fit(&x, None, &session)
            .expect("omean");
        OnlineVar::new()
            .partial_fit(&x, None, &session)
            .expect("ovar");
        OnlineCount::new()
            .partial_fit(&x, None, &session)
            .expect("ocnt");
        OnlineAutoCorr::new()
            .partial_fit(&x, None, &session)
            .expect("oac");
        OnlineVarianceThreshold::new(0.0)
            .partial_fit(&x, None, &session)
            .expect("ovt");
        OnlineGaussian::new()
            .partial_fit(&x, None, &session)
            .expect("ogauss");
        HardSamplingClassifier::new(8)
            .partial_fit(&x, Some(&yb), &session)
            .expect("hsc");
        OnlineSum::new()
            .partial_fit(&x, None, &session)
            .expect("osum");
        TransformerUnion::new()
            .partial_fit(&x, None, &session)
            .expect("tunion");
        FuncTransformer::new(ElementwiseMap::Log1p)
            .partial_fit(&x, None, &session)
            .expect("func");
        FactorizationMachine::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("fm");
        AdaGradRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adagrad");
        OnlineLof::new(3)
            .partial_fit(&x, None, &session)
            .expect("olof");
        OnlineBeta::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("obeta");
        AdamRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adam");
        RmsPropRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("rms");
        OnlineGamma::new()
            .partial_fit(&x, None, &session)
            .expect("ogamma");
        ColumnSelect::new(vec![0])
            .partial_fit(&x, None, &session)
            .expect("csel");
        ColumnDiscard::new(vec![])
            .partial_fit(&x, None, &session)
            .expect("cdis");
        FtrlRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("ftrl");
        AdaDeltaRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adadelta");
        MomentumRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("mom");
        OnlinePoisson::new()
            .partial_fit(&x, None, &session)
            .expect("opoiss");
        NesterovRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("nest");
        OnlineDirichlet::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("odir");
        ImplicitMf::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("imf");
        Bpr::new(2)
            .partial_fit(&xui, None, &session)
            .expect("bpr");
        ItemKnn::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("itemknn");
        UserKnn::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("userknn");
        SvdPp::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("svdpp");
        SlopeOne::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("slopeone");
        SrpClassifier::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("srpc");
        NormalPredictor::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("normpred");
        BaselineOnly::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("baseonly");
        CoClustering::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("coclust");
        OnlineHistogram::new(4)
            .partial_fit(&x, None, &session)
            .expect("ohist");
        OnlineWeibull::new()
            .partial_fit(&x, None, &session)
            .expect("oweib");
        OnlineExponential::new()
            .partial_fit(&x, None, &session)
            .expect("oexp");
        OnlineLogNormal::new()
            .partial_fit(&x, None, &session)
            .expect("oln");
        OnlinePareto::new()
            .partial_fit(&x, None, &session)
            .expect("opar");
        FtrlClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ftrlc");
        YogiRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("yogi");
        LionRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("lion");
        AdafactorRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adafactor");
        AdaBeliefRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adabelief");
        Fhddm::new()
            .partial_fit(&x, None, &session)
            .expect("fhddm");
        ExpandingMean::new()
            .partial_fit(&x, None, &session)
            .expect("exmean");
        AbsMax::new()
            .partial_fit(&x, None, &session)
            .expect("absmax");
        Fhddms::new()
            .partial_fit(&x, None, &session)
            .expect("fhddms");
        AveragerRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("averager");
        BayesianLinearRegression::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("bayeslin");
        StandardAbsoluteDeviation::new()
            .partial_fit(&x, None, &session)
            .expect("sad");
        HedgeRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("hedge");
        KnnAdwin::new(3)
            .partial_fit(&x, Some(&yb), &session)
            .expect("knnadwin");
        SamKnn::new(3, 8)
            .partial_fit(&x, Some(&yb), &session)
            .expect("samknn");
        OnlineOneClassSvm::new()
            .partial_fit(&x, None, &session)
            .expect("ocsvm");
        FwFmRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("fwfm");
        MultivariateGaussian::new()
            .partial_fit(&x, None, &session)
            .expect("mvg");
        ScaleCovariances::new()
            .partial_fit(&x, None, &session)
            .expect("scov");
        VeryFastDecisionRules::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("vfdr");
        Snmf::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("snmf");
        ContentRecommender::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("content");
        InverseScalingRegressor::new(0.1, 0.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("invsc");
        CosineAnnealingRegressor::new(8)
            .partial_fit(&x, Some(&y), &session)
            .expect("cosann");
        PolynomialFm::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("polyfm");
        LinearDecayRegressor::new(12)
            .partial_fit(&x, Some(&y), &session)
            .expect("lindec");
        CyclicLrRegressor::new(8)
            .partial_fit(&x, Some(&y), &session)
            .expect("cyclr");
        PolynomialDecayRegressor::new(12)
            .partial_fit(&x, Some(&y), &session)
            .expect("polydec");
        WarmRestartsRegressor::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("warmr");
        OneCycleLrRegressor::new(12)
            .partial_fit(&x, Some(&y), &session)
            .expect("onecycle");
        RAdamRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("radam");
        LambRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("lamb");
        HoeffdingTreeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("htc");
        AdaptiveRandomForestClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("arfc");
        LookaheadRegressor::new(3)
            .partial_fit(&x, Some(&y), &session)
            .expect("look");
        AlmaClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("almac");
        HoeffdingAdaptiveTreeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("hatc");
        ExtremelyFastDecisionTreeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("efdtc");
        StreamingRandomPatchesClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("srpc");
        LeveragingBaggingClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("lbagc");
        AdwinBaggingClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("adwinc");
        SoftmaxClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("smc");
        HoeffdingTreeRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("htr");
        ARFRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("arfr");
        StreamingRandomPatchesRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("srpr");
        ExtremelyFastDecisionTreeRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("efdtr");
        LeveragingBaggingRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("lbagr");
        AdwinBaggingRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("adwinr");
        HATRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("hatr");
        AdaBoostClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("adaboost");
        BaggingClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("bagc");
        MondrianForest::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("mf");
        MondrianForestClassifier::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("mfc");
        MondrianForestRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("mfr");
        AMFRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("amfr");
        StreamingGradientTreeRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("sgtr");
        StreamingGradientTreeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("sgtc");
        PaClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("pac");
        AMFClassifier::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("amfc");
        PaRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("par");
        MajorityClass::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("maj");
        NoChangeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("nochg");
        LastValueRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("lastv");
        PriorClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("prior");
        AMRulesClassifier::new(0.1)
            .partial_fit(&x, Some(&yb), &session)
            .expect("amrc");
        OnlineQuantileRegressor::new(0.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("oqr");
        OnlinePoissonRegressor::new(0.05)
            .partial_fit(&x, Some(&y), &session)
            .expect("opoisr");
        OnlineGammaRegressor::new(0.05)
            .partial_fit(&x, Some(&y), &session)
            .expect("ogamr");
        OnlineTweedieRegressor::new(1.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("otwdr");
        OnlineHuberRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ohubr");
        OnlineNegBinRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("onbr");
        OnlineBetaRegressor::new(0.05)
            .partial_fit(&x, Some(&y), &session)
            .expect("ober");
        OnlineLogCoshRegressor::new(0.05)
            .partial_fit(&x, Some(&y), &session)
            .expect("olcr");
        OnlineCauchyRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ocau");
        OnlineTukeyRegressor::new(4.685)
            .partial_fit(&x, Some(&y), &session)
            .expect("otuk");
        OnlineLaplaceRegressor::new(0.05)
            .partial_fit(&x, Some(&y), &session)
            .expect("olap");
        OnlineStudentTRegressor::new(4.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ostu");
        OnlineFairRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ofair");
        OnlineWelschRegressor::new(2.985)
            .partial_fit(&x, Some(&y), &session)
            .expect("owel");
        OnlineExpectileRegressor::new(0.25)
            .partial_fit(&x, Some(&y), &session)
            .expect("oexp");
        OnlineHampelRegressor::new(1.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("oham");
        OnlineAndrewsRegressor::new(1.339)
            .partial_fit(&x, Some(&y), &session)
            .expect("oand");
        OnlineGemanMcClureRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ogmc");
        OnlineRamsayRegressor::new(0.3)
            .partial_fit(&x, Some(&y), &session)
            .expect("oram");
        OnlineLpRegressor::new(1.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("olpr");
        OnlineCharbonnierRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ochr");
        OnlineTanhRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("otnh");
        OnlineAtanRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("oata");
        OnlineEpsilonInsensitiveRegressor::new(0.1)
            .partial_fit(&x, Some(&y), &session)
            .expect("oeps");
        OnlineBarronRegressor::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("obar");
        Loda::new(4)
            .partial_fit(&x, None, &session)
            .expect("loda");
        OnlineSoftL1Regressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("osl1");
        OnlineSquaredHingeClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("oshg");
        OnlineFocalClassifier::new(2.0)
            .partial_fit(&x, Some(&yb), &session)
            .expect("ofcl");
        OnlineModifiedHuberClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("omhb");
        OnlineArowClassifier::new(1.0)
            .partial_fit(&x, Some(&yb), &session)
            .expect("oarw");
        OnlineScwClassifier::new(0.7)
            .partial_fit(&x, Some(&yb), &session)
            .expect("oscw");
        OnlineCwClassifier::new(0.7)
            .partial_fit(&x, Some(&yb), &session)
            .expect("ocw");
        OnlinePca::new(1)
            .partial_fit(&x, None, &session)
            .expect("opca");
        DwmClassifier::new(0.5)
            .partial_fit(&x, Some(&yb), &session)
            .expect("dwm");
        OnlineIca::new(1)
            .partial_fit(&x, None, &session)
            .expect("oica");
        LearnNseClassifier::new(1.0)
            .partial_fit(&x, Some(&yb), &session)
            .expect("nse");
        AdditiveExpertClassifier::new(0.5)
            .partial_fit(&x, Some(&yb), &session)
            .expect("aex");
        OnlineNmf::new(1)
            .partial_fit(&x, None, &session)
            .expect("onmf");
        {
            let xcc = Matrix::from_fn(12, 2, |i, c| {
                if c == 0 {
                    i as f64
                } else {
                    2.0 * i as f64
                }
            });
            OnlineCca::new(0.05)
                .partial_fit(&xcc, None, &session)
                .expect("occa");
        }
        OnlineFa::new(1)
            .partial_fit(&x, None, &session)
            .expect("ofa");
        {
            let xcc = Matrix::from_fn(12, 2, |i, c| {
                if c == 0 {
                    i as f64
                } else {
                    2.0 * i as f64
                }
            });
            OnlinePls::new(0.05)
                .partial_fit(&xcc, None, &session)
                .expect("opls");
        }
        OnlineFda::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ofda");
        OnlineDictionaryLearning::new(1)
            .partial_fit(&x, None, &session)
            .expect("odl");
        AlsReco::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("alsr");
        WarpReco::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("wrp");
        OnlineSvd::new(1)
            .partial_fit(&x, None, &session)
            .expect("osvd");
        EaseReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("ease");
        OnlineKernelPca::new(1)
            .partial_fit(&x, None, &session)
            .expect("okp");
        IForestAsd::new(4)
            .partial_fit(&x, None, &session)
            .expect("ifa");
        ListNet::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("lnet");
        ListMle::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("lml");
        SoftRank::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("srk");
        ApproxNdcg::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("ndc");
        RankCosine::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("rcs");
        RankSvm::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("rsv");
        AdaRank::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("adr");
        CoordinateAscent::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("cas");
        RankBoost::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("rkb");
        RankNet::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("rnet");
        SoftImpute::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("simp");
        Wrmf::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("wrm");
        LambdaRank::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("lrank");
        Rrcf::new(4)
            .partial_fit(&x, None, &session)
            .expect("rrcf");
        XStream::new(4)
            .partial_fit(&x, None, &session)
            .expect("xst");
        KitNet::new(3)
            .partial_fit(&x, None, &session)
            .expect("kit");
        AdaptiveXgboost::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("axgb");
        LambdaMart::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("lmart");
        OzaBoost::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("oza");
        Climf::new(2)
            .partial_fit(&xui, Some(&y), &session)
            .expect("clf");
        OsElm::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("ose");
        SpotAnomaly::new(1e-3)
            .partial_fit(&x, None, &session)
            .expect("spo");
        Mddm::new(12)
            .partial_fit(&x, None, &session)
            .expect("mdd");
        CusumDrift::new(0.5, 8.0)
            .partial_fit(&x, None, &session)
            .expect("csu");
        OnlineMahalanobis::new()
            .partial_fit(&x, None, &session)
            .expect("maha");
        EwmaChart::new(0.2)
            .partial_fit(&x, None, &session)
            .expect("ewc");
        Stepd::new(6)
            .partial_fit(&x, None, &session)
            .expect("stp");
        Ecdd::new(0.2)
            .partial_fit(&x, None, &session)
            .expect("ecd");
        Rddm::new(12)
            .partial_fit(&x, None, &session)
            .expect("rdd");
        Hbos::new(8)
            .partial_fit(&x, None, &session)
            .expect("hbo");
        Ecod::new()
            .partial_fit(&x, None, &session)
            .expect("eco");
        Copod::new()
            .partial_fit(&x, None, &session)
            .expect("cpd");
        Abod::new()
            .partial_fit(&x, None, &session)
            .expect("abo");
        Inne::new()
            .partial_fit(&x, None, &session)
            .expect("inn");
        Sod::new()
            .partial_fit(&x, None, &session)
            .expect("sod");
        Loci::new()
            .partial_fit(&x, None, &session)
            .expect("lci");
        Cblof::new()
            .partial_fit(&x, None, &session)
            .expect("cbl");
        KnnAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("kna");
        Cof::new()
            .partial_fit(&x, None, &session)
            .expect("cof");
        Inflo::new()
            .partial_fit(&x, None, &session)
            .expect("ilo");
        Loop::new()
            .partial_fit(&x, None, &session)
            .expect("lop");
        Ldof::new()
            .partial_fit(&x, None, &session)
            .expect("ldo");
        Odin::new()
            .partial_fit(&x, None, &session)
            .expect("odn");
        KdeAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("kda");
        Rdos::new()
            .partial_fit(&x, None, &session)
            .expect("rdo");
        Ldf::new()
            .partial_fit(&x, None, &session)
            .expect("ldf");
        Nof::new()
            .partial_fit(&x, None, &session)
            .expect("nof");
        FastAbod::new()
            .partial_fit(&x, None, &session)
            .expect("fab");
        PcaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("pcn");
        Knnw::new()
            .partial_fit(&x, None, &session)
            .expect("knw");
        Rod::new()
            .partial_fit(&x, None, &session)
            .expect("rod");
        GmmAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("gma");
        IqrAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("iqa");
        StudentTAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("sta");
        KnnMedian::new()
            .partial_fit(&x, None, &session)
            .expect("knm");
        QuantileAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("qan");
        KnnKth::new()
            .partial_fit(&x, None, &session)
            .expect("knx");
        EnergyAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("egy");
        WindowZscore::new()
            .partial_fit(&x, None, &session)
            .expect("wzs");
        SsdAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ssd");
        WindowMad::new()
            .partial_fit(&x, None, &session)
            .expect("wmd");
        LinfAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lnx");
        WindowIqr::new()
            .partial_fit(&x, None, &session)
            .expect("wiq");
        ManhattanAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("mhn");
        WindowRange::new()
            .partial_fit(&x, None, &session)
            .expect("wrg");
        MinkowskiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("mk3");
        WindowCv::new()
            .partial_fit(&x, None, &session)
            .expect("wcv");
        GeometricAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("gkn");
        WindowRms::new()
            .partial_fit(&x, None, &session)
            .expect("wrs");
        HarmonicAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("hkn");
        WindowTrimmed::new()
            .partial_fit(&x, None, &session)
            .expect("wtr");
        CanberraAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("cbn");
        WindowSkew::new()
            .partial_fit(&x, None, &session)
            .expect("wsk");
        LorentzianAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lrz");
        WindowKurt::new()
            .partial_fit(&x, None, &session)
            .expect("wkt");
        HassanatAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("hsn");
        WindowPeak::new()
            .partial_fit(&x, None, &session)
            .expect("wpk");
        AngularAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ajn");
        WindowSlope::new()
            .partial_fit(&x, None, &session)
            .expect("wsl");
        DiceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("dkn");
        WindowGini::new()
            .partial_fit(&x, None, &session)
            .expect("wgi");
        TanimotoAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("tnm");
        WindowHarmonic::new()
            .partial_fit(&x, None, &session)
            .expect("whm");
        ClarkAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ckn");
        WindowMedian::new()
            .partial_fit(&x, None, &session)
            .expect("wmn");
        WaveHedgesAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("wvh");
        WindowLag1::new()
            .partial_fit(&x, None, &session)
            .expect("wlg");
        KulczynskiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("kzn");
        WindowLag2::new()
            .partial_fit(&x, None, &session)
            .expect("wl2");
        RuzickaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("rzk");
        WindowMin::new()
            .partial_fit(&x, None, &session)
            .expect("wmi");
        TanejaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("tnj");
        WindowAbsdev::new()
            .partial_fit(&x, None, &session)
            .expect("wab");
        KumarJohnsonAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("kja");
        WindowLag3::new()
            .partial_fit(&x, None, &session)
            .expect("wl3");
        AdditiveSymmetricAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("asa");
        WindowGeom::new()
            .partial_fit(&x, None, &session)
            .expect("wgm");
        VicisAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("vcs");
        WindowVar::new()
            .partial_fit(&x, None, &session)
            .expect("wvr");
        TopsoeAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("tps");
        WindowLag4::new()
            .partial_fit(&x, None, &session)
            .expect("wl4");
        JeffreysAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("jfr");
        WindowLag5::new()
            .partial_fit(&x, None, &session)
            .expect("wl5");
        KDivergenceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("kdy");
        WindowMaxdev::new()
            .partial_fit(&x, None, &session)
            .expect("wmx");
        HellingerAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("hln");
        WindowEntropy::new()
            .partial_fit(&x, None, &session)
            .expect("wen");
        JensenShannonAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("jsa");
        WindowLag6::new()
            .partial_fit(&x, None, &session)
            .expect("wl6");
        SquaredChordAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("scd");
        WindowLag7::new()
            .partial_fit(&x, None, &session)
            .expect("wl7");
        PearsonChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("pch");
        WindowLag8::new()
            .partial_fit(&x, None, &session)
            .expect("wl8");
        NeymanChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("nym");
        WindowLag9::new()
            .partial_fit(&x, None, &session)
            .expect("wl9");
        SymmetricChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("sxc");
        WindowLag10::new()
            .partial_fit(&x, None, &session)
            .expect("w10");
        SquaredHellingerAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("sqh");
        WindowLag11::new()
            .partial_fit(&x, None, &session)
            .expect("w11");
        MaxSymmetricChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("msy");
        WindowLag12::new()
            .partial_fit(&x, None, &session)
            .expect("w12");
        MinSymmetricChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("mns");
        WindowLag13::new()
            .partial_fit(&x, None, &session)
            .expect("w13");
        JensenDifferenceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("jdf");
        WindowLag14::new()
            .partial_fit(&x, None, &session)
            .expect("w14");
        ItakuraSaitoAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("its");
        WindowLag15::new()
            .partial_fit(&x, None, &session)
            .expect("w15");
        TriangularAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("trd");
        WindowLag16::new()
            .partial_fit(&x, None, &session)
            .expect("w16");
        IDivergenceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("idv");
        WindowLag17::new()
            .partial_fit(&x, None, &session)
            .expect("w17");
        LogEuclideanAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lge");
        WindowLag18::new()
            .partial_fit(&x, None, &session)
            .expect("w18");
        BrayCurtisAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("bcy");
        WindowLag19::new()
            .partial_fit(&x, None, &session)
            .expect("w19");
        LogChebyshevAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lcx");
        WindowLag20::new()
            .partial_fit(&x, None, &session)
            .expect("w20");
        LogManhattanAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lma");
        WindowLag21::new()
            .partial_fit(&x, None, &session)
            .expect("w21");
        LogCanberraAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lca");
        WindowLag22::new()
            .partial_fit(&x, None, &session)
            .expect("w22");
        LogLorentzianAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("llz");
        WindowLag23::new()
            .partial_fit(&x, None, &session)
            .expect("w23");
        LogWaveHedgesAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lhd");
        WindowLag24::new()
            .partial_fit(&x, None, &session)
            .expect("w24");
        LogHassanatAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lha");
        WindowLag25::new()
            .partial_fit(&x, None, &session)
            .expect("w25");
        LogDiceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ldc");
        WindowLag26::new()
            .partial_fit(&x, None, &session)
            .expect("w26");
        LogTanimotoAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lta");
        WindowLag27::new()
            .partial_fit(&x, None, &session)
            .expect("w27");
        LogRuzickaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lru");
        WindowLag28::new()
            .partial_fit(&x, None, &session)
            .expect("w28");
        LogKulczynskiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lku");
        WindowLag29::new()
            .partial_fit(&x, None, &session)
            .expect("w29");
        LogClarkAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lck");
        WindowLag30::new()
            .partial_fit(&x, None, &session)
            .expect("w30");
        LogBrayCurtisAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lbc");
        WindowLag31::new()
            .partial_fit(&x, None, &session)
            .expect("w31");
        LogTanejaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ltj");
        WindowLag32::new()
            .partial_fit(&x, None, &session)
            .expect("w32");
        LogMotykaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lmo");
        WindowLag33::new()
            .partial_fit(&x, None, &session)
            .expect("w33");
        LogHellingerAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lhe");
        WindowLag34::new()
            .partial_fit(&x, None, &session)
            .expect("w34");
        LogFidelityAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lfi");
        WindowLag35::new()
            .partial_fit(&x, None, &session)
            .expect("w35");
        LogPearsonChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lpc");
        WindowLag36::new()
            .partial_fit(&x, None, &session)
            .expect("w36");
        LogJensenShannonAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ljs");
        WindowLag37::new()
            .partial_fit(&x, None, &session)
            .expect("w37");
        LogJeffreysAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ljf");
        WindowLag38::new()
            .partial_fit(&x, None, &session)
            .expect("w38");
        LogKumarHassebrookAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lkh");
        WindowLag39::new()
            .partial_fit(&x, None, &session)
            .expect("w39");
        LogAdditiveSymmetricAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("las");
        WindowLag40::new()
            .partial_fit(&x, None, &session)
            .expect("w40");
        LogKDivergenceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lkd");
        WindowLag41::new()
            .partial_fit(&x, None, &session)
            .expect("w41");
        LogCosineAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lcn");
        WindowLag42::new()
            .partial_fit(&x, None, &session)
            .expect("w42");
        LogJaccardAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ljc");
        WindowLag43::new()
            .partial_fit(&x, None, &session)
            .expect("w43");
        LogDivergenceAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("ldg");
        WindowLag44::new()
            .partial_fit(&x, None, &session)
            .expect("w44");
        LogCzekanowskiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lcz");
        WindowLag45::new()
            .partial_fit(&x, None, &session)
            .expect("w45");
        LogKumarJohnsonAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lkj");
        WindowLag46::new()
            .partial_fit(&x, None, &session)
            .expect("w46");
        LogAverageAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lav");
        WindowLag47::new()
            .partial_fit(&x, None, &session)
            .expect("w47");
        LogNeymanAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lnm");
        WindowLag48::new()
            .partial_fit(&x, None, &session)
            .expect("w48");
        LogVicisAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lvi");
        WindowLag49::new()
            .partial_fit(&x, None, &session)
            .expect("w49");
        LogMaxChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lmx");
        WindowLag50::new()
            .partial_fit(&x, None, &session)
            .expect("w50");
        LogMinChiAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lmn");
        WindowLag51::new()
            .partial_fit(&x, None, &session)
            .expect("w51");
        LogBhattacharyyaAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lbh");
        WindowLag52::new()
            .partial_fit(&x, None, &session)
            .expect("w52");
        LogVicisWaveHedgesAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lvh");
        WindowLag53::new()
            .partial_fit(&x, None, &session)
            .expect("w53");
        LogKLAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lkl");
        WindowLag54::new()
            .partial_fit(&x, None, &session)
            .expect("w54");
        LogReverseKLAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lrk");
        WindowLag55::new()
            .partial_fit(&x, None, &session)
            .expect("w55");
        LogItakuraSaitoAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lis");
        WindowLag56::new()
            .partial_fit(&x, None, &session)
            .expect("w56");
        LogReverseItakuraSaitoAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lris");
        WindowLag57::new()
            .partial_fit(&x, None, &session)
            .expect("w57");
        LogAngularAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lang");
        WindowLag58::new()
            .partial_fit(&x, None, &session)
            .expect("w58");
        LogHarmonicAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lhm");
        WindowLag59::new()
            .partial_fit(&x, None, &session)
            .expect("w59");
        LogRatioAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lra");
        WindowLag60::new()
            .partial_fit(&x, None, &session)
            .expect("w60");
        LogMaxMinAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lmm");
        WindowLag61::new()
            .partial_fit(&x, None, &session)
            .expect("w61");
        LogSquaredRatioAnomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lsq");
        WindowLag62::new()
            .partial_fit(&x, None, &session)
            .expect("w62");
        LogMinkowski3Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm3");
        WindowLag63::new()
            .partial_fit(&x, None, &session)
            .expect("w63");
        LogMinkowski4Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm4");
        WindowLag64::new()
            .partial_fit(&x, None, &session)
            .expect("w64");
        LogMinkowski5Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm5");
        WindowLag65::new()
            .partial_fit(&x, None, &session)
            .expect("w65");
        LogMinkowski6Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm6");
        WindowLag66::new()
            .partial_fit(&x, None, &session)
            .expect("w66");
        LogMinkowski7Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm7");
        WindowLag67::new()
            .partial_fit(&x, None, &session)
            .expect("w67");
        LogMinkowski8Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm8");
        WindowLag68::new()
            .partial_fit(&x, None, &session)
            .expect("w68");
        LogMinkowski9Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm9");
        WindowLag69::new()
            .partial_fit(&x, None, &session)
            .expect("w69");
        LogMinkowski10Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm10");
        WindowLag70::new()
            .partial_fit(&x, None, &session)
            .expect("w70");
        LogMinkowski11Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm11");
        WindowLag71::new()
            .partial_fit(&x, None, &session)
            .expect("w71");
        LogMinkowski12Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm12");
        WindowLag72::new()
            .partial_fit(&x, None, &session)
            .expect("w72");
        LogMinkowski13Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm13");
        WindowLag73::new()
            .partial_fit(&x, None, &session)
            .expect("w73");
        LogMinkowski14Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm14");
        WindowLag74::new()
            .partial_fit(&x, None, &session)
            .expect("w74");
        LogMinkowski15Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm15");
        WindowLag75::new()
            .partial_fit(&x, None, &session)
            .expect("w75");
        LogMinkowski16Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm16");
        WindowLag76::new()
            .partial_fit(&x, None, &session)
            .expect("w76");
        LogMinkowski17Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm17");
        WindowLag77::new()
            .partial_fit(&x, None, &session)
            .expect("w77");
        LogMinkowski18Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm18");
        WindowLag78::new()
            .partial_fit(&x, None, &session)
            .expect("w78");
        LogMinkowski19Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm19");
        WindowLag79::new()
            .partial_fit(&x, None, &session)
            .expect("w79");
        LogMinkowski20Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm20");
        WindowLag80::new()
            .partial_fit(&x, None, &session)
            .expect("w80");
        LogMinkowski21Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm21");
        WindowLag81::new()
            .partial_fit(&x, None, &session)
            .expect("w81");
        LogMinkowski22Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm22");
        WindowLag82::new()
            .partial_fit(&x, None, &session)
            .expect("w82");
        LogMinkowski23Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm23");
        WindowLag83::new()
            .partial_fit(&x, None, &session)
            .expect("w83");
        LogMinkowski24Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm24");
        WindowLag84::new()
            .partial_fit(&x, None, &session)
            .expect("w84");
        LogMinkowski25Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm25");
        WindowLag85::new()
            .partial_fit(&x, None, &session)
            .expect("w85");
        LogMinkowski26Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm26");
        WindowLag86::new()
            .partial_fit(&x, None, &session)
            .expect("w86");
        LogMinkowski27Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm27");
        WindowLag87::new()
            .partial_fit(&x, None, &session)
            .expect("w87");
        LogMinkowski28Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm28");
        WindowLag88::new()
            .partial_fit(&x, None, &session)
            .expect("w88");
        LogMinkowski29Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm29");
        WindowLag89::new()
            .partial_fit(&x, None, &session)
            .expect("w89");
        LogMinkowski30Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm30");
        WindowLag90::new()
            .partial_fit(&x, None, &session)
            .expect("w90");
        LogMinkowski31Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm31");
        WindowLag91::new()
            .partial_fit(&x, None, &session)
            .expect("w91");
        LogMinkowski32Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm32");
        WindowLag92::new()
            .partial_fit(&x, None, &session)
            .expect("w92");
        LogMinkowski33Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm33");
        WindowLag93::new()
            .partial_fit(&x, None, &session)
            .expect("w93");
        LogMinkowski34Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm34");
        WindowLag94::new()
            .partial_fit(&x, None, &session)
            .expect("w94");
        LogMinkowski35Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm35");
        WindowLag95::new()
            .partial_fit(&x, None, &session)
            .expect("w95");
        LogMinkowski36Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm36");
        WindowLag96::new()
            .partial_fit(&x, None, &session)
            .expect("w96");
        LogMinkowski37Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm37");
        WindowLag97::new()
            .partial_fit(&x, None, &session)
            .expect("w97");
        LogMinkowski38Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm38");
        WindowLag98::new()
            .partial_fit(&x, None, &session)
            .expect("w98");
        LogMinkowski39Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm39");
        WindowLag99::new()
            .partial_fit(&x, None, &session)
            .expect("w99");
        LogMinkowski40Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm40");
        WindowLag100::new()
            .partial_fit(&x, None, &session)
            .expect("w100");
        LogMinkowski41Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm41");
        WindowLag101::new()
            .partial_fit(&x, None, &session)
            .expect("w101");
        LogMinkowski42Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm42");
        WindowLag102::new()
            .partial_fit(&x, None, &session)
            .expect("w102");
        LogMinkowski43Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm43");
        WindowLag103::new()
            .partial_fit(&x, None, &session)
            .expect("w103");
        LogMinkowski44Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm44");
        WindowLag104::new()
            .partial_fit(&x, None, &session)
            .expect("w104");
        LogMinkowski45Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm45");
        WindowLag105::new()
            .partial_fit(&x, None, &session)
            .expect("w105");
        LogMinkowski46Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm46");
        WindowLag106::new()
            .partial_fit(&x, None, &session)
            .expect("w106");
        LogMinkowski47Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm47");
        WindowLag107::new()
            .partial_fit(&x, None, &session)
            .expect("w107");
        LogMinkowski48Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm48");
        WindowLag108::new()
            .partial_fit(&x, None, &session)
            .expect("w108");
        LogMinkowski49Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm49");
        WindowLag109::new()
            .partial_fit(&x, None, &session)
            .expect("w109");
        LogMinkowski50Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm50");
        WindowLag110::new()
            .partial_fit(&x, None, &session)
            .expect("w110");
        LogMinkowski51Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm51");
        WindowLag111::new()
            .partial_fit(&x, None, &session)
            .expect("w111");
        LogMinkowski52Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm52");
        WindowLag112::new()
            .partial_fit(&x, None, &session)
            .expect("w112");
        LogMinkowski53Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm53");
        WindowLag113::new()
            .partial_fit(&x, None, &session)
            .expect("w113");
        LogMinkowski54Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm54");
        WindowLag114::new()
            .partial_fit(&x, None, &session)
            .expect("w114");
        LogMinkowski55Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm55");
        WindowLag115::new()
            .partial_fit(&x, None, &session)
            .expect("w115");
        LogMinkowski56Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm56");
        WindowLag116::new()
            .partial_fit(&x, None, &session)
            .expect("w116");
        LogMinkowski57Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm57");
        WindowLag117::new()
            .partial_fit(&x, None, &session)
            .expect("w117");
        LogMinkowski58Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm58");
        WindowLag118::new()
            .partial_fit(&x, None, &session)
            .expect("w118");
        LogMinkowski59Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm59");
        WindowLag119::new()
            .partial_fit(&x, None, &session)
            .expect("w119");
        LogMinkowski60Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm60");
        WindowLag120::new()
            .partial_fit(&x, None, &session)
            .expect("w120");
        LogMinkowski61Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm61");
        WindowLag121::new()
            .partial_fit(&x, None, &session)
            .expect("w121");
        LogMinkowski62Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm62");
        WindowLag122::new()
            .partial_fit(&x, None, &session)
            .expect("w122");
        LogMinkowski63Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm63");
        WindowLag123::new()
            .partial_fit(&x, None, &session)
            .expect("w123");
        LogMinkowski64Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm64");
        WindowLag124::new()
            .partial_fit(&x, None, &session)
            .expect("w124");
        LogMinkowski65Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm65");
        WindowLag125::new()
            .partial_fit(&x, None, &session)
            .expect("w125");
        LogMinkowski66Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm66");
        WindowLag126::new()
            .partial_fit(&x, None, &session)
            .expect("w126");
        LogMinkowski67Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm67");
        WindowLag127::new()
            .partial_fit(&x, None, &session)
            .expect("w127");
        LogMinkowski68Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm68");
        WindowLag128::new()
            .partial_fit(&x, None, &session)
            .expect("w128");
        LogMinkowski69Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm69");
        WindowLag129::new()
            .partial_fit(&x, None, &session)
            .expect("w129");
        LogMinkowski70Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm70");
        WindowLag130::new()
            .partial_fit(&x, None, &session)
            .expect("w130");
        LogMinkowski71Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm71");
        WindowLag131::new()
            .partial_fit(&x, None, &session)
            .expect("w131");
        LogMinkowski72Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm72");
        WindowLag132::new()
            .partial_fit(&x, None, &session)
            .expect("w132");
        LogMinkowski73Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm73");
        WindowLag133::new()
            .partial_fit(&x, None, &session)
            .expect("w133");
        LogMinkowski74Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm74");
        WindowLag134::new()
            .partial_fit(&x, None, &session)
            .expect("w134");
        LogMinkowski75Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm75");
        WindowLag135::new()
            .partial_fit(&x, None, &session)
            .expect("w135");
        LogMinkowski76Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm76");
        WindowLag136::new()
            .partial_fit(&x, None, &session)
            .expect("w136");
        LogMinkowski77Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm77");
        WindowLag137::new()
            .partial_fit(&x, None, &session)
            .expect("w137");
        LogMinkowski78Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm78");
        WindowLag138::new()
            .partial_fit(&x, None, &session)
            .expect("w138");
        LogMinkowski79Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm79");
        WindowLag139::new()
            .partial_fit(&x, None, &session)
            .expect("w139");
        LogMinkowski80Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm80");
        WindowLag140::new()
            .partial_fit(&x, None, &session)
            .expect("w140");
        LogMinkowski81Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm81");
        WindowLag141::new()
            .partial_fit(&x, None, &session)
            .expect("w141");
        LogMinkowski82Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm82");
        WindowLag142::new()
            .partial_fit(&x, None, &session)
            .expect("w142");
        LogMinkowski83Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm83");
        WindowLag143::new()
            .partial_fit(&x, None, &session)
            .expect("w143");
        LogMinkowski84Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm84");
        WindowLag144::new()
            .partial_fit(&x, None, &session)
            .expect("w144");
        LogMinkowski85Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm85");
        WindowLag145::new()
            .partial_fit(&x, None, &session)
            .expect("w145");
        LogMinkowski86Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm86");
        WindowLag146::new()
            .partial_fit(&x, None, &session)
            .expect("w146");
        LogMinkowski87Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm87");
        WindowLag147::new()
            .partial_fit(&x, None, &session)
            .expect("w147");
        LogMinkowski88Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm88");
        WindowLag148::new()
            .partial_fit(&x, None, &session)
            .expect("w148");
        LogMinkowski89Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm89");
        WindowLag149::new()
            .partial_fit(&x, None, &session)
            .expect("w149");
        LogMinkowski90Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm90");
        WindowLag150::new()
            .partial_fit(&x, None, &session)
            .expect("w150");
        LogMinkowski91Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm91");
        WindowLag151::new()
            .partial_fit(&x, None, &session)
            .expect("w151");
        LogMinkowski92Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm92");
        WindowLag152::new()
            .partial_fit(&x, None, &session)
            .expect("w152");
        LogMinkowski93Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm93");
        WindowLag153::new()
            .partial_fit(&x, None, &session)
            .expect("w153");
        LogMinkowski94Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm94");
        WindowLag154::new()
            .partial_fit(&x, None, &session)
            .expect("w154");
        LogMinkowski95Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm95");
        WindowLag155::new()
            .partial_fit(&x, None, &session)
            .expect("w155");
        LogMinkowski96Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm96");
        WindowLag156::new()
            .partial_fit(&x, None, &session)
            .expect("w156");
        LogMinkowski97Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm97");
        WindowLag157::new()
            .partial_fit(&x, None, &session)
            .expect("w157");
        LogMinkowski98Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm98");
        WindowLag158::new()
            .partial_fit(&x, None, &session)
            .expect("w158");
        LogMinkowski99Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm99");
        WindowLag159::new()
            .partial_fit(&x, None, &session)
            .expect("w159");
        LogMinkowski100Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm100");
        WindowLag160::new()
            .partial_fit(&x, None, &session)
            .expect("w160");
        LogMinkowski101Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm101");
        WindowLag161::new()
            .partial_fit(&x, None, &session)
            .expect("w161");
        LogMinkowski102Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm102");
        WindowLag162::new()
            .partial_fit(&x, None, &session)
            .expect("w162");
        LogMinkowski103Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm103");
        WindowLag163::new()
            .partial_fit(&x, None, &session)
            .expect("w163");
        LogMinkowski104Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm104");
        WindowLag164::new()
            .partial_fit(&x, None, &session)
            .expect("w164");
        LogMinkowski105Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm105");
        WindowLag165::new()
            .partial_fit(&x, None, &session)
            .expect("w165");
        LogMinkowski106Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm106");
        WindowLag166::new()
            .partial_fit(&x, None, &session)
            .expect("w166");
        LogMinkowski107Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm107");
        WindowLag167::new()
            .partial_fit(&x, None, &session)
            .expect("w167");
        LogMinkowski108Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm108");
        WindowLag168::new()
            .partial_fit(&x, None, &session)
            .expect("w168");
        LogMinkowski109Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm109");
        WindowLag169::new()
            .partial_fit(&x, None, &session)
            .expect("w169");
        LogMinkowski110Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm110");
        WindowLag170::new()
            .partial_fit(&x, None, &session)
            .expect("w170");
        LogMinkowski111Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm111");
        WindowLag171::new()
            .partial_fit(&x, None, &session)
            .expect("w171");
        LogMinkowski112Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm112");
        WindowLag172::new()
            .partial_fit(&x, None, &session)
            .expect("w172");
        LogMinkowski113Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm113");
        WindowLag173::new()
            .partial_fit(&x, None, &session)
            .expect("w173");
        LogMinkowski114Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm114");
        WindowLag174::new()
            .partial_fit(&x, None, &session)
            .expect("w174");
        LogMinkowski115Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm115");
        WindowLag175::new()
            .partial_fit(&x, None, &session)
            .expect("w175");
        LogMinkowski116Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm116");
        WindowLag176::new()
            .partial_fit(&x, None, &session)
            .expect("w176");
        LogMinkowski117Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm117");
        WindowLag177::new()
            .partial_fit(&x, None, &session)
            .expect("w177");
        LogMinkowski118Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm118");
        WindowLag178::new()
            .partial_fit(&x, None, &session)
            .expect("w178");
        LogMinkowski119Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm119");
        WindowLag179::new()
            .partial_fit(&x, None, &session)
            .expect("w179");
        LogMinkowski120Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm120");
        WindowLag180::new()
            .partial_fit(&x, None, &session)
            .expect("w180");
        LogMinkowski121Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm121");
        WindowLag181::new()
            .partial_fit(&x, None, &session)
            .expect("w181");
        LogMinkowski122Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm122");
        WindowLag182::new()
            .partial_fit(&x, None, &session)
            .expect("w182");
        LogMinkowski123Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm123");
        WindowLag183::new()
            .partial_fit(&x, None, &session)
            .expect("w183");
        LogMinkowski124Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm124");
        WindowLag184::new()
            .partial_fit(&x, None, &session)
            .expect("w184");
        LogMinkowski125Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm125");
        WindowLag185::new()
            .partial_fit(&x, None, &session)
            .expect("w185");
        LogMinkowski126Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm126");
        WindowLag186::new()
            .partial_fit(&x, None, &session)
            .expect("w186");
        LogMinkowski127Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm127");
        WindowLag187::new()
            .partial_fit(&x, None, &session)
            .expect("w187");
        LogMinkowski128Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm128");
        WindowLag188::new()
            .partial_fit(&x, None, &session)
            .expect("w188");
        LogMinkowski129Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm129");
        WindowLag189::new()
            .partial_fit(&x, None, &session)
            .expect("w189");
        LogMinkowski130Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm130");
        WindowLag190::new()
            .partial_fit(&x, None, &session)
            .expect("w190");
        LogMinkowski131Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm131");
        WindowLag191::new()
            .partial_fit(&x, None, &session)
            .expect("w191");
        LogMinkowski132Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm132");
        WindowLag192::new()
            .partial_fit(&x, None, &session)
            .expect("w192");
        LogMinkowski133Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm133");
        WindowLag193::new()
            .partial_fit(&x, None, &session)
            .expect("w193");
        LogMinkowski134Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm134");
        WindowLag194::new()
            .partial_fit(&x, None, &session)
            .expect("w194");
        LogMinkowski135Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm135");
        WindowLag195::new()
            .partial_fit(&x, None, &session)
            .expect("w195");
        LogMinkowski136Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm136");
        WindowLag196::new()
            .partial_fit(&x, None, &session)
            .expect("w196");
        LogMinkowski137Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm137");
        WindowLag197::new()
            .partial_fit(&x, None, &session)
            .expect("w197");
        LogMinkowski138Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm138");
        WindowLag198::new()
            .partial_fit(&x, None, &session)
            .expect("w198");
        LogMinkowski139Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm139");
        WindowLag199::new()
            .partial_fit(&x, None, &session)
            .expect("w199");
        LogMinkowski140Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm140");
        WindowLag200::new()
            .partial_fit(&x, None, &session)
            .expect("w200");
        LogMinkowski141Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm141");
        WindowLag201::new()
            .partial_fit(&x, None, &session)
            .expect("w201");
        LogMinkowski142Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm142");
        WindowLag202::new()
            .partial_fit(&x, None, &session)
            .expect("w202");
        LogMinkowski143Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm143");
        WindowLag203::new()
            .partial_fit(&x, None, &session)
            .expect("w203");
        LogMinkowski144Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm144");
        WindowLag204::new()
            .partial_fit(&x, None, &session)
            .expect("w204");
        LogMinkowski145Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm145");
        WindowLag205::new()
            .partial_fit(&x, None, &session)
            .expect("w205");
        LogMinkowski146Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm146");
        WindowLag206::new()
            .partial_fit(&x, None, &session)
            .expect("w206");
        LogMinkowski147Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm147");
        WindowLag207::new()
            .partial_fit(&x, None, &session)
            .expect("w207");
        LogMinkowski148Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm148");
        WindowLag208::new()
            .partial_fit(&x, None, &session)
            .expect("w208");
        LogMinkowski149Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm149");
        WindowLag209::new()
            .partial_fit(&x, None, &session)
            .expect("w209");
        LogMinkowski150Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm150");
        WindowLag210::new()
            .partial_fit(&x, None, &session)
            .expect("w210");
        LogMinkowski151Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm151");
        WindowLag211::new()
            .partial_fit(&x, None, &session)
            .expect("w211");
        LogMinkowski152Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm152");
        WindowLag212::new()
            .partial_fit(&x, None, &session)
            .expect("w212");
        LogMinkowski153Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm153");
        WindowLag213::new()
            .partial_fit(&x, None, &session)
            .expect("w213");
        LogMinkowski154Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm154");
        WindowLag214::new()
            .partial_fit(&x, None, &session)
            .expect("w214");
        LogMinkowski155Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm155");
        WindowLag215::new()
            .partial_fit(&x, None, &session)
            .expect("w215");
        LogMinkowski156Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm156");
        WindowLag216::new()
            .partial_fit(&x, None, &session)
            .expect("w216");
        LogMinkowski157Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm157");
        WindowLag217::new()
            .partial_fit(&x, None, &session)
            .expect("w217");
        LogMinkowski158Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm158");
        WindowLag218::new()
            .partial_fit(&x, None, &session)
            .expect("w218");
        LogMinkowski159Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm159");
        WindowLag219::new()
            .partial_fit(&x, None, &session)
            .expect("w219");
        LogMinkowski160Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm160");
        WindowLag220::new()
            .partial_fit(&x, None, &session)
            .expect("w220");
        LogMinkowski161Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm161");
        WindowLag221::new()
            .partial_fit(&x, None, &session)
            .expect("w221");
        LogMinkowski162Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm162");
        WindowLag222::new()
            .partial_fit(&x, None, &session)
            .expect("w222");
        LogMinkowski163Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm163");
        WindowLag223::new()
            .partial_fit(&x, None, &session)
            .expect("w223");
        LogMinkowski164Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm164");
        WindowLag224::new()
            .partial_fit(&x, None, &session)
            .expect("w224");
        LogMinkowski165Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm165");
        WindowLag225::new()
            .partial_fit(&x, None, &session)
            .expect("w225");
        LogMinkowski166Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm166");
        WindowLag226::new()
            .partial_fit(&x, None, &session)
            .expect("w226");
        LogMinkowski167Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm167");
        WindowLag227::new()
            .partial_fit(&x, None, &session)
            .expect("w227");
        LogMinkowski168Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm168");
        WindowLag228::new()
            .partial_fit(&x, None, &session)
            .expect("w228");
        LogMinkowski169Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm169");
        WindowLag229::new()
            .partial_fit(&x, None, &session)
            .expect("w229");
        LogMinkowski170Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm170");
        WindowLag230::new()
            .partial_fit(&x, None, &session)
            .expect("w230");
        LogMinkowski171Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm171");
        WindowLag231::new()
            .partial_fit(&x, None, &session)
            .expect("w231");
        LogMinkowski172Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm172");
        WindowLag232::new()
            .partial_fit(&x, None, &session)
            .expect("w232");
        LogMinkowski173Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm173");
        WindowLag233::new()
            .partial_fit(&x, None, &session)
            .expect("w233");
        LogMinkowski174Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm174");
        WindowLag234::new()
            .partial_fit(&x, None, &session)
            .expect("w234");
        LogMinkowski175Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm175");
        WindowLag235::new()
            .partial_fit(&x, None, &session)
            .expect("w235");
        LogMinkowski176Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm176");
        WindowLag236::new()
            .partial_fit(&x, None, &session)
            .expect("w236");
        LogMinkowski177Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm177");
        WindowLag237::new()
            .partial_fit(&x, None, &session)
            .expect("w237");
        LogMinkowski178Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm178");
        WindowLag238::new()
            .partial_fit(&x, None, &session)
            .expect("w238");
        LogMinkowski179Anomaly::new()
            .partial_fit(&x, None, &session)
            .expect("lm179");
        AdaMaxRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adamax");
        AMSGradRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("amsgrad");
        OnlineQuantile::new(0.5, 8)
            .partial_fit(&x, None, &session)
            .expect("oq");
        SlimReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("slim");
        AdaBoundRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adabound");
        NewtonRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("newton");
        NormReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("normreco");
        NadamRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("nadam");
        AdamWRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("adamw");
        HogwildRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("hogwild");
        ContentReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("content");
        TargetAgg::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("tagg");
        HoFm::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("hofm");
        BagOfWords::new(4)
            .partial_fit(&x, None, &session)
            .expect("bow");
        SgtClassifier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("sgtc");
        SgtRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("sgtr");
        IsoUPTree::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("isoup");
        OnlineSvm::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("osvm");
        MondrianTree::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("mondrian");
        HardSamplingRegressor::new(8)
            .partial_fit(&x, Some(&y), &session)
            .expect("hsr");
        AmfRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("amfr");
        PopularReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("popular");
        OnlineSmape::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("smape");
        PoissonInclusion::new(2.0)
            .partial_fit(&x, None, &session)
            .expect("poisinc");
        DummyDrift::new(0)
            .partial_fit(&x, None, &session)
            .expect("dummy");
        OnlineComplementNb::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("cnb");
        SrpRegressor::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("srpr");
        FfmRegressor::new(2, 2)
            .partial_fit(&x, Some(&y), &session)
            .expect("ffm");
        OnlineHuber::new(1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("ohuber");
        RandomNormalReco::new()
            .partial_fit(&xui, Some(&y), &session)
            .expect("rnorm");
        ComposePipeline::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("cpipe");
        OnlinePinball::new(0.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("pinball");
        AdwinBoosting::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("adwb");
        BoleClassifier::new(2)
            .partial_fit(&x, Some(&yb), &session)
            .expect("bole");
        SfmRegressor::new(2, 1)
            .partial_fit(&x, Some(&y), &session)
            .expect("sfm");
        TextClust::new(0.5)
            .partial_fit(&x, None, &session)
            .expect("textclust");
        Dbstream::new(2.0)
            .partial_fit(&x, None, &session)
            .expect("dbstream");
        MiniBatchMlp::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("mbmlp");
        FmReco::new(2)
            .partial_fit(&x, Some(&y), &session)
            .expect("fmreco");
        OnlineLogCosh::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("logcosh");
        EfdtRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("efdtr");
        OnlineStacking::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ostack");
        AdwinKnn::new(3)
            .partial_fit(&x, Some(&yb), &session)
            .expect("adwknn");
        CountMinSketch::new(3, 32)
            .partial_fit(&x, None, &session)
            .expect("cms");
        BloomFilter::new(256, 3)
            .partial_fit(&x, None, &session)
            .expect("bloom");
        HyperLogLog::new(6)
            .partial_fit(&x, None, &session)
            .expect("hll");
        OnlineVoting::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ovote");
        HeavyHitters::new(8)
            .partial_fit(&x, None, &session)
            .expect("hh");
        OnlineOneVsRest::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ovr");
        SketchSet::new()
            .partial_fit(&x, None, &session)
            .expect("sset");
        OnlineOutputCode::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ooc");
        OnlineMode::new()
            .partial_fit(&x, None, &session)
            .expect("omode");
        OnlinePeak::new()
            .partial_fit(&x, None, &session)
            .expect("opeak");
        OnlineSem::new()
            .partial_fit(&x, None, &session)
            .expect("osem");
        OnlineSpearman::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("ospear");
        HillSketch::new(6)
            .partial_fit(&x, None, &session)
            .expect("hill");
        OnlineAbsMax::new()
            .partial_fit(&x, None, &session)
            .expect("oabs");
        OnlineShift::new()
            .partial_fit(&x, None, &session)
            .expect("oshift");
        MultinomialProba::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("mproba");
        OnlineConfusion::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ocm");
        OnlineRange::new()
            .partial_fit(&x, None, &session)
            .expect("orange");
        OnlineMin::new()
            .partial_fit(&x, None, &session)
            .expect("omin");
        OnlineMcc::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("omcc");
        Swinn::new(3)
            .partial_fit(&x, Some(&yb), &session)
            .expect("swinn");
        Skyline::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("sky");
        Sdft::new(8)
            .partial_fit(&x, None, &session)
            .expect("sdft");
        AmsGradRegressor::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("ams");
        OnlineBalancedAccuracy::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("oba");
        OnlineGini::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("ogini");
        OnlineEwMean::new(0.4)
            .partial_fit(&x, None, &session)
            .expect("oew");
        NUnique::new()
            .partial_fit(&x, None, &session)
            .expect("nuniq");
        OnlineGeometricMean::new()
            .partial_fit(&x, None, &session)
            .expect("ogeom");
        OnlineMax::new()
            .partial_fit(&x, None, &session)
            .expect("omax");
        OnlineFbeta::new(0.5)
            .partial_fit(&x, Some(&yb), &session)
            .expect("ofb");
        OnlineMape::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("omape");
        TimeRolling::new(4)
            .partial_fit(&x, None, &session)
            .expect("troll");
        OnlineEwVar::new(0.4)
            .partial_fit(&x, None, &session)
            .expect("oewv");
        OnlineWeightedMean::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("owm");
        OnlineBayesianMean::new(0.0)
            .partial_fit(&x, None, &session)
            .expect("obayes");
        OnlineHarmonicMean::new()
            .partial_fit(&x, None, &session)
            .expect("oharm");
        OnlineQuadraticMean::new()
            .partial_fit(&x, None, &session)
            .expect("oqmean");
        RollingVar::new(4)
            .partial_fit(&x, None, &session)
            .expect("rvar");
        RollingCov::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rcov");
        OnlineMedian::new()
            .partial_fit(&x, None, &session)
            .expect("omed");
        RollingMedian::new(4)
            .partial_fit(&x, None, &session)
            .expect("rmed");
        OnlineStd::new()
            .partial_fit(&x, None, &session)
            .expect("ostd");
        RollingStd::new(4)
            .partial_fit(&x, None, &session)
            .expect("rstd");
        RollingSum::new(4)
            .partial_fit(&x, None, &session)
            .expect("rsum");
        RollingIqr::new(4)
            .partial_fit(&x, None, &session)
            .expect("riqr");
        OnlinePeakToPeak::new()
            .partial_fit(&x, None, &session)
            .expect("optp");
        RollingPeakToPeak::new(4)
            .partial_fit(&x, None, &session)
            .expect("rptp");
        RollingSem::new(4)
            .partial_fit(&x, None, &session)
            .expect("rsem");
        OnlineMsle::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("omsle");
        OnlineBrier::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("obrier");
        OnlineCrossEntropy::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("oce");
        RollingSkew::new(4)
            .partial_fit(&x, None, &session)
            .expect("rskew");
        RollingKurtosis::new(4)
            .partial_fit(&x, None, &session)
            .expect("rkurt");
        RollingEntropy::new(4)
            .partial_fit(&x, None, &session)
            .expect("rent");
        RollingCorr::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rcorr");
        RollingMae::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rmae");
        RollingMape::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rmape");
        RollingRmse::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rrmse");
        OnlineMatthews::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("omcc");
        OnlinePoissonDeviance::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("opois");
        OnlineGammaDeviance::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("ogam");
        MinHash::new(4)
            .partial_fit(&x, None, &session)
            .expect("minhash");
        BernoulliProba::new()
            .partial_fit(&x, Some(&yb), &session)
            .expect("bernoulli");
        OnlineTweedieDeviance::new()
            .partial_fit(&x, Some(&y), &session)
            .expect("otwd");
        RollingSpearman::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rspear");
        RollingMse::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rmse2");
        RollingLogLoss::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rll");
        RollingPinball::new(4, 0.5)
            .partial_fit(&x, Some(&y), &session)
            .expect("rpin");
        RollingHuber::new(4, 1.0)
            .partial_fit(&x, Some(&y), &session)
            .expect("rhub");
        RollingLogCosh::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rlch");
        RollingCrossEntropy::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rce");
        RollingAccuracy::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("racc");
        RollingF1::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rf1");
        RollingPrecision::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rprec");
        RollingRecall::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rrec");
        RollingR2::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rr2");
        RollingCohenKappa::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rkappa");
        RollingBalancedAccuracy::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rbal");
        ConstantThresholder::new(3.0)
            .partial_fit(&x, None, &session)
            .expect("cth");
        ThresholdFilter::new(0.5)
            .partial_fit(&x, None, &session)
            .expect("tfilt");
        RollingFbeta::new(4, 1.0)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rfb");
        RollingJaccard::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rjac");
        RollingHamming::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rham");
        RollingSmape::new(4)
            .partial_fit(&x, Some(&y), &session)
            .expect("rsmape");
        RollingMcc::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rmcc");
        RollingRocAuc::new(4)
            .partial_fit(&x, Some(&yb), &session)
            .expect("rroc");
        QuantileThresholder::new(0.8)
            .partial_fit(&x, None, &session)
            .expect("qthr");
        Discard::new(0.5)
            .partial_fit(&x, None, &session)
            .expect("disc");

        let n_expl = session
            .ledger()
            .events()
            .iter()
            .filter(|e| e.kind == EventKind::IncrementalExplanation)
            .count();
        assert!(
            n_expl >= 42,
            "expected an IncrementalExplanation per partial_fit, got {n_expl}"
        );
    }

    #[test]
    fn rls_predicts_after_fit() {
        let x = Matrix::from_fn(20, 1, |i, _| i as f64);
        let y = Vector::from_iter((0..20).map(|i| 2.0 * i as f64));
        let session = Session::new("rls", "partial_fit");
        let mut m = LinearRegression {
            fit_intercept: false,
            ..LinearRegression::default()
        };
        m.partial_fit(&x, Some(&y), &session).expect("fit");
        let pred = m.predict(&x, &session).expect("pred").value;
        assert!((pred[10] - 20.0).abs() < 1.0, "{}", pred[10]);
    }
}
